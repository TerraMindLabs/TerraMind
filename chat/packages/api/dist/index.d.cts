import { CredentialFingerprintRecord, CredentialName, CredentialRuntimeState, CredentialSource, bootstrapCredentials, credentialMetadataCollection, credentialMetadataId, credentialNames, getCredentialFingerprints, getCredentialRuntimeState, getLegacyCredentialNames, isLegacyCredential } from "./credentials.cjs";
import { $ as StreamableHTTPOptions, $i as VertexAIClientOptions, $n as ActivityLabelBatchMeta, $r as VideoResult, $t as OpenIDSessionIdentitySource, A as LCManifestTool, Ai as TCustomEndpointsConfig, An as normalizeResumeRunStepIndices, Ar as GoogleConfigOptions, At as UpstreamTokenTarget, B as MCPToolListResponse, Bi as BedrockUserCredentials, Bn as AssistantTextPhase, Br as DocumentResult, Bt as MCPOAuthTokens, C as FormattedContent, Ci as EndpointDbMethods, Cn as findUndecidedToolCalls, Cr as MistralOCRUploadResult, Ct as OboConfig, D as ImageFormatter, Di as InitializeResultBase, Dn as mapAskUserAnswer, Dr as OCRResultPage, Dt as OboTrustChecker, E as ImageContent, Ei as GetUserKeyValuesFunction, En as hydrateResumeRunSteps, Er as OCRResult, Et as OboTokenResolver, F as MCPRuntimeRequestBody, Fi as RequestBody, Fn as ACTIVITY_PHASE_INSTRUCTION, Fr as AudioFileInfo, Ft as resolveOboToken, G as ParsedServerConfig, Gi as AnthropicCredentials, Gn as createAssistantPhaseStampingHandlers, Gr as OpenAIFileBlock, Gt as OAuthStoredClientMetadata, H as OAuthHandledSource, Hi as InferenceProfileConfig, Hn as GeneratedActivityPhase, Hr as GoogleAudioBlock, Ht as OAuthClientSource, I as MCPServerSource, Ii as ServerRequest, In as ActivityPhaseEntry, Ir as AudioProcessingResult, It as selectMCPUpstreamTokenProvider, J as ResourceBody, Ji as AnthropicParameters, Jn as captureActivityBlockContext, Jr as OpenRouterVideoBlock, Jt as FlowState, K as Provider, Ki as AnthropicLLMConfigResult, Kn as ActivityLabelHostDeps, Kr as OpenAIInputFileBlock, Kt as FlowManagerOptions, L as MCPServers, Li as BedrockConfigOptions, Ln as ActivityPhaseHostDeps, Lr as AudioResult, Lt as ExtendedOAuthTokens, M as MCPOptions, Mi as resolveEndpointRuntime, Mn as resolveToolApprovalResume, Mr as GoogleParameters, Mt as createLazyOboUpstreamTokenProvider, N as MCPPrompt, Ni as EndpointTokenConfig, Nn as serializeAskUserAnswerVariants, Nr as AnthropicDocumentBlock, Nt as isOboConfigStillTrusted, O as LCAvailableTools, Oi as ProviderInitializeParams, On as mapAskUserAnswers, Or as OCRUsageInfo, Ot as UpstreamTokenProvider, P as MCPResource, Pi as TokenConfig, Pn as EarlyBufferOverflowState, Pr as AudioBlock, Pt as isRetryableOboExchangeError, Q as StdioOptions, Qi as ThinkingConfigParam, Qn as ACTIVITY_INSTRUCTION, Qr as VideoBlock, Qt as AuthIdentityTuple, R as MCPTool, Ri as BedrockCredentials, Rn as ActivityPhaseSnapshot, Rr as BedrockDocumentBlock, Rt as MCPOAuthFlowMetadata, S as FileSearchSource, Si as BaseInitializeParams, Sn as findIncompleteDecisions, Sr as MistralOCRRequest, St as GetUserRoleByAuthorId, T as FormattedToolResponse, Ti as GetUserKeyFunction, Tn as hasInvalidToolApprovalResolutions, Tr as OCRImage, Tt as OboTokenResolutionReason, U as OAuthStartHandler, Ui as BalanceUpdateFields, Un as TrackedActivity, Ur as GoogleDocumentBlock, Ut as OAuthMetadata, V as OAuthConnectionOptions, Vi as GuardrailConfiguration, Vn as GenerateActivityPhasePayload, Vr as FileObject, Vt as OAuthClientInformation, W as OAuthStartOptions, Wi as AnthropicConfigOptions, Wn as createActivityPhaseWiring, Wr as GoogleVideoBlock, Wt as OAuthProtectedResourceMetadata, X as ResourceLink, Xi as ThinkingConfigDisabled, Xn as stripActivityLabelParts, Xr as STTService, Xt as AuthIdentityContext, Y as ResourceContents, Yi as ThinkingConfigAdaptive, Yn as createActivityLabelWiring, Yr as ProcessedFile, Yt as FlowStatus, Z as SSEOptions, Zi as ThinkingConfigEnabled, Zn as synthesizeActivityLabelGapEvents, Zr as StrategyFunctions, Zt as AuthIdentitySource, _ as AudioContent, _i as ServerSentEvent, _n as attachAskUserQuestionArgs, _r as AccessiblePromptGroupsResult, _t as preProcessGraphTokens, a as ErrorHandler, aa as OpenAIModelOptions, ai as S3FileRef, an as createRefreshTokenBridgeIdentity, ar as GenerateLabelPayload, at as UserMCPConnectionOptions, b as DirectBearerRecoveryState, bi as MongoServerError, bn as findAskUserQuestionContentIndex, br as MistralFileUploadResponse, bt as resolveGraphTokensInRecord, c as GenerationJobStatus, ca as GenericClient, ci as SaveURLParams, cn as resolveAuthOpenIDIssuer, cr as createActivityLabelHook, ct as FlowStateManager, d as StreamSubscription, da as loadServiceKey, di as UploadImageParams, dn as resolveTenantId, dr as AgentEventDetachedTerminalEvidence, dt as normalizeExpiresAt, ea as LLMConfigResult, ei as BatchUpdateFn, en as RefreshTokenBridgeIdentity, er as ActivityLabelBlockContext, et as TextContent, f as SubscribeOptions, fi as UploadResult, fn as serializeAuthIdentityTuple, fr as AgentEventSuspensionProjection, ft as GraphTokenOptions, g as Artifacts, gi as FinalMessageFields, gn as attachAskUserQuestionAnswers, gr as RunLLMConfig, gt as mcpOptionsContainGraphTokenPlaceholder, h as AddServerResult, hi as FinalEvent, hn as attachAskUserQuestionAnswer, hr as JsonValue, ht as containsGraphTokenPlaceholder, i as DoneHandler, ia as OpenAIConfiguration, ii as ProcessAvatarParams, in as createOpenIDSessionIdentity, ir as ActivityLabelSlot, it as UserConnectionContext, j as LCToolManifest, ji as UserKeyValues, jn as resolveAskUserQuestionResume, jr as GoogleCredentials, jt as awaitOboOperation, k as LCFunctionTool, ki as RuntimeInitializeParams, kn as mapToolApprovalResolutions, kr as PageDimensions, kt as UpstreamTokenProviderResolver, l as ResumeState, la as GoogleServiceKey, li as SaveURLResult, ln as resolveOpenIDSubject, lr as stringifyActivityEvidence, lt as FlowStateNotFoundError, m as UnsubscribeFn, mi as CreatedEvent, mn as appendResolvedAskUserQuestion, mr as JsonPrimitive, mt as GraphTokenResponse, n as ChunkHandler, na as OpenAIConfigOptions, ni as GetURLParams, nn as createOpenIDOboIdentityTuple, nr as ActivityLabelInvokeCallbacks, nt as ToolDiscoveryOptions, o as GenerationJob, oa as OpenAIParameters, oi as SaveBufferFn, on as isOpenIDSessionIdentityMatch, or as buildPrompt, ot as WebSocketOptions, p as SubscribeWithResumeResult, pi as UrlBuilder, pn as ResolvedAskUserQuestion, pr as AgentTriggerExpectedAction, pt as GraphTokenResolver, q as RequestScopedMCPConnectionStore, qi as AnthropicModelOptions, qn as LooseContentPart, qr as OpenRouterAudioBlock, qt as FlowMetadata, r as ContentPart, ra as OpenAIConfigResult, ri as ImageUploadResult, rn as createOpenIDRefreshIdentityTuple, rr as ActivityLabelLLM, rt as ToolDiscoveryResult, s as GenerationJobMetadata, sa as AzureOptions, si as SaveBufferParams, sn as resolveAppUserId, sr as classifyBatch, st as FlowLease, t as ResolvedPrincipal, ta as OAIClientOptions, ti as DownloadURLParams, tn as createAuthIdentityContext, tr as ActivityLabelHookOptions, tt as ToolContentPart, u as ResumeSubscription, ua as checkUserKeyExpiry, ui as UploadFileParams, un as resolveRefreshSubject, ur as AgentEventAppliedAction, ut as PENDING_STALE_MS, v as BasicConnectionOptions, vi as StreamEvent, vn as buildResolvedAskUserQuestion, vr as PromptGroupsAllResponse, vt as recordContainsGraphTokenPlaceholder, w as FormattedContentResult, wi as EndpointRuntimeContext, wn as getBoundedAskUserAnswerValues, wr as MistralSignedUrlResponse, wt as OboTokenResolutionError, x as EmbeddedResource, xi as ValidationError, xn as findDisallowedDecisions, xr as MistralOCRError, xt as GetRolePermissions, y as ConnectionState, yi as CustomError, yn as createContentIndexOffsetHandlers, yr as PromptGroupsListResponse, yt as resolveGraphTokenPlaceholder, z as MCPToolCallResponse, zi as BedrockLLMConfigResult, zn as ActivityPhaseWiring, zr as DocumentBlock, zt as MCPOAuthState } from "./index-B5TXk7G9.cjs";
import { AccessRoleMethods, AddToolFavoriteResult, AdminAuditLogEntry, AgentApiKeyCreateResult, AgentApiKeyListItem, AgentEventActorDetachedAction, AgentGraphAccessContext, AgentGraphNode, AgentMethods, AgentQueuedTurnMethods, AgentQueuedTurnRecord, AgentTriggerDeliveryFailure as AgentTriggerDeliveryFailure$1, AgentTriggerDeliveryMethods, AgentTriggerDeliveryStatus as AgentTriggerDeliveryStatus$1, AgentTriggerDeliveryStatusRecord, AgentTriggerProducerLeaseStatus, AllMethods, AppConfig, AssistantQuery, AuditChainVerification, AuditContext, AuditLogFilters, AuditLogPage, BackgroundToolResultClaim, BalanceConfig, BalanceReservationRelease, BalanceReservationRenewal, BalanceReservationRequest, BalanceReservationResult, ChatProjectMethods, CodeFileCommitData, ConfigSection, ConversationMethods, ConversationTraceRefs, ConvertJsonSchemaToZodOptions, CreateSkillInput, CreateSkillResult, DeleteSessionParams, FavoriteItemType, FileOwnerScope, FormattedMemoriesResult, GroupFilterOptions, IAclEntry, IActiveSubagentThreadLease, IAgent, IAgentEventActorContextMeta, IAgentEventActorSkillIdentity, IAgentEventActorState, IAgentEventActorSummary, IAgentEventActorSuspensionEvidence, IAgentFadingTier, IAgentFadingTierEntry, IAssistant, IBalance, IBalanceUpdate, IChatProject, ICompactionSemanticIndexProjection, IConfig, IConversation, IGroup, IMongoFile, IPromptGroupDocument, IRole, ISchedule, IScheduleRun, ISession, ISkill, ISkillFile, ISkillSyncStatus, ISystemGrant, IToken, IToolFavoriteLean, IUser, IndexBuildOptions, InsightsMethods, JsonSchemaType as JsonSchemaType$1, ListSkillsByAccessParams, ListSkillsByAccessResult, MCPAuthorityBootRevision, MCPAuthorityImmutableConfig, MCPAuthorityMethods, MCPAuthorityProofV1, MCPAuthorityTargetInput, MemoryMethods, MessageMethods, OIDCTokens as OIDCTokens$1, ObjectId, PluginAuthMethods, PublishRunArtifactInput, RecordAuditEntryInput, RecordAuditEntryOptions, RemoveToolFavoriteResult, ResourceCapabilityMap, RoleMethods, RunArtifactClaim, RunArtifactFile, RunArtifactScope, ScheduleMethods, SkillSyncCredentialSummary, SkillSyncProvider, SkillSyncStatusInput, SystemCapabilities, SystemCapability, TWebSearchCategories, TWebSearchKeys, TokenMethods, TransactionData, TxMethods, UpdateSkillInput, UpdateSkillResult, UpsertSkillFileInput, UpsertSkillSyncCredentialInput, UserDeleteResult, UserMethods, ValidationIssue, decrypt as decrypt$1, decryptV2, decryptV3, encrypt as encrypt$1, encryptV2, encryptV3, getRandomValues, hashBackupCode, isAgentFadingTier, isAgentFadingTierEntries, isMemoryAgentEnabled, isMemoryEnabled, loadMemoryConfig, logger as logger$1 } from "@librechat/data-schemas";
import { AnyBulkWriteOperation, ClientSession, DeleteResult, FilterQuery, Model, Mongoose, PipelineStage, ProjectionType, QueryOptions, Types, mongo } from "mongoose";
import { AccessRoleIds, Action, ActionMetadata, ActionMetadataFilterField, Agent, AgentAvatar, AgentCapabilities, AgentGitIdentity, AgentInstructionFilterField, AgentModelParameters, AgentOwnerContact, AgentSubagentGraph, AgentSubagentsConfig, AgentToolOptions, AgentToolResources, Agents, AnthropicEffort, ArtifactModes, AuthType, AuthTypeEnum, CODE_ENVIRONMENT_DECISION_VERSION, CODE_ENVIRONMENT_MOVE_VERSION, CloudFrontConfig, CodeApprovalMode, CodeEnvKind, CodeEnvRef, CodeEnvRefMap, CodeEnvReferenceSet, CodeEnvironmentMode, CodeEnvironmentUserConfigSchema, CodeEnvironmentUserSettings, CodeWorkspaceDescriptor, CodeWorkspaceOperation, CodeWorkspaceSelection, CodeWorkspaceSelectionErrorReason, CodeWorkspaceSelectionErrorReason as CodeWorkspaceSelectionErrorReason$1, ContentTypes, ConversationStarterFilterField, ConversationTitleFilterField, DeleteFilesResponse, DynamicSettingProps, EModelEndpoint, EToolResources, EndpointFileConfig, EndpointsDropParamsMap, ErrorTypes, FeedbackFilterField, FileConfig, FileContext, FileFilterField, FileSources, FiltersConfig, GraphEdge, ImageDetail, LangfuseTraceConversationMetadataField, MCPOptions as MCPOptions$1, MCPReinitializeFailureReason, MemoryFilterField, MemoryScope, MessageFilterConfig, MessageFilterField, MessageFilterPiiConfig, ModelParameterFilterField, PermissionBits, PermissionTypes, Permissions, PrincipalModel, PrincipalType, PromptFilterField, PtcToolCallEvent, ReasoningLabelEvents, ReasoningParameterFormat, ReasoningResponseKey, ResourceType, RunFileProvenance, ScheduleMCPOutcome, ScheduleMCPStatus, SettingDefinition, SkillFilterField, SkillSyncConfig, SkillSyncGitHubSourceConfig, SkillsScope, StatefulCodeEnvironment, SubagentControlRequest, SubagentIdentity, SubagentUpdateEvent, SummarizationConfig, SummaryContentPart, TAgentsEndpoint, TAskUserQuestionConfig, TAttachment, TCheckpointerConfig, TCheckpointerType, TConfig, TContextUsageEvent, TConversation, TCreateSchedule, TCustomConfig, TCustomEndpoints, TDefaultLLMDeliveryPath, TEndpoint, TEndpointOption, TEndpointsConfig, TEphemeralAgent, TFile, TInsightsAgent, TInterfaceConfig, TMessage, TMessageContentParts, TModelSpec, TModelSpecPreset, TModelsConfig, TOpenIdDiscoveryConfig, TPendingSteer, TPeoplePickerPermissions, TPlugin, TPreset, TPrincipal, TPrincipalSearchResult, TResolvedTraceViewerConfig, TResponseUsage, TScheduleCadence, TSharedLinkStartupConfig, TSkill, TSpecsConfig, TTokenConfigMap, TTokenUsageEvent, TToolApprovalHookConfig, TToolApprovalPolicy, TTraceAvailability, TTraceErrorCode, TTracePage, TTraceRecordDetail, TTransactionsConfig, TUpdateSkillPayload, TUser, TWebSearchConfig, ThinkingDisplay, TokenExchangeMethodEnum, ToolArgumentFilterField, TurnDeliveryFile, TurnDeliveryRouting, TurnFileConsumers, UserSubmittedMessageFieldPath, agentGitIdentitySchema, buildServerNameAliases, cadenceIntervalMinutes, cadenceToCron, getEndpointFileConfig, isValidCronExpression, librechat, mergeFileConfig, normalizeActionToolName, normalizeMCPToolKey, normalizeServerName, splitMCPToolKey, stripServerNamePrefix, stripServerNamePrefixes, supportsAdaptiveThinking } from "librechat-data-provider";
import { NextFunction, Request, RequestHandler, Response as Response$2, Router } from "express";
import http, { Server } from "http";
import http$1, { Agent as Agent$1, Server as Server$1 } from "node:http";
import z$1, { z } from "zod";
import { Dispatcher } from "undici";
import { AllowedCaller, AnthropicClientOptions, AskUserQuestionResolution, CallerCapabilityProjectionSnapshot, CodeEnvFile, CompactionSemanticIndex, CompactionSemanticIndexSnapshot, EventActorCancelSuspensionResult, EventActorEvent, EventActorExecutionResult, EventActorInterrupt, EventActorSuspension, EventHandler as EventHandler$1, FileRefs, GenericTool, GoogleClientOptions, GraphEdge as GraphEdge$1, HandleLLMEnd, HookCallback, HookEvent, HookInput, HookInputByEvent, HookOutput, HookOutputByEvent, HookRegistry, IState, InMemorySubagentTaskStore, InMemorySubagentTaskStoreOptions, InjectedMessage, JsonSchemaType, JsonSchemaType as JsonSchemaType$2, LCTool, LCTool as LCTool$1, LCToolRegistry, LCToolRegistry as LCToolRegistry$1, LLMConfig, MessageContentComplex, Providers, Run, RunConfig, RunStep, StandardGraph, StreamEventData, StreamPreemption, SubagentExecutionContext, SubagentResolveContext, SubagentTaskClaim, SubagentTaskConfig, SubagentTaskControlCommand, SubagentTaskControlResult, SubagentTaskSnapshot, SubagentTaskStartRequest, SubagentTaskStartResult, SubagentUpdateEvent as SubagentUpdateEvent$1, SubagentUsageEvent as SubagentUsageEvent$1, TOOL_APPROVAL_EXECUTION_SCOPE_CONFIG_KEY, TokenCounter as TokenCounter$1, ToolApprovalDecisionMap, ToolEndCallback as ToolEndCallback$1, ToolMap, ToolPolicyConfig, ToolSessionMap, VertexAIClientOptions as VertexAIClientOptions$1, createBashProgrammaticToolCallingTool } from "@librechat/agents";
import { Readable, Transform } from "stream";
import { EventEmitter } from "events";
import { Tool as Tool$1 } from "@modelcontextprotocol/sdk/types.js";
import { OAuthClientInformation as OAuthClientInformation$1, OAuthTokens } from "@modelcontextprotocol/sdk/shared/auth.js";
import { Logger } from "winston";
import { Keyv } from "keyv";
import { AsyncLocalStorage } from "node:async_hooks";
import { BlobServiceClient, ContainerClient } from "@azure/storage-blob";
import { FirebaseStorage } from "firebase/storage";
import { FirebaseApp } from "firebase/app";
import { S3Client } from "@aws-sdk/client-s3";
import { DynamicStructuredTool, StructuredToolInterface } from "@librechat/agents/langchain/tools";
import { HttpsProxyAgent } from "https-proxy-agent";
import { AxiosError, AxiosInstance, AxiosRequestConfig } from "axios";
import https, { Agent as Agent$2 } from "node:https";
import { LookupFunction } from "node:net";
import { RequestOptions } from "@modelcontextprotocol/sdk/shared/protocol.js";
import { Client } from "@modelcontextprotocol/sdk/client/index.js";
import https$1 from "https";
import fetch$1 from "node-fetch";
import { RateLimitInfo, Store } from "express-rate-limit";
import { BaseMessage, HumanMessage } from "@librechat/agents/langchain/messages";
import { JwtPayload } from "jsonwebtoken";
import { MongoDBSaver } from "@langchain/langgraph-checkpoint-mongodb";
import { Checkpoint, CheckpointListOptions, CheckpointMetadata, CheckpointTuple, PendingWrite } from "@langchain/langgraph-checkpoint";
import { RunnableConfig } from "@langchain/core/runnables";
import FormData from "form-data";
import { Readable as Readable$1 } from "node:stream";
import { BaseMessage as BaseMessage$1 } from "@librechat/agents/langchain";
import { Cluster, ClusterOptions, Redis, RedisOptions } from "ioredis";
import { Document, ReadPreference } from "mongodb";
import { AnthropicVertex } from "@anthropic-ai/vertex-sdk";
import { ClientOptions as ClientOptions$1 } from "@anthropic-ai/sdk";
import { GoogleAIToolType } from "@librechat/agents/langchain/google-common";
import { Metadata as Metadata$1 } from "sharp";
import { RedisClientType, RedisClusterType } from "@redis/client";
import { KeyvFile } from "keyv-file";
import { RedisStore } from "rate-limit-redis";
import { MemoryStore } from "express-session";
import { RedisStore as RedisStore$1 } from "connect-redis";
import { Span } from "@opentelemetry/api";

//#region src/app/service.d.ts
type AppConfigPrincipal = {
  principalType: string;
  principalId?: string | Types.ObjectId;
};
declare const DEFAULT_OVERRIDE_CACHE_TTL = 6e4;
interface CacheStore {
  get: (key: string) => Promise<unknown>;
  set: (key: string, value: unknown, ttl?: number) => Promise<unknown>;
  delete: (key: string) => Promise<boolean>;
  /** Keyv options — used for key enumeration when clearing override caches. */
  opts?: {
    store?: {
      keys?: () => IterableIterator<string>;
    };
  };
}
interface AppConfigServiceDeps {
  /** Load the base AppConfig from YAML + AppService processing. */
  loadBaseConfig: () => Promise<AppConfig | undefined>;
  /** Cache tools after base config is loaded. */
  setCachedTools: (tools: Record<string, unknown>) => Promise<void>;
  /** Get a cache store by key. */
  getCache: (key: string) => CacheStore;
  /** The CacheKeys constants from librechat-data-provider. */
  cacheKeys: {
    APP_CONFIG: string;
  };
  /** Fetch applicable DB config overrides for a set of principals. */
  getApplicableConfigs: (principals?: AppConfigPrincipal[]) => Promise<IConfig[]>;
  /** Resolve full principal list (user + role + groups) from userId/role. */
  getUserPrincipals: (params: {
    userId: string | Types.ObjectId;
    role?: string | null;
    idOnTheSource?: string | null;
  }) => Promise<AppConfigPrincipal[]>;
  /** Add mutable principal-scoped runtime configuration after cached overrides are resolved. */
  augmentConfig?: (context: {
    appConfig: AppConfig;
    baseConfig: AppConfig;
    principals: AppConfigPrincipal[];
    options: GetAppConfigOptions;
  }) => Promise<AppConfig>;
  /** TTL in ms for per-user/role merged config caches. Defaults to 60 000. */
  overrideCacheTtl?: number;
}
interface GetAppConfigOptions {
  role?: string;
  userId?: string;
  idOnTheSource?: string | null;
  tenantId?: string;
  refresh?: boolean;
  /** When true, return only the YAML-derived base config — no DB override queries. */
  baseOnly?: boolean;
  /** Propagate principal, override, and augmentation failures for security-sensitive callers. */
  failClosed?: boolean;
  /** Reuse principals already resolved by another authorization query in the same request. */
  resolvedPrincipals?: AppConfigPrincipal[];
  /** Skip mutable runtime augmentation when the caller has already loaded that data. */
  skipRuntimeAugmentation?: boolean;
}
interface AppConfigUserLike {
  /** Resolved app user id. */
  id?: string;
  role?: string;
  tenantId?: string;
  idOnTheSource?: string | null;
}
declare function getAppConfigOptionsFromUser(user?: AppConfigUserLike | null, tenantId?: string): GetAppConfigOptions;
/** @internal Resets the memoized strict-override flag and one-time no-tenantId warning gate. Exposed for test teardown only. */
declare function _resetOverrideStrictCache(): void;
declare function createAppConfigService(deps: AppConfigServiceDeps): {
  getAppConfig: (options?: GetAppConfigOptions) => Promise<AppConfig>;
  clearAppConfigCache: () => Promise<void>;
  clearOverrideCache: (tenantId?: string) => Promise<void>;
};
type AppConfigService = ReturnType<typeof createAppConfigService>;
//#endregion
//#region src/app/config.d.ts
/**
* Retrieves the balance configuration object
* */
declare function getBalanceConfig(appConfig?: AppConfig): Partial<TCustomConfig["balance"]> | null;
/**
* Retrieves the transactions configuration object
* */
declare function getTransactionsConfig(appConfig?: AppConfig): Partial<TTransactionsConfig>;
declare const getCustomEndpointConfig: ({
  endpoint,
  appConfig
}: {
  endpoint: string | EModelEndpoint;
  appConfig?: AppConfig;
}) => Partial<TEndpoint> | undefined;
/**
* Builds a map of normalized endpoint name -> dropParams for the endpoint shapes that
* support per-endpoint dropParams: array-configured `custom` endpoints map directly to
* their dropParams, while `azureOpenAI` maps to a per-model lookup (model name ->
* dropParams) built from `modelGroupMap`/`groupMap`, mirroring the per-request group
* resolution in `initializeOpenAI` so a parameter dropped for one group doesn't hide it
* for models in another group.
*/
declare function getEndpointsDropParamsMap(endpoints: AppConfig["endpoints"]): EndpointsDropParamsMap;
//#endregion
//#region src/agents/phases.d.ts
declare const agentStartupMilestones: readonly ["request_admitted", "job_created", "ack_sent", "conversation_resolved", "metadata_persisted", "client_initialized", "history_loaded", "messages_built", "run_input_prepared", "run_created", "stream_processing_started", "request_message_queued", "first_response_event_queued", "first_content_delta_queued"];
declare const agentStartupResults: readonly ["content_queued", "completed_without_delta", "deduplicated", "rejected", "paused", "replaced", "aborted", "error"];
type AgentStartupMilestone = (typeof agentStartupMilestones)[number];
type AgentStartupResult = (typeof agentStartupResults)[number];
//#endregion
//#region src/protection/types.d.ts
type ContentSource = "message" | "prompt" | "agent_instruction" | "conversation_starter" | "skill" | "memory" | "file" | "tool_argument" | "model_parameter" | "action_metadata" | "feedback" | "conversation_title" | "assembled_context";
type ContentProvenance = "user" | "administrator" | "model" | "tool" | "retrieval" | "system" | "external_agent";
type ContentFormat = "plain" | "markdown" | "json" | "uri";
type ContentTreatment = "replaceable" | "inspect_only";
type JsonPointer = `/${string}`;
interface ContentFieldMap {
  message: MessageFilterField;
  prompt: PromptFilterField;
  agent_instruction: AgentInstructionFilterField;
  conversation_starter: ConversationStarterFilterField;
  skill: SkillFilterField;
  memory: MemoryFilterField;
  file: FileFilterField;
  tool_argument: ToolArgumentFilterField;
  model_parameter: ModelParameterFilterField;
  action_metadata: ActionMetadataFilterField;
  feedback: FeedbackFilterField;
  conversation_title: ConversationTitleFilterField;
  assembled_context: MessageFilterField;
}
interface TextContentFragmentBase {
  readonly id: string;
  readonly provenance: ContentProvenance;
  readonly path: JsonPointer;
  readonly format: ContentFormat;
  readonly treatment: ContentTreatment;
  readonly text: string;
}
type TextContentFragment = { [Source in ContentSource]: TextContentFragmentBase & {
  readonly source: Source;
  readonly field: ContentFieldMap[Source];
} }[ContentSource];
interface ProtectionFinding {
  readonly detectorId: string;
  readonly ruleId: string;
  readonly label: string;
  readonly source: ContentSource;
  readonly field: ContentFieldMap[ContentSource];
  readonly provenance: ContentProvenance;
  readonly fragmentId: string;
  readonly fragmentPath: JsonPointer;
}
//#endregion
//#region src/protection/adapters/nested.d.ts
declare const CONTENT_TRAVERSAL_MAX_DEPTH = 24;
declare const CONTENT_TRAVERSAL_MAX_NODES = 4096;
declare const CONTENT_MATERIALIZATION_MAX_CHARACTERS: number;
interface NestedStringContext {
  readonly key: string | undefined;
  readonly parent: object | undefined;
  readonly path: JsonPointer;
}
interface VisitNestedStringsBudget {
  visitedNodes: number;
  /** Optional aggregate ceiling for callers whose valid envelope spans several traversals. */
  readonly maxNodes?: number;
  /** Aggregate characters copied into joined/assembled inspection strings. */
  materializedCharacters?: number;
  readonly maxMaterializedCharacters?: number;
}
declare function reserveContentMaterialization(budget: VisitNestedStringsBudget, requestedCharacters: number): boolean;
interface VisitNestedStringsOptions {
  readonly includeKeys?: boolean;
  readonly maxDepth?: number;
  readonly maxNodes?: number;
  readonly budget?: VisitNestedStringsBudget;
  readonly shouldVisit?: (context: NestedStringContext & {
    readonly value: unknown;
  }) => boolean;
  readonly shouldInclude?: (value: string, context: NestedStringContext) => boolean;
}
declare function getBoundedOwnEnumerableEntries(value: object, limit: number): {
  readonly entries: [string, unknown][];
  readonly complete: boolean;
  readonly reason?: "object_entries" | "reflection_error";
};
interface UninspectableNestedContentResponse {
  readonly error: "content_filter_uninspectable";
  readonly message: string;
  readonly source: ContentSource;
  readonly field: ContentFieldMap[ContentSource];
}
type ContentTraversalScope = { [Source in ContentSource]: {
  readonly source: Source;
  readonly fields: readonly ContentFieldMap[Source][];
} }[ContentSource];
type ContentTraversalLimitReason = "max_depth" | "max_nodes" | "array_length" | "object_entries" | "reflection_error";
interface ContentTraversalDiagnostics {
  readonly operation: "omit_resolved_file_locators";
  readonly reason: ContentTraversalLimitReason;
  readonly visitedNodes: number;
  readonly depth: number;
}
declare class ContentTraversalLimitError extends Error {
  readonly code = "content_filter_uninspectable";
  readonly statusCode = 400;
  readonly body: UninspectableNestedContentResponse;
  readonly diagnostics?: ContentTraversalDiagnostics;
  constructor(fragments?: readonly TextContentFragment[], scopes?: readonly ContentTraversalScope[], diagnostics?: ContentTraversalDiagnostics);
}
declare function isContentTraversalLimitError(error: unknown): error is ContentTraversalLimitError;
declare function getContentTraversalFragments(error: ContentTraversalLimitError): readonly TextContentFragment[];
declare function prependContentTraversalFragments(error: ContentTraversalLimitError, fragments: readonly TextContentFragment[]): void;
declare function getContentTraversalScopes(error: ContentTraversalLimitError): readonly ContentTraversalScope[];
declare function isNestedMessageTraversalProtected(params: {
  readonly filters?: FiltersConfig;
  readonly legacyPii?: MessageFilterPiiConfig;
  readonly roles?: readonly (string | undefined)[];
}): boolean;
declare function isModelParameterTraversalProtected(params: {
  readonly error: ContentTraversalLimitError;
  readonly filters?: FiltersConfig;
}): boolean;
declare function isContentTraversalProtected(params: {
  readonly error: ContentTraversalLimitError;
  readonly filters?: FiltersConfig;
  readonly legacyPii?: MessageFilterPiiConfig;
  readonly roles?: readonly (string | undefined)[];
}): boolean;
declare function escapeJsonPointer(value: string): string;
declare function isDataUri(value: string): boolean;
declare function isLikelyEncodedPayload(value: string, key: string | undefined, parent: object | undefined): boolean;
declare function shouldIncludeNestedSubmittedText(value: string, context: NestedStringContext): boolean;
/**
* Visits textual leaves with hard object/depth budgets and cycle protection.
* Callers decide which already-classified or opaque subtrees should be skipped.
*/
declare function visitNestedStrings(value: unknown, path: JsonPointer, onString: (value: string, path: JsonPointer) => void, options?: VisitNestedStringsOptions): boolean;
//#endregion
//#region src/protection/diagnostics.d.ts
interface LocatorTraversalFailure extends ContentTraversalDiagnostics {
  readonly messageCount: number;
  readonly resolvedFileCount: number;
}
type LocatorTraversalReporter = (failure: LocatorTraversalFailure) => void;
//#endregion
//#region src/app/metrics.d.ts
declare const normalizePath: (rawPath: string) => string;
interface PrometheusMetrics {
  metricsMiddleware: (req: Request, res: Response$2, next: NextFunction) => void;
  metricsRouter: Router;
}
interface AgentEventActorStorageMetricsSnapshot {
  retainedByResolution: Record<"checkpoint_verified" | "action_compensated" | "history_repaired", number>;
  expiryEligible: number;
  retryDeliveries: number;
  deadDeliveries: number;
  pendingReconciliations: number;
  oldestPendingAgeSeconds: number;
}
interface MetricsOptions {
  collectAgentEventActorStorageMetrics?: () => Promise<AgentEventActorStorageMetricsSnapshot>;
}
type OpenIDUserLookupResult = "found" | "not_found" | "migration" | "auth_failed" | "error";
type GenerationJobStore = "memory" | "redis";
type GenerationJobResult = "created" | "completed" | "error" | "aborted" | "abort_failed";
type GenerationStreamSubscriptionType = "initial" | "resume" | "resume_state";
type GenerationStreamSubscriptionResult = "success" | "not_found" | "error" | "found" | "missing";
type GenerationStreamRecoveryMethod = "redis" | "snapshot";
type GenerationStreamRecoveryOutcome = "success" | "failed" | "not_required";
type GenerationStreamAttachmentOutcome = "attached" | "bootstrap_slow" | "disconnected" | "never_attached";
type RumProxyEndpoint = "traces" | "logs" | "unknown";
type RumProxyResult = "success" | "auth_drop" | "auth_error" | "bad_request" | "not_configured" | "collector_4xx" | "collector_5xx" | "collector_error" | "collector_timeout";
type ShareLinkOperation = "create" | "update";
type ShareLinkRejectionCode = "TARGET_MESSAGE_NOT_FOUND" | "NO_MESSAGES";
type RedisClient = "ioredis" | "keyv";
type RedisOperationStatus = "success" | "error";
declare function recordOpenIDUserLookup(result: OpenIDUserLookupResult, durationSeconds: number): void;
/** Application sink supplied explicitly to content inspection callers. */
declare function reportLocatorTraversalFailure(failure: LocatorTraversalFailure): void;
declare function recordGenerationJob(store: GenerationJobStore, result: GenerationJobResult): void;
declare function setGenerationJobsInFlight(store: GenerationJobStore, count: number): void;
declare function recordGenerationStreamSubscription(store: GenerationJobStore, type: GenerationStreamSubscriptionType, result: GenerationStreamSubscriptionResult): void;
declare function recordGenerationStreamResumePendingEvents(store: GenerationJobStore, count: number): void;
declare function recordGenerationStreamEarlyBufferOverflow(store: GenerationJobStore): void;
declare function recordGenerationStreamRecovery(store: GenerationJobStore, method: GenerationStreamRecoveryMethod, outcome: GenerationStreamRecoveryOutcome, durationSeconds: number, reconstructedEvents: number, reconstructedContent: number): void;
declare function recordGenerationStreamAttachment(store: GenerationJobStore, outcome: GenerationStreamAttachmentOutcome, delaySeconds?: number): void;
declare function recordAgentStartupMilestone(milestone: AgentStartupMilestone, durationSeconds: number): void;
declare function recordAgentStartupResult(result: AgentStartupResult): void;
declare function recordRumProxyRequest(endpoint: RumProxyEndpoint, result: RumProxyResult): void;
declare function recordShareLinkRejection(operation: ShareLinkOperation, code: ShareLinkRejectionCode): void;
declare function recordRedisOperation(client: RedisClient, useCase: string, operation: string, status: RedisOperationStatus, durationSeconds: number): void;
declare const isMetricsConfigured: () => boolean;
declare function recordMongooseQuery(model: string, operation: string, status: string, durationSeconds: number): void;
declare function instrumentMongooseQueryMetrics(mongoose: Mongoose): void;
declare function createMetrics(options?: MetricsOptions): PrometheusMetrics;
//#endregion
//#region src/app/permissions.d.ts
declare function updateInterfacePermissions({
  appConfig,
  getRoleByName,
  updateAccessPermissions,
  tenantId
}: {
  appConfig: AppConfig;
  getRoleByName: (roleName: string, fieldsToSelect?: string | string[]) => Promise<IRole | null>;
  updateAccessPermissions: (roleName: string, permissionsUpdate: Partial<Record<PermissionTypes, Record<string, boolean | undefined>>>, roleData?: IRole | null) => Promise<void>;
  /**
  * Optional tenant ID for scoping role updates to a specific tenant.
  * When provided (and not SYSTEM_TENANT_ID), runs inside `tenantStorage.run({ tenantId })`.
  * When omitted or SYSTEM_TENANT_ID, uses the caller's existing ALS context.
  */
  tenantId?: string;
}): Promise<void>;
//#endregion
//#region src/app/cdn.d.ts
/**
* Initializes file storage clients based on the configured file strategies.
* Handles both the main fileStrategy and granular fileStrategies config.
*/
declare function initializeFileStorage(appConfig: AppConfig): void;
//#endregion
//#region src/app/checks.d.ts
declare const deprecatedAzureVariables: {
  key: string;
  description: string;
}[];
declare const conflictingAzureVariables: {
  key: string;
}[];
/**
* Checks environment variables for default secrets and deprecated variables.
* Logs warnings for any default secret values being used and for usage of deprecated variables.
* Advises on replacing default secrets and updating deprecated variables.
* @param {Object} options
* @param {Function} options.isEnabled - Function to check if a feature is enabled
* @param {Function} options.checkEmailConfig - Function to check email configuration
*/
declare function checkVariables(): void;
/**
* Compares active credential fingerprints with the database marker. The marker contains hashes
* only, allowing a new instance to establish its identity without storing secret values.
*/
declare function checkCredentialDatabase(): Promise<void>;
/**
* Checks the health of auxiliary API's by attempting a fetch request to their respective `/health` endpoints.
* Logs information or warning based on the API's availability and response.
*/
declare function checkHealth(): Promise<void>;
declare function checkInterfaceConfig(appConfig: AppConfig): void;
/**
* Performs startup checks including environment variable validation and health checks.
* This should be called during application startup before initializing services.
* @param [appConfig] - The application configuration object.
*/
declare function performStartupChecks(appConfig?: AppConfig): Promise<void>;
/**
* Performs basic checks on the loaded config object.
* @param config - The loaded custom configuration.
*/
declare function checkConfig(config: Partial<TCustomConfig>): void;
/**
* Checks web search configuration values to ensure they are environment variable references.
* Warns if actual API keys or URLs are used instead of environment variable references.
* Logs debug information for properly configured environment variable references.
* @param webSearchConfig - The loaded web search configuration object.
*/
declare function checkWebSearchConfig(webSearchConfig?: Partial<TCustomConfig["webSearch"]> | null): void;
//#endregion
//#region src/app/resolve.d.ts
interface UserForConfigResolution {
  tenantId?: string;
  role?: string;
}
type GetAppConfig = (opts: {
  role?: string;
  tenantId?: string;
  baseOnly?: boolean;
}) => Promise<AppConfig>;
/**
* Resolves AppConfig scoped to the given user's tenant when available,
* falling back to YAML-only base config for new users or non-tenant deployments.
*
* Auth flows only apply role-level overrides (userId is not passed) because
* user/group principal resolution requires heavier DB work that is deferred
* to post-authentication config calls.
*
* `tenantId` is propagated through two channels that serve different purposes:
* - `tenantStorage.run()` sets the ALS context so Mongoose's `applyTenantIsolation`
*   plugin scopes any DB queries (e.g., `getApplicableConfigs`) to the tenant.
* - The explicit `tenantId` parameter to `getAppConfig` is used for cache-key
*   computation in `overrideCacheKey()`. Both channels are required.
*/
declare function resolveAppConfigForUser(getAppConfig: GetAppConfig, user: UserForConfigResolution | null | undefined): Promise<AppConfig>;
//#endregion
//#region src/app/shutdown.d.ts
type ShutdownPhase = "pre-drain" | "post-drain";
type ShutdownTaskOptions = {
  priority?: number;
  phase?: ShutdownPhase;
};
/**
* Register a cleanup task for graceful shutdown. Post-drain is the default phase.
* Higher-priority tasks run first; tasks at the same priority retain registration order.
* If one throws, subsequent tasks and the final exit are not blocked. Use this instead of
* attaching `process.on('SIGTERM', ...)` handlers directly — multiple competing signal
* handlers race with the HTTP drain because Node dispatches listeners in registration order
* and any one of them can call `process.exit` before the HTTP server has finished closing.
*/
declare function registerShutdownTask(name: string, fn: () => void | Promise<void>, options?: ShutdownTaskOptions): void;
/** Whether graceful shutdown has started, for admission paths that must fail closed. */
/**
* Milliseconds left before graceful shutdown force-exits, or `null` when not shutting down.
* Lets a shutdown task spend the budget it actually has instead of guessing at a fixed cutoff.
*/
/**
* Milliseconds since graceful shutdown began, or `null` when not shutting down. For a task whose
* real deadline is imposed from outside this process — a cluster primary that force-exits the
* whole group on its own timer — this is what lets it measure against that deadline instead.
*/
declare function getShutdownElapsedMs(): number | null;
declare function getRemainingShutdownMs(): number | null;
declare function isShutdownInProgress(): boolean;
/**
* Wires SIGTERM, SIGINT, SIGQUIT, and SIGHUP to a graceful shutdown
* sequence: initiate HTTP server close to stop accepting new connections,
* run pre-drain tasks while in-flight requests settle, await the HTTP drain,
* run post-drain tasks, then `process.exit(0)`. After
* SHUTDOWN_TIMEOUT_MS the process is force-exited with code 1 — a
* safety net for long-lived connections such as SSE streams that may
* not finish in time.
*/
declare function setupGracefulShutdown(server: Server): void;
/**
* @internal Reset module state for tests. Not part of the public API.
*/
declare function __resetShutdownStateForTests(): void;
//#endregion
//#region src/app/server.d.ts
declare const configureServerTimeouts: (server: Server$1, environment?: NodeJS.ProcessEnv, versions?: {
  bun?: string;
}) => void;
//#endregion
//#region src/app/origin.d.ts
/** A listener's `server.address()`. */
interface BoundAddress {
  address: string;
  family?: string | number;
  port: number;
}
/** Resolve an HTTP origin that reaches the current process's bound listener. */
declare function selfOriginFromAddress(address: BoundAddress | string | null | undefined): string | null;
//#endregion
//#region src/app/agents.d.ts
type AgentEventRuntimeConfig = NonNullable<TAgentsEndpoint["eventDriven"]>;
/** Applies the optional base-config routing choice before the event listener starts. */
declare const configureAgentEventRuntime: (config?: AgentEventRuntimeConfig) => void;
//#endregion
//#region src/app/build.d.ts
interface BuildInfo {
  commit: string | null;
  commitShort: string | null;
  branch: string | null;
  buildDate: string | null;
}
/**
* Resolves the current deployment's build info from (in order): explicit env vars,
* then local git metadata. Used by `/api/config` to expose commit/branch to clients
* when `interface.buildInfo` is enabled.
*/
declare function resolveBuildInfo(): BuildInfo;
//#endregion
//#region src/acl/insightsPermissions.d.ts
type InsightsPermissionPrincipal = {
  type: PrincipalType;
  id?: string | Types.ObjectId | null;
  name?: string;
};
type InsightsWrittenEntry = {
  permBits: number;
  roleId: Types.ObjectId;
  grantedBy: string | Types.ObjectId;
  grantedAt: Date;
};
type InsightsPermissionChange = {
  action: "assigned" | "removed";
  previousEntry: IAclEntry | null;
  writtenEntry: InsightsWrittenEntry | null;
  principal: InsightsPermissionPrincipal;
};
type ValidationResult$1 = {
  status: 400 | 403;
  error: string;
} | null;
type AuditDeps = {
  getAgent: (filter: Record<string, unknown>, projection: string) => Promise<{
    id: string;
    name?: string;
  } | null>;
  recordAuditEntry: (input: RecordAuditEntryInput, options?: RecordAuditEntryOptions) => Promise<unknown>;
  restoreInsightsPermissionChanges: (changes: InsightsPermissionChange[]) => Promise<void>;
  logger: {
    error: (message: string, error?: unknown) => void;
  };
};
declare function validateInsightsPermissionUpdates({
  resourceType,
  userRole,
  updatedPrincipals
}: {
  resourceType: ResourceType;
  userRole?: string;
  updatedPrincipals: unknown[];
}): ValidationResult$1;
declare function maskAgentInsightsBit({
  resourceType,
  userRole,
  permBits
}: {
  resourceType: ResourceType;
  userRole?: string;
  permBits: number;
}): number;
declare function sanitizeInsightsPermissionPrincipals({
  resourceType,
  userRole,
  principals
}: {
  resourceType: ResourceType;
  userRole?: string;
  principals: TPrincipal[];
}): TPrincipal[];
declare function getInsightsPrincipalState({
  principalType,
  principalRole,
  requesterRole,
  permBits
}: {
  principalType: PrincipalType;
  principalRole?: string;
  requesterRole?: string;
  permBits: number;
}): {
  isAdmin?: true;
  viewInsights?: boolean;
};
declare function auditInsightsPermissionChanges({
  req,
  resourceId,
  changes,
  failClosed,
  deps
}: {
  req: ServerRequest;
  resourceId: string;
  changes: InsightsPermissionChange[];
  failClosed: boolean;
  deps: AuditDeps;
}): Promise<void>;
//#endregion
//#region src/acl/accessControlService.d.ts
type BulkPrincipal = Omit<TPrincipal, "id"> & {
  id?: string | Types.ObjectId;
  memberIds?: string[];
};
type BulkPermissionUpdateResult = {
  granted: BulkPrincipal[];
  updated: BulkPrincipal[];
  revoked: BulkPrincipal[];
  insightsChanges: InsightsPermissionChange[];
  errors: Array<{
    principal: BulkPrincipal;
    error: string;
  }>;
};
declare class AccessControlService {
  private _dbMethods;
  private _mongoose;
  private _transactionSupportCache;
  constructor(mongoose: typeof import("mongoose"), dbMethods?: AllMethods);
  /**
  * Grant a permission to a principal for a resource using a role
  * @param {Object} params - Parameters for granting role-based permission
  * @param {string} params.principalType - PrincipalType.USER, PrincipalType.GROUP, or PrincipalType.PUBLIC
  * @param {string|mongoose.Types.ObjectId|null} params.principalId - The ID of the principal (null for PrincipalType.PUBLIC)
  * @param {string} params.resourceType - Type of resource (e.g., 'agent')
  * @param {string|mongoose.Types.ObjectId} params.resourceId - The ID of the resource
  * @param {string} params.accessRoleId - The ID of the role (e.g., AccessRoleIds.AGENT_VIEWER, AccessRoleIds.AGENT_EDITOR)
  * @param {Types.ObjectId} params.grantedBy - User ID granting the permission
  * @param {ClientSession} [params.session] - Optional MongoDB session for transactions
  * @param {Date} [params.expiredAt] - Optional expiration for resource-tied permissions
  * @returns {Promise<IAclEntry>} The created or updated ACL entry
  */
  grantPermission(args: {
    principalType: PrincipalType;
    principalId: string | Types.ObjectId | null;
    resourceType: string;
    resourceId: string | Types.ObjectId;
    accessRoleId: AccessRoleIds;
    grantedBy?: string | Types.ObjectId;
    session?: ClientSession;
    roleId?: string | Types.ObjectId;
    expiredAt?: Date;
  }): Promise<IAclEntry | null>;
  /**
  * Find all resources of a specific type that a user has access to with specific permission bits
  * @param {Object} params - Parameters for finding accessible resources
  * @param {string | Types.ObjectId} params.userId - The ID of the user
  * @param {string} [params.role] - Optional user role (if not provided, will query from DB)
  * @param {string} params.resourceType - Type of resource (e.g., 'agent')
  * @param {number} params.requiredPermissions - The minimum permission bits required (e.g., 1 for VIEW, 3 for VIEW+EDIT)
  * @param {Types.ObjectId[]} [params.resourceIds] - Optional candidate bound; only these
  *   resources are considered, so the query cost scales with the candidate set
  * @returns {Promise<Array>} Array of resource IDs
  */
  findAccessibleResources({
    userId,
    role,
    resourceType,
    requiredPermissions,
    resourceIds
  }: {
    userId: string | Types.ObjectId;
    role?: string;
    resourceType: string;
    requiredPermissions: number;
    resourceIds?: Types.ObjectId[];
  }): Promise<Types.ObjectId[]>;
  getUserPrincipals({
    userId,
    role
  }: {
    userId: string | Types.ObjectId;
    role?: string;
  }): Promise<ResolvedPrincipal[]>;
  findAccessibleResourcesForPrincipals({
    principalsList,
    resourceType,
    requiredPermissions,
    resourceIds
  }: {
    principalsList: ResolvedPrincipal[];
    resourceType: string;
    requiredPermissions: number;
    resourceIds?: Types.ObjectId[];
  }): Promise<Types.ObjectId[]>;
  /**
  * Find all publicly accessible resources of a specific type
  * @param {Object} params - Parameters for finding publicly accessible resources
  * @param {ResourceType} params.resourceType - Type of resource (e.g., 'agent')
  * @param {number} params.requiredPermissions - The minimum permission bits required (e.g., 1 for VIEW, 3 for VIEW+EDIT)
  * @param {Types.ObjectId[]} [params.resourceIds] - Optional candidate bound; only these
  *   resources are considered, so the query cost scales with the candidate set
  * @returns {Promise<Types.ObjectId[]>} Array of resource IDs
  */
  findPubliclyAccessibleResources({
    resourceType,
    requiredPermissions,
    resourceIds
  }: {
    resourceType: ResourceType;
    requiredPermissions: number;
    resourceIds?: Types.ObjectId[];
  }): Promise<Types.ObjectId[]>;
  /**
  * Get effective permissions for multiple resources in a batch operation
  * Returns map of resourceId → effectivePermissionBits
  *
  * @param {Object} params - Parameters
  * @param {string|mongoose.Types.ObjectId} params.userId - User ID
  * @param {string} [params.role] - User role (for group membership)
  * @param {string} params.resourceType - Resource type (must be valid ResourceType)
  * @param {Array<mongoose.Types.ObjectId>} params.resourceIds - Array of resource IDs
  * @returns {Promise<Map<string, number>>} Map of resourceId string → permission bits
  * @throws {Error} If resourceType is invalid
  */
  getResourcePermissionsMap({
    userId,
    role,
    resourceType,
    resourceIds
  }: {
    userId: string | Types.ObjectId;
    role: string;
    resourceType: ResourceType;
    resourceIds: (string | Types.ObjectId)[];
  }): Promise<Map<string, number>>;
  getResourcePermissionsMapForPrincipals({
    principalsList,
    resourceType,
    resourceIds
  }: {
    principalsList: ResolvedPrincipal[];
    resourceType: ResourceType;
    resourceIds: (string | Types.ObjectId)[];
  }): Promise<Map<string, number>>;
  /**
  * Remove all permissions for a resource (cleanup when resource is deleted)
  * @param {Object} params - Parameters for removing all permissions
  * @param {string} params.resourceType - Type of resource (e.g., 'agent', 'prompt')
  * @param {string|mongoose.Types.ObjectId} params.resourceId - The ID of the resource
  * @returns {Promise<DeleteResult>} Result of the deletion operation
  */
  removeAllPermissions({
    resourceType,
    resourceId
  }: {
    resourceType: ResourceType;
    resourceId: string | Types.ObjectId;
  }): Promise<DeleteResult>;
  bulkUpdateResourcePermissions({
    resourceType,
    resourceId,
    updatedPrincipals,
    revokedPrincipals,
    grantedBy,
    session
  }: {
    resourceType: ResourceType;
    resourceId: string | Types.ObjectId;
    updatedPrincipals?: BulkPrincipal[];
    revokedPrincipals?: BulkPrincipal[];
    grantedBy: string | Types.ObjectId;
    session?: ClientSession;
  }): Promise<BulkPermissionUpdateResult>;
  restoreInsightsPermissionChanges({
    resourceType,
    resourceId,
    changes
  }: {
    resourceType: ResourceType;
    resourceId: string | Types.ObjectId;
    changes?: InsightsPermissionChange[];
  }): Promise<void>;
  /**
  * Check if a user has specific permission bits on a resource
  * @param {Object} params - Parameters for checking permissions
  * @param {string|mongoose.Types.ObjectId} params.userId - The ID of the user
  * @param {string} [params.role] - Optional user role (if not provided, will query from DB)
  * @param {string} params.resourceType - Type of resource (e.g., 'agent')
  * @param {string|mongoose.Types.ObjectId} params.resourceId - The ID of the resource
  * @param {number} params.requiredPermissions - The permission bits required (e.g., 1 for VIEW, 3 for VIEW+EDIT)
  * @returns {Promise<boolean>} Whether the user has the required permission bits
  */
  checkPermission({
    userId,
    role,
    resourceType,
    resourceId,
    requiredPermission
  }: {
    userId: string;
    role?: string | null;
    resourceType: ResourceType;
    resourceId: string | Types.ObjectId;
    requiredPermission: number;
  }): Promise<boolean>;
  /**
  * Check if a resource has a PUBLIC AclEntry (accessible to everyone).
  * Unlike checkPermission, this does not require a user context.
  */
  hasPublicAccess({
    resourceType,
    resourceId
  }: {
    resourceType: ResourceType;
    resourceId: string | Types.ObjectId;
  }): Promise<boolean>;
  /**
  * Validates that the resourceType is one of the supported enum values
  * @param {string} resourceType - The resource type to validate
  * @throws {Error} If resourceType is not valid
  */
  private validateResourceType;
}
//#endregion
//#region src/acl/middleware.d.ts
type AgentPermissionsRequest = ServerRequest & {
  params: {
    resourceType: string;
    resourceId: string;
  };
};
type Middleware = (req: AgentPermissionsRequest, res: Response$2, next: NextFunction) => unknown;
declare function isAgentPermissionsAdmin(req: AgentPermissionsRequest): boolean;
declare function createAgentAdminPermissionAccess({
  getAgent,
  fallback
}: Pick<AllMethods, "getAgent"> & {
  fallback: Middleware;
}): Middleware;
//#endregion
//#region src/acl/principals.d.ts
interface DirectoryPrincipalUser {
  id: string;
}
interface DirectoryPrincipalUserData {
  name?: string;
  email: string;
  emailVerified: false;
  provider: "openid";
  idOnTheSource: string;
}
interface DirectoryPrincipalUserMethods {
  findUserBySourceId: (idOnTheSource: string) => Promise<DirectoryPrincipalUser | null>;
  findUserByEmail: (email: string) => Promise<DirectoryPrincipalUser | null>;
  createUser: (user: DirectoryPrincipalUserData) => Promise<string>;
}
type DirectoryPrincipal = Pick<TPrincipal, "name" | "email" | "idOnTheSource">;
declare const ensureDirectoryPrincipalUser: (principal: DirectoryPrincipal, methods: DirectoryPrincipalUserMethods) => Promise<string>;
//#endregion
//#region src/acl/search.d.ts
type SearchablePrincipalType = PrincipalType.USER | PrincipalType.GROUP | PrincipalType.ROLE;
type EntraPrincipalSearchType = "all" | "users" | "groups";
type PrincipalSearchUser = Partial<Pick<TUser, "id" | "role" | "provider">> & {
  openidId?: string;
};
type PrincipalSearchRequest = Pick<Request, "query" | "headers"> & {
  user?: PrincipalSearchUser; /** Principal types the caller may search, resolved by the people picker access check. */
  principalSearchTypes?: SearchablePrincipalType[];
};
type PeoplePickerAccess = (req: PrincipalSearchRequest, res: Response$2, next: NextFunction) => Promise<Response$2 | void>;
type PeoplePickerRole = {
  permissions?: {
    [PermissionTypes.PEOPLE_PICKER]?: Partial<TPeoplePickerPermissions>;
  };
};
type PrincipalSearch = (req: PrincipalSearchRequest, res: Response$2) => Promise<void>;
type ScoredPrincipal = TPrincipalSearchResult & {
  _searchScore: number;
};
interface PrincipalSearchDeps {
  searchPrincipals: (query: string, limitPerType: number, types: SearchablePrincipalType[]) => Promise<TPrincipalSearchResult[]>;
  calculateRelevanceScore: (item: TPrincipalSearchResult, query: string) => number;
  sortPrincipalsByRelevance: (results: ScoredPrincipal[]) => ScoredPrincipal[];
  entraIdPrincipalFeatureEnabled: (user?: PrincipalSearchUser) => boolean;
  searchEntraIdPrincipals: (accessToken: string, sub: string | undefined, query: string, type: EntraPrincipalSearchType, limit: number) => Promise<TPrincipalSearchResult[]>;
}
/**
* Collects the principal types named by the `type` and `types` query parameters, in request order.
* Each value may be a single type, a comma-separated list, or a repeated parameter.
*/
declare function getRequestedPrincipalTypes(query: PrincipalSearchRequest["query"]): SearchablePrincipalType[];
/** Maps resolved principal types to the Microsoft Graph search scope, or `null` when none apply. */
declare function getEntraPrincipalSearchType(types: readonly SearchablePrincipalType[]): EntraPrincipalSearchType | null;
/**
* Creates the people picker access check. It resolves the principal types the caller may search —
* the requested types when every one is permitted, otherwise every permitted type — and stores
* them on `req.principalSearchTypes` for the search handler.
*/
declare function createPeoplePickerAccess({
  getRoleByName
}: {
  getRoleByName: (roleName: string) => Promise<PeoplePickerRole | null>;
}): PeoplePickerAccess;
/**
* Creates the principal search handler. It searches only `req.principalSearchTypes`, as resolved by
* the people picker access check, so a request that skipped the check searches no types.
*/
declare function createPrincipalSearch(deps: PrincipalSearchDeps): PrincipalSearch;
//#endregion
//#region src/artifacts/update.d.ts
declare const ARTIFACT_START = ":::artifact";
declare const ARTIFACT_END = ":::";
type TextPart = {
  type?: string;
  text?: string;
};
type ArtifactMessage = {
  content?: TextPart[];
  text?: string;
};
type ArtifactBoundary = {
  start: number;
  end: number;
  source: "content" | "text";
  partIndex?: number;
  text: string;
};
declare const findAllArtifacts: (message: ArtifactMessage) => ArtifactBoundary[];
declare const replaceArtifactContent: (originalText: string, artifact: ArtifactBoundary, original: string, updated: string) => string | null;
//#endregion
//#region src/middleware/capabilities.d.ts
interface CapabilityDeps {
  getUserPrincipals: (params: {
    userId: string | Types.ObjectId;
    role?: string | null;
    idOnTheSource?: string | null;
  }, session?: ClientSession) => Promise<ResolvedPrincipal[]>;
  hasCapabilityForPrincipals: (params: {
    principals: ResolvedPrincipal[];
    capability: SystemCapability;
    tenantId?: string;
  }) => Promise<boolean>;
  hasAnyConfigReadAccess?: (params: {
    principals: ResolvedPrincipal[];
    tenantId?: string;
  }) => Promise<boolean>;
  getHeldCapabilities?: (params: {
    principals: ResolvedPrincipal[];
    capabilities: SystemCapability[];
    tenantId?: string;
  }) => Promise<Set<SystemCapability>>;
}
interface CapabilityUser {
  id: string;
  role: string;
  tenantId?: string;
  /** External member id; pass `null` for local users to skip the fallback lookup. */
  idOnTheSource?: string | null;
}
interface CapabilityStore {
  principals: Map<string, ResolvedPrincipal[]>;
  results: Map<string, boolean>;
}
type HasCapabilityFn = (user: CapabilityUser, capability: SystemCapability) => Promise<boolean>;
type RequireCapabilityFn = (capability: SystemCapability, options?: {
  platformOnly?: boolean;
}) => (req: ServerRequest, res: Response$2, next: NextFunction) => Promise<void>;
type HasConfigCapabilityFn = (user: CapabilityUser, section: ConfigSection | null, verb?: "manage" | "read") => Promise<boolean>;
type GetHeldCapabilitiesFn = (user: CapabilityUser, capabilities: SystemCapability[]) => Promise<Set<SystemCapability>>;
/**
* Per-request store for caching resolved principals and capability check results.
* When running inside an Express request (via `capabilityContextMiddleware`),
* duplicate `hasCapability` calls within the same request are served from
* the in-memory Map instead of hitting the database again.
* Outside a request context (background jobs, tests), the store is undefined
* and every check falls through to the database — correct behavior.
*/
declare const capabilityStore: AsyncLocalStorage<CapabilityStore>;
declare function capabilityContextMiddleware(_req: ServerRequest, _res: Response$2, next: NextFunction): void;
/**
* Reads principals from the per-request ALS cache without side effects.
* Returns `undefined` when called outside a request context or before
* `requireCapability` has populated the cache for this user.
*/
declare function getCachedPrincipals(user: CapabilityUser): ResolvedPrincipal[] | undefined;
/**
* Factory that creates `hasCapability` and `requireCapability` with injected
* database methods. Follows the same dependency-injection pattern as
* `generateCheckAccess`.
*/
type GetReadableConfigSectionsFn = (user: CapabilityUser, sections: ConfigSection[]) => Promise<{
  broad: boolean;
  sections: Set<string>;
}>;
declare function generateCapabilityCheck(deps: CapabilityDeps): {
  hasCapability: HasCapabilityFn;
  requireCapability: RequireCapabilityFn;
  hasConfigCapability: HasConfigCapabilityFn;
  getHeldCapabilities: GetHeldCapabilitiesFn;
  hasAnyConfigReadAccess: (user: CapabilityUser) => Promise<boolean>;
  getReadableConfigSections: GetReadableConfigSectionsFn;
};
//#endregion
//#region src/admin/config.d.ts
interface AdminConfigDeps {
  listAllConfigs: (filter?: {
    isActive?: boolean;
  }, session?: ClientSession) => Promise<IConfig[]>;
  findConfigByPrincipal: (principalType: PrincipalType, principalId: string | Types.ObjectId, options?: {
    includeInactive?: boolean;
  }, session?: ClientSession) => Promise<IConfig | null>;
  upsertConfig: (principalType: PrincipalType, principalId: string | Types.ObjectId, principalModel: PrincipalModel, overrides: Partial<TCustomConfig>, priority: number, session?: ClientSession, options?: {
    expectEmpty?: boolean;
    preservePriority?: boolean;
  }) => Promise<IConfig | null>;
  patchConfigFields: (principalType: PrincipalType, principalId: string | Types.ObjectId, principalModel: PrincipalModel, fields: Record<string, unknown>, priority: number, session?: ClientSession) => Promise<IConfig | null>;
  tombstoneConfigField: (principalType: PrincipalType, principalId: string | Types.ObjectId, principalModel: PrincipalModel, fieldPath: string, priority: number, session?: ClientSession) => Promise<IConfig | null>;
  unsetConfigField: (principalType: PrincipalType, principalId: string | Types.ObjectId, fieldPath: string, session?: ClientSession) => Promise<IConfig | null>;
  deleteConfig: (principalType: PrincipalType, principalId: string | Types.ObjectId, session?: ClientSession, options?: {
    expectEmpty?: boolean;
  }) => Promise<IConfig | null>;
  toggleConfigActive: (principalType: PrincipalType, principalId: string | Types.ObjectId, isActive: boolean, session?: ClientSession, options?: {
    expectEmpty?: boolean;
  }) => Promise<IConfig | null>;
  hasConfigCapability: (user: CapabilityUser, section: ConfigSection | null, verb?: "manage" | "read") => Promise<boolean>;
  /** Pre-flight-only: whether the caller holds any config-read capability at all (broad or any section), so a zero-access caller 403s before a DB fetch. */
  hasAnyConfigReadAccess?: (user: CapabilityUser) => Promise<boolean>;
  /** Resolves which of a set of sections the caller can read in a single batched query. */
  getReadableConfigSections?: (user: CapabilityUser, sections: ConfigSection[]) => Promise<{
    broad: boolean;
    sections: Set<string>;
  }>;
  hasCapability?: (user: CapabilityUser, capability: SystemCapability) => Promise<boolean>;
  getAppConfig?: (options?: {
    role?: string;
    userId?: string;
    tenantId?: string;
    baseOnly?: boolean;
  }) => Promise<AppConfig>;
  /** Invalidate all config-related caches after a mutation. */
  invalidateConfigCaches?: (tenantId?: string) => Promise<void>;
}
declare function createAdminConfigHandlers(deps: AdminConfigDeps): {
  listConfigs: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  getBaseConfig: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  getConfig: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  upsertConfigOverrides: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  patchConfigField: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  tombstoneConfigField: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  deleteConfigField: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  deleteConfigOverrides: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  toggleConfig: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
};
//#endregion
//#region src/admin/langfuse.d.ts
type LangfuseConnectionChange = "created" | "credentials_rotated" | "destination_changed" | "disabled" | "enabled" | "updated";
interface LangfuseConnectionEvent {
  event_name: "librechat.langfuse.connection.changed";
  tenant_id?: string;
  configured: boolean;
  enabled: boolean;
  destination?: string;
  change: LangfuseConnectionChange;
  changes: LangfuseConnectionChange[];
  verification_result: "skipped" | "success";
}
interface AdminLangfuseDeps {
  findConfigByPrincipal: (principalType: PrincipalType, principalId: string | Types.ObjectId, options?: {
    includeInactive?: boolean;
  }, session?: ClientSession) => Promise<IConfig | null>;
  patchConfigFields: (principalType: PrincipalType, principalId: string | Types.ObjectId, principalModel: PrincipalModel, fields: Record<string, unknown>, priority: number, session?: ClientSession) => Promise<IConfig | null>;
  toggleConfigActive: (principalType: PrincipalType, principalId: string | Types.ObjectId, isActive: boolean, session?: ClientSession) => Promise<IConfig | null>;
  getMessages: MessageMethods["getMessages"];
  invalidateConfigCaches?: (tenantId?: string) => Promise<void>;
  recordConnectionUpdate?: (event: LangfuseConnectionEvent) => void;
}
/**
* Admin handlers for the per-tenant Langfuse connection.
*
* The connection is stored as a `langfuse` override on the base config so it is
* resolved for every user in the tenant. The secret key is encrypted at rest and
* never returned by read endpoints; reads expose only non-secret metadata.
*/
declare function createAdminLangfuseHandlers(deps: AdminLangfuseDeps): {
  getConnection: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  getSessionLink: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  updateConnection: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  testConnection: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
};
//#endregion
//#region src/admin/grants.d.ts
interface AdminGrantsDeps {
  listGrants: (options?: {
    tenantId?: string;
    principalTypes?: PrincipalType[];
    limit?: number;
    offset?: number;
  }) => Promise<ISystemGrant[]>;
  countGrants: (options?: {
    tenantId?: string;
    principalTypes?: PrincipalType[];
  }) => Promise<number>;
  getCapabilitiesForPrincipal: (params: {
    principalType: PrincipalType;
    principalId: string | Types.ObjectId;
    tenantId?: string;
  }) => Promise<ISystemGrant[]>;
  getCapabilitiesForPrincipals: (params: {
    principals: Array<{
      principalType: PrincipalType;
      principalId: string | Types.ObjectId;
    }>;
    tenantId?: string;
  }) => Promise<ISystemGrant[]>;
  grantCapability: (params: {
    principalType: PrincipalType;
    principalId: string | Types.ObjectId;
    capability: SystemCapability;
    tenantId?: string;
    grantedBy?: string | Types.ObjectId;
  }) => Promise<{
    grant: ISystemGrant | null;
    created: boolean;
  }>;
  revokeCapability: (params: {
    principalType: PrincipalType;
    principalId: string | Types.ObjectId;
    capability: SystemCapability;
    tenantId?: string;
  }) => Promise<{
    deletedCount: number;
  }>;
  getUserPrincipals: (params: {
    userId: string;
    role?: string | null;
    tenantId?: string;
  }) => Promise<ResolvedPrincipal[]>;
  hasCapabilityForPrincipals: (params: {
    principals: ResolvedPrincipal[];
    capability: SystemCapability;
    tenantId?: string;
  }) => Promise<boolean>;
  getHeldCapabilities: (params: {
    principals: ResolvedPrincipal[];
    capabilities: SystemCapability[];
    tenantId?: string;
  }) => Promise<Set<SystemCapability>>;
  getCachedPrincipals?: (user: {
    id: string;
    role: string;
    tenantId?: string;
  }) => ResolvedPrincipal[] | undefined;
  checkRoleExists?: (roleId: string) => Promise<boolean>;
  /** Optional audit emission. Failure is logged but does not roll back the grant
  * unless `auditFailClosed` is set. */
  recordAuditEntry?: (input: RecordAuditEntryInput, options?: RecordAuditEntryOptions) => Promise<void>;
  /**
  * When true, a failed audit write surfaces as a 5xx instead of being
  * swallowed. The grant itself is already persisted (and grant writes are
  * idempotent upserts), so a client retry is safe; an operator must reconcile
  * the missing audit row. Defaults to fail-open.
  */
  auditFailClosed?: boolean;
}
/** Currently ROLE-only; Record/Set structure preserved for future principal-type expansion. */
type GrantPrincipalType = PrincipalType.ROLE;
/** Creates admin grant handlers with dependency injection for the /api/admin/grants routes. */
declare function createAdminGrantsHandlers(deps: AdminGrantsDeps): {
  listGrants: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  getEffectiveCapabilities: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  getPrincipalGrants: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  assignGrant: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  revokeGrant: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
};
//#endregion
//#region src/admin/groups.d.ts
type GroupListFilter = Pick<GroupFilterOptions, "source" | "search">;
interface AdminGroupsDeps {
  listGroups: (filter?: GroupListFilter & {
    limit?: number;
    offset?: number;
  }, session?: ClientSession) => Promise<IGroup[]>;
  countGroups: (filter?: GroupListFilter, session?: ClientSession) => Promise<number>;
  findGroupById: (groupId: string | Types.ObjectId, projection?: Record<string, 0 | 1>, session?: ClientSession) => Promise<IGroup | null>;
  createGroup: (groupData: Partial<IGroup>, session?: ClientSession) => Promise<IGroup>;
  updateGroupById: (groupId: string | Types.ObjectId, data: Partial<Pick<IGroup, "name" | "description" | "email" | "avatar">>, session?: ClientSession) => Promise<IGroup | null>;
  deleteGroup: (groupId: string | Types.ObjectId, session?: ClientSession) => Promise<IGroup | null>;
  addUserToGroup: (userId: string | Types.ObjectId, groupId: string | Types.ObjectId, session?: ClientSession) => Promise<{
    user: IUser;
    group: IGroup | null;
  }>;
  removeUserFromGroup: (userId: string | Types.ObjectId, groupId: string | Types.ObjectId, session?: ClientSession) => Promise<{
    user: IUser;
    group: IGroup | null;
  }>;
  removeMemberById: (groupId: string | Types.ObjectId, memberId: string, session?: ClientSession) => Promise<IGroup | null>;
  findUsers: (searchCriteria: FilterQuery<IUser>, fieldsToSelect?: string | string[] | null) => Promise<IUser[]>;
  deleteConfig: (principalType: PrincipalType, principalId: string | Types.ObjectId) => Promise<IConfig | null>;
  deleteAclEntries: (filter: {
    principalType: PrincipalType;
    principalId: string | Types.ObjectId;
  }) => Promise<DeleteResult>;
}
declare function createAdminGroupsHandlers(deps: AdminGroupsDeps): {
  listGroups: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  getGroup: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  createGroup: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  updateGroup: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  deleteGroup: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  getGroupMembers: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  addGroupMember: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  removeGroupMember: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
};
//#endregion
//#region src/admin/roles.d.ts
type RoleListItem = {
  _id: Types.ObjectId | string;
  name: string;
  description?: string;
};
interface AdminRolesDeps {
  listRoles: (options?: {
    limit?: number;
    offset?: number;
  }) => Promise<RoleListItem[]>;
  countRoles: () => Promise<number>;
  getRoleByName: (name: string, fields?: string | string[] | null) => Promise<IRole | null>;
  createRoleByName: (roleData: Partial<IRole>) => Promise<IRole>;
  updateRoleByName: (name: string, updates: Partial<IRole>) => Promise<IRole | null>;
  updateAccessPermissions: (name: string, perms: Record<string, Record<string, boolean>>, roleData?: IRole) => Promise<void>;
  deleteRoleByName: (name: string) => Promise<IRole | null>;
  findUser: (criteria: FilterQuery<IUser>, fields?: string | string[] | null) => Promise<IUser | null>;
  updateUser: (userId: string, data: Partial<IUser>) => Promise<IUser | null>;
  updateUsersByRole: (oldRole: string, newRole: string) => Promise<void>;
  findUserIdsByRole: (roleName: string) => Promise<string[]>;
  updateUsersRoleByIds: (userIds: string[], newRole: string) => Promise<void>;
  listUsersByRole: (roleName: string, options?: {
    limit?: number;
    offset?: number;
  }) => Promise<IUser[]>;
  countUsersByRole: (roleName: string) => Promise<number>;
  /** Removes the per-principal config override (keyed by type + name, not ObjectId). */
  deleteConfig: (principalType: PrincipalType, principalId: string | Types.ObjectId) => Promise<IConfig | null>;
  /** Removes all ACL entries scoped to this principal. */
  deleteAclEntries: (filter: {
    principalType: PrincipalType;
    principalId: string | Types.ObjectId;
  }) => Promise<void>;
  /** Drops cached prompt group access IDs; role deletion can remove PROMPTGROUP grants. */
  invalidatePromptGroupAccessContext?: () => Promise<void>;
  /** Removes all system capability grants held by this principal and returns
  * the removed grants so each can be audited. */
  deleteGrantsForPrincipal: (principalType: PrincipalType, principalId: string | Types.ObjectId, options?: {
    tenantId?: string;
  }) => Promise<ISystemGrant[]>;
  /** Optional audit emission for grants removed by the role-deletion cascade.
  * Failure is logged but never blocks the deletion (which has already happened). */
  recordAuditEntry?: (input: RecordAuditEntryInput, options?: RecordAuditEntryOptions) => Promise<void>;
}
declare function createAdminRolesHandlers(deps: AdminRolesDeps): {
  listRoles: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  getRole: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  createRole: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  updateRole: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  updateRolePermissions: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  deleteRole: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  getRoleMembers: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  addRoleMember: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  removeRoleMember: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
};
//#endregion
//#region src/skills/sync/adapters/types.d.ts
/**
* `submodule` covers entries that are neither readable files nor descendable
* directories (a Git submodule, which GitHub reports as `commit`). They are
* carried through rather than dropped so path-existence checks keep seeing
* everything the repository actually contains.
*/
type RepoTreeEntryType = "blob" | "tree" | "submodule";
/**
* One entry in a repository tree, normalized across providers.
*
* `id` is the provider's identifier for the entry's content (a GitHub blob SHA,
* a GitLab blob id). It is round-tripped back into `fetchFileContent` and
* persisted in `sourceMetadata`, which lets a later run skip re-downloading a
* file whose id has not moved. Treat it as opaque outside the adapter.
*
* `size` is the blob's byte length when the provider reports it in the tree
* listing. Skill import limits are enforced against it before any content is
* downloaded, so an adapter whose tree endpoint omits size must populate it
* some other way rather than leave it undefined.
*/
type RepoTreeEntry = {
  path: string;
  type: RepoTreeEntryType;
  id: string;
  size?: number;
};
/** The commit a single sync run is pinned to. */
type RepoCommit = {
  /** Persisted as the run's `commitSha`; opaque outside the adapter. */id: string;
  /**
  * Root tree identifier as of this commit — where `fetchTreeEntries` starts
  * walking. Distinct from `id` on providers whose commit and tree objects are
  * addressed separately (GitHub); equal to `id` where they are not.
  */
  treeId: string;
};
/**
* Throws when the run has been superseded or shut down. Adapters call it
* between network round trips so a long listing or download loop stops
* promptly instead of running to completion against a cancelled run.
*/
type AssertNotCancelled = () => void;
type FetchTreeEntriesParams = {
  /** Repository-root-relative directory to list, or `''` for the whole repository. */pathPrefix: string;
  assertNotCancelled: AssertNotCancelled;
};
/**
* The provider-specific surface a skill sync source needs, and nothing more.
* Everything else in a run — skill discovery, import limits, database upsert
* and reconciliation, status accounting — is provider-agnostic and depends only
* on this interface.
*
* An adapter is bound to one configured source (its repository coordinates,
* ref, and credentials) when constructed, so no call takes them again.
*/
interface GitRepoAdapter {
  /**
  * Resolves the source's configured ref to the commit that pins this run, so
  * every entry listed and file fetched within it stays consistent even if the
  * upstream ref moves mid-sync.
  */
  resolveCommit(): Promise<RepoCommit>;
  /**
  * Lists every entry beneath `pathPrefix` recursively, as of `commit`. Paths
  * are returned relative to the repository root rather than to `pathPrefix`,
  * so entries listed from several configured paths can be merged without
  * ambiguity.
  */
  fetchTreeEntries(commit: RepoCommit, params: FetchTreeEntriesParams): Promise<RepoTreeEntry[]>;
  /**
  * Fetches one file's raw bytes. The caller has already checked `entry.size`
  * against the skill import limits, so implementations do not repeat that.
  */
  fetchFileContent(commit: RepoCommit, entry: RepoTreeEntry): Promise<Buffer>;
}
//#endregion
//#region src/skills/sync/adapters/github.d.ts
declare const GITHUB_FINE_GRAINED_TOKEN_RECOMMENDATION = "Use a GitHub fine-grained personal access token scoped to the selected repository with read-only Contents and Metadata permissions.";
type FetchFn$1 = typeof fetch;
type GitHubRepoAdapterConfig = {
  source: SkillSyncGitHubSourceConfig;
  token: string;
  fetchFn: FetchFn$1;
};
//#endregion
//#region src/skills/sync/github.d.ts
type FetchFn = typeof fetch;
type SaveBufferResult = {
  filepath: string;
  source: string;
  storageKey?: string;
  storageRegion?: string;
};
type MaybePromise$3<T> = T | Promise<T>;
type GitHubSkillSyncDeps = {
  getConfig: () => MaybePromise$3<SkillSyncConfig | undefined>;
  getCredentialToken: (provider: SkillSyncProvider, credentialKey: string) => Promise<string | null>;
  getCredentialSummary: (provider: SkillSyncProvider, credentialKey: string) => Promise<SkillSyncCredentialSummary | null>;
  listCredentials: (provider: SkillSyncProvider) => Promise<SkillSyncCredentialSummary[]>;
  listStatuses: (provider: SkillSyncProvider) => Promise<ISkillSyncStatus[]>;
  upsertStatus: (input: SkillSyncStatusInput) => Promise<ISkillSyncStatus>;
  tryAcquireLock: (params: {
    provider: SkillSyncProvider;
    lockOwner: string;
    leaseMs: number;
    tenantId?: string;
  }) => Promise<boolean>;
  refreshLock: (params: {
    provider: SkillSyncProvider;
    lockOwner: string;
    leaseMs: number;
    tenantId?: string;
  }) => Promise<boolean>;
  releaseLock: (params: {
    provider: SkillSyncProvider;
    lockOwner: string;
    tenantId?: string;
  }) => Promise<void>;
  createSkill: (data: CreateSkillInput) => Promise<CreateSkillResult>;
  updateSkill: (params: {
    id: string;
    expectedVersion: number;
    update: UpdateSkillInput;
  }) => Promise<UpdateSkillResult>;
  getSkillById: (id: string | Types.ObjectId) => Promise<(ISkill & {
    _id: Types.ObjectId;
  }) | null>;
  findSkillBySourceIdentity: (params: {
    source: "github" | "notion";
    upstreamId: string;
    tenantId?: string;
  }) => Promise<(ISkill & {
    _id: Types.ObjectId;
  }) | null>;
  listSkillsBySource: (params: {
    source: "github" | "notion";
    sourceId: string;
  }) => Promise<Array<ISkill & {
    _id: Types.ObjectId;
  }>>;
  listSkillFiles: (skillId: string | Types.ObjectId) => Promise<Array<ISkillFile & {
    _id: Types.ObjectId;
  }>>;
  getSkillFileByPath: (skillId: string | Types.ObjectId, relativePath: string) => Promise<(ISkillFile & {
    _id: Types.ObjectId;
  }) | null>;
  upsertSkillFile: (row: UpsertSkillFileInput) => Promise<ISkillFile & {
    _id: Types.ObjectId;
  }>;
  deleteSkillFile: (skillId: string | Types.ObjectId, relativePath: string) => Promise<{
    deleted: boolean;
  }>;
  deleteSkill: (id: string) => Promise<{
    deleted: boolean;
  }>;
  saveBuffer: (params: {
    userId: string;
    buffer: Buffer;
    fileName: string;
    basePath?: string;
    isImage?: boolean;
    tenantId?: string;
  }) => Promise<SaveBufferResult>;
  deleteFile?: (file: {
    filepath: string;
    source: string;
    storageKey?: string;
    storageRegion?: string;
    user?: Types.ObjectId | string;
    tenantId?: string;
  }) => Promise<void>;
  grantPermission: (params: {
    principalType: string;
    principalId: string | Types.ObjectId | null;
    resourceType: string;
    resourceId: string | Types.ObjectId;
    accessRoleId: string;
    grantedBy: string | Types.ObjectId;
  }) => Promise<unknown>;
  fetchFn?: FetchFn;
  /**
  * Builds the repository client a source is synced through. Defaults to the
  * GitHub adapter; overridable so the orchestration can be exercised against a
  * fake repository without standing up provider HTTP responses.
  */
  createAdapter?: (config: GitHubRepoAdapterConfig) => GitRepoAdapter;
  lockOwner?: string;
  allowServerCredentials?: boolean;
};
type GitHubSkillSyncRunResult = {
  status: "started" | "skipped" | "completed" | "failed";
  message?: string;
  sources: Array<ISkillSyncStatus & {
    credentialPresent?: boolean;
  }>;
};
type GitHubSkillSyncStatus = {
  enabled: boolean;
  intervalMinutes: number;
  runOnStartup: boolean;
  sources: Array<ISkillSyncStatus & {
    credentialPresent: boolean;
  }>;
  credentials: SkillSyncCredentialSummary[];
  fineGrainedTokenRecommendation: string;
};
type GitHubSkillSyncRunner = {
  getStatus: () => Promise<GitHubSkillSyncStatus>;
  runOnce: () => Promise<GitHubSkillSyncRunResult>;
};
declare function createGitHubSkillSyncRunner(deps: GitHubSkillSyncDeps): GitHubSkillSyncRunner;
//#endregion
//#region src/skills/sync/orchestrator.d.ts
type MaybePromise$2<T> = T | Promise<T>;
type SkillSyncAppConfigLike = {
  skillSync?: unknown;
  config?: {
    skillSync?: unknown;
  };
};
type SkillSyncRequestUser = {
  tenantId?: string | null;
};
type SkillSyncRequestLike = {
  config?: SkillSyncAppConfigLike;
  user?: SkillSyncRequestUser;
  skillSyncAllowServerCredentials?: boolean;
};
type SkillSyncTriggerLogger = {
  warn: (message: string, metadata?: object) => void;
  error: (message: string, error?: unknown) => void;
};
type SkillSyncTriggerRunnerFactoryInput = {
  getConfig: () => MaybePromise$2<SkillSyncConfig | undefined>;
  loadAppConfig: () => MaybePromise$2<SkillSyncAppConfigLike | undefined>;
  allowServerCredentials?: boolean;
};
type SkillSyncTriggerOrchestratorDeps = {
  createRunner: (input: SkillSyncTriggerRunnerFactoryInput) => GitHubSkillSyncRunner;
  logger: SkillSyncTriggerLogger;
  minIntervalMs?: number;
  staleRunningMs?: number;
  inFlight?: Set<string>;
};
type SkillSyncTriggerOrchestrator = {
  getRunnerForAdminRequest: (request: SkillSyncRequestLike) => GitHubSkillSyncRunner;
  maybeRunForRequest: (request: SkillSyncRequestLike) => Promise<boolean>;
};
declare function createSkillSyncTriggerOrchestrator(deps: SkillSyncTriggerOrchestratorDeps): SkillSyncTriggerOrchestrator;
//#endregion
//#region src/skills/sync/scheduler.d.ts
declare const SKILL_SYNC_MAX_TIMER_INTERVAL_MINUTES: number;
type MaybePromise$1<T> = T | Promise<T>;
type GitHubSkillSyncScheduler = {
  stop: () => void;
};
declare function startGitHubSkillSyncScheduler(params: {
  getConfig: () => MaybePromise$1<SkillSyncConfig | undefined>;
  runner: GitHubSkillSyncRunner;
}): GitHubSkillSyncScheduler;
//#endregion
//#region src/admin/skills.d.ts
type AdminSkillsRequest = Request & {
  user?: {
    _id?: Types.ObjectId;
    id?: string;
    tenantId?: string;
  };
  skillSyncAllowServerCredentials?: boolean;
  skillSyncCanReadCredentials?: boolean;
};
type SkillSyncCapabilityUser = {
  id: string;
  role: string;
  tenantId?: string;
};
type AdminSkillSyncDeps = {
  runner?: GitHubSkillSyncRunner;
  getRunner?: (req: Request) => GitHubSkillSyncRunner;
  upsertCredential: (input: UpsertSkillSyncCredentialInput) => Promise<SkillSyncCredentialSummary>;
  deleteCredential: (provider: SkillSyncProvider, credentialKey: string) => Promise<{
    deleted: boolean;
  }>;
};
type AdminSkillSyncAccessDeps = {
  getAppConfig: (options: {
    baseOnly: true;
  }) => Promise<{
    skillSync?: unknown;
  } | undefined>;
  hasCapability: (user: SkillSyncCapabilityUser, capability: SystemCapability) => Promise<boolean>;
};
type AdminSkillsSyncHandler = (req: AdminSkillsRequest, res: Response$2) => Promise<Response$2>;
type AdminSkillsSyncHandlers = {
  getSyncStatus: AdminSkillsSyncHandler;
  runSync: AdminSkillsSyncHandler;
  setCredential: AdminSkillsSyncHandler;
  deleteCredential: (req: Request, res: Response$2) => Promise<Response$2>;
};
type AdminSkillsSyncAccess = {
  attachBaseSkillSyncConfig: RequestHandler;
  attachCredentialReadAccess: RequestHandler;
  requireReadSkills: RequestHandler;
  requirePlatformManageSkills: RequestHandler;
  requireSyncRunCapability: RequestHandler;
};
declare function createAdminSkillsSyncAccess(deps: AdminSkillSyncAccessDeps): AdminSkillsSyncAccess;
declare function createAdminSkillsSyncHandlers(deps: AdminSkillSyncDeps): AdminSkillsSyncHandlers;
//#endregion
//#region src/admin/users.d.ts
interface AdminUsersDeps {
  findUsers: (searchCriteria: FilterQuery<IUser>, fieldsToSelect?: string | string[] | null, options?: {
    limit?: number;
    offset?: number;
    sort?: Record<string, 1 | -1>;
  }) => Promise<IUser[]>;
  countUsers: (filter?: FilterQuery<IUser>) => Promise<number>;
  beginAgentTriggerUserDeletion: (userId: string, startedAt: Date) => Promise<"acquired" | "in_progress" | "missing">;
  cancelAgentTriggerUserDeletion: (userId: string, startedAt: Date) => Promise<boolean>;
  drainAgentTriggerDeliveriesForUser: (userId: string) => Promise<void>;
  prepareAgentTriggerUserPurge: (userId: string, fenceStartedAt: Date, tenantId?: string) => Promise<void>;
  cancelAgentTriggerUserPurge: (userId: string, fenceStartedAt: Date) => Promise<boolean>;
  purgeAgentTriggerDeliveriesForUser: (userId: string) => Promise<void>;
  revokeUserCodeEnvironmentWorkers?: (userId: string) => Promise<number>;
  /**
  * Thin data-layer delete — removes the User document only.
  * Full cascade of user-owned resources (conversations, messages, files, tokens, etc.)
  * is handled by `UserController.deleteUserController` in the self-delete flow.
  * This admin endpoint fences durable triggers around the user commit and currently
  * cascades Config and AclEntries.
  * A future iteration should consolidate the full cascade into a shared service function.
  */
  deleteUserById: (userId: string) => Promise<UserDeleteResult>;
  deleteUserCodeEnvironments: (userId: string | Types.ObjectId) => Promise<number>;
  invalidateCodeEnvironmentConfigCache: (tenantId?: string) => Promise<void>;
  deleteConfig: (principalType: PrincipalType, principalId: string | Types.ObjectId) => Promise<IConfig | null>;
  deleteAclEntries: (filter: {
    principalType: PrincipalType;
    principalId: string | Types.ObjectId;
  }) => Promise<void>;
}
declare function createAdminUsersHandlers(deps: AdminUsersDeps): {
  listUsers: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  searchUsers: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  deleteUser: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
};
//#endregion
//#region src/admin/auditLog.d.ts
interface AdminAuditLogDeps {
  listAuditLogPage: (tenantId: string | undefined, filters: AuditLogFilters) => Promise<AuditLogPage>;
  findAuditLogEntry: (tenantId: string | undefined, id: string) => Promise<AdminAuditLogEntry | null>;
  streamAuditLogEntries: (tenantId: string | undefined, filters: Omit<AuditLogFilters, "offset" | "limit" | "cursor">, onEntry: (entry: AdminAuditLogEntry) => void | Promise<void>, options?: {
    isCancelled?: () => boolean;
    maxRows?: number;
  }) => Promise<{
    count: number;
    truncated: boolean;
  }>;
  verifyAuditChain: (tenantId: string | undefined, options?: {
    isCancelled?: () => boolean;
    maxRows?: number;
  }) => Promise<AuditChainVerification>;
}
declare function createAdminAuditLogHandlers(deps: AdminAuditLogDeps): {
  listAuditLog: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  getAuditLogEntry: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  verifyAuditLog: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  exportAuditLogCsv: (req: ServerRequest, res: Response$2) => Promise<Response$2 | void>;
};
//#endregion
//#region src/admin/code.d.ts
type FetchImpl = (input: Parameters<typeof fetch>[0], init?: Parameters<typeof fetch>[1]) => ReturnType<typeof fetch>;
interface AdminCodeEnvironmentDeps {
  getAppConfig: (options: GetAppConfigOptions) => Promise<AppConfig>;
  resolveTenantId?: (req: ServerRequest) => string;
  readSecret?: (name: string) => string | undefined;
  fetchImpl?: FetchImpl;
}
declare function createAdminCodeEnvironmentHandlers(deps: AdminCodeEnvironmentDeps): {
  createPairing: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  revokeWorker: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
};
//#endregion
//#region src/admin/context.d.ts
/** Extracts forensic request context (IP, user agent, correlation id) for an
* audit record. Fields are undefined when the data isn't available. */
declare function buildAuditContext(req: ServerRequest): AuditContext;
//#endregion
//#region src/admin/secrets.d.ts
declare function resolveConfigSecret(value?: string): string | undefined;
/**
* Replaces every value of a registered secret map with a fixed mask, keeping
* the key names so an admin can still see *which* headers a deployment sets
* without receiving the gateway credentials themselves.
*/
/**
* Masks registered secret maps on a cloned config, for callers outside the
* admin read path that also serialize configuration — notably the startup
* "Custom config file loaded" log, which would otherwise copy every literal
* gateway credential into application logs.
*
* Only handles map-valued secrets; scalar secrets keep whatever handling the
* caller already applies.
*/
declare function redactConfigSecretMaps<T>(root: T): T;
//#endregion
//#region src/cdn/azure.d.ts
/**
* Initializes the Azure Blob Service client.
* This function establishes a connection by checking if a connection string is provided.
* If available, the connection string is used; otherwise, Managed Identity (via DefaultAzureCredential) is utilized.
* Note: Container creation (and its public access settings) is handled later in the CRUD functions.
* @returns The initialized client, or null if the required configuration is missing.
*/
declare const initializeAzureBlobService: () => Promise<BlobServiceClient | null>;
/**
* Retrieves the Azure ContainerClient for the given container name.
* @param [containerName=process.env.AZURE_CONTAINER_NAME || 'files'] - The container name.
* @returns The Azure ContainerClient.
*/
declare const getAzureContainerClient: (containerName?: string) => Promise<ContainerClient | null>;
//#endregion
//#region src/cdn/cloudfront.d.ts
interface CloudFrontFullConfig extends NonNullable<CloudFrontConfig> {
  privateKey: string | null;
  keyPairId: string | null;
}
declare function initializeCloudFront(config: CloudFrontConfig): boolean;
declare function getCloudFrontConfig(): Readonly<CloudFrontFullConfig> | null;
//#endregion
//#region src/cdn/cloudfront-cookies.d.ts
declare const CLOUDFRONT_SCOPE_COOKIE = "TerraMind-CloudFront-Scope";
interface CloudFrontCookieScope {
  userId?: string | null;
  tenantId?: string | null;
  storageRegion?: string | null;
  issuedAt?: number | null;
  expiresAt?: number | null;
}
type CloudFrontScopeValue = string | number | {
  toString(): string;
} | null | undefined;
type CloudFrontScopeUser = {
  _id?: CloudFrontScopeValue;
  id?: CloudFrontScopeValue;
  tenantId?: CloudFrontScopeValue;
  orgId?: CloudFrontScopeValue;
  storageRegion?: CloudFrontScopeValue;
};
type CloudFrontCookieRequest = {
  cookies?: Partial<Record<string, string>>;
  user?: CloudFrontScopeUser | null;
};
type CloudFrontAuthCookieRefreshRequest = CloudFrontCookieRequest & {
  cloudFrontAuthCookieRefreshResult?: CloudFrontAuthCookieRefreshResult;
};
type CloudFrontAuthCookieRefreshResult = {
  enabled: boolean;
  attempted: boolean;
  refreshed: boolean;
  reason?: string;
  expiresInSec?: number;
  refreshAfterSec?: number;
};
type CloudFrontCookieRefreshOptions = CloudFrontCookieScope & {
  orgId?: CloudFrontScopeValue;
  force?: boolean;
  refreshWindowSec?: number;
};
declare function getCloudFrontCookieRefreshWindowSec(cookieExpiry?: number): number;
declare function getCloudFrontCookieTiming(): {
  expiresInSec: number;
  refreshAfterSec: number;
  refreshWindowSec: number;
};
declare function parseCloudFrontCookieScope(value: string | null | undefined): CloudFrontCookieScope | null;
declare function resolveCloudFrontCookieScope(req: CloudFrontCookieRequest | null | undefined, user: CloudFrontScopeUser | null | undefined, options?: CloudFrontCookieRefreshOptions): CloudFrontCookieScope;
/**
* Clears CloudFront signed cookies from the response.
* Should be called during logout to revoke CDN access.
*/
declare function clearCloudFrontCookies(res: Response$2, scope?: CloudFrontCookieScope): void;
/**
* Sets CloudFront signed cookies on the response for CDN access.
* Returns true if cookies were set, false if CloudFront cookies are not enabled.
*/
declare function setCloudFrontCookies(res: Response$2, scope?: CloudFrontCookieScope, previousScope?: CloudFrontCookieScope | null): boolean;
declare function maybeRefreshCloudFrontAuthCookies(req: CloudFrontCookieRequest | null | undefined, res: Response$2, user: CloudFrontScopeUser | null | undefined, options?: CloudFrontCookieRefreshOptions): CloudFrontAuthCookieRefreshResult;
declare function forceRefreshCloudFrontAuthCookies(req: CloudFrontCookieRequest | null | undefined, res: Response$2, user: CloudFrontScopeUser | null | undefined, options?: CloudFrontCookieRefreshOptions): CloudFrontAuthCookieRefreshResult;
declare function maybeRefreshCloudFrontAuthCookiesMiddleware(req: CloudFrontAuthCookieRefreshRequest, res: Response$2, next: NextFunction): void;
//#endregion
//#region src/cdn/firebase.d.ts
declare const initializeFirebase: () => FirebaseApp | null;
declare const getFirebaseStorage: () => FirebaseStorage | null;
//#endregion
//#region src/cdn/s3.d.ts
/**
* Initializes and returns an instance of the AWS S3 client.
*
* If AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY are provided, they will be used.
* Otherwise, the AWS SDK's default credentials chain (including IRSA) is used.
*
* If AWS_ENDPOINT_URL is provided, it will be used as the endpoint.
*
* @returns An instance of S3Client if the region is provided; otherwise, null.
*/
declare const initializeS3: () => S3Client | null;
//#endregion
//#region src/code/environments.d.ts
type CodeEnvironmentPrincipalContext = {
  userId: string | Types.ObjectId;
  role?: string | null;
  idOnTheSource?: string | null;
  principals?: ResolvedPrincipal[];
};
type CodeEnvironmentSummary = {
  resourceId: string;
  id: string;
  name: string;
  type: "managed" | "attached";
  canEdit?: boolean;
  canDelete: boolean;
  settings?: CodeEnvironmentUserSettings;
};
type CodeEnvironmentRegistration = {
  id: string;
  name: string;
  type: "managed" | "attached";
  baseURL: string;
  controlPlaneId: string;
  workerId?: string;
  revocationTokenEnv?: string;
  workerPrincipal?: {
    type: "deployment" | "tenant" | "user" | "role" | "group";
    id: string;
  };
};
type AccessibleCodeEnvironmentConfiguration = {
  id: string;
  name: string;
  type: "managed" | "attached";
  baseURL: string;
  controlPlaneId: string;
  owner: "principal";
  workerId?: string;
  settings?: CodeEnvironmentUserSettings;
};
type AccessibleCodeEnvironmentDetails = {
  summaries: CodeEnvironmentSummary[];
  configurations: AccessibleCodeEnvironmentConfiguration[];
};
type CodeEnvironmentLifecycleTarget = CodeEnvironmentSummary & {
  baseURL: string;
  workerId?: string;
  controlPlaneId?: string;
  revocationTokenEnv?: string;
  workerPrincipal?: CodeEnvironmentRegistration["workerPrincipal"];
};
type CodeEnvironmentConfigurationCache = {
  get: (key: string) => Promise<unknown>;
  set: (key: string, value: unknown, ttl?: number) => Promise<unknown>;
};
type CodeEnvironmentRegistryOptions = {
  /** Shared cache only. Omit this dependency when Redis is unavailable so ACL reads stay live. */configurationCache?: CodeEnvironmentConfigurationCache;
};
declare function normalizeCodeEnvironmentName(input: string): string;
declare class CodeEnvironmentValidationError extends Error {
  constructor(message: string);
}
declare class CodeEnvironmentLimitError extends Error {
  constructor();
}
declare function createCodeEnvironmentRegistry(mongoose: typeof import("mongoose"), options?: CodeEnvironmentRegistryOptions): {
  register: (params: {
    actor: CodeEnvironmentPrincipalContext;
    environment: CodeEnvironmentRegistration;
    maxOwned?: number;
  }) => Promise<CodeEnvironmentSummary>;
  listAccessible: (actor: CodeEnvironmentPrincipalContext) => Promise<CodeEnvironmentSummary[]>;
  listAccessibleDetails: (actor: CodeEnvironmentPrincipalContext) => Promise<AccessibleCodeEnvironmentDetails>;
  listAccessibleConfigurations: (actor: CodeEnvironmentPrincipalContext) => Promise<AccessibleCodeEnvironmentConfiguration[]>;
  resolvePrincipals: (actor: CodeEnvironmentPrincipalContext) => Promise<ResolvedPrincipal[]>;
  listRegisteredIds: () => Promise<string[]>;
  invalidateAccessibleConfigurations: (tenantId?: string) => Promise<void>;
  markRevocationPending: (environmentId: string) => Promise<void>;
  remove: (params: {
    actor: CodeEnvironmentPrincipalContext;
    environmentId: string;
    beforeDelete?: (target: CodeEnvironmentLifecycleTarget) => Promise<void>;
  }) => Promise<CodeEnvironmentSummary | null>;
  updateSettings: (params: {
    actor: CodeEnvironmentPrincipalContext;
    environmentId: string;
    settings: CodeEnvironmentUserSettings;
  }) => Promise<CodeEnvironmentSummary | null>;
};
declare class CodeEnvironmentInUseError extends Error {
  readonly environmentId: string;
  constructor(environmentId: string);
}
//#endregion
//#region src/code/decision.d.ts
interface ConversationCodeEnvironmentDecision {
  mode: CodeEnvironmentMode;
  codeWorkspaces?: CodeWorkspaceSelection[];
}
type StoredConversationDecision = Pick<TConversation, "conversationId" | "codeEnvironmentMode" | "codeWorkspaces">;
/** Resolves one immutable conversation choice before attached tools are registered. */
declare function resolveConversationCodeEnvironmentDecision({
  conversationId,
  requestedMode,
  requestedSelections,
  conversation
}: {
  conversationId: string;
  requestedMode?: unknown;
  requestedSelections?: unknown;
  conversation?: StoredConversationDecision | null;
}): ConversationCodeEnvironmentDecision;
interface ConversationCodeEnvironmentMove {
  codeWorkspaces: CodeWorkspaceSelection[];
}
/**
* Validates an owner's explicit move of a sealed attached decision onto the environments its
* agents now use. A move may drop environments the agents stopped using and add ones they now use,
* but never changes the workspace of an environment the decision already covers and never upgrades
* a conversation that continues without an attached environment. `from` must repeat the persisted selections, so a client acting
* on a stale view of the conversation cannot replace a decision it has not seen.
*/
declare function resolveConversationCodeEnvironmentMove({
  conversation,
  from,
  to
}: {
  conversation: StoredConversationDecision;
  from: unknown;
  to: unknown;
}): ConversationCodeEnvironmentMove;
type PersistableDecisionFields = Pick<StoredConversationDecision, "codeEnvironmentMode" | "codeWorkspaces">;
/**
* Returns the decision fields a run may persist. A stored conversation keeps the decision it
* already holds, because only its owner's explicit move replaces one: a run from any ingress that
* settles after a move would otherwise write its run-start decision back over it. A legacy row
* records the mode it inferred without touching the selections it already stores. A caller that
* never resolved a decision falls back to the fields its request carried, under the same rule.
*/
declare function resolvePersistableCodeEnvironmentDecision({
  conversationId,
  decision,
  conversation,
  requested
}: {
  conversationId: string;
  decision?: ConversationCodeEnvironmentDecision | null;
  conversation?: StoredConversationDecision | null;
  requested?: PersistableDecisionFields | null;
}): PersistableDecisionFields;
//#endregion
//#region src/code/bridge.d.ts
type CodeBridgePrincipalType = "deployment" | "tenant" | "user" | "role" | "group";
type CodeBridgeWorkerBinding = {
  tenantId: string;
  principal: {
    type: CodeBridgePrincipalType;
    id: string;
  };
};
type CodeBridgePairing = {
  protocolVersion: 1;
  workerId: string;
  code: string;
  expiresAt: string;
};
type CodeBridgeWorkerStatus = {
  status: "offline" | "starting" | "ready";
  leaseExpiresInMs?: number;
  statefulWorkspace?: boolean;
  sandboxProfile?: string;
  runtimes?: string[];
  operations?: CodeWorkspaceOperation[];
  workspaces?: CodeWorkspaceDescriptor[];
};
type CodeBridgeFetch = (input: Parameters<typeof fetch>[0], init?: Parameters<typeof fetch>[1]) => ReturnType<typeof fetch>;
declare class CodeBridgePairingError extends Error {
  readonly reason: "rejected" | "invalid" | "timeout" | "failed";
  readonly upstreamStatus?: number | undefined;
  constructor(reason: "rejected" | "invalid" | "timeout" | "failed", upstreamStatus?: number | undefined);
}
declare class CodeBridgeLifecycleError extends Error {
  readonly reason: "rejected" | "invalid" | "timeout" | "failed";
  readonly upstreamStatus?: number | undefined;
  constructor(reason: "rejected" | "invalid" | "timeout" | "failed", upstreamStatus?: number | undefined);
}
declare class CodeBridgeStatusError extends Error {
  readonly reason: "rejected" | "invalid" | "timeout" | "failed" | "busy";
  readonly upstreamStatus?: number | undefined;
  constructor(reason: "rejected" | "invalid" | "timeout" | "failed" | "busy", upstreamStatus?: number | undefined);
}
declare function createCodeBridgeStatusPoller({
  fetchImpl,
  maxConcurrent,
  maxEntries,
  cacheTtlMs
}?: {
  fetchImpl?: CodeBridgeFetch;
  maxConcurrent?: number;
  maxEntries?: number;
  cacheTtlMs?: number;
}): (params: {
  baseURL: string;
  token: string;
  workerId: string;
}) => Promise<CodeBridgeWorkerStatus>;
declare function getCodeBridgeWorkerStatus({
  baseURL,
  token,
  workerId,
  fetchImpl
}: {
  baseURL: string;
  token: string;
  workerId: string;
  fetchImpl?: CodeBridgeFetch;
}): Promise<CodeBridgeWorkerStatus>;
declare function createCodeBridgePairing({
  baseURL,
  token,
  workerId,
  binding,
  fetchImpl
}: {
  baseURL: string;
  token: string;
  workerId: string;
  binding?: CodeBridgeWorkerBinding;
  fetchImpl?: CodeBridgeFetch;
}): Promise<CodeBridgePairing>;
declare function revokeCodeBridgeWorker({
  baseURL,
  token,
  workerId,
  fetchImpl
}: {
  baseURL: string;
  token: string;
  workerId: string;
  fetchImpl?: CodeBridgeFetch;
}): Promise<void>;
declare function readCodeBridgeSecret(name: string): string | undefined;
//#endregion
//#region src/stream/SteerRecovery.d.ts
/** Immutable user-visible payload a parked steer recovery is allowed to submit. */
interface RecoveredSteerPayload {
  text: string;
  /** Sorted, unique file ids. Display metadata is deliberately excluded: the
  * normal send path re-resolves files by owner and only identity is binding. */
  fileIds: string[];
  /** Normalized quoted excerpts, order-significant. Model-bound exactly like
  * the text, so the proof must bind them too: a stale client presenting the
  * same recoverySteerId with altered or missing quotes must not consume the
  * parked source. Empty when the source carried none. */
  quotes: string[];
}
/** A recovery-shaped request did not reproduce the parked source exactly. */
declare class RecoveredSteerPayloadMismatchError extends Error {
  readonly code = "RECOVERY_PAYLOAD_MISMATCH";
  constructor();
}
/** Canonical attachment identity: every entry must name a file; ordering,
* duplicates, and mutable display metadata do not affect identity. */
declare function canonicalRecoveryFileIds(files: unknown): string[] | null;
/** Build the trusted payload proof passed from the HTTP controller into the
* atomic job-creation transaction. `null` means the recovery request itself
* is malformed and must be rejected before claiming a generation. */
declare function buildRecoveredSteerPayload(text: unknown, files: unknown, quotes?: unknown): RecoveredSteerPayload | null;
//#endregion
//#region src/stream/interfaces/IJobStore.d.ts
/**
* Detached Event Actor execution guarantee advertised by a generation store.
*
* `process_local` keeps the lifecycle coherent while this process is alive.
* `distributed` additionally permits restart recovery and replica handoff.
* Absence means the store cannot host detached Event Actor actions.
*/
type DetachedAgentEventActionStoreMode = "process_local" | "distributed";
/**
* Job status enum.
*
* `requires_action` is non-terminal: the run has paused for human review
* (e.g. tool approval) and is expected to be resumed by an approval route.
* Stores must NOT cleanup `requires_action` jobs as if they were complete.
*/
type JobStatus = "running" | "complete" | "error" | "aborted" | "requires_action";
/** Immutable wire/storage contract selected when a generation is created.
* Missing markers on pre-rollout records are interpreted as protocol v1. */
type GenerationProtocolVersion = 1 | 2;
/**
* Serializable job data - no object references, suitable for Redis/external storage
*/
interface SerializableJobData {
  streamId: string;
  userId: string;
  tenantId?: string;
  status: JobStatus;
  createdAt: number;
  generationProtocolVersion?: GenerationProtocolVersion;
  /** Saver-level checkpoint scope for this exact generation. New jobs use a
  * globally unique opaque identity; LangGraph still sees an empty root
  * `checkpoint_ns`, which the saver adapter maps to this storage scope.
  * Legacy paused jobs omit it and use the historical unscoped storage. */
  checkpointNamespace?: string;
  completedAt?: number;
  conversationId?: string;
  error?: string;
  /** Durable, non-sensitive identity and one-shot outcome for an early replay
  * buffer overflow. This lets another replica account for recovery. */
  earlyBufferOverflow?: EarlyBufferOverflowState;
  /** Generation-level first subscriber claim shared across replicas. */
  firstSubscriberAttachedAt?: number;
  /** Expiring local subscriber-group leases (in-memory store only). */
  activeSubscriberLeases?: Record<string, number>;
  /** Generation-wide durable chunk frontier maintained by the store. */
  durableEventCount?: number;
  /** Stable identity of the HTTP submission that created this generation.
  * Internal-only: lets an expired idempotency lease recognize the same live
  * job instead of replacing and billing it again. */
  idempotencyClientRequestId?: string;
  /** Parked steer leased into this ordinary recovery turn. The source text is
  * hidden while this exact generation is active and consumed only after the
  * user message is durably persisted. */
  recoveredSteerId?: string;
  /** User message metadata */
  userMessage?: {
    messageId: string;
    parentMessageId?: string;
    conversationId?: string;
    text?: string;
    /** Quoted excerpts referenced on this turn, carried so resumable/aborted
    *  reconstructions of the user message keep their `MessageQuotes`. */
    quotes?: string[]; /** Skill selections, carried so a HITL-resumed turn's requestMessage keeps its pills. */
    manualSkills?: string[];
    alwaysAppliedSkills?: string[];
    /** Uploaded files for the turn, carried so a HITL resume sources them from the job
    *  rather than a user DB row whose save can still be racing the approval prompt. */
    files?: unknown[];
  };
  /** Response message ID for reconnection */
  responseMessageId?: string;
  /** Whether this generation replaces an existing assistant branch. */
  isRegenerate?: boolean;
  /** Exact normalized MCP placeholder identity for this turn. */
  mcpRequestBody?: MCPRuntimeRequestBody;
  /** Exact assistant-message fields authored by the user during this running job. */
  userSubmittedPaths?: string[];
  /** Exact request-only message fields embedded at caller-authored paths. */
  userSubmittedMessageFieldPaths?: UserSubmittedMessageFieldPath[];
  /**
  * Whether this run has activity labels enabled (per-endpoint
  * `activityLabel: true`). Set once at run start so the resume path can
  * decide whether to reconcile label gaps WITHOUT reading content — the
  * first label of a run can be claimed inside the snapshot->subscribe
  * window, so the snapshot itself is not a reliable signal.
  */
  activityLabels?: boolean;
  /**
  * Deferred-tool names discovered (via `tool_search`) before a HITL pause, captured
  * so a resume can replay them into `createRun` — the rebuilt graph uses `messages: []`
  * (state comes from the checkpoint), so without these the rebuilt model would lose
  * the discovered tool schemas.
  */
  discoveredTools?: string[];
  /** Bounded collector state for continuing a phase across HITL resume. */
  activityPhaseSnapshot?: ActivityPhaseSnapshot;
  /** Exact bounded compaction guidance captured atomically with a HITL pause. */
  compactionSemanticIndex?: ICompactionSemanticIndexProjection;
  /** Calibration and fading state captured atomically with a HITL pause, so a resume seeds its rebuilt pruner from the same tier. */
  contextMeta?: IAgentEventActorContextMeta;
  /**
  * Whether the replica that OWNS this generation can seal mid-stream
  * (`PreemptBoundary` wiring). Recorded at createJob because the steer route
  * may land on a different replica — during a rolling deploy its own SDK
  * probe would answer for the wrong process. Absent on jobs created before
  * preempt shipped, which reads as incapable: the honest outcome.
  */
  preemptCapable?: boolean;
  /**
  * Transient owner assertion that this replica's drain merges
  * `SteerQueueItem.quotes` into the injected turn. Never stored as-is:
  * createJob and `ApprovalLifecycle.resolve` translate it into
  * `steerQuotesExecutionId` bound to the asserting owner's execution.
  */
  steerQuotesCapable?: boolean;
  /**
  * The `providerExecutionId` of the owner that asserted quote capability.
  * Valid only while it equals the LIVE `providerExecutionId`: a legacy
  * replica winning a HITL resume rewrites the execution id but cannot know
  * this field, so its stale assertion self-invalidates — which a bare
  * boolean could not do (an old resume patch omits rather than clears it).
  * The fenced enqueue evaluates the equality atomically and strips
  * `item.quotes` on mismatch, keeping the persisted item and the
  * `quotesAccepted` echo honest; the client re-stages dropped excerpts.
  */
  steerQuotesExecutionId?: string;
  /** Explicitly false until the provider-owning replica has installed its
  * generation-fenced abort subscription. Missing is conservative legacy
  * evidence and must be treated like true by replacement handoff. */
  providerAbortReady?: boolean;
  /** Opaque identity of the currently executing provider segment. A HITL resume
  * replaces it so an earlier paused segment cannot acknowledge the new run. */
  providerExecutionId?: string;
  /** Durable evidence that the current provider owner crossed its start CAS.
  * Unlike `providerDrained`, this identity survives terminal drain so host
  * compensation can distinguish a projected-but-never-started resume. */
  providerExecutionStartedId?: string;
  /** False while the identified provider segment can still mutate user data;
  * true before provider startup and after the owner has fully unwound. */
  providerDrained?: boolean;
  /** Whether the user-message created event has been emitted */
  createdEventEmitted?: boolean;
  /** Sender name for UI display */
  sender?: string;
  /** Whether sync has been sent to a client */
  syncSent: boolean;
  /** Trusted schedule identity copied atomically into the generation job. */
  scheduleId?: string;
  scheduledFor?: string;
  scheduleConfigRevision?: number;
  scheduleManual?: boolean;
  /** Terminal outcome evidence retained when the schedule row could not be updated. */
  scheduleOutcome?: "success" | "error" | "interrupted" | "skipped_balance";
  scheduleOutcomeError?: string;
  preserveForScheduleReconcile?: boolean;
  /**
  * A terminal transition (currently approval expiry) still owes a durable host
  * lifecycle hook. Set atomically with that transition and cleared only once the host
  * adapter acknowledges success, so the job is retained (not reaped) and enumerable by
  * cleanup across restarts and replicas until the hook completes. Generic: a host with
  * no action clears it immediately on its no-op success, so nothing accumulates.
  */
  terminalHostActionPending?: boolean;
  /** Redis-only durable marker for a detached Event Actor completion hook.
  * Capable stores expose it through `terminalHostActionPending` as well, but
  * keep the persisted field distinct so legacy reconciliation cannot index or
  * claim the completion through the ordinary terminal-action lane. */
  detachedAgentEventTerminalHostActionPending?: boolean;
  /** Logical terminal state hidden behind a versioned fail-closed shell while
  * a detached Event Actor host action remains unacknowledged. */
  detachedAgentEventTerminalStatus?: Extract<JobStatus, "complete" | "aborted" | "error">;
  /**
  * Last time a cleanup pass enumerated this pending host action for retry. Retention is
  * measured from this rather than `completedAt`, so evidence survives as long as some
  * replica is still actively retrying the hook (e.g. Mongo unreachable for days), while a
  * deployment that stops retrying entirely still lets it age out instead of leaking.
  */
  terminalHostActionRefreshedAt?: number;
  /** Serialized final event for replay */
  finalEvent?: string;
  /** Abort won its terminal CAS but the route is still saving the partial
  * response / pruning HITL state. No normal terminal payload may be exposed
  * while true. */
  terminalPersistencePending?: boolean;
  /** Crash-recovery deadline basis for `terminalPersistencePending`. */
  terminalPersistenceStartedAt?: number;
  /** Serialized title event for replay during active-stream resume */
  titleEvent?: string;
  /** Serialized replay-only stream events for active-stream resume */
  replayEvents?: string;
  /** Serialized latest context usage snapshot for active-stream resume */
  contextUsage?: string;
  /** Serialized token usage events for active-stream resume (cross-replica safe) */
  tokenUsage?: string;
  /** Endpoint metadata for abort handling - avoids storing functions */
  endpoint?: string;
  iconURL?: string;
  model?: string;
  promptTokens?: number;
  /**
  * Agent that initiated the run. Persisted so a HITL resume can verify it rebuilds
  * the SAME agent that paused — resuming Agent A's checkpoint on Agent B's graph
  * would mis-execute the paused tool calls.
  */
  agent_id?: string;
  /**
  * Whether the originating turn was a temporary (non-persisted) chat. Persisted so
  * a HITL resume keeps the resumed response temporary instead of saving it — the
  * resume request can't be trusted to re-send the flag.
  */
  isTemporary?: boolean;
  /** Original server-authenticated retention deadline, serialized across replicas. */
  retentionExpiresAt?: string;
  agentEventDeliveryKey?: string;
  /** Original actor invocation when an internal completion delivery owns this generation. */
  agentEventInvocationKey?: string;
  /** Original actor invocation generation retained across completion HITL resumes. */
  agentEventInvocationGenerationCreatedAt?: number;
  /** This generation must resume on a durable detached-action producer. */
  agentEventDetachedActionProducerRequired?: boolean;
  /** Durable retry payload captured before detached terminal evidence is written to Mongo. */
  agentEventDetachedTerminalEvidence?: AgentEventDetachedTerminalEvidence;
  /** Trusted actor binding copied from the authenticated delivery envelope. */
  agentEventBindingId?: string;
  agentEventExpectedAction?: AgentTriggerExpectedAction;
  /** Versioned pointer to the canonical signed Conversation suspension. */
  agentEventSuspension?: AgentEventSuspensionProjection;
  /** Exact durable legacy-turn fence carried across a HITL pause/resume. */
  agentEventLegacyTurnToken?: string;
  /**
  * Set when status is `requires_action`. Describes the human review the
  * run is waiting on. Cleared by the resume path before the job returns to `running`.
  */
  pendingAction?: Agents.PendingAction;
  /** Durable bridge between the resume claim and content reconstruction. An
  * abort can win while the resume controller is still rebuilding the client;
  * retaining the accepted answer here lets that terminal owner stamp it onto
  * the persisted partial response instead of losing it with the request. */
  resolvedAskUserQuestions?: ResolvedAskUserQuestion[];
  /**
  * Flat mirror of `pendingAction.actionId`, kept as a top-level field so an
  * atomic status transition can guard on it (a nested JSON field can't be
  * compared inside a Redis Lua CAS). Lets `resolve`/`expire` reject a stale
  * decision that targets a different action than the one currently pending.
  */
  pendingActionId?: string;
  /**
  * Liveness basis for the stale-running failsafe, refreshed when a paused job
  * is resumed. Without it, cleanup keys off `createdAt`, so an approval that
  * sat in `requires_action` past the running window would be reaped on the
  * next tick right after resuming. Falls back to `createdAt` when unset.
  */
  lastActiveAt?: number;
  /**
  * Flat flag set by the terminal close-and-drain (Redis: raw hash field the
  * enqueue Lua guards on; in-memory: a parallel set). Once set, new steers
  * are rejected until `createJob` reuses the stream id. Never written through
  * `updateJob` — listed here so cleanup paths can reference the key name.
  */
  steersClosed?: boolean;
}
/** Exact active hash replaced by one atomic job creation. Built-in stores keep
* this as non-enumerable transaction metadata; Redis also retains a private
* receipt in the replacement hash so a committed create with a lost reply can
* reconstruct it without exposing it through normal job serialization. */
type ReplacedGeneration = Pick<SerializableJobData, "createdAt" | "status" | "conversationId" | "providerAbortReady" | "providerExecutionId" | "providerDrained">;
/** Latest generation epoch checked by a conditional create. A retained epoch
* can outlive its job hash, so inactive mismatches intentionally omit job-only
* metadata while still telling the caller which generation won the race.
* When all predecessor evidence has expired, `createdAt` safely echoes the
* caller's finite expected epoch and `verified` is false; the create is still
* rejected, but response consumers can preserve the queued turn without
* mistaking that fallback for an observed generation. */
type GenerationPredecessorState = Pick<SerializableJobData, "createdAt"> & Partial<Pick<SerializableJobData, "status" | "conversationId">> & {
  active: boolean;
  /** False only when neither the job nor its retained epoch was observable.
  * Missing values are compatible with pre-marker mismatch producers. */
  verified?: boolean;
};
interface CreatedJobData extends SerializableJobData {
  /** The predecessor observed inside the same transaction that installed this
  * job. Non-enumerable in the built-in stores to keep it out of serializers. */
  replacedJob?: ReplacedGeneration;
  /** Transitive transaction receipts inherited from replacements whose
  * managers may have lost their create replies. Ordered oldest to newest. */
  replacedJobs?: readonly ReplacedGeneration[];
  /** Opaque manager-minted proof for one create invocation. Non-enumerable in
  * built-in stores and used only to reconcile a commit whose reply was lost. */
  creationAttemptId?: string;
}
/** A conditional create observed a different current generation than the
* caller's last authoritative status read. The store rejects before replacing
* that generation, so queued client work can be restored safely. */
declare class JobPredecessorMismatchError extends Error {
  readonly currentJob: GenerationPredecessorState;
  readonly code = "GENERATION_PREDECESSOR_MISMATCH";
  constructor(currentJob: GenerationPredecessorState);
}
type JobMetadataPatch = Partial<Pick<SerializableJobData, "responseMessageId" | "isRegenerate" | "mcpRequestBody" | "userSubmittedPaths" | "userSubmittedMessageFieldPaths" | "sender" | "conversationId" | "userMessage" | "endpoint" | "iconURL" | "model" | "agent_id" | "isTemporary" | "retentionExpiresAt" | "agentEventDeliveryKey" | "agentEventInvocationKey" | "agentEventInvocationGenerationCreatedAt" | "agentEventDetachedActionProducerRequired" | "agentEventDetachedTerminalEvidence" | "agentEventBindingId" | "agentEventExpectedAction" | "agentEventSuspension" | "agentEventLegacyTurnToken" | "scheduleId" | "scheduledFor" | "scheduleConfigRevision" | "scheduleManual" | "scheduleOutcome" | "scheduleOutcomeError" | "preserveForScheduleReconcile" | "promptTokens" | "discoveredTools" | "activityPhaseSnapshot" | "compactionSemanticIndex" | "contextMeta" | "preemptCapable" | "steerQuotesCapable" | "steerQuotesExecutionId" | "providerExecutionId" | "providerDrained" | "generationProtocolVersion" | "resolvedAskUserQuestions">>;
/**
* Whether a job's pending review has passed its `expiresAt`. Shared by the
* stores so an expired approval is kept out of active-job listings (the client
* stops polling; cleanup/expiry finalizes it).
*/
declare function isPendingActionExpired(job: Pick<SerializableJobData, "pendingAction">): boolean;
/**
* Whether a `requires_action` job has no live, resolvable prompt — either the
* pendingAction is missing/malformed (e.g. dropped on deserialize) or past its
* `expiresAt`. Such a job can't be rendered or resolved, so it must be kept out
* of active listings and finalized by cleanup rather than left stuck active.
*/
declare function isPendingActionStale(job: Pick<SerializableJobData, "pendingAction">): boolean;
/**
* A user steering message queued for mid-run injection. Enqueued by the steer
* route on any instance; drained FIFO by the owning process's run-scoped
* PostToolBatch hook at the next tool-batch boundary.
*/
interface SteerQueueItem {
  steerId: string;
  /** Client-generated correlation id. It lets a terminal event that beats the
  *  202 ACK match the server item to its optimistic local chip. */
  clientSteerId?: string;
  text: string;
  userId: string;
  createdAt: number;
  /** Attachment refs steered with the message. Display metadata only — the
  *  drain re-fetches each file by id scoped to the run's user and encodes
  *  fresh, so nothing here is trusted beyond identifying the file. */
  files?: Partial<TFile>[];
  /** Quoted excerpts steered with the message, normalized at admission
  *  (`getReferencedQuotes`). Kept separate from `text` so the persisted
  *  steer part stays clean; merged into the model-bound turn at injection. */
  quotes?: string[];
  /** The steer asked to seal the live model stream at the next provider-safe
  *  boundary instead of waiting for a tool step. Durable so a parked,
  *  claimed, or replayed chip keeps its "interrupting" label. */
  preempt?: boolean;
  /** Monotonic per-steer arm revision. A capability downgrade increments it
  * so an older cross-replica arm cannot resurrect after the disarm, while a
  * later explicit arm with a newer revision remains valid. */
  preemptRevision?: number;
}
/** Durable acknowledgement keyed by `clientSteerId`. It outlives the queue
* and live job for a bounded window so a lost 202 cannot make Retry inject
* the same instruction twice after drain, terminal cleanup, or replacement. */
interface SteerReceipt {
  clientSteerId: string;
  /** Quote-INDEPENDENT content hash (text/files/preempt) — the one shape every
  * replica version computes, so lost-ACK retries replay across a rolling
  * deploy in both directions. */
  fingerprint: string;
  /** Identity of the REQUESTED quotes (pre any owner-capability strip),
  * recorded beside the fingerprint so quote-aware readers enforce quote
  * identity without making the fingerprint unreadable to legacy admission.
  * Absent on receipts written by pre-quotes replicas or for quote-less
  * requests. */
  requestedQuotesFingerprint?: string;
  userId: string;
  tenantId?: string;
  agentId?: string;
  endpoint?: string;
  /** Generation epoch that accepted the item. Receipts intentionally survive
  * replacement, so queue absence is only "delivered" while this exact
  * generation remains active; a newer epoch means the predecessor dropped
  * an undrained item and the client must recover it as a leftover. */
  generationCreatedAt: number;
  item: SteerQueueItem;
  position: number;
  /** `claimed` is the crash-recoverable window between an atomic queue drain
  * and the durable applied-content record. It is not settled: a live client
  * keeps the chip pending, and a dead/replaced generation repairs it to a
  * leftover instead of silently dropping the instruction. */
  /** `recovered` means one recovery surface already claimed a leftover. A
  * later retry is settled without re-queuing the same follow-up. */
  state: "queued" | "claimed" | "delivered" | "leftover" | "recovered" | "cancelled";
}
type SteerReceiptInput = Omit<SteerReceipt, "item" | "position" | "state">;
type SteerEnqueueReceiptResult = SteerReceipt | SteerEnqueueResult | number;
/** Capability-normalized atomic enqueue result for callers without a stable
* client receipt id (legacy/API compatibility path). */
interface SteerEnqueueResult {
  item: SteerQueueItem;
  position: number;
}
type TerminalSteerAdmissionResult = {
  outcome: "claimed";
  items: SteerQueueItem[];
} | {
  outcome: "open" | "sealed" | "unavailable";
};
interface TerminalSteerAdmissionPolicy {
  allowClaim: boolean;
  keepOpenWhenEmpty: boolean;
}
type SteerEnqueueVersionedResult = SteerEnqueueResult | number;
/**
* Cross-replica preempt signal. Unlike abort this does NOT stop the run — it
* asks the generating replica to seal its current model stream at the next
* provider-safe boundary so the queued steer can inject there. Fenced by
* `createdAt`: a stale publish must never arm a replacement job on the same
* streamId.
*/
interface PreemptMessage {
  op: "arm" | "clear";
  /** Generation identity (`SerializableJobData.createdAt`) this belongs to. */
  createdAt: number;
  steerIds: string[];
  /** Per-id arm revision. Omitted by legacy senders (treated as revision 0). */
  revisions?: Record<string, number>;
}
/** {@link IJobStoreV2.armSteer}: `armed` flipped the flag in place; `missing`
*  covers every unavailable interleaving (paused, drained, cancelled, closed,
*  replaced generation); `incapable` means the live owner cannot seal. */
type SteerArmOutcome = "armed" | "missing" | "incapable";
interface SteerArmResult {
  outcome: SteerArmOutcome;
  /** Present only for an armed item. */
  revision?: number;
  item?: SteerQueueItem;
}
/** Maximum steers a single run can have queued at once. */
declare const STEER_QUEUE_MAX_DEPTH = 10;
/** `enqueueSteer` rejection: the job is missing or not `running`. */
declare const STEER_ENQUEUE_NOT_RUNNING = -1;
/** `enqueueSteer` rejection: the queue is at {@link STEER_QUEUE_MAX_DEPTH}. */
declare const STEER_ENQUEUE_QUEUE_FULL = -2;
/** `enqueueSteerWithReceipt` rejection: the bounded, unexpired receipt
* history is full. Existing ids still replay; only a new identity is refused
* so idempotency evidence is never evicted inside its recovery window. */
declare const STEER_ENQUEUE_RECEIPT_FULL = -3;
/**
* Arguments for an atomic {@link IJobStore.transitionStatus} compare-and-set.
*/
interface JobStatusTransition {
  /** Only fire the transition if the job is currently in this status. */
  from: JobStatus;
  /** Status to move to when the `from` guard holds. */
  to: JobStatus;
  /** Fields written in the same atomic step as the status change. */
  patch?: Partial<SerializableJobData>;
  /** Field names removed in the same atomic step (e.g. `pendingAction`). */
  clear?: Array<keyof SerializableJobData & string>;
  /**
  * Additional guard: only fire if the job's `pendingActionId` equals this.
  * Checked atomically alongside the `from` status so a stale decision can't
  * resolve a job that has since paused for a different action.
  */
  expectActionId?: string;
  /**
  * Additional guard: only fire if the job's creation epoch equals this value.
  * Prevents a stale owner from transitioning a replacement job that reuses
  * the same stream ID.
  */
  expectCreatedAt?: number;
  /**
  * Additional guard: reject the transition when the store's clock has reached
  * this absolute deadline. The comparison is part of the same atomic operation
  * as the status change, so queueing or storage latency cannot publish stale state.
  */
  notAfterMs?: number;
  /** Extend all current steer receipts in the SAME atomic step as this
  * transition. Used by running→requires_action so no enqueue can land between
  * a pre-pause TTL pass and the status CAS. */
  steerReceiptTtlSeconds?: number;
}
/** Value stored under an idempotency claim: the stream a retried request should attach to. */
interface IdempotencyClaimValue {
  streamId: string;
  conversationId: string;
  /** Wire/storage protocol selected by the request that first won this claim.
  * Missing values are legacy v1. Keeping the marker in both idempotency
  * tombstones lets a retry recover the original contract after its job hash
  * has already been cleaned up. */
  generationProtocolVersion?: GenerationProtocolVersion;
  /** Epoch ms the claim was written — lets a losing duplicate tell a winner that is still
  *  starting (recent, no job yet → retry) from one that already finished and was cleaned
  *  up (old, no job → attach and let the client refetch). */
  claimedAt?: number;
  /** Lease owner verified atomically by createJob; stale-request takeover
  * changes it so the abandoned winner can no longer create a generation. */
  claimToken?: string;
  /** Proof that a takeover token was derived by CAS from this predecessor.
  * If a connection drops after the primary CAS but before the legacy CAS,
  * the next replica can finish only that exact, same-coordinate split rather
  * than treating an arbitrary token mismatch as repairable. */
  previousClaimToken?: string;
  /** Written atomically with job creation. Once present, a missing job is an
  * already-started/cleaned generation and can never be taken over as an
  * abandoned pre-create lease. */
  startedAt?: number;
}
/** Result of an atomic {@link IJobStore.claimIdempotencyKey} attempt. */
interface IdempotencyClaimResult {
  /** True when this caller won the claim and should create the job. */
  claimed: boolean;
  /** When `claimed` is false, the stream the original request is already driving. */
  existing?: IdempotencyClaimValue;
  /** The durable namespace that established the result. Manager-level claims
  * use `legacy` only for an old, tokenless claim that must remain
  * duplicate-only during a rolling upgrade. Store implementations may omit
  * this field because they operate on one physical key at a time. */
  source?: "primary" | "legacy";
}
/** Owner-authorized parked recovery payload plus the protocol that controls
* its delivery semantics. V1 is destructive (legacy GETDEL); v2 is leased. */
interface ParkedSteerClaim {
  payload: string;
  generationProtocolVersion: GenerationProtocolVersion;
}
/**
* Usage metadata for token spending across different LLM providers.
*
* This interface supports two mutually exclusive cache token formats:
*
* **OpenAI format** (GPT-4, o1, etc.):
* - Uses `input_token_details.cache_creation` and `input_token_details.cache_read`
* - Cache tokens are nested under the `input_token_details` object
*
* **Anthropic format** (Claude models):
* - Uses `cache_creation_input_tokens` and `cache_read_input_tokens`
* - Cache tokens are top-level properties
*
* When processing usage data, check both formats:
* ```typescript
* const cacheCreation = usage.input_token_details?.cache_creation
*   || usage.cache_creation_input_tokens || 0;
* ```
*/
interface UsageMetadata {
  /** Logical usage bucket for accounting/reporting. Defaults to model response usage. */
  usage_type?: "message" | "summarization" | "subagent" | "sequential";
  /** Total input tokens (prompt tokens) */
  input_tokens?: number;
  /** Total output tokens (completion tokens) */
  output_tokens?: number;
  /** Total billed tokens when provided by the model/runtime */
  total_tokens?: number;
  /** Model identifier that generated this usage */
  model?: string;
  /** Provider identifier that generated this usage */
  provider?: string;
  /** Agent that produced this usage (graph agent id / subagent agent id). Lets
  *  multi-endpoint graphs price each call with its own endpoint token config. */
  agentId?: string;
  /** Authoritative display cost attached by the host before durable child persistence. */
  cost?: number;
  /**
  * OpenAI-style cache token details.
  * Present for OpenAI models (GPT-4, o1, etc.)
  */
  input_token_details?: {
    /** Tokens written to cache */cache_creation?: number; /** Tokens read from cache */
    cache_read?: number; /** OpenAI GPT-5.6+ cache-write tokens (billed above the input rate) */
    cache_write_tokens?: number;
  };
  /**
  * Anthropic-style cache creation tokens.
  * Present for Claude models. Mutually exclusive with input_token_details.
  */
  cache_creation_input_tokens?: number;
  /**
  * OpenAI GPT-5.6+ cache-write tokens, reported at the top level of
  * `prompt_tokens_details`/`input_tokens_details`. Distinct from cached
  * (read) tokens and billed at a premium over the input rate.
  */
  cache_write_tokens?: number;
  /**
  * Anthropic-style cache read tokens.
  * Present for Claude models. Mutually exclusive with input_token_details.
  */
  cache_read_input_tokens?: number;
  /**
  * Breakdown of output token counts. Per the LangChain core contract,
  * `output_tokens` is the sum of all output token types — these fields
  * are subsets of `output_tokens`, *not* additional charges.
  */
  output_token_details?: {
    /** Reasoning/thinking tokens generated as chain-of-thought (o1, Gemini thinking, etc.) */reasoning?: number; /** Alternate provider/runtime alias for reasoning tokens. */
    reasoning_tokens?: number;
    audio?: number;
  };
}
/**
* Result returned from aborting a job - contains all data needed
* for token spending and message saving without storing callbacks
*/
interface AbortResult {
  /** Whether the abort was successful */
  success: boolean;
  /** Why the abort did not land. EVERY `success: false` return carries one, so a
  * caller can separate a generation it must not settle (`generation_replaced`,
  * `job_not_found`) from one that is still live (`job_still_active`) and from one
  * that had already reached a terminal state (`already_settled` — the provider has
  * also drained when `awaitProviderDrain` was requested). The ABSENCE of this field
  * is not a stop confirmation; use `isStopConfirmed`. */
  failureReason?: "generation_replaced" | "job_still_active" | "job_not_found" | "already_settled";
  /** The generation was stopped, but the caller's required durable side
  * effects failed before normal FINAL publication. The manager emitted a
  * conservative reconciliation frame instead. */
  persistenceFailed?: boolean;
  /** The job data at time of abort */
  jobData: SerializableJobData | null;
  /** Aggregated content from the stream */
  content: Agents.MessageContentComplex[];
  /** Final event to send to client */
  finalEvent: unknown;
  /** Concatenated text from all content parts for token counting fallback */
  text: string;
  /** Collected usage metadata from all models for token spending */
  collectedUsage: UsageMetadata[];
  /** Steers drained at abort time (never injected); surfaced to the client for restore */
  pendingSteers?: TPendingSteer[];
}
/**
* Canonical "did this generation actually stop?" predicate — one definition shared by
* every caller that settles durable state on the answer (schedule outcomes, checkpoint
* pruning, capacity release).
*
* A landed abort confirms the stop. So does `already_settled`: the generation reached a
* terminal state on its own and, when the caller asked for `awaitProviderDrain`, its
* provider segment has drained, so nothing can still write. Every OTHER failure leaves a
* generation that is either still live (`job_still_active`), owned by someone else
* (`generation_replaced`), or unobservable from here without a drain (`job_not_found`) —
* none of which may be settled on.
*/
declare function isStopConfirmed(result: AbortResult | null | undefined): boolean;
/**
* Backward-compatible public job-store contract.
*
* This is the pre-v2 extension surface kept for third-party stores compiled
* against earlier `@librechat/api` releases. The generation manager validates
* the additional {@link IJobStoreV2} capabilities before accepting a custom
* store at runtime.
*/
interface IJobStore {
  readonly detachedAgentEventActionStoreMode?: DetachedAgentEventActionStoreMode;
  initialize(): Promise<void>;
  createJob(streamId: string, userId: string, conversationId?: string, tenantId?: string, initialMetadata?: JobMetadataPatch): Promise<SerializableJobData>;
  getJob(streamId: string): Promise<SerializableJobData | null>;
  updateJob(streamId: string, updates: Partial<SerializableJobData>, expectedCreatedAt?: number): Promise<void>;
  transitionStatus(streamId: string, args: JobStatusTransition): Promise<boolean>;
  claimIdempotencyKey(key: string, value: IdempotencyClaimValue, ttlSeconds: number): Promise<IdempotencyClaimResult>;
  releaseIdempotencyKey(key: string): Promise<void>;
  /** Read-only existence probe used to identify a confirmed retry before
  * request-rate admission. Optional stores keep the conservative behavior
  * where every request remains subject to the limiter. */
  hasIdempotencyKey?(key: string): Promise<boolean>;
  /** Read-only claim receipt used by durable source reconcilers. Optional
  * stores fall back to inspecting the current generation only. */
  getIdempotencyClaim?(key: string): Promise<IdempotencyClaimValue | null>;
  deleteJob(streamId: string, expectedCreatedAt?: number): Promise<boolean>;
  hasJob(streamId: string): Promise<boolean>;
  getRunningJobs(): Promise<SerializableJobData[]>;
  /** Optional durable paused-job enumeration. Built-in stores implement it so
  * the manager can own approval expiry even after the original runtime died. */
  getRequiresActionJobs?(): Promise<SerializableJobData[]>;
  /** Optional durable enumeration of terminal jobs that still owe a host lifecycle
  * hook (see `terminalHostActionPending`). Built-in stores implement it so cleanup can
  * retry the host adapter after a restart / on another replica, even though the job is
  * no longer in the requires_action index. */
  getTerminalHostActionJobs?(): Promise<SerializableJobData[]>;
  /** Enumerates detached Event Actor completion generations from a versioned
  * retry lane known only to capable consumers. Redis keeps this lane separate
  * from `getTerminalHostActionJobs` so a rolling-deployment replica that only
  * understands the legacy job identity can never claim it. */
  getDetachedAgentEventTerminalHostActionJobs?(): Promise<SerializableJobData[]>;
  /** Clears the pending-host-action marker once the adapter acknowledges success.
  * Identity-fenced on `expectedCreatedAt` so a replacement generation at the same
  * streamId is never cleared through its predecessor. */
  clearTerminalHostAction?(streamId: string, expectedCreatedAt?: number): Promise<void>;
  cleanup(): Promise<number>;
  recordActivity?(streamId: string, expectedCreatedAt?: number): void;
  getJobCount(): Promise<number>;
  getJobCountByStatus(status: JobStatus): Promise<number>;
  destroy(): Promise<void>;
  getActiveJobIdsByUser(userId: string, tenantId?: string): Promise<string[]>;
  /** Complete owner cleanup query, including terminal host work and legacy-index recovery.
  * Managers retain the global-index fallback for older third-party stores. */
  getCleanupJobIdsByUser?(userId: string, tenantId?: string): Promise<string[]>;
  /** Enumerates every extant job in the owner's retained index, including a
  * stale paused generation that account deletion must erase before its worker
  * finalizes it. Optional custom stores fall back to cleanup-blocking jobs. */
  getRetainedJobIdsByUser?(userId: string, tenantId?: string): Promise<string[]>;
  setGraph(streamId: string, graph: StandardGraph, expectedCreatedAt?: number): void;
  setContentParts(streamId: string, contentParts: Agents.MessageContentComplex[], expectedCreatedAt?: number): void;
  getContentParts(streamId: string, expectedCreatedAt?: number, options?: {
    durableOnly?: boolean;
  }): Promise<{
    content: Agents.MessageContentComplex[];
    reconstructedEventCount?: number;
    durableEventCount?: number;
  } | null>;
  getRunSteps(streamId: string, expectedCreatedAt?: number): Promise<Agents.RunStep[]>;
  /** Legacy stores returned `void`; v2 stores return whether the epoch-fenced
  * append committed. Accept both here so existing implementations remain
  * source-compatible. */
  appendChunk(streamId: string, event: unknown, expectedCreatedAt?: number): Promise<void | boolean>;
  /** Optional batching capability: persist any coalesced appends buffered for
  * this stream now. Stores without append coalescing simply omit it. */
  flushPendingAppends?(streamId: string): Promise<void>;
  clearContentState(streamId: string, expectedCreatedAt?: number): void;
  saveRunSteps?(streamId: string, runSteps: Agents.RunStep[], expectedCreatedAt?: number): Promise<void>;
  setCollectedUsage(streamId: string, collectedUsage: UsageMetadata[], expectedCreatedAt?: number): void;
  getCollectedUsage(streamId: string, expectedCreatedAt?: number): UsageMetadata[];
  enqueueSteer(streamId: string, item: SteerQueueItem, expectedCreatedAt?: number): Promise<number>;
  drainSteers(streamId: string, expectedCreatedAt?: number): Promise<SteerQueueItem[]>;
  closeAndDrainSteers(streamId: string, expectedCreatedAt?: number): Promise<SteerQueueItem[]>;
  peekSteers(streamId: string, expectedCreatedAt?: number): Promise<SteerQueueItem[]>;
  removeSteer(streamId: string, steerId: string): Promise<boolean>;
  parkSteers(streamId: string, payload: string, expectedCreatedAt?: number): Promise<void>;
  claimParkedSteers(streamId: string, ownerFragment: string): Promise<string | undefined>;
  clearSteers(streamId: string): Promise<void>;
}
/**
* Full store contract required by the current generation runtime.
*
* Built-in stores implement this interface. Its overrides deliberately narrow
* the legacy return types while its extra methods provide the atomicity needed
* for terminal ownership, durable steering receipts, recovery, and idempotent
* generation startup.
*/
interface IJobStoreV2 extends IJobStore {
  /** Initialize the store (e.g., connect to Redis, start cleanup intervals) */
  initialize(): Promise<void>;
  /** Create a new job */
  createJob(streamId: string, userId: string, conversationId?: string, tenantId?: string, initialMetadata?: JobMetadataPatch, recoveredSteerId?: string, idempotencyClaimKey?: string, idempotencyClaimToken?: string, idempotencyClientRequestId?: string, recoveredSteerPayload?: RecoveredSteerPayload, creationAttemptId?: string, expectedPredecessorCreatedAt?: number, rejectActivePredecessor?: boolean): Promise<CreatedJobData>;
  /** Remove transaction-time predecessor receipts after their handoff was
  * delivered. The current creation attempt id fences a late acknowledgement
  * from clearing receipts inherited by a replacement. */
  acknowledgeReplacedJobs?(streamId: string, creationAttemptId: string, replacedCreatedAts: readonly number[]): Promise<boolean>;
  /** Get a job by streamId (streamId === conversationId) */
  getJob(streamId: string): Promise<SerializableJobData | null>;
  /**
  * Update job data. When `expectedCreatedAt` is supplied, apply the write only
  * if the stream still belongs to that generation.
  */
  updateJob(streamId: string, updates: Partial<SerializableJobData>, expectedCreatedAt?: number): Promise<void>;
  /** Atomically marks only the exact provider segment as fully unwound. */
  markProviderExecutionDrained(streamId: string, expectedCreatedAt: number, providerExecutionId: string): Promise<boolean>;
  /** Activates the provider only while its exact generation is still running. */
  beginProviderExecution(streamId: string, expectedCreatedAt: number, providerExecutionId: string): Promise<boolean>;
  /** Includes terminal jobs whose provider still owns user-data writes. */
  getCleanupBlockingJobIdsByUser(userId: string, tenantId?: string): Promise<string[]>;
  /** Atomically replaces an abort persistence-pending marker with the one
  * terminal payload that late subscribers may consume. Exactly one of the
  * owner, its failure path, or stale-owner recovery wins. */
  finalizeTerminalPersistence(streamId: string, expectedCreatedAt: number, finalEvent: string): Promise<boolean>;
  /**
  * Atomically transition a job's status, **only if** it is currently `from`.
  * Returns `true` when the transition fired, `false` when the job was missing
  * or no longer in `from` (lost a race / illegal transition).
  *
  * `patch` fields are written and `clear` fields removed in the same atomic
  * step, and the running / requires_action membership sets plus live-key TTLs
  * are reconciled to match `to`. This is the race-safe primitive behind the
  * approval lifecycle — it prevents two concurrent resumes from both driving a
  * paused run (a double-drive would re-execute tools / double-bill).
  *
  * Distinct from {@link updateJob}, which writes status unconditionally for
  * callers that don't know the prior state. Reach for `transitionStatus`
  * whenever the legal prior state is known.
  *
  * Atomicity: fully atomic on in-memory and single-node / sentinel Redis
  * (Lua). On Redis Cluster the status guard is best-effort — the membership
  * sets live on a different hash slot from the job hash — matching the store's
  * existing cluster posture for status writes.
  */
  transitionStatus(streamId: string, args: JobStatusTransition): Promise<boolean>;
  /**
  * Terminal variant of {@link transitionStatus} that atomically captures and
  * parks every queued/claimed steer owned by the winning generation. `null`
  * means the status CAS lost; an array (including an empty one) means it won.
  *
  * Abort uses this primitive so it can include exact leftovers in its final
  * event without destructively draining a run before terminal ownership is
  * established.
  */
  transitionStatusAndDrainSteers(streamId: string, args: JobStatusTransition): Promise<SteerQueueItem[] | null>;
  /**
  * Atomically claim an idempotency key so a retried start-generation request
  * attaches to the original stream instead of starting a second billed
  * generation. The first caller gets `{ claimed: true }` and should create the
  * job; a later caller for the same key gets `{ claimed: false, existing }`
  * carrying the stream the original request is already driving.
  *
  * Atomicity: single-key `SET NX` on Redis (one hash slot, cluster-safe) /
  * check-and-set on the single-threaded in-memory store.
  *
  * @param key - Caller-scoped key, e.g. `${userId}:${clientRequestId}`.
  * @param value - The stream a duplicate request should attach to.
  * @param ttlSeconds - Claim lifetime; outlive the generation so a late retry still dedups.
  */
  claimIdempotencyKey(key: string, value: IdempotencyClaimValue, ttlSeconds: number): Promise<IdempotencyClaimResult>;
  /** Compare-and-swap an abandoned pre-create claim. */
  takeoverIdempotencyKey(key: string, expected: IdempotencyClaimValue, value: IdempotencyClaimValue, ttlSeconds: number): Promise<boolean>;
  /** Mark a token-owned claim as having created its generation. Used by the
  * rolling-upgrade bridge to tombstone the legacy key after the same-slot
  * primary key was committed by createJob. */
  markIdempotencyKeyStarted(key: string, claimToken: string, startedAt: number, ttlSeconds: number): Promise<boolean>;
  /** Atomically attach a freshly reacquired claim to the still-live job that
  * was created by the same client request. The job and claim keys share a
  * Redis hash slot, so a replacement cannot cross this validation. */
  adoptIdempotencyKeyForJob(key: string, expected: IdempotencyClaimValue, streamId: string, userId: string, clientRequestId: string, tenantId: string | undefined, expectedCreatedAt: number, ttlSeconds: number, allowMissingClientRequestId?: boolean): Promise<boolean>;
  /**
  * Release a previously-claimed idempotency key so the submission can be retried
  * (e.g. the start failed before generation began). No-op if the key is absent.
  */
  releaseIdempotencyKey(key: string, expected?: IdempotencyClaimValue): Promise<void>;
  /**
  * Delete a job, optionally only when the stream still belongs to the expected
  * generation. Returns true only when a matching job was actually deleted.
  */
  deleteJob(streamId: string, expectedCreatedAt?: number): Promise<boolean>;
  /** Check if job exists */
  hasJob(streamId: string): Promise<boolean>;
  /** Get all running jobs (for cleanup) */
  getRunningJobs(): Promise<SerializableJobData[]>;
  /** Get durable paused jobs so approval expiry is not process-runtime-dependent. */
  getRequiresActionJobs?(): Promise<SerializableJobData[]>;
  /** Cleanup expired jobs */
  cleanup(): Promise<number>;
  /**
  * Record generation activity for a job (e.g. a chunk was emitted), refreshing
  * its "last active" timestamp so the stale-running-job failsafe does not reap a
  * stream that is still producing output.
  *
  * In-memory: updates an internal last-activity timestamp used by cleanup().
  * Redis: no-op — the running-job TTL is already refreshed on each appendChunk.
  *
  * @param streamId - The stream identifier
  * @param expectedCreatedAt - Optional generation identity. When supplied, replacement activity
  *   is not refreshed by a stale emitter.
  */
  recordActivity?(streamId: string, expectedCreatedAt?: number): void;
  /**
  * Persist any coalesced chunk appends still buffered for this stream.
  * Terminal transitions must flush first so the batch lands under the
  * generation's live status instead of fencing against its own completion.
  *
  * Presence of this method is how a store advertises append-coalescing
  * support; see the transport's flushPendingChunks for the pairing rule.
  */
  flushPendingAppends?(streamId: string): Promise<void>;
  /** Get total job count */
  getJobCount(): Promise<number>;
  /** Get job count by status */
  getJobCountByStatus(status: JobStatus): Promise<number>;
  /** Destroy the store and release resources */
  destroy(): Promise<void>;
  /**
  * Get active job IDs for a user.
  * Returns conversation IDs of running jobs belonging to the user.
  * Also performs self-healing cleanup of stale entries.
  *
  * @param userId - The user ID to query
  * @returns Array of conversation IDs with active jobs
  */
  getActiveJobIdsByUser(userId: string, tenantId?: string): Promise<string[]>;
  /**
  * Set the graph reference for a job (in-memory only).
  * The graph provides live access to contentParts and contentData (run steps).
  *
  * In-memory: Stores WeakRef to graph
  * Redis: No-op (graph not transferable, uses chunks instead)
  *
  * @param streamId - The stream identifier
  * @param graph - The StandardGraph instance
  */
  setGraph(streamId: string, graph: StandardGraph, expectedCreatedAt?: number): void;
  /**
  * Set content parts reference for a job.
  *
  * In-memory: Stores direct reference to content array
  * Redis: No-op (content built from chunks)
  *
  * @param streamId - The stream identifier
  * @param contentParts - The content parts array
  */
  setContentParts(streamId: string, contentParts: Agents.MessageContentComplex[], expectedCreatedAt?: number): void;
  /**
  * Get aggregated content for a job.
  *
  * In-memory: Returns live content from graph.contentParts or stored reference
  * Redis: Reconstructs from stored chunks
  *
  * @param streamId - The stream identifier
  * @returns Content parts or null if not available
  */
  getContentParts(streamId: string, expectedCreatedAt?: number, options?: {
    durableOnly?: boolean;
  }): Promise<{
    content: Agents.MessageContentComplex[];
    reconstructedEventCount?: number;
    durableEventCount?: number;
  } | null>;
  /** Atomically records the only recovery outcome for one overflow identity. */
  settleEarlyBufferRecovery(streamId: string, expectedCreatedAt: number, overflowId: string, settlement: Pick<EarlyBufferOverflowState, "recoveryMethod" | "recoveryOutcome" | "recoveryCompletedAt" | "recoveryFailureReason">): Promise<boolean>;
  /** Atomically replaces an unresolved pending overflow marker with its
  * finalized durable frontier. A concurrent recovery settlement wins over
  * this owner-side finalization. */
  finalizeEarlyBufferOverflow(streamId: string, expectedCreatedAt: number, overflowId: string, overflow: EarlyBufferOverflowState): Promise<boolean>;
  /** Whether this generation has admitted any subscriber on any replica. */
  hasSubscriberAttached(streamId: string, expectedCreatedAt: number): Promise<boolean>;
  /** Atomically claims the first subscriber for one generation epoch. */
  claimFirstSubscriber(streamId: string, expectedCreatedAt: number, attachedAt: number, subscriberId: string, leaseExpiresAt: number): Promise<boolean>;
  detachSubscriber(streamId: string, expectedCreatedAt: number, subscriberId: string): Promise<void>;
  hasActiveSubscriber(streamId: string, expectedCreatedAt: number, observedAt: number): Promise<boolean>;
  /**
  * Get run steps for a job (for resume state).
  *
  * In-memory: Returns live run steps from graph.contentData
  * Redis: Fetches from persistent storage
  *
  * @param streamId - The stream identifier
  * @returns Run steps or empty array
  */
  getRunSteps(streamId: string, expectedCreatedAt?: number): Promise<Agents.RunStep[]>;
  /**
  * Append a streaming chunk for later reconstruction.
  *
  * In-memory: No-op (content available via graph reference)
  * Redis: Uses XADD for append-only log efficiency
  *
  * @param streamId - The stream identifier
  * @param event - The SSE event to append
  * @param expectedCreatedAt - Optional generation identity. When supplied, the append is
  *   refused if the stream ID now belongs to a replacement generation.
  */
  appendChunk(streamId: string, event: unknown, expectedCreatedAt?: number, deliveredSteer?: SteerQueueItem, options?: {
    coalesce?: boolean;
  }): Promise<boolean>;
  /**
  * Clear all content state for a job.
  * Called on job completion/cleanup.
  *
  * @param streamId - The stream identifier
  * @param expectedCreatedAt - Optional generation identity. When supplied, replacement content
  *   is not cleared by a stale terminal cleanup.
  */
  clearContentState(streamId: string, expectedCreatedAt?: number): void;
  /**
  * Save run steps to persistent storage.
  * In-memory: No-op (run steps accessed via graph reference)
  * Redis: Persists for resume across instances
  *
  * @param streamId - The stream identifier
  * @param runSteps - Run steps to save
  * @param expectedCreatedAt - Optional generation identity. When supplied, the save is refused
  *   if the stream ID now belongs to a replacement generation.
  */
  saveRunSteps?(streamId: string, runSteps: Agents.RunStep[], expectedCreatedAt?: number): Promise<void>;
  /**
  * Set collected usage reference for a job.
  * This array accumulates token usage from all models during generation.
  *
  * @param streamId - The stream identifier
  * @param collectedUsage - Array of usage metadata from all models
  */
  setCollectedUsage(streamId: string, collectedUsage: UsageMetadata[], expectedCreatedAt?: number): void;
  /**
  * Get collected usage for a job.
  *
  * @param streamId - The stream identifier
  * @returns Array of usage metadata or empty array
  */
  getCollectedUsage(streamId: string, expectedCreatedAt?: number): UsageMetadata[];
  /**
  * Atomically append a steer, guarded on the job being `running` AND the
  * queue not being closed by a terminal drain. Returns the new queue depth,
  * {@link STEER_ENQUEUE_NOT_RUNNING} when the job is missing, not running,
  * or closed, or {@link STEER_ENQUEUE_QUEUE_FULL} at max depth.
  */
  enqueueSteer(streamId: string, item: SteerQueueItem, expectedCreatedAt?: number): Promise<number>;
  /** Atomic capability-normalized enqueue. Unlike enqueue→arm, no fallible
  * mutation occurs after the item becomes durable and before its ACK. */
  enqueueSteerVersioned(streamId: string, item: SteerQueueItem, wantsPreempt: boolean, expectedCreatedAt?: number): Promise<SteerEnqueueVersionedResult>;
  /** Atomic receipt lookup + capability-normalized enqueue. An existing
  * receipt wins even if the live queue is now full, paused, drained, or the
  * generation has ended/replaced. Numeric results are enqueue rejection
  * codes (including {@link STEER_ENQUEUE_RECEIPT_FULL}); a receipt reports
  * either the existing or newly inserted item. */
  enqueueSteerWithReceipt(streamId: string, item: SteerQueueItem, receipt: SteerReceiptInput, wantsPreempt: boolean, expectedCreatedAt?: number): Promise<SteerEnqueueReceiptResult>;
  /** Read a bounded receipt without requiring the live job/queue. */
  getSteerReceipt(streamId: string, clientSteerId: string): Promise<SteerReceipt | null>;
  /**
  * Atomically take ALL queued steers, FIFO. Empty array when none. With
  * `expectedCreatedAt`, the drain is refused (atomically, inside the store)
  * when the live job's `createdAt` differs — a stale run's drain must never
  * consume a replacement job's queue.
  */
  drainSteers(streamId: string, expectedCreatedAt?: number): Promise<SteerQueueItem[]>;
  /** Put a failed claimed batch back at the FIFO front, preserving its order
  * and receipt state. This is the rollback for a durable applied-part write
  * that failed after the atomic drain. */
  restoreClaimedSteers(streamId: string, items: SteerQueueItem[], expectedCreatedAt?: number): Promise<boolean>;
  /**
  * Terminal admission fence. When `allowClaim` and queued work are both
  * present, atomically claim the FIFO batch while leaving admission open for
  * the continued run. Otherwise atomically close admission so a racing steer
  * is rejected and remains an ordinary follow-up, unless
  * `keepOpenWhenEmpty` proves another folded Stop hook already planned a
  * continuation. V1 generations always seal because they lack
  * crash-recoverable claimed-steer receipts.
  */
  admitTerminalSteers(streamId: string, policy: TerminalSteerAdmissionPolicy, expectedCreatedAt?: number): Promise<TerminalSteerAdmissionResult>;
  /**
  * Atomically CLOSE the queue to new steers, then take all queued items
  * FIFO. Used by the terminal paths (final event, abort) so a steer POST
  * racing finalization can never be 202-ACKed after the last drain and then
  * silently cleared — once closed, `enqueueSteer` rejects until the next
  * `createJob` reopens the stream id. `expectedCreatedAt` guards exactly
  * like {@link drainSteers}: a stale run's finalization can neither close
  * nor steal a replacement job's queue.
  */
  closeAndDrainSteers(streamId: string, expectedCreatedAt?: number): Promise<SteerQueueItem[]>;
  /**
  * Non-destructive FIFO read of the queued steers (status/resume surfaces).
  * With `expectedCreatedAt`, returns an empty snapshot if the stream belongs
  * to another generation.
  */
  peekSteers(streamId: string, expectedCreatedAt?: number): Promise<SteerQueueItem[]>;
  /** Drained but not yet durably applied items. Kept separate from the live
  * FIFO so a reconnect can render them without a second owner injecting them. */
  peekClaimedSteers(streamId: string, expectedCreatedAt?: number): Promise<SteerQueueItem[]>;
  /** Remove ONE queued steer by id (user-cancelled before injection).
  *  False when it was no longer queued — already drained or run ended — or
  *  when `expectedCreatedAt` belongs to a replaced generation. */
  removeSteer(streamId: string, steerId: string, expectedCreatedAt?: number): Promise<boolean>;
  /**
  * Atomically set `preempt: true` on ONE queued steer IN PLACE, preserving
  * its FIFO position (the user escalated a waiting steer to an interrupt;
  * the whole queue drains at the seal, so its order must not change).
  * Guarded like {@link enqueueSteer}: `missing` when the job is not running,
  * the steer is no longer queued, the queue is closed, or (with
  * `expectedCreatedAt`) the stream belongs to another generation. The owner's
  * LIVE `preemptCapable` is part of the same atomic predicate — a HITL resume
  * on a rolling deploy can rewrite it for the SAME generation, so a value
  * read before the call is not trustworthy — and an incapable owner answers
  * `incapable` with the item left unflagged.
  */
  armSteer(streamId: string, steerId: string, expectedCreatedAt?: number): Promise<SteerArmOutcome>;
  /** Versioned form used by the HTTP arm route so its publication carries
  * the revision atomically assigned with the durable flag. */
  armSteerVersioned(streamId: string, steerId: string, expectedCreatedAt?: number): Promise<SteerArmResult>;
  /** Atomically remove `preempt` from every queued steer only when the
  * generation's current owner is incapable. Returns the changed steer ids
  * (used to tombstone late arm publications), or `null` when the generation
  * is missing/replaced or currently capable. */
  downgradeSteerPreempts(streamId: string, expectedCreatedAt?: number): Promise<SteerQueueItem[] | null>;
  /**
  * Persist terminally-drained steers under their OWN bounded-TTL key so a
  * client with no live subscriber can recover them via the status route.
  * Deliberately independent of the job record — the default `completeJob`
  * path deletes the job immediately, and recovery must survive that.
  * Merges with any prior payload. `createJob` leases the selected recovery
  * item while that generation is active; the item is removed only after its
  * ordinary user message is durably persisted.
  * When `expectedCreatedAt` is supplied, the write is conditional on that
  * generation still owning the stream ID.
  */
  parkSteers(streamId: string, payload: string, expectedCreatedAt?: number): Promise<void>;
  /** Owner-gated recovery read. A source leased to a matching active recovery
  * generation is hidden but retained; it becomes visible again if startup
  * fails, and is removed only after durable user-message persistence. */
  claimParkedSteers(streamId: string, ownerUserId: string, ownerTenantId?: string): Promise<string | undefined>;
  /** Protocol-aware form of {@link claimParkedSteers}. Existing callers may
  * keep using the payload-only method. */
  claimParkedSteersDetailed(streamId: string, ownerUserId: string, ownerTenantId?: string, requestedProtocolVersion?: GenerationProtocolVersion): Promise<ParkedSteerClaim | undefined>;
  /** Commit a recovery handoff after its ordinary user message is durable.
  * Guarded by generation identity and owner, and idempotent for retries. */
  consumeParkedSteer(streamId: string, steerId: string, ownerUserId: string, ownerTenantId: string | undefined, expectedCreatedAt: number): Promise<boolean>;
  /** Owner-gated terminal reclaim. Atomically removes one leftover from the
  * parked payload and marks its receipt cancelled, making edit/queue/dismiss
  * retries idempotent without reviving an already-started recovery. */
  discardSteerLeftover(streamId: string, clientSteerId: string, steerId: string, ownerUserId: string, ownerTenantId?: string, expectedGenerationCreatedAt?: number): Promise<boolean>;
  /** Drop any queued steers (terminal cleanup backstop). */
  clearSteers(streamId: string): Promise<void>;
}
type GenerationTerminalEventType = "done" | "error";
/** A terminal publication lost the generation fence to a replacement. This is
* an expected safety outcome: the successor owns all further stream output. */
declare class GenerationPublicationFencedError extends Error {
  readonly eventType: GenerationTerminalEventType;
  readonly streamId: string;
  readonly generationId?: number | undefined;
  readonly code = "GENERATION_PUBLICATION_FENCED";
  constructor(eventType: GenerationTerminalEventType, streamId: string, generationId?: number | undefined);
}
/**
* Interface for pub/sub event transport.
* Implementations can use EventEmitter, Redis Pub/Sub, etc.
*/
interface IEventTransport {
  /**
  * Subscribe to events for a stream. `ready` resolves once the transport can receive messages.
  *
  * Redis callers can defer sequenced delivery until `syncReorderBuffer()` establishes the
  * replay frontier. This prevents pub/sub copies of locally buffered events from racing ahead
  * of, and then being duplicated by, first-subscriber replay.
  */
  subscribe(streamId: string, handlers: {
    /** `generationId` identifies the immutable generation that emitted the chunk. */onChunk: (event: unknown, generationId?: number) => void; /** `generationId` identifies the immutable generation that emitted the done event. */
    onDone?: (event: unknown, generationId?: number) => void; /** `generationId` identifies the immutable generation that emitted the error. */
    onError?: (error: string, generationId?: number) => void;
  }, options?: {
    /** Hold sequenced events until syncReorderBuffer establishes the replay frontier. */deferSequenceDelivery?: boolean;
    /** After opening a fresh Pub/Sub channel, atomically capture its sequence frontier
    * and fence delivery so synchronization cannot lose an attachment-time frame. */
    captureSequenceFrontier?: boolean;
  }): {
    unsubscribe: () => void;
    ready?: Promise<void>; /** Synchronize only the transport state captured by this concrete subscription. */
    syncReorderBuffer?: () => void | Promise<void>;
  };
  /**
  * Publish a chunk event.
  * Redis returns the assigned absolute sequence so locally replayed events can
  * advance a subscriber to the exact ordering frontier.
  */
  emitChunk(streamId: string, event: unknown, generationId?: number): void | Promise<void | number>;
  /**
  * Publish a done event - returns Promise in Redis mode for ordered delivery.
  * `generationId` is optional for compatibility with legacy, untagged publishers.
  */
  emitDone(streamId: string, event: unknown, generationId?: number): void | Promise<void>;
  /**
  * Publish an error event - returns Promise in Redis mode for ordered delivery.
  * `generationId` is optional for compatibility with legacy, untagged publishers.
  */
  emitError(streamId: string, error: string, generationId?: number): void | Promise<void>;
  /** Optional live-view demand marker used by observational streams that do not replay. */
  renewDemand?(streamId: string, ttlMs: number): void | Promise<void>;
  /** Returns whether at least one live viewer recently renewed demand for this stream. */
  hasDemand?(streamId: string): boolean | Promise<boolean>;
  /**
  * Publish an abort signal to all replicas (Redis mode).
  * Enables cross-replica abort: user aborts on Replica B,
  * generating Replica A receives signal and stops.
  * Optional - only implemented in Redis transport.
  */
  emitAbort?(streamId: string, generationId?: number): void;
  /** Awaitable, generation-correlated abort handoff. Resolves true only after
  * the replica owning that generation processes the abort. */
  emitAbortConfirmed?(streamId: string, generationId: number): Promise<boolean>;
  /** Persist proof that this process synchronously stopped the exact generation.
  * A delayed replacement can use the proof after the owner's listeners retire. */
  recordAbortAcknowledgement?(streamId: string, generationId: number): Promise<boolean>;
  /** Persist/read exact proof that a provider segment can no longer mutate user data. */
  recordProviderDrain?(streamId: string, generationId: number, providerExecutionId: string): Promise<boolean>;
  hasProviderDrain?(streamId: string, generationId: number, providerExecutionId: string): Promise<boolean>;
  /** Publish a predecessor DONE only while the current job's opaque creation
  * attempt still carries that predecessor in its durable receipt chain. */
  emitReplacedDoneConfirmed?(streamId: string, event: unknown, replacedGenerationId: number, creationAttemptId: string): Promise<void>;
  /**
  * Register callback for abort signals from any replica (Redis mode).
  * Called when abort is triggered from any replica.
  * An async implementation resolves only after it can receive abort messages.
  * The returned function removes only this registration, allowing a terminal
  * generation to release its channel without affecting a same-stream replacement.
  * Optional - only implemented in Redis transport.
  */
  onAbort?(streamId: string, callback: (generationId?: number) => void | boolean): void | (() => void) | Promise<void | (() => void)>;
  /**
  * Publish a preempt arm/clear to all replicas (Redis mode). Unlike abort
  * this does NOT stop the run — it asks the generating replica to seal its
  * current model stream at the next provider-safe boundary. Fenced by
  * {@link PreemptMessage.createdAt} against replacement jobs.
  * Optional - only implemented in Redis transport.
  */
  emitPreempt?(streamId: string, msg: PreemptMessage): void | Promise<number>;
  /**
  * Register callback for preempt signals from any replica (Redis mode).
  * An async implementation resolves only after it can receive messages.
  * The returned function removes only this registration, allowing a terminal
  * generation to release its channel without affecting a same-stream replacement.
  * Optional - only implemented in Redis transport.
  */
  onPreempt?(streamId: string, callback: (msg: PreemptMessage) => void): void | (() => void) | Promise<void | (() => void)>;
  /** Get subscriber count for a stream */
  getSubscriberCount(streamId: string): number;
  /** Check if this is the first subscriber (for ready signaling) */
  isFirstSubscriber(streamId: string): boolean;
  /** Listen for all subscribers leaving */
  onAllSubscribersLeft(streamId: string, callback: () => void): void;
  /**
  * Advance subscriber reorder buffer to match publisher sequence (cross-replica safe).
  * @param replayedNextSeq - Absolute Redis sequence immediately after the last event replayed
  *   from the local early-event buffer. Pending entries below it are duplicates; entries at
  *   or above it are live. Undefined means no local replay, so the Redis counter is trusted.
  */
  syncReorderBuffer?(streamId: string, replayedNextSeq?: number): void | Promise<void>;
  /**
  * Notify and detach subscribers attached to this process without broadcasting a terminal event.
  * Must trigger all-subscribers-left cleanup so graceful shutdown can drain partial persistence.
  */
  closeLocalSubscribers?(streamId: string, error: string): void;
  /**
  * Publish any coalesced chunk publications still buffered for this stream.
  * Callers about to transition a generation's status must flush first, or the
  * batch would land behind the transition and fence itself.
  *
  * Presence of this method is how a transport advertises delta-coalescing
  * support: the generation manager only sends `coalesce` hints (and only
  * stops awaiting per-delta receipts) when both the transport and the job
  * store expose their flush capability.
  */
  flushPendingChunks?(streamId: string): Promise<void>;
  /** Cleanup transport resources for a specific stream */
  cleanup(streamId: string): void;
  /** Get all tracked stream IDs (for orphan cleanup) */
  getTrackedStreamIds(): string[];
  /** Destroy all transport resources */
  destroy(): void;
}
//#endregion
//#region src/code/http.d.ts
type Registry = {
  register: (params: {
    actor: CodeEnvironmentPrincipalContext;
    environment: CodeEnvironmentRegistration;
    maxOwned?: number;
  }) => Promise<CodeEnvironmentSummary>;
  listAccessible: (actor: CodeEnvironmentPrincipalContext) => Promise<CodeEnvironmentSummary[]>;
  listAccessibleDetails?: (actor: CodeEnvironmentPrincipalContext) => Promise<AccessibleCodeEnvironmentDetails>;
  listAccessibleConfigurations?: (actor: CodeEnvironmentPrincipalContext) => Promise<AccessibleCodeEnvironmentConfiguration[]>;
  resolvePrincipals?: (actor: CodeEnvironmentPrincipalContext) => Promise<NonNullable<CodeEnvironmentPrincipalContext["principals"]>>;
  updateSettings?: (params: {
    actor: CodeEnvironmentPrincipalContext;
    environmentId: string;
    settings: import("librechat-data-provider").CodeEnvironmentUserSettings;
  }) => Promise<CodeEnvironmentSummary | null>;
  remove: (params: {
    actor: CodeEnvironmentPrincipalContext;
    environmentId: string;
    beforeDelete?: (target: CodeEnvironmentLifecycleTarget) => Promise<void>;
  }) => Promise<CodeEnvironmentSummary | null>;
  markRevocationPending?: (environmentId: string) => Promise<void>;
};
/** Owner-scoped conversation access for moving a sealed code-environment decision. */
interface CodeEnvironmentConversationDeps {
  get: (userId: string, conversationId: string) => Promise<StoredConversationDecision | null>;
  replaceDecision: (params: {
    user: string;
    conversationId: string;
    expected: Pick<StoredConversationDecision, "codeEnvironmentMode" | "codeWorkspaces">;
    codeWorkspaces: CodeWorkspaceSelection[];
  }) => Promise<StoredConversationDecision | null>;
}
/** Generation lookups a move needs to tell whether any run can still act for a conversation. */
interface CodeEnvironmentGenerationDeps {
  getJob: (streamId: string) => Promise<CodeEnvironmentGenerationJob | null | undefined>;
  /** Remote API runs use response IDs as stream identities, so the conversation's own stream
  *  is not the only generation that can still act in its environment. */
  getCleanupBlockingJobIdsForConversations: (userId: string, conversationIds: readonly string[], tenantId?: string) => Promise<string[]>;
}
/** The generation state a move reads to tell whether a run can still save its own decision. */
type CodeEnvironmentGenerationJob = {
  status: JobStatus;
  metadata?: {
    terminalPersistencePending?: boolean;
  };
};
interface CodeEnvironmentHttpDeps {
  getAppConfig: (options: GetAppConfigOptions) => Promise<AppConfig>;
  registry: Registry;
  conversations?: CodeEnvironmentConversationDeps;
  generations?: CodeEnvironmentGenerationDeps;
  createEnvironmentId?: () => string;
  readSecret?: (name: string) => string | undefined;
  resolveTenantId?: (req: ServerRequest) => string;
  principalAuthEnabled?: () => boolean;
  principalAuthReady?: () => Promise<void> | void;
  principalIsActive?: (userId: string) => Promise<boolean>;
  maxPrincipalEnvironments?: number;
  fetchImpl?: CodeBridgeFetch;
}
declare function createCodeEnvironmentHttpHandlers(deps: CodeEnvironmentHttpDeps): {
  list: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  register: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  pair: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  status: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  updateSettings: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  remove: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  moveConversationDecision: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
};
//#endregion
//#region src/code/config.d.ts
type ConfigurationRegistry = {
  listAccessibleConfigurations: (actor: CodeEnvironmentPrincipalContext) => Promise<AccessibleCodeEnvironmentConfiguration[]>;
  listRegisteredIds: () => Promise<string[]>;
};
/**
* Resolves the deployment-wide browser protocol gate. The exact version match
* keeps older and future wire shapes on the legacy-safe path.
*/
declare function resolveCodeEnvironmentDecisionVersion(configuredVersion?: string): typeof CODE_ENVIRONMENT_DECISION_VERSION | undefined;
/** Advertises owner moves of a sealed decision only where the effective policy enables them. */
declare function resolveCodeEnvironmentMoveVersion(appConfig?: Pick<AppConfig, "endpoints"> | null): typeof CODE_ENVIRONMENT_MOVE_VERSION | undefined;
/** Enables the implicit managed route only after the versioned rollout is complete. */
declare function isImplicitStatefulCodeRouteAvailable(configuredVersion?: string, statefulBaseURL?: string): boolean;
declare function mergeAccessibleCodeEnvironments({
  appConfig,
  deploymentConfig,
  actor,
  registry
}: {
  appConfig: AppConfig;
  deploymentConfig: AppConfig;
  actor: CodeEnvironmentPrincipalContext;
  registry: ConfigurationRegistry;
}): Promise<AppConfig>;
//#endregion
//#region src/code/lifecycle.d.ts
declare function reconcileCodeEnvironmentLifecycle({
  mongoose,
  readSecret,
  fetchImpl,
  limit
}: {
  mongoose: typeof import("mongoose");
  readSecret?: (name: string) => string | undefined;
  fetchImpl?: CodeBridgeFetch;
  limit?: number;
}): Promise<void>;
declare function startCodeEnvironmentLifecycleReconciler(options: Parameters<typeof reconcileCodeEnvironmentLifecycle>[0]): void;
declare function revokeUserCodeEnvironmentWorkers({
  mongoose,
  userId,
  appConfig,
  readSecret,
  fetchImpl
}: {
  mongoose: typeof import("mongoose");
  userId: string;
  appConfig: AppConfig;
  readSecret?: (name: string) => string | undefined;
  fetchImpl?: CodeBridgeFetch;
}): Promise<number>;
//#endregion
//#region src/code/workspace.d.ts
declare const WORKSPACE_WRITE_MAX_BYTES: number;
declare const WORKSPACE_EDIT_MAX_COUNT: number;
/** Keep aligned with data-provider's deployment schema defaults and hard cap. */
declare const WORKSPACE_COMMAND_DEFAULT_TIMEOUT_MS: number;
declare const WORKSPACE_COMMAND_MAX_TIMEOUT_MS: number;
interface WorkspaceReadRequest {
  protocolVersion: 1;
  operation: "read_file";
  workspaceId: string;
  path: string;
  startLine?: number;
  maxLines?: number;
}
interface WorkspaceSearchRequest {
  protocolVersion: 1;
  operation: "search_text";
  workspaceId: string;
  query: string;
  path?: string;
  maxResults?: number;
}
interface WorkspaceListRequest {
  protocolVersion: 1;
  operation: "list_files";
  workspaceId: string;
  path?: string;
  maxResults?: number;
  afterPath?: string;
}
interface WorkspaceExecuteCommandRequest {
  protocolVersion: 1;
  operation: "execute_command";
  workspaceId: string;
  command: string;
  cwd?: string;
  timeoutMs?: number;
  maxOutputBytes?: number;
  environmentAction?: {
    name: string;
    fingerprint: string;
  };
}
interface WorkspaceWriteRequest {
  protocolVersion: 1;
  operation: "write_file";
  workspaceId: string;
  path: string;
  content: string;
  overwrite?: boolean;
}
interface WorkspaceTextEdit {
  oldText: string;
  newText: string;
}
interface WorkspaceEditRequest {
  protocolVersion: 1;
  operation: "edit_file";
  workspaceId: string;
  path: string;
  edits: WorkspaceTextEdit[];
  expectedBaseSha256?: string;
}
interface WorkspacePreviewEditRequest {
  protocolVersion: 1;
  operation: "preview_edit";
  workspaceId: string;
  path: string;
  edits: WorkspaceTextEdit[];
}
type WorkspaceToolRequest = WorkspaceReadRequest | WorkspaceSearchRequest | WorkspaceListRequest | WorkspaceWriteRequest | WorkspacePreviewEditRequest | WorkspaceEditRequest | WorkspaceExecuteCommandRequest;
interface WorkspaceReadResult {
  protocolVersion: 1;
  operation: "read_file";
  workspaceId: string;
  path: string;
  content: string;
  startLine: number;
  endLine: number;
  truncated: boolean;
  nextStartLine?: number;
}
interface WorkspaceSearchResult {
  protocolVersion: 1;
  operation: "search_text";
  workspaceId: string;
  matches: Array<{
    path: string;
    line: number;
    column: number;
    text: string;
  }>;
  truncated: boolean;
}
interface WorkspaceListResult {
  protocolVersion: 1;
  operation: "list_files";
  workspaceId: string;
  paths: string[];
  truncated: boolean;
  nextAfterPath?: string;
}
interface WorkspaceExecuteCommandResult {
  protocolVersion: 1;
  operation: "execute_command";
  workspaceId: string;
  exitCode: number | null;
  signal?: string;
  stdout: string;
  stderr: string;
  truncated: boolean;
  timedOut: boolean;
}
interface WorkspaceWriteResult {
  protocolVersion: 1;
  operation: "write_file";
  workspaceId: string;
  path: string;
  created: boolean;
  bytesWritten: number;
}
interface WorkspaceEditResult {
  protocolVersion: 1;
  operation: "edit_file";
  workspaceId: string;
  path: string;
  replacements: number;
  bytesWritten: number;
}
interface WorkspacePreviewEditResult {
  protocolVersion: 1;
  operation: "preview_edit";
  workspaceId: string;
  path: string;
  content: string;
  hasUtf8Bom: boolean;
  baseSha256: string;
  replacements: number;
  bytesWritten: number;
}
type WorkspaceToolResult = WorkspaceReadResult | WorkspaceSearchResult | WorkspaceListResult | WorkspaceWriteResult | WorkspacePreviewEditResult | WorkspaceEditResult | WorkspaceExecuteCommandResult;
declare class WorkspaceToolHttpError extends Error {
  readonly reason: "rejected" | "invalid" | "timeout" | "failed";
  readonly upstreamStatus?: number | undefined;
  readonly upstreamBody?: string | undefined;
  readonly upstreamBodyTruncated: boolean;
  constructor(reason: "rejected" | "invalid" | "timeout" | "failed", upstreamStatus?: number | undefined, upstreamBody?: string | undefined, upstreamBodyTruncated?: boolean);
}
declare function executeWorkspaceTool({
  baseURL,
  authHeaders,
  request,
  signal,
  fetchImpl
}: {
  baseURL: string;
  authHeaders: Record<string, string>;
  request: WorkspaceToolRequest;
  signal?: AbortSignal;
  fetchImpl?: CodeBridgeFetch;
}): Promise<WorkspaceToolResult>;
//#endregion
//#region src/code/command.d.ts
declare const ATTACHED_WORKSPACE_BASH_DESCRIPTION = "Runs bash commands inside the selected attached environment and returns stdout/stderr. The workspace may be an existing project, a Git repository, or an empty directory; Git is not required.\n\nSession behavior:\n- Files in the registered workspace persist between calls.\n- Each call runs in a fresh sandboxed process; shell variables, the working directory, temporary files, and background processes do not survive the call.\n- Network access follows the sandbox policy configured on the worker and may be unavailable.\n- Commands and file access remain confined by the worker's runtime policy.\n- Input code is already displayed to the user; do not repeat it unless asked.\n- Explicitly print every result the user should see.\n- Never use this tool to execute malicious commands.";
declare function resolveAttachedWorkspaceCommandTimeoutMax(configSchema?: CodeEnvironmentUserConfigSchema): number;
declare function buildAttachedWorkspaceBashSchema(maxTimeoutMs?: number, environment?: CodeWorkspaceDescriptor["environment"]): NonNullable<LCTool$1["parameters"]>;
/**
* This definition is shared with agent metadata. LangChain's JSON Schema
* dereferencer annotates schemas during validation, so each tool receives an
* isolated mutable clone instead of mutating this shared definition.
*/
declare const ATTACHED_WORKSPACE_BASH_SCHEMA: NonNullable<LCTool$1["parameters"]>;
declare function buildAttachedWorkspaceBashDescription(enableToolOutputReferences: boolean, environment?: CodeWorkspaceDescriptor["environment"]): string;
/** Apply authorship before the SDK prepares the script and its replay requests. */
declare function createGitIdentityProgrammaticBashTool(options: Parameters<typeof createBashProgrammaticToolCallingTool>[0], identity?: AgentGitIdentity | null): DynamicStructuredTool;
declare function createAttachedWorkspaceBashTool({
  baseUrl,
  authHeaders,
  workspaceId,
  environment,
  gitIdentity,
  maxTimeoutMs,
  fetchImpl
}: {
  baseUrl: string;
  authHeaders: () => Promise<Record<string, string>> | Record<string, string>;
  workspaceId: string;
  environment?: CodeWorkspaceDescriptor["environment"];
  gitIdentity?: AgentGitIdentity | null; /** Deployment ceiling already intersected with the protocol hard cap. */
  maxTimeoutMs?: number;
  fetchImpl?: CodeBridgeFetch;
}): DynamicStructuredTool;
//#endregion
//#region src/agents/execution.d.ts
declare const CODE_API_EXPECTED_PROFILE_HEADER = "X-CodeAPI-Expected-Profile";
declare const CODE_API_BRIDGE_WORKER_HEADER = "X-TerraMind-Code-Worker-ID";
type CodeExecutionProfile = "default" | "stateful";
type CodeEnvironmentConfig = NonNullable<NonNullable<TAgentsEndpoint["statefulCodeSessions"]>["environments"]>[number] & {
  /** Server-resolved control plane for a principal-owned attached environment. */controlPlaneId?: string;
};
interface CodeExecutionContext {
  baseUrl: string;
  codeSessionKey: string;
  executionProfile: CodeExecutionProfile;
  /** Stable server-side namespace for deployment-local file references and
  * priming work. Unlike `executionProfile`, this distinguishes configured
  * Code API deployments that all use the `stateful` wire profile. */
  executionRouteKey?: string;
  runtimeSessionHint?: string;
  statefulSessions: boolean;
  environmentId?: string;
  environmentType?: CodeEnvironmentConfig["type"];
  bridgeWorkerId?: string;
  codeEnvironmentConfigSchema?: CodeEnvironmentUserConfigSchema;
  codeEnvironmentSettings?: CodeEnvironmentUserSettings;
  /** Live, server-validated directory selection. Never derive session reuse from this field. */
  codeWorkspace?: CodeWorkspaceSelection & {
    operations: CodeWorkspaceOperation[];
    environment?: CodeWorkspaceDescriptor["environment"];
  };
}
/** Removes live capability data before a workspace binding is persisted. */
declare function getCodeWorkspaceSelections(contexts: Array<Pick<CodeExecutionContext, "environmentId" | "environmentType" | "codeWorkspace"> | null | undefined>): CodeWorkspaceSelection[] | undefined;
type CodeExecutionApprovalAgent = {
  id?: string | null;
  codeExecutionContext?: CodeExecutionContext | null;
};
/**
* Captures an opaque identity for every stateful code target reachable by a
* paused run. Raw base URLs, worker IDs and session hints never enter the
* pending-action client projection.
*/
declare function captureCodeExecutionApprovalBinding(agents: readonly (CodeExecutionApprovalAgent | null | undefined)[]): Agents.CodeExecutionApprovalBinding | undefined;
declare class CodeExecutionApprovalTargetChangedError extends Error {
  readonly code = "CODE_EXECUTION_APPROVAL_TARGET_CHANGED";
  constructor();
}
/** Backward-compatible for old pauses; present bindings always fail closed. */
declare function assertCodeExecutionApprovalBinding(expected: unknown, agents: readonly (CodeExecutionApprovalAgent | null | undefined)[]): void;
declare function createCodeExecutionRouteKey(profile: CodeExecutionProfile, environment?: Pick<CodeEnvironmentConfig, "id" | "baseURL" | "workerId" | "pairing">): string;
declare function getCodeExecutionRouteKey(context: Pick<CodeExecutionContext, "executionProfile" | "executionRouteKey">): string;
declare function normalizeStatefulCodeEnvironment(environment?: StatefulCodeEnvironment | string | null): StatefulCodeEnvironment;
declare function getCodeExecutionBaseUrl(profile: CodeExecutionProfile, environment?: CodeEnvironmentConfig): string;
declare function resolveCodeExecutionContext(params: {
  statefulSessions: boolean;
  environment?: StatefulCodeEnvironment | string | null;
  environmentId?: string | null;
  environments?: readonly CodeEnvironmentConfig[];
  userId?: string | null;
  agentId?: string | null;
  conversationId?: string | null;
}): CodeExecutionContext;
declare function codeExecutionHeaders(context: Pick<CodeExecutionContext, "executionProfile" | "bridgeWorkerId">): Record<string, string>;
/**
* `@librechat/agents` replaces any throw from this callback with a fixed
* "not authorized" string before the model or the operator sees it, and its own
* console diagnostic carries no request or user id. This log is the only
* request-correlated record of why the headers could not be resolved.
*/
declare function codeExecutionAuthHeaders(authHeaders: (bridgeWorkerId?: string) => Promise<Record<string, string>> | Record<string, string>, context: Pick<CodeExecutionContext, "executionProfile" | "bridgeWorkerId">): Promise<Record<string, string>>;
//#endregion
//#region src/code/capabilities.d.ts
type CodeCapabilityConfigLoader = ReturnType<typeof createAppConfigService>["getAppConfig"];
declare class CodeWorkspaceSelectionError extends Error {
  readonly reason: CodeWorkspaceSelectionErrorReason$1;
  readonly code: ErrorTypes.CODE_WORKSPACE_UNAVAILABLE;
  readonly status: number;
  readonly statusCode: number;
  constructor(reason: CodeWorkspaceSelectionErrorReason$1);
}
/** Resolves an immutable conversation selection into a live worker capability. */
declare function resolveCodeExecutionWorkspaceContext({
  context,
  requestedSelections,
  persistedSelections,
  environments,
  getAppConfig
}: {
  context: CodeExecutionContext;
  requestedSelections?: unknown;
  persistedSelections?: unknown;
  environments?: readonly CodeEnvironmentConfig[];
  getAppConfig?: CodeCapabilityConfigLoader;
}): Promise<CodeExecutionContext>;
/** Attached workers must confirm both a stateful workspace and the Bash runtime. */
declare function supportsProgrammaticCodeExecution(context?: CodeExecutionContext, environments?: readonly CodeEnvironmentConfig[], getAppConfig?: CodeCapabilityConfigLoader): Promise<boolean>;
//#endregion
//#region src/code/errors.d.ts
interface CodeWorkspaceErrorLike {
  code?: string;
  reason?: string;
}
interface CodeWorkspaceSelectionErrorDetails {
  reason?: CodeWorkspaceSelectionErrorReason$1;
}
declare function getCodeWorkspaceSelectionErrorDetails(error?: CodeWorkspaceErrorLike | null): CodeWorkspaceSelectionErrorDetails;
/**
* A rejected decision must remain retryable. Publishing the generation error
* is safe, but persisting a first-turn conversation without a validated
* decision would turn the retry into a locked legacy conversation.
*/
declare function shouldPersistCodeWorkspaceInitializationError({
  streamStarted,
  isNewConversation,
  failureCode,
  hasValidatedDecision
}: {
  streamStarted: boolean;
  isNewConversation: boolean;
  failureCode?: string;
  hasValidatedDecision: boolean;
}): boolean;
//#endregion
//#region src/auth/ip.d.ts
/**
* Checks if an IP address belongs to a private, reserved, or link-local range.
* Handles IPv4, IPv6, and IPv4-mapped IPv6 addresses (::ffff:A.B.C.D).
*/
declare function isPrivateIP(ip: string): boolean;
//#endregion
//#region src/auth/domain.d.ts
/**
* @param email
* @param allowedDomains
*/
declare function isEmailDomainAllowed(email: string, allowedDomains?: string[] | null): boolean;
/**
* Checks whether a hostname/IP literal and port appear in an admin-supplied
* exemption list. Address match is case-insensitive and bracket-stripped, so
* `[::1]` matches `::1` and `LOCALHOST` matches `localhost` when the port
* also matches.
*
* The normalization and scoping rules live in `./allowedAddresses` so the
* connect-time DNS lookup in `agent.ts` and this preflight helper share a
* single implementation. See that module for the security invariants.
*/
declare function isAddressAllowed(hostnameOrIP: string, allowedAddresses?: string[] | null, port?: string | number | null): boolean;
/**
* Checks if a hostname resolves to a private/reserved IP address.
* Directly validates literal IPv4 and IPv6 addresses without DNS lookup.
* For hostnames, resolves via DNS and checks all returned addresses.
* Fails open on DNS errors (returns false), since the HTTP request would also fail.
*
* When `allowedAddresses` is provided, the hostname/port and any resolved
* IP/port are matched against the list — a match short-circuits to `false`
* so admin-exempted private services bypass the SSRF block.
*/
declare function resolveHostnameSSRF(hostname: string, allowedAddresses?: string[] | null, port?: string | number | null): Promise<boolean>;
/**
* SSRF Protection: Checks if a hostname/IP is a potentially dangerous internal target.
* Blocks private IPs, localhost, cloud metadata IPs, and common internal hostnames.
*
* When `allowedAddresses` is provided, a literal host:port match exempts the
* target — used by admins to permit known-good internal services (self-hosted
* Ollama, Docker host, etc.) without disabling SSRF protection for every port
* on the same host.
*
* @param hostname - The hostname or IP to check
* @param allowedAddresses - Optional admin exemption list of host:port pairs
* @returns true if the target is blocked (SSRF risk), false if safe
*/
declare function isSSRFTarget(hostname: string, allowedAddresses?: string[] | null, port?: string | number | null): boolean;
/** Supported protocols for domain validation (HTTP, HTTPS, WebSocket) */
type SupportedProtocol = "http:" | "https:" | "ws:" | "wss:";
declare function getEffectivePort(protocol: SupportedProtocol | string | null, port?: string | null): string;
/**
* Validates domain for OpenAPI Agent Actions (HTTP/HTTPS only).
* SECURITY: WebSocket protocols are NOT allowed per OpenAPI specification.
* @param domain - The domain to check (can include protocol/port)
* @param allowedDomains - List of allowed domain patterns
* @param allowedAddresses - Optional admin exemption list of host:port pairs
*/
declare function isActionDomainAllowed(domain?: string | null, allowedDomains?: string[] | null, allowedAddresses?: string[] | null): Promise<boolean>;
/**
* Extracts full domain spec (protocol://hostname:port) from MCP server config URL.
* Returns the full origin for proper protocol/port matching against allowedDomains.
* @returns The full origin string, or null when:
*   - No `url` property, non-string, or empty (stdio transport — always allowed upstream)
*   - URL string present but cannot be parsed (rejected fail-closed upstream when allowlist active)
*   Callers must distinguish these two null cases; see {@link isMCPDomainAllowed}.
* @param config - MCP server configuration (accepts any config with optional url field)
*/
declare function extractMCPServerDomain(config: Record<string, unknown>): string | null;
/**
* Validates MCP server domain against allowedDomains.
* Supports HTTP, HTTPS, WS, and WSS protocols (per MCP specification).
* Stdio transports (no URL) are always allowed.
* Configs with a non-empty URL that cannot be parsed are rejected fail-closed when an
* allowlist is active, preventing template placeholders (e.g. `{{HOST}}`) from bypassing
* domain validation after `processMCPEnv` resolves them at connection time.
* When no allowlist is configured, unparseable URLs fall through to connection-level
* SSRF protection (`createSSRFSafeUndiciConnect`).
* @param config - MCP server configuration with optional url field
* @param allowedDomains - List of allowed domains (with wildcard support)
*/
declare function isMCPDomainAllowed(config: Record<string, unknown>, allowedDomains?: string[] | null, allowedAddresses?: string[] | null): Promise<boolean>;
/**
* Checks whether an OAuth URL matches any entry in the MCP allowedDomains list,
* honoring protocol and port constraints when specified by the admin.
*
* Mirrors the allowlist-matching logic of {@link isDomainAllowedCore} (hostname,
* protocol, and explicit-port checks) but is synchronous — no DNS resolution is
* needed because the caller is deciding whether to *skip* the subsequent
* SSRF/DNS checks, not replace them.
*
* @remarks `parseDomainSpec` normalizes `www.` prefixes, so both the input URL
* and allowedDomains entries starting with `www.` are matched without that prefix.
*/
declare function isOAuthUrlAllowed(url: string, allowedDomains?: string[] | null, allowedAddresses?: string[] | null): boolean;
/**
* Validates that a user-provided endpoint URL does not target private/internal addresses.
* Throws if the URL is unparseable, uses a non-HTTP(S) scheme, targets a known SSRF hostname,
* or DNS-resolves to a private IP.
*
* When `allowedAddresses` is provided, hostname/IP + port pairs are matched
* against the list — admin-exempted private services bypass the SSRF block.
* This lets operators permit known-good private services (self-hosted Ollama,
* Docker host, etc.) without disabling protection for every port on the same host.
*
* @note DNS rebinding: validation performs a single DNS lookup. An adversary controlling
*   DNS with TTL=0 could respond with a public IP at validation time and a private IP
*   at request time. This is an accepted limitation of point-in-time DNS checks.
* @note Fail-open on DNS errors: a resolution failure here implies a failure at request
*   time as well, matching {@link resolveHostnameSSRF} semantics.
*/
declare function validateEndpointURL(url: string, endpoint: string, allowedAddresses?: string[] | null): Promise<void>;
//#endregion
//#region src/auth/openid.d.ts
type OpenIdEmailClaims = {
  email?: unknown;
  preferred_username?: unknown;
  upn?: unknown;
  [claim: string]: unknown;
};
type OpenIdIssuerSource = {
  iss?: string;
  issuer?: string;
  serverMetadata?: () => {
    issuer?: string;
  } | undefined;
};
type OpenIdLookupField = "openidId" | "idOnTheSource";
type OpenIdUserResolution = {
  user: IUser | null;
  error: string | null;
  migration: boolean;
};
declare function normalizeOpenIdIssuer(issuer: string | undefined): string | undefined;
declare function getOpenIdIssuer(...sources: Array<OpenIdIssuerSource | null | undefined>): string | undefined;
declare function getIssuerBoundConditions(field: OpenIdLookupField, value: string | undefined, openidIssuer: string | undefined): FilterQuery<IUser>[];
declare function isUserIssuerAllowed(user: IUser, openidIssuer: string | undefined): boolean;
/**
* Resolves the OpenID user identifier claim, honoring OPENID_EMAIL_CLAIM before
* email/preferred_username/upn fallbacks.
*/
declare function getOpenIdEmail(claims: OpenIdEmailClaims | null | undefined, strategyName?: string): string | undefined;
/**
* Finds or migrates a user for OpenID authentication
* @returns user object (with migration fields if needed), error message, and whether migration is needed
*/
declare function findOpenIDUser({
  openidId,
  findUser,
  email,
  openidIssuer,
  idOnTheSource,
  strategyName
}: {
  openidId: string;
  findUser: UserMethods["findUser"];
  email?: string;
  openidIssuer?: string;
  idOnTheSource?: string;
  strategyName?: string;
}): Promise<OpenIdUserResolution>;
//#endregion
//#region src/auth/saml.d.ts
declare const TRANSIENT_SAML_NAME_ID_FORMAT = "urn:oasis:names:tc:SAML:2.0:nameid-format:transient";
type SamlSubjectError = "missing_name_id" | "transient_name_id" | "issuer_mismatch";
interface SamlSubjectProfile {
  nameID?: string;
  nameIDFormat?: string;
  issuer?: string;
}
type SamlSubjectResolution = {
  nameID: string;
  error?: never;
} | {
  nameID?: never;
  error: SamlSubjectError;
};
declare function resolveSamlSubject(profile: SamlSubjectProfile | null | undefined, expectedIssuer?: string): SamlSubjectResolution;
//#endregion
//#region src/utils/proxy.d.ts
type ProxyEnvConfig = {
  httpProxy?: string;
  httpsProxy?: string;
  noProxy?: string;
};
type HttpsProxyAgentInstance = InstanceType<typeof HttpsProxyAgent>;
declare function getProxyEnvConfig(): ProxyEnvConfig | undefined;
declare function getEnvProxyDispatcher(): Dispatcher | undefined;
declare function getProxyDispatcher(proxyUrl?: string | null): Dispatcher | undefined;
declare function shouldBypassProxy(targetUrl: string | URL, noProxy?: string): boolean;
declare function getProxyUrlForUrl(targetUrl?: string | URL): string | undefined;
declare function getHttpsProxyAgent(targetUrl?: string | URL): HttpsProxyAgentInstance | undefined;
declare function applyAxiosProxyConfig(config: AxiosRequestConfig, targetUrl?: string | URL): AxiosRequestConfig;
declare const getOpenIdProxyDispatcher: () => Dispatcher | undefined;
//#endregion
//#region src/auth/exchange.d.ts
/**
* Gets the admin panel URL from environment or falls back to default.
* @returns The admin panel URL
*/
declare function getAdminPanelUrl(): string;
/**
* User data stored in the exchange cache
*/
interface AdminExchangeUser {
  _id: string;
  id: string;
  email: string;
  name: string;
  username: string;
  role: string;
  avatar?: string;
  provider?: string;
  openidId?: string;
}
/**
* Data stored in cache for admin OAuth exchange.
*
* `expiresAt` is the absolute expiry of `token` (ms epoch). Used by admin
* panel clients to drive proactive refresh before the bearer expires.
*/
interface AdminExchangeData {
  userId: string;
  user: AdminExchangeUser;
  token: string;
  refreshToken?: string;
  origin?: string;
  codeChallenge?: string;
  expiresAt?: number;
}
/**
* Response from the exchange endpoint
*/
interface AdminExchangeResponse {
  token: string;
  /**
  * When Google rotates the refresh token on use, this will differ from the
  * token the client originally sent. Clients MUST persist this value; failing
  * to do so causes future refresh calls to fail once Google's original grant
  * expires or is revoked.
  */
  refreshToken?: string;
  user: AdminExchangeUser;
  expiresAt?: number;
}
/**
* Serializes user data for the exchange cache.
* @param user - The authenticated user object
* @returns Serialized user data for admin panel
*/
declare function serializeUserForExchange(user: IUser): AdminExchangeUser;
/**
* Verifies a PKCE code_verifier against a stored code_challenge.
* Uses hex-encoded SHA-256 comparison (not RFC 7636 S256 which uses base64url).
* @param verifier - The code_verifier provided during exchange
* @param challenge - The hex-encoded SHA-256 code_challenge stored during code generation
* @returns True if the verifier matches the challenge
*/
declare function verifyCodeChallenge(verifier: string, challenge: string): boolean;
/**
* Generates an exchange code and stores user data for admin panel OAuth flow.
* @param cache - The Keyv cache instance for storing exchange data
* @param user - The authenticated user object
* @param token - The JWT access token
* @param refreshToken - Optional refresh token for OpenID users
* @param origin - The admin panel origin (scheme://host:port) for origin binding
* @param codeChallenge - PKCE code_challenge (hex-encoded SHA-256 of code_verifier)
* @param expiresAt - Optional absolute expiry of `token` (ms epoch); admin panel uses for proactive refresh
* @returns The generated exchange code
*/
declare function generateAdminExchangeCode(cache: Keyv, user: IUser, token: string, refreshToken?: string, origin?: string, codeChallenge?: string, expiresAt?: number): Promise<string>;
/**
* Exchanges an authorization code for tokens and user data.
* The code is deleted immediately after retrieval (one-time use).
* @param cache - The Keyv cache instance for retrieving exchange data
* @param code - The authorization code to exchange
* @param requestOrigin - The origin of the requesting client for origin binding
* @param codeVerifier - PKCE code_verifier to verify against the stored code_challenge
* @returns The exchange response with token, refreshToken, and user data, or null if invalid/expired
*/
declare function exchangeAdminCode(cache: Keyv, code: string, requestOrigin?: string, codeVerifier?: string): Promise<AdminExchangeResponse | null>;
/** PKCE challenge cache TTL: 5 minutes (enough for user to authenticate with IdP) */
declare const PKCE_CHALLENGE_TTL: number;
/** Regex pattern for valid PKCE challenges: 64 hex characters (SHA-256 hex digest) */
declare const PKCE_CHALLENGE_PATTERN: RegExp;
/** Minimal request shape needed by {@link stripCodeChallenge}. */
interface PkceStrippableRequest {
  query: Record<string, unknown>;
  originalUrl: string;
  url: string;
}
/**
* Strips admin-panel-only params from the request query and URL strings.
*
* openid-client v6's Passport Strategy uses `currentUrl.searchParams.size === 0`
* to distinguish an initial authorization request from an OAuth callback.
* Admin-panel-specific query params would cause the strategy to misclassify the
* request as a callback and return 401.
*
* Applied defensively to all providers to ensure the admin-panel-private
* parameters never reach any Passport strategy.
*/
declare function stripCodeChallenge(req: PkceStrippableRequest): void;
/**
* Stores the admin-panel PKCE challenge in cache, then strips admin-panel-only
* params from the request so they don't interfere with the Passport strategy.
*
* Must be called before `passport.authenticate()` — the two operations are
* logically atomic: read the challenge from the query, persist it, then remove
* those parameters from the request URL.
* @param cache - The Keyv cache instance for storing PKCE challenges.
* @param req - The Express request to read and mutate.
* @param state - The OAuth state value (cache key).
* @param provider - Provider name for logging.
* @returns True if stored (or no challenge provided); false on cache failure.
*/
declare function storeAndStripChallenge(cache: Keyv, req: PkceStrippableRequest, state: string, provider: string): Promise<boolean>;
/**
* Checks if the redirect URI targets the admin panel.
*
* Supported cases:
* - Cross-origin admin panel: redirect origin must match admin origin.
* - Same-origin admin panel under a subpath: redirect path must be within
*   the configured admin subpath.
*
* @param redirectUri - The redirect URI to check.
* @param adminPanelUrl - The admin panel URL (defaults to ADMIN_PANEL_URL env var)
* @param domainClient - The main client domain
* @returns True if redirecting to admin panel.
*/
declare function isAdminPanelRedirect(redirectUri: string, adminPanelUrl: string, domainClient: string): boolean;
//#endregion
//#region src/auth/refresh.d.ts
interface AdminRefreshClaims {
  sub?: string;
  iss?: string;
}
/**
* The minimal shape this module needs from an `openid-client` tokenset.
* Avoids a hard import dependency on `openid-client` types in this package.
*/
interface RefreshTokenset {
  access_token?: string;
  refresh_token?: string;
  claims: () => AdminRefreshClaims;
}
interface OpenIDRefreshParams {
  scope?: string;
  audience?: string;
}
interface MintedToken {
  /** Bearer the admin panel will send on subsequent requests. */
  token: string;
  /** Absolute expiry of `token` (ms epoch). Drives the admin panel's proactive refresh. */
  expiresAt: number;
}
interface AdminRefreshDeps {
  findUsers: (filter: FilterQuery<IUser>, projection: string, options: {
    sort: Record<string, 1 | -1>;
    limit: number;
  }) => Promise<IUser[]>;
  getUserById: (id: string, projection: string) => Promise<IUser | null>;
  /**
  * Mints the bearer the admin panel will send on subsequent requests, and
  * reports its absolute expiry. The minter is authoritative for the bearer's
  * lifetime — the helper does not attempt to derive expiry from the IdP's
  * `exp` claim. Default OSS callers pass `generateToken(user, sessionExpiry)`
  * and `Date.now() + sessionExpiry`.
  */
  mintToken: (user: IUser, tokenset: RefreshTokenset) => Promise<MintedToken>;
  /**
  * Authorizes the resolved user against the same admin-access invariant
  * the initial OAuth callback enforces. The IdP refresh token can outlive
  * a capability/role change, so the bearer must not be reissued for a
  * user who no longer holds `ACCESS_ADMIN`. Optional only for backward
  * compatibility with existing callers; route handlers that mint admin
  * bearers should always inject this.
  */
  canAccessAdmin?: (user: IUser) => Promise<boolean>;
  /**
  * Re-runs the deployment's `registration.allowedDomains` check against the
  * resolved user's email. Returns true to allow refresh, false to reject.
  * Mirrors the domain check the initial OAuth callback enforces so a domain
  * removed from the allowlist after issuance can't refresh.
  */
  isEmailAllowed?: (user: IUser) => Promise<boolean>;
  /**
  * Optional post-success hook for forks that need to do additional work
  * with the refreshed tokenset and resolved user (e.g. update a server-side
  * token cache or reconcile downstream session state). Errors thrown here
  * propagate to the route handler.
  */
  onRefreshSuccess?: (user: IUser, tokenset: RefreshTokenset) => Promise<void>;
}
interface AdminRefreshOptions {
  /**
  * Optional user `_id` from the previous admin-panel session. When provided,
  * the resolved user must have a matching `openidId` — otherwise the refresh
  * is rejected as a possible identity-swap attempt.
  */
  userId?: string;
  /**
  * The refresh token the admin panel sent in. Preserved as the response
  * `refreshToken` when the IdP doesn't rotate (Auth0 with rotation off,
  * Microsoft personal accounts in some flows). Without this fallback, the
  * admin panel would receive `undefined` and lose its refresh capability.
  */
  previousRefreshToken?: string;
  /**
  * Issuer URL of the OpenID provider this server trusts. When provided
  * alongside an `iss` claim on the refreshed tokenset, the helper rejects
  * the refresh if the two don't match. Defends against tokensets emitted
  * by an unexpected issuer, even though `IUser` lookup is still keyed by
  * `openidId` alone.
  */
  expectedIssuer?: string;
  /**
  * Trusted tenant id resolved by the route layer (e.g. `getTenantId()` from
  * the pre-auth tenant ALS scope). When provided, the helper:
  *   - constrains the fallback `findUsers` lookup with `tenantId`, and
  *   - asserts a direct `getUserById` result has a matching `tenantId`,
  * so the same `(sub, iss)` existing in another tenant can never resolve
  * here. Multi-tenant deployments MUST pass this. Single-tenant deployments
  * may omit it.
  */
  tenantId?: string;
}
declare class AdminRefreshError extends Error {
  readonly code: string;
  readonly status: number;
  constructor(code: string, status: number, message: string);
}
declare function buildOpenIDRefreshParams(): OpenIDRefreshParams;
/**
* Looks up the active admin user for a freshly-refreshed OpenID tokenset,
* mints the bearer the admin panel should send on subsequent requests, and
* returns the response shape used by `/api/admin/oauth/exchange`.
*
* The route handler is responsible for the IdP `refreshTokenGrant` call;
* this helper takes the resulting tokenset and turns it into an admin-panel
* exchange response.
*/
declare function applyAdminRefresh(tokenset: RefreshTokenset, deps: AdminRefreshDeps, options?: AdminRefreshOptions): Promise<AdminExchangeResponse>;
//#endregion
//#region src/auth/googleRefresh.d.ts
interface MintedGoogleAdminToken {
  token: string;
  expiresAt: number;
}
interface GoogleAdminRefreshDeps {
  findUsers: (filter: FilterQuery<IUser>, projection: string, options: {
    sort: Record<string, 1 | -1>;
    limit: number;
  }) => Promise<IUser[]>;
  getUserById: (id: string, projection: string) => Promise<IUser | null>;
  canAccessAdmin: (user: IUser) => Promise<boolean>;
  /**
  * Re-runs the deployment's `registration.allowedDomains` check against the
  * resolved user's email. Returns true to allow refresh, false to reject.
  * Mirrors the `isEmailDomainAllowed` call the initial OAuth login enforces
  * so a domain removed from the allowlist after issuance can't refresh.
  */
  isEmailAllowed?: (user: IUser) => Promise<boolean>;
  mintToken: (user: IUser) => Promise<MintedGoogleAdminToken>;
}
interface GoogleAdminRefreshOptions {
  refreshToken: string;
  userId?: string;
  tenantId?: string;
  clientId?: string;
  clientSecret?: string;
}
/**
* Refresh a Google admin OAuth session.
*
* Mirrors the OpenID admin refresh contract from `applyAdminRefresh` but
* speaks Google's OAuth 2.0 refresh-token grant. Calls Google's token
* endpoint, resolves the user's `sub` (preferring an `id_token` claim, with
* a userinfo-endpoint fallback per Google's documented behavior of returning
* id_token only conditionally on refresh), looks up the admin by `googleId`,
* enforces tenant + `ACCESS_ADMIN`, and mints a fresh TerraMind JWT in the
* same response shape as `/api/admin/oauth/exchange`.
*/
declare function applyGoogleAdminRefresh(deps: GoogleAdminRefreshDeps, options: GoogleAdminRefreshOptions): Promise<AdminExchangeResponse>;
//#endregion
//#region src/auth/agent.d.ts
interface SSRFProtectionOptions {
  /** Also reject IP-literal hosts, covering literal destinations and literal redirect targets. */
  blockLiteralHosts?: boolean;
}
/**
* Creates HTTP and HTTPS agents that block TCP connections to private/reserved IP addresses.
* Provides TOCTOU-safe SSRF protection by validating the resolved IP at connect time,
* preventing DNS rebinding attacks where a hostname resolves to a public IP during
* pre-validation but to a private IP when the actual connection is made.
*
* @param allowedAddresses - Optional admin exemption list of host:port pairs that bypass the block.
* @param agentOptions - Agent options, e.g. `{ keepAlive: true }` to retain pooling that the
*   default global agents provide and a bare `new http.Agent()` does not.
*/
declare function createSSRFSafeAgents(allowedAddresses?: string[] | null, agentOptions?: (http$1.AgentOptions & https.AgentOptions) | null, protection?: SSRFProtectionOptions): {
  httpAgent: http$1.Agent;
  httpsAgent: https.Agent;
};
/**
* Returns undici-compatible `connect` options with SSRF-safe DNS lookup.
* Pass the result as the `connect` property when constructing an undici `Agent`.
*
* @param allowedAddresses - Optional admin exemption list of host:port pairs that bypass the block.
*/
declare function createSSRFSafeUndiciConnect(allowedAddresses?: string[] | null, port?: string | number | null): {
  lookup: LookupFunction;
};
/**
* Attaches SSRF-safe HTTP(S) agents to an axios config for a direct request.
* Rejects non-http(s) (and unparseable) target urls, since the agents validate
* the resolved IP at connect time but never inspect the scheme. Sets
* `maxRedirects: 0` unconditionally so a redirect cannot bypass that check, and
* leaves the agents untouched when a proxy or agent is already set.
*
* @param config - The axios request config to mutate.
* @param url - The request target URL (http/https only).
* @param allowedAddresses - Optional admin exemption list of host:port pairs.
*/
declare function applySSRFSafeAgentIfDirect(config: AxiosRequestConfig, url: string, allowedAddresses?: string[] | null): AxiosRequestConfig;
//#endregion
//#region src/auth/password.d.ts
interface UserWithPassword {
  password?: string;
  [key: string]: unknown;
}
interface ComparePasswordDeps {
  compare: (candidatePassword: string, hash: string) => Promise<boolean>;
}
/** Compares a candidate password against a user's hashed password. */
declare function comparePassword(user: UserWithPassword, candidatePassword: string, deps: ComparePasswordDeps): Promise<boolean>;
//#endregion
//#region src/auth/invite.d.ts
interface InviteDeps {
  createToken: (data: {
    userId: Types.ObjectId;
    email: string;
    token: string;
    createdAt: number;
    expiresIn: number;
  }) => Promise<unknown>;
  findToken: (filter: {
    token: string;
    email: string;
  }) => Promise<unknown>;
}
/** Creates a new user invite and returns the encoded token. */
declare function createInvite(email: string, deps: InviteDeps): Promise<string | {
  message: string;
}>;
/** Retrieves and validates a user invite by encoded token and email. */
declare function getInvite(encodedToken: string, email: string, deps: InviteDeps): Promise<unknown>;
//#endregion
//#region src/auth/codeapi.d.ts
declare function assertCodeApiJwtSigningReady(): void;
declare function getCodeApiTenantId(req: ServerRequest): string;
declare function isCodeApiJwtAuthEnabled(): boolean;
declare function mintCodeApiToken(req: ServerRequest, codeWorkerId?: string): Promise<string>;
declare function getCodeApiAuthHeaders(req?: ServerRequest, codeWorkerId?: string): Promise<Record<string, string>>;
//#endregion
//#region src/auth/openidRoleSync.d.ts
type OpenIdRoleSyncClaimSource = "access" | "id" | "userinfo";
type OpenIdRoleSyncOptions = {
  enabled: boolean;
  apiEnabled: boolean;
  claimSource: OpenIdRoleSyncClaimSource;
  claim?: string;
  rolePriority: string[];
  fallbackRole?: string;
};
type OpenIdRoleSyncSelectionInput = {
  currentRole?: string;
  openIdRoleValues?: string | unknown[];
  rolePriority: string[];
  fallbackRole?: string;
};
type OpenIdRoleSyncSelectionResult = {
  selectedRole?: string;
  reason?: "matched_priority" | "kept_current" | "fallback" | "no_matching_role";
};
type OpenIdRolesForOpenIdSyncInput = {
  options: OpenIdRoleSyncOptions;
  accessToken?: string;
  accessClaims?: unknown;
  idToken?: string;
  claims?: unknown;
  userinfo?: unknown;
  decodeToken: (token: string) => unknown;
  resolveGroupOverage?: () => Promise<string[] | null>;
};
type OpenIdRoleSyncRolesLookup = (roleNames: string[], fieldsToSelect?: string | string[] | null) => Promise<Array<{
  name?: string | null;
}> | null | undefined>;
type TerraMindRolesForOpenIdSyncInput = {
  rolePriority: string[];
  fallbackRole?: string;
  getRolesByNames: OpenIdRoleSyncRolesLookup;
  logPrefix?: string;
};
type TerraMindRolesForOpenIdSync = {
  rolePriority: string[];
  fallbackRole?: string;
};
/**
* Reads and validates the OPENID_ROLE_SYNC_* environment configuration.
*/
declare function getOpenIdRoleSyncOptions(env?: NodeJS.ProcessEnv): OpenIdRoleSyncOptions;
/**
* Extracts the configured role claim from the configured OpenID source.
* Callers provide token decoding and optional group-overage resolution.
*/
declare function getOpenIdRolesForOpenIdSync({
  options,
  accessToken,
  accessClaims,
  idToken,
  claims,
  userinfo,
  decodeToken,
  resolveGroupOverage
}: OpenIdRolesForOpenIdSyncInput): Promise<string | unknown[] | undefined>;
/**
* Gets the configured TerraMind roles that OpenID role sync is allowed to assign.
* The names are read from config, checked against storage, and returned as canonical role names.
*/
declare function getTerraMindRolesForOpenIdSync(input: TerraMindRolesForOpenIdSyncInput): Promise<TerraMindRolesForOpenIdSync>;
/**
* Chooses the TerraMind role to assign from normalized OpenID token values and validated config.
* Priority roles win first, then the current fallback role can be preserved, then fallback applies.
*/
declare function selectOpenIdRole(input: OpenIdRoleSyncSelectionInput): OpenIdRoleSyncSelectionResult;
//#endregion
//#region src/auth/userDocCache.d.ts
declare const AUTH_USER_DOC_CACHE_TTL_MS = 5e3;
type AuthUserDocCacheMode = "off" | "on";
interface AuthUserDocCacheStore {
  get: <T = unknown>(key: string) => Promise<T | undefined>;
  set: (key: string, value: unknown, ttl?: number) => Promise<unknown>;
  delete: (key: string) => Promise<unknown>;
}
interface AuthUserDocCacheKeyInput {
  strategy: string;
  subject?: string;
  issuer?: string;
  tenantId?: string;
  userId?: string;
  scope?: string;
}
type CachedAuthUser = Omit<Partial<IUser>, "_id"> & {
  _id?: string;
  id?: string;
};
declare function getAuthUserDocCacheTtlMs(): number;
declare function getAuthUserDocCacheMode(): AuthUserDocCacheMode;
declare function buildAuthUserDocCacheKey(input: AuthUserDocCacheKeyInput): string | undefined;
declare function buildAuthUserDocReverseIndexKey(userId: string): string;
declare function getCachedAuthUserDoc(store: AuthUserDocCacheStore, cacheKey: string): Promise<CachedAuthUser | undefined>;
declare function setCachedAuthUserDoc(store: AuthUserDocCacheStore, cacheKey: string, user: Partial<IUser>): Promise<void>;
declare function invalidateCachedAuthUserDoc(store: AuthUserDocCacheStore | undefined, input: {
  userId?: string;
  cacheKey?: string;
}): Promise<void>;
//#endregion
//#region src/auth/reuse.d.ts
declare function getValidOpenIdReuseUserId(openidUserId: string | undefined, secret?: string | undefined): string | null;
//#endregion
//#region src/auth/openid/types.d.ts
type TokenPreference = "access_token" | "id_token";
type AsyncVoidCallback = (error?: Error | null) => void;
type LeaseAssertion = () => Promise<object | null | boolean>;
type LogArgument = string | number | boolean | Error | object | null | undefined;
interface OpenIDPublicationGeneration {
  key: string;
  ownerId: string;
  createdAt?: number;
}
interface OpenIDClaims {
  sub: string;
  oid?: string;
  email?: string;
  iss?: string;
}
interface OpenIDTokenSet extends OIDCTokens$1 {
  access_token?: string;
  expires_in?: number | string;
  claims?: () => OpenIDClaims;
  /**
  * Set non-enumerably when an inline refresh strips an expired carried-forward `id_token` from
  * the result. It is identity material only — never an authentication response token.
  */
  __identityIdToken?: string;
  /** Serializable identity evidence for shared-flight followers. */
  __identityClaims?: OpenIDClaims;
  /** Access token that the shared candidate advanced from. */
  __predecessorAccessToken?: string;
}
interface SharedOpenIDRefreshResult {
  tokenset: OpenIDTokenSet;
  claims: OpenIDClaims;
  openidIssuer?: string;
  expires_at?: number;
  appAuthToken: string;
  predecessorAccessToken?: string;
  acceptedIdentity?: AuthIdentityContext;
  /** Non-enumerable durable generation marker restored by the flight service. */
  __flightOwnerId?: string;
  __flightCreatedAt?: number;
}
interface SessionOpenIDTokens {
  accessToken?: string;
  idToken?: string;
  refreshToken?: string;
  browserRefreshToken?: string;
  expiresAt?: number;
  lastRefreshedAt?: number;
  appUserId?: string;
  openidSubject?: string;
  tenantId?: string;
  openidIssuer?: string;
  accessTokenExpiresAt?: number;
  /** Durable coordination generation that authorized this session publication. */
  publicationFlightKey?: string;
  publicationFlightOwnerId?: string;
  publicationFlightCreatedAt?: number;
}
interface OpenIDSession {
  openidTokens?: SessionOpenIDTokens;
  save?: (callback: AsyncVoidCallback) => void;
  reload?: (callback: AsyncVoidCallback) => void;
  destroy?: (callback: AsyncVoidCallback) => void;
}
interface OpenIDRequest {
  headers?: {
    authorization?: string;
    cookie?: string;
  };
  session?: OpenIDSession;
  sessionID?: string;
  user?: OpenIDUser;
}
interface OpenIDResponse {
  headersSent?: boolean;
  cookie?: (name: string, value: string, options?: {
    expires?: Date;
  }) => void;
  clearCookie?: (name: string) => void;
}
interface OpenIDUser extends AuthIdentitySource {
  _id?: string | number | {
    toString(): string;
  };
  id?: string;
  email?: string;
  provider?: string;
  openidId?: string;
  tenantId?: string;
  openidIssuer?: string;
  federatedTokens?: OIDCTokens$1;
}
interface OpenIDRefreshResolution {
  tokenset: OpenIDTokenSet;
  claims: OpenIDClaims;
  openidIssuer?: string;
  user?: OpenIDUser | null;
  error?: string | null;
  migration?: boolean;
}
interface OpenIDLogger {
  debug: (...args: LogArgument[]) => void;
  info: (...args: LogArgument[]) => void;
  warn: (...args: LogArgument[]) => void;
  error: (...args: LogArgument[]) => void;
}
interface LeaseContext {
  assertLeaseOwned: LeaseAssertion;
  markLeaseSettled: () => void;
}
interface RefreshFlightAcquireResult {
  acquired: boolean;
  key: string | null;
  ownerId: string;
  flight?: RefreshFlightRecord | null;
}
interface RefreshFlightRecord {
  status?: "pending" | "completed" | "failed" | "revoked";
  ownerId?: string;
  createdAt?: Date | string;
  deliveryId?: string;
  deliveryExpiresAt?: Date | string;
  revocationRequestedAt?: Date | string;
  encryptedResult?: string;
  errorMessage?: string;
  expiresAt?: Date | string;
}
interface RefreshTokenBridgeInput {
  oldRefreshToken: string;
  newRefreshToken: string;
  userId: string;
  tenantId?: string;
  openidIssuer?: string;
  ttl?: number;
}
interface RefreshTokenBridgeDeleteInput {
  refreshTokens: string[];
  userId: string;
  tenantId?: string;
  version?: string;
}
interface RefreshKeyInput {
  req?: OpenIDRequest;
  user?: OpenIDUser;
  refreshToken?: string;
  identityContext?: AuthIdentityContext;
}
//#endregion
//#region src/auth/openid/errors.d.ts
declare const OPENID_REFRESH_OWNERSHIP_LOST = "OPENID_REFRESH_OWNERSHIP_LOST";
declare const OPENID_REFRESH_CANCELLED_BEFORE_GRANT = "OPENID_REFRESH_CANCELLED_BEFORE_GRANT";
/** Express-session uses this exact error when reload cannot find the persisted session. */
declare function isOpenIDSessionMissingError(error: unknown): boolean;
/**
* Reloads the persisted Express session before a publication decision. express-session rejects with
* `failed to load session` when the record is merely absent — the session-store TTL elapsed, the
* entry was evicted, or a logout in another replica removed it — which describes an empty session
* rather than one that advanced past this result. Tolerate that case so a refresh backed by a
* still-valid refresh token seeds a new record instead of demanding an interactive sign-in, and let
* every other store failure (an outage) propagate to the caller.
*
* Logout safety does not depend on this record: `revokeOpenIDRefreshTokenChain` writes a durable
* revoked publication flight for every refresh token it retires, so a retired token still fails.
*
* @returns `true` when the persisted record was read, `false` when it was gone.
*/
declare function reloadOpenIDSessionIfPersisted(session?: {
  reload?: (callback: (error?: Error | null) => void) => void;
} | null): Promise<boolean>;
declare function toOpenIDLogArgument(error: unknown): LogArgument;
/**
* Marks the one failure mode that proves another worker owns this refresh: the coordination
* record is no longer pending under our owner id. Callers that compensate a published side
* effect must distinguish it from a transient coordination read failure, where ownership is
* merely unknown and the side effect is still the caller's only usable credential.
*/
declare function createOpenIDRefreshOwnershipError(message: string): Error;
declare function isOpenIDRefreshOwnershipError(error: unknown): boolean;
//#endregion
//#region src/auth/openid/flight.d.ts
interface TokenResult extends Omit<OpenIDTokenSet, "claims"> {
  tokenset?: OpenIDTokenSet;
  claims?: OpenIDClaims | (() => OpenIDClaims);
  openidIssuer?: string;
  __browserRefreshToken?: string;
  __predecessorRefreshToken?: string;
  __predecessorAccessToken?: string;
  __deferredPublication?: boolean;
  __flightOwnerId?: string;
  __flightCreatedAt?: number;
  predecessorAccessToken?: string;
  acceptedIdentity?: AuthIdentityContext;
}
interface FlightAcquireData {
  key: string;
  ownerId: string;
  lockExpiresAt: Date;
  expiresAt: Date;
}
interface FlightOwnerData {
  key: string;
  ownerId: string;
  expiresAt: Date;
}
interface FlightCompleteData extends FlightOwnerData {
  encryptedResult: string;
}
interface FlightRenewData extends FlightOwnerData {
  lockExpiresAt: Date;
}
interface FlightFailData extends FlightOwnerData {
  errorMessage: string;
}
interface FlightDeliveryData {
  key: string;
  ownerId: string;
  deliveryId: string;
}
interface OpenIDRefreshFlightService {
  acquireOpenIDRefreshFlight: (args: {
    key?: string | null;
    ownerId?: string;
    ttl?: number;
    lockTtl?: number;
  }) => Promise<RefreshFlightAcquireResult>;
  completeOpenIDRefreshFlight: (args: {
    key?: string | null;
    ownerId?: string;
    tokens?: TokenResult | null;
    ttl?: number;
    onWriteStart?: () => void;
  }) => Promise<RefreshFlightRecord | null>;
  createOpenIDRefreshFlightKey: (input: RefreshKeyInput) => string | null;
  failOpenIDRefreshFlight: (args: {
    key?: string | null;
    ownerId?: string;
    error?: Error | {
      message?: string;
    } | null;
    ttl?: number;
  }) => Promise<RefreshFlightRecord | null>;
  renewOpenIDRefreshFlight: (args: {
    key?: string | null;
    ownerId?: string;
    lockTtl?: number;
    ttl?: number;
  }) => Promise<RefreshFlightRecord | null>;
  assertOpenIDRefreshFlightAvailable: (args: {
    key?: string | null;
    ownerId?: string;
  }) => Promise<RefreshFlightRecord | boolean>;
  assertOpenIDRefreshSessionGenerationAvailable: (args: {
    key?: string | null;
    ownerId?: string;
  }) => Promise<RefreshFlightRecord | boolean>;
  claimOpenIDRefreshFlightDelivery: (args: {
    key: string;
    ownerId: string;
    createdAt?: number;
    deliveryId?: string;
    ttl?: number;
  }) => Promise<RefreshFlightRecord>;
  assertOpenIDRefreshFlightDeliveryAvailable: (args: FlightDeliveryData) => Promise<void>;
  releaseOpenIDRefreshFlightDelivery: (args: FlightDeliveryData) => Promise<void>;
  revokeOpenIDRefreshFlights: (args: {
    keys?: Array<string | null | undefined>;
    ttl?: number;
  }) => Promise<Array<TokenResult | null>>;
  waitForOpenIDRefreshFlight: (args: {
    key?: string | null;
    timeoutMs?: number;
    intervalMs?: number;
    requirePublication?: boolean;
    signal?: AbortSignal;
  }) => Promise<TokenResult | null>;
  withOpenIDRefreshFlightLease: <T>(args: {
    key?: string | null;
    ownerId?: string;
    operation: (context: LeaseContext) => Promise<T>;
    heartbeatInterval?: number;
    lockTtl?: number;
    ttl?: number;
  }) => Promise<T>;
  __internals: {
    sha256: (value: string) => string;
    readCompletedFlight: (flight: RefreshFlightRecord | null) => Promise<TokenResult | null>;
    DEFAULT_FLIGHT_TTL_MS: number;
    DEFAULT_LOCK_TTL_MS: number;
    DEFAULT_WAIT_TIMEOUT_MS: number;
    DEFAULT_WAIT_INTERVAL_MS: number;
    DEFAULT_HEARTBEAT_INTERVAL_MS: number;
    DEFAULT_DELIVERY_TTL_MS: number;
    INTERNAL_PREDECESSOR_REFRESH_TOKEN_FIELD: string;
    getRenewedWaitDeadline: (deadline: number, flight: RefreshFlightRecord | null) => number;
  };
}
interface OpenIDRefreshFlightDeps {
  db: {
    acquireOpenIDRefreshFlight: (data: FlightAcquireData) => Promise<{
      acquired: boolean;
      flight?: RefreshFlightRecord | null;
    }>;
    completeOpenIDRefreshFlight: (data: FlightCompleteData) => Promise<RefreshFlightRecord | null>;
    renewOpenIDRefreshFlight: (data: FlightRenewData) => Promise<RefreshFlightRecord | null>;
    failOpenIDRefreshFlight: (data: FlightFailData) => Promise<RefreshFlightRecord | null>;
    revokeOpenIDRefreshFlight: (data: {
      key: string;
      expiresAt: Date;
    }) => Promise<RefreshFlightRecord | null>;
    findOpenIDRefreshFlight: (data: {
      key: string;
    }) => Promise<RefreshFlightRecord | null>;
    claimOpenIDRefreshFlightDelivery: (data: FlightDeliveryData & {
      deliveryExpiresAt: Date;
      createdAt?: Date;
    }) => Promise<RefreshFlightRecord | null>;
    releaseOpenIDRefreshFlightDelivery: (data: FlightDeliveryData) => Promise<RefreshFlightRecord | null>;
  };
  logger: Pick<OpenIDLogger, "warn">;
  encrypt: (value: string) => Promise<string>;
  decrypt: (value: string) => Promise<string>;
}
declare function createOpenIDRefreshFlightService({
  db,
  logger,
  encrypt,
  decrypt
}: OpenIDRefreshFlightDeps): OpenIDRefreshFlightService;
//#endregion
//#region src/auth/openid/bridge.d.ts
interface StoredRefreshTokenBridge {
  encryptedNewRefreshToken: string;
  userId: string;
  tenantId?: string;
  openidIssuer?: string;
  version?: string;
  createdAt: Date | string;
}
interface BridgeQuery {
  oldRefreshTokenHash?: string;
  oldRefreshTokenHashes?: string[];
  encryptedNewRefreshToken?: string;
  userId: string;
  tenantId?: string;
  openidIssuer?: string;
  version?: string;
  expiresAt?: Date;
}
type IdentityInput = Partial<RefreshTokenBridgeIdentity>;
interface RefreshTokenBridgeService {
  OPENID_REFRESH_BRIDGE_GRACE_MS: number;
  createRefreshTokenBridgeFlightKey: (args: {
    oldRefreshToken?: string;
    userId?: string;
    tenantId?: string;
    openidIssuer?: string;
  }) => string | null;
  deleteAllRefreshTokenBridges: (args: {
    userId?: string;
    tenantId?: string;
  }) => Promise<object | null>;
  deleteRefreshTokenBridges: (args: RefreshTokenBridgeDeleteInput) => Promise<object | null>;
  storeRefreshTokenBridge: (args: RefreshTokenBridgeInput) => Promise<string | null>;
  getRefreshTokenBridge: (args: {
    oldRefreshToken?: string;
    userId?: string;
    tenantId?: string;
    openidIssuer?: string;
  }) => Promise<string | null>;
  __internals: {
    hashRefreshToken: (refreshToken: string) => string;
    getBridgeTtlMs: () => number;
    resolveBridgeIdentity: (input: IdentityInput) => RefreshTokenBridgeIdentity | null;
  };
}
interface RefreshTokenBridgeDeps {
  db: {
    upsertRefreshTokenBridge: (data: BridgeQuery) => Promise<StoredRefreshTokenBridge | null>;
    findRefreshTokenBridge: (data: BridgeQuery) => Promise<StoredRefreshTokenBridge | null>;
    deleteRefreshTokenBridges: (data: BridgeQuery) => Promise<object>;
  };
  logger: Pick<OpenIDLogger, "warn" | "debug" | "info">;
  encrypt: (value: string) => Promise<string>;
  decrypt: (value: string) => Promise<string>;
  math: (value: string | undefined, fallback: number) => number;
  defaultRefreshTokenExpiry: number;
  createIdentity: (data: IdentityInput) => RefreshTokenBridgeIdentity | null;
}
declare function createRefreshTokenBridgeService(deps: RefreshTokenBridgeDeps): RefreshTokenBridgeService;
//#endregion
//#region src/auth/openid/recovery.d.ts
type FindUser = (...args: Array<string | object | undefined>) => Promise<OpenIDUser | null>;
interface FindOpenIDUserArgs {
  findUser: FindUser;
  email: string;
  openidId: string;
  openidIssuer?: string;
  idOnTheSource?: string;
  strategyName: string;
}
interface RefreshOpenIDUserArgs {
  req: OpenIDRequest;
  res?: OpenIDResponse;
  user: OpenIDUser;
  refreshToken: string;
  browserRefreshToken?: string;
  strategyName: string;
  assertLeaseOwned?: LeaseAssertion;
  deferPublication?: boolean;
}
interface BridgeUser extends OpenIDUser {
  _id: string | number | {
    toString(): string;
  };
}
interface ResolveOpenIDRefreshInput {
  tokenset: OpenIDTokenSet | null;
  strategyName: string;
}
type SeedRefreshSessionInput = Omit<RefreshOpenIDUserArgs, "strategyName" | "assertLeaseOwned" | "deferPublication">;
interface RecoverOpenIDRefreshBridgeInput {
  req: OpenIDRequest;
  res: OpenIDResponse;
  refreshToken: string;
  bridgedRefreshToken: string;
  bridgeUser: BridgeUser;
}
interface RevokeOpenIDRefreshTokenChainInput {
  req: OpenIDRequest;
  user: OpenIDUser;
  identityContext: AuthIdentityContext;
  refreshTokens: string[];
  publicationKeys?: string[];
  ttl: number;
}
interface SendOpenIDAuthResponseInput {
  tokenset: OpenIDTokenSet;
  user: BridgeUser;
  existingRefreshToken?: string;
  openidSubject?: string;
  openidIssuer?: string;
  predecessorIdentity?: RefreshTokenBridgeIdentity;
  predecessorAccessToken?: string;
  rejectedRefreshTokens?: string[];
  req: OpenIDRequest;
  res: OpenIDResponse;
  assertLeaseOwned?: LeaseAssertion;
  publicationGeneration?: OpenIDPublicationGeneration;
  commitPublication?: (appAuthToken: string, publishedTokenset: OpenIDTokenSet, metadata: {
    predecessorAccessToken?: string;
    acceptedIdentity: AuthIdentityContext;
  }) => Promise<void>;
  preparePublication?: boolean;
  /**
  * A fresh authorization-code login supersedes whatever token set the Express session still
  * holds from an earlier authentication. The advanced-session comparison exists for refresh
  * races and must not republish that stale set in place of the tokens the IdP just issued.
  */
  discardSessionTokens?: boolean;
}
interface OpenIDRefreshRecoveryService {
  recoverOpenIDRefreshBridge: (input: RecoverOpenIDRefreshBridgeInput) => Promise<SharedOpenIDRefreshResult>;
  refreshOpenIDUser: (input: RefreshOpenIDUserArgs) => Promise<OpenIDRefreshResolution>;
  resolveOpenIDRefreshResult: (input: ResolveOpenIDRefreshInput) => Promise<OpenIDRefreshResolution>;
  sendOpenIDAuthResponse: (input: SendOpenIDAuthResponseInput) => Promise<string | undefined>;
  revokeOpenIDRefreshTokenChain: (input: RevokeOpenIDRefreshTokenChainInput) => Promise<string[]>;
  __internals: {
    getTokenClaims: (tokenset: OpenIDTokenSet) => OpenIDClaims;
    seedRefreshSession: (input: SeedRefreshSessionInput) => AuthIdentityContext;
  };
}
interface OpenIDRefreshRecoveryDeps {
  jwt: {
    decode: (token: string) => OpenIDClaims | string | null;
  };
  logger: Pick<OpenIDLogger, "debug" | "warn">;
  findOpenIDUser: (args: FindOpenIDUserArgs) => Promise<{
    user?: OpenIDUser | null;
    error?: string | null;
    migration?: boolean;
  }>;
  findUser: FindUser;
  getOpenIdConfig: () => object;
  getOpenIdEmail: (claims: OpenIDClaims) => string;
  getOpenIdIssuer: (claims: OpenIDClaims, config: object) => string | undefined;
  createAuthIdentityContext: (args: {
    user?: OpenIDUser;
    requestUser?: OpenIDUser;
  }) => AuthIdentityContext;
  refreshOpenIDSession: (req: OpenIDRequest, res: OpenIDResponse | undefined, user: OpenIDUser, preference: TokenPreference, identity: AuthIdentityContext, options: {
    forceRefresh: boolean;
    assertLeaseOwned?: LeaseAssertion;
    deferPublication?: boolean;
  }) => Promise<OpenIDTokenSet | null>;
  storeOpenIDSession: (userId: string, refreshToken: string, tenantId?: string, previousRefreshToken?: string) => Promise<void>;
  setOpenIDAuthTokens: (tokens: OpenIDTokenSet, req: OpenIDRequest, res: OpenIDResponse, identity: {
    userId: string;
    existingRefreshToken?: string;
    tenantId?: string;
    openidSubject?: string;
    openidIssuer?: string;
  }) => string | undefined;
  getOpenIDAppAuthToken: (tokens: OpenIDTokenSet, sessionIdToken?: string) => string | undefined;
  clearOpenIDAuthTokens: (req: OpenIDRequest, res: OpenIDResponse, userId: string, tenantId?: string) => void;
  deleteOpenIDSession: (refreshToken: string) => Promise<object | null>;
  createRefreshTokenBridgeFlightKey: (args: {
    oldRefreshToken: string;
    userId: string;
    tenantId?: string;
    openidIssuer?: string;
  }) => string | null;
  createOpenIDRefreshFlightKey: (args: {
    req: OpenIDRequest;
    user: OpenIDUser;
    refreshToken: string;
    identityContext: AuthIdentityContext;
  }) => string | null;
  storeRefreshTokenBridge: (args: RefreshTokenBridgeInput) => Promise<string | null>;
  deleteRefreshTokenBridges: (args: RefreshTokenBridgeDeleteInput) => Promise<object | null>;
  acquireOpenIDRefreshFlight: (args: {
    key: string;
  }) => Promise<RefreshFlightAcquireResult>;
  completeOpenIDRefreshFlight: (args: {
    key: string;
    ownerId: string;
    tokens: SharedOpenIDRefreshResult;
    onWriteStart?: () => void;
  }) => Promise<RefreshFlightRecord | null>;
  failOpenIDRefreshFlight: (args: {
    key: string;
    ownerId: string;
    error: Error;
  }) => Promise<RefreshFlightRecord | null>;
  waitForOpenIDRefreshFlight: (args: {
    key: string;
  }) => Promise<SharedOpenIDRefreshResult | null>;
  assertOpenIDRefreshFlightAvailable: (args: {
    key: string;
    ownerId: string;
  }) => Promise<RefreshFlightRecord | boolean>;
  assertOpenIDRefreshSessionGenerationAvailable: (args: {
    key?: string | null;
    ownerId?: string;
  }) => Promise<RefreshFlightRecord | boolean>;
  revokeOpenIDRefreshFlights: (args: {
    keys: Array<string | null>;
    ttl: number;
  }) => Promise<Array<TokenResult | null>>;
  withOpenIDRefreshFlightLease: <T>(args: {
    key: string;
    ownerId: string;
    operation: (context: LeaseContext) => Promise<T>;
  }) => Promise<T>;
  bridgeGraceMs: number;
}
declare function createOpenIDRefreshRecoveryService(deps: OpenIDRefreshRecoveryDeps): OpenIDRefreshRecoveryService;
//#endregion
//#region src/images/session.d.ts
interface OpenIdSessionParams {
  userId: string;
  refreshToken?: string;
  tenantId?: string;
  previousRefreshToken?: string;
}
interface OpenIdSessionDeps {
  upsertSession: (userId: string, refreshToken: string, options: {
    expiration: Date;
    tenantId?: string;
  }) => Promise<ISession>;
  deleteSession: (params: DeleteSessionParams) => Promise<{
    deletedCount?: number;
  }>;
}
declare function storeOpenIdSession(params: OpenIdSessionParams, deps: OpenIdSessionDeps): Promise<boolean>;
//#endregion
//#region src/auth/openid/session.d.ts
interface OpenIDSessionRefreshDeps {
  jwt: {
    decode: (token: string) => (Partial<OpenIDClaims> & {
      exp?: number;
    }) | string | null;
    verify: (token: string, secret: string) => {
      id?: string;
      refreshTokenHash?: string;
    } | string;
  };
  cookies: {
    parse: (header: string) => Record<string, string>;
  };
  crypto: {
    createHash: (algorithm: string) => {
      update: (value: string) => {
        digest: (encoding: "hex" | "base64url") => string;
      };
    };
  };
  openIdClient: {
    refreshTokenGrant: (config: object, refreshToken: string, params: Record<string, string>) => Promise<OpenIDTokenSet>;
  };
  logger: OpenIDLogger;
  defaultRefreshTokenExpiry: number;
  isEnabled: (value?: string) => boolean;
  math: (value: string | undefined, fallback: number) => number;
  createAuthIdentityContext: (args: {
    user?: AuthIdentitySource | null;
    requestUser?: AuthIdentitySource | null;
    tenantId?: string;
    openidIssuer?: string;
  }) => AuthIdentityContext;
  isOpenIDSessionIdentityMatch: (session: OpenIDSessionIdentitySource, expected: OpenIDSessionIdentitySource) => boolean;
  createOpenIDRefreshIdentityTuple: (args: {
    user?: AuthIdentitySource | null;
    requestUser?: AuthIdentitySource | null;
  }) => AuthIdentityTuple | null;
  createRefreshTokenBridgeIdentity: (args: {
    user?: AuthIdentitySource | null;
    requestUser?: AuthIdentitySource | null;
    userId?: string;
    tenantId?: string;
    openidIssuer?: string;
  }) => RefreshTokenBridgeIdentity | null;
  serializeAuthIdentityTuple: (tuple: AuthIdentityTuple) => string;
  buildOpenIDRefreshParams: () => Record<string, string>;
  setRefreshTokenCookie: (res: OpenIDResponse, token: string, expires: Date) => void;
  setOpenIDMarkerCookies: (res: OpenIDResponse, args: {
    userId?: string;
    expires: Date;
    refreshExpiryMs: number;
    refreshToken: string;
  }) => void;
  storeOpenIdSession: (data: OpenIdSessionParams, methods: OpenIdSessionDeps) => Promise<boolean>;
  normalizeExpiresIn: (value?: number | string) => number | undefined;
  upsertSession: OpenIdSessionDeps["upsertSession"];
  deleteSession: OpenIdSessionDeps["deleteSession"];
  getOpenIdConfig: () => object;
  OPENID_REFRESH_BRIDGE_GRACE_MS: number;
  storeRefreshTokenBridge: (input: RefreshTokenBridgeInput) => Promise<string | null>;
  deleteRefreshTokenBridges: (input: RefreshTokenBridgeDeleteInput) => Promise<object | null>;
  acquireOpenIDRefreshFlight: (args: {
    key?: string | null;
  }) => Promise<RefreshFlightAcquireResult>;
  completeOpenIDRefreshFlight: (args: {
    key?: string | null;
    ownerId?: string;
    tokens?: TokenResult | null;
  }) => Promise<RefreshFlightRecord | null>;
  createOpenIDRefreshFlightKey: (input: RefreshKeyInput) => string | null;
  createRefreshTokenBridgeFlightKey?: (input: RefreshTokenBridgeIdentity & {
    oldRefreshToken: string;
  }) => string | null;
  failOpenIDRefreshFlight: (args: {
    key?: string | null;
    ownerId?: string;
    error?: Error | null;
  }) => Promise<RefreshFlightRecord | null>;
  waitForOpenIDRefreshFlight: (args: {
    key?: string | null;
    requirePublication?: boolean;
    signal?: AbortSignal;
    timeoutMs?: number;
    intervalMs?: number;
  }) => Promise<TokenResult | null>;
  assertOpenIDRefreshFlightAvailable: (args: {
    key?: string | null;
    ownerId?: string;
  }) => Promise<RefreshFlightRecord | boolean>;
  assertOpenIDRefreshSessionGenerationAvailable: (args: {
    key?: string | null;
    ownerId?: string;
  }) => Promise<RefreshFlightRecord | boolean>;
  withOpenIDRefreshFlightLease: <T>(args: {
    key?: string | null;
    ownerId?: string;
    operation: (context: LeaseContext) => Promise<T>;
  }) => Promise<T>;
}
interface MarkedOIDCTokens extends OIDCTokens$1 {
  __browserRefreshToken?: string;
  __identityClaims?: OpenIDClaims;
  __predecessorRefreshToken?: string;
  __predecessorAccessToken?: string;
  __deferredPublication?: boolean;
  __flightOwnerId?: string;
  __flightCreatedAt?: number;
  __identityIdToken?: string;
}
interface RefreshSessionOptions {
  forceRefresh?: boolean;
  signal?: AbortSignal;
  assertLeaseOwned?: LeaseAssertion;
  deferPublication?: boolean;
}
interface CreateOpenIDSessionTokenProviderInput {
  req?: OpenIDRequest;
  res?: OpenIDResponse;
  user?: OpenIDUser;
  tokenPreference: TokenPreference;
  identityContext?: AuthIdentityContext;
}
interface OpenIDSessionRefreshService {
  createOpenIDSessionTokenProvider: (input: CreateOpenIDSessionTokenProviderInput) => (options?: {
    forceRefresh?: boolean;
    signal?: AbortSignal;
  }) => Promise<OIDCTokens$1 | null>;
  refreshOpenIDSession: (req: OpenIDRequest, res: OpenIDResponse | undefined, user: OpenIDUser, tokenPreference: TokenPreference, identityContext?: AuthIdentityContext, options?: RefreshSessionOptions) => Promise<MarkedOIDCTokens | null>;
  __internals: {
    UPSTREAM_TOKEN_EXPIRY_BUFFER_SECONDS: number;
    inFlightRefreshes: Map<string, Promise<MarkedOIDCTokens | null>>;
    getSingleFlightKey: (req: OpenIDRequest, user: OpenIDUser, identityContext?: AuthIdentityContext) => string | null;
    isLiveSessionTokenStillValid: (sessionTokens: SessionOpenIDTokens, tokenPreference: TokenPreference) => boolean;
    getAccessTokenExp: (sessionTokens: SessionOpenIDTokens) => number | null;
  };
}
/**
* OpenID session refresh implementation. Runtime-only Express, model, and strategy dependencies
* are supplied by the thin /api wrapper; the authentication and coordination logic lives here.
*/
declare function createOpenIDSessionRefreshService(deps: OpenIDSessionRefreshDeps): OpenIDSessionRefreshService;
//#endregion
//#region src/auth/openid/retry.d.ts
type OpenIdDiscoveryEnv = {
  startupAttempts?: string;
  retryDelayMs?: string;
};
type OpenIdRegistrationOptions<TConfig> = {
  /** Runs discovery and registers the `openid` strategy; resolves `null` when discovery fails. */setupOpenId: () => Promise<TConfig | null>; /** Registers the `openidJwt` strategy for a discovered configuration. */
  registerJwtStrategy: (config: TConfig) => void;
  reuseTokens: boolean; /** Settings from `registration.openidDiscovery` in `librechat.yaml`. */
  discovery?: Partial<TOpenIdDiscoveryConfig>; /** Raw `OPENID_DISCOVERY_RETRY_*` values, used for fields the YAML leaves unset. */
  env?: OpenIdDiscoveryEnv;
};
/** Resolves discovery retry settings: YAML first, then environment, then the schema defaults. */
declare function resolveOpenIdDiscovery(discovery?: Partial<TOpenIdDiscoveryConfig>, env?: OpenIdDiscoveryEnv): TOpenIdDiscoveryConfig;
/**
* Registers the OpenID strategies, retrying discovery during startup and then in the
* background until the provider becomes reachable.
*/
declare function registerOpenIdWithRetry<TConfig>({
  setupOpenId,
  registerJwtStrategy,
  reuseTokens,
  discovery,
  env
}: OpenIdRegistrationOptions<TConfig>): Promise<void>;
//#endregion
//#region src/apiKeys/service.d.ts
interface ApiKeyServiceDependencies {
  validateAgentApiKey: AllMethods["validateAgentApiKey"];
  createAgentApiKey: AllMethods["createAgentApiKey"];
  listAgentApiKeys: AllMethods["listAgentApiKeys"];
  deleteAgentApiKey: AllMethods["deleteAgentApiKey"];
  getAgentApiKeyById: AllMethods["getAgentApiKeyById"];
  findUser: (query: {
    _id: string | Types.ObjectId;
  }) => Promise<IUser | null>;
}
interface RemoteAgentAccessResult {
  hasAccess: boolean;
  permissions: number;
  agent: {
    _id: Types.ObjectId;
    [key: string]: unknown;
  } | null;
}
declare class AgentApiKeyService {
  private deps;
  constructor(deps: ApiKeyServiceDependencies);
  validateApiKey(apiKey: string): Promise<{
    userId: Types.ObjectId;
    keyId: Types.ObjectId;
  } | null>;
  createApiKey(params: {
    userId: string | Types.ObjectId;
    name: string;
    expiresAt?: Date | null;
  }): Promise<AgentApiKeyCreateResult>;
  listApiKeys(userId: string | Types.ObjectId): Promise<AgentApiKeyListItem[]>;
  deleteApiKey(keyId: string | Types.ObjectId, userId: string | Types.ObjectId): Promise<boolean>;
  getApiKeyById(keyId: string | Types.ObjectId, userId: string | Types.ObjectId): Promise<AgentApiKeyListItem | null>;
  getUserFromApiKey(apiKey: string): Promise<IUser | null>;
}
declare function createApiKeyServiceDependencies(mongoose: typeof import("mongoose")): ApiKeyServiceDependencies;
interface GetRemoteAgentPermissionsDeps {
  getEffectivePermissions: (params: {
    userId: string;
    role?: string;
    resourceType: ResourceType;
    resourceId: string | Types.ObjectId;
  }) => Promise<number>;
}
/** AGENT owners automatically have full REMOTE_AGENT permissions */
declare function getRemoteAgentPermissions(deps: GetRemoteAgentPermissionsDeps, userId: string, role: string | undefined, resourceId: string | Types.ObjectId): Promise<number>;
declare function checkRemoteAgentAccess(params: {
  userId: string;
  role?: string;
  agentId: string;
  getAgent: (query: {
    id: string;
  }) => Promise<{
    _id: Types.ObjectId;
    [key: string]: unknown;
  } | null>;
  getEffectivePermissions: (params: {
    userId: string;
    role?: string;
    resourceType: ResourceType;
    resourceId: string | Types.ObjectId;
  }) => Promise<number>;
}): Promise<RemoteAgentAccessResult>;
//#endregion
//#region src/apiKeys/middleware.d.ts
interface ApiKeyAuthDependencies {
  validateAgentApiKey: (apiKey: string) => Promise<{
    userId: Types.ObjectId;
    keyId: Types.ObjectId;
  } | null>;
  findUser: (query: {
    _id: string | Types.ObjectId;
  }) => Promise<IUser | null>;
  isPrincipalActive: (userId: string) => Promise<boolean>;
}
interface RemoteAgentAccessDependencies {
  getAgent: (query: {
    id: string;
  }) => Promise<{
    _id: Types.ObjectId;
    [key: string]: unknown;
  } | null>;
  getEffectivePermissions: (params: {
    userId: string;
    role?: string;
    resourceType: ResourceType;
    resourceId: string | Types.ObjectId;
  }) => Promise<number>;
}
interface ApiKeyAuthRequest extends Request {
  user?: IUser & {
    id: string;
  };
  apiKeyId?: Types.ObjectId;
}
interface RemoteAgentAccessRequest extends ApiKeyAuthRequest {
  agent?: {
    _id: Types.ObjectId;
    [key: string]: unknown;
  };
  agentPermissions?: number;
}
declare function createRequireApiKeyAuth(deps: ApiKeyAuthDependencies): (req: ApiKeyAuthRequest, res: Response$2, next: NextFunction) => Promise<Response$2 | undefined>;
declare function createCheckRemoteAgentAccess(deps: RemoteAgentAccessDependencies): RequestHandler;
declare function createCheckAgentTriggerAccess(deps: RemoteAgentAccessDependencies): RequestHandler;
//#endregion
//#region src/apiKeys/handlers.d.ts
interface ApiKeyHandlerDependencies {
  createAgentApiKey: (params: {
    userId: string | Types.ObjectId;
    name: string;
    expiresAt?: Date | null;
  }) => Promise<{
    id: string;
    name: string;
    key: string;
    keyPrefix: string;
    createdAt: Date;
    expiresAt?: Date;
  }>;
  listAgentApiKeys: (userId: string | Types.ObjectId) => Promise<Array<{
    id: string;
    name: string;
    keyPrefix: string;
    lastUsedAt?: Date;
    expiresAt?: Date;
    createdAt: Date;
  }>>;
  deleteAgentApiKey: (keyId: string | Types.ObjectId, userId: string | Types.ObjectId) => Promise<boolean>;
  getAgentApiKeyById: (keyId: string | Types.ObjectId, userId: string | Types.ObjectId) => Promise<{
    id: string;
    name: string;
    keyPrefix: string;
    lastUsedAt?: Date;
    expiresAt?: Date;
    createdAt: Date;
  } | null>;
}
interface AuthenticatedRequest extends Request {
  user?: {
    id: string;
    _id: Types.ObjectId;
  };
}
declare function createApiKeyHandlers(deps: ApiKeyHandlerDependencies): {
  createApiKey: (req: AuthenticatedRequest, res: Response$2) => Promise<Response$2 | undefined>;
  listApiKeys: (req: AuthenticatedRequest, res: Response$2) => Promise<void>;
  getApiKey: (req: AuthenticatedRequest, res: Response$2) => Promise<Response$2 | undefined>;
  deleteApiKey: (req: AuthenticatedRequest, res: Response$2) => Promise<Response$2 | undefined>;
};
//#endregion
//#region src/apiKeys/permissions.d.ts
interface Principal {
  type: string;
  id: string;
  name: string;
  email?: string;
  avatar?: string;
  source?: string;
  idOnTheSource?: string;
  accessRoleId: string;
  isImplicit?: boolean;
}
interface EnricherDependencies {
  aggregateAclEntries: (pipeline: PipelineStage[]) => Promise<Record<string, unknown>[]>;
  bulkWriteAclEntries: (ops: AnyBulkWriteOperation[], options?: Record<string, unknown>) => Promise<unknown>;
  findRoleByIdentifier: (accessRoleId: string) => Promise<{
    _id: Types.ObjectId;
    permBits: number;
  } | null>;
  logger: {
    error: (msg: string, ...args: unknown[]) => void;
  };
}
interface EnrichResult {
  principals: Principal[];
  entriesToBackfill: Types.ObjectId[];
}
/** Enriches REMOTE_AGENT principals with implicit AGENT owners */
declare function enrichRemoteAgentPrincipals(deps: EnricherDependencies, resourceId: string | Types.ObjectId, principals: Principal[]): Promise<EnrichResult>;
/** Backfills REMOTE_AGENT ACL entries for AGENT owners (fire-and-forget) */
declare function backfillRemoteAgentPermissions(deps: EnricherDependencies, resourceId: string | Types.ObjectId, entriesToBackfill: Types.ObjectId[]): void;
//#endregion
//#region src/mcp/mcpConfig.d.ts
/**
* Centralized configuration for MCP-related environment variables.
* Provides typed access to MCP settings with default values.
*/
declare const mcpConfig$1: {
  OAUTH_ON_AUTH_ERROR: boolean;
  OAUTH_DETECTION_TIMEOUT: number; /** How long (ms) to wait for the user to complete an OAuth flow before timing out. Default: 10 minutes */
  OAUTH_HANDLING_TIMEOUT: number; /** TTL (ms) for OAuth flow state. Must outlive OAUTH_HANDLING_TIMEOUT so the state survives the wait. Default: 15 minutes */
  OAUTH_FLOW_TTL: number;
  CONNECTION_CHECK_TTL: number;
  /** Max number of `tools/list` pages to request when an MCP server paginates its tool list.
  * Bounds the pagination loop so a misbehaving server cannot stall tool discovery. Default: 50 */
  TOOLS_LIST_MAX_PAGES: number; /** Max total tools to retain from paginated `tools/list` responses. Default: 1000 */
  TOOLS_LIST_MAX_TOOLS: number; /** Max approximate JSON bytes to retain from paginated `tools/list` responses. Default: 5 MiB */
  TOOLS_LIST_MAX_BYTES: number; /** Max elapsed time (ms) for paginated `tools/list` discovery. Default: 30000 */
  TOOLS_LIST_TIMEOUT_MS: number; /** Idle timeout (ms) after which user connections are disconnected. Default: 15 minutes */
  USER_CONNECTION_IDLE_TIMEOUT: number; /** Max connect/disconnect cycles before the circuit breaker trips. Default: 7 */
  CB_MAX_CYCLES: number; /** Sliding window (ms) for counting cycles. Default: 45s */
  CB_CYCLE_WINDOW_MS: number; /** Cooldown (ms) after the cycle breaker trips. Default: 15s */
  CB_CYCLE_COOLDOWN_MS: number; /** Max consecutive failed connection rounds before backoff. Default: 3 */
  CB_MAX_FAILED_ROUNDS: number; /** Sliding window (ms) for counting failed rounds. Default: 120s */
  CB_FAILED_WINDOW_MS: number; /** Base backoff (ms) after failed round threshold is reached. Default: 30s */
  CB_BASE_BACKOFF_MS: number; /** Max backoff cap (ms) for exponential backoff. Default: 300s */
  CB_MAX_BACKOFF_MS: number;
};
//#endregion
//#region src/mcp/authority/index.d.ts
interface MCPAuthorityProofResolverOptions {
  methods: Pick<MCPAuthorityMethods, "resolveMCPAuthorityProof" | "assertMCPAuthorityProofsCurrent">;
  bootRevision: string;
  immutableConfig: MCPAuthorityImmutableConfig;
  beforeExecute?: () => void | Promise<void>;
}
interface MCPAuthorityResolutionInput<TParsedConfig, TSchemas> {
  userId: string;
  tenantId?: string;
  targets: readonly MCPAuthorityTargetInput[];
  parsedConfig: TParsedConfig;
  schemas: TSchemas;
  calculateArtifactRevision: (artifacts: {
    parsedConfig: TParsedConfig;
    schemas: TSchemas;
  }) => string;
  session?: ClientSession;
}
interface MCPAuthorityResolution<TParsedConfig, TSchemas> {
  readonly parsedConfig: TParsedConfig;
  readonly schemas: TSchemas;
  readonly authorityProof: MCPAuthorityProofV1;
}
declare class MCPAuthorityProofResolver {
  private readonly methods;
  private readonly boot;
  private readonly beforeExecute?;
  private readonly issuedResolutions;
  constructor(options: MCPAuthorityProofResolverOptions);
  get bootRevision(): MCPAuthorityBootRevision;
  resolve<TParsedConfig, TSchemas>({
    userId,
    tenantId,
    targets,
    parsedConfig,
    schemas,
    calculateArtifactRevision,
    session
  }: MCPAuthorityResolutionInput<TParsedConfig, TSchemas>): Promise<MCPAuthorityResolution<TParsedConfig, TSchemas>>;
  assertCurrent(proofs: MCPAuthorityProofV1 | readonly MCPAuthorityProofV1[], session?: ClientSession): Promise<void>;
  private useIssuedResolution;
  publishWithCurrentAuthority<TParsedConfig, TSchemas, Result>(resolution: MCPAuthorityResolution<TParsedConfig, TSchemas>, publish: (current: MCPAuthorityResolution<TParsedConfig, TSchemas>) => Result | Promise<Result>, session?: ClientSession): Promise<Result>;
  bindWithCurrentAuthority<TParsedConfig, TSchemas, Result>(resolution: MCPAuthorityResolution<TParsedConfig, TSchemas>, bind: (current: MCPAuthorityResolution<TParsedConfig, TSchemas>) => Result | Promise<Result>, session?: ClientSession): Promise<Result>;
  executeWithCurrentAuthority<TParsedConfig, TSchemas, Result>(resolution: MCPAuthorityResolution<TParsedConfig, TSchemas>, execute: (current: MCPAuthorityResolution<TParsedConfig, TSchemas>) => Result | Promise<Result>, session?: ClientSession): Promise<Result>;
}
//#endregion
//#region src/mcp/registry/MCPServersRegistry.d.ts
/** A request stopped while its config initialization was still queued. Healthy
* joiners retry ownership instead of inheriting that request-local cancellation. */
declare class MCPConfigInitializationCanceledError extends Error {}
/** Request context for resolving the effective MCP allowlists. */
interface MCPAllowlistContext {
  userId?: string;
  role?: string;
}
/**
* Resolves the effective `mcpSettings` allowlists for a request. Injected from the app
* layer (where the merged, tenant-scoped config lives) so the registry keeps no app-config
* dependency. Reads the ALS tenant context internally; pass the acting user to also pick up
* user/role-scoped overrides.
*/
type MCPAllowlistResolver = (ctx?: MCPAllowlistContext) => Promise<{
  allowedDomains?: string[] | null;
  allowedAddresses?: string[] | null;
}>;
/**
* Central registry for managing MCP server configurations.
* Authoritative source of truth for all MCP servers provided by TerraMind.
*
* Uses a three-layer architecture:
* - YAML Cache (cacheConfigsRepo): Operator-defined configs loaded at startup (in-memory or Redis)
* - Config Cache (configCacheRepo): Admin-defined configs from Config overrides, lazily initialized
* - DB Repository (dbConfigsRepo): User-provided configs created at runtime (MongoDB + ACL)
*
* Query priority: Config cache → YAML cache → DB.
*/
declare class MCPServersRegistry {
  private static instance;
  private readonly dbConfigsRepo;
  private readonly cacheConfigsRepo;
  private readonly configCacheRepo;
  /** YAML-derived base allowlists; used at boot and as the fallback when no resolver is set. */
  private readonly allowedDomains?;
  private readonly allowedAddresses?;
  /** Resolves the per-request (tenant-scoped) merged allowlists; falls back to the base above. */
  private readonly allowlistResolver?;
  private readonly readThroughCache;
  private readonly readThroughCacheAll;
  private readonly pendingGetAllPromises;
  /** Tracks in-flight config server initializations to prevent duplicate work. */
  private readonly pendingConfigInits;
  /** In-flight reinspections, shared by callers that would inspect the same stored entry
  *  under the same allowlists. */
  private readonly pendingReinspections;
  /** The in-flight reinspection each settling flight waits on, by key, so waits never form a cycle. */
  private readonly reinspectionWaits;
  /** Memoized YAML server names — set once after boot-time init, never changes. */
  private yamlServerNames;
  private yamlServerNamesPromise;
  constructor(mongoose: typeof import("mongoose"), allowedDomains?: string[] | null, allowedAddresses?: string[] | null, allowlistResolver?: MCPAllowlistResolver);
  /** Creates and initializes the singleton MCPServersRegistry instance */
  static createInstance(mongoose: typeof import("mongoose"), allowedDomains?: string[] | null, allowedAddresses?: string[] | null, allowlistResolver?: MCPAllowlistResolver): MCPServersRegistry;
  /** Returns the singleton MCPServersRegistry instance */
  static getInstance(): MCPServersRegistry;
  /** YAML base allowlist (boot/fallback). For request-time decisions use {@link resolveAllowlists}. */
  getAllowedDomains(): string[] | null | undefined;
  /** YAML base allowlist (boot/fallback). For request-time decisions use {@link resolveAllowlists}. */
  getAllowedAddresses(): string[] | null | undefined;
  /** Returns true when no explicit allowedDomains allowlist is configured, enabling SSRF TOCTOU protection */
  shouldEnableSSRFProtection(): boolean;
  /**
  * Resolves the effective domain/address allowlists for the current request.
  *
  * MCP allowlists live in `mcpSettings`, which is tenant/principal-scoped admin config, so
  * they must be read per-request from the merged config — not from a process-global value
  * that would leak across tenants. The injected resolver reads the ALS tenant context; pass
  * the acting user so user/role-scoped overrides resolve too (config-source inspection has no
  * user and resolves at tenant scope). Falls back to the YAML base allowlists when no resolver
  * is injected or the resolver fails, so a transient lookup error fails to the operator's
  * baseline rather than disabling the allowlist.
  */
  resolveAllowlists(ctx?: MCPAllowlistContext): Promise<{
    allowedDomains?: string[] | null;
    allowedAddresses?: string[] | null;
    useSSRFProtection: boolean;
  }>;
  /**
  * Returns the config for a single server, mirroring the precedence used by
  * getAllServerConfigs so list views and single-server lookups agree on
  * the same name:
  *   1. user-tier base entry wins absolutely over a config-tier candidate
  *   2. process-backed base entries win absolutely over a config-tier candidate
  *   3. healthy YAML/DB base wins over a failed (inspectionFailed) candidate
  *   4. healthy candidate overlays its fields onto the base, preserving the
  *      base entry's source tag so downstream recovery routes correctly
  *   5. with no base, the candidate is returned as-is (config-only server)
  *
  * readThroughCache memoizes only the global YAML/DB lookup; the per-call
  * configServers candidate is tenant-scoped and is never cached, so a
  * failed stub from one tenant can never satisfy a no-userId lookup from
  * another.
  */
  getServerConfig(serverName: string, userId?: string, configServers?: Record<string, ParsedServerConfig>): Promise<ParsedServerConfig | undefined>;
  /** Returns whether an effective config exactly matches the operator-owned base config. */
  isAppServerConfig(serverName: string, effectiveConfig: ParsedServerConfig): Promise<boolean>;
  /**
  * Returns the full server config map after merging YAML cache, Config-tier overrides,
  * and User-DB entries.
  *
  * Precedence (lowest to highest): YAML cache > Config-tier overrides (success only) > User DB.
  * Three guards keep the merge safe:
  *   1. Config-tier entries carrying `inspectionFailed: true` never overlay an existing
  *      base entry; the healthy base is preserved for the duration of the retry window.
  *   2. User-DB entries (`source: 'user'`) are never replaced by Config-tier overlays.
  *   3. Process-backed base entries are never replaced by Config-tier overlays.
  * On a successful overlay the base entry's `source` field is preserved so downstream
  * recovery logic routes to the correct storage location — except a `'plugin'` base,
  * whose no-resolve provenance must not transfer to the operator override (see
  * `overlaySource`).
  */
  getAllServerConfigs(userId?: string, configServers?: Record<string, ParsedServerConfig>, role?: string): Promise<Record<string, ParsedServerConfig>>;
  /**
  * Returns YAML + user-DB server configs, cached via `readThroughCacheAll`.
  * YAML wins on name collisions so a user-created server cannot hide global config.
  * Always called by `getAllServerConfigs` so the DB query is amortized across
  * requests within the TTL window regardless of whether `configServers` is present.
  */
  private getBaseServerConfigs;
  private fetchBaseServerConfigs;
  /**
  * Stores a minimal config stub so the server remains "known" to the registry
  * even when inspection fails at startup. This enables reinitialize to recover.
  */
  addServerStub(serverName: string, config: MCPOptions, storageLocation: "CACHE", userId?: string): Promise<AddServerResult>;
  addServer(serverName: string, config: MCPOptions, storageLocation: "CACHE" | "DB", userId?: string, reservedServerNames?: Iterable<string>): Promise<AddServerResult>;
  /**
  * Backfills the inspector-derived `resolvedInstructions` for a server whose
  * operator explicitly deferred startup inspection. An enabled
  * `serverInstructions` declaration has no fetched text to resolve in that
  * case. Identity- or request-scoped servers are deliberately rejected:
  * their live instructions cannot safely be stored in a shared config.
  *
  * First write wins: once the stored entry carries any text, later calls are
  * no-ops. Without this, identities racing the first backfill under stale
  * config snapshots would churn the shared copy and rotate the global read
  * caches on every divergence.
  *
  * YAML-tier servers only. Config-overlay servers are cached under
  * config-hash keys and cannot be addressed by name here; DB-backed user
  * servers need an identity-preserving write through mongoose timestamps and
  * the credential-sanitization pipeline, which is its own change. Both are
  * left untouched. An overlaid effective config carries its base's `'yaml'`
  * source tag (`overlaySource`), so `connectedConfig` — the config the
  * delivering connection was actually created from — is compared against the
  * stored entry on every admin-configurable field: text fetched from an
  * overridden endpoint must never be stored as the shared base's.
  *
  * The entry's `updatedAt` is deliberately preserved (the storage `patch`
  * contract): the config identity did not change, and bumping it would mark
  * every live connection for this server stale.
  *
  * @returns true when a stored config was updated.
  */
  setResolvedInstructions(serverName: string, instructions: string, userId?: string, connectedConfig?: ParsedServerConfig): Promise<boolean>;
  /**
  * Resolves a config that failed inspection to the config a connection should be made with.
  *
  * Once the server is reachable this is the stored, inspected config — whether this call
  * recovered it or a concurrent request did, on this replica or another — so a caller that
  * read the stub connects with what inspection found rather than the stub. Resolves undefined
  * while the server is still unreachable. Config-tier stubs are left to `ensureConfigServers`,
  * which retries them on its own schedule.
  */
  recoverServerConfig(serverName: string, config: ParsedServerConfig, userId?: string): Promise<ParsedServerConfig | undefined>;
  /**
  * Re-inspects a server whose stored config failed inspection and replaces that stub with the
  * inspected config.
  *
  * Inspection is a network round trip, and every caller that read the stub before a recovery
  * was written would otherwise inspect and write again. Each write bumps `updatedAt`, which
  * marks connections made after the previous write stale, so a caller already holding one
  * reads an empty tool list. Callers in this process that read the same stub and are judged
  * against the same allowlists therefore share one inspection and one write, and the write
  * replaces only the stub that was inspected, so a replica that lost the race writes nothing.
  *
  * Every decision reads the entry past the store's process-local snapshot. An entry that is no
  * longer failed was already recovered and resolves to the stored config without another
  * inspection, including when that recovery is found only after this call's own inspection
  * failed. A stub that a registry re-initialization replaced while it was inspected resolves
  * through an inspection of the newer stub. A server that is still unreachable rejects with
  * `MCPInspectionFailedError`; an allowlist rejection is rethrown as is.
  */
  reinspectServer(serverName: string, storageLocation: "CACHE" | "DB", userId?: string): Promise<AddServerResult>;
  /**
  * Shares one inspection and write of `stub` among callers judged against the same allowlists.
  * A flight settling against another stub passes its own key as `waiter` and joins that stub's
  * flight, unless the flight already waits on the waiter; then the waiter inspects it itself.
  */
  private joinReinspection;
  /** Whether the flight for `key` is `waiter` or waits on it, directly or through other flights. */
  private waitsOn;
  private reinspectStub;
  /**
  * Settles a reinspection whose own inspection did not replace `stub` against the entry stored
  * now. A recovery another writer stored is the outcome, and this replica's read caches drop
  * the stub they may still memoize from before it. A different stub from a registry
  * re-initialization is settled through its own flight, which this one joins when another
  * request already started it. A stub still in place rejects with `failure`.
  */
  private resolveStoredEntry;
  private getReinspectionEntry;
  /** Writes an inspected config over the stub it came from; undefined when another writer
  *  replaced the stub first. DB storage holds no startup stubs, so it keeps a plain update. */
  private replaceStub;
  /**
  * Identity of a reinspection: the stored stub it inspects — a DB entry is visible per user and
  * tenant — and the allowlists it is judged against, so neither a user's DB visibility nor a
  * tenant's allowlist decision reaches another caller through a shared inspection, and a newer
  * stub is never answered by an inspection of the one it replaced.
  */
  private reinspectionKey;
  /**
  * Inspects an update without mutating its backing repository. Callers that must
  * coordinate an external fence with persistence can prepare first, fence, and
  * then commit the returned config.
  */
  inspectServerUpdate(serverName: string, config: MCPOptions, storageLocation: "CACHE" | "DB", userId?: string): Promise<ParsedServerConfig>;
  /** Persists a previously inspected update without opening a second MCP connection. */
  commitServerUpdate(serverName: string, parsedConfig: ParsedServerConfig, storageLocation: "CACHE" | "DB", userId?: string): Promise<ParsedServerConfig>;
  updateServer(serverName: string, config: MCPOptions, storageLocation: "CACHE" | "DB", userId?: string): Promise<ParsedServerConfig>;
  /**
  * Ensures that config-source MCP servers (from admin Config overrides) are initialized.
  * Identifies servers in `resolvedMcpConfig` that are not from YAML, lazily initializes
  * any not yet in the config cache, and returns their parsed configs.
  *
  * Config cache keys are scoped by a hash of the raw config to prevent cross-tenant
  * cache poisoning when two tenants define a server with the same name but different configs.
  */
  ensureConfigServers(resolvedMcpConfig: Record<string, MCPOptions>, limit?: <T>(task: () => Promise<T>) => Promise<T>): Promise<Record<string, ParsedServerConfig>>;
  /**
  * Returns true when `rawConfig` matches the YAML cache entry for this server
  * on every admin-configurable field, so an unmodified YAML-defined server
  * can skip lazy-init and avoid being re-inspected or shadowed in the
  * config tier.
  */
  private isUnmodifiedYamlServer;
  /**
  * Ensures a single config-source server is initialized.
  * Cache key is scoped by config hash to prevent cross-tenant poisoning.
  * Deduplicates concurrent init requests for the same server+config.
  * Stale failure stubs are retried after `CONFIG_STUB_RETRY_MS` to recover from transient errors.
  */
  private ensureSingleConfigServer;
  /**
  * Lazily initializes a config-source MCP server: inspects capabilities/tools, then
  * stores the parsed config in the config cache with `source: 'config'`.
  */
  private lazyInitConfigServer;
  /**
  * Writes a config to `configCacheRepo` using the atomic upsert operation.
  * Safe for cross-process races — the underlying cache handles add-or-update internally.
  */
  private upsertConfigCache;
  /**
  * Clears the config-source server cache, forcing re-inspection on next access.
  * Called when admin config overrides change (e.g., mcpServers mutation).
  *
  * @returns Names of servers that were evicted from the config cache.
  *          Callers should disconnect active connections for these servers.
  */
  invalidateConfigCache(): Promise<string[]>;
  getOAuthServers(userId?: string): Promise<Set<string>>;
  reset(): Promise<void>;
  removeServer(serverName: string, storageLocation: "CACHE" | "DB", userId?: string): Promise<void>;
  private getConfigRepository;
  /** Tenant-scoped because DB-backed lookups are filtered by the active tenant:
  *  an entry populated in one tenant's context must never satisfy another's. */
  private getReadThroughCacheKey;
  /**
  * DB-backed servers and their ACL grants are tenant-scoped data, so their
  * invalidation stays within the acting tenant. CACHE-tier (YAML/App
  * repository) entries are global, so those mutations evict across tenants:
  * the per-server cache cannot enumerate tenant-scoped keys, so it clears the
  * namespace, and the aggregate map uses its global path.
  */
  private invalidateServerReadCaches;
  private getOperatorManagedServerNames;
  private parseServerNameFromConfigCacheKey;
  private warnOnOperatorManagedNameCollisions;
  private resetYamlServerNamesMemo;
  /**
  * Returns memoized YAML server names. Populated lazily on first call after boot/reset.
  * YAML servers don't change after boot, so this avoids repeated `getAll()` calls.
  * Uses promise deduplication to prevent concurrent cold-start double-fetch.
  */
  private getYamlServerNames;
  /**
  * Produces a config-cache key scoped by server name AND a hash of the raw config plus the
  * effective allowlists. Hashing the raw config prevents cross-tenant poisoning when two
  * tenants define the same server name with different configurations; hashing the allowlists
  * prevents poisoning when the same config resolves differently because tenants have different
  * `mcpSettings.allowedDomains` / `allowedAddresses` (so one tenant's inspection result — e.g.
  * an `inspectionFailed` stub from a rejected domain — never satisfies another tenant's lookup).
  */
  private configCacheKey;
}
//#endregion
//#region src/mcp/oauth/handler.d.ts
declare class MCPOAuthHandler {
  private static readonly FLOW_TYPE;
  /**
  * Creates a fetch function with custom headers injected
  */
  private static createOAuthFetch;
  /**
  * Discovers OAuth metadata from the server
  */
  private static discoverMetadata;
  private static discoverResourceMetadata;
  private static discoverAuthorizationMetadata;
  private static discoverPreconfiguredMetadataWithTimeout;
  /**
  * Discovers OAuth authorization server metadata, retrying with just the origin
  * when discovery fails for a path-based URL. Shared implementation used by
  * `discoverMetadata` and both `refreshOAuthTokens` branches.
  */
  private static discoverWithOriginFallback;
  private static hasUnpinnedClientSecret;
  private static oauthUrlsMatch;
  static buildStoredClientMetadata(metadata?: OAuthMetadata, resourceMetadata?: OAuthProtectedResourceMetadata, serverUrl?: string, clientSource?: OAuthClientSource): OAuthStoredClientMetadata | undefined;
  /**
  * Ensures stored tokens and client credentials are only reused with the MCP resource and
  * configured-client provenance captured by the authorization flow that created them.
  */
  static assertStoredClientBinding(serverName: string, serverUrl: string | undefined, clientInfo: OAuthClientInformation | undefined, storedMetadata: Partial<OAuthStoredClientMetadata> | undefined, config?: MCPOptions$1["oauth"]): void;
  private static appendResourceParameter;
  private static assertNoUnpinnedClientSecret;
  /**
  * Registers an OAuth client dynamically
  */
  private static registerOAuthClient;
  /**
  * Initiates the OAuth flow for an MCP server
  */
  static initiateOAuthFlow(serverName: string, serverUrl: string, userId: string, oauthHeaders: Record<string, string>, config?: MCPOptions$1["oauth"], allowedDomains?: string[] | null, findToken?: TokenMethods["findToken"], allowedAddresses?: string[] | null, tenantId?: string): Promise<{
    authorizationUrl: string;
    flowId: string;
    flowMetadata: MCPOAuthFlowMetadata;
  }>;
  /**
  * Completes the OAuth flow by exchanging the authorization code for tokens.
  *
  * The token exchange reuses the SSRF policy captured during
  * {@link initiateOAuthFlow} and enforces it again at connect time. This closes
  * DNS rebinding gaps between the preflight validation and the callback request.
  */
  static completeOAuthFlow(flowId: string, authorizationCode: string, flowManager: FlowStateManager<MCPOAuthTokens>, oauthHeaders: Record<string, string>, persistBeforeComplete?: (tokens: MCPOAuthTokens, completePersistedFlow: (tokens: MCPOAuthTokens) => Promise<void>) => Promise<MCPOAuthTokens>, rollbackPersistedTokens?: (tokens: MCPOAuthTokens) => Promise<void>, expectedAttempt?: {
    createdAt: number;
    state: string;
  }): Promise<MCPOAuthTokens>;
  /**
  * Gets the OAuth flow metadata
  */
  static getFlowState(flowId: string, flowManager: FlowStateManager<MCPOAuthTokens>): Promise<MCPOAuthFlowMetadata | null>;
  /**
  * Generates a flow ID for the OAuth flow
  * @returns Consistent ID so concurrent requests share the same flow
  */
  static generateFlowId(userId: string, serverName: string, tenantId?: string): string;
  static parseFlowId(flowId: string): {
    userId: string;
    serverName: string;
    tenantId?: string;
  } | null;
  /** Same shape as `generateFlowId`; kept distinct so token-fetch flows can diverge from OAuth flows */
  static generateTokenFlowId(userId: string, serverName: string, tenantId?: string): string;
  /**
  * Generates a secure state parameter
  */
  private static generateState;
  /**
  * Enforces RFC 9728 §3.3 / §7.3: the `resource` identifier advertised by an OAuth
  * Protected Resource Metadata document MUST match the URL the client used to fetch
  * the document. A mismatch means the metadata is attacker-controlled (or the server
  * is badly misconfigured); per the RFC the whole document MUST be discarded, and in
  * practice we must fail the OAuth flow because `authorization_servers` on the same
  * document is also untrustworthy and was the primary theft vector in
  * GHSA-gvpj-vm2f-2m23.
  *
  * Uses the MCP SDK's own {@link checkResourceAllowed} so the semantics (same origin
  * plus configured-path-prefix) match what the SDK enforces internally via
  * {@link selectResourceURL}, a code path TerraMind does not go through.
  */
  private static assertResourceBoundToServer;
  /**
  * Validates an OAuth URL is not targeting a private/internal address.
  * Skipped when the full URL (hostname + protocol + port) matches an admin-trusted
  * allowedDomains entry, honoring protocol/port constraints when the admin specifies them.
  *
  * SECURITY: when `allowedDomains` is configured and the URL did NOT match it
  * (i.e., we are in the SSRF fallback path because the admin's strict bound
  * rejected this URL), the address exemption must NOT broaden that bound — a
  * malicious MCP server could otherwise advertise OAuth metadata at any
  * private address the admin permitted for an unrelated purpose (e.g. a
  * self-hosted LLM). When `allowedDomains` is empty, `allowedAddresses` is
  * the only opt-in mechanism for permitting private OAuth endpoints, so it
  * is consulted normally.
  */
  private static validateOAuthUrl;
  private static readonly STATE_MAP_TYPE;
  /**
  * Stores a mapping from the opaque OAuth state parameter to the flowId.
  * This allows the callback to resolve the flowId from an unguessable state
  * value, preventing attackers from forging callback requests.
  */
  static storeStateMapping(state: string, flowId: string, flowManager: FlowStateManager<MCPOAuthTokens | null>): Promise<void>;
  /**
  * Resolves an opaque OAuth state parameter back to the original flowId.
  * Returns null if the state is not found (expired or never stored).
  */
  static resolveStateToFlowId(state: string, flowManager: FlowStateManager<MCPOAuthTokens | null>): Promise<string | null>;
  /**
  * Deletes an orphaned state mapping when a flow is replaced.
  * Prevents old authorization URLs from resolving after a flow restart.
  * Returns `false` when the underlying store rejected the delete.
  */
  static deleteStateMapping(state: string, flowManager: FlowStateManager<MCPOAuthTokens | null>): Promise<boolean>;
  /** Fails one observed OAuth attempt and makes its callback state unusable. */
  static failFlowAndDeleteStateMapping(flowId: string, flowState: FlowState<MCPOAuthTokens | null>, flowManager: FlowStateManager<MCPOAuthTokens | null>, error: Error | string): Promise<void>;
  /**
  * Deletes an OAuth flow together with its state mapping, for teardown paths
  * that don't already hold the flow (e.g. server uninstall). The flow is
  * guarded by the observed attempt identity so stale teardown cannot remove
  * a concurrently created replacement. The old opaque mapping remains safe
  * to delete because every attempt receives a distinct state value.
  */
  static deleteFlowAndStateMapping(flowId: string, flowManager: FlowStateManager<MCPOAuthTokens | null>): Promise<void>;
  /**
  * Gets the default redirect URI for a server
  */
  private static getDefaultRedirectUri;
  /**
  * Processes and logs a token refresh response from an OAuth server.
  * Normalizes the response to MCPOAuthTokens format and logs debug info about refresh token rotation.
  */
  private static processRefreshResponse;
  /**
  * Posts a `refresh_token` grant, transparently retrying without the `scope`
  * parameter when — and only when — the authorization server rejects the request
  * because of that parameter.
  *
  * RFC 6749 §6 makes `scope` optional on refresh (the server reuses the originally
  * granted scope when it is omitted). TerraMind sends it by default because some
  * servers expect it, but others — notably Salesforce — reject any `scope` on the
  * refresh grant with HTTP 400 "scope parameter not supported". A failed refresh
  * forces a full re-authentication, which on multi-replica deployments amplifies the
  * PKCE retry storm, so we recover automatically instead of surfacing the failure.
  */
  private static postRefreshRequest;
  /**
  * Narrowly detects "this server does not accept a `scope` parameter on the refresh
  * grant" so the retry-without-scope fallback can never mask unrelated refresh
  * failures (e.g. `invalid_grant` for an expired/revoked refresh token). Matches the
  * RFC 6749 `invalid_scope` error and Salesforce's "scope parameter not supported".
  */
  private static isScopeParameterRejection;
  /**
  * Refreshes OAuth tokens using a refresh token
  */
  static refreshOAuthTokens(refreshToken: string, metadata: {
    serverName: string;
    serverUrl?: string;
    clientInfo?: OAuthClientInformation;
    storedTokenEndpoint?: string;
    storedAuthMethods?: string[];
    storedServerUrl?: string;
    clientSource?: OAuthClientSource;
    resource?: string;
  }, oauthHeaders: Record<string, string>, config?: MCPOptions$1["oauth"], allowedDomains?: string[] | null, allowedAddresses?: string[] | null, signal?: AbortSignal): Promise<MCPOAuthTokens>;
  /**
  * Revokes OAuth tokens at the authorization server (RFC 7009)
  */
  static revokeOAuthToken(serverName: string, token: string, tokenType: "refresh" | "access", metadata: {
    serverUrl: string;
    clientId: string;
    clientSecret: string;
    revocationEndpoint?: string;
    revocationEndpointAuthMethodsSupported?: string[];
  }, oauthHeaders?: Record<string, string>, allowedDomains?: string[] | null, allowedAddresses?: string[] | null): Promise<void>;
}
//#endregion
//#region src/mcp/oauth/tokens.d.ts
declare class ReauthenticationRequiredError extends Error {
  constructor(serverName: string, reason: "expired" | "missing" | "invalid_client" | "binding");
}
/** Durable credentials could not be read or decrypted. This is retryable infrastructure state,
* never evidence that the user must authorize the server again. */
declare class MCPTokenStorageUnavailableError extends Error {
  constructor(serverName: string, cause: unknown);
}
/** The refresh credential may still be usable, but its provider could not complete this attempt. */
declare class MCPTokenRefreshUnavailableError extends Error {
  constructor(serverName: string, cause: unknown);
}
interface StoreTokensParams {
  userId: string;
  serverName: string;
  tokens: OAuthTokens | ExtendedOAuthTokens | MCPOAuthTokens;
  createToken: TokenMethods["createToken"];
  updateToken?: TokenMethods["updateToken"];
  deleteTokens?: TokenMethods["deleteTokens"];
  findToken?: TokenMethods["findToken"];
  clientInfo?: OAuthClientInformation$1;
  metadata?: Partial<OAuthStoredClientMetadata>;
  /** Existing generation that must still own every stored record before a refresh is persisted. */
  expectedCredentialSetId?: string;
  /** Internal refresh-teardown fence; interactive authorization writes omit it. */
  signal?: AbortSignal;
  /** Optional: Pass existing token state to avoid duplicate DB calls */
  existingTokens?: {
    accessToken?: IToken | null;
    refreshToken?: IToken | null;
    clientInfoToken?: IToken | null;
  };
  /** Runs after all token rows are written but while the rollback journal is still available. */
  onStoreCommitted?: (tokens: MCPOAuthTokens) => Promise<void>;
  /** Runs after preflight reads and encryption, immediately before the first token-row write. */
  onStorePreparing?: () => Promise<void>;
}
interface GetTokensParams {
  userId: string;
  serverName: string;
  findToken: TokenMethods["findToken"];
  refreshTokens?: (refreshToken: string, metadata: {
    userId: string;
    serverName: string;
    identifier: string;
    clientInfo?: OAuthClientInformation$1;
    storedTokenEndpoint?: string;
    storedAuthMethods?: string[];
    storedServerUrl?: string;
    clientSource?: OAuthStoredClientMetadata["client_source"];
    resource?: string;
  }, signal?: AbortSignal) => Promise<MCPOAuthTokens>;
  createToken?: TokenMethods["createToken"];
  updateToken?: TokenMethods["updateToken"];
  /** Enables cleanup of stale client registration and refresh token on invalid_client errors during refresh. */
  deleteTokens?: TokenMethods["deleteTokens"];
  /** Waiter-specific: aborting resolves this caller's wait with `null` without cancelling the shared redemption. */
  signal?: AbortSignal;
  /**
  * Invoked inside the shared redemption after rotated tokens are persisted.
  * Runs even when the initiating waiter has already aborted its wait, so
  * cache invalidation tied to the fresh tokens cannot be skipped by a timeout.
  */
  onRefreshSuccess?: (tokens: MCPOAuthTokens) => Promise<void>;
  /** Creates the exact post-write callback after durable fence intent is safely stored. */
  onRefreshPreparing?: () => Promise<(tokens?: MCPOAuthTokens) => Promise<void>>;
  /** Separates in-flight redemptions for the same named server under different OAuth bindings. */
  singleFlightScope?: string;
  /** Shared cache-backed fence used to serialize refresh persistence with server teardown. */
  flowManager?: Pick<FlowStateManager, "getLeaseGeneration" | "acquireLease">;
}
declare const getMCPOAuthLeaseId: (userId: string, serverName: string, tenantId?: string | undefined) => string;
declare class MCPTokenStorage {
  /**
  * Process-local in-flight refresh-token redemptions, keyed by
  * `tenantId:userId:serverName:bindingScope`. Every code path that redeems a refresh token
  * (expired-token refresh via `getTokens`, silent refresh on 401, reconnect
  * retries) converges on `forceRefreshTokens`, so coalescing here guarantees
  * at most one wire call to the token endpoint per user/server at a time.
  * RFC 9700 servers treat a replayed (already-consumed) refresh token as
  * theft and revoke the entire grant family, so concurrent redemptions are
  * not merely wasteful — they destroy the freshly issued tokens. Only
  * in-flight promises are held (no result caching): each new refresh request
  * after settlement triggers a fresh redemption.
  */
  private static inflightRefreshes;
  private static inflightRefreshControllers;
  private static inflightRefreshOwners;
  private static refreshTeardownCounts;
  /**
  * How long an in-flight redemption may run before it is aborted. Generous
  * relative to a healthy refresh round trip (well under a minute) so the
  * abort only fires for genuinely wedged executions. The single-flight slot
  * is freed when the aborted execution settles — never while it might still
  * reach the token endpoint with the old refresh token.
  */
  static readonly INFLIGHT_REFRESH_STALE_MS = 6e4;
  static getLogPrefix(userId: string, serverName: string): string;
  private static getRefreshOwnerKey;
  static isRefreshTeardownActive(userId: string, serverName: string, tenantId?: string | undefined): boolean;
  /** Holds a per-user/server gate, then aborts and joins every process-local refresh that entered
  * before it. The returned release keeps successor refreshes out until teardown finishes. */
  static beginRefreshTeardown(userId: string, serverName: string): Promise<() => void>;
  /** Returns whether storage contains a currently usable, generation-bound authorization. */
  static hasStoredAuthorization({
    userId,
    serverName,
    findToken,
    validateClientBinding
  }: {
    userId: string;
    serverName: string;
    findToken: TokenMethods["findToken"];
    validateClientBinding: (clientInfo: OAuthClientInformation$1, storedMetadata: Partial<OAuthStoredClientMetadata>) => void;
  }): Promise<boolean>;
  /**
  * Confirms a flow-cached access token is still the token in persistent storage. This prevents
  * an old `mcp_get_tokens` result from being paired with newer client-binding metadata.
  */
  static isCurrentAccessToken({
    userId,
    serverName,
    accessToken,
    credentialSetId,
    findToken
  }: {
    userId: string;
    serverName: string;
    accessToken: string;
    credentialSetId: string | undefined;
    findToken: TokenMethods["findToken"];
  }): Promise<boolean>;
  /** Fails closed when separately stored OAuth records are not from the same authorization. */
  static assertCredentialSetBinding(serverName: string, tokenCredentialSetId: string | undefined, clientMetadata: Partial<OAuthStoredClientMetadata> | Record<string, unknown> | undefined): void;
  /**
  * Stores OAuth tokens for an MCP server
  *
  * @param params.existingTokens - Optional: Pass existing token state to avoid duplicate DB calls.
  * This is useful when refreshing tokens, as getTokens() already has the token state.
  */
  static storeTokens({
    userId,
    serverName,
    tokens,
    createToken,
    updateToken,
    deleteTokens,
    findToken,
    clientInfo,
    existingTokens,
    metadata,
    expectedCredentialSetId,
    signal,
    onStoreCommitted,
    onStorePreparing
  }: StoreTokensParams): Promise<MCPOAuthTokens>;
  /**
  * Forces a refresh of OAuth tokens using the stored refresh token, regardless
  * of whether the access token appears locally expired. Use this when the
  * server has signaled token invalidity (e.g. a 401 mid-session) — the 401 is
  * the authoritative signal, not the local `expires_at`.
  *
  * Single-flighted per `(tenantId, userId, serverName, bindingScope)`: concurrent callers
  * (tool-call 401s, pings, reconnect retries, expired-token reads) share one
  * redemption and receive the same rotated result instead of each replaying
  * the refresh token at the token endpoint.
  *
  * Returns the new tokens, or `null` when refresh is not possible (no refresh
  * token stored, no refresh callback, etc.). Throws `ReauthenticationRequiredError`
  * when the refresh server response indicates the client registration is stale.
  */
  static forceRefreshTokens(params: GetTokensParams & {
    existingAccessToken?: IToken | null;
  }): Promise<MCPOAuthTokens | null>;
  /**
  * Wraps the shared redemption promise for a single waiter: if the waiter's
  * `signal` aborts first, that waiter receives `null` (matching the prior
  * abort contract) while the shared execution continues for other callers.
  */
  private static raceWithAbort;
  /**
  * Runs a single refresh-token redemption under the `inflightRefreshes`
  * single-flight lock. The refresh token is read from storage at execution
  * time — never from a caller-provided snapshot — so a redemption that starts
  * after another refresh completed uses the rotated token instead of
  * replaying the consumed one (which RFC 9700 reuse detection punishes by
  * revoking the whole grant family).
  */
  private static executeTokenRefresh;
  /**
  * Retrieves OAuth tokens for an MCP server
  */
  static getTokens({
    userId,
    serverName,
    findToken,
    createToken,
    updateToken,
    deleteTokens,
    refreshTokens,
    singleFlightScope,
    flowManager,
    onRefreshSuccess,
    onRefreshPreparing
  }: GetTokensParams): Promise<MCPOAuthTokens | null>;
  static getClientInfoAndMetadata({
    userId,
    serverName,
    findToken
  }: {
    userId: string;
    serverName: string;
    findToken: TokenMethods["findToken"];
  }): Promise<{
    clientInfo: OAuthClientInformation$1;
    clientMetadata: Record<string, unknown>;
  } | null>;
  /** Deletes only the stored client registration for a specific user and server */
  static deleteClientRegistration({
    userId,
    serverName,
    deleteTokens,
    credentialSetId
  }: {
    userId: string;
    serverName: string;
    deleteTokens: TokenMethods["deleteTokens"];
    credentialSetId?: string;
  }): Promise<void>;
  /**
  * Deletes all OAuth-related tokens for a specific user and server
  */
  static deleteUserTokens({
    userId,
    serverName,
    deleteToken
  }: {
    userId: string;
    serverName: string;
    deleteToken: (filter: {
      userId: string;
      type: string;
      identifier: string;
    }) => Promise<void>;
  }): Promise<void>;
}
//#endregion
//#region src/mcp/oauth/detectOAuth.d.ts
interface OAuthDetectionResult {
  requiresOAuth: boolean;
  method: "protected-resource-metadata" | "401-challenge-metadata" | "no-metadata-found";
  metadata?: Record<string, unknown> | null;
}
/**
* Detects if an MCP server requires OAuth authentication using proactive discovery methods.
*
* Strategy (RFC 9728 §5.1 aligned):
* 1. Probe the server for a 401 challenge and extract the `resource_metadata` URL from
*    the `WWW-Authenticate` header, if any.
* 2. Call the SDK's Protected Resource Metadata discovery. When the hint is present it
*    overrides the path-aware well-known endpoint, matching the behavior of Claude
*    Desktop / MCP Inspector / Copilot and avoiding stale path-aware metadata.
* 3. If no metadata was found but the server advertised `Bearer`, report OAuth-required
*    without metadata (legacy servers without `.well-known` still need auth).
* 4. Optional fallback: treat any 401/403 as an OAuth requirement when
*    `MCP_OAUTH_ON_AUTH_ERROR=true`.
*
* @param serverUrl - The MCP server URL to check for OAuth requirements
*/
declare function detectOAuthRequirement(serverUrl: string, allowedDomains?: string[] | null, allowedAddresses?: string[] | null): Promise<OAuthDetectionResult>;
//#endregion
//#region src/mcp/oauth/methods.d.ts
type ClientAuthMethod = "client_secret_basic" | "client_secret_post" | "none";
/** Maps a user-facing `TokenExchangeMethodEnum` to an OAuth auth method string. */
declare function getForcedTokenEndpointAuthMethod(tokenExchangeMethod?: TokenExchangeMethodEnum): "client_secret_basic" | "client_secret_post" | undefined;
/**
* Selects the auth method to request during dynamic client registration.
*
* Priority:
* 1. Forced override from `tokenExchangeMethod` config
* 2. First credential-based method from server's advertised list (skips `none` per RFC 7591 —
*    `none` declares a public client, which is incorrect for DCR with a generated secret)
* 3. `none` if the server only advertises `none`
* 4. Server's first listed method (unsupported exotic method — best-effort)
* 5. Falls through to `undefined` (caller keeps its default)
*/
declare function selectRegistrationAuthMethod(serverAdvertised: string[] | undefined, tokenExchangeMethod?: TokenExchangeMethodEnum): string | undefined;
/**
* Resolves the auth method for token endpoint requests (refresh, pre-configured flows).
*
* Priority:
* 1. Forced override from `tokenExchangeMethod` config
* 2. Preferred method from client registration response (`clientInfo.token_endpoint_auth_method`)
* 3. First match from server's advertised methods
*/
declare function resolveTokenEndpointAuthMethod(options: {
  tokenExchangeMethod?: TokenExchangeMethodEnum;
  tokenAuthMethods: string[];
  preferredMethod?: string;
}): "client_secret_basic" | "client_secret_post" | undefined;
/**
* Infers the client auth method from request state when `clientInfo.token_endpoint_auth_method`
* is not set. Used inside the fetch wrapper to determine how credentials were applied by the SDK.
*
* Per RFC 8414 Section 2, defaults to `client_secret_basic` for confidential clients.
*/
declare function inferClientAuthMethod(hasAuthorizationHeader: boolean, hasBodyClientId: boolean, hasBodyClientSecret: boolean, hasClientSecret: boolean): ClientAuthMethod;
//#endregion
//#region src/mcp/oauth/pending.d.ts
type PendingOAuthFlowState = Pick<FlowState<MCPOAuthTokens | null>, "createdAt" | "metadata" | "status">;
type PendingOAuthFlowManager = Pick<FlowStateManager<MCPOAuthTokens | null>, "getFlowState">;
type PendingOAuthStart = {
  authURL: string;
  options?: OAuthStartOptions;
};
type ReplayablePendingMCPOAuthStartOptions = {
  flowManager?: PendingOAuthFlowManager | null;
  userId: string;
  serverName: string;
};
declare function getReplayablePendingMCPOAuthStartFromFlow(flow: PendingOAuthFlowState | null | undefined, now?: number): PendingOAuthStart | undefined;
declare function getReplayablePendingMCPOAuthStart({
  flowManager,
  userId,
  serverName
}: ReplayablePendingMCPOAuthStartOptions): Promise<PendingOAuthStart | undefined>;
type OAuthEndHandler = () => Promise<void>;
declare class OAuthLifecycleRelay {
  private readonly oauthStarts;
  private readonly oauthEnds;
  private readonly emittedAuthUrls;
  private readonly emittedOAuthEnds;
  private lastOAuthStart?;
  private oauthEnded;
  constructor({
    oauthStart,
    oauthEnd,
    logPrefix
  }: {
    oauthStart?: OAuthStartHandler;
    oauthEnd?: OAuthEndHandler;
    logPrefix: string;
  });
  private readonly logPrefix;
  readonly start: OAuthStartHandler;
  /** Completion notifications are best effort and cannot invalidate received OAuth tokens. */
  readonly end: OAuthEndHandler;
  add({
    oauthStart,
    oauthEnd,
    flowManager,
    userId,
    serverName
  }: ReplayablePendingMCPOAuthStartOptions & {
    oauthStart?: OAuthStartHandler;
    oauthEnd?: OAuthEndHandler;
  }): Promise<void>;
  private emit;
  private emitEnd;
}
//#endregion
//#region src/mcp/oauth/events.d.ts
type OAuthPromptOptions = {
  expiresAt?: number;
};
type OAuthToolCall = {
  id?: string;
  name: string;
  type: "tool_call" | "tool_call_chunk";
  args?: string;
  output?: string;
};
declare function getOAuthPromptExpiresAt(options?: OAuthPromptOptions, now?: number): number;
declare function getMCPServerNamesFromTools(tools?: unknown[] | null, knownServerNames?: readonly string[]): Set<string>;
declare function buildMCPAuthStepId(serverName: string): string;
declare function buildMCPAuthToolCall({
  id,
  args,
  output,
  serverName,
  type
}: {
  id?: string;
  args?: string;
  output?: string;
  serverName: string;
  type?: OAuthToolCall["type"];
}): OAuthToolCall;
declare function buildMCPAuthRunStepEvent({
  runId,
  stepId,
  toolCall,
  index
}: {
  runId?: string | null;
  stepId: string;
  toolCall: OAuthToolCall;
  index?: number;
}): ServerSentEvent;
declare function buildMCPAuthRunStepDeltaEvent({
  authURL,
  stepId,
  toolCall,
  options
}: {
  authURL: string;
  stepId: string;
  toolCall: OAuthToolCall;
  options?: OAuthPromptOptions;
}): ServerSentEvent;
declare function buildMCPAuthRunStepEndDeltaEvent({
  stepId,
  toolCall
}: {
  stepId: string;
  toolCall: OAuthToolCall;
}): ServerSentEvent;
declare function buildMCPAuthRunStepCompletedEvent({
  stepId,
  toolCall,
  index
}: {
  stepId: string;
  toolCall: OAuthToolCall;
  index?: number;
}): ServerSentEvent;
//#endregion
//#region src/mcp/oauth/resume.d.ts
/**
* Projects the current client-safe MCP OAuth prompts from the durable event
* representation retained for mixed-version resume compatibility.
*/
declare function projectPendingMCPOAuthPrompts(replayEvents: ResumeState["replayEvents"], runSteps: readonly Agents.RunStep[], now?: number): Agents.PendingMCPOAuthPrompt[] | undefined;
//#endregion
//#region src/mcp/oauth/cleanup.d.ts
declare function getMCPServerGeneration(config: ParsedServerConfig): string;
interface CleanupConfig {
  mcpSettings?: {
    allowedDomains?: string[] | null;
    allowedAddresses?: string[] | null;
  };
  mcpServers?: Record<string, MCPOptions$1>;
}
interface OAuthAclSubject {
  principalType: string;
  principalId?: {
    toString(): string;
  } | string | null;
}
interface MCPServerOAuthDeletionSnapshot {
  tokenUserIds: string[];
  aclEntries: OAuthAclSubject[];
}
interface PrepareDeletionParams {
  getTokenUserIds: () => Promise<Array<{
    toString(): string;
  } | string>>;
  getAclEntries: () => Promise<OAuthAclSubject[]>;
}
declare function prepareMCPServerOAuthDeletion({
  getTokenUserIds,
  getAclEntries
}: PrepareDeletionParams): Promise<MCPServerOAuthDeletionSnapshot>;
interface CleanupDeletedUsersParams {
  ownerUserId: string;
  serverName: string;
  serverConfig: MCPOptions$1;
  snapshot: MCPServerOAuthDeletionSnapshot;
  getTokenUserIds: () => Promise<Array<{
    toString(): string;
  } | string>>;
  getUserPrincipals: (userId: string) => Promise<OAuthAclSubject[]>;
  resolveAllowlists: (userId: string) => Promise<{
    allowedDomains?: string[] | null;
    allowedAddresses?: string[] | null;
  }>;
  fenceAndDisconnectUser?: (userId: string) => Promise<void>;
  uninstallOAuthMCP?: (userId: string, pluginKey: string, appConfig: CleanupConfig, serverConfig: MCPOptions$1) => Promise<void>;
}
declare function cleanupDeletedMCPServerOAuthUsers({
  ownerUserId,
  serverName,
  serverConfig,
  snapshot,
  getTokenUserIds,
  getUserPrincipals,
  resolveAllowlists,
  fenceAndDisconnectUser,
  uninstallOAuthMCP
}: CleanupDeletedUsersParams): Promise<void>;
interface MCPOAuthCleanupDependencies {
  flowManager: FlowStateManager<MCPOAuthTokens | null>;
  oauthHandler: Pick<typeof MCPOAuthHandler, "generateFlowId" | "generateTokenFlowId" | "deleteFlowAndStateMapping" | "revokeOAuthToken">;
  tokenStorage: Pick<typeof MCPTokenStorage, "deleteUserTokens" | "getClientInfoAndMetadata" | "getTokens" | "assertCredentialSetBinding"> & Partial<Pick<typeof MCPTokenStorage, "beginRefreshTeardown">>;
  findToken: TokenMethods["findToken"];
  deleteTokens: TokenMethods["deleteTokens"];
  getServerConfig: (serverName: string, userId: string) => Promise<MCPOptions$1 | undefined>;
  isRegisteredOAuthServer: (serverName: string, userId: string) => Promise<boolean>;
}
interface ClearStateParams {
  userId: string;
  serverName: string;
  dependencies: Pick<MCPOAuthCleanupDependencies, "flowManager" | "deleteTokens" | "oauthHandler" | "tokenStorage">;
  skipOAuthFlows?: boolean;
  credentialSetId?: string | null;
  tokenSnapshot?: Map<string, string>;
}
declare function clearStoredMCPOAuthState({
  userId,
  serverName,
  dependencies,
  skipOAuthFlows,
  credentialSetId,
  tokenSnapshot
}: ClearStateParams): Promise<void>;
interface UninstallParams {
  userId: string;
  pluginKey: string;
  appConfig?: CleanupConfig;
  serverConfigOverride?: MCPOptions$1;
  dependencies: MCPOAuthCleanupDependencies;
}
declare function cleanupMCPServerOAuth(params: UninstallParams): Promise<void>;
//#endregion
//#region src/mcp/catalog/recovery.d.ts
interface MCPServerCatalogRecoveryInput {
  serverName: string;
  serverConfig: ParsedServerConfig;
}
interface MCPServerCatalogRecoveryDeps {
  loadUserMCPAuthMap: (userId: string, serverNames: readonly string[]) => Promise<Record<string, Record<string, string>>>;
  discoverServerTools: (options: ToolDiscoveryOptions) => Promise<{
    tools: Tool$1[] | null;
    oauthRequired?: boolean;
    authenticationKind?: "oauth" | "obo" | "server";
  }>;
  formatServerTools: (serverName: string, tools: Tool$1[]) => LCAvailableTools;
  recoveryTracker?: MCPServerCatalogRecoveryTracker;
  getRecoveryGeneration?: MCPRecoveryGenerationReader;
  /** Fences a credential refresh performed during discovery; recovery observes what it publishes. */
  onOAuthCredentialsChanging?: ToolDiscoveryOptions["onOAuthCredentialsChanging"];
}
interface MCPRecoveryGenerationScope {
  userId: string;
  serverName: string;
}
type MCPRecoveryGenerationReader = (scope: MCPRecoveryGenerationScope) => Promise<string | undefined>;
declare function publishMCPAuthorizationMutation(scope: MCPRecoveryGenerationScope, deps: {
  invalidateRecoveryGeneration: (scope: MCPRecoveryGenerationScope) => Promise<unknown>; /** Receives the generation the publication wrote, when it reports one. */
  clearLocalRecovery?: (userId: string, serverName: string, generation?: string) => void;
  persistPublicationRetry?: (scope: MCPRecoveryGenerationScope) => Promise<string>;
  clearPublicationRetry?: (scope: MCPRecoveryGenerationScope, publicationRetryVersion: string) => Promise<void>;
  retryDelaysMs?: readonly number[];
  attemptTimeoutMs?: number;
}): Promise<string | undefined>;
interface MCPServerCatalogRecoveryPolicy {
  discoveryBackoffMs: readonly number[];
  discoveryTimeoutMs: number;
  /** How long past its budget a discovery may keep its catalog slot and coalesced requests. */
  discoverySettleGraceMs: number;
  reauthRetryMs: number;
  maxStateEntries: number;
  /** How many released discoveries may still run before no new discovery starts. */
  maxDetachedDiscoveries: number;
  generationReadTimeoutMs: number;
  authorizationFenceRetryMs: readonly number[];
  authorizationFenceTimeoutMs: number;
  authorizationFenceRetryIntervalMs: number;
  authorizationFenceRetryBatchSize: number;
}
interface MCPServerCatalogSnapshot {
  tools: LCAvailableTools | null;
  publicationGeneration?: string;
  publicationRevision?: string;
}
interface MCPServerCatalogLoaderDeps extends MCPServerCatalogRecoveryDeps {
  getCachedServerTools: (userId: string, serverName: string, serverConfig: ParsedServerConfig) => Promise<LCAvailableTools | null>;
  getServerToolFunctionsSnapshot: (userId: string, serverName: string, serverConfig: ParsedServerConfig, options?: {
    deadlineMs?: number;
    signal?: AbortSignal;
  }) => Promise<MCPServerCatalogSnapshot>;
  cacheServerTools: (params: {
    userId: string;
    serverName: string;
    serverTools: LCAvailableTools;
    serverConfig: ParsedServerConfig;
    publicationGeneration?: string;
    publicationRevision?: string;
  }) => Promise<void>;
}
interface MCPServerCatalogLoaderResult {
  serverTools: Map<string, LCAvailableTools>;
  serversWithoutTools: string[];
  reauthRequiredServers: Set<string>;
  reauthRequiredGenerations: Map<string, string | undefined>;
}
interface RecoveryCandidate extends MCPServerCatalogRecoveryInput {
  customUserVars?: Record<string, string>;
}
type RecoveryOutcome = {
  serverName: string;
  tools: LCAvailableTools | null;
  state?: "reauth_required" | "backoff";
  recoveryGeneration?: string; /** Marks a flight's own result when its generation came from its own publication; never retained. */
  adopted?: boolean; /** Marks a flight's own result when a clear for another generation spared it; never retained. */
  unconfirmed?: boolean;
};
/** Runs a credential publication made by a flight's own discovery and returns what it wrote. */
type PublicationTracker = (publish: () => Promise<string | undefined>) => Promise<string | undefined>;
/** Hands the tracker a discovery released past its settle grace that is still running. */
type DiscoveryDetacher = (discovery: Promise<unknown>) => void;
declare class MCPCatalogCapacityError extends Error {
  readonly code = "MCP_CATALOG_CAPACITY";
  constructor();
}
/** Reads the cross-replica credential fence without making status or recovery depend on shared
* cache availability. The application supplies the cache implementation at its wiring boundary. */
declare function readMCPRecoveryGeneration(scope: MCPRecoveryGenerationScope, reader?: MCPRecoveryGenerationReader, options?: {
  timeoutMs?: number;
  signal?: AbortSignal;
}): Promise<string | undefined>;
declare function readMCPRecoveryGenerationAround<T>(scope: MCPRecoveryGenerationScope, reader: MCPRecoveryGenerationReader | undefined, operation: () => Promise<T>, options?: {
  timeoutMs?: number;
  signal?: AbortSignal;
}): Promise<{
  value: T;
  generation?: string;
}>;
declare class MCPServerCatalogRecoveryTracker {
  private readonly maxStateEntries;
  private readonly maxDetachedDiscoveries;
  private readonly states;
  /** Discoveries released past their settle grace that are still running, by recovery key. */
  private readonly detachedDiscoveries;
  private detachedDiscoveryCount;
  constructor(maxStateEntries?: number, maxDetachedDiscoveries?: number);
  /** Whether a request-bound discovery, which never joins a flight, may start. */
  admitsDiscovery(user: IUser, candidate: RecoveryCandidate, policy: MCPServerCatalogRecoveryPolicy): boolean;
  /** Counts a request-bound discovery released past its settle grace until it settles. */
  detachDiscovery(user: IUser, candidate: RecoveryCandidate, policy: MCPServerCatalogRecoveryPolicy, discovery: Promise<unknown>): void;
  /**
  * A released discovery keeps running, so another attempt under the same observed state would stack
  * on the same stuck dependency. Released discoveries also share one process-wide limit: once that
  * many still run, no new discovery starts until one settles. A changed configuration or credential
  * generation is new state and may discover within that limit. Only settling lifts a hold; clears
  * do not. Discoveries already running when the limit fills can still be released, so the count
  * can exceed it by at most the catalog concurrency.
  */
  private isDiscoveryHeld;
  private detach;
  private touch;
  /**
  * Clears suppression after a credential/config mutation commits. A clear from a publication
  * carries the generation it wrote; see `clearState` for the state it spares.
  */
  clear(userId: string, serverName?: string, generation?: string): void;
  /**
  * A clear from a publication carries the generation it wrote, which only signals that the shared
  * generation advanced. Generations carry no order, so that signal cannot tell whether a flight
  * still in flight predates it: such a flight, when it has a known generation or is publishing its
  * own change, is spared and records the generation, and settles against it once it finishes
  * (see `run`). Any other state is cleared, as is everything on a clear that carries no generation.
  */
  private clearState;
  run(user: IUser, candidate: RecoveryCandidate, policy: MCPServerCatalogRecoveryPolicy, recoveryGeneration: string | undefined, discover: (trackPublication: PublicationTracker, detach: DiscoveryDetacher) => Promise<RecoveryOutcome>): Promise<RecoveryOutcome>;
  private trim;
}
/** Preserves the public recovery helper's Map contract for existing package consumers. */
declare function recoverMCPServerCatalogs(params: {
  user: IUser;
  servers: readonly MCPServerCatalogRecoveryInput[];
  signal?: AbortSignal;
  recoveryPolicy?: Partial<MCPServerCatalogRecoveryPolicy>;
}, deps: MCPServerCatalogRecoveryDeps): Promise<Map<string, LCAvailableTools>>;
/** Loads cached, connected, then passive MCP catalogs for a marketplace-style list request. */
declare function loadMCPServerCatalogs(params: {
  user: IUser;
  servers: readonly MCPServerCatalogRecoveryInput[];
  signal?: AbortSignal;
  recoveryPolicy?: Partial<MCPServerCatalogRecoveryPolicy>;
}, deps: MCPServerCatalogLoaderDeps): Promise<MCPServerCatalogLoaderResult>;
//#endregion
//#region src/mcp/connection.d.ts
interface MCPConnectionParams {
  serverName: string;
  serverConfig: MCPOptions;
  userId?: string;
  oauthTokens?: MCPOAuthTokens | null;
  useSSRFProtection?: boolean;
  allowedAddresses?: string[] | null;
  ephemeralConnection?: boolean;
  /** The owner will replace this connection after a tools/list authentication rejection. */
  directBearerRecoveryEnabled?: boolean;
}
/** Result of an MCP `tools/list` request: one page of tools plus an optional pagination cursor. */
type MCPListToolsResult = Awaited<ReturnType<Client["listTools"]>>;
interface MCPToolsSnapshot {
  tools: MCPListToolsResult["tools"];
  complete: boolean;
  /** Authentication rejection observed by this exact catalog read. */
  authenticationError?: unknown;
  /** Ordering ticket reserved before this snapshot's `tools/list`; app scope only. */
  publicationRevision?: string;
  /** Set when reserving that ticket failed, which is retryable — unlike a scope that simply
  * has no ordering to reserve, where the ticket is absent because none was ever needed. */
  orderingUnavailable?: boolean;
}
declare class MCPConnection extends EventEmitter {
  client: Client;
  private options;
  private transport;
  private connectionState;
  private connectPromise;
  private readonly MAX_RECONNECT_ATTEMPTS;
  readonly serverName: string;
  private shouldStopReconnecting;
  private isReconnecting;
  private isInitializing;
  private reconnectAttempts;
  /** Set once per transport, so only the first of a conflict's repeat reports escalates. */
  private reportedStandaloneSseConflict;
  private agents;
  private readonly userId?;
  private lastPingTime;
  private lastConnectionCheckAt;
  private lastConnectionCheckError?;
  private oauthTokens?;
  private requestHeaders?;
  private oauthRequired;
  private oauthRecovery;
  private readonly useSSRFProtection;
  private readonly allowedAddresses?;
  private readonly ephemeralConnection;
  private readonly directBearerRecoveryEnabled;
  private readonly proxyConfig?;
  private toolListChangeGeneration;
  private handledToolListChangeGeneration;
  private toolListRefreshFailures;
  private toolListRefreshPromise;
  private toolListRefreshRetryTimer;
  private toolListRefreshEpoch;
  private toolListRefreshSuspended;
  private suspendedToolListSnapshot?;
  private publishedToolListSnapshot;
  private hasConnected;
  private isDisposed;
  iconPath?: string;
  timeout?: number;
  sseReadTimeout?: number;
  url?: string;
  /**
  * Timestamp when this connection was created.
  * Used to detect if connection is stale compared to updated config.
  */
  readonly createdAt: number;
  private static circuitBreakers;
  static clearCooldown(serverName: string): void;
  private getCircuitBreaker;
  private isCircuitOpen;
  private recordCycle;
  private recordFailedRound;
  private resetFailedRounds;
  static decrementCycleCount(serverName: string): void;
  setRequestHeaders(headers: Record<string, string> | null): void;
  getRequestHeaders(): Record<string, string> | null | undefined;
  /**
  * Replaces only the runtime bearer, leaving the rest of the per-request headers
  * a tool call installed intact. Runtime headers outrank the ones baked into the
  * transport (see `buildFetchInit`), so a credential refreshed mid-connection has
  * to land here as well or the stale `authorization` from the last tool call would
  * keep winning on the retry.
  */
  setAuthorizationHeader(accessToken: string): void;
  /**
  * Halts reconnect attempts for a credential this connection cannot refresh on its
  * own. Retrying would replay the rejected bearer through every backoff step and
  * spend the circuit breaker's cycle budget; the connection is instead left for its
  * next borrower to dispose and rebuild with a live credential.
  */
  stopReconnecting(): void;
  constructor(params: MCPConnectionParams);
  /** Helper to generate consistent log prefixes */
  private getLogPrefix;
  /**
  * Factory function to create fetch functions without capturing the entire `this` context.
  * This helps prevent memory leaks by only passing necessary dependencies.
  *
  * When `sseBodyTimeout` is provided, a second Agent is created with a much longer
  * body timeout for GET requests (the Streamable HTTP SSE stream). POST requests
  * continue using the normal timeout so they fail fast on real errors.
  */
  private createFetchFunction;
  private emitError;
  private constructTransport;
  private setupEventListeners;
  private handleReconnection;
  private subscribeToResources;
  /**
  * A server that builds tools at runtime tells us so instead of us polling for it.
  *
  * The spec's list-changed flow is: the server notifies, the client asks for the list again. Until
  * this was handled the tool list stayed at whatever it was when the connection came up, so a
  * server that adds a tool mid-session was invisible until a restart (#7117).
  */
  private subscribeToToolListChanges;
  /** Queues a live tool-list refresh through the same coalescing path used by notifications. */
  refreshToolList(signal?: AbortSignal): Promise<MCPToolsSnapshot | undefined>;
  private clearToolListRefreshRetry;
  private scheduleToolListRefreshRetry;
  private startToolListRefresh;
  private refreshChangedTools;
  private dispatchToolsChanged;
  connectClient(): Promise<void>;
  /**
  * Tears down a transport this attempt created after the connection was already disposed.
  * Returns whether the caller should abandon the rest of the connect sequence.
  */
  private abandonIfDisposed;
  private patchTransportSend;
  private setupTransportOnMessageHandler;
  connect(): Promise<void>;
  private setupTransportErrorHandlers;
  private closeAgents;
  /**
  * Ends a Streamable HTTP session with the spec-mandated `HTTP DELETE` before
  * the transport is discarded. Stateful MCP servers (the SDK default) keep each
  * session's transport, tasks, and buffers in memory with no TTL, so without an
  * explicit DELETE every idle eviction, reconnect, or restart leaks one
  * server-side session. Must run before `client.close()`, which aborts the
  * transport's controller and would cancel the in-flight DELETE.
  *
  * `terminateSession()` no-ops when there is no session id and already tolerates
  * a 405 (server opts out of termination). Any other failure is non-fatal to
  * teardown, so it is bounded by a timeout and swallowed.
  *
  * The transport's `onerror` is detached first: on any non-405 failure (the
  * session already expired, a network error, or `client.close()` aborting the
  * request after the timeout) `terminateSession()` invokes `onerror` before
  * rejecting. Left attached, our handler would emit `connectionChange('error')`
  * and trigger `handleReconnection()`, reopening the connection we are tearing
  * down and re-leaking the session. The transport is discarded right after, so
  * clearing the handler is safe.
  */
  private terminateStreamableSession;
  disconnect(resetCycleTracking?: boolean, forceAgentClose?: boolean): Promise<void>;
  /** Permanently tears down a connection that will never be reused. */
  dispose(): Promise<void>;
  fetchResources(): Promise<MCPResource[]>;
  /**
  * Fetches the server's tools, following MCP `tools/list` cursor pagination so a
  * server that spans multiple pages (e.g. an aggregating gateway exposing many
  * tools) is loaded in full instead of being truncated to the first page.
  *
  * Pagination is bounded by {@link mcpConfig.TOOLS_LIST_MAX_PAGES}, aggregate
  * tool count, approximate serialized size, elapsed time, and a repeated-cursor
  * guard. On error, the tools already fetched are returned rather than discarded,
  * and the method never throws.
  */
  fetchTools(): Promise<MCPListToolsResult["tools"]>;
  /**
  * Fetches a bounded tool snapshot while preserving whether every requested page succeeded.
  * Notification refreshes use `complete` to avoid replacing a known-good cache with an empty or
  * partial list after a transient `tools/list` failure.
  *
  * @param deadlineMs Absolute epoch-ms cap for a caller working to a fixed budget. Pagination
  * stops at whichever comes first, this or `TOOLS_LIST_TIMEOUT_MS`, and the partial result is
  * returned as incomplete so it is never published as an authoritative catalog.
  * @param signal Cancels the in-flight `tools/list` request itself, not just the wait for it —
  * the SDK raises an abort from `request()` and the failed page ends pagination gracefully.
  */
  fetchToolsSnapshot(deadlineMs?: number, signal?: AbortSignal): Promise<MCPToolsSnapshot>;
  /**
  * Allocates app-catalog ordering. A reservation failure must not fail the request that asked
  * for the tools, so it is reported on the snapshot for publishers to retry on instead.
  */
  reserveToolsPublicationRevision(): Promise<{
    publicationRevision?: string;
    orderingUnavailable?: boolean;
  }>;
  /**
  * Returns a complete snapshot that cannot precede a concurrent `list_changed` refresh.
  * If the notification refresh cannot complete, the caller receives an incomplete snapshot
  * instead of publishing a request result that may already be stale.
  *
  * @param deadlineMs Absolute epoch-ms cap; see `fetchToolsSnapshot`. It also bounds the wait
  * for a concurrent refresh, so a caller on a budget is never held by another caller's fetch.
  * @param signal Cancels this caller's own `tools/list` requests; see `fetchToolsSnapshot`. A
  * concurrent refresh is shared work and is never aborted on one caller's behalf.
  */
  fetchOrderedToolsSnapshot(deadlineMs?: number, signal?: AbortSignal): Promise<MCPToolsSnapshot>;
  /** Waits for `promise` only until `deadlineMs` or an abort, reporting whether it settled first. */
  private settlesBefore;
  private warnToolsListBudgetExceeded;
  /** Fetches one `tools/list` page. The public snapshot interface classifies any rejection. */
  private listToolsPage;
  fetchPrompts(): Promise<MCPPrompt[]>;
  /**
  * @param signal Aborts the in-flight verification request (ping or its fallback) so a caller on
  * a budget is not held for the probe's own SDK timeout. An aborted probe reports `false` for
  * this caller only; it never mutates connection state, so a shared connection stays usable.
  */
  isConnected(signal?: AbortSignal): Promise<boolean>;
  setOAuthTokens(tokens: MCPOAuthTokens): void;
  /** Whether this connection's resolved runtime config uses MCP OAuth. */
  usesOAuth(): boolean;
  isOAuthAuthenticationError(error: unknown): boolean;
  getLastConnectionCheckError(): unknown;
  /**
  * Check if this connection is stale compared to config update time.
  * A connection is stale if it was created before the config was updated.
  *
  * @param configUpdatedAt - Unix timestamp (ms) when config was last updated
  * @returns true if connection was created before config update, false otherwise
  */
  isStale(configUpdatedAt: number): boolean;
  /**
  * Checks if an error indicates rate limiting (HTTP 429).
  * Rate limited requests should stop reconnection attempts to avoid making the situation worse.
  */
  private isRateLimitError;
}
//#endregion
//#region src/mcp/ConnectionsRepository.d.ts
interface ConnectionLoadOptions {
  continueOnError?: boolean;
  refreshTools?: boolean;
}
/**
* Manages MCP connections with lazy loading and reconnection.
* Maintains a pool of connections and handles connection lifecycle management.
* Queries server configurations dynamically from the MCPServersRegistry (single source of truth).
*
* Scope-aware: Each repository is tied to a specific owner scope:
* - ownerId = undefined → manages app-level servers only
* - ownerId = userId → manages user-level and private servers for that user
*/
declare class ConnectionsRepository {
  protected connections: Map<string, MCPConnection>;
  protected oauthOpts: OAuthConnectionOptions | undefined;
  private readonly ownerId;
  private readonly connectionOperations;
  private shuttingDown;
  constructor(ownerId?: string, oauthOpts?: OAuthConnectionOptions);
  /** Returns the number of active connections in this repository */
  getConnectionCount(): number;
  /** Serializes connection lifecycle transitions for one server without blocking other servers. */
  private runConnectionOperation;
  /** Checks whether this repository can connect to a specific server */
  has(serverName: string): Promise<boolean>;
  /** Gets or creates a connection for the specified server with lazy loading */
  get(serverName: string, options?: ConnectionLoadOptions): Promise<MCPConnection | null>;
  private loadConnection;
  /** Gets or creates connections for multiple servers concurrently */
  getMany(serverNames: string[], options?: ConnectionLoadOptions): Promise<Map<string, MCPConnection>>;
  /** Returns all currently loaded connections without creating new ones */
  getLoaded(): Promise<Map<string, MCPConnection>>;
  /** Gets or creates connections for all configured servers in this repository's scope */
  getAll(options?: ConnectionLoadOptions): Promise<Map<string, MCPConnection>>;
  /** Disconnects and removes a specific server connection from the pool */
  disconnect(serverName: string): Promise<void>;
  private disconnectConnection;
  /** Disconnects all active connections and returns array of disconnect promises */
  disconnectAll(): Promise<void>[];
  private drainAndDisconnectAll;
  protected prefix(): string;
  /**
  * App-level (shared) connections cannot serve servers that need per-user context:
  * env/header placeholders like `{{MY_KEY}}` are only resolved by `processMCPEnv()`
  * when real `customUserVars` values exist — which requires a user-level connection.
  * OBO servers also require a user-level connection because each tool call
  * uses the current user's bearer token.
  */
  private isAllowedToConnectToServer;
}
//#endregion
//#region src/mcp/UserConnectionManager.d.ts
type PendingConnection = {
  promise: Promise<MCPConnection>;
  oauth: OAuthLifecycleRelay;
  directBearerRecoveryState: DirectBearerRecoveryState;
  signal?: AbortSignal;
};
/**
* Why a connection is being torn down. A `mutation` teardown follows a committed change to the
* server config or its credentials, so whatever an in-flight creation resolved before it may be
* stale and that creation cannot be re-established from the same inputs. A `lifecycle` teardown
* — an idle sweep, a forced replacement, a dead connection cleaned up — leaves those inputs
* exactly as valid as they were, and only the connection object itself goes away.
*/
type ConnectionTeardownReason = "mutation" | "lifecycle";
type ConnectionTeardownOptions = {
  /** The creation that requested the teardown; it is fenced by its own replacement. */preservedCreation?: ConnectionCreationGuard;
  reason?: ConnectionTeardownReason;
};
type ConnectionCreationGuard = {
  cancelledBy: ConnectionTeardownReason | null;
};
type ConnectionMutationFence = {
  assertCurrent: () => void;
  release: () => void;
};
/**
* Abstract base class for managing user-specific MCP connections with lifecycle management.
* Only meant to be extended by MCPManager.
* Much of the logic was move here from the old MCPManager to make it more manageable.
* User connections will soon be ephemeral and not cached anymore:
* https://github.com/danny-avila/TerraMind/discussions/8790
*/
declare abstract class UserConnectionManager {
  appConnections: ConnectionsRepository | null;
  protected userConnections: Map<string, Map<string, MCPConnection>>;
  /** Last activity timestamp for users (not per server) */
  protected userLastActivity: Map<string, number>;
  /** In-flight connection promises keyed by `userId:serverName` — coalesces concurrent attempts */
  protected pendingConnections: Map<string, PendingConnection>;
  /** Recovery state owned by request-scoped promises, which the public store intentionally keeps opaque. */
  private readonly requestPendingDirectBearerRecoveryStates;
  private readonly connectionBorrowers;
  private readonly connectionBorrowerDrainWaiters;
  private readonly deferredConnectionDisposalHolds;
  private readonly deferredConnectionDisposals;
  /** All durable creations, including forced replacements, visible to mutation teardown. */
  private readonly activeConnectionCreations;
  /** Serializes explicit durable replacements without coalescing their callers. */
  private readonly forceNewConnectionQueues;
  /** Fences durable connections whose credentials were invalidated on another replica. */
  protected readonly toolPublicationGenerations: WeakMap<MCPConnection, string>;
  /** Binds a durable connection to the stored config that created it, independently of Redis. */
  protected readonly toolConfigGenerations: WeakMap<MCPConnection, string>;
  /** Limits Redis lease refreshes while ensuring active connections cannot outlive their lease. */
  private readonly toolPublicationLeaseRefreshes;
  /** Coalesces concurrent activity updates for the same durable connection. */
  private readonly toolPublicationLeaseRenewals;
  /** Records connections whose distributed publication authority was replaced during renewal. */
  private readonly lostToolPublicationLeases;
  /** Returns the cache-publication generation captured for a durable connection. */
  getToolPublicationGeneration(connection: MCPConnection): string | undefined;
  /** Returns the config identity captured when a durable connection was created. */
  getToolConfigGeneration(connection: MCPConnection): string | undefined;
  private runWithForceNewConnectionQueue;
  private registerConnectionCreation;
  private unregisterConnectionCreation;
  private assertCreationNotCancelled;
  /** Registers work that resolves a replacement before the ordinary creation queue begins. */
  protected createConnectionMutationFence(userId: string, serverName: string): ConnectionMutationFence;
  /** A mutation fence outranks a lifecycle one: the stale inputs stay stale either way. */
  private cancelConnectionCreations;
  private renewUserToolPublicationLeases;
  /** Updates activity and keeps every durable connection retained by that user leased. */
  protected updateUserLastActivity(userId: string): Promise<void>;
  private assertToolPublicationLeaseCurrent;
  /**
  * A teardown fences every creation in flight for the same user and server, since one of them
  * may install the connection it is removing, or hand back the one it just disposed. What the
  * fenced attempt deserves depends on why the teardown ran. After a `lifecycle` teardown it is
  * next in line rather than stale — nothing its caller resolved changed — so the attempt is
  * discarded and re-established. After a `mutation` teardown the config and credentials that
  * caller resolved may be the ones that were just replaced, and only the caller can resolve
  * them again, so the fence stays fatal. A caller that has since aborted gets neither: there
  * is nobody left to hand the connection to.
  *
  * Re-establishment happens inside the queue slot the attempt already holds, so a replacement
  * queued behind another one keeps its position instead of moving to the tail and overwriting
  * a newer connection later.
  */
  private createUserConnectionWithLifecycleRestarts;
  /** Gets or creates a connection for a specific user, coalescing concurrent attempts */
  getUserConnection(opts: UserMCPConnectionOptions): Promise<MCPConnection>;
  private createUserConnectionInternal;
  /**
  * An explicitly startup-deferred, context-independent YAML server has no
  * fetched text until its first live connection. Persist that static text so
  * subsequent context builds include it. OAuth/OBO, user credentials, custom
  * variables, and runtime placeholders stay connection-scoped: their
  * instructions can vary by identity or request and must never enter the
  * shared registry. Best-effort: a failure here must never break connection
  * creation.
  */
  protected backfillResolvedInstructions(serverName: string, config: ParsedServerConfig | undefined, connection: MCPConnection, userId: string): Promise<void>;
  /**
  * Mirrors the resolution MCPConnectionFactory performs internally
  * (preProcessGraphTokens + processMCPEnv). Both must stay in sync so the
  * config validated here matches the one the factory actually connects with.
  */
  protected resolveRuntimeConfig({
    config,
    user,
    customUserVars,
    requestBody,
    graphTokenResolver
  }: {
    config: ParsedServerConfig;
    user?: UserMCPConnectionOptions["user"];
    customUserVars?: Record<string, string>;
    requestBody?: UserMCPConnectionOptions["requestBody"];
    graphTokenResolver?: UserMCPConnectionOptions["graphTokenResolver"];
  }): Promise<ParsedServerConfig>;
  protected assertResolvedRuntimeConfigAllowed({
    config,
    user,
    customUserVars,
    requestBody,
    graphTokenResolver,
    allowedDomains,
    allowedAddresses,
    logPrefix
  }: {
    config: ParsedServerConfig;
    user?: UserMCPConnectionOptions["user"];
    customUserVars?: Record<string, string>;
    requestBody?: UserMCPConnectionOptions["requestBody"];
    graphTokenResolver?: UserMCPConnectionOptions["graphTokenResolver"];
    allowedDomains?: string[] | null;
    allowedAddresses?: string[] | null;
    logPrefix: string;
  }): Promise<ParsedServerConfig>;
  private applyRuntimeOAuthDetection;
  /** Returns all connections for a specific user */
  getUserConnections(userId: string): Map<string, MCPConnection> | undefined;
  /** Removes a specific user connection entry */
  protected removeUserConnection(userId: string, serverName: string): void;
  protected retainConnection(connection: MCPConnection): void;
  protected getActiveConnectionRecovery(_connection: MCPConnection): Promise<void> | undefined;
  protected propagateDirectBearerRecoveryState(_connection: MCPConnection, _state?: DirectBearerRecoveryState): void;
  protected waitForConnectionRecovery(recovery: Promise<void>, _signal?: AbortSignal): Promise<void>;
  protected holdDeferredConnectionDisposal(connection: MCPConnection): void;
  protected releaseDeferredConnectionDisposal(connection: MCPConnection): Promise<void>;
  protected releaseConnection(connection: MCPConnection): Promise<void>;
  protected hasConnectionBorrowers(connection: MCPConnection): boolean;
  protected waitForConnectionBorrowersToDrain(connection: MCPConnection): Promise<void>;
  protected bindRequestScopedConnectionStore(requestScopedConnections?: RequestScopedMCPConnectionStore): void;
  protected disposeEvictedConnection(connection: MCPConnection, logPrefix: string): Promise<void>;
  private finalizeDeferredConnectionDisposal;
  private disposeConnection;
  /**
  * Disconnects and removes a specific user connection. Teardowns default to `mutation`, the
  * conservative reading for external callers, which all tear down after committing a change.
  */
  disconnectUserConnection(userId: string, serverName: string, {
    preservedCreation,
    reason
  }?: ConnectionTeardownOptions): Promise<void>;
  /** Disconnects and removes all connections for a specific user */
  disconnectUserConnections(userId: string, {
    preservedCreation,
    reason
  }?: ConnectionTeardownOptions): Promise<void>;
  /** Check for and disconnect idle connections */
  protected checkIdleConnections(currentUserId?: string): void;
  /** Returns counts of tracked users and connections for diagnostics */
  getConnectionStats(): {
    trackedUsers: number;
    totalConnections: number;
    activityEntries: number;
    appConnectionCount: number;
  };
}
//#endregion
//#region src/mcp/MCPManager.d.ts
/**
* Centralized manager for MCP server connections and tool execution.
* Extends UserConnectionManager to handle both app-level and user-specific connections.
*/
declare class MCPManager extends UserConnectionManager {
  private static instance;
  private readonly catalogRecoveryTracker;
  private readonly recoveryCancellation;
  private readonly oauthRecoveries;
  constructor(catalogRecoveryMaxStateEntries?: number, catalogRecoveryMaxDetachedDiscoveries?: number);
  /** Creates and initializes the singleton MCPManager instance */
  static createInstance(configs: MCPServers, options?: {
    catalogRecoveryMaxStateEntries?: number;
    catalogRecoveryMaxDetachedDiscoveries?: number;
  }): Promise<MCPManager>;
  /** Returns the singleton MCPManager instance */
  static getInstance(): MCPManager;
  /** Initializes the MCPManager by setting up server registry and app connections */
  initialize(configs: MCPServers): Promise<void>;
  getCatalogRecoveryTracker(): MCPServerCatalogRecoveryTracker;
  clearCatalogRecoveryState(userId: string, serverName?: string, generation?: string): void;
  override disconnectUserConnection(userId: string, serverName: string, options?: Parameters<UserConnectionManager["disconnectUserConnection"]>[2]): Promise<void>;
  override getUserConnection(opts: UserMCPConnectionOptions): Promise<MCPConnection>;
  /** Runs work against a user connection while preventing recovery from replacing its SDK client. */
  withUserConnectionLease<TResult>(opts: UserMCPConnectionOptions, operation: (connection: MCPConnection) => Promise<TResult>): Promise<TResult>;
  private waitForActiveRecovery;
  protected override getActiveConnectionRecovery(connection: MCPConnection): Promise<void> | undefined;
  protected override propagateDirectBearerRecoveryState(connection: MCPConnection, state?: DirectBearerRecoveryState): void;
  protected override waitForConnectionRecovery(recovery: Promise<void>, signal?: AbortSignal): Promise<void>;
  private claimRecoveryTakeover;
  /** Retrieves an app-level or user-specific connection based on provided arguments */
  getConnection(args: {
    serverName: string;
    user?: IUser;
    forceNew?: boolean;
    ephemeralConnection?: boolean;
    flowManager?: FlowStateManager<MCPOAuthTokens | null>; /** Pre-resolved config for config-source servers not in YAML/DB */
    serverConfig?: ParsedServerConfig; /** One-shot direct-bearer recovery budget shared with the invoking tool call. */
    directBearerRecoveryState?: DirectBearerRecoveryState;
  } & Omit<OAuthConnectionOptions, "useOAuth" | "user" | "flowManager">): Promise<MCPConnection>;
  /**
  * Discovers tools from an MCP server, even when OAuth is required.
  * Per MCP spec, tool listing should be possible without authentication.
  * Use this for agent initialization to get tool schemas before OAuth flow.
  */
  discoverServerTools(args: ToolDiscoveryOptions): Promise<ToolDiscoveryResult>;
  /** Returns all available tool functions from app-level connections */
  getAppToolFunctions(): Promise<LCAvailableTools>;
  /** Opens eligible app-shared sessions after the inspected startup catalog has been cached. */
  connectAppServers(): Promise<void>;
  /** Closes app-shared MCP sessions during graceful process shutdown. */
  disconnectAppServers(): Promise<void>;
  /** Returns tool functions with the generation bound to their originating user connection. */
  getServerToolFunctionsSnapshot(userId: string, serverName: string, serverConfig?: ParsedServerConfig, options?: {
    deadlineMs?: number;
    signal?: AbortSignal;
  }): Promise<{
    tools: LCAvailableTools | null;
    publicationGeneration?: string;
    publicationRevision?: string;
  }>;
  /** Returns all available tool functions from all connections available to user. */
  getServerToolFunctions(userId: string, serverName: string): Promise<LCAvailableTools | null>;
  /**
  * Get instructions for MCP servers
  * @param serverNames Optional array of server names. If not provided or empty, returns all servers.
  * @returns Object mapping server names to their instructions
  */
  private getInstructions;
  /**
  * Format MCP server instructions for injection into context
  * @param serverNames Optional array of server names to include. If not provided, includes all servers.
  * @returns Formatted instructions string ready for context injection
  */
  formatInstructionsForContext(serverNames?: string[], configServers?: Record<string, ParsedServerConfig>): Promise<string>;
  private recoverOAuthConnection;
  private recoverDirectOpenIDBearerConnection;
  private connectAfterOAuthRecovery;
  private connectWithTransientRetries;
  private waitForOAuthReconnectRetry;
  private connectOnceAfterOAuth;
  private waitForOAuthRecovery;
  /**
  * Calls a tool on an MCP server, using either a user-specific connection
  * (if userId is provided) or an app-level connection. Updates the last activity timestamp
  * for user-specific connections upon successful call initiation.
  *
  * @param graphTokenResolver - Optional function to resolve Graph API tokens via OBO flow.
  *   When provided and the server config contains `{{LIBRECHAT_GRAPH_ACCESS_TOKEN}}` placeholders,
  *   they will be resolved to actual Graph API tokens before the tool call.
  */
  callTool({
    user,
    serverName,
    serverConfig: providedConfig,
    toolName,
    provider,
    toolArguments,
    options,
    tokenMethods,
    requestBody,
    requestScopedConnections,
    flowManager,
    oauthStart,
    oauthEnd,
    customUserVars,
    graphTokenResolver,
    oboTokenResolver,
    oboTrustChecker,
    upstreamTokenProvider,
    upstreamTokenProviderResolver,
    oboIdentityContext,
    onOAuthCredentialsChanged,
    onOAuthCredentialsChanging
  }: {
    user?: IUser;
    serverName: string; /** Pre-resolved config from tool creation context — avoids readThrough TTL and cross-tenant issues */
    serverConfig?: ParsedServerConfig;
    toolName: string;
    provider: Provider;
    toolArguments?: Record<string, unknown>;
    options?: RequestOptions;
    requestBody?: RequestBody;
    requestScopedConnections?: RequestScopedMCPConnectionStore;
    tokenMethods?: TokenMethods;
    customUserVars?: Record<string, string>;
    flowManager: FlowStateManager<MCPOAuthTokens | null>;
    oauthStart?: OAuthStartHandler;
    oauthEnd?: () => Promise<void>;
    graphTokenResolver?: GraphTokenResolver;
    oboTokenResolver?: OboTokenResolver;
    oboTrustChecker?: OboTrustChecker;
    upstreamTokenProvider?: UpstreamTokenProvider;
    upstreamTokenProviderResolver?: UpstreamTokenProviderResolver;
    oboIdentityContext?: AuthIdentityContext;
    onOAuthCredentialsChanged?: UserConnectionContext["onOAuthCredentialsChanged"];
    onOAuthCredentialsChanging?: UserConnectionContext["onOAuthCredentialsChanging"];
  }): Promise<FormattedToolResponse>;
}
//#endregion
//#region src/mcp/toolsChanged.d.ts
/** Returns a stable token for the connection-relevant portion of an MCP config. */
declare function getMCPAppToolsPublicationGeneration(config: ParsedServerConfig): string;
/** A complete tool-list snapshot and the cache scope it belongs to. */
interface MCPToolsChangedEvent {
  serverName: string;
  tools: Tool$1[];
  serverConfig: MCPOptions;
  userId?: string;
  /** Connection-bound token used to fence stale cross-replica cache publications. */
  publicationGeneration?: string;
  /** Monotonic ticket assigned before an app-level tools/list request begins. */
  publicationRevision?: string;
}
type MCPToolsChangedHandler = (event: MCPToolsChangedEvent) => Promise<void> | void;
type MCPToolsChangedScope = Pick<MCPToolsChangedEvent, "serverName" | "userId">;
type MCPToolsChangedGenerationHandler = (scope: MCPToolsChangedScope) => Promise<string | undefined> | string | undefined;
type MCPToolsChangedGenerationRenewalHandler = (scope: MCPToolsChangedScope & {
  publicationGeneration: string;
}) => Promise<boolean> | boolean;
type MCPToolsChangedRevisionHandler = (scope: {
  serverName: string;
  configGeneration: string;
}) => Promise<string> | string;
/** Registers the app-layer publisher for refreshed MCP tool snapshots. */
declare function setMCPToolsChangedHandler(fn: MCPToolsChangedHandler | null): void;
declare function hasMCPToolsChangedHandler(): boolean;
/** Registers the app-layer provider for connection-bound publication generations. */
declare function setMCPToolsChangedGenerationHandler(fn: MCPToolsChangedGenerationHandler | null): void;
/** Registers the app-layer lease renewer for active durable user connections. */
declare function setMCPToolsChangedGenerationRenewalHandler(fn: MCPToolsChangedGenerationRenewalHandler | null): void;
/** Registers the shared app-catalog revision allocator. */
declare function setMCPToolsChangedRevisionHandler(fn: MCPToolsChangedRevisionHandler | null): void;
/** Captures the current cache generation before a durable user connection is created. */
declare function getMCPToolsChangedGeneration(scope: MCPToolsChangedScope): Promise<string | undefined>;
/** Renews a connection's publication lease without allowing a stale generation to revive. */
declare function renewMCPToolsChangedGeneration(scope: MCPToolsChangedScope & {
  publicationGeneration: string;
}): Promise<boolean | undefined>;
/** Reserves ordering before an app-level tools/list request starts. */
declare function reserveMCPToolsChangedRevision(scope: {
  serverName: string;
  serverConfig: ParsedServerConfig;
  userId?: string;
}): Promise<string | undefined>;
/**
* Publishes the latest snapshot for a server. Concurrent notifications are single-flighted and
* cache-write failures retain the latest snapshot for bounded-backoff retries.
*/
declare function notifyMCPToolsChanged(event: MCPToolsChangedEvent): Promise<void>;
/** Cancels queued retries and drains an in-flight publication before cache invalidation. */
declare function cancelMCPToolsChanged(scope: MCPToolsChangedScope): Promise<void>;
//#endregion
//#region src/mcp/auth.d.ts
/** Reads one server's customUserVars from a `getUserMCPAuthMap` result */
declare function getServerCustomUserVars(userMCPAuthMap: Record<string, Record<string, string>> | undefined, serverName: string): Record<string, string> | undefined;
declare function getUserMCPAuthMap({
  userId,
  tools,
  servers,
  toolInstances,
  serverNames,
  findPluginAuthsByKeys
}: {
  userId: string;
  tools?: (string | undefined)[];
  servers?: (string | undefined)[];
  toolInstances?: (GenericTool | null)[];
  /**
  * Configured server names in their RAW config form. Tool keys embed the
  * normalized form, so the boundary is resolved against both spellings and
  * the auth-map key is always built from the raw name — the form plugin-auth
  * rows are stored under and every reader passes.
  */
  serverNames?: readonly string[];
  findPluginAuthsByKeys: PluginAuthMethods["findPluginAuthsByKeys"];
}): Promise<Record<string, Record<string, string>>>;
//#endregion
//#region src/mcp/zod.d.ts
/**
* Helper function to resolve $ref references. Resolves `#/$defs/...` and
* `#/definitions/...` refs via the definitions map, and any other local
* `#/...` pointer (e.g. `#/properties/foo`) against the root schema —
* MCP servers with OpenAPI-derived schemas commonly emit both forms.
* @param schema - The schema to resolve
* @param definitions - The definitions to use
* @param visited - The set of visited references
* @param root - The root schema local pointers resolve against (defaults to `schema`)
* @returns The resolved schema
*/
/**
* Caps how many nodes a single resolution may emit. A remote MCP server controls
* this schema, and sibling references to the same definition each re-expand, so a
* compact acyclic graph can blow up exponentially (`Dn` holding two refs to
* `Dn-1` is 2^n). Past the cap the reference is left unexpanded rather than
* exhausting memory during registration.
*/
declare const MAX_RESOLVED_SCHEMA_NODES = 5e4;
interface ResolveBudget {
  remaining: number;
}
declare function resolveJsonSchemaRefs<T extends Record<string, unknown>>(schema: T, definitions?: Record<string, unknown>, visited?: Set<string>, root?: Record<string, unknown>, budget?: ResolveBudget): T;
declare function normalizeJsonSchema<T extends Record<string, unknown>>(schema: T): T;
/**
* Sanitizes a JSON schema to Gemini/Vertex AI's function-calling Schema subset
* (https://ai.google.dev/api/caching#Schema), recursively. Gemini accepts only a
* restricted slice of JSON Schema, and `@langchain/google-common`'s
* `zod_to_gemini_parameters` additionally throws on any union — so MCP tools that
* ship richer schemas crash on the Google endpoint while working on OpenAI/Claude.
*
* Transforms (all lossy and Gemini-specific — gate on the Google/Vertex provider,
* run after `normalizeJsonSchema`):
* - Collapses `anyOf`/`oneOf` to the first non-null member (merging parent + branch
*   `properties`/`required`), and merges `allOf` intersections.
* - Collapses multi-entry `type` arrays to a single type, tracking `nullable`.
* - Keeps only string `enum` values — Gemini's `enum` is `Type.STRING`-only — and
*   drops the keyword entirely for non-string types (e.g. a boolean `const`
*   normalized to `enum: [true]`).
* - Folds `exclusiveMinimum`/`exclusiveMaximum` into `minimum`/`maximum`.
* - Strips `const` (after enum conversion) and every keyword in `GEMINI_UNSUPPORTED_KEYS`
*   (`additionalProperties`, `examples`, `readOnly`, `multipleOf`, `uniqueItems`,
*   `patternProperties`, `prefixItems`, etc.) that the Gemini schema validator rejects.
*
* @param schema - The JSON schema to sanitize
* @returns The Gemini-compatible schema
*/
declare function sanitizeGeminiSchema<T extends Record<string, unknown>>(schema: T): T;
/**
* Converts a JSON Schema to a Zod schema.
*
* @deprecated This function is deprecated in favor of using JSON schemas directly.
* LangChain.js now supports JSON schemas natively, eliminating the need for Zod conversion.
* Use `resolveJsonSchemaRefs` to handle $ref references and pass the JSON schema directly to tools.
*
* @see https://js.langchain.com/docs/how_to/custom_tools/
*/
declare function convertJsonSchemaToZod(schema: JsonSchemaType$1 & Record<string, unknown>, options?: ConvertJsonSchemaToZodOptions): z.ZodType | undefined;
/**
* Helper function that resolves refs before converting to Zod.
*
* @deprecated This function is deprecated in favor of using JSON schemas directly.
* LangChain.js now supports JSON schemas natively, eliminating the need for Zod conversion.
* Use `resolveJsonSchemaRefs` to handle $ref references and pass the JSON schema directly to tools.
*
* @see https://js.langchain.com/docs/how_to/custom_tools/
*/
declare function convertWithResolvedRefs(schema: JsonSchemaType$1 & Record<string, unknown>, options?: ConvertJsonSchemaToZodOptions): z.ZodType | undefined;
//#endregion
//#region src/mcp/errors.d.ts
/** These outcomes must reach the owning run instead of hiding a tool during discovery. */
declare function isMCPInitializationError(error: unknown, signal?: AbortSignal): boolean;
declare const MCPErrorCodes: {
  readonly DOMAIN_NOT_ALLOWED: "MCP_DOMAIN_NOT_ALLOWED";
  readonly INSPECTION_FAILED: "MCP_INSPECTION_FAILED";
  readonly OAUTH_SECRET_REENTRY_REQUIRED: "MCP_OAUTH_SECRET_REENTRY_REQUIRED";
  readonly AUTHENTICATION_REJECTED: "MCP_AUTHENTICATION_REJECTED";
  readonly AUTHENTICATION_REFRESH_FAILED: "MCP_AUTHENTICATION_REFRESH_FAILED";
};
type MCPErrorCode = (typeof MCPErrorCodes)[keyof typeof MCPErrorCodes];
/** A JSON-RPC tool error is never evidence that the MCP transport rejected its bearer. */
declare function isMCPTransportAuthenticationError(error: unknown): boolean;
/** Preserves the actual HTTP status before legacy SSE reduces a failed POST to plain text. */
declare class MCPTransportAuthenticationError extends Error {
  readonly status: 401 | 403;
  constructor(status: 401 | 403);
}
/** Detects HTTP authentication failures and OAuth protocol errors without matching unrelated IDs. */
declare function isOAuthAuthenticationError(error: unknown): boolean;
/**
* A `409` opening the standalone `GET` SSE stream means the server still has the previous
* stream mapped to our session. The spec allows only one such stream per session, and the
* server drops its mapping from the response stream's cancel callback — which never runs when
* the connection dies at a proxy instead of at the client, leaving the server holding a stream
* no one is reading.
*
* Retrying cannot clear it: every `GET` carrying that session id conflicts for the same reason.
* Only a transport rebuild does, because the teardown `DELETE` drops the server's session along
* with the stream it leaked.
*/
declare function isStandaloneSseConflict(error: unknown): boolean;
interface SSEErrorDetails {
  message: string;
  code?: number;
  isProxyHint: boolean;
  isTransient: boolean;
}
/**
* Extracts a meaningful error message from SSE transport errors.
* The MCP SDK's SSEClientTransport can produce "SSE error: undefined" when the
* underlying eventsource library encounters connection issues without a specific message.
*
* @returns Object containing:
*   - message: Human-readable error description
*   - code: HTTP status code if available
*   - isProxyHint: Whether this error suggests proxy misconfiguration
*   - isTransient: Whether this is likely a transient error that will auto-reconnect
*/
declare function extractSSEErrorMessage(error: unknown): SSEErrorDetails;
/**
* Custom error for MCP domain restriction violations.
* Thrown when a user attempts to connect to an MCP server whose domain is not in the allowlist.
*/
declare class MCPDomainNotAllowedError extends Error {
  readonly code: "MCP_DOMAIN_NOT_ALLOWED";
  readonly statusCode = 403;
  readonly domain: string;
  constructor(domain: string);
}
/**
* Custom error for MCP server inspection failures.
* Thrown when attempting to connect/inspect an MCP server fails.
*/
declare class MCPInspectionFailedError extends Error {
  readonly code: "MCP_INSPECTION_FAILED";
  readonly statusCode = 400;
  readonly serverName: string;
  constructor(serverName: string, cause?: Error);
}
/** Raised when an update would move a retained OAuth client secret across trust boundaries. */
declare class MCPOAuthSecretReentryRequiredError extends Error {
  readonly code: "MCP_OAUTH_SECRET_REENTRY_REQUIRED";
  readonly statusCode = 400;
  readonly changedFields: readonly string[];
  constructor(changedFields: readonly string[]);
}
/**
* A tool invocation was rejected before TerraMind could know whether the MCP
* server executed it. Recovery may prepare a later deliberate retry, but this
* outcome never authorizes an automatic replay of the original call.
*/
declare class MCPAuthenticationRejectedError extends Error {
  readonly serverName: string;
  readonly connectionRefreshed: boolean;
  readonly code: "MCP_AUTHENTICATION_REJECTED";
  readonly statusCode = 403;
  readonly retryable: boolean;
  constructor(serverName: string, connectionRefreshed: boolean, cause?: unknown);
}
/** A temporary upstream-session refresh failure, distinct from expired login state. */
declare class MCPAuthenticationRefreshError extends Error {
  readonly code: "MCP_AUTHENTICATION_REFRESH_FAILED";
  readonly statusCode = 503;
  readonly retryable = true;
  constructor(cause?: unknown);
}
/**
* Type guard to check if an error is an MCPDomainNotAllowedError
*/
declare function isMCPDomainNotAllowedError(error: unknown): error is MCPDomainNotAllowedError;
/**
* Type guard to check if an error is an MCPInspectionFailedError
*/
declare function isMCPInspectionFailedError(error: unknown): error is MCPInspectionFailedError;
/** Type guard for OAuth client-secret binding violations. */
declare function isMCPOAuthSecretReentryRequiredError(error: unknown): error is MCPOAuthSecretReentryRequiredError;
//#endregion
//#region src/mcp/openid.d.ts
type DirectBearerConfig = MCPOptions & {
  dbId?: string;
  source?: "yaml" | "config" | "user" | "plugin";
};
/** Whether a trusted operator config explicitly routes its OpenID bearer to this server. */
declare function isDirectOpenIDBearerRecoveryEnabled(config: DirectBearerConfig): boolean;
/** Whether a trusted direct-bearer config still needs its live placeholder resolved. */
declare function usesDirectOpenIDBearerRecovery(config: DirectBearerConfig): boolean;
/** Resolves the live bearer before a connection or request reaches the MCP transport. */
declare function resolveDirectOpenIDBearerConfig({
  config,
  upstreamTokenProvider,
  forceRefresh,
  resolvedConfig,
  signal
}: {
  config: DirectBearerConfig;
  upstreamTokenProvider?: UpstreamTokenProvider;
  forceRefresh?: boolean;
  resolvedConfig?: MCPOptions;
  signal?: AbortSignal;
}): Promise<DirectBearerConfig>;
//#endregion
//#region src/mcp/cache.d.ts
/**
* Clears config-source MCP server inspection cache so servers are re-inspected on next access.
* Best-effort disconnection of app-level connections for evicted servers.
*
* User-level connections (used by config-source servers) are cleaned up lazily via
* the stale-check mechanism on the next tool call — this is an accepted design tradeoff
* since iterating all active user sessions is expensive and config mutations are rare.
*/
declare function clearMcpConfigCache(): Promise<void>;
//#endregion
//#region src/mcp/tools.d.ts
type MCPToolInput = Pick<Tool$1, "name" | "description"> & Partial<Pick<Tool$1, "inputSchema">>;
interface MCPToolCacheDeps {
  getCachedTools: (options?: {
    userId?: string;
    serverName?: string;
    configGeneration?: string;
  }) => Promise<LCAvailableTools | null>;
  updateCachedGlobalTools?: (update: (tools: LCAvailableTools) => LCAvailableTools) => Promise<void>;
  setCachedTools: (tools: LCAvailableTools, options?: {
    userId?: string;
    serverName?: string;
    configGeneration?: string;
  }) => Promise<boolean>;
  setCachedToolsIfCurrent?: (tools: LCAvailableTools, options: {
    userId: string;
    serverName: string;
    configGeneration: string;
    publicationGeneration: string;
  }) => Promise<boolean>;
  getCachedAppServerTools: (serverName: string, configGeneration: string) => Promise<LCAvailableTools | null>;
  setCachedAppServerTools: (serverName: string, configGeneration: string, tools: LCAvailableTools, publicationRevision?: string) => Promise<boolean>;
  getServerConfig: (serverName: string, userId?: string) => Promise<ParsedServerConfig | undefined>;
  getAllServerConfigs?: () => Promise<Record<string, ParsedServerConfig>>;
  isAppServerConfig?: (serverName: string, effectiveConfig: ParsedServerConfig) => Promise<boolean>;
}
interface MCPToolCacheService {
  updateMCPServerTools: (params: {
    userId?: string;
    serverName: string;
    tools: MCPToolInput[] | null;
    serverConfig?: ParsedServerConfig;
    publicationGeneration?: string;
    publicationRevision?: string;
  }) => Promise<LCAvailableTools | null>;
  syncStaticTools: (staticTools: LCAvailableTools) => Promise<void>;
  mergeAppTools: (appTools: LCAvailableTools, staticTools: LCAvailableTools) => Promise<void>;
  replaceAppServerTools: (params: {
    serverName: string;
    serverTools: LCAvailableTools;
    publicationGeneration?: string;
    publicationRevision?: string;
  }) => Promise<boolean>;
  cacheMCPServerTools: (params: {
    userId: string;
    serverName: string;
    serverTools: LCAvailableTools;
    serverConfig?: ParsedServerConfig;
    publicationGeneration?: string;
    publicationRevision?: string;
  }) => Promise<void>;
  getMCPServerTools: (userId: string, serverName: string, serverConfig?: ParsedServerConfig) => Promise<LCAvailableTools | null>;
}
/** Converts an MCP tools/list response into TerraMind's server-qualified catalog format. */
declare function formatMCPServerTools(serverName: string, tools: MCPToolInput[]): LCAvailableTools;
declare function createMCPToolCacheService(deps: MCPToolCacheDeps): MCPToolCacheService;
//#endregion
//#region src/mcp/catalog/store.d.ts
declare const ToolCacheKeys: {
  GLOBAL: string;
  MCP_APP_SERVER: (serverName: string, configGeneration: string) => string;
  MCP_SERVER: (userId: string, serverName: string, configGeneration?: string) => string;
  MCP_SERVER_GENERATION: (userId: string, serverName: string) => string;
  MCP_SERVER_LEGACY_FENCE: (userId: string, serverName: string) => string;
};
interface CatalogCache {
  get(key: string): Promise<unknown>;
  set(key: string, value: unknown, ttl?: number): Promise<boolean | void>;
  delete(key: string): Promise<boolean | void>;
}
interface LockRedisClient {
  set(key: string, value: string, mode: "PX", ttl: number, condition: "NX"): Promise<string | null>;
  eval(script: string, numberOfKeys: number, ...args: string[]): Promise<unknown>;
}
interface KeyvRedisClient {
  eval(script: string, options: {
    keys: string[];
    arguments: string[];
  }): Promise<unknown>;
}
interface CatalogStoreDeps {
  getCache: () => CatalogCache;
  cacheConfig: {
    FORCED_IN_MEMORY_CACHE_NAMESPACES?: string[];
    REDIS_KEY_PREFIX?: string;
    GLOBAL_PREFIX_SEPARATOR?: string;
  };
  ioredisClient?: LockRedisClient | null;
  keyvRedisClient?: KeyvRedisClient | null;
  waitForRedis?: () => Promise<void>;
  userConnectionIdleTimeout?: number | string;
}
interface CachedToolsOptions {
  userId?: string;
  serverName?: string;
  configGeneration?: string;
  ttl?: number;
}
interface GuardedToolsOptions extends CachedToolsOptions {
  userId: string;
  serverName: string;
  configGeneration: string;
  publicationGeneration: string;
}
interface MCPCatalogStore {
  getCachedTools: (options?: CachedToolsOptions) => Promise<LCAvailableTools | null>;
  updateCachedGlobalTools: (update: (tools: LCAvailableTools) => LCAvailableTools) => Promise<void>;
  setCachedTools: (tools: LCAvailableTools, options?: CachedToolsOptions) => Promise<boolean>;
  setCachedToolsWithinGlobalLock: (tools: LCAvailableTools, options?: CachedToolsOptions) => Promise<boolean>;
  setCachedToolsIfCurrent: (tools: LCAvailableTools, options: GuardedToolsOptions) => Promise<boolean>;
  getMCPToolsCacheGeneration: (scope: {
    userId: string;
    serverName: string;
  }) => Promise<string | undefined>;
  renewMCPToolsCacheGeneration: (scope: {
    userId: string;
    serverName: string;
    publicationGeneration: string;
  }) => Promise<boolean>;
  getCachedAppServerTools: (serverName: string, configGeneration: string) => Promise<LCAvailableTools | null>;
  getNextAppToolsPublicationRevision: (serverName: string, configGeneration: string) => Promise<string>;
  setCachedAppServerTools: (serverName: string, configGeneration: string, tools: LCAvailableTools, publicationRevision?: string, ttl?: number) => Promise<boolean>;
  runWithGlobalCacheLock: <T>(operation: () => Promise<T>) => Promise<T>;
  invalidateCachedTools: (options?: {
    userId?: string;
    serverName?: string;
    invalidateGlobal?: boolean;
  }) => Promise<string | undefined>;
}
declare function createMCPCatalogStore(deps: CatalogStoreDeps): MCPCatalogStore;
//#endregion
//#region src/mcp/authorization.d.ts
interface MCPAuthorizationPublicationDeps {
  invalidateRecoveryGeneration: (scope: MCPRecoveryGenerationScope) => Promise<unknown>;
  /** Receives the generation the publication wrote, when it reports one. */
  clearLocalRecovery?: (userId: string, serverName: string, generation?: string) => void;
  persistPublicationRetry?: (scope: MCPRecoveryGenerationScope) => Promise<string>;
  clearPublicationRetry?: (scope: MCPRecoveryGenerationScope, version: string) => Promise<void>;
  retryDelaysMs?: readonly number[];
  attemptTimeoutMs?: number;
}
interface FinalizeMCPAuthorizationMutationParams {
  scope: MCPRecoveryGenerationScope;
  mutationResults: readonly unknown[];
  /** Durable intent written before the first credential mutation. */
  publicationRetryVersion?: string;
  /** Teardown also fences when the credential delete itself was a no-op or failed. */
  teardown?: boolean;
}
interface FinalizeMCPAuthorizationMutationDeps extends MCPAuthorizationPublicationDeps {
  disconnectUserConnection: (userId: string, serverName: string) => Promise<void>;
  retryDelaysMs?: readonly number[];
  attemptTimeoutMs?: number;
  afterDisconnect?: () => Promise<void>;
  onDisconnectError?: (error: unknown) => void;
}
interface CompleteMCPAuthorizationWithTokenWaitersParams<TTokens> {
  flowIds: readonly string[];
  tokens: TTokens;
  completeAuthorization: (tokens: TTokens) => Promise<void>;
}
interface CompleteMCPAuthorizationWithTokenWaitersDeps<TTokens> {
  flowManager: {
    getFlowState?: (flowId: string, type: "mcp_get_tokens") => Promise<unknown>;
    deleteFlow?: (flowId: string, type: "mcp_get_tokens") => Promise<unknown>;
    settleFlowIfCurrent?: (flowId: string, type: "mcp_get_tokens", expectedCreatedAt: number, expectedState: string, tokens: TTokens) => Promise<"updated" | "stale" | "missing">;
  };
  onTokenFlowError?: (phase: "prepare" | "complete", error: unknown) => void;
}
/**
* Settles the guarded OAuth flow before exposing its tokens to connection-factory waiters. Stale
* token-flow results are removed before settlement, while live waiters are retained and woken only
* after the authorization completion can no longer roll the credential write back.
*/
declare function completeMCPAuthorizationWithTokenWaiters<TTokens>(params: CompleteMCPAuthorizationWithTokenWaitersParams<TTokens>, deps: CompleteMCPAuthorizationWithTokenWaitersDeps<TTokens>): Promise<void>;
interface PersistMCPAuthorizationTransactionParams<TTokens> {
  scope: MCPRecoveryGenerationScope;
  flowIds: readonly string[];
  tokens: TTokens;
  completeAuthorization: (tokens: TTokens) => Promise<void>;
  persistTokens: (tokens: TTokens, onStoreCommitted: (tokens: TTokens) => Promise<void>) => Promise<TTokens>;
}
interface PersistMCPAuthorizationTransactionDeps<TTokens> extends CompleteMCPAuthorizationWithTokenWaitersDeps<TTokens>, MCPAuthorizationPublicationDeps {
  ensureServerActive: () => Promise<boolean>;
  inactiveServerError: () => Error;
}
/**
* Writes a durable intent now and returns the exact publication that clears only that intent.
* The publication reports the generation it wrote so its own caller can adopt it.
*/
declare function prepareMCPAuthorizationMutation(scope: MCPRecoveryGenerationScope, deps: MCPAuthorizationPublicationDeps): Promise<() => Promise<string | undefined>>;
/**
* Owns the OAuth authorization transaction while the token store's rollback journal is live. The
* tokens released to the authorization flow, to pending token waiters, and to the caller carry the
* generation the publication wrote, so a connection built on them leases under that generation.
*/
declare function persistMCPAuthorizationTransaction<TTokens extends object>(params: PersistMCPAuthorizationTransactionParams<TTokens>, deps: PersistMCPAuthorizationTransactionDeps<TTokens>): Promise<TTokens>;
/**
* Publishes every committed credential batch before disconnecting its live connection. Partial
* batches still advance the generation. Teardown fences the credential change, disconnects, then
* writes a second durable intent before OAuth cleanup and fences that cleanup independently.
*/
declare function finalizeMCPAuthorizationMutation(params: FinalizeMCPAuthorizationMutationParams, deps: FinalizeMCPAuthorizationMutationDeps): Promise<boolean>;
//#endregion
//#region src/mcp/authorizationRetry.d.ts
interface MCPAuthorizationFenceRetryRecord extends MCPRecoveryGenerationScope {
  tenantId?: string | null;
  version: string;
  updatedAt: Date;
}
interface MCPAuthorizationFenceRetryStorage {
  upsert(input: {
    scope: MCPRecoveryGenerationScope;
    tenantId?: string | null;
    version: string;
    now: Date;
  }): Promise<void>;
  deleteVersion(input: {
    scope: MCPRecoveryGenerationScope;
    tenantId?: string | null;
    version: string;
  }): Promise<void>;
  deferVersion(input: {
    scope: MCPRecoveryGenerationScope;
    tenantId?: string | null;
    version: string;
    updatedAt: Date;
  }): Promise<void>;
  list(limit: number): Promise<readonly MCPAuthorizationFenceRetryRecord[]>;
}
interface MCPAuthorizationFenceRetryServiceDeps {
  storage: MCPAuthorizationFenceRetryStorage;
  getTenantId: () => string | null | undefined;
  runInRetryScope: (retry: MCPAuthorizationFenceRetryRecord, operation: () => Promise<void>) => Promise<void>;
  registerShutdown?: typeof registerShutdownTask;
}
interface MCPAuthorizationFenceRetryWorkerPolicy {
  intervalMs?: number;
  batchSize?: number;
  attemptTimeoutMs?: number;
}
interface MCPAuthorizationFenceRetryService {
  clear(scope: MCPRecoveryGenerationScope, version: string): Promise<void>;
  drain(): Promise<void>;
  persist(scope: MCPRecoveryGenerationScope): Promise<string>;
  start(invalidator: (scope: MCPRecoveryGenerationScope) => Promise<unknown>, policy?: MCPAuthorizationFenceRetryWorkerPolicy): void;
  stop(): Promise<void>;
}
/** Owns durable authorization-fence retry lifecycle independently of the legacy server adapter. */
declare function createMCPAuthorizationFenceRetryService(deps: MCPAuthorizationFenceRetryServiceDeps): MCPAuthorizationFenceRetryService;
//#endregion
//#region src/mcp/assistants.d.ts
interface AssistantMCPUser {
  id?: string;
  role?: string;
}
type AssistantToolReference = string | {
  type?: string;
  function?: {
    name?: string;
  };
};
interface AssistantToolDefinitionsParams {
  user: AssistantMCPUser | undefined;
  tools?: readonly AssistantToolReference[];
  staticTools: LCAvailableTools;
  mcpConfig: Record<string, MCPOptions$1>;
}
interface AssistantToolCatalogSnapshot {
  tools: LCAvailableTools | null;
  publicationGeneration?: string;
  publicationRevision?: string;
}
interface AssistantToolDefinitionsDeps {
  ensureConfigServers: (mcpConfig: Record<string, MCPOptions$1>) => Promise<Record<string, ParsedServerConfig>>;
  getAllServerConfigs: (userId: string, configServers: Record<string, ParsedServerConfig>, role?: string) => Promise<Record<string, ParsedServerConfig>>;
  getMCPServerTools: (userId: string, serverName: string, serverConfig: ParsedServerConfig) => Promise<LCAvailableTools | null>;
  getServerToolFunctionsSnapshot: (userId: string, serverName: string, serverConfig: ParsedServerConfig) => Promise<AssistantToolCatalogSnapshot>;
  recoverServerTools: (serverName: string, serverConfig: ParsedServerConfig) => Promise<LCAvailableTools | null>;
  cacheMCPServerTools: (params: {
    userId: string;
    serverName: string;
    serverTools: LCAvailableTools;
    serverConfig: ParsedServerConfig;
    publicationGeneration?: string;
    publicationRevision?: string;
  }) => Promise<void>;
}
interface AssistantToolDefinitionsResult {
  toolDefinitions: LCAvailableTools;
  /**
  * Every server name the principal can reach, from the same merged registry
  * read that resolved the catalogs — the legacy-key heal reuses it instead
  * of repeating the app-config and registry round trips on the write path.
  * `undefined` when the payload references no MCP tools (nothing to heal).
  */
  accessibleServerNames?: string[];
}
/** Loads the static catalog with the configuration-addressed MCP slices referenced by an assistant. */
declare function getAssistantToolDefinitions(params: AssistantToolDefinitionsParams, deps: AssistantToolDefinitionsDeps): Promise<AssistantToolDefinitionsResult>;
/**
* Assistant writers submit tool entries VERBATIM as provider tool definitions
* (`assistantData.tools` in the v1/v2 controllers), and providers reject
* unknown fields — the internal `serverToolName` mapping must never leave the
* catalog. Strings and entries without the mapping pass through by reference;
* the cached catalog keeps the mapping for the runtime call path.
*/
declare function toProviderToolDefinition<T>(tool: T): T | LCFunctionTool;
//#endregion
//#region src/mcp/request.d.ts
/**
* Builds the complete request context that runtime MCP placeholders may resolve.
* An explicit null parent means a known root turn and becomes the root sentinel.
* An omitted parent stays omitted so protocols without parent-message identity
* fail closed for configurations that require that BODY placeholder.
*/
declare function createMCPRuntimeRequestBody({
  messageId,
  conversationId,
  parentMessageId,
  codeEnvironmentMode,
  codeWorkspaces
}: {
  messageId: string;
  conversationId: string;
  parentMessageId?: string | null;
  codeEnvironmentMode?: MCPRuntimeRequestBody["codeEnvironmentMode"];
  codeWorkspaces?: MCPRuntimeRequestBody["codeWorkspaces"];
}): MCPRuntimeRequestBody;
interface MCPRequestContext extends RequestScopedMCPConnectionStore {
  cleanupStarted: boolean;
  cleanupOnResponse: boolean;
  responseCleanupAttached: boolean;
}
interface MCPRequestContextOptions {
  cleanupOnResponse?: boolean;
}
interface MCPResponseLike {
  writableEnded?: boolean;
  finished?: boolean;
  destroyed?: boolean;
  once?: (event: "finish" | "close", listener: () => void) => unknown;
}
declare function createMCPRequestContext(): MCPRequestContext;
declare function cleanupMCPRequestContext(context?: MCPRequestContext): Promise<void>;
declare function getMCPRequestContext(req?: object, res?: MCPResponseLike, options?: MCPRequestContextOptions): MCPRequestContext | undefined;
declare function cleanupMCPRequestContextForReq(req?: object): Promise<void>;
//#endregion
//#region src/mcp/reinitialize.d.ts
/** Result that ends a reinitialization before it connects. */
interface MCPReinitializeStopResult {
  availableTools: null;
  success: false;
  message: string;
  failureReason: MCPReinitializeFailureReason;
  oauthRequired: false;
  serverName: string;
  oauthUrl: null;
  tools: null;
}
/** The config a reinitialization connects with, or the result that ends it first. */
type MCPReinitializeConfigResolution = {
  serverConfig: ParsedServerConfig | undefined;
  result?: undefined;
} | {
  serverConfig?: undefined;
  result: MCPReinitializeStopResult;
};
/**
* Resolves the server config a reinitialization connects with. A config that failed inspection
* is recovered through the registry, so a caller holding the stub connects with what inspection
* stored, including a recovery another request made. While the server stays unreachable, the
* reinitialization stops with an `unreachable` result.
*/
declare function resolveMCPReinitializeConfig(registry: Pick<MCPServersRegistry, "recoverServerConfig">, serverName: string, serverConfig: ParsedServerConfig | undefined, userId?: string): Promise<MCPReinitializeConfigResolution>;
//#endregion
//#region src/mcp/icons.d.ts
/**
* Sanitizes a user-provided MCP `iconPath` at the trust boundary. SVG data URIs
* are re-sanitized with the shared policy and re-encoded as base64; other values
* pass through. Any value still over `MAX_MCP_ICON_PATH_LENGTH` becomes an empty
* string, so editing a server whose stored icon predates the cap clears the icon
* instead of failing validation.
*/
declare function sanitizeMcpIconPath(iconPath: string): string;
//#endregion
//#region src/mcp/utils.d.ts
declare const mcpToolPattern: RegExp;
/** Validates an effective MCP config without stripping its server-managed metadata. */
declare function validateMCPServerConfig(config: unknown): ParsedServerConfig;
/**
* Prefix of the lazily-expanded MCP placeholder `mcp_all<delim><server>`,
* pushed into an agent's `tools` for overlay/user-connection servers whose
* tool names are not known until the definitions loader expands them.
*
* The name is reserved by that convention: the definitions loader treats ANY
* matching entry as "expand every tool on this server", so a remote tool
* literally named `mcp_all` cannot be addressed individually anywhere in the
* pipeline. Kept here as the one definition of the prefix.
*/
declare const MCP_ALL_PLACEHOLDER_PREFIX: string;
/** Whether a tool entry is the lazily-expanded `mcp_all` placeholder. */
declare function isMCPAllPlaceholder(toolName: string): boolean;
/**
* Later-configured server names whose normalized form is already claimed by an
* earlier different name. Such a pair produces IDENTICAL model-facing tool
* keys, so tool selection and execution cannot tell the servers apart —
* `buildServerNameAliases` deterministically routes to the first name, and
* the shadowed later server must be EXCLUDED from tool exposure entirely:
* offering its tools would let a user select a tool that silently executes
* against the first server's configuration.
*/
/**
* Whether a resolved server name is normalization-sensitive — its own name
* needs normalizing, or it EQUALS the normalized form of some configured
* special-character name. Under an incomplete collision audit these are the
* references whose routing cannot be proven unambiguous, so they fail closed.
*/
declare function isNormalizationSensitiveName(serverName: string, rawServerNames: readonly string[]): boolean;
declare function findShadowedServerNames(rawServerNames: readonly string[]): Set<string>;
/**
* Heals legacy persisted agent data whose MCP tool keys embed a RAW server
* name: model-facing keys carry `normalizeServerName(server)` (matching cache
* keys, definition names, and runtime instance names), so an agent document
* saved before that convention — or through the old raw-keyed cache — would
* neither load its tools nor have its `tool_options` (defer / programmatic /
* background / intent) honored for a server whose name needs normalizing.
*
* Placeholder and server-pin tokens are left untouched: they are
* config-identity references consumed against raw config names (the client's
* selectors, the definitions loader's expansion), never model-facing names.
* Returns the same references when nothing needs rewriting, so the common
* path (every server name already normalized) allocates nothing.
*/
declare function normalizeAgentToolKeys(params: {
  tools: string[] | undefined;
  toolOptions: AgentToolOptions | undefined;
  rawServerNames: readonly string[];
}): {
  tools: string[] | undefined;
  toolOptions: AgentToolOptions | undefined;
};
type PlaceholderValue = string | number | boolean | null | undefined | readonly PlaceholderValue[] | {
  readonly [key: string]: PlaceholderValue;
};
interface MCPRequestScope {
  requestScoped: boolean;
  requiredBodyFields: Array<keyof RequestBody>;
}
type UserScopedConnectionConfig = Pick<ParsedServerConfig, "requiresOAuth" | "source" | "dbId" | "startup"> & {
  /** Loosened like the fields below: raw (pre-inspection) configs carry
  *  optional API-key fields, and the gating predicates only inspect them. */
  apiKey?: {
    key?: string;
    source?: "user" | "admin";
  } | null;
  args?: string[];
  /** Loosened from the parsed shapes so raw (pre-inspection) configs qualify;
  *  scoping predicates only check key presence */
  obo?: {
    scopes?: string;
  } | null;
  customUserVars?: Record<string, {
    description?: string;
    title?: string;
    sensitive?: boolean;
  } | undefined>;
  env?: Record<string, string | undefined>;
  headers?: Record<string, string | undefined>;
  oauth?: PlaceholderValue;
  oauth_headers?: Record<string, string | undefined>;
  url?: string;
};
/** Whether a server should use MCP OAuth handling. */
declare function isOAuthServer(config: Pick<ParsedServerConfig, "requiresOAuth" | "oauth">): boolean;
/**
* Whether a server needs the OAuth-style connection wiring (flow manager,
* token methods, OBO/OAuth resolvers). Distinct from `isOAuthServer`: OBO
* servers reuse the same wiring even though they don't run an OAuth handshake,
* because the runtime needs `oboTokenResolver`/`oboTrustChecker` plumbed through.
*
* Without this, an OBO server with `requiresOAuth: false` would land in the
* non-OAuth branch of MCPManager.discoverServerTools / UserConnectionManager,
* which omits the OBO resolver — `usesObo` then evaluates to false in the
* factory and the connection sends a bare request that the upstream rejects.
*/
declare function requiresOAuthMachinery(config: ParsedServerConfig): boolean;
/** Checks that `customUserVars` is present AND non-empty (guards against truthy `{}`) */
declare function hasCustomUserVars(config: Pick<UserScopedConnectionConfig, "customUserVars">): boolean;
/**
* Trusted YAML/config servers may use per-user/request placeholders that can
* only be resolved once a real request context exists. User-sourced DB servers
* deliberately stay sandboxed and only resolve customUserVars, while plugin
* configs preserve every placeholder literally as a security boundary.
*/
declare function hasRuntimeContextPlaceholders(config: UserScopedConnectionConfig): boolean;
declare function hasRuntimeUrlPlaceholders(config: UserScopedConnectionConfig): boolean;
declare function getMCPRequestScope(config: UserScopedConnectionConfig): MCPRequestScope;
declare function getRuntimeBodyPlaceholderFields(config: UserScopedConnectionConfig): Array<keyof RequestBody>;
declare function getMissingRuntimeBodyPlaceholderFields(config: UserScopedConnectionConfig, requestBody?: RequestBody): string[];
/**
* `BODY` placeholders vary by chat request, so the normal userId:serverName
* cache would reuse a connection built with stale request context.
*
* Ephemeral connections are created and torn down per tool call — configs using
* these placeholders pay a full connect + initialize on every invocation.
*
* User/OpenID/Graph placeholders still require user-scoped connections, but they
* are not request-scoped by themselves. HTTP transports refresh resolved headers
* before each tool call, so token/user headers can remain on the cached user
* connection without forcing a reconnect for every invocation.
*/
declare function requiresEphemeralUserConnection(config: UserScopedConnectionConfig): boolean;
/**
* Whether a resolved server config may be reached from the chat MCP picker.
*
* Mirrors `selectableServers` in the client's `useMCPServerManager`, which is
* the list the dropdown offers: `chatMenu: false` is the operator hiding a
* server from chat, and `consumeOnly` marks a server the user reaches only
* through an agent that references it, never on its own.
*
* An unresolved config is selectable. A name the registry cannot resolve is
* either request-tier (declared on the request body and never registered) or
* genuinely unknown, and both already have their own handling downstream —
* failing closed here would silently drop request-scoped servers instead.
*/
declare function isChatSelectableMCPServer(config?: Pick<ParsedServerConfig, "chatMenu" | "consumeOnly"> | null): boolean;
/**
* Narrows a chat picker selection to the servers that picker is allowed to
* offer, so a stale client, a replayed body, or a hand-written request cannot
* reach a server the menu hides.
*
* Pass only the picker's own selection. Servers a model spec pins
* (`modelSpec.mcpServers`) are the operator's choice and stay attached even
* when hidden, so they must be added after this call, not through it.
*
* The accessible set comes from the registry — the same resolution that feeds
* the client's catalog, with its own tier precedence already applied — rather
* than being re-derived from the request's config overlay. A user-tier or
* process-backed server outranks a config entry of the same name, and this must
* agree with whatever the picker was actually offered.
*
* Names are deduplicated before the lookup, since the selection is
* request-supplied, and are matched through the same normalized-name aliasing
* that tool loading uses, built over the whole accessible set so an exact name
* always wins over another server's normalized form. A name the registry does
* not know is request-tier — declared on the body, never registered — and is
* kept, as is every name if the lookup fails: this narrows an
* already-authenticated selection and is not the authorization boundary.
*
* Returns the names as they were sent, so callers keep addressing servers the
* way the rest of the request does.
*/
declare function filterChatSelectableMCPServers(selectedServers: string[] | null | undefined, {
  userId,
  role,
  getAccessibleMCPServers
}: {
  userId: string;
  role?: string;
  getAccessibleMCPServers?: (userId: string, role?: string) => Promise<Record<string, Pick<ParsedServerConfig, "chatMenu" | "consumeOnly">>>;
}): Promise<string[]>;
/**
* Returns true when a server requires a per-user connection instead of an
* app-shared connection.
*/
declare function requiresUserScopedConnection(config: UserScopedConnectionConfig): boolean;
/** Whether a server can share one operator-owned connection across all users. */
declare function canUseAppConnection(config: UserScopedConnectionConfig): boolean;
/**
* Server instructions fetched from a connection are safe to retain in the
* shared YAML registry only when the connection cannot vary by identity or
* request. `startup: false` is the one context-independent reason startup
* inspection leaves instructions unresolved; every other deferred case can
* expose authenticated or request-specific instructions to another user.
*/
declare function canBackfillSharedServerInstructions(config: UserScopedConnectionConfig): boolean;
/**
* Returns the names of `customUserVars` declared on the server config for which
* the user has not supplied a non-blank value (unset, empty, or whitespace-only
* values count as missing, since they still fail auth). An empty array means
* every declared variable is satisfied (or the server declares none).
*
* Used to gate tool exposure: a server that requires user-provided credentials
* should not surface its tools to the model until those values are set,
* otherwise every tool call fails authentication. See issue #10969.
*/
declare function getMissingCustomUserVars(config: Pick<ParsedServerConfig, "customUserVars">, providedVars?: Record<string, string> | null): string[];
/**
* Determines whether a server config is user-sourced (sandboxed placeholder resolution).
* When `source` is set, it is authoritative. When absent (pre-upgrade cached configs),
* falls back to the legacy `dbId` heuristic for backward compatibility.
*/
declare function isUserSourced(config: Pick<ParsedServerConfig, "source" | "dbId">): boolean;
/**
* Resolves the instructions text for a server, mirroring how the inspector fills them:
* an enabled flag resolves to the text fetched from the server, while an operator-authored
* string is used verbatim. Anything else (`false`, unset) yields no instructions.
*/
declare function resolveServerInstructions(config: ParsedServerConfig): string | undefined;
type RedactedServerConfig = Partial<ParsedServerConfig> & {
  /** True when this config needs chat request fields before it can connect. */requestScoped?: boolean;
};
/**
* Allowlist-based sanitization for API responses. Only explicitly listed fields are included;
* new fields added to ParsedServerConfig are excluded by default until allowlisted here.
*
* `url` and the oauth flow URLs (`authorization_url`, `token_url`, `revocation_endpoint`) can
* encode internal infrastructure, so they are stripped unless `canEdit` is set: the same
* disclosure threshold the PATCH route enforces. Callers derive `canEdit` per server (operator
* YAML/config-tier servers from the MANAGE_MCP_SERVERS capability, user-sourced servers from
* per-resource ACL EDIT), so a user-sourced config shared view-only does not disclose its URL
* to the viewer.
* DB-stored configs reject ${VAR} patterns at validation time (MCPServerUserInputSchema);
* env variable resolution is handled at the schema/input boundary.
*/
declare function redactServerSecrets(config: ParsedServerConfig, options?: {
  canEdit?: boolean;
}): RedactedServerConfig;
/** Applies allowlist-based sanitization to a map of server configs. */
declare function redactAllServerSecrets(configs: Record<string, ParsedServerConfig>, options?: {
  canEditByServer?: ReadonlyMap<string, boolean>;
}): Record<string, RedactedServerConfig>;
/**
* Builds the synthetic tool-call name used during MCP OAuth flows.
* Format: `oauth<mcp_delimiter><normalizedServerName>`
*
* Guards against the caller passing a pre-wrapped name (one that already
* starts with the oauth prefix in its original, un-normalized form) to
* prevent double-wrapping.
*/
declare function buildOAuthToolCallName(serverName: string): string;
/** Checks whether a message indicates the stored client registration is invalid/stale. */
declare function isInvalidClientMessage(message: string): boolean;
/**
* Checks whether a message indicates the OAuth client registration was rejected.
* Superset of `isInvalidClientMessage`: also matches `unauthorized_client`
* (grant-type refusal), which has different recovery semantics.
*/
declare function isClientRejectionMessage(message: string): boolean;
/**
* Sanitizes a URL by removing query parameters to prevent credential leakage in logs.
* @param url - The URL to sanitize (string or URL object)
* @returns The sanitized URL string without query parameters
*/
declare function sanitizeUrlForLogging(url: string | URL): string;
/**
* Escapes special regex characters in a string so they are treated literally.
* @param str - The string to escape
* @returns The escaped string safe for use in a regex pattern
*/
declare function escapeRegex(str: string): string;
/**
* Generates a URL-friendly server name from a title.
* Converts to lowercase, replaces spaces with hyphens, removes special characters.
* @param title - The display title to convert
* @returns A slug suitable for use as serverName (e.g., "GitHub MCP Tool" → "github-mcp-tool")
*/
/**
* One cancellation signal for a budgeted operation: the remaining budget and the caller's own
* signal, whichever fires first. Undefined when neither bound exists, so unbudgeted callers pay
* nothing.
*/
declare function createDeadlineAbortSignal(deadlineMs?: number, callerSignal?: AbortSignal): AbortSignal | undefined;
type DeadlineWaitResult<T> = {
  settled: true;
  value: T;
} | {
  settled: false;
};
/**
* Waits for `promise` only until `deadlineMs` passes or `signal` aborts, and never cancels it:
* shared work such as an OAuth token refresh keeps running for whoever depends on its outcome. A
* rejection that lands first is rethrown; one that lands after the wait ended stays observed, so
* abandoned work never surfaces as an unhandled rejection.
*/
declare function waitUntilDeadline<T>(promise: Promise<T>, deadlineMs?: number, signal?: AbortSignal): Promise<DeadlineWaitResult<T>>;
declare function generateServerNameFromTitle(title: string): string;
//#endregion
//#region src/mcp/context.d.ts
interface MCPServerContext {
  /** Config-source servers that needed lazy initialization. */
  configServers: Record<string, ParsedServerConfig>;
  /** Every configured server, in the normalized form tool keys are built from. */
  serverNames: string[];
  /** Every configured server under its raw config name — the form the
  *  registry, config maps, tool cache, and plugin-auth rows are keyed by.
  *  Consumers that parse a (normalized) server out of a tool key resolve
  *  back to these via `buildServerNameAliases`. */
  rawServerNames: string[];
}
interface ResolveMCPServerContextParams {
  mcpConfig: Record<string, MCPOptions$1>;
  ensureConfigServers: (mcpConfig: Record<string, MCPOptions$1>) => Promise<Record<string, ParsedServerConfig>>;
}
/**
* Resolves the MCP server context for one request from a single config snapshot.
*
* `ensureConfigServers` deliberately skips unmodified YAML servers, so its keys are
* not the configured set. Tool-key boundary resolution needs every configured name,
* normalized the way keys embed it, or a server absent from the lazy-init result
* silently falls back to positional parsing.
*/
declare function resolveMCPServerContext({
  mcpConfig,
  ensureConfigServers
}: ResolveMCPServerContextParams): Promise<MCPServerContext>;
//#endregion
//#region src/utils/axios.d.ts
/**
* Logs Axios errors based on the error object and a custom message.
* @param options - The options object.
* @param options.message - The custom message to be logged.
* @param options.error - The Axios error object.
* @returns The log message.
*/
declare const logAxiosError: ({
  message,
  error
}: {
  message: string;
  error: AxiosError | Error | unknown;
}) => string;
/** Adds upload context without discarding the Axios response metadata used by
* Code API recovery. Transport-specific branching remains in this typed module. */
declare function wrapCodeApiUploadError(error: unknown, message: string): Error;
/**
* Creates and configures an Axios instance with optional proxy settings.

* @returns A configured Axios instance
* @throws If there's an issue creating the Axios instance or parsing the proxy URL
*/
declare function createAxiosInstance(): AxiosInstance;
//#endregion
//#region src/utils/azure.d.ts
/**
* Sanitizes the model name to be used in the URL by removing or replacing disallowed characters.
* @param modelName - The model name to be sanitized.
* @returns The sanitized model name.
*/
declare const sanitizeModelName: (modelName: string) => string;
/**
* Generates the Azure OpenAI API endpoint URL.
* @param params - The parameters object.
* @param params.azureOpenAIApiInstanceName - The Azure OpenAI API instance name.
* @param params.azureOpenAIApiDeploymentName - The Azure OpenAI API deployment name.
* @returns The complete endpoint URL for the Azure OpenAI API.
*/
declare const genAzureEndpoint: ({
  azureOpenAIApiInstanceName,
  azureOpenAIApiDeploymentName
}: {
  azureOpenAIApiInstanceName: string;
  azureOpenAIApiDeploymentName: string;
}) => string;
/**
* Generates the Azure OpenAI API chat completion endpoint URL with the API version.
* If both deploymentName and modelName are provided, modelName takes precedence.
* @param azureConfig - The Azure configuration object.
* @param azureConfig.azureOpenAIApiInstanceName - The Azure OpenAI API instance name.
* @param azureConfig.azureOpenAIApiDeploymentName - The Azure OpenAI API deployment name (optional).
* @param azureConfig.azureOpenAIApiVersion - The Azure OpenAI API version.
* @param modelName - The model name to be included in the deployment name (optional).
* @param client - The API Client class for optionally setting properties (optional).
* @returns The complete chat completion endpoint URL for the Azure OpenAI API.
* @throws Error if neither azureOpenAIApiDeploymentName nor modelName is provided.
*/
declare const genAzureChatCompletion: ({
  azureOpenAIApiInstanceName,
  azureOpenAIApiDeploymentName,
  azureOpenAIApiVersion
}: {
  azureOpenAIApiInstanceName: string;
  azureOpenAIApiDeploymentName?: string;
  azureOpenAIApiVersion: string;
}, modelName?: string, client?: GenericClient) => string;
/**
* Retrieves the Azure OpenAI API credentials from environment variables.
* @returns An object containing the Azure OpenAI API credentials.
*/
declare const getAzureCredentials: () => AzureOptions;
/**
* Constructs a URL by replacing placeholders in the baseURL with values from the azure object.
* It specifically looks for '${INSTANCE_NAME}' and '${DEPLOYMENT_NAME}' within the baseURL and replaces
* them with 'azureOpenAIApiInstanceName' and 'azureOpenAIApiDeploymentName' from the azure object.
* If the respective azure property is not provided, the placeholder is replaced with an empty string.
*
* @param params - The parameters object.
* @param params.baseURL - The baseURL to inspect for replacement placeholders.
* @param params.azureOptions - The azure options object containing the instance and deployment names.
* @returns The complete baseURL with credentials injected for the Azure OpenAI API.
*/
declare function constructAzureURL({
  baseURL,
  azureOptions
}: {
  baseURL: string;
  azureOptions?: AzureOptions;
}): string;
declare function isCanonicalAzureURL(baseURL: string): boolean;
/**
* The deployments path for a full-hostname instance without a base URL. A resource name needs
* none: the client derives `<name>.openai.azure.com` itself, which a full hostname would double.
*/
declare function constructAzureInstanceBasePath(azureOptions: AzureOptions): string | undefined;
declare function constructAzureChatBasePath(baseURL: string, azureOptions: AzureOptions): string;
/** Azure permits the deployment to be supplied entirely by the configured base URL. */
declare function getAzureDeploymentName(baseURL?: string | null, azureOptions?: AzureOptions): string | undefined;
declare function constructAzureResponsesURL(baseURL: string | null | undefined, azureOptions: AzureOptions): URL;
//#endregion
//#region src/utils/code.d.ts
/**
* Dedicated agents for code-server requests, preventing socket pool contamination.
* follow-redirects (used by axios) leaks `socket.destroy` as a timeout listener;
* on Node 19+ (keepAlive: true by default), tainted sockets re-enter the global pool
* and kill unrelated requests (e.g., node-fetch in CodeExecutor) after the idle timeout.
*/
declare const codeServerHttpAgent: http.Agent;
declare const codeServerHttpsAgent: https$1.Agent;
/**
* Wait implied by a Code API 429, in milliseconds, or `null` when the error
* is not a rate-limit response. The service answers with both the standard
* `Retry-After` header and a `retry_after_seconds` body field; the header is
* preferred so a body the caller asked for as a stream/buffer never has to be
* parsed, and the body is the fallback for proxies that drop the header.
*/
declare function getCodeApiRetryAfterMs(error: unknown): number | null;
/** Total time one operation may spend waiting out Code API rate limits.
*  Keeps recovery bounded inside a live chat turn. */
declare const MAX_CODE_API_RATE_LIMIT_WAIT_MS = 2e4;
declare const CODE_API_UPLOAD_CONCURRENCY_DEFAULT = 3;
interface CodeApiUploadRegistry {
  scopes: Map<string, UploadScopeState>;
}
declare function createCodeApiUploadRegistry(): CodeApiUploadRegistry;
declare function createCodeApiUploadScope(params: {
  route: string;
  principalId: string;
  tenantId?: string;
}): string;
declare function getCodeApiUploadOptions(req: ServerRequest, route: string): {
  scope: string;
  concurrency: number;
  retryWaitMs: number;
};
interface QueuedUpload {
  run: () => void;
  reject: (error: unknown) => void;
  signal?: AbortSignal;
  onAbort?: () => void;
}
interface UploadScopeState {
  active: number;
  concurrency: number;
  queue: QueuedUpload[];
}
interface CodeApiRateLimitWaitToken {
  endAt: number;
}
/** Limits uploads within one Code API route and principal. A throttled bucket
* holds only its own slots, and canceled callers leave the queue immediately. */
declare function withCodeApiUploadSlot<T>(params: {
  registry: CodeApiUploadRegistry;
  task: () => Promise<T>;
  scope: string;
  concurrency?: number;
  signal?: AbortSignal;
}): Promise<T>;
/** Actual rate-limit wait time shared by every request of one operation. */
interface CodeApiRateLimitBudget {
  limitMs: number;
  waitedMs: number;
  waitingSince?: number;
  activeWaitEnds: Set<CodeApiRateLimitWaitToken>;
}
declare function createCodeApiRateLimitBudget(totalMs?: number): CodeApiRateLimitBudget;
/**
* Runs a Code API request, waiting out a rate limit whenever the shared
* budget still covers the server's `Retry-After`. Multi-request operations
* (a windowed image read, a batch upload) would otherwise abandon the work
* already done the moment a limiter window closes mid-flight. A 429 the
* budget cannot absorb throws a named error rather than a bare
* "status code 429", which reads as an unspecified failure instead of one
* that clears on its own.
*/
declare function withCodeApiRateLimit<T>(params: {
  attempt: () => Promise<T>;
  label: string;
  budget?: CodeApiRateLimitBudget;
  onWait?: (waitMs: number) => void;
  signal?: AbortSignal; /** Narrows retry classification when an attempt also performs non-CodeAPI work. */
  isRateLimit?: (error: unknown) => boolean;
}): Promise<T>;
/** Owns upload admission, retry waiting, and source reopening for every Code API
* upload producer. Callers supply storage and transport adapters only. */
declare function withCodeApiUploadRecovery<S, T>(params: {
  registry: CodeApiUploadRegistry;
  scope: string;
  concurrency?: number;
  budget?: CodeApiRateLimitBudget;
  signal?: AbortSignal;
  label: string;
  onWait?: (waitMs: number) => void;
  openSource: () => Promise<S>;
  upload: (source: S) => Promise<T>;
}): Promise<T>;
declare function isCodeApiRateLimitError(error: unknown): error is Error & {
  code: "CODE_API_RATE_LIMITED";
  status: 429;
};
//#endregion
//#region src/utils/common.d.ts
/**
* Checks if the given value is truthy by being either the boolean `true` or a string
* that case-insensitively matches 'true'.
*
* @param value - The value to check.
* @returns Returns `true` if the value is the boolean `true` or a case-insensitive
*                    match for the string 'true', otherwise returns `false`.
* @example
*
* isEnabled("True");  // returns true
* isEnabled("TRUE");  // returns true
* isEnabled(true);    // returns true
* isEnabled("false"); // returns false
* isEnabled(false);   // returns false
* isEnabled(null);    // returns false
* isEnabled();        // returns false
*/
declare function isEnabled(value?: string | boolean | null | undefined): boolean;
/**
* Like {@link isEnabled}, but missing or blank values stay unset so the caller
* can apply its own default instead of forcing `false`.
*/
declare function optionalEnabled(value?: string | boolean | null | undefined): boolean | undefined;
/**
* Checks if the provided value is 'user_provided'.
*
* @param value - The value to check.
* @returns - Returns true if the value is 'user_provided', otherwise false.
*/
declare const isUserProvided: (value?: string) => boolean;
/**
* @param values
*/
declare function optionalChainWithEmptyCheck(...values: (string | number | undefined)[]): string | number | undefined;
//#endregion
//#region src/utils/content.d.ts
/**
* Produces the durable content array: drops malformed tool calls and empty summaries,
* compacts away empty slots, and rebases parent activity-phase bounds onto the
* resulting coordinates.
*
* Compaction is not incidental — the source array is frequently sparse, because the
* aggregator writes parts at provider-source indexes. Since that shifts positions, any
* index-referencing metadata is rebased to match; see {@link compactContentParts}.
*
* Non-array input is returned unchanged.
*
* @param contentParts - Array of content parts to compact
* @returns Compacted array with malformed tool calls removed and phase bounds rebased
*
* @example
* // Removes malformed tool_call without the tool_call property
* const parts = [
*   { type: 'tool_call', tool_call: { id: '123', name: 'test' } }, // valid - kept
*   { type: 'tool_call' }, // invalid - filtered out
*   { type: 'text', text: 'Hello' }, // valid - kept (other types pass through)
* ];
* const filtered = filterMalformedContentParts(parts);
* // Returns all parts except the malformed tool_call
*
* @example
* // A hole shifts later parts, so a phase marker's bounds move with them
* const parts = []; // parts[0] never materialized
* parts[1] = { type: 'tool_call', tool_call: { id: 'a' } };
* parts[2] = { type: 'text', text: 'Final answer' };
* parts[3] = { type: 'activity_label', activity_label_type: 'phase',
*              activity_start_index: 0, activity_end_index: 2 };
* const filtered = filterMalformedContentParts(parts);
* // Final answer is now at index 1, and activity_end_index is rebased to 1
*/
declare function filterMalformedContentParts(contentParts: TMessageContentParts[]): TMessageContentParts[];
declare function filterMalformedContentParts<T>(contentParts: T): T;
//#endregion
//#region src/utils/email.d.ts
/**
* Check if email configuration is set
* @returns Returns `true` if either Mailgun or SMTP is properly configured
*/
declare function checkEmailConfig(): boolean;
//#endregion
//#region src/utils/env.d.ts
/**
* Provenance marker for MCP servers contributed by an Agent Plugins package.
* Applied by the plugin loader, never by a plugin-authored `mcp.json` — that
* schema is closed, so a package declaring this field is rejected outright.
*/
declare const MCP_PLUGIN_SOURCE = "plugin";
/**
* True when a server configuration came from an Agent Plugins package, and so
* must reach the transport with every placeholder it declared left literal.
*/
declare function isPluginSourced(config?: {
  source?: string;
} | null): boolean;
/**
* List of allowed user fields that can be used in MCP environment variables.
* These are non-sensitive string/boolean fields from the IUser interface.
*/
declare const ALLOWED_USER_FIELDS: readonly ["id", "name", "username", "email", "provider", "role", "googleId", "facebookId", "openidId", "samlId", "ldapId", "githubId", "discordId", "appleId", "emailVerified", "twoFactorEnabled", "termsAccepted", "termsAcceptedAt"];
type AllowedUserField = (typeof ALLOWED_USER_FIELDS)[number];
type SafeUser = Pick<IUser, AllowedUserField>;
/**
* Encodes a string value to be safe for HTTP headers.
* HTTP headers are restricted to ASCII characters (0-255) per the Fetch API standard.
* Non-ASCII characters with Unicode values > 255 are Base64 encoded with 'b64:' prefix.
*
* NOTE: This is a TerraMind-specific encoding scheme to work around Fetch API limitations.
* MCP servers receiving headers with the 'b64:' prefix should:
* 1. Detect the 'b64:' prefix in header values
* 2. Remove the prefix and Base64-decode the remaining string
* 3. Use the decoded UTF-8 string as the actual value
*
* Example decoding (Node.js):
*   if (headerValue.startsWith('b64:')) {
*     const decoded = Buffer.from(headerValue.slice(4), 'base64').toString('utf8');
*   }
*
* @param value - The string value to encode
* @returns ASCII-safe string (encoded if necessary)
*
* @example
* encodeHeaderValue("José")   // Returns "José" (é = 233, safe)
* encodeHeaderValue("Marić")  // Returns "b64:TWFyacSH" (ć = 263, needs encoding)
*/
declare function encodeHeaderValue(value: string): string;
/**
* Creates a safe user object containing only allowed fields.
* Preserves federatedTokens for OpenID token template variable resolution.
*
* @param user - The user object to extract safe fields from
* @returns A new object containing only allowed fields plus federatedTokens if present
*/
declare function createSafeUser(user: IUser | null | undefined): Partial<SafeUser> & {
  federatedTokens?: IUser["federatedTokens"];
};
/**
* List of allowed request body fields that can be used in header placeholders.
* These are common fields from the request body that are safe to expose in headers.
*/
declare const ALLOWED_BODY_FIELDS: readonly ["conversationId", "parentMessageId", "messageId"];
/**
* Replaces resolvable-but-unresolved placeholders with an empty string so
* TerraMind's internal template syntax is never sent upstream as if it were
* real user data (e.g. a gateway trusting a literal
* `{{LIBRECHAT_USER_OPENIDID}}` as an account identity would pool unrelated
* users under that one string). Only for final resolution passes — staged
* flows that resolve again later with more context must not strip.
*/
declare function stripUnresolvedPlaceholders(value: string): string;
/**
* Recursively processes an object to replace environment variables in string values
* @param params - Processing parameters
* @param params.options - The MCP options to process
* @param params.user - The user object containing all user fields
* @param params.customUserVars - vars that user set in settings
* @param params.body - the body of the request that is being processed
* @returns - The processed object with environment variables replaced
*/
declare function processMCPEnv(params: {
  options: Readonly<MCPOptions$1> & {
    dbId?: string;
    source?: string;
  };
  user?: Partial<IUser>;
  customUserVars?: Record<string, string>;
  body?: RequestBody; /** When true, only resolve customUserVars — skip env vars, user/OpenID/body placeholders (for DB-stored servers) */
  dbSourced?: boolean;
}): MCPOptions$1;
/**
* Recursively resolves placeholders in a nested object structure while preserving types.
* Only processes string values - leaves numbers, booleans, arrays, and nested objects intact.
*
* @param options - Configuration object
* @param options.obj - The object to process
* @param options.user - Optional user object for replacing user field placeholders
* @param options.body - Optional request body object for replacing body field placeholders
* @param options.customUserVars - Optional custom user variables to replace placeholders
* @returns The processed object with placeholders replaced in string values
*/
declare function resolveNestedObject<T = unknown>(options?: {
  obj: T | undefined;
  user?: Partial<IUser> | {
    id: string;
  };
  body?: RequestBody;
  customUserVars?: Record<string, string>;
}): T;
/**
* Resolves header values by replacing user placeholders, body variables, custom variables, and environment variables.
* Automatically encodes non-ASCII characters for header safety.
*
* @param options - Optional configuration object
* @param options.headers - The headers object to process
* @param options.user - Optional user object for replacing user field placeholders (can be partial with just id)
* @param options.body - Optional request body object for replacing body field placeholders
* @param options.customUserVars - Optional custom user variables to replace placeholders
* @param options.stripUnresolved - When true (final resolution passes only), replaces any
*   remaining resolvable placeholders with an empty string so internal template syntax is
*   never forwarded upstream. Leave unset for staged flows whose values are resolved again
*   later with more context.
* @returns The processed headers with all placeholders replaced
*/
declare function resolveHeaders(options?: {
  headers: Record<string, string> | undefined;
  user?: Partial<IUser> | {
    id: string;
  };
  body?: RequestBody;
  customUserVars?: Record<string, string>;
  stripUnresolved?: boolean;
}): Record<string, string>;
//#endregion
//#region src/utils/errors.d.ts
interface SafeErrorMetadata {
  readonly type: string;
  readonly status?: number;
}
/**
* Returns bounded diagnostic metadata without error messages, stacks, headers,
* request bodies, or provider response data that may echo submitted content.
*/
declare function getSafeErrorMetadata(error: unknown): SafeErrorMetadata;
/**
* The request-boundary counterpart to {@link getSafeErrorMetadata}: the error's own
* description and stack, with every URL reduced to its origin and bearer credentials
* removed. A signed storage URL carries the object path and signature in the parts
* that are dropped, while the origin is what an operator needs to place the failure.
*
* Returned as text because the caller must log it inside the message and pass no
* winston metadata: metadata arms `format.splat()` and promotes an SDK error's own
* enumerable properties — `request.url`, `config`, `response.headers` — onto the
* record, which is the wider leak an error object causes at a log site.
*
* Use this where a handler catches every failure on a route and the class of error is
* not known ahead of time. Prefer `getSafeErrorMetadata` where the error may echo
* submitted content, such as a provider response body.
*/
declare function getSafeErrorText(error: unknown): string;
/**
* Whether a caught error is a cancellation rather than a genuine failure.
*
* An aborted signal on its own proves only that the run is over: a permission,
* OAuth, or upstream error can reject in the same tick a user presses Stop, and
* treating those as cancellations hides real faults from operational alerts.
* Callers that want to quiet a cancellation should require this as well as the
* signal state. Walks `cause` because intermediate layers rewrap the rejection.
*/
declare function isAbortError(error: unknown): boolean;
/** Whether an error represents cancellation by this specific owning signal. */
declare function isOwnedAbortError(error: unknown, signal?: AbortSignal | null): boolean;
//#endregion
//#region src/utils/promises.d.ts
/**
* Resolves with `promise`, but rejects as soon as `signal` aborts.
*
* Unlike handing the signal to the work itself, the underlying promise keeps
* running. Use it for shared work whose lifetime outlives this caller — an OAuth
* flow keyed by user and action that a concurrent run may also be awaiting and
* that a browser callback will complete. Cancelling such work on one waiter's
* abort strands the others and discards an authorization the user has already
* granted; leaving the caller attached is equally wrong, because the value
* arrives after the user pressed Stop and the caller acts on it.
*
* The detached promise's later settlement is swallowed rather than surfacing as
* an unhandled rejection.
*/
declare function detachOnAbort<T>(operation: T | PromiseLike<T>, signal?: AbortSignal | null): Promise<T>;
//#endregion
//#region src/utils/events.d.ts
/**
* Sends a Server-Sent Event to the client.
* Empty-string StreamEvent data is silently dropped.
*/
declare function sendEvent(res: Response$2, event: ServerSentEvent): void;
/**
* Sends error data in Server Sent Events format and ends the response.
* @param res - The server response.
* @param message - The error message.
*/
declare function handleError(res: Response$2, message: string): void;
//#endregion
//#region src/utils/files.d.ts
declare const FILENAME_SEGMENT_MAX_BYTES = 255;
/**
* Composes a leaf as `<stem><suffix><ext>`, trimming the stem so the result
* stays inside `maxBytes`. Unlike {@link truncateLeafWithSuffix}, whose suffix
* marks a truncation and is therefore only applied when one happens, this
* always applies the suffix — callers use it to disambiguate one name from
* another, so a trim that dropped the suffix would hand back the name they
* were trying to move away from.
*/
declare function appendLeafSuffix(leaf: string, suffix: string, maxBytes: number): string;
/**
* Resolves a user-facing error message from a file upload error.
* When redaction is enabled, maps known error patterns to fixed messages
* without exposing provider, filesystem, filename, or submitted-content details.
*/
declare function resolveUploadErrorMessage(error: {
  message?: string;
} | null | undefined, defaultMessage?: string, redactDetails?: boolean): string;
/**
* Sanitize a filename by removing any directory components, replacing unsafe characters
* @param inputName
*/
declare function sanitizeFilename(inputName: string): string;
/**
* Deterministic disambiguator suffix for truncated names. The original
* `sanitizeFilename` used `crypto.randomBytes(3)`, which made the
* truncated form non-deterministic — a re-upload of the same long
* filename would compute a *different* storage key, orphaning the
* previous on-disk file under `claimCodeFile`'s reused `file_id`.
*
* Hashing the input gives stability across calls (same input → same
* output) while still disambiguating distinct inputs that share a
* truncation prefix. SHA-256 truncated to 6 hex chars is collision-safe
* for our scale (24 bits ≈ 16M values; we'd need ~5K simultaneous
* truncated names in one (filename, conversationId) bucket before a
* collision becomes likely, vs. the realistic ceiling of single-digit
* artifacts per turn).
*/
declare function deterministicHexSuffix(input: string): string;
/**
* Sanitize a code-execution artifact path while preserving directory components
* so subsequent prime() runs can place the file at the same nested location in
* the next sandbox session. Each segment is sanitized independently with the
* same rules as `sanitizeFilename`. Falls back to the basename for absolute
* paths or names containing `..` traversal.
*
* Each path component is capped at `ARTIFACT_PATH_SEGMENT_MAX_BYTES` (255 bytes)
* — the leaf with extension preservation (matching `sanitizeFilename`),
* non-leaf segments with a plain truncate-and-disambiguate. Without the
* cap, long artifact names flow into `saveBuffer`'s storage key
* unbounded and trip `ENAMETOOLONG` instead of persisting.
*/
declare function sanitizeArtifactPath(inputName: string): string;
/**
* Map a (sanitized) artifact path to a flat storage-safe key. The local
* storage strategies key by single component; this avoids creating
* unintended subdirectories on disk while the DB record retains the
* nested path for the next prime().
*
* Optionally caps the result at `maxLength` UTF-8 bytes. Per-segment caps
* applied by `sanitizeArtifactPath` aren't enough on their own —
* `${file_id}__${flatName}` has to fit in one filesystem path component
* (NAME_MAX = 255 on most filesystems), so a deeply-nested path whose
* segments are individually within bounds can still produce a flat form
* that overflows once `${file_id}__` is prepended. The caller passes
* `255 - prefix.length` and the truncation preserves the leaf extension
* with the same disambiguating hex suffix that `sanitizeFilename` uses.
*
* Without this cap, oversized flat keys hit `ENAMETOOLONG` inside
* `saveBuffer`, the artifact falls back to a download URL, and the next
* prime() never sees it.
*/
declare function flattenArtifactPath(safePath: string, maxLength?: number): string;
/**
* Options for reading files
*/
interface ReadFileOptions {
  encoding?: BufferEncoding;
  /** Size threshold in bytes. Files larger than this will be streamed. Default: 10MB */
  streamThreshold?: number;
  /** Size of chunks when streaming. Default: 64KB */
  highWaterMark?: number;
  /** File size in bytes if known (e.g. from multer). Avoids extra stat() call. */
  fileSize?: number;
}
/**
* Result from reading a file
*/
interface ReadFileResult<T> {
  content: T;
  bytes: number;
}
/**
* Reads a file asynchronously. Uses streaming for large files to avoid memory issues.
*
* @param filePath - Path to the file to read
* @param options - Options for reading the file
* @returns Promise resolving to the file contents and size
* @throws Error if the file cannot be read
*/
declare function readFileAsString(filePath: string, options?: ReadFileOptions): Promise<ReadFileResult<string>>;
/**
* Reads a file as a Buffer asynchronously. Uses streaming for large files.
*
* @param filePath - Path to the file to read
* @param options - Options for reading the file
* @returns Promise resolving to the file contents and size
* @throws Error if the file cannot be read
*/
declare function readFileAsBuffer(filePath: string, options?: Omit<ReadFileOptions, "encoding">): Promise<ReadFileResult<Buffer>>;
/**
* Reads a JSON file asynchronously
*
* @param filePath - Path to the JSON file to read
* @param options - Options for reading the file
* @returns Promise resolving to the parsed JSON object
* @throws Error if the file cannot be read or parsed
*/
declare function readJsonFile<T = unknown>(filePath: string, options?: Omit<ReadFileOptions, "encoding">): Promise<T>;
//#endregion
//#region src/utils/import.d.ts
/** 250 MiB — default max file size for conversation imports */
declare const DEFAULT_IMPORT_MAX_FILE_SIZE = 262144e3;
/** Resolves the import file-size limit from the env var, falling back to the 250 MiB default */
declare function resolveImportMaxFileSize(): number;
//#endregion
//#region src/utils/generators.d.ts
type SSRFSafeAgents = {
  httpAgent: Agent$1;
  httpsAgent: Agent$2;
};
/**
* Makes a function to make HTTP request and logs the process.
* @param params
* @param params.directEndpoint - Whether to use a direct endpoint.
* @param params.reverseProxyUrl - The reverse proxy URL to use for the request.
* @param params.ssrfAgents - Optional SSRF-safe agents for user-provided URLs.
* @param params.redirect - Optional redirect policy for user-provided URLs.
* @returns A promise that resolves to the response of the fetch request.
*/
declare function createFetch({
  directEndpoint,
  reverseProxyUrl,
  ssrfAgents,
  redirect
}: {
  directEndpoint?: boolean;
  reverseProxyUrl?: string;
  ssrfAgents?: SSRFSafeAgents;
  redirect?: fetch$1.RequestRedirect;
}): (_url: fetch$1.RequestInfo, init: fetch$1.RequestInit) => Promise<fetch$1.Response>;
/**
* Creates event handlers for stream events that don't capture client references
* @param res - The response object to send events to
* @returns Object containing handler functions
*/
declare function createStreamEventHandlers(res: Response$2): {
  on_run_step: (event: ServerSentEvent) => void;
  on_message_delta: (event: ServerSentEvent) => void;
  on_reasoning_delta: (event: ServerSentEvent) => void;
};
/**
* @deprecated No longer used internally: stream pacing lives in
* `@librechat/agents` (adaptive smoothing via `_lc_stream_delay` /
* `streamRate`), and LangChain runs callback handlers in the background, so
* a sleep here never paced token delivery. Kept as a compatibility shim for
* external `@librechat/api` consumers; removal is reserved for a major
* release.
*/
declare function createHandleLLMNewToken(streamRate: number): () => Promise<void>;
//#endregion
//#region src/utils/headers.d.ts
/**
* The media type of a `Content-Type` header — the lowercased `type/subtype` pair with any
* parameters stripped, or `''` when the header is absent or empty.
*
* Substring-matching the raw header is wrong in both directions. A `text/plain;
* boundary=text/event-stream` value contains the SSE type without being one, and a
* `TEXT/EVENT-STREAM` value is one without containing it in the expected case. Callers
* classifying a response by its type must compare against this, not the raw header.
*/
declare function mediaTypeEssence(header: string | null | undefined): string;
/**
* Merges two header maps, with `override` winning on key collisions. Matching is
* case-insensitive (HTTP header names are), so an `override` key replaces any
* case variant from `base` rather than leaving both names in the output (which
* clients may collapse, breaking auth or protocol headers); the `override`
* casing is kept. The `anthropic-beta` header is special-cased: values from both
* sides are comma-unioned (deduped) so a custom beta coexists with
* provider-managed betas instead of clobbering them.
*
* Used both to layer endpoint headers over global (`endpoints.all`) headers and
* to attach admin-configured custom headers beneath provider-managed headers
* (auth/version/beta), so the provider integration's own headers always win.
*
* @returns the merged map, or `undefined` when neither side has any headers.
*/
declare function mergeHeaders(base?: Record<string, string>, override?: Record<string, string>): Record<string, string> | undefined;
/**
* Resolves model-header templates with the request-scoped tenant supplied by
* the run host. Tenant substitution is intentionally scoped to model headers;
* it does not widen the user fields exposed to other template consumers.
*/
declare function resolveModelHeaders({
  headers,
  user,
  tenantId,
  body,
  customUserVars
}: {
  headers: Record<string, string> | undefined;
  user?: Partial<IUser> | {
    id: string;
  };
  tenantId?: string;
  body?: RequestBody;
  customUserVars?: Record<string, string>;
}): Record<string, string>;
/**
* Resolves placeholder templates in the outbound request headers of a built LLM
* config, mutating it in place. Handles the OpenAI-compatible
* `configuration.defaultHeaders` (OpenAI / Azure / custom) and the native
* Anthropic `clientOptions.defaultHeaders` (including Vertex) carriers, plus
* tenant-only substitution in native Google `customHeaders`. Google's map is
* otherwise resolved at initialization so provider-managed authorization never
* passes through environment or user-template expansion.
*
* Resolution runs at request time so request-body placeholders (e.g.
* `{{LIBRECHAT_BODY_CONVERSATIONID}}`) resolve against the live request. It is a
* no-op for header values without placeholders, and idempotent under config reuse.
*
* This is the last resolution pass before the outbound provider request, so any
* placeholder still unresolved here (missing user context, a user without the
* configured field) is stripped rather than forwarded as literal template text
* the upstream could mistake for real user data.
*/
declare function resolveConfigHeaders({
  llmConfig,
  user,
  tenantId,
  body,
  customUserVars
}: {
  /** Partial: this only reads the three provider header carriers, so
  *  callers with a bare ClientOptions (e.g. auxiliary generations like
  *  titles/activity labels) can resolve headers without assembling a
  *  full run config. */
  llmConfig?: Partial<RunLLMConfig> | null;
  user?: Partial<IUser> | {
    id: string;
  }; /** Authoritative request tenant used only for model-header templates. */
  tenantId?: string;
  body?: RequestBody;
  customUserVars?: Record<string, string>;
}): void;
//#endregion
//#region src/utils/path.d.ts
/**
* Gets the base path from the DOMAIN_CLIENT environment variable.
* This is useful for constructing URLs when TerraMind is served from a subdirectory.
* @returns {string} The base path (e.g., '/librechat' or '')
*/
declare function getBasePath(): string;
//#endregion
//#region src/utils/latex.d.ts
/**
* Unescapes LaTeX preprocessing done by the frontend preprocessLaTeX function.
* This reverses the escaping of currency dollar signs and other LaTeX transformations.
*
* The frontend escapes dollar signs for proper LaTeX rendering (e.g., $14 → \\$14),
* but the database stores the original unescaped versions. This function reverses
* that transformation to match database content.
*
* @param text - The escaped text from the frontend
* @returns The unescaped text matching the database format
*/
declare function unescapeLaTeX(text: string | null | undefined): string | null | undefined;
//#endregion
//#region src/utils/limiter.d.ts
interface RateLimitReset {
  /** Epoch milliseconds at which the client's window reopens. */
  resetAt: number;
  /** Whole seconds until `resetAt`, never below one, as a `Retry-After` header expects. */
  retryAfterSeconds: number;
}
/**
* When a rate-limited client may retry. `express-rate-limit` reports the reset on
* `req.rateLimit.resetTime` for stores that track it; without one, the window is assumed to reopen
* one full window from now, the latest its current count can still apply.
*/
declare function getRateLimitReset(rateLimit: Pick<RateLimitInfo, "resetTime"> | undefined, windowMs: number, now?: number): RateLimitReset;
//#endregion
//#region src/utils/llm.d.ts
type TerraMindKeys = keyof typeof librechat;
type TerraMindParams = {
  modelOptions: Omit<NonNullable<DynamicSettingProps["conversation"]>, TerraMindKeys>;
  resendFiles: boolean;
  promptPrefix?: string | null;
  maxContextTokens?: number;
  fileTokenLimit?: number;
  modelLabel?: string | null;
  imageDetail?: ImageDetail;
};
/**
* Separates TerraMind-specific parameters from model options
* @param options - The combined options object
*/
declare function extractTerraMindParams(options?: DynamicSettingProps["conversation"]): TerraMindParams;
//#endregion
//#region src/utils/math.d.ts
/**
* Evaluates a mathematical expression provided as a string and returns the result.
*
* If the input is already a number, it returns the number as is.
* If the input is not a string or contains invalid characters, an error is thrown.
* If the evaluated result is not a number, an error is thrown.
*
* Uses mathjs for safe expression evaluation instead of eval().
*
* @param str - The mathematical expression to evaluate, or a number.
* @param fallbackValue - The default value to return if the input is not a string or number, or if the evaluated result is not a number.
*
* @returns The result of the evaluated expression or the input number.
*
* @throws Throws an error if the input is not a string or number, contains invalid characters, or does not evaluate to a number.
*/
declare function math(str: string | number | undefined, fallbackValue?: number): number;
//#endregion
//#region src/utils/oidc.d.ts
interface OpenIDTokenInfo {
  accessToken?: string;
  idToken?: string;
  expiresAt?: number;
  idTokenExpiresAt?: number;
  userId?: string;
  userEmail?: string;
  userName?: string;
  claims?: Record<string, unknown>;
}
declare const OPENID_TOKEN_FIELDS: readonly ["ACCESS_TOKEN", "ID_TOKEN", "USER_ID", "USER_EMAIL", "USER_NAME", "EXPIRES_AT"];
type OpenIDTokenField = (typeof OPENID_TOKEN_FIELDS)[number];
/**
* Placeholder for Microsoft Graph API access token.
* This placeholder is resolved asynchronously via OBO (On-Behalf-Of) flow
* and requires special handling outside the synchronous processMCPEnv pipeline.
*/
declare const GRAPH_TOKEN_PLACEHOLDER = "{{LIBRECHAT_GRAPH_ACCESS_TOKEN}}";
/**
* Default Microsoft Graph API scopes for OBO token exchange.
* Can be overridden via GRAPH_API_SCOPES environment variable.
*/
declare const DEFAULT_GRAPH_SCOPES = "https://graph.microsoft.com/.default";
/** Claims consulted when deciding whether a verified JWT is an access token rather than an ID token. */
interface JwtTypeClaims {
  aud?: string | string[];
  scp?: unknown;
  scope?: unknown;
  at_hash?: unknown;
  c_hash?: unknown;
}
/** The configured audiences a verified JWT is weighed against. */
interface AccessTokenAudiences {
  /** Audiences that name a protected resource rather than the OIDC client. */
  resources?: ReadonlySet<string>;
  /** The OIDC client id, so an `aud` that still names it is not read as resource-bound. */
  clientId?: string;
}
/**
* Decides whether a verified bearer JWT is an OAuth 2.0 access token, so it may stand in for a
* stored access token. Passing this strategy's audience check does not settle the question: an
* OIDC ID token is minted for the client id and satisfies the same check, and using one as the
* On-Behalf-Of assertion is rejected by the IdP (Entra answers `AADSTS240002`).
*
* Only two signals distinguish the two by specification rather than by provider convention:
* - an RFC 9068 `at+jwt` header type, which is defined for access tokens alone;
* - an `aud` that omits the OIDC client id, which OIDC Core §2 requires every ID token to carry,
*   so a token without it cannot be an ID token for this deployment.
*
* Claim presence is deliberately not proof on its own. Providers add claims freely in both
* directions — Keycloak has emitted `nonce` and `auth_time` in access tokens, and maps `scope`
* into ID tokens — so `scp`/`scope` only qualifies a token whose audience has already ruled out
* an ID token. While the client id is in `aud` the token may be either, and it fails closed.
*
* `at_hash` and `c_hash` veto regardless, since they exist only to bind an ID token to its
* companion access token or code.
*/
declare function isAccessTokenJwt(token: string | undefined, claims: JwtTypeClaims | undefined, audiences?: AccessTokenAudiences): boolean;
/**
* Signals that the stored OpenID credentials cannot satisfy a placeholder, so the user must
* re-authenticate. `ErrorController` maps this to a 401, and `statusCode` additionally lets
* status-reading callers (the agent generation path's `getInitializationFailure`) answer 401
* instead of a bare 500. Deliberately carries no `body`, so the structural `isCustomError`
* guard cannot capture it ahead of the explicit mapping.
*/
declare class OpenIDReauthRequiredError extends Error {
  readonly statusCode = 401;
  constructor(message: string);
}
declare function extractOpenIDTokenInfo(user: Partial<IUser> | null | undefined): OpenIDTokenInfo | null;
declare function isOpenIDTokenValid(tokenInfo: OpenIDTokenInfo | null): boolean;
/**
* @param fields Restricts which placeholders may resolve. Callers holding a token set whose
* access token is unusable pass `['ID_TOKEN']`, so the ID token resolves on its own expiry while
* every other placeholder keeps its literal-then-strip behaviour.
*/
declare function processOpenIDPlaceholders(value: string, tokenInfo: OpenIDTokenInfo | null, fields?: readonly OpenIDTokenField[]): string;
declare function createBearerAuthHeader(tokenInfo: OpenIDTokenInfo | null): string;
declare function isOpenIDAvailable(): boolean;
//#endregion
//#region src/utils/openid.d.ts
/**
* Helper function to safely log sensitive data when debug mode is enabled
* @param obj - Object to stringify
* @param maxLength - Maximum length of the stringified output
* @returns Stringified object with sensitive data masked
*/
declare function safeStringify(obj: unknown, maxLength?: number): string;
/**
* Describes an OpenID request body without serializing any values. OAuth form
* bodies routinely contain authorization codes, refresh tokens, PKCE verifiers,
* and client secrets, so even partially masked values are unsafe to log.
*/
declare function logOpenIdRequestBody(body: unknown): string;
/**
* Helper to log headers without revealing sensitive information
* @param headers - Headers object to log
* @returns Stringified headers with sensitive data masked
*/
declare function logHeaders(headers: Headers | undefined | null): string;
//#endregion
//#region src/utils/promise.d.ts
/**
* Wraps a promise with a timeout. If the promise doesn't resolve/reject within
* the specified time, it will be rejected with a timeout error.
*
* @param promise - The promise to wrap with a timeout
* @param timeoutMs - Timeout duration in milliseconds
* @param errorMessage - Custom error message for timeout (optional)
* @param logger - Optional logger function to log timeout errors (e.g., console.warn, logger.warn)
* @returns Promise that resolves/rejects with the original promise or times out
*
* @example
* ```typescript
* const result = await withTimeout(
*   fetchData(),
*   5000,
*   'Failed to fetch data within 5 seconds',
*   console.warn
* );
* ```
*/
declare function withTimeout<T>(promise: Promise<T>, timeoutMs: number, errorMessage?: string, logger?: (message: string, error: Error) => void): Promise<T>;
/**
* Create an in-process concurrency limiter. Returns a `run` function that
* wraps async tasks: at most `concurrency` invocations may execute at once;
* additional calls queue and dequeue in FIFO order as slots free.
*
* Use to bound the parallelism of expensive CPU-or-IO work that fans out
* from a single producer (e.g. an agent emitting many office artifacts in
* one tool result), so the work doesn't compete with the still-running
* agent inference for event-loop time. Tasks remain queued — they are
* never dropped or rejected by the limiter itself — so the overall workload
* still completes; only peak concurrency is capped.
*
* Each task is wrapped in a thunk so timeouts and other side effects do
* not start until the limiter actually invokes it.
*
* @example
* ```typescript
* const limit = createConcurrencyLimiter(2);
* const results = await Promise.all(files.map((f) => limit(() => parse(f))));
* ```
*/
declare function createConcurrencyLimiter(concurrency: number): <T>(task: () => Promise<T>) => Promise<T>;
//#endregion
//#region src/utils/query.d.ts
type QueryValue = Request["query"][string];
declare const DEFAULT_PAGE_LIMIT = 25;
declare const MAX_PAGE_LIMIT = 100;
type SortDirection = "asc" | "desc";
/** The sort fields `getConvosByCursor` accepts; anything else makes it throw. Keep this
*  in sync with the whitelist in `packages/data-schemas/src/methods/conversation.ts`. */
type ConversationSortField = "title" | "createdAt" | "updatedAt" | "archivedAt";
declare const CONVERSATION_SORT_FIELDS: Readonly<Record<ConversationSortField, true>>;
/** Express parses a repeated `?limit=a&limit=b` into an array, which `parseInt` reads as its first element's digits. */
declare const queryString: (value: QueryValue) => string | undefined;
declare const normalizeSortField: <T extends string>(value: QueryValue, {
  fields,
  fallback
}: {
  fields: Readonly<Record<T, true>>;
  fallback: T;
}) => T;
declare const normalizeSortDirection: (value: QueryValue, {
  fallback
}?: {
  fallback?: SortDirection;
}) => SortDirection;
/**
* Clamps a page size from user input. An unclamped limit reaches Mongo's `.limit()`,
* where a negative value means "one batch" and `0` means "no limit at all".
*/
declare const normalizeLimit: (value: QueryValue, {
  fallback,
  max
}?: {
  fallback?: number;
  max?: number;
}) => number;
//#endregion
//#region src/utils/ports.d.ts
/**
* Strips port suffix from req.ip for use as a rate-limiter key (IPv4 and IPv6-safe).
* Bracket notation for the ip property avoids express-rate-limit v8's toString()
* heuristic that scans for the literal substring "req.ip" (ERR_ERL_KEY_GEN_IPV6).
*/
declare function removePorts(req: Request): string | undefined;
//#endregion
//#region src/utils/sanitizeTitle.d.ts
/** Max character length for sanitized titles (the output will never exceed this). */
declare const MAX_TITLE_LENGTH = 200;
declare const DEFAULT_TITLE_FALLBACK = "Untitled Conversation";
/**
* Sanitizes LLM-generated chat titles by removing {@link https://en.wikipedia.org/wiki/Chain-of-thought_prompting <think>}
* reasoning blocks, normalizing whitespace, and truncating to {@link MAX_TITLE_LENGTH} characters.
*
* Titles exceeding the limit are truncated at a code-point-safe boundary and suffixed with `...`.
*
* @param rawTitle - The raw LLM-generated title string, potentially containing <think> blocks.
* @returns A sanitized, potentially truncated title string, never empty (fallback used if needed).
*/
declare function sanitizeTitle(rawTitle: string): string;
//#endregion
//#region src/utils/text.d.ts
/** Token count function that can be sync or async */
type TokenCountFn = (text: string) => number | Promise<number>;
declare function normalizeString(value: unknown): string | undefined;
/**
* Processes text content by counting tokens and truncating if it exceeds the specified limit.
* Uses ratio-based estimation to minimize expensive tokenCountFn calls.
*
* @param text - The text content to process
* @param tokenLimit - The maximum number of tokens allowed
* @param tokenCountFn - Function to count tokens (can be sync or async)
* @returns Promise resolving to object with processed text, token count, and truncation status
*
* @remarks
* This function uses a ratio-based estimation algorithm instead of binary search.
* Binary search would require O(log n) tokenCountFn calls (~17 for 100k chars),
* while this approach typically requires only 2-3 calls for a 90%+ reduction in CPU usage.
*/
declare function processTextWithTokenLimit({
  text,
  tokenLimit,
  tokenCountFn,
  preserve
}: {
  text: string;
  tokenLimit: number;
  tokenCountFn: TokenCountFn;
  preserve?: "start" | "end";
}): Promise<{
  text: string;
  tokenCount: number;
  wasTruncated: boolean;
}>;
/**
* Truncates to `maxChars` keeping head and tail around an explicit marker, so
* the consumer (usually a model) can tell the content was cut and by how much.
*/
declare function truncateMiddle(value: string, maxChars: number): string;
//#endregion
//#region src/utils/yaml.d.ts
declare function loadYaml(filepath: string): unknown;
//#endregion
//#region src/utils/http.d.ts
/**
* Normalizes an error-like object into an HTTP status and message.
* Ensures we always respond with a valid numeric status to avoid UI hangs.
*/
declare function normalizeHttpError(err: Error | {
  status?: number;
  message?: string;
} | unknown, fallbackStatus?: number): {
  status: number;
  message: string;
};
//#endregion
//#region src/utils/tokens.d.ts
declare const maxTokensMap: Record<string, Record<string, number>>;
declare const modelMaxOutputs: {
  o1: number;
  "o1-mini": number;
  "o1-preview": number;
  "gpt-5": number;
  "gpt-5.1": number;
  "gpt-5.2": number;
  "gpt-5.3": number;
  "gpt-5.4": number;
  "gpt-5.4-pro": number;
  "gpt-5.4-mini": number;
  "gpt-5.4-nano": number;
  "gpt-5.5": number;
  "gpt-5.5-pro": number;
  "gpt-5.6": number;
  "gpt-5.6-terra": number;
  "gpt-5.6-luna": number;
  "gpt-6-astra": number;
  "chat-latest": number;
  "gpt-5-mini": number;
  "gpt-5-nano": number;
  "gpt-5-pro": number;
  "gpt-5.2-pro": number;
  "gpt-oss-20b": number;
  "gpt-oss-120b": number;
  system_default: number;
};
declare const maxOutputTokensMap: Record<string, Record<string, number>>;
/**
* Any model-keyed map. Matching reads keys only and never inspects a value, so
* the value type is deliberately open rather than a union that every caller has
* to cast into.
*/
type ModelKeyedMap = Readonly<Record<string, unknown>>;
/**
* Finds the longest matching key in the tokens map via substring match.
*
* A vendor prefix can outrank the id it precedes: `moonshotai/kimi-k2` matched
* `moonshot` (8) over `kimi-k2` (7) and reported half the real window. When the
* whole id resolves to a prefix-only match, retry against the model segment.
*
* A key that carries the vendor itself is already the most specific answer and
* is kept. `EndpointTokenConfig` is an arbitrary record, and one built from
* OpenRouter is keyed by `org/model`, so those must still beat a bare `model`.
*/
declare function findMatchingPattern(modelName: string, tokensMap: ModelKeyedMap): string | null;
/**
* Retrieves a token value for a given model name from a tokens map.
*
* @param modelName - The name of the model to look up.
* @param tokensMap - The map of model names to token values.
* @param [key='context'] - The key to look up in the tokens map.
* @returns The token value for the given model or undefined if no match is found.
*/
declare function getModelTokenValue(modelName: string, tokensMap?: EndpointTokenConfig | Record<string, number>, key?: keyof TokenConfig): number | undefined;
/**
* Retrieves the maximum tokens for a given model name.
*
* @param modelName - The name of the model to look up.
* @param endpoint - The endpoint (default is 'openAI').
* @param [endpointTokenConfig] - Token Config for current endpoint to use for max tokens lookup
* @returns The maximum tokens for the given model or undefined if no match is found.
*/
declare function getModelMaxTokens(modelName: string, endpoint?: EModelEndpoint, endpointTokenConfig?: EndpointTokenConfig): number | undefined;
/**
* Retrieves the maximum output tokens for a given model name.
*
* @param modelName - The name of the model to look up.
* @param endpoint - The endpoint (default is 'openAI').
* @param [endpointTokenConfig] - Token Config for current endpoint to use for max tokens lookup
* @returns The maximum output tokens for the given model or undefined if no match is found.
*/
declare function getModelMaxOutputTokens(modelName: string, endpoint?: EModelEndpoint, endpointTokenConfig?: EndpointTokenConfig): number | undefined;
/**
* Retrieves the model name key for a given model name input. If the exact model name isn't found,
* it searches for partial matches within the model name, checking keys in reverse order.
*
* @param modelName - The name of the model to look up.
* @param endpoint - The endpoint (default is 'openAI').
* @returns The model name key for the given model; returns input if no match is found and is string.
*
* @example
* matchModelName('gpt-4-32k-0613'); // Returns 'gpt-4-32k-0613'
* matchModelName('gpt-4-32k-unknown'); // Returns 'gpt-4-32k'
* matchModelName('unknown-model'); // Returns undefined
*/
declare function matchModelName(modelName: string, endpoint?: EModelEndpoint): string | undefined;
declare const modelSchema: z$1.ZodObject<{
  id: z$1.ZodString;
  pricing: z$1.ZodObject<{
    prompt: z$1.ZodString;
    completion: z$1.ZodString;
  }, "strip", z$1.ZodTypeAny, {
    prompt: string;
    completion: string;
  }, {
    prompt: string;
    completion: string;
  }>;
  context_length: z$1.ZodNumber;
}, "strip">;
declare const inputSchema: z$1.ZodObject<{
  data: z$1.ZodArray<z$1.ZodObject<{
    id: z$1.ZodString;
    pricing: z$1.ZodObject<{
      prompt: z$1.ZodString;
      completion: z$1.ZodString;
    }, "strip", z$1.ZodTypeAny, {
      prompt: string;
      completion: string;
    }, {
      prompt: string;
      completion: string;
    }>;
    context_length: z$1.ZodNumber;
  }, "strip", z$1.ZodTypeAny, {
    id: string;
    pricing: {
      prompt: string;
      completion: string;
    };
    context_length: number;
  }, {
    id: string;
    pricing: {
      prompt: string;
      completion: string;
    };
    context_length: number;
  }>, "many">;
}, "strip">;
/**
* Processes a list of model data from an API and organizes it into structured data based on URL and specifics of rates and context.
* @param {{ data: Array<z.infer<typeof modelSchema>> }} input The input object containing base URL and data fetched from the API.
* @returns {EndpointTokenConfig} The processed model data.
*/
declare function processModelData(input: z$1.infer<typeof inputSchema>): EndpointTokenConfig;
//#endregion
//#region src/utils/tokenMap.d.ts
/** Signature for a function that counts tokens in a LangChain message. */
type TokenCounter = (message: BaseMessage) => number;
/**
* Lazily fills missing token counts for formatted LangChain messages.
* Preserves precomputed counts and only computes undefined indices.
*
* This is used after `formatAgentMessages` to ensure every message index
* has a token count before passing `indexTokenCountMap` to the agent run.
*/
declare function hydrateMissingIndexTokenCounts({
  messages,
  indexTokenCountMap,
  tokenCounter
}: {
  messages: BaseMessage[];
  indexTokenCountMap: Record<number, number | undefined> | undefined;
  tokenCounter: TokenCounter;
}): Record<number, number>;
//#endregion
//#region src/utils/url.d.ts
/**
* Extracts a valid OpenAI baseURL from a given string, matching "url/v1," followed by an optional suffix.
* The suffix can be one of several predefined values (e.g., 'openai', 'azure-openai', etc.),
* accommodating different proxy patterns like Cloudflare, LiteLLM, etc.
* Returns the original URL if no valid pattern is found.
*
* Examples:
* - `https://open.ai/v1/chat` -> `https://open.ai/v1`
* - `https://open.ai/v1/chat/completions` -> `https://open.ai/v1`
* - `https://gateway.ai.cloudflare.com/v1/account/gateway/azure-openai/completions` -> `https://gateway.ai.cloudflare.com/v1/account/gateway/azure-openai`
* - `https://open.ai/v1/hi/openai` -> `https://open.ai/v1/hi/openai`
* - `https://api.example.com/v1/replicate` -> `https://api.example.com/v1/replicate`
*
* @param url - The URL to be processed.
* @returns The matched pattern or input if no match is found.
*/
declare function extractBaseURL(url: string): string | null | undefined;
/**
* Extracts the base URL (protocol + hostname + port) from the provided URL.
* Used primarily for Ollama endpoints to derive the host.
* @param fullURL - The full URL.
* @returns The base URL (protocol://hostname:port).
*/
declare function deriveBaseURL(fullURL: string): string;
//#endregion
//#region src/utils/message.d.ts
/** Minimal shape for request file entries (from `req.body.files`) */
type RequestFile = {
  file_id?: string;
};
type GetMessagesByParentId = (filter: {
  user: string;
  messageId: string;
  conversationId?: string;
}, select: "_id") => Promise<unknown[]>;
/** Fields to strip from files before client transmission */
declare const FILE_STRIP_FIELDS: readonly ["text", "_id", "__v"];
/** Fields to strip from messages before client transmission */
declare const MESSAGE_STRIP_FIELDS: readonly ["fileContext"];
/**
* Strips large/unnecessary fields from a file object before transmitting to client.
* Use this within existing loops when building file arrays to avoid extra iterations.
*
* @param file - The file object to sanitize
* @returns A new file object without the stripped fields
*
* @example
* // Use in existing file processing loop:
* for (const attachment of client.options.attachments) {
*   if (messageFiles.has(attachment.file_id)) {
*     userMessage.files.push(sanitizeFileForTransmit(attachment));
*   }
* }
*/
declare function sanitizeFileForTransmit<T extends Partial<TFile>>(file: T): Omit<T, (typeof FILE_STRIP_FIELDS)[number]>;
/** Filters attachments to those whose `file_id` appears in `requestFiles`, then sanitizes each. */
declare function buildMessageFiles<T extends Partial<TFile>>(requestFiles: RequestFile[], attachments: T[]): Omit<T, (typeof FILE_STRIP_FIELDS)[number]>[];
/**
* Sanitizes a message object before transmitting to client.
* Removes large fields like `fileContext` and strips `text` from embedded files.
*
* @param message - The message object to sanitize
* @returns A new message object safe for client transmission
*
* @example
* sendEvent(res, {
*   final: true,
*   requestMessage: sanitizeMessageForTransmit(userMessage),
*   responseMessage: response,
* });
*/
declare function sanitizeMessageForTransmit<T extends Partial<TMessage>>(message: T): Omit<T, (typeof MESSAGE_STRIP_FIELDS)[number]>;
declare function isPreliminaryMessageId(messageId: unknown): messageId is string;
declare function isUnpersistedPreliminaryParent({
  userId,
  conversationId,
  parentMessageId,
  getMessages
}: {
  userId: string;
  conversationId?: string | null;
  parentMessageId?: string | null;
  getMessages: GetMessagesByParentId;
}): Promise<boolean>;
/** Minimal message shape for thread traversal.
*
* Both `files` and `attachments` carry `file_id` references, but they
* land on different roles by convention: user-uploaded files live on
* `messages.files` (set when the user attaches a file in chat), while
* code-execution outputs live on `messages.attachments` (set by
* `processCodeOutput` when a tool call produces a file). The thread
* walk must visit both so the next turn's `tool_resources.execute_code.file_ids`
* picks up files the assistant generated, not just files the user
* uploaded. */
type ThreadMessage = {
  messageId: string;
  parentMessageId?: string | null;
  files?: Array<{
    file_id?: string;
  }>;
  attachments?: Array<{
    file_id?: string;
  }>;
};
/** Result of thread data extraction */
type ThreadData = {
  messageIds: string[];
  fileIds: string[];
};
/**
* Extracts thread message IDs and file IDs in a single O(n) pass.
* Builds a Map for O(1) lookups, then traverses the thread collecting both IDs.
*
* @param messages - All messages in the conversation (should be queried with select for efficiency)
* @param parentMessageId - The ID of the parent message to start traversal from
* @returns Object containing messageIds and fileIds arrays
*/
declare function getThreadData(messages: ThreadMessage[], parentMessageId: string | null | undefined): ThreadData;
//#endregion
//#region src/utils/quotes.d.ts
/** Max characters kept per quoted excerpt (defense-in-depth over the client cap). */
declare const QUOTE_MAX_LENGTH = 1500;
/** Max number of quoted excerpts merged into a single user turn. */
declare const QUOTE_MAX_COUNT = 10;
interface ReferencedQuoteEntry {
  readonly sourceIndex: number;
  readonly text: string;
}
/**
* Normalizes the `quotes` field off a request body into a clean string array.
* Trims, drops empties/non-strings, caps each excerpt length and the overall
* count. Returns `null` when there is nothing usable so callers can skip the
* merge entirely.
*/
declare function getReferencedQuotes(raw: unknown): string[] | null;
/** Normalizes quotes while retaining their source-array positions. */
declare function getReferencedQuoteEntries(raw: unknown): ReferencedQuoteEntry[] | null;
/**
* Renders quoted excerpts as Markdown blockquote blocks, one block per excerpt
* separated by a blank line. Provider-agnostic (no bespoke tags). Returns an
* empty string when there are no quotes.
*/
declare function formatQuotesAsMarkdown(quotes: string[]): string;
/**
* Merges quoted excerpts into a body of text as Markdown blockquotes, prepended
* before the text so the excerpts read as part of the user turn. Returns the
* original text unchanged when there are no quotes.
*/
declare function mergeQuotedText(text: string, quotes: string[]): string;
/**
* Resolves the prompt text to tokenize for a (possibly quoted) user turn,
* mirroring the send path: a user message's persisted `quotes` are prepended
* into the prompt on every turn, so an edit that only changes `text` must count
* the merged text+quotes to keep the stored `tokenCount` authoritative. Returns
* `text` unchanged for non-user messages or when there are no usable quotes.
*/
declare function mergeQuotedTextForCount(text: string, rawQuotes: unknown, isCreatedByUser: boolean): string;
//#endregion
//#region src/utils/tracing.d.ts
/**
* Runs `fn` outside the LangGraph/LangSmith tracing AsyncLocalStorage context
* so I/O handles (child processes, sockets, timers) created during `fn`
* do not permanently retain the RunTree → graph config → message data chain.
*
* Relies on the private symbol `ls:tracing_async_local_storage` from `@langchain/core`.
* If the symbol is absent, falls back to calling `fn()` directly.
*/
declare function runOutsideTracing<T>(fn: () => T): T;
//#endregion
//#region src/utils/tokenizer.d.ts
type EncodingName = "o200k_base" | "claude";
declare class Tokenizer {
  private tokenizersCache;
  private loadingPromises;
  /** Pre-loads an encoding so that subsequent getTokenCount calls are accurate. */
  initEncoding(encoding: EncodingName): Promise<void>;
  /** Returns a counter that avoids expensive tokenization for oversized content. */
  createExactTokenCounter(encoding: EncodingName): Promise<(text: string) => number>;
  getTokenCount(text: string, encoding?: EncodingName): number;
  /**
  * Token count for content that must not be estimated, or `undefined` when it
  * cannot be produced, so the caller can omit a figure instead of publishing a
  * guess. Used for a value that is ADDED to exact provider accounting — see the
  * retained tool results of a turn stopped at the tool-call limit — where
  * {@link getTokenCount}'s two fallbacks would corrupt the sum: the cold-start
  * estimate is a character ratio, and the oversized-input shortcut returns byte
  * length, several times the real count on ordinary text.
  *
  * The input is tokenized whole. Counting it in slices would be cheaper but not
  * exact — a BPE merge spanning a seam is charged twice, ~1 token per 4 KiB
  * measured — and this is the one caller that needs the real number. It costs
  * ~60 ms/MB, so the caller decides how much content is worth counting
  * (`endpoints.agents.maxRetainedToolCountChars`) rather than a bound here.
  */
  countExactTokens(text: string, encoding?: EncodingName): number | undefined;
  private handleCountError;
}
declare const TokenizerSingleton: Tokenizer;
/**
* Counts the number of tokens in a given text using ai-tokenizer with o200k_base encoding.
* @param text - The text to count tokens in. Defaults to an empty string.
* @returns The number of tokens in the provided text.
*/
declare function countTokens(text?: string): Promise<number>;
//#endregion
//#region src/db/utils.d.ts
/**
* Ensures that a collection exists in the database.
* For DocumentDB compatibility, it tries multiple approaches.
* @param db - The MongoDB database instance
* @param collectionName - The name of the collection to ensure exists
*/
declare function ensureCollectionExists(db: mongo.Db, collectionName: string): Promise<void>;
/**
* Ensures that all required collections exist for the permission system.
* This includes aclentries, groups, accessroles, and any other collections
* needed for migrations and permission checks.
* @param db - The MongoDB database instance
*/
declare function ensureRequiredCollectionsExist(db: mongo.Db): Promise<void>;
//#endregion
//#region src/html/bootstrap.d.ts
interface BootstrapInjection {
  /** Attribute marking this injection's script, so serving one shell through
  *  two injectors — or the same one twice — does not stack it. */
  sentinel: string;
  values: Record<string, boolean | number | string>;
}
/**
* Writes server-known values into the shell's `<head>` (or the top of `<body>`
* in a document without one), ahead of the app's own scripts so the first
* render reads them. The caller stamps the CSP nonce afterwards.
*/
declare const injectBootstrapConfig: (html: string, {
  sentinel,
  values
}: BootstrapInjection) => string;
//#endregion
//#region src/html/devtools.d.ts
declare const QUERY_DEVTOOLS_HEADER = "x-librechat-enable-query-devtools";
interface QueryDevtoolsRequest {
  get(header: string): string | undefined;
}
declare const shouldEnableQueryDevtools: (req: QueryDevtoolsRequest) => boolean;
declare const maybeInjectQueryDevtoolsBootstrap: (html: string, req: QueryDevtoolsRequest) => string;
//#endregion
//#region src/html/footer.d.ts
/** What the deployment configured, as the server holds it: the footer env var
*  and the interface config the app loaded. */
interface ConfiguredFooterSource {
  customFooter?: string | null;
  interfaceConfig?: Pick<TInterfaceConfig, "privacyPolicy" | "termsOfService"> | null;
}
/**
* Emits the deployment's footer answer with the HTML shell.
*
* A conversation's composer lays out against whether a footer bar sits beneath
* it, and `/api/config` answers that only after the composer has painted. The
* server already knows when it serves the document, so it says so and no client
* has to guess: a first-ever visit to a configured deployment paints the
* composer in its final position instead of correcting by the bar's height.
* The answer is the deployment's own configuration — `librechat.yaml` plus
* `CUSTOM_FOOTER` — because the document is served before there is a caller to
* resolve: a per-tenant, role or user config override of `privacyPolicy` or
* `termsOfService` is resolved by `/api/config`, and the client prefers that
* resolved answer over this one. Reading the effective config here would cost
* an override query on the document that carries the conversation's LCP, and
* still could not resolve role or user overrides pre-authentication.
*
* Always injected, including the negative answer, so an absent flag means "no
* server said" (the Vite dev server) rather than "no footer".
*/
declare const injectConfiguredFooterBootstrap: (html: string, source?: ConfiguredFooterSource | null) => string;
//#endregion
//#region src/oauth/csrf.d.ts
declare const OAUTH_CSRF_COOKIE = "oauth_csrf";
declare const OAUTH_CSRF_MAX_AGE: number;
declare const OAUTH_SESSION_COOKIE = "oauth_session";
declare const OAUTH_SESSION_MAX_AGE: number;
declare const OAUTH_SESSION_COOKIE_PATH = "/api";
/**
* Determines if secure cookies should be used.
* SESSION_COOKIE_SECURE=true/false explicitly overrides the environment heuristic.
* Returns `true` in production unless DOMAIN_SERVER uses a localhost-style hostname.
* This allows cookies to work on localhost during local development
* even when `NODE_ENV=production` (common in Docker Compose setups).
*/
declare function shouldUseSecureCookie(): boolean;
declare const REFRESH_TOKEN_COOKIE = "refreshToken";
declare const TOKEN_PROVIDER_COOKIE = "token_provider";
declare const OPENID_USER_ID_COOKIE = "openid_user_id";
/**
* Writes the IdP refresh token to the `refreshToken` cookie. Single source of
* truth for the cookie's options so the login/refresh path
* (`setOpenIDAuthTokens`) and the inline OBO refresh path (`performIdpRefresh`)
* stay byte-for-byte in sync. The cookie outlives the (shorter) express-session
* cookie and is the fallback `refreshController` reads when the session copy is
* gone, so a rotated refresh token must land here too — otherwise a later
* session loss replays an invalidated token and signs the user out.
*/
declare function setRefreshTokenCookie(res: Response$2, refreshToken: string, expires: Date): void;
interface OpenIDMarkerCookieOptions {
  userId?: string | null;
  expires: Date;
  refreshExpiryMs: number;
  reuseTokens?: boolean;
  /** Binds the marker to the refresh token it was issued alongside. */
  refreshToken?: string | null;
}
declare function setOpenIDMarkerCookies(res: Response$2, {
  userId,
  expires,
  refreshExpiryMs,
  reuseTokens,
  refreshToken
}: OpenIDMarkerCookieOptions): void;
/** Generates an HMAC-based token for OAuth CSRF protection */
declare function generateOAuthCsrfToken(flowId: string, secret?: string): string;
/** Sets a SameSite=Lax CSRF cookie bound to a specific OAuth flow */
declare function setOAuthCsrfCookie(res: Response$2, flowId: string, cookiePath: string): void;
/**
* Validates the per-flow CSRF cookie against the expected HMAC.
* Uses timing-safe comparison and always clears the cookie to prevent replay.
*/
declare function validateOAuthCsrf(req: Request, res: Response$2, flowId: string, cookiePath: string): boolean;
/**
* Express middleware that sets the OAuth session cookie after JWT authentication.
* Chain after requireJwtAuth on routes that precede an OAuth redirect (e.g., reinitialize, bind).
*/
declare function setOAuthSession(req: Request, res: Response$2, next: NextFunction): void;
/** Sets a SameSite=Lax session cookie that binds the browser to the authenticated userId */
declare function setOAuthSessionCookie(res: Response$2, userId: string): void;
/** Validates the session cookie against the expected userId using timing-safe comparison */
declare function validateOAuthSession(req: Request, userId: string): boolean;
//#endregion
//#region src/oauth/state.d.ts
declare const OAUTH_STATE_COOKIE_PREFIX = "oauth_state_";
interface OAuthStateStoreOptions {
  /** Strategy name; names the cookie, scopes the signature and labels rejections in logs. */
  provider: string;
  /** Signs each state; every replica must share it. */
  secret: string;
  /** Whether the deployment serves `Secure` cookies; secure deployments get a `__Host-` cookie. */
  secureCookie: boolean;
  /** How long a started login may take to reach its callback. */
  maxAgeMs?: number;
  /**
  * The provider returns with a cross-site form POST (Apple's `form_post`), which a
  * `SameSite=Lax` cookie does not accompany.
  */
  crossSiteCallback?: boolean;
}
type StoreCallback = (err: Error | null, state?: string) => void;
type VerifyCallback = (err: Error | null, ok: boolean, info?: {
  message: string;
}) => void;
/**
* State store for `passport-oauth2`, which dispatches on each method's arity:
* `store(req, callback)` and `verify(req, providedState, callback)`.
*/
interface OAuthStateStore {
  store(req: Request, callback: StoreCallback): void;
  verify(req: Request, providedState: unknown, callback: VerifyCallback): void;
}
/** A passport strategy whose `authorizationParams` may fill in a `state` of its own. */
interface PresetStateStrategy {
  authorizationParams(options: object): {
    state?: string;
  };
}
/**
* Binds an OAuth login to the browser that started it. The browser holds a random binding in an
* HttpOnly cookie, reused by every flow it starts, and each `state` is an HMAC over that binding
* and the time the flow started. The callback proceeds only when the returned `state` was signed
* for one of this browser's bindings and is younger than `maxAgeMs`, so a callback reaching a
* browser that did not start the flow ends before its code is exchanged. Nothing is stored per
* flow, so logins started in separate tabs complete independently. A browser without a binding
* gets one under a fresh cookie name, so first starts racing in two tabs each keep their own.
*/
declare function createOAuthStateStore({
  provider,
  secret,
  secureCookie,
  maxAgeMs,
  crossSiteCallback
}: OAuthStateStoreOptions): OAuthStateStore;
/**
* Leaves `state` to the configured store for strategies that fill in their own. passport-apple
* assigns one inside `authorizationParams` (on the route's shared options, so it never changes
* after the first request), and a preset `state` bypasses the store entirely.
*/
declare function deferStateToStore(strategy: PresetStateStrategy): void;
//#endregion
//#region src/oauth/expiry.d.ts
/**
* RFC 6749 §5.1 makes `expires_in` only RECOMMENDED, so a token response may legally omit it, and
* providers have been observed sending it as a string. Deriving a lifetime from the raw field is
* therefore unsafe: `undefined * 1000` is `NaN`, and `NaN` is neither an error nor a default.
*
* A `NaN` cache TTL means an entry that never expires. `@keyv/redis` writes the key without `PX`
* because `NaN` is falsy, and Keyv's own expiry checks compare with `>`, which is always false
* against `NaN`. The namespace default does not stand in either, since Keyv applies it with `??=`
* and `NaN` is neither `null` nor `undefined`. A `NaN` timestamp is just as sharp: `new Date(NaN)`
* is an Invalid Date and `toISOString()` on it throws `RangeError`.
*
* Every lifetime derived from a token response goes through here so the rule has one home.
*/
/**
* Fallback lifetime for a token response that declares none. One hour matches every hand-written
* default this helper replaces, and is short enough that a wrongly-guessed lifetime self-corrects.
*/
declare const DEFAULT_OAUTH_TOKEN_TTL_SECONDS = 3600;
/**
* Margin a credential needs to survive the trip to whoever will use it. A token served inside this
* window expires in transit and is rejected downstream, so it is treated as already gone — both
* when validating the user's own federated token (`isOpenIDTokenValid`, `isIdTokenCurrent`) and
* when caching a token obtained by exchange. Lives here, with the rest of the lifetime math, so a
* consumer that stubs OpenID token validation cannot leave the arithmetic reading `undefined`.
*/
declare const OPENID_EXPIRY_BUFFER_SECONDS = 30;
/**
* The lifetime a token response declares, in seconds, or `undefined` when it declares none usable.
*
* A non-positive value is preserved rather than discarded: the provider is stating the credential
* is already expired, which is information, and collapsing it into "unknown" would hand it the
* fallback lifetime and keep a dead credential alive.
*/
declare function normalizeExpiresIn(expiresIn: unknown): number | undefined;
/**
* Cache TTL in milliseconds for a token response. A provider that omits `expires_in` gets
* `fallbackSeconds` rather than an entry that outlives the credential it holds; one that declares
* an elapsed lifetime gets the shortest positive TTL rather than `0`, which Keyv reads as no expiry.
*
* The declared lifetime is shortened by {@link OPENID_EXPIRY_BUFFER_SECONDS} — the same margin
* `isOpenIDTokenValid` applies to the user's own federated token. A credential served in the final
* seconds of its life expires in transit and is rejected downstream, and because the read side
* re-serves whatever the cache still holds, that entry must be gone before the credential is.
* An unknown lifetime takes the fallback unshortened: there is no declared expiry to protect.
*/
declare function getTokenCacheTtlMs(expiresIn: unknown, fallbackSeconds: number): number;
/**
* Absolute expiry in epoch milliseconds for a token response, preferring an expiry the caller
* already holds. `fallbackSeconds` covers a response that declares no usable lifetime.
*/
declare function getTokenExpiresAtMs({
  expiresAt,
  expiresIn,
  fallbackSeconds,
  now
}: {
  expiresAt?: number | null;
  expiresIn?: unknown;
  fallbackSeconds?: number;
  now: number;
}): number;
/**
* The expiry a downstream consumer should honour: the real one pulled back by the in-transit
* buffer, so a credential handed on with this stamp cannot be accepted into its final seconds.
*/
declare function getSkewedTokenExpiresAtMs(expiresAt: number, now: number): number;
/** Cache TTL for a token whose absolute expiry is already known, buffered as above. */
declare function getSkewedTokenCacheTtlMs(expiresAt: number, now: number): number;
/**
* Whether a cached credential still has enough life to survive the trip downstream. A cache entry
* that outlived its TTL check — a shared store with a coarser clock, an entry written before the
* buffer existed — is rejected here rather than handed out to fail at the far end.
*/
declare function hasUsableTokenExpiry(expiresAt?: number | null, now?: number): boolean;
/**
* Absolute expiry for a token response, or `undefined` when its lifetime is unknown. Callers store
* nothing rather than an Invalid Date, so an unknown expiry stays distinguishable from an elapsed
* one — an elapsed lifetime still yields a past timestamp, so callers refresh instead of guessing.
*/
declare function getTokenExpiresAt(expiresIn: unknown): Date | undefined;
//#endregion
//#region src/oauth/failure.d.ts
type OAuthFailureRequest = Pick<Request, "headers" | "method" | "path" | "originalUrl" | "query"> & {
  id?: string;
  requestId?: string;
  session?: {
    messages?: unknown[];
  };
};
type OAuthFailureLog = {
  provider: string;
  code?: string;
  name?: string;
  message?: string;
  cause_code?: string;
  cause_name?: string;
  cause_message?: string;
  has_code: boolean;
  has_state: boolean;
  query_error?: string;
  query_error_description?: string;
  method?: string;
  path?: string;
  request_id?: string;
  host?: string;
  forwarded_host?: string;
  forwarded_proto?: string;
  forwarded_for?: string;
  real_ip?: string;
  user_agent?: string;
};
type BuildOAuthFailureLogParams = {
  provider: string;
  req: OAuthFailureRequest;
  err?: unknown;
  info?: unknown;
  defaultMessage?: string;
};
declare function getOAuthFailureMessage(req: OAuthFailureRequest, defaultMessage?: string): string;
declare function buildOAuthFailureLog({
  provider,
  req,
  err,
  info,
  defaultMessage
}: BuildOAuthFailureLogParams): OAuthFailureLog;
declare function isOAuthProtocolFailure(err?: unknown, info?: unknown): boolean;
//#endregion
//#region src/oauth/callback.d.ts
type LoginFunction = (user: unknown, options: {
  session: false;
}, done: (err?: unknown) => void) => void;
type OpenIDCallbackRequest = OAuthFailureRequest & {
  logIn?: LoginFunction;
  user?: unknown;
};
type OpenIDCallback = (err: unknown, user: unknown, info: unknown) => void;
type PassportMiddleware = (req: OpenIDCallbackRequest, res: Response$2, next: NextFunction) => unknown;
type PassportLike = {
  authenticate: (strategy: "openid", options: {
    failureMessage: true;
    session: false;
  }, callback: OpenIDCallback) => PassportMiddleware;
};
type OAuthCallbackLogLevel = "warn" | "error";
type OAuthCallbackLogger = Record<OAuthCallbackLogLevel, (message: string, details: OAuthFailureLog) => void>;
type AuthFailureRedirectOptions = {
  clientDomain?: string;
  authFailedError: string;
};
type LogOpenIDCallbackFailureOptions = {
  logger: OAuthCallbackLogger;
  req: OAuthFailureRequest;
  err?: unknown;
  info?: unknown;
  level?: OAuthCallbackLogLevel;
};
type OpenIDCallbackAuthenticatorOptions = AuthFailureRedirectOptions & {
  logger: OAuthCallbackLogger;
  passport: PassportLike;
};
declare function redirectToAuthFailure(res: Response$2, {
  clientDomain,
  authFailedError
}: AuthFailureRedirectOptions): void;
declare function logOpenIDCallbackFailure({
  logger,
  req,
  err,
  info,
  level
}: LogOpenIDCallbackFailureOptions): void;
declare function createOpenIDCallbackAuthenticator({
  passport,
  logger,
  clientDomain,
  authFailedError
}: OpenIDCallbackAuthenticatorOptions): (req: OpenIDCallbackRequest, res: Response$2, next: NextFunction) => unknown;
//#endregion
//#region src/oauth/tokens.d.ts
declare function createHandleOAuthToken({
  findToken,
  updateToken,
  createToken
}: {
  findToken: TokenMethods["findToken"];
  updateToken: TokenMethods["updateToken"];
  createToken: TokenMethods["createToken"];
}): ({
  token,
  userId,
  identifier,
  expiresIn,
  metadata,
  type
}: {
  token: string;
  userId: string;
  identifier: string;
  expiresIn?: number | string | null;
  metadata?: Record<string, unknown>;
  type?: string;
}) => Promise<IToken | null>;
/**
* Refreshes the access token using the refresh token.
* @param fields
* @param fields.userId - The ID of the user.
* @param fields.client_url - The URL of the OAuth provider.
* @param fields.identifier - The identifier for the token.
* @param fields.refresh_token - The refresh token to use.
* @param fields.token_exchange_method - The token exchange method ('default_post' or 'basic_auth_header').
* @param fields.encrypted_oauth_client_id - The client ID for the OAuth provider.
* @param fields.encrypted_oauth_client_secret - The client secret for the OAuth provider.
*/
declare function refreshAccessToken({
  userId,
  client_url,
  identifier,
  refresh_token,
  token_exchange_method,
  encrypted_oauth_client_id,
  encrypted_oauth_client_secret,
  allowedAddresses
}: {
  userId: string;
  client_url: string;
  identifier: string;
  refresh_token: string;
  token_exchange_method: TokenExchangeMethodEnum;
  encrypted_oauth_client_id: string;
  encrypted_oauth_client_secret: string;
  allowedAddresses?: string[] | null;
}, {
  findToken,
  updateToken,
  createToken
}: {
  findToken: TokenMethods["findToken"];
  updateToken: TokenMethods["updateToken"];
  createToken: TokenMethods["createToken"];
}): Promise<{
  access_token: string;
  expires_in: number;
  refresh_token?: string;
  refresh_token_expires_in?: number;
}>;
/**
* Handles the OAuth callback and exchanges the authorization code for tokens.
* @param {object} fields
* @param {string} fields.code - The authorization code returned by the provider.
* @param {string} fields.userId - The ID of the user.
* @param {string} fields.identifier - The identifier for the token.
* @param {string} fields.client_url - The URL of the OAuth provider.
* @param {string} fields.redirect_uri - The redirect URI for the OAuth provider.
* @param {string} fields.token_exchange_method - The token exchange method ('default_post' or 'basic_auth_header').
* @param {string} fields.encrypted_oauth_client_id - The client ID for the OAuth provider.
* @param {string} fields.encrypted_oauth_client_secret - The client secret for the OAuth provider.
*/
declare function getAccessToken({
  code,
  userId,
  identifier,
  client_url,
  redirect_uri,
  token_exchange_method,
  encrypted_oauth_client_id,
  encrypted_oauth_client_secret,
  allowedAddresses
}: {
  code: string;
  userId: string;
  identifier: string;
  client_url: string;
  redirect_uri: string;
  token_exchange_method: TokenExchangeMethodEnum;
  encrypted_oauth_client_id: string;
  encrypted_oauth_client_secret: string;
  allowedAddresses?: string[] | null;
}, {
  findToken,
  updateToken,
  createToken
}: {
  findToken: TokenMethods["findToken"];
  updateToken: TokenMethods["updateToken"];
  createToken: TokenMethods["createToken"];
}): Promise<{
  access_token: string;
  expires_in: number;
  refresh_token?: string;
  refresh_token_expires_in?: number;
}>;
//#endregion
//#region src/oauth/validation.d.ts
type ActionOAuthEndpointField = "authorization_url" | "client_url";
interface ActionOAuthAuthMetadata {
  type?: AuthTypeEnum | string | null;
  authorization_url?: string | null;
  client_url?: string | null;
}
declare function validateActionOAuthEndpoint(url: string | null | undefined, fieldName: ActionOAuthEndpointField, allowedAddresses?: string[] | null): Promise<void>;
declare function validateActionOAuthMetadata(auth?: ActionOAuthAuthMetadata | null, allowedAddresses?: string[] | null): Promise<void>;
//#endregion
//#region src/mcp/oauth/OAuthReconnectionTracker.d.ts
declare class OAuthReconnectionTracker {
  private failedMeta;
  /** Map of userId -> Set of serverNames that are actively reconnecting */
  private active;
  /** Map of userId:serverName -> timestamp when reconnection started */
  private activeTimestamps;
  /** Maximum time (ms) a server can be in reconnecting state before auto-cleanup */
  private readonly RECONNECTION_TIMEOUT_MS;
  isFailed(userId: string, serverName: string): boolean;
  /** Check if server is in the active set (original simple check) */
  isActive(userId: string, serverName: string): boolean;
  /** Check if server is still reconnecting (considers timeout) */
  isStillReconnecting(userId: string, serverName: string): boolean;
  /** Clean up server if it has timed out - returns true if cleanup was performed */
  cleanupIfTimedOut(userId: string, serverName: string): boolean;
  setFailed(userId: string, serverName: string): void;
  setActive(userId: string, serverName: string): void;
  removeFailed(userId: string, serverName: string): void;
  removeActive(userId: string, serverName: string): void;
  /** Returns map sizes for diagnostics */
  getStats(): {
    usersWithFailedServers: number;
    usersWithActiveReconnections: number;
    activeTimestamps: number;
  };
}
//#endregion
//#region src/mcp/oauth/OAuthReconnectionManager.d.ts
declare class OAuthReconnectionManager {
  private static instance;
  protected readonly flowManager: FlowStateManager<MCPOAuthTokens | null>;
  protected readonly tokenMethods: TokenMethods;
  private readonly mcpManager;
  private readonly onOAuthCredentialsChanged?;
  private readonly onOAuthCredentialsChanging?;
  private readonly reconnectionsTracker;
  static getInstance(): OAuthReconnectionManager;
  static createInstance(flowManager: FlowStateManager<MCPOAuthTokens | null>, tokenMethods: TokenMethods, reconnections?: OAuthReconnectionTracker, onOAuthCredentialsChanged?: (scope: {
    userId: string;
    serverName: string;
  }) => Promise<void>, onOAuthCredentialsChanging?: UserConnectionContext["onOAuthCredentialsChanging"]): Promise<OAuthReconnectionManager>;
  constructor(flowManager: FlowStateManager<MCPOAuthTokens | null>, tokenMethods: TokenMethods, reconnections?: OAuthReconnectionTracker, onOAuthCredentialsChanged?: (scope: {
    userId: string;
    serverName: string;
  }) => Promise<void>, onOAuthCredentialsChanging?: UserConnectionContext["onOAuthCredentialsChanging"]);
  isReconnecting(userId: string, serverName: string): boolean;
  /**
  * Reconnects the user's eligible OAuth servers.
  * @param configServers Tenant-scoped Config-tier candidates used to resolve effective overlays.
  */
  reconnectServers(userId: string, configServers?: Record<string, ParsedServerConfig>): Promise<void>;
  /**
  * Fire-and-forget wrapper around {@link tryReconnect} that guarantees any
  * unexpected rejection is surfaced via the logger instead of propagating as
  * an unhandled promise rejection. Also runs the failed-reconnect cleanup so
  * the tracker does not get stuck in `active` state for the
  * `RECONNECTION_TIMEOUT_MS` window if an error escapes
  * {@link tryReconnect}'s internal try/catch.
  */
  private safeTryReconnect;
  private cleanupOnFailedReconnect;
  /**
  * Attempts to reconnect a single OAuth MCP server.
  * @param configServers Tenant-scoped Config-tier candidates used to resolve the effective config.
  * @returns true if reconnection succeeded, false otherwise.
  */
  reconnectServer(userId: string, serverName: string, configServers?: Record<string, ParsedServerConfig>): Promise<boolean>;
  clearReconnection(userId: string, serverName: string): void;
  private tryReconnect;
  getTrackerStats(): {
    usersWithFailedServers: number;
    usersWithActiveReconnections: number;
    activeTimestamps: number;
  };
  private canReconnect;
}
//#endregion
//#region src/crypto/jwt.d.ts
declare const AGENT_TRIGGER_SCOPE = "agent_trigger";
type AgentTriggerRequest = {
  headers?: Record<string, string | string[] | undefined>;
  _isAgentTrigger?: boolean;
};
/**
* Generate a short-lived JWT token.
* @param {String} userId - The ID of the user.
* @param {String} [expireIn='5m'] - The expiration time for the token.
* @returns {String} - The generated JWT token.
*/
declare const generateShortLivedToken: (userId: string, expireIn?: string) => string;
/** Mint the server-only identity used by durable agent trigger admission. */
declare const generateAgentTriggerToken: (userId: string, expireIn?: string) => string;
/** Verify the signed trigger scope together with its explicit transport marker. */
declare const isAgentTriggerRequest: (req?: AgentTriggerRequest) => boolean;
/** Skip the shared loopback IP bucket while retaining per-user and concurrency limits. */
declare const exemptAgentTriggerFromIpLimiter: (req?: AgentTriggerRequest) => boolean;
//#endregion
//#region src/middleware/access.d.ts
declare function skipAgentCheck(req?: Request): boolean;
interface CheckAccessParams {
  user: IUser;
  req?: Request;
  permissionType: PermissionTypes;
  permissions: Permissions[];
  bodyProps?: Record<Permissions, string[]>;
  checkObject?: object;
  /** If skipCheck function is provided and returns true, skip permission checking */
  skipCheck?: (req?: Request) => boolean;
  getRoleByName: (roleName: string, fieldsToSelect?: string | string[]) => Promise<IRole | null>;
}
type CheckAccessWithRequestCacheParams = Omit<CheckAccessParams, "bodyProps" | "checkObject" | "skipCheck">;
/**
* Core function to check if a user has one or more required permissions
* @param user - The user object
* @param permissionType - The type of permission to check
* @param permissions - The list of specific permissions to check
* @param bodyProps - An optional object where keys are permissions and values are arrays of properties to check
* @param checkObject - The object to check properties against
* @param skipCheck - An optional function that takes the checkObject and returns true to skip permission checking
* @returns Whether the user has the required permissions
*/
declare const checkAccess: ({
  req,
  user,
  permissionType,
  permissions,
  getRoleByName,
  bodyProps,
  checkObject,
  skipCheck
}: CheckAccessParams) => Promise<boolean>;
/**
* Checks simple role permissions using a per-request promise cache.
* Use this only for checks whose result is fully described by user, role, permission type, and permissions.
*/
declare const checkAccessWithRequestCache: (params: CheckAccessWithRequestCacheParams) => Promise<boolean>;
/**
* Middleware to check if a user has one or more required permissions, optionally based on `req.body` properties.
* @param permissionType - The type of permission to check.
* @param permissions - The list of specific permissions to check.
* @param bodyProps - An optional object where keys are permissions and values are arrays of `req.body` properties to check.
* @param skipCheck - An optional function that takes req.body and returns true to skip permission checking.
* @param getRoleByName - A function to get the role by name.
* @returns Express middleware function.
*/
declare const generateCheckAccess: ({
  permissionType,
  permissions,
  bodyProps,
  skipCheck,
  getRoleByName
}: {
  permissionType: PermissionTypes;
  permissions: Permissions[];
  bodyProps?: Record<Permissions, string[]>;
  skipCheck?: (req?: Request) => boolean;
  getRoleByName: (roleName: string, fieldsToSelect?: string | string[]) => Promise<IRole | null>;
}) => ((req: Request, res: Response$2, next: NextFunction) => Promise<unknown>);
//#endregion
//#region src/middleware/admin.d.ts
/**
* Middleware to check if authenticated user has admin role.
* Should be used AFTER authentication middleware (requireJwtAuth, requireLocalAuth, etc.)
*/
declare const requireAdmin: (req: ServerRequest, res: Response$2, next: NextFunction) => Response$2 | undefined;
//#endregion
//#region src/middleware/error.d.ts
/**
* Builds an error that `ErrorController` relays to the client verbatim. `isCustomError` matches only
* when both `statusCode` and `body` are present, so a plain `Error` falls through to a bare 500 and
* its message never leaves the server log. Use this wherever the caller needs to see the reason.
*/
declare const createCustomError: (statusCode: number, message: string) => CustomError;
declare const ErrorController: (err: Error | CustomError, req: Request, res: Response$2, next: NextFunction) => Response$2 | void;
//#endregion
//#region src/middleware/email.d.ts
declare function validateEmailLogin(req: Request, res: Response$2, next: NextFunction): Response$2 | void;
//#endregion
//#region src/middleware/notFound.d.ts
declare function apiNotFound(req: Request, res: Response$2): void;
//#endregion
//#region src/middleware/origin.d.ts
interface SameOriginGuardOptions {
  /** Origins allowed to submit from another origin, such as `DOMAIN_CLIENT` and `DOMAIN_SERVER`. */
  trustedOrigins: ReadonlyArray<string | undefined>;
}
/**
* Accepts a request only when the browser sent it from this application's own origin (or a
* trusted one). Login endpoints set session cookies, so they should only honor submissions made
* by the application's own pages.
*/
declare function createSameOriginGuard({
  trustedOrigins
}: SameOriginGuardOptions): RequestHandler;
//#endregion
//#region src/middleware/balance.d.ts
interface BalanceMiddlewareOptions {
  getAppConfig: (options?: {
    role?: string;
    userId?: string;
    tenantId?: string;
    refresh?: boolean;
  }) => Promise<AppConfig>;
  findBalanceByUser: (userId: string) => Promise<IBalance | null>;
  upsertBalanceFields: (userId: string, fields: IBalanceUpdate, insertOnly?: IBalanceUpdate) => Promise<IBalance | null>;
}
/**
* Build an object containing fields that need updating
* @param config - The balance configuration
* @param userRecord - The user's current balance record, if any
* @param userId - The user's ID
* @returns Fields that need updating
*/
declare function buildBalanceUpdateFields(config: BalanceConfig, userRecord: IBalance | null, userId: string): BalanceUpdateFields;
/**
* Factory function to create middleware that synchronizes user balance settings with current balance configuration.
* @param options - Options containing getBalanceConfig function and Balance model
* @returns Express middleware function
*/
declare function createSetBalanceConfig({
  getAppConfig,
  findBalanceByUser,
  upsertBalanceFields
}: BalanceMiddlewareOptions): (req: Request, res: Response$2, next: NextFunction) => Promise<void>;
//#endregion
//#region src/middleware/json.d.ts
/**
* Middleware to handle JSON parsing errors from express.json()
* Prevents user input from being reflected in error messages (XSS prevention)
*
* This middleware should be placed immediately after express.json() middleware.
*
* @param err - Error object from express.json()
* @param req - Express request object
* @param res - Express response object
* @param next - Express next function
*
* @example
* app.use(express.json({ limit: '3mb' }));
* app.use(handleJsonParseError);
*/
declare function handleJsonParseError(err: Error & {
  status?: number;
  body?: unknown;
}, req: Request, res: Response$2, next: NextFunction): void;
//#endregion
//#region src/middleware/auth.d.ts
type AuthLogValue = string | number | boolean | readonly string[];
type AuthLogHeaderValue = string | string[] | undefined;
type AuthRoutePath = string | RegExp | readonly (string | RegExp)[];
type AuthLogRequest = {
  headers?: Record<string, AuthLogHeaderValue>;
  method?: string;
  path?: string;
  originalUrl?: string;
  url?: string;
  baseUrl?: string;
  route?: {
    path?: AuthRoutePath;
  };
  id?: string;
  requestId?: string;
};
type AuthLogState = {
  tokenProvider?: string | null;
  tokenSource?: string | null;
  openidReuseEnabled: boolean;
  openidJwtAvailable: boolean;
  hasOpenIdReuseUserId: boolean;
};
type RequestLogContext = {
  request_id?: string;
  request_method?: string;
  request_path?: string;
};
type AuthLogContext = RequestLogContext & Record<string, AuthLogValue>;
type AuthFailureReasonCategory = "expired_jwt" | "malformed_jwt" | "principal_mismatch" | "missing_or_unrecognized_token" | "authentication_error";
declare function buildSafeRequestLogContext(req: AuthLogRequest): RequestLogContext;
declare function getAuthFailureReason(err: unknown, info: unknown, fallback?: string): string;
declare function getAuthFailureErrorName(err: unknown, info: unknown): string | undefined;
declare function getAuthFailureReasonCategory(err: unknown, info: unknown): AuthFailureReasonCategory;
declare function buildSafeAuthLogContext(req: AuthLogRequest, authState: AuthLogState, extra?: object): AuthLogContext;
/**
* @deprecated Pass `{ message, ...context }` to the logger so structured fields
* survive JSON message truncation. Retained for package API compatibility only.
*/
declare function formatAuthLogMessage(message: string, context: AuthLogContext): string;
declare function buildTenantIsolationErrorLogContext(req: AuthLogRequest, err: unknown): AuthLogContext | undefined;
//#endregion
//#region src/middleware/tenant.d.ts
type ContextUser = {
  tenantId?: string;
  id?: string;
  _id?: {
    toString: () => string;
  };
} | null;
type ContextRequest = {
  headers: ServerRequest["headers"];
  tenantId?: string;
  user?: ContextUser;
  id?: string;
  requestId?: string;
  method?: string;
  path?: string;
  originalUrl?: string;
  url?: string;
  baseUrl?: string;
  route?: {
    path?: string | RegExp | readonly (string | RegExp)[];
  };
};
/**
* Establishes safe, request-level correlation before authentication. It carries
* no tenant or user identity, so strict tenant isolation remains fail-closed.
*/
declare function requestContextMiddleware(req: ContextRequest, _res: Response$2, next: NextFunction): void;
/**
* Express middleware that propagates the authenticated user's `tenantId` into
* the AsyncLocalStorage context used by the Mongoose tenant-isolation plugin
* and request-scoped logging.
*
* **Placement**: Chained automatically by `requireJwtAuth` after successful
* passport authentication (req.user is populated). Must NOT be registered at
* global `app.use()` scope — `req.user` is undefined at that stage.
*
* Behaviour:
* - Authenticated request with context → wraps downstream in `tenantStorage.run(context)`
* - Authenticated request **without** `tenantId`:
*   - Strict mode (`TENANT_ISOLATION_STRICT=true`) → responds 403
*   - Non-strict (default) → passes through with user/request context only
* - Unauthenticated request → propagates request context when available
*/
declare function tenantContextMiddleware(req: ServerRequest, res: Response$2, next: NextFunction): void;
type RequestTenantSource = {
  tenantId?: string;
  user?: {
    tenantId?: string;
  } | null;
};
declare function resolveRequestTenantId(req: RequestTenantSource): string | undefined;
/**
* Re-enters tenant ALS from the server-resolved request tenant.
*
* Use this after middleware that may cross async stream boundaries (for example
* multipart parsers) and before tenant-isolated model calls. The tenant source
* is restricted to authenticated/resolved request fields, never form data.
*/
declare function restoreTenantContextFromReq(req: ServerRequest, res: Response$2, next: NextFunction): void | Promise<void>;
//#endregion
//#region src/middleware/preAuthTenant.d.ts
declare function preAuthTenantMiddleware(req: Request, res: Response$2, next: NextFunction): void;
//#endregion
//#region src/middleware/concurrency.d.ts
interface PendingRequestResult {
  allowed: boolean;
  pendingRequests: number;
  limit: number;
}
interface ViolationInfo {
  type: string;
  limit: number;
  pendingRequests: number;
  score: number;
}
/**
* Check if a user can make a new concurrent request and increment the counter if allowed.
* This is designed for resumable streams where the HTTP response lifecycle doesn't match
* the actual request processing lifecycle.
*
* When Redis is available, uses atomic INCR to prevent race conditions.
* Falls back to non-atomic get/set for in-memory cache.
*
* @param userId - The user's ID
* @returns Object with `allowed` (boolean), `pendingRequests` (current count), and `limit`
*/
declare function checkAndIncrementPendingRequest(userId: string): Promise<PendingRequestResult>;
/**
* Decrement the pending request counter for a user.
* Should be called when a generation job completes, errors, or is aborted.
*
* This function handles errors internally and will never throw - it's a cleanup
* operation that should not interrupt the main flow if cache operations fail.
*
* When Redis is available, uses atomic DECR to prevent race conditions.
* Falls back to non-atomic get/set for in-memory cache.
*
* @param userId - The user's ID
*/
declare function decrementPendingRequest(userId: string): Promise<void>;
/**
* Get violation info for logging purposes when a user exceeds the concurrent request limit.
*/
declare function getViolationInfo(pendingRequests: number, limit: number): ViolationInfo;
/**
* Check if concurrent message limiting is enabled.
*/
declare function isConcurrentLimitEnabled(): boolean;
//#endregion
//#region src/middleware/checkBalance.d.ts
interface TxData {
  user: string;
  model?: string;
  endpoint?: string;
  valueKey?: string;
  tokenType?: string;
  amount: number;
  endpointTokenConfig?: unknown;
  generations?: unknown[];
}
interface CheckBalanceDeps {
  getMultiplier: (params: Record<string, unknown>) => number;
  reserveBalance: (request: BalanceReservationRequest) => Promise<BalanceReservationResult | null>;
  renewBalanceReservation: (params: BalanceReservationRenewal) => Promise<void>;
  releaseBalanceReservation: (params: BalanceReservationRelease) => Promise<void>;
  logViolation: (req: unknown, res: unknown, type: string, errorMessage: Record<string, unknown>, score: number) => Promise<void>;
  /** Balance config for lazy initialization when no record exists, and the reservation TTL */
  balanceConfig?: BalanceConfig;
}
/**
* Credits held for an admitted request until its usage has been recorded. The reservation is
* renewed every half TTL until released, so only a reservation whose process stopped expires.
*/
interface BalanceReservation {
  /** Idempotent; stops renewal. A failed release is logged and left to expire. */
  release: () => Promise<void>;
}
/** The balance reservations admitted during one turn. */
interface BalanceReservations {
  /** Tracks an admission that may still be pending; returns the same promise. */
  track: <T extends BalanceReservation | undefined>(admission: Promise<T>) => Promise<T>;
  /** Keeps the turn's reservations held until `work` settles, such as a run continuing in the background. */
  holdUntil: (work: Promise<unknown>) => void;
  /**
  * Releases every tracked reservation, first waiting for admissions still pending so a
  * reservation that settles after its turn failed is released too, and for work passed to
  * `holdUntil`. Failed admissions hold nothing.
  */
  release: () => Promise<void>;
}
declare function createBalanceReservations(): BalanceReservations;
/** Runs one turn and releases whatever balance reservations it admitted once it settles. */
declare function withBalanceReservations<T>(run: (reservations: BalanceReservations) => Promise<T>): Promise<T>;
/**
* Admits a request against the user's balance and holds its token cost until the returned
* reservation is released, so concurrent requests are admitted only against credits that no
* other in-flight request holds. A missing balance record is created from `startBalance`.
* Throws with the balance info if the credits are insufficient.
*/
declare function checkBalance({
  req,
  res,
  txData
}: {
  req: ServerRequest;
  res: Response$2;
  txData: TxData;
}, deps: CheckBalanceDeps): Promise<BalanceReservation>;
//#endregion
//#region src/middleware/remoteAgentAuth.d.ts
interface RemoteAgentAuthDeps {
  apiKeyMiddleware: RequestHandler;
  findUser: UserMethods["findUser"];
  getRolesByNames: RoleMethods["findRolesByNames"];
  updateUser: UserMethods["updateUser"];
  isPrincipalActive: (userId: string) => Promise<boolean>;
  getAppConfig: (options?: GetAppConfigOptions) => Promise<AppConfig>;
}
declare function clearRemoteAgentAuthCache(): void;
/**
* Factory for Remote Agent API auth middleware.
*
* Validates Bearer tokens against configured OIDC issuer via JWKS,
* falling back to API key auth when enabled. Stateless — no session dependency.
*
* ```yaml
* endpoints:
*   agents:
*     remoteApi:
*       auth:
*         apiKey:
*           enabled: false
*         oidc:
*           enabled: true
*           issuer: <issuer>
*           jwksUri: <jwksUri>
*           audience: <audience>
*           scope: <scope>
* ```
*/
declare function createRemoteAgentAuth({
  apiKeyMiddleware,
  findUser,
  getRolesByNames,
  updateUser,
  isPrincipalActive,
  getAppConfig
}: RemoteAgentAuthDeps): RequestHandler;
//#endregion
//#region src/middleware/share.d.ts
type ShareResourcePermissions = Partial<Record<Permissions, boolean>>;
interface SharePermissionCache {
  cacheKey: string;
  resourcePerms: ShareResourcePermissions;
}
type ShareRequest = ServerRequest & {
  params: {
    resourceType?: string;
  };
  body: RequestBody & {
    public?: boolean;
    updated?: Array<{
      type?: string;
    }>;
  };
  sharePermissionContext?: SharePermissionCache;
};
interface SharePolicyDeps {
  getRoleByName: (roleName: string, fieldsToSelect?: string | string[]) => Promise<IRole | null>;
  hasCapability: HasCapabilityFn;
}
type ShareMiddleware = (req: ShareRequest, res: Response$2, next: NextFunction) => Promise<Response$2 | void>;
declare function createSharePolicyMiddleware({
  getRoleByName,
  hasCapability
}: SharePolicyDeps): {
  checkShareAccess: ShareMiddleware;
  checkSharePublicAccess: ShareMiddleware;
};
//#endregion
//#region src/protection/files.d.ts
interface UninspectableFileBlockResponse {
  readonly error: "content_filter_uninspectable";
  readonly message: string;
  readonly source: "file";
  readonly field: FileFilterField;
}
interface ContentFilterInputTooLargeResponse {
  readonly error: "content_filter_input_too_large";
  readonly message: string;
  readonly source: "file";
  readonly field: "content" | "extracted_text";
}
interface CanonicalFileInspectionFile {
  readonly file_id?: string;
  readonly filename?: string;
  readonly filepath?: string;
  readonly uri?: string;
  readonly url?: string;
  readonly preview?: string;
  readonly type?: string;
  readonly source?: string;
  /** Unified uploads persist their extracted text alongside the backing storage
  *  source, so delivery path carries the provenance `source: 'text'` used to. */
  readonly llmDeliveryPath?: string | null;
  readonly content?: string | null;
  readonly extractedText?: string | null;
  readonly text?: string | null;
  readonly transcript?: string | null;
}
interface CanonicalFileInspectionCoverage {
  readonly content?: string;
  readonly extractedText?: string;
  readonly transcript?: string;
  /** The canonical transcript came from the persisted `text` fallback. */
  readonly textProvidesTranscript: boolean;
  /** `null` means the MIME is absent or malformed, so strict transcript
  * policy must fail closed instead of assuming the file is non-audio. */
  readonly transcriptApplicable: boolean | null;
}
interface CanonicalFileInspectionUser {
  readonly id?: string;
  readonly tenantId?: string | null;
}
type GetCanonicalFilesForInspection = (filter: {
  file_id: {
    $in: string[];
  };
  user: string;
  tenantId?: string | null;
}, sort: object, select: object) => Promise<CanonicalFileInspectionFile[] | null | undefined>;
interface CanonicalFileReferenceInspectionInput<T> {
  readonly messageCount?: number;
  readonly onTraversalFailure?: LocatorTraversalReporter;
  readonly filters?: FiltersConfig;
  readonly input: T;
  readonly user?: CanonicalFileInspectionUser;
  /**
  * Server-derived runtime rows whose extraction fields belong to the exact
  * model-bound generation. Never populate this from request or resumable-job
  * metadata: a matching file_id alone is not an extraction attestation.
  */
  readonly trustedLiveFiles?: readonly CanonicalFileInspectionFile[];
  readonly getFiles: GetCanonicalFilesForInspection;
}
interface CanonicalFileReferenceInspection<T> {
  readonly sanitizedInput: T;
  readonly hydratedFiles: CanonicalFileInspectionFile[];
  readonly hydratedFilters?: FiltersConfig;
}
declare function getTranscriptApplicability(mimeType: unknown): boolean | null;
declare function isTextualFileMimeType(mimeType: unknown): boolean;
/**
* A file policy needs canonical file hydration only when it can inspect text
* patterns or fail closed for one of the derived content surfaces.
*/
declare function hasActiveFilePolicy(filters: FiltersConfig | undefined): boolean;
/** Whether a file policy can enforce on at least one requested field. */
declare function hasActiveFileFieldPolicy(filters: FiltersConfig | undefined, candidates: readonly FileFilterField[]): boolean;
declare function isFileFilterFieldEnabled(filters: FiltersConfig | undefined, field: FileFilterField): boolean;
declare function getBlockedUninspectableFileField(filters: FiltersConfig | undefined, fields: readonly FileFilterField[]): FileFilterField | null;
/** Whether a context upload has a downstream STT step that can inspect its transcript. */
declare function canInspectUploadTranscriptAfterProcessing(input: {
  readonly endpoint?: string;
  readonly toolResource?: string;
  readonly mimeType?: string;
  readonly sttSupported: boolean;
}): boolean;
/** Resolves upload-time transcript fail-close after accounting for downstream STT. */
declare function getBlockedUploadTranscriptField(input: {
  readonly filters?: FiltersConfig;
  readonly endpoint?: string;
  readonly toolResource?: string;
  readonly mimeType?: string;
  readonly sttSupported: boolean;
}): FileFilterField | null;
declare const UPLOAD_EXTRACTED_TEXT_PLANS: {
  readonly configuredOCR: "configured_ocr";
  readonly configuredRAG: "configured_rag";
  readonly documentParser: "document_parser";
};
type UploadExtractedTextPlan = (typeof UPLOAD_EXTRACTED_TEXT_PLANS)[keyof typeof UPLOAD_EXTRACTED_TEXT_PLANS];
interface UploadExtractedTextPlanInput {
  readonly endpoint?: string;
  readonly toolResource?: string;
  readonly mimeType: string;
  readonly fileConfig: FileConfig;
  readonly ocrConfigured: boolean;
  readonly ragConfigured: boolean;
}
/**
* Selects only extraction paths that produce meaningful derived text without
* falling back to decoding arbitrary binary bytes as UTF-8.
*/
declare function getUploadExtractedTextPlan(input: UploadExtractedTextPlanInput): UploadExtractedTextPlan | null;
/** Whether a context upload has a downstream extraction step that can inspect derived text. */
declare function canInspectUploadExtractedTextAfterProcessing(input: UploadExtractedTextPlanInput): boolean;
/**
* Skill bundle text cannot be safely skipped when the skill policy selects
* `file_text`, even if the independent file policy is absent or explicitly in
* compatibility mode. The returned file field preserves the existing
* uninspectable response contract used by file upload and runtime paths.
*/
declare function getBlockedUninspectableSkillFileField(filters: FiltersConfig | undefined, fileFields?: readonly FileFilterField[]): FileFilterField | null;
declare function contentFilterUninspectableResponse(field: FileFilterField): UninspectableFileBlockResponse;
declare class UninspectableFileError extends Error {
  readonly code = "content_filter_uninspectable";
  readonly statusCode = 400;
  readonly body: UninspectableFileBlockResponse;
  constructor(field: FileFilterField);
}
declare class ContentFilterInputTooLargeError extends Error {
  readonly code = "content_filter_input_too_large";
  readonly statusCode = 413;
  readonly body: ContentFilterInputTooLargeResponse;
  constructor(field: ContentFilterInputTooLargeResponse["field"]);
}
/**
* Returns the first configured file field whose submitted content is opaque.
* Detection is skipped unless fail-close is enabled.
*/
declare function getBlockedOpaqueFileField(filters: FiltersConfig | undefined, input: unknown): FileFilterField | null;
declare function getCanonicalFileInspectionCoverage(file: CanonicalFileInspectionFile): CanonicalFileInspectionCoverage;
declare function assertHydratedFileInspectable(filters: FiltersConfig | undefined, file: CanonicalFileInspectionFile): void;
declare function omitResolvedCanonicalFileLocators<T>(input: T, resolvedFilesById: ReadonlyMap<string, CanonicalFileInspectionFile>, context?: {
  readonly messageCount?: number;
  readonly onTraversalFailure?: LocatorTraversalReporter;
}): T;
declare function allowHydratedFileReferences(filters: FiltersConfig | undefined): FiltersConfig | undefined;
/**
* Resolves durable TerraMind file references against the authenticated owner
* before fail-close checks run. Only locators backed by an owner-scoped file
* row are removed from the inspection copy; unresolved IDs and unrelated
* opaque payloads remain visible to `getBlockedOpaqueFileField`.
*/
declare function resolveCanonicalFileReferences<T>(input: CanonicalFileReferenceInspectionInput<T>): Promise<CanonicalFileReferenceInspection<T>>;
/** Shares one owner-scoped lookup across independently bounded inspection units. */
declare function resolveCanonicalFileReferenceUnits<T>(input: CanonicalFileReferenceInspectionInput<readonly T[]>): Promise<CanonicalFileReferenceInspection<readonly T[]>>;
//#endregion
//#region src/middleware/contentFilter.d.ts
interface ContentFilterBlockResponse {
  readonly error: "content_filter_block";
  readonly message: string;
  readonly source: ProtectionFinding["source"];
  readonly field: ProtectionFinding["field"];
}
declare function contentFilterBlockResponse(finding: ProtectionFinding): ContentFilterBlockResponse;
declare function contentFilterModelBoundBlockResponse(finding: Pick<ProtectionFinding, "source" | "field">): ContentFilterBlockResponse;
declare class ContentFilterError extends Error {
  readonly code = "content_filter_block";
  readonly statusCode = 400;
  readonly body: ContentFilterBlockResponse;
  constructor(finding: ProtectionFinding);
}
type ContentPolicyError = ContentFilterError | ContentFilterInputTooLargeError | ContentTraversalLimitError | UninspectableFileError;
declare function getContentFilterError(error: unknown): ContentPolicyError | null;
declare function isContentFilterError(error: unknown): error is ContentFilterError | ContentTraversalLimitError | UninspectableFileError;
interface CreateContentFilterOptions {
  readonly messageCount?: number;
  readonly onTraversalFailure?: LocatorTraversalReporter;
  getFilters: (req: Request) => FiltersConfig | undefined;
  getLegacyPii?: (req: Request) => MessageFilterPiiConfig | undefined;
  getMessageRoles?: (req: Request) => readonly (string | undefined)[];
  getOpaqueFileInput?: (req: Request) => unknown;
  getFiles?: GetCanonicalFilesForInspection;
  extract: (req: Request) => Iterable<TextContentFragment>;
}
declare function createContentFilter(options: CreateContentFilterOptions): RequestHandler;
//#endregion
//#region src/protection/adapters/submissions.d.ts
interface AgentEdgeInput {
  readonly description?: string;
  readonly prompt?: string;
  readonly promptKey?: string;
}
interface SupportContactInput {
  readonly name?: string;
  readonly email?: string;
}
interface ModelParameterContentInput {
  readonly stop?: unknown;
  readonly additionalModelRequestFields?: unknown;
  readonly additional_model_request_fields?: unknown;
  readonly response_format?: unknown;
  readonly responseFormat?: unknown;
  readonly metadata?: unknown;
  readonly model_parameters?: unknown;
  readonly options?: unknown;
}
interface AgentContentInput extends ModelParameterContentInput {
  readonly name?: string | null;
  readonly category?: string | null;
  readonly description?: string | null;
  readonly instructions?: string | null;
  readonly additional_instructions?: string | null;
  readonly conversation_starters?: readonly string[];
  readonly edges?: readonly (AgentEdgeInput | null | undefined)[];
  readonly artifacts?: string;
  readonly support_contact?: SupportContactInput | null;
  /** Final definitions exposed to the model after agent/tool initialization. */
  readonly toolDefinitions?: readonly (FunctionToolContentInput | null | undefined)[];
}
interface FunctionDefinitionContentInput {
  readonly name?: string;
  readonly description?: string;
  readonly parameters?: unknown;
}
interface FunctionToolContentInput extends FunctionDefinitionContentInput {
  readonly type?: string;
  readonly function?: FunctionDefinitionContentInput;
}
interface AssistantContentInput extends AgentContentInput {
  readonly tools?: readonly (string | FunctionToolContentInput | null | undefined)[];
}
interface ActionAuthContentInput {
  readonly authorization_type?: unknown;
  readonly custom_auth_header?: unknown;
  readonly authorization_content_type?: unknown;
  readonly authorization_url?: unknown;
  readonly client_url?: unknown;
  readonly scope?: unknown;
  readonly token_exchange_method?: unknown;
}
interface ActionMetadataContentInput extends ActionAuthContentInput {
  readonly raw_spec?: unknown;
  readonly domain?: unknown;
  readonly privacy_policy_url?: unknown;
  readonly api_key?: unknown;
  readonly oauth_client_id?: unknown;
  readonly oauth_client_secret?: unknown;
  readonly auth?: ActionAuthContentInput | null;
}
interface AssistantActionContentInput {
  readonly functions?: readonly (FunctionToolContentInput | null | undefined)[];
  readonly metadata?: ActionMetadataContentInput | null;
}
interface PromptRecordInput {
  readonly prompt?: string;
  readonly name?: string;
  readonly description?: string;
  readonly oneliner?: string;
  readonly category?: string;
  readonly command?: string;
}
interface PromptContentInput extends Omit<PromptRecordInput, "prompt"> {
  readonly prompt?: string | PromptRecordInput;
  readonly group?: PromptRecordInput;
}
interface PresetExampleContentInput {
  readonly content?: string;
}
interface PresetExampleInput {
  readonly input?: string | PresetExampleContentInput;
  readonly output?: string | PresetExampleContentInput;
}
interface PresetContentInput extends PromptRecordInput, ModelParameterContentInput {
  readonly title?: string;
  readonly promptPrefix?: string | null;
  readonly system?: string;
  readonly context?: string;
  readonly instructions?: string | null;
  readonly additional_instructions?: string | null;
  readonly greeting?: string;
  readonly examples?: readonly (PresetExampleInput | null | undefined)[];
}
interface SkillFileContentInput {
  readonly name?: string;
  readonly filename?: string;
  readonly text?: string;
  readonly content?: string;
}
interface SkillContentInput {
  readonly name?: string;
  readonly displayTitle?: string;
  readonly description?: string;
  readonly category?: string;
  readonly body?: string;
  readonly instructions?: string;
  readonly importedText?: string;
  readonly frontmatter?: Readonly<Record<string, unknown>>;
  readonly files?: readonly (SkillFileContentInput | null | undefined)[];
}
interface MemoryContentInput {
  readonly key?: string;
  readonly value?: string;
  readonly summary?: string;
}
interface FileContentInput {
  readonly file_id?: string;
  readonly name?: string;
  readonly filename?: string;
  readonly originalname?: string;
  readonly type?: string;
  readonly source?: string;
  readonly content?: string | null;
  readonly extractedText?: string | null;
  readonly text?: string | null;
  readonly transcript?: string | null;
  readonly uri?: string;
  readonly filepath?: string;
  readonly url?: string;
  readonly preview?: string;
}
interface FeedbackContentInput {
  readonly feedback?: {
    readonly text?: string;
  } | null;
  readonly text?: string;
}
interface StoredMessagePartInput {
  readonly type?: string;
  readonly text?: string | {
    readonly value?: string;
  };
  readonly think?: string | {
    readonly value?: string;
  };
  readonly original?: string;
  readonly updated?: string;
  readonly steer?: string;
  readonly quotes?: readonly (string | {
    readonly text?: string;
  } | null | undefined)[];
  readonly error?: string;
  readonly image_url?: string | {
    readonly url?: string;
  };
  readonly video_url?: {
    readonly url?: string;
  };
  readonly input_audio?: {
    readonly data?: string;
    readonly format?: string;
  };
  readonly image_file?: {
    readonly file_id?: string;
    readonly filename?: string;
  };
  readonly file?: StoredFileReferenceInput;
  readonly files?: readonly (StoredFileReferenceInput | null | undefined)[];
  readonly content?: readonly ({
    readonly text?: string | {
      readonly value?: string;
    };
    readonly [key: string]: unknown;
  } | null | undefined)[];
  readonly tool_call?: StoredToolCallInput;
  readonly [key: string]: unknown;
}
interface StoredToolCallInput {
  readonly name?: unknown;
  readonly args?: unknown;
  readonly arguments?: unknown;
  readonly output?: unknown;
  readonly function?: {
    readonly name?: unknown;
    readonly arguments?: unknown;
    readonly output?: unknown;
  };
  readonly code_interpreter?: {
    readonly input?: unknown;
    readonly outputs?: unknown;
  };
}
interface StoredTopLevelToolCallInput {
  readonly name?: unknown;
  readonly arguments?: unknown;
  readonly output?: unknown;
  readonly function?: {
    readonly name?: unknown;
    readonly arguments?: unknown;
    readonly output?: unknown;
  };
  readonly code_interpreter?: {
    readonly input?: unknown;
    readonly outputs?: unknown;
  };
}
interface StoredFileReferenceInput {
  readonly file_id?: string;
  readonly name?: string;
  readonly filename?: string;
  readonly originalname?: string;
  readonly filepath?: string;
  readonly uri?: string;
  readonly url?: string;
  readonly preview?: string;
}
interface StoredMessageContentInput extends FeedbackContentInput {
  readonly role?: string;
  readonly name?: string;
  readonly sender?: string;
  readonly text?: string;
  readonly summary?: string;
  readonly quotes?: readonly (string | {
    readonly text?: string;
  } | null | undefined)[];
  readonly content?: readonly (StoredMessagePartInput | null | undefined)[];
  readonly tool_calls?: readonly (StoredTopLevelToolCallInput | null | undefined)[];
  readonly files?: readonly (StoredFileReferenceInput | null | undefined)[];
  readonly attachments?: readonly (StoredFileReferenceInput | null | undefined)[];
  readonly original?: string;
  readonly updated?: string;
}
interface ToolArgumentContentInput {
  readonly name?: unknown;
  readonly arguments?: unknown;
  readonly output?: unknown;
}
interface ImportedConversationMetadataInput extends ModelParameterContentInput {
  readonly title?: string;
  readonly promptPrefix?: string | null;
  readonly system?: string;
  readonly context?: string;
  readonly greeting?: string;
  readonly examples?: PresetContentInput["examples"];
  readonly instructions?: string | null;
  readonly additional_instructions?: string | null;
  readonly artifacts?: string;
  readonly presetOverride?: PresetContentInput | null;
}
interface ConversationImportContentInput {
  readonly conversations: readonly (ImportedConversationMetadataInput | null | undefined)[];
  readonly messages: readonly (StoredMessageContentInput | null | undefined)[];
}
/**
* Extracts provider-bound text without treating every field in a conversation
* or agent object as a model parameter. Registered fields may appear directly,
* under a persisted `model_parameters` object, or in an import's `options`
* object.
*/
declare function extractModelParameterContent(input: ModelParameterContentInput | null | undefined, path?: JsonPointer | ""): readonly TextContentFragment[];
declare function visitBoundedSubmittedArray<Value>(candidate: unknown, budget: VisitNestedStringsBudget, visit: (value: Value | null | undefined, index: number) => void): boolean;
declare function extractAgentContent(input: AgentContentInput | null | undefined): readonly TextContentFragment[];
declare function extractAssistantContent(input: AssistantContentInput | null | undefined): readonly TextContentFragment[];
declare function extractAssistantActionContent(input: AssistantActionContentInput | null | undefined): readonly TextContentFragment[];
declare function extractPromptContent(input: PromptContentInput | null | undefined): readonly TextContentFragment[];
declare function extractPresetPromptContent(input: PresetContentInput | null | undefined): readonly TextContentFragment[];
declare function extractPresetContent(input: PresetContentInput | null | undefined): readonly TextContentFragment[];
declare function extractSkillContent(input: SkillContentInput | null | undefined): readonly TextContentFragment[];
declare function extractMemoryContent(input: MemoryContentInput | null | undefined): readonly TextContentFragment[];
declare function extractFileContent(input: FileContentInput | null | undefined): readonly TextContentFragment[];
declare function extractFeedbackContent(input: FeedbackContentInput | null | undefined): readonly TextContentFragment[];
declare function extractConversationTitleContent(input: string | {
  readonly title?: string;
} | null | undefined): readonly TextContentFragment[];
declare function extractStoredMessageContent(input: StoredMessageContentInput | null | undefined, aggregateBudget?: VisitNestedStringsBudget): readonly TextContentFragment[];
declare function extractConversationImportContent(input: ConversationImportContentInput): Generator<TextContentFragment, void, undefined>;
declare function extractToolArgumentContent(input: ToolArgumentContentInput | null | undefined, budget?: VisitNestedStringsBudget): readonly TextContentFragment[];
//#endregion
//#region src/protection/adapters/messages.d.ts
interface ExternalMessagePart {
  readonly type?: string;
  readonly text?: string;
  readonly data?: string;
  readonly url?: string;
  readonly source_type?: string;
  readonly image_url?: string | {
    readonly url?: string;
  };
  readonly file_id?: string;
  readonly file_data?: string;
  readonly filename?: string;
  readonly source?: {
    readonly type?: string;
    readonly data?: string;
    readonly url?: string;
    readonly [key: string]: unknown;
  };
  readonly input_audio?: {
    readonly data?: string;
    readonly format?: string;
  };
  readonly file?: {
    readonly file_id?: string;
    readonly file_data?: string;
    readonly filename?: string;
  };
  readonly [key: string]: unknown;
}
interface ExternalToolCall {
  readonly function?: {
    readonly name?: string;
    readonly arguments?: string;
  };
}
interface ExternalChatMessage {
  readonly role?: string;
  readonly name?: string;
  readonly content?: string | readonly (ExternalMessagePart | null | undefined)[];
  readonly tool_calls?: readonly (ExternalToolCall | null | undefined)[];
}
declare const PREPARED_EXTERNAL_MESSAGES: unique symbol;
interface PreparedExternalMessages {
  readonly messages: readonly (ExternalChatMessage | null | undefined)[];
  readonly roles: readonly (string | undefined)[];
  readonly traversalError: ContentTraversalLimitError | null;
  readonly traversalBudget: VisitNestedStringsBudget;
  readonly [PREPARED_EXTERNAL_MESSAGES]: true;
}
/**
* Captures the bounded message graph once so file and text inspection observe identical values.
* The returned graph is frozen and can be passed directly to extractMessageContent.
*/
declare function snapshotExternalMessages(submittedMessages: readonly (ExternalChatMessage | null | undefined)[], traversalBudget?: VisitNestedStringsBudget): PreparedExternalMessages;
declare function extractMessageContent(input: readonly (ExternalChatMessage | null | undefined)[] | PreparedExternalMessages, sharedTraversalBudget?: VisitNestedStringsBudget): Generator<TextContentFragment, void, undefined>;
//#endregion
//#region src/middleware/modelBoundContent.d.ts
type ModelBoundProviderAttribution = "user" | "model" | "tool" | "synthetic";
interface ModelBoundProviderProvenancePart {
  readonly attribution: ModelBoundProviderAttribution;
  readonly sourceMessageId?: string;
  readonly sourceContentPartIndices?: readonly number[];
}
interface ModelBoundProviderProvenance {
  readonly version: 1;
  readonly parts: readonly ModelBoundProviderProvenancePart[];
}
type ModelBoundProviderMessage = ExternalChatMessage & Omit<StoredMessageContentInput, "content"> & {
  readonly id?: string;
  readonly messageId?: string;
  readonly text?: string;
  readonly additional_kwargs?: {
    readonly injected?: boolean;
    readonly isMeta?: boolean;
    readonly source?: string;
    readonly sourceMessageId?: string;
    readonly sourceMessageIds?: readonly string[];
    readonly provenance?: ModelBoundProviderProvenance;
  };
  readonly _getType?: () => string;
};
type ModelBoundMessage = ExternalChatMessage & {
  readonly _getType?: () => string;
};
type StoredModelBoundMessage = StoredMessageContentInput & {
  readonly id?: string;
  readonly messageId?: string;
  readonly isCreatedByUser?: boolean;
  readonly isUserSubmitted?: boolean;
  readonly userSubmittedPaths?: readonly string[];
  readonly userSubmittedMessageFieldPaths?: readonly UserSubmittedMessageFieldPath[];
};
type ModelBoundCanonicalFile = FileContentInput & CanonicalFileInspectionFile;
interface ModelBoundProviderContentInput {
  readonly onTraversalFailure?: LocatorTraversalReporter;
  readonly filters?: FiltersConfig;
  readonly legacyPii?: MessageFilterPiiConfig;
  readonly providerMessages: readonly ModelBoundProviderMessage[];
  readonly storedMessages?: readonly (StoredModelBoundMessage | null | undefined)[];
  readonly resolvedFiles?: readonly (ModelBoundCanonicalFile | null | undefined)[];
  /** Canonical files actually materialized for each persisted source row. */
  readonly fileIdsBySourceMessageId?: ReadonlyMap<string, readonly string[]>;
  /** A bounded upstream source/file projection was incomplete. */
  readonly sourceFileProjectionOverflowed?: boolean;
}
type ModelBoundFileReference = {
  readonly file_id?: string;
} | null | undefined;
interface ModelBoundSourceFileInput {
  readonly messageFilesBySourceMessageId?: Readonly<Record<string, readonly ModelBoundFileReference[] | null | undefined>>;
  readonly sourceMessages?: readonly ({
    readonly messageId?: string;
    readonly id?: string;
  } | null | undefined)[];
  readonly steerFileIdsBySourceMessageId?: ReadonlyMap<string, readonly string[] | ReadonlySet<string>>;
  readonly replayHistoricalFiles: boolean;
  readonly historicalFiles?: readonly (ModelBoundCanonicalFile | null | undefined)[] | ReadonlyMap<string, ModelBoundCanonicalFile | null | undefined>;
  readonly processedCurrentFiles?: readonly (ModelBoundCanonicalFile | null | undefined)[];
  readonly canonicalCurrentFiles?: readonly (ModelBoundCanonicalFile | null | undefined)[];
  readonly initiallyOverflowed?: boolean;
}
interface ModelBoundSourceFileProjection {
  readonly fileIdsBySourceMessageId: ReadonlyMap<string, readonly string[]>;
  readonly resolvedFiles: readonly ModelBoundCanonicalFile[];
  readonly overflowed: boolean;
}
interface ModelBoundHistoricalFileIdState {
  readonly fileIds: string[];
  readonly overflowed: boolean;
}
/** Collects every provider-supported persisted file locator for owner hydration. */
declare function collectModelBoundHistoricalFileIdState(messages: readonly (StoredMessageContentInput | null | undefined)[]): ModelBoundHistoricalFileIdState;
/** Backward-compatible ID-only view for callers that do not enforce model-bound content. */
declare function collectModelBoundHistoricalFileIds(messages: readonly (StoredMessageContentInput | null | undefined)[]): string[];
/**
* Builds the security-sensitive provider source/file association in typed
* backend code. Legacy clients supply runtime state only; this helper owns
* source normalization, deduplication, replay selection, and canonical-row
* precedence for the final model-bound guard.
*/
declare function projectModelBoundSourceFiles(input: ModelBoundSourceFileInput): ModelBoundSourceFileProjection;
interface ModelBoundChatModelCallback {
  readonly name: "librechat-model-bound-content-filter";
  readonly raiseError: true;
  readonly awaitHandlers: true;
  readonly handleChatModelStart: (llm: object | undefined, messageBatches: readonly (readonly ModelBoundProviderMessage[])[]) => void;
}
interface InitialModelBoundAdmissionCallback {
  readonly name: "librechat-initial-model-bound-admission";
  readonly raiseError: true;
  readonly awaitHandlers: true;
  readonly handleChatModelStart: (llm: object | undefined, messageBatches: readonly (readonly ModelBoundProviderMessage[])[], runId: string, parentRunId?: string, extraParams?: Record<string, unknown>, tags?: string[], metadata?: Record<string, unknown>) => void;
  readonly handleChainStart: (chain: object | undefined, inputs: unknown, runId: string, parentRunId?: string, tags?: string[], metadata?: Record<string, unknown>, runType?: string, runName?: string, extra?: Record<string, unknown>) => void;
  readonly handleLLMEnd: (output: unknown, runId: string) => void;
  readonly handleLLMError: (error: unknown, runId: string) => void;
  readonly handleChainEnd: (outputs: unknown, runId: string) => void;
  readonly handleChainError: (error: unknown, runId: string) => void;
}
interface InitialModelBoundAdmission {
  readonly agentIds: readonly string[];
  readonly isActive: () => boolean;
  readonly onAllowed: () => void;
}
interface ModelBoundContentInput {
  readonly onTraversalFailure?: LocatorTraversalReporter;
  readonly filters?: FiltersConfig;
  readonly legacyPii?: MessageFilterPiiConfig;
  /** Fresh API input: every role is caller-submitted. */
  readonly submittedMessages?: readonly ModelBoundMessage[];
  /** Persisted chat history: user rows plus structured tool fragments are re-inspected. */
  readonly storedMessages?: readonly StoredModelBoundMessage[];
  readonly agents?: readonly (AgentContentInput | null | undefined)[];
  readonly assistants?: readonly (AssistantContentInput | null | undefined)[];
  readonly actions?: readonly (AssistantActionContentInput | null | undefined)[];
  readonly skills?: readonly (SkillContentInput | null | undefined)[];
  readonly memories?: readonly (MemoryContentInput | string | null | undefined)[];
  readonly files?: readonly (FileContentInput | string | null | undefined)[];
  /**
  * Owner-scoped canonical file rows that authorize durable file locators in
  * stored messages. The rows are inspected before their IDs are omitted from
  * the fail-close traversal copy.
  */
  readonly resolvedFiles?: readonly (ModelBoundCanonicalFile | null | undefined)[];
  /** Internal fail-closed provenance errors evaluated after files and findings. */
  readonly deferredTraversalErrors?: readonly ContentTraversalLimitError[];
  /** Internal aggregate nested-work budget shared by one provider callback invocation. */
  readonly traversalBudget?: VisitNestedStringsBudget;
}
/**
* Whether a policy can inspect content at a model/provider boundary.
* Management-only sources and attribution behavior do not activate this gate.
*/
declare function hasModelBoundContentProtection(filters: FiltersConfig | null | undefined, legacyPii?: MessageFilterPiiConfig | null): boolean;
/** Inspects the exact provider selection while retaining persisted provenance. */
declare function assertModelBoundProviderContent(input: ModelBoundProviderContentInput): void;
/** Creates a run-stable callback shared by root, summary, and subagent model clients. */
declare function createModelBoundChatModelCallback(input: Omit<ModelBoundProviderContentInput, "providerMessages">, options?: {
  readonly onContentRejected?: (error: unknown) => void;
}): ModelBoundChatModelCallback;
/**
* Holds a deferred parent write until every top-level starting agent clears
* its first exact provider boundary and the corresponding model-node chain
* completes. This callback belongs on the root RunnableConfig only: intrinsic
* model callbacks intentionally propagate into subagents, whose reused agent
* IDs must never satisfy a parent graph's admission barrier.
*/
declare function createInitialModelBoundAdmissionCallback(admission: InitialModelBoundAdmission): InitialModelBoundAdmissionCallback;
/**
* Re-inspects the final model-bound representation. This makes a newly
* enabled or tightened policy apply to persisted messages and reusable
* agent/skill/memory/file context, not only to the request that created it.
*/
declare function assertModelBoundContent(input: ModelBoundContentInput): void;
//#endregion
//#region src/middleware/messageFilterPii.d.ts
/**
* Wire config parsing to the same linear-time regex engine used at runtime.
* This prevents an operator pattern accepted by JavaScript RegExp but unsupported
* by RE2 from being silently omitted when the filter is created.
*/
declare function configureMessageFilterRegexValidator(): void;
interface PiiMatch {
  id: string;
  label: string;
  /** Set when a configured custom pattern cannot be enforced by the runtime engine. */
  misconfigured?: boolean;
}
type ContentPart$1 = {
  type?: string;
  text?: string;
  [key: string]: unknown;
};
type ChatLikeMessage = {
  role?: string;
  content?: string | ContentPart$1[];
};
declare function findPiiMatchInMessages(messages: ChatLikeMessage[] | undefined, config: MessageFilterPiiConfig | undefined): PiiMatch | null;
interface CreateMessageFilterPiiOptions {
  readonly onTraversalFailure?: LocatorTraversalReporter;
  getConfig: (req: Request) => MessageFilterPiiConfig | undefined;
  getFilters?: (req: Request) => FiltersConfig | undefined;
  getFiles?: GetCanonicalFilesForInspection;
}
declare function createMessageFilterPii(options: CreateMessageFilterPiiOptions): RequestHandler;
//#endregion
//#region src/middleware/messageValidation.d.ts
type MessageValidationUser = {
  id: string;
  tenantId?: string | null;
};
type MessageValidationBody = {
  conversationId?: string;
  message?: {
    conversationId?: string;
  };
};
type MessageValidationParams = {
  conversationId?: string;
  messageId?: string;
};
type MessageValidationRequest = {
  method?: string;
  params?: MessageValidationParams;
  body?: MessageValidationBody;
  user: MessageValidationUser;
  messageRequestValidation?: MessageRequestValidation;
};
type ConversationRecord = {
  user?: string;
  subagentThread?: unknown;
} | null;
type PendingActionRecord = unknown;
type GenerationJobRecord = {
  status?: string;
  metadata?: {
    userId?: string;
    tenantId?: string | null;
    pendingAction?: PendingActionRecord;
  };
} | null;
type MessageValidationResult = {
  ok: true;
} | {
  ok: false;
  status: number;
  body: unknown;
  send?: boolean;
};
type FailedMessageValidationResult = Extract<MessageValidationResult, {
  ok: false;
}>;
type MessageRequestValidation = {
  conversationId?: string;
  shouldFetchMessages: boolean;
  promise: Promise<MessageValidationResult>;
};
type MessageValidationLogger = {
  warn: (message: string, error: unknown) => void;
};
type MessageValidationDeps = {
  getConvo: (userId: string, conversationId?: string) => Promise<ConversationRecord>;
  getJob: (conversationId?: string) => Promise<GenerationJobRecord>;
  isPendingActionStale: (job: {
    pendingAction?: PendingActionRecord;
  }) => boolean;
  logger: MessageValidationLogger;
};
type MessageRequestMiddleware = {
  canReadActiveJobConversation: (req: MessageValidationRequest, conversationId?: string) => Promise<boolean>;
  createMessageRequestValidation: (req: MessageValidationRequest) => MessageRequestValidation;
  prepareMessageRequestValidation: (req: MessageValidationRequest, res: Response$2, next: NextFunction) => void;
  sendValidationResponse: (res: Response$2, result: FailedMessageValidationResult) => Response$2;
  validateMessageReq: (req: MessageValidationRequest, res: Response$2, next: NextFunction) => Promise<Response$2 | void>;
};
declare function createMessageRequestMiddleware(deps: MessageValidationDeps): MessageRequestMiddleware;
//#endregion
//#region src/middleware/feedback.d.ts
/**
* Rejects message feedback writes when the deployment set `interface.feedback: false`,
* so a deployment that hides the controls also stores no ratings. Requires the app
* config to already be resolved onto the request.
*/
declare function requireFeedbackEnabled(req: ServerRequest, res: Response$2, next: NextFunction): void;
//#endregion
//#region src/middleware/generationRetry.d.ts
declare const confirmedGenerationRetry: unique symbol;
declare const GENERATION_RETRY_WINDOW_MS = 6e4;
declare const GENERATION_RETRY_MAX = 10;
declare const GENERATION_RETRY_PROBE_MAX = 60;
type GenerationRetryRequest = Request & {
  user?: {
    id?: string;
  };
  [confirmedGenerationRetry]?: boolean;
};
/**
* Classifies only retries already represented by a durable generation claim.
* The controller still owns the authoritative claim/read transition.
*/
declare function detectGenerationRetry(req: GenerationRetryRequest, _res: Response$2, next: NextFunction): Promise<void>;
declare function isConfirmedGenerationRetry(req: Request): boolean;
/**
* Bounds read-only claim probes before they touch the shared generation store.
* The retryable response participates in the client's existing 120-second
* readiness loop and express-rate-limit supplies its Retry-After header.
*/
declare const generationRetryProbeLimiter: RequestHandler;
/**
* Confirmed retries bypass the ordinary message buckets so a lost response can
* be recovered, but they still receive a small user-scoped allowance before
* moderation and the rest of the request pipeline. This bounds replay costs
* while leaving the authoritative generation claim unchanged.
*/
declare const generationRetryLimiter: RequestHandler;
//#endregion
//#region src/middleware/code.d.ts
declare const codeEnvironmentPairingLimiter: RequestHandler;
declare const codeEnvironmentStatusLimiter: RequestHandler;
declare const codeEnvironmentStatusIpLimiter: RequestHandler;
//#endregion
//#region src/auth/oidc.d.ts
interface OidcAccessTokenConfig {
  audience: string;
  issuer: string;
  jwksUri?: string;
}
//#endregion
//#region src/middleware/management.d.ts
interface AgentManagementAuthDeps {
  findUser: UserMethods["findUser"];
  isPrincipalActive: (userId: string) => Promise<boolean>;
  getAppConfig: (options?: GetAppConfigOptions) => Promise<AppConfig>;
  verifyAccessToken?: (token: string, config: OidcAccessTokenConfig) => Promise<JwtPayload>;
}
declare function getMachineClientId(payload: JwtPayload): string;
declare function createAgentManagementAuth(deps: AgentManagementAuthDeps): RequestHandler;
//#endregion
//#region src/security/env.d.ts
declare function normalizeEnvValue(value: string | undefined): string;
declare function isFalsyEnvValue(value: string): boolean;
/**
* Resolves a security toggle. Unrecognized values warn and fall back rather than
* being treated as the opposite of the default, so a typo cannot silently flip a
* deployment into the riskier setting.
*/
declare function parseEnvSwitch(name: string, value: string | undefined, fallback: boolean): boolean;
//#endregion
//#region src/security/headers.d.ts
type FrameOptionsAction = "deny" | "sameorigin";
type OpenerPolicy = "same-origin" | "same-origin-allow-popups" | "noopener-allow-popups" | "unsafe-none";
type ResourcePolicy = "same-origin" | "same-site" | "cross-origin";
type ReferrerPolicyToken = "no-referrer" | "no-referrer-when-downgrade" | "same-origin" | "origin" | "strict-origin" | "origin-when-cross-origin" | "strict-origin-when-cross-origin" | "unsafe-url";
interface HstsOptions {
  maxAge: number;
  includeSubDomains: boolean;
  preload: boolean;
}
interface SecurityHeaderOptions {
  contentSecurityPolicy: false;
  hsts: HstsOptions | false;
  frameguard: {
    action: FrameOptionsAction;
  } | false;
  crossOriginOpenerPolicy: {
    policy: OpenerPolicy;
  } | false;
  crossOriginResourcePolicy: {
    policy: ResourcePolicy;
  } | false;
  referrerPolicy: {
    policy: ReferrerPolicyToken;
  } | false;
}
/**
* Builds helmet options from the environment. CSP is always disabled here so the
* CSP-independent headers never depend on a directive allow-list staying current
* with whichever optional features a deployment has enabled.
*/
declare function buildSecurityHeaderOptions(env?: NodeJS.ProcessEnv): SecurityHeaderOptions | null;
/**
* Creates the baseline security-header middleware, or `null` when the operator
* disabled it via `SECURITY_HEADERS=false`.
*
* @example
* const securityHeaders = createSecurityHeaders();
* if (securityHeaders) {
*   app.use(securityHeaders);
* }
*/
declare function createSecurityHeaders(env?: NodeJS.ProcessEnv): RequestHandler | null;
//#endregion
//#region src/security/csp.d.ts
type CspDirective = [string, string[]];
/** Precomputed once at startup; only the nonce varies per response. */
interface CspPolicy {
  headerName: "Content-Security-Policy" | "Content-Security-Policy-Report-Only";
  prefix: string;
  suffix: string;
}
interface CspResponse {
  headerName: CspPolicy["headerName"];
  headerValue: string;
  nonce: string;
}
declare function buildCspDirectives(env?: NodeJS.ProcessEnv): CspDirective[];
declare function serializeCspDirectives(directives: CspDirective[]): string;
/**
* Resolves the policy once at startup. Returns `null` unless `CSP_ENABLED` is set,
* so existing deployments are unaffected until they opt in.
*/
declare function createCspPolicy(env?: NodeJS.ProcessEnv): CspPolicy | null;
interface ShellCacheHeaders {
  "Cache-Control": string;
  Pragma: string;
  Expires: string;
}
/**
* Cache headers for the SPA shell. A nonce policy is only as strong as the
* response being unique, so when CSP is on the shell is forced non-storable and a
* cacheable `INDEX_CACHE_CONTROL` override is refused: a cached shell would pin one
* nonce across page loads and users, which is exactly what an injected script needs.
*/
declare function shellCacheHeaders(cspEnabled: boolean, env?: NodeJS.ProcessEnv): ShellCacheHeaders;
/** Mints the per-response nonce and its header value. */
declare function issueCsp(policy: CspPolicy): CspResponse;
/**
* Stamps the response nonce onto the shell's `<script>` elements and onto the
* module preloads a production build emits, which are parser-inserted and so are
* not covered by `'strict-dynamic'`. Any nonce already in the markup is replaced,
* never kept: only the current response's nonce is authorized by the header.
*
* Styles are deliberately untouched; see `defaultDirectives`.
*/
declare function applyCspNonce(html: string, nonce: string): string;
//#endregion
//#region src/security/html.d.ts
/** Escapes text before interpolating it into a quoted HTML attribute. */
declare function escapeHtmlAttribute(value: string): string;
//#endregion
//#region src/protection/runtime.d.ts
interface ContentInspectionConfig {
  readonly filters?: FiltersConfig;
  readonly legacyPii?: MessageFilterPiiConfig;
}
interface ConfiguredContentInspector {
  readonly hasAuditRules: boolean;
  createSession(): ConfiguredContentInspectionSession;
  inspect(fragments: Iterable<TextContentFragment>): ProtectionFinding | null;
}
interface ConfiguredContentInspectionSession {
  readonly hasAuditRules: boolean;
  inspectFragment(fragment: TextContentFragment): ProtectionFinding | null;
  inspect(fragments: Iterable<TextContentFragment>): ProtectionFinding | null;
}
interface TraversalAwareContentInspectionConfig extends ContentInspectionConfig {
  readonly roles?: readonly (string | undefined)[];
}
interface TraversalAwareContentInspectionResult {
  readonly finding: ProtectionFinding | null;
  readonly traversalError: ContentTraversalLimitError | null;
}
declare function createConfiguredContentInspector(config: ContentInspectionConfig): ConfiguredContentInspector | null;
declare function inspectContent$1(fragments: Iterable<TextContentFragment>, config: ContentInspectionConfig): ProtectionFinding | null;
/** Inspects bounded extraction fragments before enforcing a protected traversal failure. */
declare function inspectContentWithTraversal(extract: () => Iterable<TextContentFragment>, config: TraversalAwareContentInspectionConfig): TraversalAwareContentInspectionResult;
//#endregion
//#region src/protection/title.d.ts
declare const SAFE_CONVERSATION_TITLE = "New Chat";
interface ResolveConversationTitleOptions {
  readonly filters?: FiltersConfig;
  readonly candidate?: string | null;
  readonly fallback?: string | null;
}
declare function resolveConversationTitle({
  filters,
  candidate,
  fallback
}: ResolveConversationTitleOptions): string | null;
//#endregion
//#region src/protection/legacy.d.ts
interface LegacyPiiMatch {
  readonly id: string;
  readonly label: string;
}
interface LegacyPiiInspector {
  inspect(fragments: Iterable<TextContentFragment>): ProtectionFinding | null;
}
declare function isLegacyPiiFragment(fragment: TextContentFragment): boolean;
declare function createLegacyPiiInspector(config: MessageFilterPiiConfig | undefined): LegacyPiiInspector | null;
declare function inspectLegacyPii(fragments: Iterable<TextContentFragment>, config: MessageFilterPiiConfig | undefined): ProtectionFinding | null;
declare function toLegacyPiiMatch(finding: ProtectionFinding | null): LegacyPiiMatch | null;
//#endregion
//#region src/protection/provenance.d.ts
declare const MAX_USER_SUBMITTED_PATHS = 256;
declare const MAX_USER_SUBMITTED_PATH_LENGTH = 2048;
interface UserSubmittedPathState {
  readonly paths: JsonPointer[];
  readonly overflowed: boolean;
}
interface UserSubmittedPathOptions {
  readonly scope?: "stored_message" | "shared_message";
  /** Restricts semantic steer discovery to provider-retained source parts. */
  readonly semanticContentPartIndices?: Iterable<number>;
  /** Allows callers that cache explicit metadata separately to defer content scans. */
  readonly includeSemanticContent?: boolean;
  /** Allows callers to add semantic paths to an already-captured explicit snapshot. */
  readonly includeExplicitPaths?: boolean;
  /** Optional caller-owned aggregate budget shared across provenance carriers. */
  readonly budget?: UserSubmittedProvenanceWorkBudget;
  /** Reuses a caller-captured content carrier across path and field snapshots. */
  readonly capturedContent?: unknown;
  readonly hasCapturedContent?: boolean;
  /** Reuses the caller's single validated array-length observation. */
  readonly capturedContentLength?: number;
  /** Reuses each content-part value across attribution and projection phases. */
  readonly capturedContentParts?: Map<number, unknown>;
  /** Materializes a content part once before any attribution or projection read. */
  readonly captureContentPart?: (part: unknown, index: number) => unknown;
}
interface UserSubmittedProvenanceWorkBudget {
  remaining: number;
  overflowed: boolean;
}
type UserSubmittedPathCarrier = object & {
  readonly userSubmittedPaths?: readonly unknown[];
  readonly userSubmittedMessageFieldPaths?: readonly unknown[];
  readonly content?: readonly unknown[];
  readonly text?: unknown;
};
interface UserSubmittedMessageFieldPathState {
  readonly entries: UserSubmittedMessageFieldPath[];
  readonly overflowed: boolean;
}
interface CapturedUserSubmittedPathMetadata {
  readonly contentParts: ReadonlyMap<number, unknown>;
  readonly steerPaths: ReadonlySet<string>;
}
declare function getCapturedUserSubmittedPathMetadata(state: UserSubmittedPathState): CapturedUserSubmittedPathMetadata;
declare function getSafeUserSubmittedPathSegments(path: JsonPointer): readonly string[] | undefined;
/**
* Resolves durable caller-authorship pointers against the exact stored row.
* Ineffective or unsafe pointers never suppress strict whole-row attribution;
* excessive unique bounded candidates still fail closed via `overflowed`.
*/
declare function getUserSubmittedPathState(message: UserSubmittedPathCarrier, options?: UserSubmittedPathOptions): UserSubmittedPathState;
/**
* Resolves exact HITL message-field identities against the stored row. Invalid,
* stale, or unsafe metadata is ignored; excessive distinct entries retain a
* fail-closed overflow signal without broadening any individual field label.
*/
declare function getUserSubmittedMessageFieldPathState(message: UserSubmittedPathCarrier, options?: UserSubmittedPathOptions): UserSubmittedMessageFieldPathState;
//#endregion
//#region src/protection/adapters/chat.d.ts
interface ChatSubmissionDecision {
  readonly responseText?: string;
  readonly reason?: string;
  readonly editedArguments?: unknown;
}
interface ChatSubmissionBody extends ModelParameterContentInput {
  readonly input?: string;
  readonly text?: string;
  readonly quotes?: unknown;
  readonly answer?: string;
  readonly promptPrefix?: string | null;
  readonly system?: string;
  readonly context?: string | null;
  readonly instructions?: string | null;
  readonly additional_instructions?: string | null;
  readonly greeting?: string;
  readonly artifacts?: string;
  readonly manualSkills?: readonly unknown[];
  readonly examples?: readonly ({
    readonly input?: string | {
      readonly content?: string;
    };
    readonly output?: string | {
      readonly content?: string;
    };
  } | null | undefined)[];
  readonly ephemeralAgent?: {
    readonly artifacts?: string;
  } | null;
  readonly addedConvo?: {
    readonly promptPrefix?: string | null;
    readonly additional_instructions?: string | null;
    readonly artifacts?: string;
    readonly ephemeralAgent?: {
      readonly artifacts?: string;
    } | null;
  } | null;
  readonly editedContent?: {
    readonly text?: string;
  } | null;
  readonly decisions?: readonly (ChatSubmissionDecision | null | undefined)[];
}
declare function extractChatContent(body: ChatSubmissionBody | null | undefined): readonly TextContentFragment[];
//#endregion
//#region src/protection/messageMutations.d.ts
/** Re-inspect the exact stored-message projection produced by a mutation. */
declare function assertStoredMessageMutationAllowed(filters: FiltersConfig | undefined, message: StoredMessageContentInput): void;
/** Re-inspect chat fields, including persisted quotes merged into edited user text. */
declare function assertChatMutationAllowed(filters: FiltersConfig | undefined, chat: Pick<ChatSubmissionBody, "text" | "quotes">): void;
interface StoredMessageBranchPolicyInput {
  readonly filters?: FiltersConfig;
  readonly legacyPii?: MessageFilterPiiConfig;
  readonly message: StoredMessageContentInput;
  readonly user?: CanonicalFileInspectionUser;
}
/**
* Hydrate canonical file rows and inspect the exact branch message before it
* can be persisted. The legacy route supplies only database and transport
* adapters; policy projection and fail-close semantics remain typed here.
*/
declare function assertStoredMessageBranchAllowed(input: StoredMessageBranchPolicyInput, dependencies: {
  readonly getFiles: GetCanonicalFilesForInspection;
  readonly onTraversalFailure?: LocatorTraversalReporter;
}): Promise<void>;
//#endregion
//#region src/imports.d.ts
interface ConversationImportMessage extends StoredMessageContentInput {
  readonly isCreatedByUser?: boolean;
  readonly isUserSubmitted?: boolean;
  readonly userSubmittedPaths?: readonly string[];
  readonly userSubmittedMessageFieldPaths?: readonly UserSubmittedMessageFieldPath[];
}
interface ConversationImportSnapshot {
  readonly conversations: ConversationImportContentInput["conversations"];
  readonly messages: readonly ConversationImportMessage[];
}
interface ConversationImportProtectionContext {
  readonly onTraversalFailure?: LocatorTraversalReporter;
  readonly user?: CanonicalFileInspectionUser;
  readonly getFiles?: GetCanonicalFilesForInspection;
  readonly trustedLiveFiles?: readonly CanonicalFileInspectionFile[];
  readonly legacyPii?: MessageFilterPiiConfig | null;
  /** Injectable only at the legacy package boundary and in focused tests. */
  readonly assertModelBoundContent?: (input: ModelBoundContentInput) => void;
}
/**
* Applies the active policy to the complete normalized import snapshot before
* the legacy importer starts any writes. Canonical resolution operates on an
* inspection copy, so a rejected preflight never mutates the pending batch.
* Audit findings are aggregated across the snapshot so a large conversation
* reports each finding once instead of once per matching fragment.
*/
declare function assertConversationImportContentAllowed(filters: FiltersConfig | null | undefined, snapshot: ConversationImportSnapshot, context?: ConversationImportProtectionContext): Promise<void>;
//#endregion
//#region src/memory/authorization.d.ts
type AgentMemoryPartitionAccess = "allowed" | "denied" | "not_found";
interface CheckPermissionParams {
  userId: string;
  role?: string;
  resourceType: ResourceType;
  resourceId: IAgent["_id"];
  requiredPermission: number;
}
interface AgentMemoryPartitionAccessDependencies {
  getAgent: (query: {
    id: string;
  }, projection: {
    _id: 1;
  }) => Promise<Pick<IAgent, "_id"> | null>;
  getRoleByName: CheckAccessParams["getRoleByName"];
  hasCapability: (user: IUser, capability: (typeof ResourceCapabilityMap)[ResourceType.AGENT]) => Promise<boolean>;
  checkPermission: (params: CheckPermissionParams) => Promise<boolean>;
}
interface AgentMemoryPartitionAccessParams extends AgentMemoryPartitionAccessDependencies {
  req: Request;
  user: IUser;
  agentId: string;
  allowMissingAgent?: boolean;
}
interface AgentMemoryPartitionMiddlewareParams extends AgentMemoryPartitionAccessDependencies {
  source: "body" | "query";
  allowMissingAgent?: boolean;
}
declare function getMemoryAgentIdParam(value: unknown): string | undefined;
declare function getAgentMemoryPartitionAccess({
  req,
  user,
  agentId,
  allowMissingAgent,
  getAgent,
  getRoleByName,
  hasCapability,
  checkPermission
}: AgentMemoryPartitionAccessParams): Promise<AgentMemoryPartitionAccess>;
declare function createAgentMemoryPartitionMiddleware({
  source,
  allowMissingAgent,
  ...dependencies
}: AgentMemoryPartitionMiddlewareParams): (req: Request, res: Response$2, next: NextFunction) => Promise<unknown>;
//#endregion
//#region src/memory/protection.d.ts
type ProjectedStoredMemory<T extends MemoryContentInput> = T & {
  readonly contentFilterBlocked?: true;
};
interface StoredMemoryProjectionDeps {
  readonly extract?: (memory: MemoryContentInput) => Iterable<TextContentFragment>;
}
/** Reapplies current memory policy while preserving structural management fields. */
declare function projectStoredMemories<T extends MemoryContentInput>(memories: readonly T[], filters?: FiltersConfig, deps?: StoredMemoryProjectionDeps): ProjectedStoredMemory<T>[];
//#endregion
//#region src/memory/handlers.d.ts
interface MemoryUpdateBody {
  key?: string;
  value?: string;
}
interface MemoryQuery {
  agentId?: string | string[];
}
interface MemoryRequestConfig {
  filters?: FiltersConfig;
  memory?: {
    charLimit?: number;
  };
}
type MemoryRequest = Request<{
  id: string;
}, object, MemoryUpdateBody, MemoryQuery> & {
  user?: {
    id?: string;
  };
  config?: MemoryRequestConfig;
};
type ProjectStoredMemories = <T extends MemoryContentInput>(memories: readonly T[], filters?: FiltersConfig) => ProjectedStoredMemory<T>[];
interface MemoryManagementHandlersDeps extends Pick<MemoryMethods, "setMemoryById" | "deleteMemoryById"> {
  countTokens: (value: string) => number;
  projectStoredMemories: ProjectStoredMemories;
}
declare function blockFilteredMemoryContent(req: MemoryRequest, res: Response$2, memory: MemoryContentInput): boolean;
declare function createMemoryManagementHandlers(deps: MemoryManagementHandlersDeps): {
  updateById: (req: MemoryRequest, res: Response$2) => Promise<Response$2>;
  deleteById: (req: MemoryRequest, res: Response$2) => Promise<Response$2>;
};
//#endregion
//#region src/modelSpecs/index.d.ts
declare const PRIVATE_MODEL_SPEC_PRESET_FIELDS: readonly ["promptPrefix", "instructions", "additional_instructions", "system", "context", "examples"];
type PrivateModelSpecPresetField = (typeof PRIVATE_MODEL_SPEC_PRESET_FIELDS)[number];
type ModelSpecParsedBody = Partial<TConversation | TPreset | TModelSpecPreset> & Record<string, unknown>;
declare const ENFORCED_MODEL_SPEC_REQUEST_FIELDS: readonly ["chatProjectId"];
type ApplyModelSpecPresetParams = {
  modelSpec: TModelSpec;
  parsedBody: ModelSpecParsedBody;
  endpoint?: string | null;
  endpointType?: string | null;
  defaultParamsEndpoint?: string | null;
  includePresetDefaults?: boolean;
};
type ApplyModelSpecPresetResult = {
  parsedBody: ModelSpecParsedBody;
  appliedPrivateFields: Set<PrivateModelSpecPresetField>;
};
declare function findModelSpecByName(modelSpecs: Pick<TSpecsConfig, "list"> | undefined, spec: string | null | undefined): TModelSpec | undefined;
declare function isModelSpecEndpointMatch(modelSpec: Pick<TModelSpec, "preset"> | undefined, endpoint: string | null | undefined): boolean;
type ModelSpecEndpointResolution = {
  modelSpec: TModelSpec;
} | {
  error: "invalid-model-spec" | "model-spec-mismatch";
};
/**
* Resolves the selected spec only when it serves the requested endpoint.
* Authorization and endpoint construction use this same result so a preset
* cannot change the resource identity after its access check has completed.
*/
declare function resolveModelSpecForEndpoint({
  modelSpecs,
  spec,
  endpoint
}: {
  modelSpecs: Pick<TSpecsConfig, "list"> | undefined;
  spec: string;
  endpoint: string | null | undefined;
}): ModelSpecEndpointResolution;
declare function applyModelSpecPreset({
  modelSpec,
  parsedBody,
  endpoint,
  endpointType,
  defaultParamsEndpoint,
  includePresetDefaults
}: ApplyModelSpecPresetParams): ApplyModelSpecPresetResult;
declare function resolveModelSpecPromptPrefixVariables<T extends {
  promptPrefix?: string | null;
}>(parsedBody: T, user?: TUser | null, now?: string | number | Date): T;
/**
* Drops specs marked `showInMenu: false` so they are not advertised to clients
* (the model selector menu and the startup config). Such specs remain resolvable
* server-side by name, so a caller that sets `spec: "<name>"` can still use them.
* Returns the config unchanged when there is no list. Apply before `sanitizeModelSpecs`.
*/
declare function excludeHiddenModelSpecs<T extends Partial<TSpecsConfig> | null | undefined>(modelSpecs: T): T;
declare function sanitizeModelSpecs<T extends Partial<TSpecsConfig> | null | undefined>(modelSpecs: T): T;
//#endregion
//#region src/agents/avatars.d.ts
declare const MAX_AVATAR_REFRESH_AGENTS = 1e3;
declare const AVATAR_REFRESH_BATCH_SIZE = 20;
type RefreshS3UrlFn = (avatar: AgentAvatar) => Promise<string | undefined>;
type UpdateAgentFn = (searchParams: {
  id: string;
}, updateData: {
  avatar: AgentAvatar;
}, options: {
  updatingUserId: string;
  skipVersioning: boolean;
}) => Promise<unknown>;
type RefreshListAvatarsParams = {
  agents: Agent[];
  userId: string;
  refreshS3Url: RefreshS3UrlFn;
  updateAgent: UpdateAgentFn;
};
type RefreshStats = {
  updated: number;
  not_s3: number;
  no_id: number;
  no_change: number;
  s3_error: number;
  persist_error: number; /** Maps agentId to the latest valid presigned filepath for re-application on cache hits */
  urlCache: Record<string, string>;
};
/**
* Opportunistically refreshes S3-backed avatars for agent list responses.
* Processes agents in batches to prevent database connection pool exhaustion.
* Only list responses are refreshed because they're the highest-traffic surface and
* the avatar URLs have a short-lived TTL. The refresh is cached per-user for 30 minutes
* so we refresh once per interval at most.
*
* Any user with VIEW access to an agent can refresh its avatar URL. This ensures
* avatars remain accessible even when the owner hasn't logged in recently.
* The agents array should already be filtered to only include agents the user can access.
*/
declare const refreshListAvatars: ({
  agents,
  userId,
  refreshS3Url,
  updateAgent
}: RefreshListAvatarsParams) => Promise<RefreshStats>;
//#endregion
//#region src/agents/errors.d.ts
declare const AGENT_EXPECTED_MCP_TOOLS_UNAVAILABLE = "AGENT_EXPECTED_MCP_TOOLS_UNAVAILABLE";
declare const AGENT_ATTACHMENT_LIMIT_EXCEEDED = "AGENT_ATTACHMENT_LIMIT_EXCEEDED";
declare function createStatefulCodeEnvironmentPolicyError(environment: string): Error;
interface FatalAgentInitializationOptions {
  signal?: AbortSignal;
  /**
  * Skill `allowed-tools` may add an MCP tool beyond the agent's configured
  * baseline. That union load is allowed to retry without the skill extras;
  * a second failure from the baseline still propagates normally.
  */
  allowExpectedMCPFallback?: boolean;
}
/**
* Returns whether agent initialization must abort instead of using the
* legacy soft-failure behavior for unavailable optional tools or agents.
* Keep fatal initialization policy centralized here so every topology and
* ingress path makes the same decision when new invariant errors are added.
*/
declare function isFatalAgentInitializationError(error: unknown, options?: FatalAgentInitializationOptions): boolean;
/** Fallback shown when provider error text must not reach the user. */
declare const GENERIC_PROVIDER_ERROR = "An error occurred while processing the request";
/**
* Reads LangChain's classification off the error, falling back to the docs URL it stamps into the
* message so a re-thrown or serialized error still classifies.
*/
declare function getLangChainErrorCode(error: unknown): string | undefined;
/**
* Typed payload the client localizes for a classified LangChain failure, or `undefined` when the
* code has no localized copy and the provider's own message should be shown instead.
*/
declare function resolveLangChainError(error: unknown): string | undefined;
/**
* Provider failure text for OpenAI-compatible responses, which carry raw strings rather than the
* typed payloads the TerraMind client localizes.
*/
declare function getUserFacingProviderError(error: unknown, protectionEnabled: boolean): string;
/**
* Whether `error` is the agent graph exhausting its per-turn step budget
* (`recursionLimit`), as opposed to anything actually going wrong.
*
* This is a normal terminal condition, not a failure: the turn is persisted as
* `unfinished` with `Constants.TOOL_CALL_LIMIT_FINISH_REASON` so the UI can offer
* to continue, instead of surfacing a red error bubble the user cannot act on.
*
* Both markers are checked because they fail independently. `lc_error_code` is the
* documented contract but is only present on errors constructed with the fields
* argument, while `name` is assigned in the constructor body and therefore survives
* class-name minification. Either one alone is sufficient evidence.
*/
declare function isStepLimitError(error: unknown): boolean;
//#endregion
//#region src/agents/attachments.d.ts
type FileWithId = {
  file_id?: string | null;
};
type AttachmentTelemetryFile = FileWithId & {
  bytes?: number | null;
  source?: string | null;
  type?: string | null;
  text?: string | null;
  llmDeliveryPath?: string | null;
  metadata?: (IMongoFile["metadata"] & {
    pageCount?: number | null;
  }) | null;
};
/** Whether a hydrated file contributes content to the model prompt itself. */
declare function isModelBoundAttachmentFile(file: AttachmentTelemetryFile | null | undefined): boolean;
type AgentAttachmentLimitRequest = {
  config?: {
    fileConfig?: Parameters<typeof mergeFileConfig>[0];
  };
};
type AgentAttachmentLimit = "count" | "bytes" | "extracted_text";
interface AgentAttachmentStats {
  attachmentCount: number;
  totalKnownBytes: number;
  extractedTextChars: number;
  files: Array<{
    fileId?: string;
    mimeType?: string;
    bytes?: number;
    extractedTextChars?: number;
    pageCount?: number;
  }>;
}
declare class AgentAttachmentLimitError extends Error {
  readonly limitType: AgentAttachmentLimit;
  readonly observed: number;
  readonly limit: number;
  readonly code: typeof AGENT_ATTACHMENT_LIMIT_EXCEEDED;
  readonly status = 413;
  readonly statusCode = 413;
  constructor(limitType: AgentAttachmentLimit, observed: number, limit: number);
}
declare class AgentAttachmentPolicyError extends Error {
  readonly code: typeof AGENT_ATTACHMENT_LIMIT_EXCEEDED;
  readonly status = 413;
  readonly statusCode = 413;
  constructor();
}
declare function isAgentAttachmentLimitError(error: unknown): error is AgentAttachmentLimitError | AgentAttachmentPolicyError;
declare function collectAgentAttachmentStats(attachments?: Iterable<AttachmentTelemetryFile | null | undefined> | null, options?: {
  countRepeatedExtractedText?: boolean;
  countRepeatedBytes?: boolean;
}): AgentAttachmentStats;
declare function assertAgentAttachmentLimits({
  attachments,
  req,
  fileConfig: providedFileConfig,
  endpoint,
  endpointType,
  countRepeatedExtractedText,
  countRepeatedBytes,
  enforceAttachmentCount,
  useGlobalContextSizeLimit
}: {
  attachments?: Iterable<AttachmentTelemetryFile | null | undefined> | null;
  req?: AgentAttachmentLimitRequest;
  fileConfig?: Parameters<typeof mergeFileConfig>[0];
  endpoint?: string | null;
  endpointType?: string | null;
  countRepeatedExtractedText?: boolean;
  countRepeatedBytes?: boolean;
  enforceAttachmentCount?: boolean;
  useGlobalContextSizeLimit?: boolean;
}): AgentAttachmentStats;
declare function assertAgentAttachmentTopology({
  sharedAttachments,
  scopedAttachmentsByAgentId,
  req,
  endpoint,
  endpointType,
  endpointsByAgentId
}: {
  sharedAttachments?: IMongoFile[];
  scopedAttachmentsByAgentId?: Map<string, IMongoFile[]>;
  req?: ServerRequest;
  endpoint?: string | null;
  endpointType?: string | null;
  endpointsByAgentId?: AgentAttachmentEndpointsByAgentId;
}): void;
type AgentMemoryPhase = "before_process_attachments" | "after_process_attachments" | "before_encode_documents" | "after_encode_documents" | "before_context_assembly" | "after_context_assembly" | "before_model" | "after_model" | "model_error" | "before_terminal_save" | "after_terminal_save" | "before_final_publish" | "after_final_publish";
interface AgentMemoryContext {
  req?: ServerRequest;
  conversationId?: string | null;
  messageId?: string | null;
  attachments?: Iterable<AttachmentTelemetryFile | null | undefined> | null;
  countRepeatedExtractedText?: boolean;
}
declare function logAgentMemorySnapshot(phase: AgentMemoryPhase, context: AgentMemoryContext, modelRunId?: string): void;
declare function createAgentMemoryCallback(context: AgentMemoryContext): {
  name: string;
  handleChatModelStart: (...args: unknown[]) => void;
  handleLLMStart: (...args: unknown[]) => void;
  handleLLMEnd: (_output: unknown, runId: string) => void;
  handleLLMError: (_error: unknown, runId: string) => void;
};
type AgentContextAttachmentCarrier<TFile extends FileWithId = IMongoFile> = {
  id?: string | null;
  agentContextAttachments?: TFile[] | null;
  subagentAgentConfigs?: AgentContextAttachmentCarrier<TFile>[] | null;
  subagentGraphConfigs?: Array<{
    memberConfigs?: AgentContextAttachmentCarrier<TFile>[] | null;
  }> | null;
};
type AgentContextAttachmentsByAgentId<TFile extends FileWithId = IMongoFile> = Map<string, TFile[]> | Record<string, TFile[] | undefined> | null | undefined;
type AgentAttachmentEndpointsByAgentId = Map<string, {
  endpoint?: string | null;
  endpointType?: string | null;
}> | Record<string, {
  endpoint?: string | null;
  endpointType?: string | null;
}>;
declare function collectFileIds<TFile extends FileWithId>(files?: Array<TFile | null | undefined> | null): Set<string>;
declare function buildAgentContextAttachmentsByAgentId<TFile extends FileWithId>(configs: Iterable<AgentContextAttachmentCarrier<TFile> | null | undefined>): Map<string, TFile[]>;
declare function getAgentContextAttachments<TFile extends FileWithId>({
  agentId,
  attachmentsByAgentId,
  excludeFileIds
}: {
  agentId: string;
  attachmentsByAgentId: AgentContextAttachmentsByAgentId<TFile>;
  excludeFileIds?: Set<string>;
}): TFile[];
declare function buildAgentScopedAttachmentMap({
  agentIds,
  attachmentsByAgentId,
  sharedRunAttachmentIds,
  req,
  endpoint,
  endpointType,
  endpointsByAgentId
}: {
  agentIds: string[];
  attachmentsByAgentId: AgentContextAttachmentsByAgentId<IMongoFile>;
  sharedRunAttachmentIds?: Set<string>;
  req?: ServerRequest;
  endpoint?: string | null;
  endpointType?: string | null;
  endpointsByAgentId?: AgentAttachmentEndpointsByAgentId;
}): Map<string, IMongoFile[]>;
declare function buildAgentScopedContext({
  agentIds,
  attachmentsByAgentId,
  sharedRunAttachmentIds,
  sharedAttachments,
  req,
  tokenCountFn,
  endpoint,
  endpointType,
  endpointsByAgentId
}: {
  agentIds: string[];
  attachmentsByAgentId: AgentContextAttachmentsByAgentId<IMongoFile>;
  sharedRunAttachmentIds?: Set<string>;
  sharedAttachments?: IMongoFile[];
  req?: ServerRequest;
  tokenCountFn?: TokenCountFn;
  endpoint?: string | null;
  endpointType?: string | null;
  endpointsByAgentId?: AgentAttachmentEndpointsByAgentId;
}): Promise<Map<string, string>>;
//#endregion
//#region src/agents/chain.d.ts
/**
* Helper function to create sequential chain edges with buffer string prompts
*
* @deprecated Agent Chain helper
* @param agentIds - Array of agent IDs in order of execution
* @param promptTemplate - Optional prompt template string; defaults to a predefined template if not provided
* @returns Array of edges configured for sequential chain with buffer prompts
*/
declare function createSequentialChainEdges(agentIds: string[], promptTemplate?: string): Promise<GraphEdge$1[]>;
//#endregion
//#region src/agents/callerCapabilities.d.ts
/**
* Accepts only complete snapshots for the version this host understands.
* Missing or future versions intentionally fall back to the legacy registry
* projection during a rolling SDK/host deployment.
*/
declare function resolveCallerCapabilityProjectionSnapshot(value: unknown): CallerCapabilityProjectionSnapshot | undefined;
//#endregion
//#region src/agents/client.d.ts
declare const omitTitleOptions: Set<string>;
declare function payloadParser({
  req,
  endpoint
}: {
  req: ServerRequest;
  endpoint: string;
}): Record<string, unknown> | undefined;
/**
* Anthropic's API consistently reports ~10% more tokens than the local
* claude tokenizer due to internal message framing and content encoding.
* Verified empirically across content types via the count_tokens endpoint.
*/
declare const CLAUDE_TOKEN_CORRECTION = 1.1;
type FormattedMessageContentPart = {
  type?: string;
  text?: string;
  [key: string]: unknown;
};
type FormattedMessageWithContent = {
  role?: string;
  content?: string | FormattedMessageContentPart[];
};
/**
* Substitutes stand-in text for a user turn that carries attachments but has
* nothing the provider can see: file search and code environment files reach
* the model out-of-band, so the content stays empty and Anthropic rejects the
* message outright. Apply after the file-context and quote merges so a turn
* that already gained inline content is left alone. The stored `message.text`
* keeps its empty value, so the UI still renders the attachment on its own.
*
* Takes the turn's files rather than the message because the current turn does
* not carry them yet: `BaseClient` assigns `userMessage.files` only after
* `buildMessages` returns, so callers pass the resolved attachments instead.
*/
declare function applyAttachmentOnlyText(formattedMessage: FormattedMessageWithContent, files?: TMessage["files"] | null): void;
/**
* Prepends context text to a formatted message: joined ahead of its string
* content or first text part, or added as a leading text part when it has
* neither. Array content is replaced, never edited in place: a formatted copy
* shares its array with the stored row it came from, so the stored row and
* every other reader of it stay untouched.
*/
declare function prependContextText(formattedMessage: FormattedMessageWithContent, text: string | null | undefined, separator?: string): void;
declare function prependFileContext(formattedMessage: FormattedMessageWithContent, fileContext?: string | null): void;
/**
* Prepends quoted excerpts (the "Add to chat" selections persisted on
* `message.quotes`) to a formatted message's content as Markdown blockquotes.
* Applied to every user message that carries quotes — current and historical —
* so the model durably receives the referenced context and the token count
* stays consistent with what was persisted. The stored `message.text` is left
* clean; the excerpts live on `message.quotes` for the UI.
*/
declare function prependQuotes(formattedMessage: FormattedMessageWithContent, quotes?: string[] | null): void;
/**
* Estimates token cost for image and document blocks in a message's
* content array. Covers: image_url, image, image_file, document, file.
*/
declare function estimateMediaTokensForMessage(content: unknown, isClaude: boolean, getTokenCount?: (text: string) => number): number;
/**
* Single-pass token counter for formatted messages (plain objects with role/content/name).
* Handles text, tool_call, image, and document content types in one iteration,
* then applies Claude correction when applicable.
*/
declare function countFormattedMessageTokens(message: Partial<Record<string, unknown>>, encoding: Parameters<typeof TokenizerSingleton.getTokenCount>[1]): number;
/**
* Exact token count of the tool results a turn retains beyond its last context
* snapshot.
*
* Snapshots are dispatched pre-invoke, so the results of the tools the call they
* precede requested are never in them; normally the next call's snapshot carries
* those results as kept-message context, but a run that stops at the tool-call
* limit makes no next call — the result stays on the response and in no snapshot.
* `priorToolCallIds` are the calls the last snapshot already saw, so a call whose
* id is absent from it belongs to that final, unsnapshotted step. The boundary is
* the call id rather than a content index because completion reshapes the array —
* skill cards are unshifted onto the front and `hide_sequential_outputs` replaces
* it with a filtered one — so any index recorded mid-run means something else by
* the time the turn is saved. A call with no id cannot be placed and is skipped.
*
* Only the tool OUTPUT counts. A call's name and arguments are model output,
* already carried by the snapshot's `completedOutputTokens`, so counting them
* again would double them. The result-message framing the next turn adds (role,
* tool_call_id, per-message overhead) is left out as well: the figure is added to
* exact provider accounting, so it errs low rather than overstating the context.
* Non-string outputs are skipped, matching {@link countFormattedMessageTokens}.
*
* `maxCountChars` bounds the tokenization this turn may cost — one call can request
* several tools, so the budget spans all their results — and `countExact` returns
* `undefined` for content it cannot count exactly. Either limit withdraws the whole
* figure: a turn whose gauge is missing the retained result is better than one
* whose exact figures absorbed a guess. The Claude framing correction is applied,
* matching the counter that produced the snapshot.
*/
declare function countRetainedToolTokens({
  contentParts,
  priorToolCallIds,
  countExact,
  maxCountChars,
  isClaude
}: {
  contentParts: ReadonlyArray<unknown> | null | undefined;
  priorToolCallIds: ReadonlySet<string> | null | undefined;
  countExact: (text: string) => number | undefined;
  maxCountChars?: number;
  isClaude?: boolean;
}): number | undefined;
/** The tool calls a content array carries, by provider id — the boundary a later
*  save path measures "retained past this snapshot" against. */
declare function collectToolCallIds(contentParts: ReadonlyArray<unknown> | null | undefined): Set<string>;
declare function createTokenCounter(encoding: Parameters<typeof TokenizerSingleton.getTokenCount>[1]): TokenCounter$1;
declare function createCachedTokenCounter(encoding: Parameters<typeof TokenizerSingleton.getTokenCount>[1]): Promise<TokenCounter$1>;
declare function logToolError(_graph: unknown, error: unknown, toolId: string): void;
/** Finds the primary agent ID within a set of agent IDs (no suffix or lowest suffix number) */
declare function findPrimaryAgentId(agentIds: Set<string>): string | null;
/**
* Creates a mapMethod for getMessagesForConversation that processes agent content.
* - Strips agentId/groupId metadata from all content
* - For parallel agents (addedConvo with groupId): filters each group to its primary agent
* - For handoffs (agentId without groupId): keeps all content from all agents
* - For multi-agent: applies agent labels to content
*
* The key distinction:
* - Parallel execution (addedConvo): Parts have both agentId AND groupId
* - Handoffs: Parts only have agentId, no groupId
*/
declare function createMultiAgentMapper(primaryAgent: Agent, agentConfigs?: Map<string, Agent>): (message: TMessage) => TMessage;
//#endregion
//#region src/agents/config.d.ts
/**
* Resolves the effective recursion limit for an agent run via a 3-step cascade:
* 1. YAML endpoint config default (falls back to 50)
* 2. Per-agent DB override (if set and positive)
* 3. Global max cap from YAML (if set and positive)
*/
declare function resolveRecursionLimit(agentsEConfig: Partial<TAgentsEndpoint> | undefined, agent: {
  recursion_limit?: number;
} | undefined): number;
/**
* Resolves a subagent's `maxTurns` so its graph `recursionLimit`
* (`maxTurns * SUBAGENT_RECURSION_MULTIPLIER` in the SDK) tracks the same
* resolved recursion limit as a top-level run. Without this, subagents ignore
* both the YAML `recursionLimit`/`maxRecursionLimit` and the per-agent
* `recursion_limit`, always running at the SDK default of 75 graph steps.
*
* `floor` keeps the effective graph limit at or below the resolved value, which
* (since `resolveRecursionLimit` already caps at `maxRecursionLimit`) also keeps
* it within the admin cap — so a lowered limit applies to subagents too, and
* `maxTurns * 3` never overshoots the ceiling. A resolved limit below the
* multiplier yields 0 turns: like a top-level run with `recursionLimit < 3`, the
* child can't take a full step, and the SDK returns a graceful recursion error
* rather than silently granting more steps than the cap allows.
*/
declare function resolveSubagentMaxTurns(agentsEConfig: Partial<TAgentsEndpoint> | undefined, agent: {
  recursion_limit?: number;
} | undefined): number;
/** Mirrors `StreamLimits` in `@librechat/agents` (agents#381). */
interface StreamLimitsConfig {
  maxToolCallArgBytes?: number;
  maxToolCallArgBytesByTool?: Record<string, number>;
  maxDeltaEventsPerTurn?: number;
}
/**
* Maps the librechat.yaml stream circuit-breaker fields
* (`endpoints.agents.maxToolCallArgBytes` / `maxToolCallArgBytesByTool` /
* `maxDeltaEventsPerTurn`) to the SDK's `RunConfig.streamLimits`. Unset
* global fields keep the SDK defaults (64 KiB per streamed tool call's
* arguments, per-turn delta event cap off), while the per-tool map always
* ships the create_file override; a yaml entry for the same tool wins. Value
* normalization (0 disables, NaN falls back) lives in the SDK's
* `resolveStreamLimits`.
*/
declare function resolveStreamLimits(agentsEConfig: Partial<TAgentsEndpoint> | undefined): StreamLimitsConfig;
//#endregion
//#region src/agents/checkpoints/saver.d.ts
declare const LIBRECHAT_CHECKPOINT_OWNER_KEY = "__librechat_checkpoint_owner";
declare const LIBRECHAT_LEGACY_CHECKPOINT_KEY = "__librechat_legacy_checkpoint_id";
/** Isolate physical storage by owner while preserving logical SDK references. */
declare class OwnedMongoSaver extends MongoDBSaver {
  private recordStorage;
  private tuple;
  override getTuple(config: RunnableConfig): Promise<CheckpointTuple | undefined>;
  override list(config: RunnableConfig, options?: CheckpointListOptions): AsyncGenerator<CheckpointTuple>;
  override put(config: RunnableConfig, checkpoint: Checkpoint, metadata: CheckpointMetadata): Promise<RunnableConfig>;
  override putWrites(config: RunnableConfig, writes: PendingWrite[], taskId: string): Promise<void>;
}
//#endregion
//#region src/stream/checkpoints.d.ts
declare const DEFAULT_CHECKPOINT_TTL_SECONDS: number;
/** Stable prefix derived only from the authenticated owner identity. */
declare function checkpointOwnerNamespacePrefix(userId: string, tenantId?: string): string;
//#endregion
//#region src/agents/checkpoints/config.d.ts
/** Checkpointer settings with all defaults applied. */
interface ResolvedCheckpointerConfig {
  type: "mongo" | "memory";
  /** Approval window / TTL in seconds. */
  ttlSeconds: number;
  checkpointCollectionName: string;
  checkpointWritesCollectionName: string;
}
/**
* Apply defaults to the YAML `endpoints.agents.checkpointer` block. Mirrors
* {@link resolveRecursionLimit} — the schema stays descriptive, defaults live here.
*/
declare function resolveCheckpointerConfig(cfg: TCheckpointerConfig | undefined): ResolvedCheckpointerConfig;
//#endregion
//#region src/agents/checkpoints/storage.d.ts
declare const LIBRECHAT_CHECKPOINT_STORAGE_OWNER_KEY = "__librechat_checkpoint_storage_owner";
/** Descriptors locate stores; caller ownership and captured identities authorize deletion. */
declare function checkpointStorageConfigs(userId: string, tenantId: string | undefined, cfg?: TCheckpointerConfig): Promise<ResolvedCheckpointerConfig[]>;
//#endregion
//#region src/agents/checkpointer.d.ts
/**
* LangGraph reserves `checkpoint_ns` for nested graph namespaces and forcibly
* resets a non-empty value to `''` for every root invocation. Carry TerraMind's
* immutable generation scope on a private configurable key instead; the saver
* adapter below maps it into Mongo's storage namespace without changing the
* graph-visible conversation `thread_id`.
*/
declare const LIBRECHAT_CHECKPOINT_NAMESPACE_KEY = "__librechat_checkpoint_ns";
/** Marks a checkpoint write as belonging to an isolated event-actor attempt.
* Unlike ordinary clean chat exits, these exits are durable candidate heads. */
declare const LIBRECHAT_EVENT_ACTOR_INVOCATION_KEY = "__librechat_event_actor_invocation_id";
/**
* Reject a checkpoint whose serialized state exceeds this. The pause is unrecoverable either
* way (the document can't be written), so failing here as a typed {@link CheckpointTooLargeError}
* turns an opaque `BSONObjectTooLarge` crash into an actionable one.
*/
declare const CHECKPOINT_HARD_LIMIT_BYTES: number;
/**
* Warn once a persisted checkpoint crosses this soft threshold (~50% of the ceiling), so a
* conversation's checkpoint growth is visible in logs well before it reaches the hard limit.
*/
declare const CHECKPOINT_WARN_BYTES: number;
/**
* A durable checkpoint whose serialized state exceeds {@link CHECKPOINT_HARD_LIMIT_BYTES} — more
* than MongoDB can hold in a single document. Thrown BEFORE the doomed write so the run fails
* with a clear, typed message instead of a raw driver `BSONObjectTooLarge`. The checkpoint cannot
* be persisted regardless of how it is handled upstream, so a durable resume is impossible.
*/
declare class CheckpointTooLargeError extends Error {
  readonly bytes: number;
  readonly limit: number;
  readonly threadId?: string | undefined;
  readonly code = "CHECKPOINT_TOO_LARGE";
  constructor(bytes: number, limit: number, threadId?: string | undefined);
}
/**
* Construction options for {@link LazyMongoSaver}: the base saver options plus optional
* size-guard overrides. The overrides default to the module thresholds and exist so tests can
* exercise the guard at small sizes; production always uses the defaults.
*/
type LazyMongoSaverOptions = ConstructorParameters<typeof MongoDBSaver>[0] & {
  /** Soft warn threshold in bytes. Defaults to {@link CHECKPOINT_WARN_BYTES}. */warnBytes?: number; /** Hard reject limit in bytes. Defaults to {@link CHECKPOINT_HARD_LIMIT_BYTES}. */
  hardLimitBytes?: number;
};
declare class LazyMongoSaver extends OwnedMongoSaver {
  /** checkpoint id → time the resumable `putWrites` anchoring it arrived; consumed by `put`. */
  private readonly writeAnchorIds;
  /** checkpoint id → time its anchored `put` persisted it, so a bookkeeping batch that lands
  * after the `put` (concurrent dispatch) is forwarded instead of buffered forever. */
  private readonly persistedIds;
  /** checkpoint id → bookkeeping batches parked until the checkpoint persists or is discarded. */
  private readonly bufferedBookkeeping;
  /** Soft threshold (bytes) past which a persisted checkpoint is warned about. */
  private readonly warnBytes;
  /** Hard limit (bytes) past which a checkpoint is refused with {@link CheckpointTooLargeError}. */
  private readonly hardLimitBytes;
  constructor(options: LazyMongoSaverOptions);
  /**
  * LangGraph normalizes every root invocation to `checkpoint_ns: ''` before
  * touching the saver. Map TerraMind's private generation key into Mongo's
  * namespace at this storage boundary, then restore the graph-visible config
  * on the way out. This keeps callbacks/tools on the real conversation
  * `thread_id` while making replacement generations physically disjoint.
  */
  override getTuple(config: RunnableConfig): Promise<CheckpointTuple | undefined>;
  override list(config: RunnableConfig, options?: CheckpointListOptions): AsyncGenerator<CheckpointTuple>;
  override putWrites(config: RunnableConfig, writes: PendingWrite[], taskId: string): Promise<void>;
  override put(config: RunnableConfig, checkpoint: Checkpoint, metadata: CheckpointMetadata): Promise<RunnableConfig>;
  /**
  * Track a checkpoint id whose `put` must persist it. Evicts ONLY genuinely-stale ids
  * (older than {@link WRITE_ANCHOR_STALE_MS}, i.e. from a crashed run whose `put` never
  * landed) — never a recent in-flight id — so a slow-I/O interrupt `put` is never
  * mis-classified as a clean exit. If nothing is stale the map is allowed to grow rather
  * than drop a valid id; the next sweep reclaims the crashed ones.
  */
  private recordWriteAnchor;
  /**
  * Forward the bookkeeping batches parked for `checkpointId` while its fate was undecided,
  * now that the checkpoint is being persisted. Snapshot-and-delete before awaiting so a batch
  * that arrives afterwards can't be double-forwarded — by then the anchor/persisted marker is
  * set, so it forwards directly instead of parking. Shared by the anchoring `putWrites` and by
  * `put` (for a batch parked during the size-check serialization window).
  */
  private flushBufferedBookkeeping;
  /**
  * Measure the checkpoint's serialized size on the persist path and act on it: `debug`-log it,
  * `warn` past {@link warnBytes}, and throw {@link CheckpointTooLargeError} past
  * {@link hardLimitBytes} — BEFORE the write, so an oversize pause fails legibly rather than as a
  * raw `BSONObjectTooLarge`. Serializes with the same `serde` the base `put` uses, so the measured
  * bytes match what would be stored. The extra serialization runs only when a checkpoint is
  * selected for durable retention: HITL pauses and event-actor invocation heads.
  */
  private assertCheckpointFitsDocument;
}
/**
* Exact checkpoint ids present before a legacy, unscoped generation is claimed.
*
* New jobs delete their immutable saver scope wholesale at terminal ownership.
* Legacy jobs share storage, so cleanup deletes only this captured set; a later
* replacement's fresh checkpoint ids cannot be removed by the delayed cleanup.
*/
interface AgentCheckpointGeneration {
  threadId: string;
  /** Nonempty saver-level generation scope. Missing means a legacy
  * thread-wide capture; an empty string is invalid because it would omit the
  * legacy generation's nested LangGraph namespaces during deletion. */
  checkpointNamespace?: string;
  checkpointIds: string[];
}
/** Approval-window milliseconds from the resolved config; drives pending-action expiry. */
declare function getApprovalTtlMs(cfg: TCheckpointerConfig | undefined): number;
/**
* Prove that the durable saver contains a complete interrupt checkpoint for one generation.
*
* A pending Redis action is useful only when LangGraph can reload the state it
* interrupted. Read the exact checkpoint selected by the current interrupt and
* require its matching interrupt id, so an older retained pause cannot satisfy
* verification for a missing or misrouted re-pause.
* This runs once per interrupt, never on the ordinary generation path.
*/
declare function hasDurableAgentInterruptCheckpoint(threadId: string, cfg?: TCheckpointerConfig, options?: {
  checkpointNamespace?: string;
  checkpointId: string;
  checkpointNs?: string;
  interruptId: string;
}): Promise<boolean>;
/**
* The durable saver to hand to `graphConfig.compileOptions.checkpointer`, or
* `undefined` to let the SDK fall back to its in-process `MemorySaver`.
*
* Returns `undefined` (without caching) when the config selects `memory` or when
* Mongo isn't connected yet, so a later run retries once the connection is up.
* The SDK types the checkpointer as `unknown`, so a `MongoDBSaver` passes directly.
*/
declare function getAgentCheckpointer(cfg: TCheckpointerConfig | undefined): Promise<MongoDBSaver | undefined>;
interface AgentEventCheckpointReference {
  threadId: string;
  checkpointId: string;
  checkpointNs: string;
}
interface AgentEventCheckpointMessageOverlay {
  source: string;
  messages: readonly BaseMessage[];
}
/** Copies one committed actor head into an invocation-owned namespace. */
declare function forkAgentEventCheckpoint(source: AgentEventCheckpointReference, checkpointNs: string, invocationId: string, cfg?: TCheckpointerConfig, messageOverlay?: AgentEventCheckpointMessageOverlay, owner?: string): Promise<AgentEventCheckpointReference | null>;
/** Historical evidence authorizes only this exact ID, never the whole legacy namespace. */
declare function deleteAgentEventCheckpointReference(reference: AgentEventCheckpointReference, cfg?: TCheckpointerConfig, owner?: string): Promise<boolean>;
/** Consume exact historical proofs in bounded batches, retaining intent on any failure. */
declare function deleteAgentEventCheckpointReferences(references: readonly AgentEventCheckpointReference[], owner: string, cfg?: TCheckpointerConfig): Promise<void>;
/** Reads the terminal checkpoint produced inside one invocation namespace. */
declare function captureAgentEventCheckpoint(threadId: string, checkpointNs: string, invocationId: string, cfg?: TCheckpointerConfig, owner?: string, legacyCheckpointId?: string): Promise<AgentEventCheckpointReference | null>;
/**
* `MongoDBSaver.setup()` starts the compound and TTL builds of each collection
* concurrently and reports failures instead of throwing. Amazon DocumentDB
* admits one index build per collection at a time, so there the second build of
* each pair is rejected (code 40333) and the saver would otherwise run with its
* TTL index missing — checkpoints then never expire. `setup()` is idempotent (an
* index that already exists is a no-op), so re-running it lets one more build
* through per pass until every index exists. Every other error is returned for
* the caller to log, exactly as `setup()` reports it — including those reported
* beside a conflict that outlasts the deadline. A rejection of `setup()` itself
* propagates, so the caller keeps its in-process fallback.
*/
declare function setupCheckpointIndexes(saver: Pick<MongoDBSaver, "setup">, options?: IndexBuildOptions): Promise<Error[]>;
/**
* Snapshot the durable checkpoint ids that belong to the generation about to
* resume. Capture this before atomically claiming the paused job; a replacement
* that wins before the claim makes that claim fail, while one that starts after
* the claim writes ids outside this snapshot.
*/
declare function captureAgentCheckpointGeneration(threadId: string, cfg?: TCheckpointerConfig, options?: {
  throwOnError?: boolean;
  checkpointNamespace?: string;
}): Promise<AgentCheckpointGeneration>;
/**
* Prune a thread's checkpoints on a terminal transition — natural completion,
* abort, or expiry — so the durable store stays bounded. The TTL index is the
* safety net; this is the eager cleanup. No-op in memory mode or before any run
* has built the saver (nothing to delete).
*
* @param threadId - the LangGraph `thread_id` (TerraMind's conversationId).
* @param generation - when present, delete only the checkpoint ids captured for
* this resumed generation; omitted by legacy callers that intentionally prune
* the untagged legacy rows on the thread.
*/
declare function deleteAgentCheckpoint(threadId: string | undefined, cfg?: TCheckpointerConfig, generation?: AgentCheckpointGeneration, options?: {
  throwOnError?: boolean;
  checkpointNamespace?: string;
}): Promise<void>;
/** Erase only payload rows carrying this authenticated actor owner. */
declare function deleteOwnedActorCheckpointScope(threadId: string, checkpointNs: string, owner: string, cfg?: TCheckpointerConfig): Promise<void>;
/** Delete durable rows by authenticated ownership, independently of job/receipt lifetimes. */
declare function deleteOwnedAgentCheckpoints(userId: string, tenantId: string | undefined, conversationIds: readonly string[] | undefined, cfg?: TCheckpointerConfig): Promise<void>;
/** Test-only: drop the memoized saver so a fresh build is forced. */
declare function __resetCheckpointerForTests(): void;
//#endregion
//#region src/agents/checkpoints/deletion.d.ts
interface CheckpointDeletion {
  conversationIds(): string[];
  remember(conversationIds: readonly string[]): Promise<void>;
  cleanup(): Promise<void>;
  acknowledge(): Promise<void>;
}
/** Durable deletion intent preserves cascade identity after topology is removed.
* These records contain no checkpoint payload and exist only during deletion,
* not once per generation. They must survive until both drain and cleanup succeed. */
declare function openCheckpointDeletion(userId: string, tenantId: string | undefined, rootConversationId: string | undefined, cfg?: TCheckpointerConfig): Promise<CheckpointDeletion>;
/** Replay captured identities after topology deletion; never sweep new generations. */
declare function createCheckpointDeletionReclaimer(getOwnerJobs: (userId: string, tenantId?: string) => Promise<string[]>): (limit: number) => Promise<number>;
//#endregion
//#region src/agents/compatibility.d.ts
declare const AGENT_CONTEXT_FINGERPRINT_VERSION = 1;
declare const AGENT_GRAPH_SCHEMA_VERSION = 1;
declare const AGENT_CHECKPOINT_FORMAT_VERSION = 1;
interface AgentContextFingerprint {
  algorithm: "sha256";
  version: number;
  digest: string;
}
interface AgentContextSkillIdentity {
  id: string;
  name: string;
  version: number;
  contentDigest?: string;
}
interface AgentContextMemorySnapshot {
  scope: string;
  withKeys?: string;
  withoutKeys?: string;
}
interface AgentContextDefinition {
  id: string;
  version?: number | string;
  provider?: string;
  model?: string;
  instructions?: string;
  additionalInstructions?: string;
  modelParameters?: object;
  toolDefinitions?: readonly object[];
  toolRegistryDefinitions?: readonly object[];
  toolOptions?: object;
  execution?: object;
  skills?: readonly AgentContextSkillIdentity[];
}
interface AgentTurnSemanticContext {
  agents: readonly AgentContextDefinition[];
  approvalPolicy?: object;
  retainedAnswers?: {
    enabled: boolean;
    maxTokens: number;
  };
  memory?: readonly AgentContextMemorySnapshot[];
  checkpointerType?: string;
  discoveredToolNames?: readonly string[];
  checkpointFormatVersion?: number;
  graphSchemaVersion?: number;
}
interface InitializedAgentContextSource {
  id: string;
  version?: number | string;
  provider?: string;
  model?: string;
  instructions?: string;
  additional_instructions?: string;
  model_parameters?: object;
  toolDefinitions?: readonly object[];
  toolRegistryDefinitions?: readonly object[];
  tool_options?: object;
  execution?: object;
  manualSkillPrimes?: readonly {
    _id: {
      toString(): string;
    } | string;
    name: string;
    version?: number;
    body?: string;
  }[];
  alwaysApplySkillPrimes?: readonly {
    _id: {
      toString(): string;
    } | string;
    name: string;
    version?: number;
    body?: string;
  }[];
}
declare const MAX_AGENT_CONTEXT_SKILLS = 64;
declare function normalizeAgentEventActorDiscoveredTools(names: readonly string[] | undefined): string[];
/**
* The summary a run records as event-actor state, stamped with the provenance
* version. The version records the writer, not a judgement about this summary:
* a build that reaches here has already filtered unusable summaries out of
* every source it reads — the turn's own content parts, a validated restore,
* or the formatter over the stripped payload. Stamping at the point state is
* assembled is what keeps an inherited `{ text, tokenCount }` from the SDK
* from being refused by the next event and forcing a cold reload.
*/
declare function createAgentEventActorSummary(summary: {
  text: string;
  tokenCount: number;
} | null | undefined): IAgentEventActorSummary | undefined;
/**
* A stored event-actor summary a warm continuation may carry forward. Throws
* for anything it cannot vouch for, including a state written before the
* version existed: those kept only `{ text, tokenCount }`, so a round that
* failed or never finished reads exactly like a checkpoint, and a warm run
* would continue from a truncated prefix. Refusing one costs a cold
* continuation, which rebuilds context from durable history.
*/
declare function normalizeAgentEventActorSummary(summary: IAgentEventActorSummary | null | undefined): IAgentEventActorSummary | undefined;
declare function createSkillContentDigest(body: string): string;
/** Hashes only semantic, model-bound context; request and delivery metadata never enter this module. */
declare function createAgentContextFingerprint(input: AgentTurnSemanticContext): AgentContextFingerprint;
declare function agentContextFingerprintsMatch(stored: AgentContextFingerprint | undefined, current: AgentContextFingerprint): boolean;
/** Projects initialized runtime facts into the single semantic compatibility module. */
declare function createInitializedAgentContextFingerprint(input: {
  agents: readonly InitializedAgentContextSource[];
  invokedSkills?: readonly AgentContextSkillIdentity[];
  approvalPolicy?: object;
  retainedAnswers?: {
    enabled: boolean;
    maxTokens: number;
  };
  memory?: readonly AgentContextMemorySnapshot[];
  checkpointerType?: string;
  discoveredToolNames?: readonly string[];
}): AgentContextFingerprint;
//#endregion
//#region src/agents/compaction.d.ts
/** Text of a summary content part, in any persisted shape — `content` blocks
*  today, a string `content` or a bare `text` on rows written before them.
*  Empty for anything else. */
declare function getSummaryPartText(part: TMessageContentParts | null | undefined): string;
/**
* A summary that can stand for the history it covers: it carries text, and its
* round both finished and did not error. A round that failed or was cut off
* keeps whatever deltas it streamed, so its text is a truncated prefix of the
* history it was summarizing rather than a checkpoint for it — the same test
* `isCompactedLeaf` applies when deciding whether a compaction can be retried.
*
* `failed` has only been stamped since the server began recording errored
* rounds, so the flags alone cannot vouch for older rows. The aggregator gives
* the structural answer: deltas stream `content` blocks into the part, and only
* a completed round replaces it with the final block, which is the only writer
* of `boundary`. A `content`-block summary without one therefore never
* finished, however it was stored. Rows in the bare-`text` shape predate that
* aggregator and are left to the flags.
*/
declare function isUsableSummaryPart(part: unknown): part is SummaryContentPart;
/**
* The summary a message offers as the conversation's checkpoint: the last
* usable one in its content (last-summary-wins). Null when the message carries
* none — an empty or failed summary leaves the history it hangs off in place.
*/
declare function findCheckpointSummaryPart(content: unknown): SummaryContentPart | null;
/**
* The summary a warm event-actor continuation carries forward: the last usable
* one in the run's content parts, stamped as actor state. A failed or
* unfinished round's partial deltas would otherwise be persisted as actor state
* and handed to the next run as its `initialSummary`, which skips durable
* history entirely. A missing or invalid token count is recorded as zero.
*/
declare function getLatestEventActorSummary(contentParts: unknown): IAgentEventActorSummary | undefined;
/**
* The content fields a failed turn is persisted with. A manual compaction owns
* its identity through content, so its row carries the marked failure; every
* other failed turn contributes nothing and keeps its text-only shape. Callers
* spread the result rather than deciding which turns are compactions.
*/
declare function resolveFailedTurnContent(requestBody: {
  compact?: boolean;
} | null | undefined, errorText: string): {
  content?: TMessageContentParts[];
};
/**
* Stamps `initiatedBy: 'user'` on the part that carries a manual compaction's
* outcome, which is the turn's only record of having been one: the run emits no
* text of its own, and a compaction hangs off whatever leaf the branch ends
* with, so a reader cannot infer it from the turn's shape or its parent.
*
* Every outcome is marked. A run that produced a summary marks it; a run that
* recorded why it could not (an error part, e.g. a skipped compaction) marks
* that instead; a run that produced neither records the typed failure here, so
* it persists and streams like any other failed compaction rather than as an
* empty assistant message. A cancelled run keeps failing as a typed error: the
* turn stopped early rather than failing, and the abort path owns it.
*/
declare function markCompactionOutcome(contentParts: TMessageContentParts[], {
  aborted
}?: {
  aborted?: boolean;
}): void;
/**
* Points one model-facing message at content free of the summary parts that
* cannot bound history, and reports whether anything went. The SDK's summary
* scan takes the last summary part carrying text as the conversation's history
* boundary and drops every message before it, reading neither `failed` nor
* `summarizing`: a round that errored or was cut off keeps the deltas it
* streamed, so leaving that part in would replace the history it never
* finished summarizing with the prefix it produced. An empty summary is left
* alone — it bounds nothing, and the renderer owns how it appears.
*
* The message gets a NEW content array rather than a spliced one, because a
* formatted prompt copy shares its content array with the stored message it
* came from: splicing would reindex the persisted row's parts under every
* reader that holds it. This belongs on a prompt copy before its token count
* is taken, so the count, the prompt total an admission check reads, and any
* later per-index adjustment all describe what the model actually receives.
*/
declare function dropUnusableSummaryParts(message: {
  content?: unknown;
}): boolean;
/**
* The same rule for a payload whose messages the caller may not touch:
* returns a payload of messages carrying no unusable summary part, leaving the
* input and its messages untouched and returning the same reference when
* nothing needed dropping. Message positions are preserved, so an index-keyed
* token map stays aligned.
*/
declare function stripUnusableSummaryParts<T extends {
  content?: unknown;
}>(payload: T[]): T[];
declare function createCompactionSemanticIndexProjection(input: CompactionSemanticIndex | CompactionSemanticIndexSnapshot | undefined): ICompactionSemanticIndexProjection | undefined;
declare function restoreCompactionSemanticIndexSnapshot(projection: ICompactionSemanticIndexProjection | null | undefined): CompactionSemanticIndexSnapshot | undefined;
declare function restoreCompactionSemanticIndex(projection: ICompactionSemanticIndexProjection | null | undefined): CompactionSemanticIndex | undefined;
//#endregion
//#region src/agents/contact.d.ts
interface AgentContactSource {
  authorName?: string | null;
  support_contact?: {
    name?: string | null;
    email?: string | null;
  } | null;
}
interface AgentOwnerContactSource {
  name?: string | null;
  username?: string | null;
}
declare const hasSupportContact: (agent: AgentContactSource) => boolean;
/**
* Resolves a display-only owner contact for agents without an explicit support contact.
* Never includes the owner's account email, nor email-shaped display names; emails are
* only exposed when the owner opts in by configuring `support_contact`.
*/
declare function resolveAgentOwnerContact(agent: AgentContactSource, owner: AgentOwnerContactSource | null): AgentOwnerContact | undefined;
//#endregion
//#region src/agents/context.d.ts
/**
* Agent type with optional tools array that can contain DynamicStructuredTool or string.
* For context operations, we only require id and instructions, other Agent fields are optional.
*/
type AgentWithTools = Pick<Agent, "id"> & Partial<Omit<Agent, "id" | "tools">> & {
  tools?: Array<DynamicStructuredTool | string>; /** Serializable tool definitions for event-driven mode */
  toolDefinitions?: LCTool$1[];
};
/**
* Extracts unique MCP server names from an agent's tools or tool definitions.
* Supports both full tool instances (tools) and serializable definitions (toolDefinitions).
* @param agent - The agent with tools and/or tool definitions
* @returns Array of unique MCP server names
*/
declare function extractMCPServers(agent: AgentWithTools): string[];
/**
* Fetches MCP instructions for the given server names.
* @param {string[]} mcpServers - Array of MCP server names
* @param {MCPManager} mcpManager - MCP manager instance
* @param {Logger} [logger] - Optional logger instance
* @returns {Promise<string>} MCP instructions string, empty if none
*/
declare function getMCPInstructionsForServers(mcpServers: string[], mcpManager: MCPManager, logger?: Logger, configServers?: Record<string, ParsedServerConfig>): Promise<string>;
/**
* Builds stable instructions for an agent by combining agent-specific context and MCP context.
* Order: baseInstructions -> mcpInstructions
*
* @param {Object} params
* @param {string} [params.baseInstructions] - Agent's base instructions
* @param {string} [params.mcpInstructions] - Agent's MCP server instructions
* @returns {string | undefined} Combined instructions, or undefined if empty
*/
declare function buildAgentInstructions({
  baseInstructions,
  mcpInstructions
}: {
  baseInstructions?: string;
  mcpInstructions?: string;
}): string | undefined;
/**
* Builds dynamic system-tail instructions for an agent.
* Order: existing additional instructions -> shared run context.
*/
declare function buildAgentAdditionalInstructions({
  additionalInstructions,
  sharedRunContext
}: {
  additionalInstructions?: string;
  sharedRunContext?: string;
}): string | undefined;
/**
* Applies run context and MCP instructions to an agent's configuration.
* Mutates the agent object in place.
*
* @param {Object} params
* @param {Agent} params.agent - The agent to update
* @param {string} params.sharedRunContext - Run-level shared context
* @param {MCPManager} params.mcpManager - MCP manager instance
* @param {Object} [params.ephemeralAgent] - Ephemeral agent config (for MCP override)
* @param {string} [params.agentId] - Agent ID for logging
* @param {Logger} [params.logger] - Optional logger instance
* @returns {Promise<void>}
*/
declare function applyContextToAgent({
  agent,
  sharedRunContext,
  mcpManager,
  ephemeralAgent,
  agentId,
  logger,
  configServers
}: {
  agent: AgentWithTools;
  sharedRunContext: string;
  mcpManager: MCPManager;
  ephemeralAgent?: TEphemeralAgent;
  agentId?: string;
  logger?: Logger;
  configServers?: Record<string, ParsedServerConfig>;
}): Promise<void>;
//#endregion
//#region src/agents/control.d.ts
type ControlStore = {
  controlTask(scopeId: string, taskId: string, command: SubagentTaskControlCommand, invocationId: string): Promise<SubagentTaskControlResult>;
};
type Dependencies = Pick<ConversationMethods, "getConvoOwnership" | "getSubagentThreadForParent"> & Pick<MessageMethods, "getMessages" | "getSubagentTaskControlReceipt"> & {
  store: ControlStore;
};
/** Cheap structural admission shared by the Express route and authoritative
* handler. It must run before filters, moderation, or owner routing. */
declare const isValidSubagentControlRequest: (value: unknown) => value is SubagentControlRequest;
/** Applies one parent-authorized control to the live owner and returns only its public receipt. */
declare function createSubagentControlHandler(deps: Dependencies): (req: ServerRequest, res: Response$2) => Promise<void>;
//#endregion
//#region src/agents/conversation.d.ts
/** Never turn another conversation's stored workspace into an explicit run selection. */
declare function resolveRunCodeWorkspaces({
  conversationId,
  requestedSelections,
  conversation
}: {
  conversationId: string;
  requestedSelections?: CodeWorkspaceSelection[] | null;
  conversation?: Pick<TConversation, "conversationId" | "codeWorkspaces"> | null;
}): CodeWorkspaceSelection[] | undefined;
/** Reuse resolved state only for the conversation the run will actually execute. */
declare function resolveRunConversation<TConversation>({
  request,
  conversationId,
  loadConversation
}: {
  request: {
    body?: {
      conversationId?: string;
    };
    resolvedConversation?: TConversation | null;
  };
  conversationId?: string;
  loadConversation: (conversationId: string) => Promise<TConversation | null | undefined>;
}): Promise<TConversation | null | undefined>;
interface ConversationAnchorSource {
  createdAt?: Date | string | number | null;
}
interface ConversationAnchor<TConversation extends ConversationAnchorSource> {
  createdAt: string;
  conversation: TConversation | null | undefined;
}
interface ResolveConversationAnchorOptions<TConversation extends ConversationAnchorSource> {
  isNewConversation: boolean;
  loadConversation: () => Promise<TConversation | null | undefined>;
  now?: () => Date;
  onLoadError?: (error: Error) => void;
}
declare function resolveConversationAnchor<TConversation extends ConversationAnchorSource>({
  isNewConversation,
  loadConversation,
  now,
  onLoadError
}: ResolveConversationAnchorOptions<TConversation>): Promise<ConversationAnchor<TConversation>>;
//#endregion
//#region src/agents/creates.d.ts
type AgentCreateHandler = (req: Request, res: Response$2) => Promise<Response$2 | void> | Response$2 | void;
interface AgentManagementCreateDeps {
  getRoleByName: (roleName: string, fieldsToSelect?: string | string[]) => Promise<IRole | null>;
  createAgent: AgentCreateHandler;
}
/** Validate and authorize Agent Management creation before reusing the browser creation flow. */
declare function createAgentManagementCreateHandler(deps: AgentManagementCreateDeps): (req: Request, res: Response$2) => Promise<Response$2>;
//#endregion
//#region src/agents/validation.d.ts
/**
* Permissive Request alias used by {@link validateAgentModel}. Accepts either
* the default Express `Request` or the project-specific `ServerRequest`
* (see `~/types/http`), whose `params` type is widened to `unknown`.
*/
type LooseRequest = Request<unknown, unknown, unknown>;
declare const agentCodeWorkspaceIdSchema: z.ZodUnion<[z.ZodLiteral<"">, z.ZodString]>;
/** Avatar schema shared between create and update */
declare const agentAvatarSchema: z.ZodObject<{
  filepath: z.ZodString;
  source: z.ZodString;
}, "strip">;
/** Persisted resource schema. Full file records are populated only after database authorization. */
declare const agentBaseResourceSchema: z.ZodObject<{
  file_ids: z.ZodOptional<z.ZodArray<z.ZodString, "many">>;
}, "strip">;
/** File resource schema extends base with vector_store_ids. */
declare const agentFileResourceSchema: z.ZodObject<{
  file_ids: z.ZodOptional<z.ZodArray<z.ZodString, "many">>;
  vector_store_ids: z.ZodOptional<z.ZodArray<z.ZodString, "many">>;
}, "strip">;
/** Persisted tool resources accepted by Agent create and update APIs. */
declare const agentToolResourcesSchema: z.ZodOptional<z.ZodObject<{
  image_edit: z.ZodOptional<typeof agentBaseResourceSchema>;
  execute_code: z.ZodOptional<typeof agentBaseResourceSchema>;
  file_search: z.ZodOptional<typeof agentFileResourceSchema>;
  context: z.ZodOptional<typeof agentBaseResourceSchema>;
  ocr: z.ZodOptional<typeof agentBaseResourceSchema>;
}, "strip">>;
/** Support contact schema for agent */
declare const agentSupportContactSchema: z.ZodOptional<z.ZodObject<{
  name: z.ZodOptional<z.ZodString>;
  email: z.ZodOptional<z.ZodUnion<[z.ZodLiteral<"">, z.ZodString]>>;
}, "strip", z.ZodTypeAny, {
  name?: string | undefined;
  email?: string | undefined;
}, {
  name?: string | undefined;
  email?: string | undefined;
}>>;
/** Graph edge schema for agent handoffs */
declare const graphEdgeSchema: z.ZodObject<{
  from: z.ZodUnion<[z.ZodString, z.ZodArray<z.ZodString, "many">]>;
  to: z.ZodUnion<[z.ZodString, z.ZodArray<z.ZodString, "many">]>;
  description: z.ZodEffects<z.ZodOptional<z.ZodString>, string | undefined, string | undefined>;
  edgeType: z.ZodOptional<z.ZodEnum<["handoff", "direct"]>>;
  prompt: z.ZodEffects<z.ZodOptional<z.ZodUnion<[z.ZodString, z.ZodFunction<z.ZodTuple<[], z.ZodUnknown>, z.ZodUnknown>]>>, string | ((...args: unknown[]) => unknown) | undefined, string | ((...args: unknown[]) => unknown) | undefined>;
  excludeResults: z.ZodOptional<z.ZodBoolean>;
  promptKey: z.ZodEffects<z.ZodOptional<z.ZodString>, string | undefined, string | undefined>;
}, "strip">;
/** Per-tool options schema (defer_loading, allowed_callers, run_in_background, describe_intent) */
declare const toolOptionsSchema: z.ZodObject<{
  defer_loading: z.ZodOptional<z.ZodBoolean>;
  allowed_callers: z.ZodOptional<z.ZodArray<z.ZodEnum<["direct", "code_execution"]>, "many">>;
  run_in_background: z.ZodOptional<z.ZodBoolean>;
  describe_intent: z.ZodOptional<z.ZodBoolean>;
}, "strip">;
/** Agent tool options - map of tool_id to tool options */
declare const agentToolOptionsSchema: z.ZodOptional<z.ZodRecord<z.ZodString, z.ZodObject<{
  defer_loading: z.ZodOptional<z.ZodBoolean>;
  allowed_callers: z.ZodOptional<z.ZodArray<z.ZodEnum<["direct", "code_execution"]>, "many">>;
  run_in_background: z.ZodOptional<z.ZodBoolean>;
  describe_intent: z.ZodOptional<z.ZodBoolean>;
}, "strip", z.ZodTypeAny, {
  defer_loading?: boolean | undefined;
  allowed_callers?: ("direct" | "code_execution")[] | undefined;
  run_in_background?: boolean | undefined;
  describe_intent?: boolean | undefined;
}, {
  defer_loading?: boolean | undefined;
  allowed_callers?: ("direct" | "code_execution")[] | undefined;
  run_in_background?: boolean | undefined;
  describe_intent?: boolean | undefined;
}>>>;
declare const agentSubagentsSchema: z.ZodOptional<z.ZodType<AgentSubagentsConfig>>;
declare const agentGitIdentityUpdateSchema: z.ZodType<AgentGitIdentity | null | undefined>;
declare const agentBaseSchema: z.ZodObject<{
  name: z.ZodOptional<z.ZodNullable<z.ZodString>>;
  description: z.ZodOptional<z.ZodNullable<z.ZodString>>;
  instructions: z.ZodOptional<z.ZodNullable<z.ZodString>>;
  avatar: z.ZodOptional<z.ZodNullable<z.ZodObject<{
    filepath: z.ZodString;
    source: z.ZodString;
  }, "strip", z.ZodTypeAny, {
    source: string;
    filepath: string;
  }, {
    source: string;
    filepath: string;
  }>>>;
  model_parameters: z.ZodOptional<z.ZodRecord<z.ZodString, z.ZodUnknown>>;
  tools: z.ZodOptional<z.ZodArray<z.ZodString, "many">>;
  skills: z.ZodOptional<z.ZodArray<z.ZodString, "many">>;
  skills_enabled: z.ZodOptional<z.ZodBoolean>;
  skill_authoring_enabled: z.ZodOptional<z.ZodBoolean>;
  skills_scope: z.ZodOptional<z.ZodNativeEnum<typeof SkillsScope>>;
  memory_scope: z.ZodOptional<z.ZodNativeEnum<typeof MemoryScope>>; /** @deprecated Use edges instead */
  agent_ids: z.ZodOptional<z.ZodArray<z.ZodString, "many">>;
  edges: z.ZodOptional<z.ZodArray<z.ZodObject<{
    from: z.ZodUnion<[z.ZodString, z.ZodArray<z.ZodString, "many">]>;
    to: z.ZodUnion<[z.ZodString, z.ZodArray<z.ZodString, "many">]>;
    description: z.ZodEffects<z.ZodOptional<z.ZodString>, string | undefined, string | undefined>;
    edgeType: z.ZodOptional<z.ZodEnum<["handoff", "direct"]>>;
    prompt: z.ZodEffects<z.ZodOptional<z.ZodUnion<[z.ZodString, z.ZodFunction<z.ZodTuple<[], z.ZodUnknown>, z.ZodUnknown>]>>, string | ((...args: unknown[]) => unknown) | undefined, string | ((...args: unknown[]) => unknown) | undefined>;
    excludeResults: z.ZodOptional<z.ZodBoolean>;
    promptKey: z.ZodEffects<z.ZodOptional<z.ZodString>, string | undefined, string | undefined>;
  }, "strip", z.ZodTypeAny, {
    from: string | string[];
    to: string | string[];
    description?: string | undefined;
    prompt?: string | ((...args: unknown[]) => unknown) | undefined;
    edgeType?: "direct" | "handoff" | undefined;
    excludeResults?: boolean | undefined;
    promptKey?: string | undefined;
  }, {
    from: string | string[];
    to: string | string[];
    description?: string | undefined;
    prompt?: string | ((...args: unknown[]) => unknown) | undefined;
    edgeType?: "direct" | "handoff" | undefined;
    excludeResults?: boolean | undefined;
    promptKey?: string | undefined;
  }>, "many">>;
  end_after_tools: z.ZodOptional<z.ZodBoolean>;
  hide_sequential_outputs: z.ZodOptional<z.ZodBoolean>;
  stateful_code_sessions: z.ZodOptional<z.ZodBoolean>;
  stateful_code_environment: z.ZodOptional<z.ZodEnum<["user", "agent-user", "conversation"]>>;
  code_environment_id: z.ZodOptional<z.ZodString>;
  code_workspace_id: z.ZodOptional<typeof agentCodeWorkspaceIdSchema>;
  git_identity: typeof agentGitIdentitySchema;
  artifacts: z.ZodOptional<z.ZodString>;
  recursion_limit: z.ZodOptional<z.ZodNumber>;
  conversation_starters: z.ZodOptional<z.ZodArray<z.ZodString, "many">>;
  tool_resources: typeof agentToolResourcesSchema;
  tool_options: z.ZodOptional<z.ZodRecord<z.ZodString, z.ZodObject<{
    defer_loading: z.ZodOptional<z.ZodBoolean>;
    allowed_callers: z.ZodOptional<z.ZodArray<z.ZodEnum<["direct", "code_execution"]>, "many">>;
    run_in_background: z.ZodOptional<z.ZodBoolean>;
    describe_intent: z.ZodOptional<z.ZodBoolean>;
  }, "strip", z.ZodTypeAny, {
    defer_loading?: boolean | undefined;
    allowed_callers?: ("direct" | "code_execution")[] | undefined;
    run_in_background?: boolean | undefined;
    describe_intent?: boolean | undefined;
  }, {
    defer_loading?: boolean | undefined;
    allowed_callers?: ("direct" | "code_execution")[] | undefined;
    run_in_background?: boolean | undefined;
    describe_intent?: boolean | undefined;
  }>>>;
  subagents: typeof agentSubagentsSchema;
  support_contact: z.ZodOptional<z.ZodObject<{
    name: z.ZodOptional<z.ZodString>;
    email: z.ZodOptional<z.ZodUnion<[z.ZodLiteral<"">, z.ZodString]>>;
  }, "strip", z.ZodTypeAny, {
    name?: string | undefined;
    email?: string | undefined;
  }, {
    name?: string | undefined;
    email?: string | undefined;
  }>>;
  category: z.ZodOptional<z.ZodString>;
}, "strip">;
/** Create schema extends base with required fields for creation */
declare const agentCreateSchema: z.ZodObject<{
  name: z.ZodOptional<z.ZodNullable<z.ZodString>>;
  description: z.ZodOptional<z.ZodNullable<z.ZodString>>;
  instructions: z.ZodOptional<z.ZodNullable<z.ZodString>>;
  avatar: z.ZodOptional<z.ZodNullable<z.ZodObject<{
    filepath: z.ZodString;
    source: z.ZodString;
  }, "strip", z.ZodTypeAny, {
    source: string;
    filepath: string;
  }, {
    source: string;
    filepath: string;
  }>>>;
  model_parameters: z.ZodOptional<z.ZodRecord<z.ZodString, z.ZodUnknown>>;
  skills: z.ZodOptional<z.ZodArray<z.ZodString, "many">>;
  skills_enabled: z.ZodOptional<z.ZodBoolean>;
  skill_authoring_enabled: z.ZodOptional<z.ZodBoolean>;
  skills_scope: z.ZodOptional<z.ZodNativeEnum<typeof SkillsScope>>;
  memory_scope: z.ZodOptional<z.ZodNativeEnum<typeof MemoryScope>>;
  agent_ids: z.ZodOptional<z.ZodArray<z.ZodString, "many">>;
  edges: z.ZodOptional<z.ZodArray<z.ZodObject<{
    from: z.ZodUnion<[z.ZodString, z.ZodArray<z.ZodString, "many">]>;
    to: z.ZodUnion<[z.ZodString, z.ZodArray<z.ZodString, "many">]>;
    description: z.ZodEffects<z.ZodOptional<z.ZodString>, string | undefined, string | undefined>;
    edgeType: z.ZodOptional<z.ZodEnum<["handoff", "direct"]>>;
    prompt: z.ZodEffects<z.ZodOptional<z.ZodUnion<[z.ZodString, z.ZodFunction<z.ZodTuple<[], z.ZodUnknown>, z.ZodUnknown>]>>, string | ((...args: unknown[]) => unknown) | undefined, string | ((...args: unknown[]) => unknown) | undefined>;
    excludeResults: z.ZodOptional<z.ZodBoolean>;
    promptKey: z.ZodEffects<z.ZodOptional<z.ZodString>, string | undefined, string | undefined>;
  }, "strip", z.ZodTypeAny, {
    from: string | string[];
    to: string | string[];
    description?: string | undefined;
    prompt?: string | ((...args: unknown[]) => unknown) | undefined;
    edgeType?: "direct" | "handoff" | undefined;
    excludeResults?: boolean | undefined;
    promptKey?: string | undefined;
  }, {
    from: string | string[];
    to: string | string[];
    description?: string | undefined;
    prompt?: string | ((...args: unknown[]) => unknown) | undefined;
    edgeType?: "direct" | "handoff" | undefined;
    excludeResults?: boolean | undefined;
    promptKey?: string | undefined;
  }>, "many">>;
  end_after_tools: z.ZodOptional<z.ZodBoolean>;
  hide_sequential_outputs: z.ZodOptional<z.ZodBoolean>;
  stateful_code_sessions: z.ZodOptional<z.ZodBoolean>;
  stateful_code_environment: z.ZodOptional<z.ZodEnum<["user", "agent-user", "conversation"]>>;
  code_environment_id: z.ZodOptional<z.ZodString>;
  git_identity: typeof agentGitIdentitySchema;
  code_workspace_id: z.ZodOptional<typeof agentCodeWorkspaceIdSchema>;
  artifacts: z.ZodOptional<z.ZodString>;
  recursion_limit: z.ZodOptional<z.ZodNumber>;
  conversation_starters: z.ZodOptional<z.ZodArray<z.ZodString, "many">>;
  tool_resources: typeof agentToolResourcesSchema;
  tool_options: z.ZodOptional<z.ZodRecord<z.ZodString, z.ZodObject<{
    defer_loading: z.ZodOptional<z.ZodBoolean>;
    allowed_callers: z.ZodOptional<z.ZodArray<z.ZodEnum<["direct", "code_execution"]>, "many">>;
    run_in_background: z.ZodOptional<z.ZodBoolean>;
    describe_intent: z.ZodOptional<z.ZodBoolean>;
  }, "strip", z.ZodTypeAny, {
    defer_loading?: boolean | undefined;
    allowed_callers?: ("direct" | "code_execution")[] | undefined;
    run_in_background?: boolean | undefined;
    describe_intent?: boolean | undefined;
  }, {
    defer_loading?: boolean | undefined;
    allowed_callers?: ("direct" | "code_execution")[] | undefined;
    run_in_background?: boolean | undefined;
    describe_intent?: boolean | undefined;
  }>>>;
  subagents: typeof agentSubagentsSchema;
  support_contact: z.ZodOptional<z.ZodObject<{
    name: z.ZodOptional<z.ZodString>;
    email: z.ZodOptional<z.ZodUnion<[z.ZodLiteral<"">, z.ZodString]>>;
  }, "strip", z.ZodTypeAny, {
    name?: string | undefined;
    email?: string | undefined;
  }, {
    name?: string | undefined;
    email?: string | undefined;
  }>>;
  category: z.ZodOptional<z.ZodString>;
} & {
  provider: z.ZodString;
  model: z.ZodNullable<z.ZodString>;
  tools: z.ZodDefault<z.ZodOptional<z.ZodArray<z.ZodString, "many">>>;
}, "strip">;
/** Update schema extends base with all fields optional and additional update-only fields */
declare const agentUpdateSchema: z.ZodObject<{
  name: z.ZodOptional<z.ZodNullable<z.ZodString>>;
  description: z.ZodOptional<z.ZodNullable<z.ZodString>>;
  instructions: z.ZodOptional<z.ZodNullable<z.ZodString>>;
  model_parameters: z.ZodOptional<z.ZodRecord<z.ZodString, z.ZodUnknown>>;
  tools: z.ZodOptional<z.ZodArray<z.ZodString, "many">>;
  skills: z.ZodOptional<z.ZodArray<z.ZodString, "many">>;
  skills_enabled: z.ZodOptional<z.ZodBoolean>;
  skill_authoring_enabled: z.ZodOptional<z.ZodBoolean>;
  skills_scope: z.ZodOptional<z.ZodNativeEnum<typeof SkillsScope>>;
  memory_scope: z.ZodOptional<z.ZodNativeEnum<typeof MemoryScope>>;
  agent_ids: z.ZodOptional<z.ZodArray<z.ZodString, "many">>;
  edges: z.ZodOptional<z.ZodArray<z.ZodObject<{
    from: z.ZodUnion<[z.ZodString, z.ZodArray<z.ZodString, "many">]>;
    to: z.ZodUnion<[z.ZodString, z.ZodArray<z.ZodString, "many">]>;
    description: z.ZodEffects<z.ZodOptional<z.ZodString>, string | undefined, string | undefined>;
    edgeType: z.ZodOptional<z.ZodEnum<["handoff", "direct"]>>;
    prompt: z.ZodEffects<z.ZodOptional<z.ZodUnion<[z.ZodString, z.ZodFunction<z.ZodTuple<[], z.ZodUnknown>, z.ZodUnknown>]>>, string | ((...args: unknown[]) => unknown) | undefined, string | ((...args: unknown[]) => unknown) | undefined>;
    excludeResults: z.ZodOptional<z.ZodBoolean>;
    promptKey: z.ZodEffects<z.ZodOptional<z.ZodString>, string | undefined, string | undefined>;
  }, "strip", z.ZodTypeAny, {
    from: string | string[];
    to: string | string[];
    description?: string | undefined;
    prompt?: string | ((...args: unknown[]) => unknown) | undefined;
    edgeType?: "direct" | "handoff" | undefined;
    excludeResults?: boolean | undefined;
    promptKey?: string | undefined;
  }, {
    from: string | string[];
    to: string | string[];
    description?: string | undefined;
    prompt?: string | ((...args: unknown[]) => unknown) | undefined;
    edgeType?: "direct" | "handoff" | undefined;
    excludeResults?: boolean | undefined;
    promptKey?: string | undefined;
  }>, "many">>;
  end_after_tools: z.ZodOptional<z.ZodBoolean>;
  hide_sequential_outputs: z.ZodOptional<z.ZodBoolean>;
  stateful_code_sessions: z.ZodOptional<z.ZodBoolean>;
  stateful_code_environment: z.ZodOptional<z.ZodEnum<["user", "agent-user", "conversation"]>>;
  code_environment_id: z.ZodOptional<z.ZodNullable<z.ZodString>>;
  code_workspace_id: z.ZodOptional<typeof agentCodeWorkspaceIdSchema>;
  git_identity: typeof agentGitIdentityUpdateSchema;
  artifacts: z.ZodOptional<z.ZodString>;
  recursion_limit: z.ZodOptional<z.ZodNumber>;
  conversation_starters: z.ZodOptional<z.ZodArray<z.ZodString, "many">>;
  tool_resources: typeof agentToolResourcesSchema;
  tool_options: z.ZodOptional<z.ZodRecord<z.ZodString, z.ZodObject<{
    defer_loading: z.ZodOptional<z.ZodBoolean>;
    allowed_callers: z.ZodOptional<z.ZodArray<z.ZodEnum<["direct", "code_execution"]>, "many">>;
    run_in_background: z.ZodOptional<z.ZodBoolean>;
    describe_intent: z.ZodOptional<z.ZodBoolean>;
  }, "strip", z.ZodTypeAny, {
    defer_loading?: boolean | undefined;
    allowed_callers?: ("direct" | "code_execution")[] | undefined;
    run_in_background?: boolean | undefined;
    describe_intent?: boolean | undefined;
  }, {
    defer_loading?: boolean | undefined;
    allowed_callers?: ("direct" | "code_execution")[] | undefined;
    run_in_background?: boolean | undefined;
    describe_intent?: boolean | undefined;
  }>>>;
  subagents: typeof agentSubagentsSchema;
  support_contact: z.ZodOptional<z.ZodObject<{
    name: z.ZodOptional<z.ZodString>;
    email: z.ZodOptional<z.ZodUnion<[z.ZodLiteral<"">, z.ZodString]>>;
  }, "strip", z.ZodTypeAny, {
    name?: string | undefined;
    email?: string | undefined;
  }, {
    name?: string | undefined;
    email?: string | undefined;
  }>>;
  category: z.ZodOptional<z.ZodString>;
} & {
  avatar: z.ZodOptional<z.ZodUnion<[z.ZodObject<{
    filepath: z.ZodString;
    source: z.ZodString;
  }, "strip", z.ZodTypeAny, {
    source: string;
    filepath: string;
  }, {
    source: string;
    filepath: string;
  }>, z.ZodNull]>>;
  provider: z.ZodOptional<z.ZodString>;
  model: z.ZodOptional<z.ZodNullable<z.ZodString>>;
}, "strip">;
interface ValidateAgentModelParams {
  req: LooseRequest;
  res: Response$2;
  agent: Agent;
  modelsConfig: TModelsConfig;
  logViolation: (req: LooseRequest, res: Response$2, type: string, errorMessage: Record<string, unknown>, score?: number | string) => Promise<void>;
}
interface ValidateAgentModelResult {
  isValid: boolean;
  error?: {
    message: string;
  };
}
/**
* Validates an agent's model against the available models configuration.
* This is a non-middleware version of validateModel that can be used
* in service initialization flows.
*
* @param params - Validation parameters
* @returns Object indicating whether the model is valid and any error details
*/
declare function validateAgentModel(params: ValidateAgentModelParams): Promise<ValidateAgentModelResult>;
//#endregion
//#region src/agents/management.d.ts
type AgentManagementCreate = Omit<z.output<typeof agentCreateSchema>, "model"> & {
  model: string;
};
type AgentManagementCreateInput = Omit<z.input<typeof agentCreateSchema>, "model"> & {
  model: string;
};
type AgentManagementUpdate = Omit<z.output<typeof agentUpdateSchema>, "name" | "description" | "instructions" | "model" | "avatar"> & {
  name?: string;
  description?: string;
  instructions?: string;
  model?: string;
  avatar?: null;
};
type AgentManagementList = {
  limit: number;
  cursor?: string;
};
type AgentManagementResponse = Omit<z.output<typeof agentUpdateSchema>, "provider" | "model"> & {
  id: string;
  provider: string;
  model: string | null;
  version: number;
  createdAt: string;
  updatedAt: string;
};
type AgentManagementProjectionSource = Partial<Omit<AgentManagementResponse, "id" | "provider" | "model" | "version" | "createdAt" | "updatedAt" | "avatar">> & {
  id?: string;
  provider?: string;
  model?: string | null;
  avatar?: AgentManagementResponse["avatar"] | string;
  version?: number;
  versions?: readonly object[];
  createdAt?: string | Date;
  updatedAt?: string | Date;
};
type AgentManagementListProjectionSource = {
  data?: AgentManagementProjectionSource[];
  has_more?: boolean;
  after?: string | null;
};
type AgentManagementListResponse = {
  object: "list";
  data: AgentManagementResponse[];
  first_id: string | null;
  last_id: string | null;
  has_more: boolean;
  after: string | null;
};
type AgentManagementDeleteResponse = {
  id: string;
  deleted: true;
};
type AgentManagementErrorCode = "invalid_request" | "not_found" | "permission_denied" | "internal_error" | "conflict";
type AgentManagementError = {
  error: {
    code: AgentManagementErrorCode;
    message: string;
    details?: Array<{
      path: Array<string | number>;
      message: string;
    }>;
  };
};
/**
* Agent Management accepts the browser-supported configuration fields, but unlike the
* browser endpoints it rejects unknown top-level fields instead of silently stripping them.
*/
declare const agentManagementCreateSchema: z.ZodType<AgentManagementCreate, z.ZodTypeDef, AgentManagementCreateInput>;
declare const agentManagementUpdateSchema: z.ZodType<AgentManagementUpdate>;
declare const agentManagementListSchema: z.ZodType<AgentManagementList, z.ZodTypeDef, unknown>;
/** The externally supported Agent shape. Persistence and ownership fields are intentionally absent. */
declare const agentManagementResponseSchema: z.ZodType<AgentManagementResponse>;
declare const agentManagementListResponseSchema: z.ZodType<AgentManagementListResponse>;
/** Minimal tombstone returned after an Agent is successfully deleted. */
declare const agentManagementDeleteResponseSchema: z.ZodType<AgentManagementDeleteResponse>;
declare const agentManagementErrorCodeSchema: z.ZodType<AgentManagementErrorCode>;
declare const agentManagementErrorSchema: z.ZodType<AgentManagementError>;
/** Build an external response from a persistence result using an explicit allowlist. */
declare function projectAgentManagementResponse(source: AgentManagementProjectionSource): AgentManagementResponse;
declare function projectAgentManagementListResponse(source: AgentManagementListProjectionSource): AgentManagementListResponse;
/** Map known failure classes to a stable envelope without exposing internal error messages. */
declare function mapAgentManagementError(code: AgentManagementErrorCode, error?: unknown): {
  status: number;
  body: AgentManagementError;
};
//#endregion
//#region src/agents/deletion.d.ts
type AgentManagementRecord$2 = AgentManagementProjectionSource & {
  _id: Types.ObjectId;
};
interface AgentManagementDeleteDeps {
  getRoleByName: (roleName: string, fieldsToSelect?: string | string[]) => Promise<IRole | null>;
  getAgentWithVersionCount: (search: {
    id: string;
    tenantId: string;
  }) => Promise<AgentManagementRecord$2 | null>;
  checkPermission: (params: {
    userId: string;
    role?: string;
    resourceType: ResourceType;
    resourceId: Types.ObjectId;
    requiredPermission: PermissionBits;
  }) => Promise<boolean>;
  hasCapability: (user: IUser, capability: SystemCapability) => Promise<boolean>;
  deleteAgent: (search: {
    id: string;
    tenantId: string;
  }) => Promise<IAgent | null>;
}
/** Authorize a tenant-scoped deletion, then reuse the Agent model's existing cleanup path. */
declare function createAgentManagementDeleteHandler(deps: AgentManagementDeleteDeps): (req: Request, res: Response$2) => Promise<Response$2>;
//#endregion
//#region src/agents/resources.d.ts
/** Deferred DB update from provisioning (batched after all files are provisioned) */
type TFileUpdate = {
  file_id: string;
  metadata?: Record<string, unknown>;
  embedded?: boolean;
};
/**
* Function type for provisioning a file to the code execution environment.
* @returns The codeEnvRef and a deferred DB update object
*/
type TProvisionToCodeEnv = (params: {
  req: ServerRequest;
  file: TFile;
  entity_id?: string;
}) => Promise<{
  referenceSet: CodeEnvReferenceSet;
  fileUpdate: TFileUpdate;
}>;
/**
* Function type for provisioning a file to the vector DB for file_search.
* @returns Object with embedded status and a deferred DB update object
*/
type TProvisionToVectorDB = (params: {
  req: ServerRequest;
  file: TFile;
  entity_id?: string;
  existingStream?: unknown;
}) => Promise<{
  embedded: boolean;
  fileUpdate: TFileUpdate | null;
}>;
/**
* Function type for batch-checking code env file liveness.
* Groups files by session, makes one API call per session.
* @returns Set of file_ids that are confirmed alive
*/
type TCheckSessionsAlive = (params: {
  files: TFile[];
  req?: ServerRequest;
  apiKey?: string;
  staleSafeWindowMs?: number;
  baseURL?: string;
  routeKey?: string;
}) => Promise<Set<string>>;
/** Loads CODE_API_KEY for a user. Call once per request. */
type TLoadCodeApiKey = (userId: string) => Promise<string>;
/** State computed during primeResources for lazy provisioning at tool invocation time */
type ProvisionState = {
  /** Files that need uploading to the code execution environment */codeEnvFiles: TFile[]; /** Names from cleared refs on this state's active route; never reusable storage pointers. */
  codeEnvRecoveryNames?: Map<string, {
    name: string;
    isTargetScope: boolean;
  }>; /** Files that need embedding into the vector DB for file_search */
  vectorDBFiles: TFile[]; /** Set of file_ids confirmed alive in code env (from staleness check) */
  aliveFileIds: Set<string>;
  /** The active agent's own resource files, which are the only ones provisioned under its
  *  shared identity. A record carrying an agent context is not enough: a user may attach
  *  another agent's setup file to this conversation, and provisioning it here would put
  *  it in a namespace this agent's other users can read. */
  agentScopedFileIds: Set<string>;
};
/**
* Function type for retrieving files from the database
* @param filter - MongoDB filter query for files
* @param _sortOptions - Sorting options (currently unused)
* @param selectFields - Field selection options
* @returns Promise resolving to array of files
*/
type TGetFiles = (filter: FilterQuery<IMongoFile>, _sortOptions: ProjectionType<IMongoFile> | null | undefined, selectFields: QueryOptions<IMongoFile> | null | undefined) => Promise<Array<TFile>>;
/**
* Function type for filtering files by agent access permissions.
* Used to enforce that only files the user has access to (via ownership or agent attachment)
* are returned after a raw DB query.
*/
type TFilterFilesByAgentAccess = (params: {
  files: Array<TFile>;
  userId: string;
  role?: string;
  agentId: string;
}) => Promise<Array<TFile>>;
/**
* Helper function to add a file to a specific tool resource category
* Prevents duplicate files within the same resource category
* @param params - Parameters object
* @param params.file - The file to add to the resource
* @param params.resourceType - The type of tool resource (e.g., execute_code, file_search, image_edit)
* @param params.tool_resources - The agent's tool resources object to update
* @param params.processedResourceFiles - Set tracking processed files per resource type
*/
declare const addFileToResource: ({
  file,
  resourceType,
  tool_resources,
  processedResourceFiles
}: {
  file: TFile;
  resourceType: EToolResources;
  tool_resources: AgentToolResources;
  processedResourceFiles: Set<string>;
}) => void;
declare const isAgentScopedFile: (file: Pick<TFile, "context">) => boolean;
declare const primeResources: ({
  req,
  principal,
  appConfig,
  getFiles,
  filterFiles,
  requestFileSet,
  attachments: _attachments,
  tool_resources: _tool_resources,
  agentId,
  enabledToolResources,
  checkSessionsAlive,
  loadCodeApiKey,
  provisionCandidates,
  screenPersistentFiles,
  codeRouteKey,
  codeBaseUrl
}: {
  req?: ServerRequest;
  principal?: Pick<IUser, "id" | "role">;
  appConfig?: AppConfig;
  requestFileSet: Set<string>;
  attachments: Promise<Array<TFile | null>> | undefined;
  tool_resources: AgentToolResources | undefined;
  getFiles: TGetFiles;
  filterFiles?: TFilterFilesByAgentAccess;
  agentId?: string; /** Set of tool resource types the agent has enabled (e.g., execute_code, file_search) */
  enabledToolResources?: Set<EToolResources>; /** Optional callback to batch-check code env file liveness by session */
  checkSessionsAlive?: TCheckSessionsAlive; /** Optional callback to load CODE_API_KEY once per request */
  loadCodeApiKey?: TLoadCodeApiKey;
  /** Attachments from earlier turns that were never provisioned. Considered for
  *  provisioning only and never returned as attachments: re-delivering an earlier
  *  upload to the model on every later turn is not the intent. */
  provisionCandidates?: Array<TFile>;
  /** True when this endpoint still shows the explicit upload-destination chooser. */
  /** Applies the caller's endpoint and content policies. Persistent agent files are read
  *  here rather than by the caller, so the caller has no chance to screen them itself
  *  and a configuration or policy change since they were attached would otherwise let
  *  their bytes reach the model, the Code API or RAG. */
  screenPersistentFiles?: (files: Array<TFile>) => Array<TFile>;
  /** The code deployment this turn will execute on, which decides whether an existing
  *  sandbox reference is usable here. */
  codeRouteKey?: string;
  /** Base URL of that deployment. Refs are deployment-local, so the liveness probe runs
  *  against the Code API that issued them rather than the default one. */
  codeBaseUrl?: string;
}) => Promise<{
  attachments: Array<TFile | undefined> | undefined;
  requestAttachments: Array<TFile | undefined> | undefined;
  agentContextAttachments: Array<TFile | undefined> | undefined;
  tool_resources: AgentToolResources | undefined;
  provisionState?: ProvisionState;
  warnings: string[];
}>;
//#endregion
//#region src/agents/skills.d.ts
/**
* Load a single skill by name, constrained to an ACL-accessible ID set.
* Returns the full document (including `body`) so manual invocation can
* prime SKILL.md without a second DB round-trip.
*
* `preferUserInvocable` (manual paths): on a same-name collision,
* prefer the newest doc with `userInvocable !== false`.
* `preferModelInvocable` (model paths — `skill` / `read_file`): on a
* same-name collision, prefer the newest doc with
* `disableModelInvocation !== true`. Both fall back to the newest match
* so the explicit-rejection error paths still fire when only the
* non-preferred variant exists.
*/
type TGetSkillByName = (name: string, accessibleIds: Types.ObjectId[], options?: {
  preferUserInvocable?: boolean;
  preferModelInvocable?: boolean;
}) => Promise<{
  _id: Types.ObjectId;
  name: string;
  body: string; /** Monotonic Skill document version used by checkpoint context compatibility. */
  version?: number;
  author: Types.ObjectId; /** Structured SKILL.md metadata retained for model-bound policy checks. */
  frontmatter?: Record<string, unknown>;
  /**
  * Skill-declared tool allowlist, forwarded verbatim from the skill doc.
  * Surfaced so the resolver can carry it onto `ResolvedManualSkill` for
  * future runtime enforcement without a second round-trip.
  */
  allowedTools?: string[];
  /**
  * Set when the skill was authored with `disable-model-invocation: true`.
  * The skill tool handler short-circuits on this so a model that names
  * such a skill (e.g. via hallucination or stale catalog) gets a clear
  * rejection instead of silently executing.
  */
  disableModelInvocation?: boolean;
  /**
  * Set when the skill was authored with `user-invocable: false`. The
  * manual-invocation resolver skips with a warn log so an API-direct
  * caller can't bypass the popover-side filter.
  */
  userInvocable?: boolean; /** True for deployment-directory skills that are loaded in memory. */
  deployment?: boolean;
} | null>;
/** List skill summaries for catalog injection (paginated, omits body/frontmatter). */
type TListSkillsByAccess = (params: {
  accessibleIds: Types.ObjectId[];
  limit: number;
  cursor?: string | null;
}) => Promise<{
  skills: Array<{
    _id: Types.ObjectId;
    name: string;
    description: string;
    author: Types.ObjectId;
    /**
    * When `true`, the skill is excluded from the catalog injected into
    * the agent's additional_instructions and the model cannot invoke it
    * via the `skill` tool. Manual `$` invocation is unaffected.
    */
    disableModelInvocation?: boolean;
    /**
    * When `false`, the skill is hidden from the `$` popover and rejected
    * by the manual-invocation resolver. Defaults to `true`.
    */
    userInvocable?: boolean; /** True for deployment-directory skills that are loaded in memory. */
    deployment?: boolean;
  }>;
  has_more?: boolean;
  after?: string | null;
}>;
/**
* Hard ceiling on skill names resolved per request via `$` popover or
* `always-apply`. The popover realistically surfaces only a few per turn;
* the cap is a defense-in-depth against a crafted payload fanning out into
* many concurrent `getSkillByName` DB lookups.
*/
declare const MAX_MANUAL_SKILLS = 10;
/**
* Hard ceiling on `always-apply` skills primed per turn. Larger than
* `MAX_MANUAL_SKILLS` because these are admin / author curated (not
* per-request user input), so the defensive-payload concern is weaker —
* but still bounded so a pathological team config can't push dozens of
* skill bodies into every turn.
*/
declare const MAX_ALWAYS_APPLY_SKILLS = 20;
/**
* Combined hard ceiling applied in `injectSkillPrimes`. When the total
* (manual + always-apply) exceeds this, always-apply gets truncated
* first — manual invocation is explicit user intent and should never
* be silently dropped.
*/
declare const MAX_PRIMED_SKILLS_PER_TURN = 30;
/**
* Hard ceiling on individual skill name length. Real skill names are
* short slugs (e.g. `pptx`, `brand-guidelines`); anything beyond this is
* a crafted payload. Filtered out before the DB round-trip so pathological
* strings can't reach `getSkillByName` / Mongo's query planner.
*/
declare const MAX_SKILL_NAME_LENGTH = 200;
/**
* Canonical namespace prefix for skill files. Single source of truth for
* three layers that must agree:
* - the `read_file`/`create_file`/`edit_file` authoring namespace shown to
*   the model (`skills/{skillName}/...`),
* - the `handleReadFileCall` routing + bash-fallback paths in `handlers.ts`,
* - the physical mount layout under `/mnt/data` (see `skillFiles.ts`, which
*   primes bundled files at `skills/{skillName}/...` so bash and the
*   model-facing namespace resolve to the same path on disk).
*
* Keep the trailing slash — call sites concatenate `${SKILL_FILE_PREFIX}${skillName}/...`.
*/
declare const SKILL_FILE_PREFIX = "skills/";
/** Whether a model-facing file path is routed to persistent TerraMind skill storage. */
declare function isSkillFilePath(filePath: string): boolean;
/**
* Marker tagged onto every skill-primed message (as `additional_kwargs.source`
* on a LangChain `HumanMessage`, or as `source` on the `InjectedMessage` that
* `handleSkillToolCall` emits). Downstream filtering/telemetry keys off this,
* so both construction paths must agree — keep the literal exported from one
* place rather than repeated inline.
*/
declare const SKILL_MESSAGE_SOURCE = "skill";
/**
* Discriminator tag on a skill prime message that records *why* the skill
* was primed into the turn. Stored on `additional_kwargs.trigger` of the
* `HumanMessage` produced by `injectSkillPrimes`; surfaced on UI
* (pill variants) and accessible to downstream filtering / telemetry.
*/
declare const SKILL_TRIGGER_MANUAL = "manual";
/**
* Reserved for the model-invoked path (runtime catalog → tool-call
* resolution). Declared here so the `SkillTrigger` union and the pill
* UI already speak in terms of three sources rather than growing a
* fourth value later.
*/
declare const SKILL_TRIGGER_MODEL = "model";
declare const SKILL_TRIGGER_ALWAYS_APPLY = "always-apply";
type SkillTrigger = typeof SKILL_TRIGGER_MANUAL | typeof SKILL_TRIGGER_MODEL | typeof SKILL_TRIGGER_ALWAYS_APPLY;
/**
* Predicate that identifies a LangChain message as one we spliced in via
* `injectManualSkillPrimes` (or the equivalent model-invoked path). Callers
* like `runMemory` use this to strip synthetic skill content from windows
* that should only contain real user chat — without this filter, SKILL.md
* bodies pollute memory extraction and crowd out genuine turns.
*/
declare function isSkillPrimeMessage(msg: unknown): boolean;
/**
* Scopes user-accessible skill IDs to only those configured on the agent.
*
* Semantics (pinned by unit tests):
* - `undefined` / `null` → not configured, returns the full accessible catalog.
* - `[]` (empty array) → no allowlist, returns the full accessible catalog.
*   Removing all selected skills in the builder restores the default full-catalog
*   behavior while `skills_enabled` remains true.
* - non-empty array of skill `_id` hex strings → intersection of accessible IDs
*   and agent-configured IDs.
*
* @internal Building block for {@link resolveAgentScopedSkillIds}; runtime
* call sites should prefer the resolver so the activation predicate
* (`skillsCapabilityEnabled`, ephemeral toggle, persisted `skills_enabled`)
* is enforced uniformly.
*/
declare function scopeSkillIds(accessibleSkillIds: Types.ObjectId[], agentSkills: string[] | null | undefined): Types.ObjectId[];
interface ResolveModelSpecSkillIdsParams {
  /** Skill names configured on a model spec. */
  names: string[];
  /** Full VIEW-accessible skill IDs for this user before model-spec scoping. */
  accessibleSkillIds: Types.ObjectId[];
  /** DB lookup: name → skill doc constrained to the user's accessible IDs. */
  getSkillByName?: TGetSkillByName;
}
/**
* Resolves model-spec skill names against the current user's accessible skill
* set. Config is advisory: unrecognized, inaccessible, malformed, or errored
* names are skipped with a warning so a stale model-spec entry never blocks
* the actual chat request.
*/
declare function resolveModelSpecSkillIds({
  names,
  accessibleSkillIds,
  getSkillByName
}: ResolveModelSpecSkillIdsParams): Promise<Types.ObjectId[]>;
interface ResolveAgentScopedSkillIdsParams {
  /** Agent being initialized. Reads its persisted skill capability and catalog scope. */
  agent: Pick<Agent, "id" | "skills" | "skills_enabled" | "skills_scope">;
  /** Full set of skill IDs the user can VIEW (pre-scoped by ACL). */
  accessibleSkillIds: Types.ObjectId[];
  /** Admin capability: `AgentCapabilities.skills` on the agents endpoint. */
  skillsCapabilityEnabled: boolean;
  /** Per-conversation skills badge toggle (`req.body.ephemeralAgent.skills`). */
  ephemeralSkillsToggle: boolean;
}
/**
* Strict opt-in resolver for per-agent skill scope. Activation requires an
* explicit signal from the user or the agent author:
*  - Ephemeral agent  → model-spec `skills` wins when configured:
*    `true` = full accessible catalog, string list = scoped allowlist,
*    empty list / `false` = no skills. Otherwise the skills badge toggle
*    controls the full accessible catalog.
*  - Persisted agent  → the builder's `skills_enabled` master switch and
*    optional explicit `skills_scope`. Legacy agents without a scope retain
*    enabled + empty = full catalog behavior.
*
* When not activated, returns `[]` so `injectSkillCatalog`,
* `resolveManualSkills`, and `resolveAlwaysApplySkills` all no-op.
*
* Without this gate, an `agent.skills` of `undefined` on a persisted agent
* would fall through to the "full catalog" branch of `scopeSkillIds`,
* exposing the skill tool on runs where the author never opted in.
*/
declare function resolveAgentScopedSkillIds(params: ResolveAgentScopedSkillIdsParams): Types.ObjectId[];
interface ResolveSkillActiveParams {
  /** Skill being evaluated. Only `_id` and `author` matter for resolution. */
  skill: {
    _id: Types.ObjectId | string;
    author: Types.ObjectId | string;
    deployment?: boolean;
  };
  /** Per-user overrides: `{ [skillId]: boolean }`. Missing entries use the default. */
  skillStates?: Record<string, boolean>;
  /** Current user ID. When absent, the function fails closed for all non-overridden skills. */
  userId?: string;
  /** Admin-configured default for shared skills. `true` = shared skills auto-activate. */
  defaultActiveOnShare?: boolean;
}
/**
* Resolves whether a skill should be injected into the agent catalog for the
* current user. Precedence (pinned by unit tests):
*
* 1. Explicit override in `skillStates` wins above all.
* 2. Absent `userId` → fail closed. The caller lost user context, so we do
*    not fall back to ownership-based defaults that could leak shared skills.
* 3. Owned skills (author === userId) default to **active**.
* 4. Shared skills default to `defaultActiveOnShare` (admin-configured, default `false`).
*/
declare function resolveSkillActive(params: ResolveSkillActiveParams): boolean;
interface InjectSkillCatalogParams {
  agent: Agent;
  toolDefinitions: LCTool$1[] | undefined;
  toolRegistry: LCToolRegistry$1 | undefined;
  accessibleSkillIds: Types.ObjectId[];
  contextWindowTokens: number;
  listSkillsByAccess: TListSkillsByAccess | undefined;
  /** When true, registers bash_tool alongside skill + read_file. */
  codeEnvAvailable?: boolean;
  /** When true, bash_tool registers with the hedged stateful-session description. */
  statefulSessions?: boolean;
  /** When true, read_file exposes the attached worker's workspace namespace. */
  workspaceTools?: boolean;
  /** Live operation ceiling for the selected attached workspace. */
  workspaceOperations?: ReadonlySet<CodeWorkspaceOperation>;
  /** Deployment ceiling advertised on attached Bash tool definitions. */
  workspaceCommandTimeoutMaxMs?: number;
  workspaceEnvironment?: CodeWorkspaceDescriptor["environment"];
  /** Current user ID — used to determine skill ownership for active-state resolution. */
  userId?: string;
  /** Per-user skill overrides: `{ [skillId]: boolean }`. Missing entries use the default. */
  skillStates?: Record<string, boolean>;
  /** Admin-configured default for shared skills. `true` = shared skills auto-activate. */
  defaultActiveOnShare?: boolean;
  /** Admin-configured cap on the model-visible catalog. Defaults to 100. */
  maxCatalogSkills?: number;
  /** Read-only catalog snapshot preloaded for current-policy inspection. */
  resolvedCatalog?: ResolvedSkillCatalog;
}
type SkillCatalogSummary = Awaited<ReturnType<NonNullable<TListSkillsByAccess>>>["skills"][number];
interface ResolvedSkillCatalog {
  activeSkills: SkillCatalogSummary[];
  catalogLimit: number;
  visibleCount: number;
  reachedEnd: boolean;
}
interface ResolveSkillCatalogParams {
  accessibleSkillIds: Types.ObjectId[];
  listSkillsByAccess: TListSkillsByAccess | undefined;
  userId?: string;
  skillStates?: Record<string, boolean>;
  defaultActiveOnShare?: boolean;
  maxCatalogSkills?: number;
}
interface InjectSkillCatalogResult {
  toolDefinitions: LCTool$1[] | undefined;
  skillCount: number;
  /**
  * Tool names the skills capability manages this run: the `skill` tool (when
  * anything is model-invocable) and `read_file` (always, for primed skill
  * references). `bash_tool` is excluded even when this call registers it —
  * it belongs to the `execute_code` capability, which reports it itself.
  * `initializeAgent` records these under the `skills` marker so spec
  * selections naming `skills` govern exactly these definitions.
  */
  toolNames: string[];
  /**
  * IDs of skills the runtime is authorized to resolve via `getSkillByName`.
  * Includes `disable-model-invocation: true` skills even though they're
  * absent from the catalog text — the skill-tool handler needs to be able
  * to fetch the doc to fire the explicit "cannot be invoked by the model"
  * rejection (instead of a generic "not found"). Per-user deactivated
  * skills are still excluded — explicit user opt-out shouldn't be
  * resolvable.
  */
  activeSkillIds: Types.ObjectId[];
  /**
  * Names of skills the runtime can resolve, mirroring `activeSkillIds`.
  * Surfaced so host-side handlers (e.g. `read_file`) can decide whether a
  * `{firstSegment}/...` path is a real skill reference vs. a code-env path
  * (`/mnt/data/...`) that should be routed to the bash fallback — without
  * issuing an extra `getSkillByName` round-trip just to discover the name
  * doesn't resolve.
  */
  activeSkillNames: Set<string>;
}
/**
* Loads the exact active catalog snapshot without mutating an agent or tool
* registry. Callers may inspect this user-authored content before performing
* provider/resource side effects, then pass the snapshot to
* `injectSkillCatalog` to avoid a second query and TOCTOU drift.
*/
declare function resolveSkillCatalog(params: ResolveSkillCatalogParams): Promise<ResolvedSkillCatalog>;
/**
* Queries accessible skills, formats a budget-aware catalog, appends it to the
* agent's additional_instructions, and registers the SkillTool definition.
* Returns updated toolDefinitions and the skill count.
*
* No tool instance is created — SkillTool is event-driven only. The tool
* definition in toolDefinitions is sufficient for the LLM to see and call it;
* the host handler intercepts the call via ON_TOOL_EXECUTE.
*
* The caller is responsible for gating on the skills capability before calling.
*/
declare function injectSkillCatalog(params: InjectSkillCatalogParams): Promise<InjectSkillCatalogResult>;
/**
* Builds the meta user message that carries a skill's SKILL.md body into a
* turn's context. Shape mirrors what `handleSkillToolCall` emits when the
* model invokes the skill tool directly, so downstream message handling
* treats all three paths identically.
*
* Used by:
*  - Phase 3 manual invocation (`$skill-name` popover) — called at turn start.
*  - Phase 5 `always-apply` frontmatter — called at turn start.
*  - `handleSkillToolCall` for model-invoked skills — called from the tool
*    execution handler.
*/
declare function buildSkillPrimeMessage(skill: {
  name: string;
  body: string;
}): InjectedMessage;
/** Builds the exact live Skill overlay placed at the tail of an event actor checkpoint fork. */
declare function buildAgentEventActorSkillMessages(skills: ReadonlyMap<string, string>): HumanMessage[];
interface ResolveManualSkillsParams {
  /** Skill names the user invoked (via `$` popover or `always-apply`). */
  names: string[];
  /** DB lookup: name → skill doc, constrained to ACL-accessible IDs.
  *
  * Resolver always passes `options.preferUserInvocable: true` so a
  * same-name newer `userInvocable: false` (model-only) duplicate can't
  * shadow the older user-invocable doc the popover surfaced. Disable-
  * model-invocation status is irrelevant for the manual path.
  */
  getSkillByName: (name: string, accessibleIds: Types.ObjectId[], options?: {
    preferUserInvocable?: boolean;
    preferModelInvocable?: boolean;
  }) => Promise<{
    _id: Types.ObjectId;
    name: string;
    body: string;
    version?: number;
    author: Types.ObjectId | string;
    deployment?: boolean; /** Structured SKILL.md metadata retained for model-bound policy checks. */
    frontmatter?: Record<string, unknown>;
    /**
    * Skill-declared tool allowlist, forwarded verbatim from the skill doc.
    * Surfaced on `ResolvedManualSkill` so future runtime enforcement can
    * union it into the agent's effective tool set for the turn without
    * re-fetching the document. Populated by the DB method when available.
    */
    allowedTools?: string[];
    /**
    * When `false`, the skill author opted out of manual invocation. The
    * resolver skips with a warn log rather than priming SKILL.md.
    * Defaults to `true` when omitted.
    */
    userInvocable?: boolean;
  } | null>;
  /** ACL-accessible skill IDs for this user (already scoped by `scopeSkillIds`). */
  accessibleSkillIds: Types.ObjectId[];
  /** Current user ID — required for ownership-based active-state defaults. */
  userId?: string;
  /** Per-user skill active/inactive overrides. */
  skillStates?: Record<string, boolean>;
  /** Admin-configured default for shared skills. */
  defaultActiveOnShare?: boolean;
}
/**
* Canonical shape of a skill resolved into prime-ready form. Both
* manual-invocation (`$` popover) and `always-apply` resolvers emit this
* shape so downstream pipeline stages (`injectSkillPrimes`,
* `unionPrimeAllowedTools`, `buildSkillPrimedIdsByName`) can treat either
* source uniformly. The per-prime distinction lives on
* `additional_kwargs.trigger` of the spliced `HumanMessage` (see
* `SkillTrigger`), not on this resolver output.
*/
interface ResolvedSkillPrime {
  /**
  * `_id` of the exact doc that was primed. Plumbed to the runtime so the
  * `read_file` handler can constrain its name lookup to this id and avoid
  * resolving to a different same-name doc on collisions (which would
  * cause the model's reads to hit files from a different skill than the
  * body it sees).
  */
  _id: Types.ObjectId;
  name: string;
  body: string;
  /** Monotonic Skill revision used by checkpoint compatibility. */
  version?: number;
  /** Structured SKILL.md metadata retained for model-bound policy checks. */
  frontmatter?: Record<string, unknown>;
  /**
  * Skill-declared tool allowlist passed through from the skill doc. Present
  * only when the skill author declared `allowed-tools` in frontmatter.
  * Currently populated but not consumed — future runtime enforcement will
  * union these into the agent's effective tool set for the turn.
  */
  allowedTools?: string[];
}
/**
* Back-compat alias for manual-invocation primes (`$` popover). Semantic
* aliases over `ResolvedSkillPrime` keep the per-source naming at call
* sites (so `manualPrimes: ResolvedManualSkill[]` stays readable) without
* maintaining parallel interfaces.
*/
type ResolvedManualSkill = ResolvedSkillPrime;
/**
* Back-compat alias for always-apply primes (auto-applied every turn).
* See `ResolvedSkillPrime` for the canonical definition.
*/
type ResolvedAlwaysApplySkill = ResolvedSkillPrime;
/**
* Resolves user-provided skill names to `{ name, body }` pairs ready for
* priming. Filters out:
*  - names not backed by an accessible skill (ACL miss or typo),
*  - skills the user has toggled inactive (respects ownership-aware defaults).
*
* The active-state filter is intentional even for explicit `$` selections:
*  1. Phase 2 closes the loop on the UI side by hiding inactive skills from
*     the popover, so a deactivated skill shouldn't be reachable through the
*     normal flow in the first place.
*  2. For API-direct callers bypassing the popover, honoring the user's
*     own activation toggle is the safer default — an opt-out toggle that
*     stops working the moment someone crafts a raw payload isn't much of
*     a toggle.
*
* Silently skips unresolvable names with a warn log — a missing `$skill` must
* never block the user's actual message from going through.
*
* Preserves input order and drops duplicate names (first wins) so a user who
* types `$foo $foo` doesn't end up with the body primed twice.
*/
declare function resolveManualSkills(params: ResolveManualSkillsParams): Promise<ResolvedManualSkill[]>;
interface ResolveAlwaysApplySkillsParams {
  /**
  * Paginated DB lookup for accessible skills with `alwaysApply: true`,
  * eagerly loaded with `body` and optional `allowedTools`. Scoped to
  * `accessibleIds` (post-`scopeSkillIds`). The resolver pages until the
  * active-state budget is filled so inactive early rows cannot starve
  * the prime catalog.
  */
  listAlwaysApplySkills: (params: {
    accessibleIds: Types.ObjectId[];
    limit: number;
    cursor?: string | null;
  }) => Promise<{
    skills: Array<{
      _id: Types.ObjectId;
      name: string;
      body: string;
      author: Types.ObjectId | string;
      frontmatter?: Record<string, unknown>;
      allowedTools?: string[];
      version?: number;
      deployment?: boolean;
    }>;
    has_more?: boolean;
    after?: string | null;
  }>;
  /** ACL-accessible skill IDs for this user (already scoped by `scopeSkillIds`). */
  accessibleSkillIds: Types.ObjectId[];
  /** Current user ID — required for ownership-based active-state defaults. */
  userId?: string;
  /** Per-user skill active/inactive overrides. */
  skillStates?: Record<string, boolean>;
  /** Admin-configured default for shared skills. */
  defaultActiveOnShare?: boolean;
  /** Override cap on the number of always-apply primes to resolve. Defaults to `MAX_ALWAYS_APPLY_SKILLS`. */
  maxAlwaysApplySkills?: number;
}
/**
* Resolves accessible skills with `alwaysApply: true` into prime-ready
* form. Mirrors `resolveManualSkills`' contract on purpose so the two feed
* the same `injectSkillPrimes` + `unionPrimeAllowedTools` pipeline.
*
* Paginates the DB listing until the active budget is filled (or we hit
* `MAX_ALWAYS_APPLY_PAGES`). Applying the budget pre-filter would silently
* starve the prime catalog whenever early-sorted rows happen to be
* inactive for this user (e.g. shared skills with
* `defaultActiveOnShare: false`, or explicit `skillStates` overrides).
*
* Name-level dedup across pages — the schema's uniqueness index is
* `(name, author, tenantId)`, so two authors in the same tenant can
* ship skills with the same `name` shared with a third user. First
* occurrence wins (DB sort is `updatedAt` desc, so freshest definition).
*/
declare function resolveAlwaysApplySkills(params: ResolveAlwaysApplySkillsParams): Promise<ResolvedAlwaysApplySkill[]>;
interface InjectManualSkillPrimesParams {
  /** Formatted LangChain messages produced by `formatAgentMessages`. Mutated in place. */
  initialMessages: BaseMessage[];
  /** Per-index token count map returned by `formatAgentMessages`. */
  indexTokenCountMap: Record<number, number> | undefined;
  /**
  * Resolved skill primes to splice in. Only `name` and `body` are used
  * to construct the meta `HumanMessage`; widening the type to `Pick<...>`
  * lets tests pass minimal `{ name, body }` literals without inventing
  * `_id`s. The resolver always returns full primes in production.
  */
  manualSkillPrimes: Pick<ResolvedManualSkill, "name" | "body">[];
}
interface InjectManualSkillPrimesResult {
  /** Same array reference as input (mutated). */
  initialMessages: BaseMessage[];
  /** New token map with indices shifted; same reference as input when no-op. */
  indexTokenCountMap: Record<number, number> | undefined;
  /** Number of prime messages actually inserted (for logging by the caller). */
  inserted: number;
  /** Position where primes were inserted, or -1 when no-op. */
  insertIdx: number;
}
/**
* Splices skill prime messages into a formatted message array just before
* the latest user message, and shifts the `indexTokenCountMap` so downstream
* `hydrateMissingIndexTokenCounts` fills counts for the new positions
* without corrupting accounting for pre-existing ones.
*
* Insertion semantics:
*  - `insertIdx = initialMessages.length - 1`. Empty input → no-op.
*  - Single-message input → `insertIdx = 0`, primes appear before the lone message.
*  - Map shift: entries with `idx >= insertIdx` move forward by `numPrimes`.
*    Using `>=` (not `>`) is load-bearing: the message that was at `insertIdx`
*    is pushed to `insertIdx + numPrimes` by the splice, so its count must
*    follow it.
*
* Callers are responsible for scoping (e.g. single-agent runs only — see
* `AgentClient.chatCompletion`). This helper is agent-agnostic.
*
* @deprecated Use {@link injectSkillPrimes} instead. That function accepts
* both manual and always-apply primes, applies cross-list dedup, and
* enforces the combined `MAX_PRIMED_SKILLS_PER_TURN` ceiling. Retained here
* for backward compatibility with external consumers of
* `@librechat/api` that import the manual-only splicer directly.
*/
declare function injectManualSkillPrimes(params: InjectManualSkillPrimesParams): InjectManualSkillPrimesResult;
/**
* Collects the set of skill names primed fresh this turn — the union of manual
* ($-popover) and always-apply (frontmatter) primes. Passed to
* `formatAgentMessages` as `skipSkillBodyNames` so a skill that is BOTH primed
* this turn AND present in history (as a `skill` tool_call) has its SKILL.md
* body injected exactly once — by the fresh prime via `injectSkillPrimes` — and
* is NOT also reconstructed from history. Names absent from this set still
* reconstruct from history, preserving sticky manual re-priming across turns.
*/
declare function collectFreshSkillPrimeNames({
  manualSkillPrimes,
  alwaysApplySkillPrimes
}: {
  manualSkillPrimes?: Pick<ResolvedManualSkill, "name">[];
  alwaysApplySkillPrimes?: Pick<ResolvedAlwaysApplySkill, "name">[];
}): Set<string>;
interface InjectSkillPrimesParams {
  /** Formatted LangChain messages produced by `formatAgentMessages`. Mutated in place. */
  initialMessages: BaseMessage[];
  /** Per-index token count map returned by `formatAgentMessages`. */
  indexTokenCountMap: Record<number, number> | undefined;
  /** Resolved manual-invocation primes ($-popover). */
  manualSkillPrimes?: Pick<ResolvedManualSkill, "name" | "body">[];
  /** Resolved `always-apply` primes (frontmatter-driven, auto-applied every turn). */
  alwaysApplySkillPrimes?: Pick<ResolvedAlwaysApplySkill, "name" | "body">[];
  /**
  * Combined ceiling on primes per turn. Defaults to
  * `MAX_PRIMED_SKILLS_PER_TURN`. When the sum of `manualSkillPrimes` +
  * `alwaysApplySkillPrimes` exceeds the cap, always-apply primes are
  * truncated first — manual invocation is explicit user intent and must
  * never be silently dropped.
  */
  maxPrimesPerTurn?: number;
}
interface InjectSkillPrimesResult {
  initialMessages: BaseMessage[];
  indexTokenCountMap: Record<number, number> | undefined;
  inserted: number;
  insertIdx: number;
  alwaysApplyDropped: number;
  /**
  * Count of always-apply primes dropped because the same skill name already
  * appears in the manual list — dedup prevents the same SKILL.md body from
  * being spliced in twice in one turn.
  */
  alwaysApplyDedupedFromManual: number;
}
interface SelectSkillPrimesForTurnResult<ManualPrime, AlwaysApplyPrime> {
  manualSkillPrimes: ManualPrime[];
  alwaysApplySkillPrimes: AlwaysApplyPrime[];
  alwaysApplyDropped: number;
  alwaysApplyDedupedFromManual: number;
}
/**
* Resolves the one authoritative set of skill primes for a turn. Keeping this
* selection separate from message injection lets every earlier consumer
* (content inspection, allowed-tool union, persisted pills) operate on exactly
* the same deduped and capped lists that the model will eventually receive.
*/
declare function selectSkillPrimesForTurn<ManualPrime extends Pick<ResolvedManualSkill, "name">, AlwaysApplyPrime extends Pick<ResolvedAlwaysApplySkill, "name">>(params: {
  manualSkillPrimes: readonly ManualPrime[];
  alwaysApplySkillPrimes: readonly AlwaysApplyPrime[];
  maxPrimesPerTurn?: number;
}): SelectSkillPrimesForTurnResult<ManualPrime, AlwaysApplyPrime>;
/**
* Splices manual + always-apply skill prime messages into a formatted
* message array just before the latest user message. Ordering: always-apply
* primes first (further from the user message, ambient context), then
* manual primes (closer to the user message, explicit user intent). More
* recent context gets more attention in most LLMs, so we want explicit `$`
* picks landing closest to the latest user turn and ambient priming
* sitting further back. Shifts `indexTokenCountMap` for the combined
* splice.
*
* Cross-list dedup: if a user `$`-invokes a skill that is also marked
* `always-apply`, the always-apply copy is dropped so the SKILL.md body
* is primed only once. Manual wins (drops the always-apply side) because
* manual primes sit closer to the user message and carry explicit intent.
*
* Enforces a combined ceiling (`maxPrimesPerTurn`, default
* `MAX_PRIMED_SKILLS_PER_TURN`) by truncating always-apply first so
* manual is never silently dropped. Dedup runs before the cap so the
* cap reflects the real prime count, not the pre-dedup total.
*/
declare function injectSkillPrimes(params: InjectSkillPrimesParams): InjectSkillPrimesResult;
interface SkillPrimeContentPart {
  type: "tool_call";
  tool_call: {
    id: string;
    name: string;
    args: string;
    output: string;
    progress: number;
    type: "tool_call";
  };
}
interface BuildSkillPrimeContentPartsParams {
  /** Run / response message ID. Used as a stable seed for synthetic tool_call IDs. */
  runId: string;
  /**
  * Optional index offset. When the primes are prepended to an existing
  * contentParts array, callers can leave this 0 (IDs still stay unique
  * within the message). Exposed for explicitness only.
  */
  startOffset?: number;
}
/**
* Build completed tool_call content parts for each manually-invoked skill.
*
* Matches the shape the aggregator produces for a model-invoked skill call
* on `ON_RUN_STEP_COMPLETED` — `{ type: 'tool_call', tool_call: { id, name,
* args, output, progress: 1, type: 'tool_call' } }` — so the existing
* `SkillCall` renderer on the frontend shows identical "Skill X loaded"
* cards without any new client work.
*
* Callers (e.g. `AgentClient.chatCompletion`) prepend these to
* `this.contentParts` after `runAgents` returns so the cards persist on
* the response message and appear ahead of the model's content — matching
* the semantic that priming ran before the LLM turn.
*
* ──────────────────────────────────────────────────────────────────────
* Intentional: sticky re-priming across turns.
* ──────────────────────────────────────────────────────────────────────
* Shape parity with model-invoked tool_calls means `extractInvokedSkills
* FromPayload` (packages/api/src/agents/run.ts) treats these as ordinary
* skill invocations when scanning history, so `primeInvokedSkills` re-
* prepares the files + body on every subsequent turn for the rest of
* the conversation. This is deliberate: the skill-card UX on the assistant
* message is the user's persistent visual cue that the skill is active,
* and re-priming keeps the LLM's view consistent with what the user sees
* (and matches how model-invoked skills behave — there is no separate
* "one-shot vs. conversation-scoped" mode). To opt out of a sticky skill,
* users need to start a new conversation or edit the originating user
* message to remove the pills and resubmit — regenerate alone preserves
* the picks (`useChatFunctions.regenerate` forwards them via
* `overrideManualSkills`). A future "one-shot manual prime" mode would
* need a distinct marker on the synthetic tool_call so the history
* scanner could skip it.
*/
declare function buildSkillPrimeContentParts(primes: Pick<ResolvedManualSkill, "name" | "body">[], {
  runId,
  startOffset
}: BuildSkillPrimeContentPartsParams): SkillPrimeContentPart[];
interface SkillPrimeWithTools {
  /** Skill name — used for log attribution only. */
  name: string;
  /** Author-declared `allowed-tools` from frontmatter, if any. */
  allowedTools?: string[];
}
interface UnionPrimeAllowedToolsParams {
  /**
  * Resolved skill primes (manual + always-apply once Phase 5 lands) that
  * may carry an `allowedTools` allowlist. Order is irrelevant — the union
  * is set-based — but stable iteration helps debug logs read consistently.
  */
  primes: SkillPrimeWithTools[];
  /** Tool names already configured on the agent. Skipped when computing extras. */
  agentToolNames: string[];
}
interface UnionPrimeAllowedToolsResult {
  /**
  * Tool names contributed by skill primes that aren't already on the
  * agent. Caller is responsible for actually loading these and merging
  * the resulting tool definitions into the agent's effective set; the
  * helper itself stays pure so it can be unit-tested in isolation and
  * reused for the always-apply path.
  */
  extraToolNames: string[];
  /**
  * Per-skill breakdown of which tool names that skill contributed to the
  * final union. Useful for the debug log when the runtime drops names
  * that the registry doesn't recognize — operators can see which skill
  * asked for the missing tool.
  */
  perSkillExtras: Map<string, string[]>;
}
/**
* Computes the union of `allowed-tools` declared by a turn's resolved skill
* primes, minus tools already configured on the agent. The agent-provided
* tools are the authoritative baseline; allowed-tools only ever ADDS to
* the surface, never replaces or removes.
*
* Tolerant of unknown tool names: validation against the runtime registry
* happens at the caller (in `initialize.ts`) so we can support skills
* authored against tools TerraMind hasn't implemented yet — the registry
* intersection silently drops them with a debug log, but the import path
* never rejects them.
*
* Pure function; returns set-style data so callers can dedupe across
* concurrent always-apply + manual paths without re-implementing the
* fold themselves.
*/
declare function unionPrimeAllowedTools(params: UnionPrimeAllowedToolsParams): UnionPrimeAllowedToolsResult;
/**
* Safely pulls `manualSkills` from an untyped request body.
*
* Accepts only string[] in practice: filters out non-string elements,
* empty strings, and pathologically long names so a crafted payload
* (numbers, objects, nulls, giga-strings) can't reach `getSkillByName`
* and waste DB round-trips, matching the TypeScript contract
* (`manualSkills?: string[]` on TPayload). Returns `undefined` for
* missing / non-array inputs OR for arrays that contain no valid strings —
* callers treat undefined as "no manual invocation this turn."
*/
declare function extractManualSkills(body: unknown): string[] | undefined;
//#endregion
//#region src/tools/classification.d.ts
interface ToolDefinition {
  name: string;
  description?: string;
  parameters?: JsonSchemaType;
  /** MCP server name extracted from tool name */
  serverName?: string;
  /** Raw upstream tool name when the model-facing key stripped a redundant server-name prefix */
  serverToolName?: string;
  /** Current catalog tool name when a LEGACY persisted key kept its pre-strip spelling */
  currentToolName?: string;
}
/** An MCP tool name plus its OTHER spelling (legacy for stripped instances, current for legacy-named ones). */
interface MCPToolAlias {
  name: string;
  aliasName: string;
}
/**
* Collects both directions of identity aliases from MCP tool definitions, so
* approval policies and hook matchers written against EITHER spelling keep
* applying: a stripped instance aliases its pre-strip name, and a
* legacy-named instance (persisted key retained) aliases its current catalog
* name. Works in both loading modes because both funnel their definitions
* through {@link buildToolClassification}.
*/
declare function collectMCPToolAliases(mcpToolDefs: ToolDefinition[]): MCPToolAlias[];
/**
* Extracts the MCP server name from a tool name.
* Tool names follow the pattern: toolName_mcp_ServerName
* @param toolName - The full tool name
* @returns The server name or undefined if not an MCP tool
*/
declare function getServerNameFromTool(toolName: string): string | undefined;
/**
* Aliases persisted `tool_options` keys onto the instance names IN PLACE, so
* every downstream reader of `agent.tool_options` (the registry build for
* defer/programmatic, the background and intent passes) sees the healed keys
* in BOTH loading modes and BOTH spelling directions: options keyed by a
* pre-strip spelling follow a renamed (wildcard-expanded) instance, and
* options the editor migrated to the CURRENT catalog spelling still reach a
* legacy-named instance an unedited `agent.tools` entry retained.
* Identity-gated through {@link collectMCPToolAliases}, and an explicit
* entry under the instance's own name always wins.
*/
declare function aliasMCPToolOptions(aliases: readonly MCPToolAlias[], agentToolOptions?: AgentToolOptions): void;
/**
* Builds a tool registry from agent-level tool_options.
*
* @param tools - Array of tool definitions
* @param agentToolOptions - Per-tool configuration from the agent
* @returns Map of tool name to tool definition with classification
*/
declare function buildToolRegistryFromAgentOptions(tools: ToolDefinition[], agentToolOptions: AgentToolOptions): LCToolRegistry;
interface MCPToolInstance {
  name: string;
  description?: string;
  mcp?: boolean;
  /** Original JSON schema attached at MCP tool creation time */
  mcpJsonSchema?: JsonSchemaType;
  /** Server this tool came from, carried from resolution instead of re-parsed */
  mcpRawServerName?: string;
  /** Raw upstream tool name when the instance name stripped a redundant server-name prefix */
  mcpServerToolName?: string;
  /** Current catalog tool name when a legacy persisted key kept its pre-strip spelling */
  mcpCurrentToolName?: string;
}
/**
* Extracts MCP tool definition from a loaded tool instance.
* MCP tools have the original JSON schema attached as `mcpJsonSchema` property.
*
* @param tool - The loaded tool instance
* @returns Tool definition
*/
declare function extractMCPToolDefinition(tool: MCPToolInstance): ToolDefinition;
/**
* Checks if a tool is an MCP tool based on its properties.
* @param tool - The tool to check (can be any object with potential mcp property)
* @returns Whether the tool is an MCP tool
*/
declare function isMCPTool(tool: unknown): tool is MCPToolInstance;
/**
* Cleans up the temporary mcpJsonSchema property from MCP tools after registry is populated.
* This property is only needed during registry building and can be safely removed afterward.
*
* @param tools - Array of tools to clean up
*/
declare function cleanupMCPToolSchemas(tools: MCPToolInstance[]): void;
/** Parameters for building tool classification and creating PTC/tool search tools */
interface BuildToolClassificationParams {
  /** All loaded tools (will be filtered for MCP tools) */
  loadedTools: GenericTool[];
  /** User ID for auth lookup */
  userId: string;
  /** Agent ID (used for logging and context) */
  agentId?: string;
  /** Per-tool configuration from the agent */
  agentToolOptions?: AgentToolOptions;
  /** Whether the deferred_tools capability is enabled (from agent config) */
  deferredToolsEnabled?: boolean;
  /** Whether the programmatic_tools capability is enabled (from agent config) */
  programmaticToolsEnabled?: boolean;
  /** Whether code execution is enabled and requested by this agent */
  codeExecutionEnabled?: boolean;
  /** When true, skip creating tool instances (for event-driven mode) */
  definitionsOnly?: boolean;
  /** Agent provider — Gemini/Vertex rejects union types, so injected tool schemas get sanitized */
  provider?: Providers | string;
  /** Optional host-supplied Code API auth headers for remote programmatic execution. */
  authHeaders?: () => Promise<Record<string, string>> | Record<string, string>;
  /** Trusted Code API route selected for the executing agent. */
  codeExecutionContext?: CodeExecutionContext;
  codeEnvironments?: readonly CodeEnvironmentConfig[];
  getAppConfig?: CodeCapabilityConfigLoader;
}
/** Result from building tool classification */
interface BuildToolClassificationResult {
  /** Tool registry built from MCP tools (undefined if no MCP tools) */
  toolRegistry?: LCToolRegistry;
  /** Tool definitions array for event-driven execution (built simultaneously with registry) */
  toolDefinitions: LCTool[];
  /** Additional tools created (PTC and/or tool search) */
  additionalTools: GenericTool[];
  /** Whether any tools have defer_loading enabled (precomputed for efficiency) */
  hasDeferredTools: boolean;
  /** Both-direction identity aliases for MCP tools whose key spelling changed (see {@link collectMCPToolAliases}) */
  mcpToolAliases: MCPToolAlias[];
}
/**
* Checks if an agent's tools have any that match PTC patterns (programmatic only or dual context).
* @param toolRegistry - The tool registry to check
* @returns Whether any tools are configured for programmatic calling
*/
declare function agentHasProgrammaticTools(toolRegistry: LCToolRegistry): boolean;
/**
* Checks if an agent's tools have any that are deferred.
* @param toolRegistry - The tool registry to check
* @returns Whether any tools are configured as deferred
*/
declare function agentHasDeferredTools(toolRegistry: LCToolRegistry): boolean;
/**
* Builds the tool registry from MCP tools and conditionally creates PTC and tool search tools.
*
* This function:
* 1. Filters loaded tools for MCP tools
* 2. Extracts tool definitions and builds the registry from agent's tool_options
* 3. Cleans up temporary mcpJsonSchema properties
* 4. Creates PTC tool only if capabilities allow the agent's programmatic tools
* 5. Creates tool search tool only if agent has deferred tools
*
* @param params - Parameters including loaded tools, userId, agentId, agentToolOptions, and dependencies
* @returns Tool registry and any additional tools created
*/
declare function buildToolClassification(params: BuildToolClassificationParams): Promise<BuildToolClassificationResult>;
//#endregion
//#region src/agents/runtime.d.ts
/**
* Runtime-only state required to initialize and execute an Agent run.
*
* This context deliberately contains no transport objects. Ingress adapters
* may derive it from HTTP, while future execution hosts can rehydrate it from
* an authenticated principal and deployment configuration.
*/
interface AgentExecutionContext {
  user?: IUser;
  appConfig?: AppConfig;
  requestBody: RequestBody;
  /** Server-captured logical turn start time used by prompt variables. */
  turnStartedAt: number;
  /** Server-captured conversation creation time retained as historical metadata. */
  conversationCreatedAt?: string;
  /** Conversation already resolved by ingress. Presence distinguishes "not read" from absent. */
  resolvedConversation?: Partial<IConversation> | null;
}
/** Creates the transport-free context at the existing HTTP adapter seam. */
declare function createAgentExecutionContext({
  user,
  appConfig,
  requestBody,
  turnStartedAt,
  conversationCreatedAt,
  resolvedConversation,
  hasResolvedConversation
}: {
  user?: IUser;
  appConfig?: AppConfig;
  requestBody: RequestBody;
  turnStartedAt?: number;
  conversationCreatedAt?: string;
  resolvedConversation?: Partial<IConversation> | null;
  hasResolvedConversation?: boolean;
}): AgentExecutionContext;
/** Temporary adapter for request-backed Agent entry points. */
declare function createRequestAgentExecutionContext(req: ServerRequest, requestBody?: RequestBody): AgentExecutionContext;
//#endregion
//#region src/agents/initialize.d.ts
/**
* Bytes these files spend against the endpoint's total-size allowance, counting a file
* that appears in more than one set once. The sets overlap, an embedded attachment still
* missing the active code route being the case in point, and they are merged with the
* same deduplication downstream, so charging it twice spends an allowance the request
* never uses and drops another file that fits.
*/
/**
* Splits persistent files into those this request already screened and charged, and those
* still to screen. A setup file can also be the turn's attachment, and the sets are merged
* by id downstream, so charging it again against the remaining size allowance spends it
* twice and drops another file that would have fit.
*/
declare function partitionCommittedFiles<T extends {
  file_id?: string;
}>(files: T[], committed: Array<{
  file_id?: string;
}>): {
  committed: T[];
  pending: T[];
};
/**
* The request middleware already read this conversation once (`null` = looked up, absent).
* A stored document without `files` genuinely has none; only the branded lineage-only
* partial from a bound agent-event continuation cannot speak for the database.
*/
declare function readResolvedConversationFiles(runtime: Pick<AgentExecutionContext, "resolvedConversation">, conversationId: string): string[] | undefined;
interface ResolveResendToolResourcesParams {
  /** Tool names as configured on the agent. */
  tools?: string[] | null;
  /** `execute_code` capability AND the caller's `RUN_CODE` grant. */
  codeEnvAvailable: boolean;
  /**
  * `file_search` capability AND the caller's `FILE_SEARCH` grant. `undefined`
  * where the caller resolved neither, which leaves priming as it was.
  */
  fileSearchAvailable?: boolean;
}
/**
* Tool resources whose prior-turn files this run re-hydrates on resend.
*
* Both role-gated tools are filtered here rather than after hydration, because
* priming is not free: the files are read, their usage counters are bumped, and
* they are primed into `tool_resources` for a tool the loader is about to drop.
* Each flag is the deployment capability AND the role grant, so this reaches the
* same verdict the loader will.
*/
declare function resolveResendToolResources({
  tools,
  codeEnvAvailable,
  fileSearchAvailable
}: ResolveResendToolResourcesParams): Set<EToolResources>;
/**
* Extended agent type with additional fields needed after initialization
*/
type InitializedAgent = Agent & {
  /** Request-resolved Azure identity for self-summarization; never persisted on the agent. */azureOptions?: InitializeResultBase["azureOptions"];
  tools: GenericTool[]; /** @deprecated use requestAttachments or agentContextAttachments based on sharing semantics. */
  attachments: IMongoFile[]; /** Message files admitted for this agent, including historical files when resend is enabled. */
  requestAttachments: IMongoFile[]; /** Only hydrated attachments from the current request; excludes history and agent setup files. */
  currentRequestAttachments: TFile[]; /** Files attached to this agent's permanent context via tool_resources. */
  agentContextAttachments: IMongoFile[]; /** File-reading tools this turn runs, which decide when a tool-routed file falls back to text. */
  fileConsumers?: TurnFileConsumers;
  toolContextMap: Record<string, unknown>;
  dynamicToolContextMap?: Record<string, unknown>;
  maxContextTokens: number; /** Pre-ratio context budget (agentMaxContextNum - maxOutputTokensNum). Used by createRun to apply a configurable reserve ratio. */
  baseContextTokens?: number;
  useLegacyContent: boolean;
  resendFiles: boolean;
  /** Detail level TerraMind encodes image content blocks with, from the agent's
  * model parameters. Absent when the agent does not configure one. */
  imageDetail?: ImageDetail; /** How this agent receives its attachments this turn, settled once every routing input is final. */
  deliveryRouting: TurnDeliveryRouting;
  tool_resources?: AgentToolResources;
  userMCPAuthMap?: Record<string, Record<string, string>>; /** Tool map for ToolNode to use when executing tools (required for PTC) */
  toolMap?: ToolMap; /** Tool registry for PTC and tool search (only present when MCP tools with env classification exist) */
  toolRegistry?: LCToolRegistry$1; /** Run-scoped MCP tool definitions for request-scoped servers. */
  mcpAvailableTools?: Record<string, LCAvailableTools>; /** Run-scoped MCP connections for request-scoped servers. */
  requestScopedConnections?: RequestScopedMCPConnectionStore; /** Serializable tool definitions for event-driven execution */
  toolDefinitions?: LCTool$1[]; /** Both-direction identity aliases for MCP tools whose key spelling changed */
  mcpToolAliases?: MCPToolAlias[]; /** Precomputed flag indicating if any tools have defer_loading enabled (for efficient runtime checks) */
  hasDeferredTools?: boolean;
  /**
  * Names of this agent's tools that were injected with the `run_in_background`
  * param (capability enabled AND per-tool opt-in AND eligible). Threaded to the
  * tool executor via `configurable` so it can enforce the per-tool background
  * opt-in and gate the `check_background_task` poll tool at execution time.
  */
  backgroundToolNames?: string[];
  /**
  * Names of this agent's tools that received the host-injected `intent`
  * param (capability enabled AND opted in AND eligible). Threaded to the
  * tool executor via `configurable` so the arg is stripped before invoking
  * tools that don't declare it, and stripped from schemas a self-spawn
  * child or PTC sandbox inherits. SDK-native intent schemas are the tools'
  * own and are never listed here.
  */
  intentToolNames?: string[]; /** Marker-verified tool names whose model-authored intent labels are safe compaction guidance. */
  semanticIntentToolNames?: string[]; /** Tool names whose unverified schemas veto global semantic-intent trust for same-name calls. */
  semanticIntentBlockedToolNames?: string[];
  /** Whether the inline memory tools (`set_memory`/`delete_memory`) were
  *  registered for this agent. Authoritative TerraMind-only signal of the
  *  inline memory opt-in for the execution path, since some contexts hold the
  *  initialized config (the `memory` marker already expanded out of `tools`)
  *  rather than the raw agent document. */
  memoryToolsRegistered?: boolean; /** Whether the actions capability is enabled (resolved during tool loading) */
  actionsEnabled?: boolean;
  /**
  * The COMPLETE accessible-server audit this initialization resolved (via
  * the agent-key or skill-prime heal). Retained so deferred/event-driven
  * execution reuses the same snapshot instead of repeating the merged
  * registry read — a transient failure there would fail-closed a tool the
  * turn already advertised from the successful first audit.
  */
  accessibleMcpServerNames?: readonly string[]; /** Maximum characters allowed in a single tool result before truncation. */
  maxToolResultChars?: number; /** Response field to read model reasoning from for custom OpenAI-compatible endpoints. */
  reasoningKey?: ReasoningResponseKey; /** Whether to reconstruct `reasoning_content` from persisted history across turns (custom endpoint opt-in). */
  includeReasoningHistory?: boolean;
  /**
  * Whether the code-execution environment is available *for this agent*.
  * Narrower than the incoming `params.codeEnvAvailable` admin flag — this
  * is `admin_capability_enabled && agent.tools.includes('execute_code')`,
  * computed once here so downstream code (`injectSkillCatalog`,
  * `enrichWithSkillConfigurable`, `primeInvokedSkills`) doesn't have to
  * re-scan the tool list on every runtime handler invocation.
  * Authoritative for both persisted and ephemeral agents: the
  * ephemeral-agent toggle is reconciled into `agent.tools` upstream
  * (`packages/api/src/agents/added.ts`), so the check is uniform.
  */
  codeEnvAvailable: boolean;
  /**
  * Whether stateful code sessions are active *for this agent*: the admin
  * `stateful_code_sessions` capability AND the agent's builder opt-in
  * (`agent.stateful_code_sessions`) AND `codeEnvAvailable`. Resolved once
  * here and carried with the agent so execution routing never needs a
  * graph-global stateful flag.
  */
  statefulCodeSessions: boolean; /** Sharing scope for this agent's stateful code environment. */
  statefulCodeEnvironment: Agent["stateful_code_environment"]; /** Trusted partition for transient code session ids and file references. */
  codeSessionKey: string; /** Trusted endpoint/profile context for artifact processing and runtime tools. */
  codeExecutionContext: CodeExecutionContext; /** Whether host-side skill file authoring is available for this agent/run. */
  skillAuthoringAvailable: boolean; /** Host-side file authoring tool names registered for this run. */
  fileAuthoringToolNames?: Set<string>; /** Accessible skill IDs for ACL checking at execute time */
  accessibleSkillIds?: import("mongoose").Types.ObjectId[];
  /**
  * Names of skills the runtime can resolve, mirroring `accessibleSkillIds`.
  * Surfaced to the runtime so handlers like `read_file` can decide if a
  * `{firstSegment}/...` path refers to a real skill (vs. a code-env path
  * that should be routed to the bash fallback) without an extra DB lookup.
  */
  activeSkillNames?: Set<string>; /** Number of skills in the catalog (used to determine if SkillTool should be registered) */
  skillCount?: number;
  /**
  * Skills the user manually invoked for this turn via the `$` popover, resolved
  * to their SKILL.md bodies. The AgentClient injects these as meta user
  * messages right before the latest user message in the LLM's formatted
  * message array — deterministic priming without a tool roundtrip.
  */
  manualSkillPrimes?: ResolvedManualSkill[];
  /**
  * Skills auto-primed this turn because their `always-apply` frontmatter
  * flag is set. Resolved against the same `accessibleSkillIds` set and
  * subjected to the same active-state / ACL filters as the catalog, then
  * handed to the AgentClient for splicing alongside manual primes. Their
  * `allowedTools` entries also union into the agent's effective tool set
  * via `unionPrimeAllowedTools` (same pipeline as manual primes).
  */
  alwaysApplySkillPrimes?: ResolvedAlwaysApplySkill[];
  /**
  * Pre-uploaded code-env file refs from `tool_resources.execute_code`
  * (carries the conversation's prior-turn generated artifacts and any
  * user uploads). Captured by the `loadTools` callback; the AgentClient
  * merges these across all run agents into `Graph.sessions[EXECUTE_CODE]`
  * before run start so the very first `execute_code` / `bash_tool` call
  * sees them as `_injected_files`. Without this seed the agents-side
  * `CodeExecutor` falls back to `/files/{session_id}` — but `session_id`
  * is itself only populated by a previous successful execution, so on
  * call #1 the sandbox can't see the files at all.
  */
  primedCodeFiles?: import("@librechat/agents").CodeEnvFile[];
  /**
  * Resolved token/pricing config for this agent's endpoint (admin static
  * `tokenConfig` and/or fetched custom-endpoint config). Surfaced from the
  * provider `getOptions` result so the AgentClient bills, prices, and resolves
  * context limits with the same numbers the UI shows — not default rates.
  */
  endpointTokenConfig?: EndpointTokenConfig; /** Warnings from lazy file provisioning (e.g., failed uploads) */
  provisionWarnings?: string[]; /** State for deferred file provisioning — actual uploads happen at tool invocation time */
  provisionState?: ProvisionState;
};
declare const DEFAULT_MAX_CONTEXT_TOKENS = 32e3;
/** Returns true when a conversation-level choice disables an attached environment. */
declare function optsOutOfAttachedCodeEnvironment(agent: Agent, requestBody: RequestBody | undefined, environments: readonly CodeEnvironmentConfig[] | undefined, implicitStatefulRouteAvailable?: boolean): boolean;
/**
* Parameters for initializing an agent
* Matches the CJS signature from api/server/services/Endpoints/agents/agent.js
*/
interface InitializeAgentParams {
  /** Cancellation signal owned by the run performing initialization. */
  signal?: AbortSignal;
  /** Explicit transport-free execution state. */
  runtime?: AgentExecutionContext;
  /** Request-backed compatibility adapter for callers not yet migrated. */
  req?: ServerRequest;
  /** Deprecated response adapter retained only for source compatibility. */
  res?: Response$2;
  /** Agent to initialize */
  agent: Agent;
  /** Conversation ID (optional) */
  conversationId?: string | null;
  /** Parent message ID for determining the current thread (optional) */
  parentMessageId?: string | null;
  /** Normalized body used by MCP runtime placeholders during tool discovery. */
  requestBody?: RequestBody;
  /** Request files */
  requestFiles?: IMongoFile[];
  /** Host-authorized, hydrated inputs for an isolated execution. Suppresses parent history reads. */
  authorizedRunFiles?: readonly TFile[];
  /** Function to load agent tools */
  loadTools?: (params: {
    provider: string;
    agentId: string;
    tools: string[];
    model: string | null;
    tool_options: AgentToolOptions | undefined;
    tool_resources: AgentToolResources | undefined;
    requestBody?: RequestBody; /** Trusted endpoint/profile resolved for this agent before any code-file priming. */
    codeExecutionContext: CodeExecutionContext;
    /** Full accessible MCP server names (operator + user DB) when the heal
    *  already fetched them — lets execution-side collision guards see
    *  cross-tier shadowing without another registry round-trip. */
    accessibleMcpServerNames?: readonly string[];
  }) => Promise<{
    /** Full tool instances (only present when definitionsOnly=false) */tools?: GenericTool[];
    toolContextMap?: Record<string, unknown>;
    dynamicToolContextMap?: Record<string, unknown>;
    userMCPAuthMap?: Record<string, Record<string, string>>;
    toolRegistry?: LCToolRegistry$1;
    mcpAvailableTools?: Record<string, LCAvailableTools>;
    requestScopedConnections?: RequestScopedMCPConnectionStore; /** Serializable tool definitions for event-driven mode */
    toolDefinitions?: LCTool$1[];
    hasDeferredTools?: boolean;
    mcpToolAliases?: MCPToolAlias[];
    actionsEnabled?: boolean; /** Action tool names backed by OAuth — excluded from background dispatch. */
    oauthActionToolNames?: string[];
    /**
    * Pre-uploaded code-env file refs for the agent's
    * `tool_resources.execute_code`. Bubbled up so the run host can seed
    * `Graph.sessions[EXECUTE_CODE]` before the first tool call —
    * otherwise `_injected_files` is empty on call #1 and prior-turn
    * artifacts don't reach the sandbox.
    */
    primedCodeFiles?: import("@librechat/agents").CodeEnvFile[]; /** Live workspace binding resolved by the execution-side loader. */
    codeExecutionContext?: CodeExecutionContext;
  } | null>;
  /** Endpoint option (contains model_parameters and endpoint info) */
  endpointOption?: Partial<TEndpointOption>;
  /** Set of allowed providers */
  allowedProviders: Set<string>;
  /** Whether this is the initial agent */
  isInitialAgent?: boolean;
  /** Accessible skill IDs for this user (pre-computed by the caller via ACL query) */
  accessibleSkillIds?: import("mongoose").Types.ObjectId[];
  /** Whether skill file authoring should be exposed even before a user has viewable skills. */
  skillAuthoringAvailable?: boolean;
  /** Whether the code execution environment is available (execute_code capability enabled) */
  codeEnvAvailable?: boolean;
  /**
  * Whether `file_search` is available to this caller — the capability AND the
  * `FILE_SEARCH` role grant. Read only when re-hydrating a conversation's
  * prior-turn files: `false` skips priming resources for a tool the loader will
  * drop anyway. Absent leaves priming unconditional, so a caller that has not
  * resolved the grant keeps its current behavior.
  */
  fileSearchAvailable?: boolean;
  /**
  * Resolves this caller's `WEB_SEARCH` role grant for provider-native web search.
  * Called only when the built provider config turns native search on, so an
  * agent without it costs no role read. Callers that reach `initializeAgent`
  * with `runtime` and no `req` — the OpenAI-compatible and Responses routes,
  * and the embedder surface in `agents/openai/service.ts` — pass one that joins
  * the grants their request already memoizes. Absent, the grant is resolved
  * from `db.getRoleByName`.
  */
  resolveWebSearchGrant?: () => Promise<boolean>;
  /**
  * Whether the `run_in_background` capability is enabled for this run. When
  * true, tools the agent opted in via `tool_options[name].run_in_background`
  * (plus the background-native code pair, unless explicitly opted out) get a
  * `run_in_background` schema param and the `check_background_task` poll
  * tool is registered.
  */
  backgroundToolsAvailable?: boolean;
  /**
  * Whether the `tool_intents` capability is enabled for this run. When true,
  * tools opted in via `tool_options[name].describe_intent` (native host
  * tools default on) get an `intent` string injected as the FIRST schema
  * property, rendered by the client as the call's live status label.
  */
  toolIntentsAvailable?: boolean;
  /** Whether stateful code sessions are available (stateful_code_sessions capability enabled) */
  statefulSessionsAvailable?: boolean;
  /** Explicit deployment allowlist for request types that do not carry TerraMind config on req. */
  allowedStatefulCodeEnvironments?: readonly StatefulCodeEnvironment[];
  /** Whether inline memory tools are available (memory capability enabled, memory
  *  configured, and the user permitted). When true and the agent lists the `memory`
  *  capability, `set_memory` + `delete_memory` are registered for the LLM. */
  memoryAvailable?: boolean;
  /** Per-user skill active/inactive overrides for filtering the skill catalog. */
  skillStates?: Record<string, boolean>;
  /** Admin-configured default for shared skills (`true` = shared skills auto-activate). */
  defaultActiveOnShare?: boolean;
  /**
  * Skill names the user invoked manually for this turn via the `$` popover.
  * Resolved here (ACL + active-state filtered) and attached to the returned
  * InitializedAgent as `manualSkillPrimes` for the AgentClient to inject as
  * meta user messages before the LLM call.
  */
  manualSkills?: string[];
}
/**
* Database methods required for agent initialization
* Most methods come from data-schemas via createMethods()
* getConvoFiles not yet in data-schemas but included here for consistency
*/
interface InitializeAgentDbMethods extends EndpointDbMethods {
  /**
  * Names of every MCP server the user can reach (operator config + user DB).
  * Consulted by the legacy-key heal ONLY when a configured name needs
  * normalization: collision detection must see user-DB servers, or healing a
  * raw key could produce a key that direct-first resolution routes to a
  * different (DB) server. Optional — without it the heal falls back to the
  * operator-config names.
  */
  getAccessibleMcpServerNames?: (userId?: string, role?: string) => Promise<string[]>;
  /** Update usage tracking for multiple files */
  updateFilesUsage: (files: Array<{
    file_id: string;
  }>, fileIds?: string[], options?: {
    user?: string;
    tenantId?: string | null;
  }) => Promise<unknown[]>;
  /** Get files from database */
  getFiles: (filter: unknown, sort: unknown, select: unknown) => Promise<unknown[]>;
  /** Filter files by agent access permissions (ownership or agent attachment) */
  filterFilesByAgentAccess?: TFilterFilesByAgentAccess;
  /** Get tool files by IDs (user-uploaded files only, code files handled separately) */
  getToolFilesByIds: (fileIds: string[], toolSet: Set<EToolResources>, ownerScope?: FileOwnerScope) => Promise<unknown[]>;
  /** Get conversation file IDs */
  getConvoFiles: (conversationId: string) => Promise<string[] | null>;
  /** Get code-generated files by conversation ID and the file_ids
  *  referenced from messages in the current thread (collected via
  *  `messages.files[].file_id` during thread walk). */
  getCodeGeneratedFiles?: (conversationId: string, threadFileIds?: string[], ownerScope?: FileOwnerScope) => Promise<unknown[]>;
  /** Get user-uploaded execute_code files by file IDs (from message.files in thread) */
  getUserCodeFiles?: (fileIds: string[], ownerScope: FileOwnerScope) => Promise<unknown[]>;
  getDeferredProvisionFiles?: (fileIds: string[], ownerScope: FileOwnerScope, resources?: {
    code?: boolean;
    search?: boolean;
    codeRouteKey?: string;
    searchNamespaces?: string[];
    hydrateProvisioned?: boolean;
  }) => Promise<unknown[]>;
  /** Get messages for a conversation (supports select for field projection) */
  getMessages?: (filter: {
    conversationId: string;
  }, select?: string) => Promise<Array<{
    messageId: string;
    parentMessageId?: string;
    files?: Array<{
      file_id: string;
    }>;
  }> | null>;
  /** List skill summaries for catalog injection (paginated, omits body/frontmatter) */
  listSkillsByAccess?: TListSkillsByAccess;
  /** Load a single skill by name, constrained to an ACL-accessible ID set. */
  getSkillByName?: TGetSkillByName;
  /**
  * Load accessible skills with `alwaysApply: true`, eagerly including
  * `body` so the priming pipeline can splice at turn start without a
  * per-skill round-trip. Cursor-paginated so the resolver can fill its
  * active-state budget even when early-sorted rows are inactive for
  * the current user.
  */
  listAlwaysApplySkills?: (params: {
    accessibleIds: import("mongoose").Types.ObjectId[];
    limit: number;
    cursor?: string | null;
  }) => Promise<{
    skills: Array<{
      _id: import("mongoose").Types.ObjectId;
      name: string;
      body: string;
      author: import("mongoose").Types.ObjectId;
      allowedTools?: string[]; /** True for deployment-directory skills that are loaded in memory. */
      deployment?: boolean;
    }>;
    has_more?: boolean;
    after?: string | null;
  }>;
  /** Optional: provision a file to the code execution environment */
  provisionToCodeEnv?: TProvisionToCodeEnv;
  /** Optional: provision a file to the vector DB for file_search */
  provisionToVectorDB?: TProvisionToVectorDB;
  /** Optional: batch-check code env file liveness */
  checkSessionsAlive?: TCheckSessionsAlive;
  /** Optional: load CODE_API_KEY once per request */
  loadCodeApiKey?: TLoadCodeApiKey;
  /** Optional: persist file metadata updates after provisioning */
  updateFile?: (data: TFileUpdate) => Promise<unknown>;
  /** Resolves a role by name for the tool role-permission grants. Optional: when
  *  absent the role half of the web-search gate is not applied. */
  getRoleByName?: CheckAccessParams["getRoleByName"];
}
/**
* Initializes an agent for use in requests.
* Handles file processing, tool loading, provider configuration, and context token calculations.
*
* This function is exported from @librechat/api and replaces the CJS version from
* api/server/services/Endpoints/agents/agent.js
*
* @param params - Initialization parameters
* @param deps - Optional dependency injection for testing
* @returns Promise resolving to initialized agent with tools and configuration
* @throws Error if agent provider is not allowed or if required dependencies are missing
*/
declare function initializeAgent(params: InitializeAgentParams, db?: InitializeAgentDbMethods): Promise<InitializedAgent>;
//#endregion
//#region src/agents/discovery.d.ts
/**
* Callback invoked after a sub-agent is successfully initialized.
* Used by callers that need to track per-agent tool context (e.g., for
* the ON_TOOL_EXECUTE event handler closure).
*/
type OnAgentInitializedCallback = (agentId: string, agent: Agent, config: InitializedAgent) => void;
/**
* Minimal permission check signature used to verify VIEW access on a
* candidate sub-agent before it is loaded into the run.
*/
type CheckAgentPermission = (params: {
  userId: string;
  role?: string;
  resourceType: string;
  resourceId: unknown;
  requiredPermission: number;
}) => Promise<boolean>;
interface DiscoverConnectedAgentsParams {
  req: ServerRequest;
  res: Response$2;
  /** Owning run signal used to distinguish cancellation from dependency aborts. */
  signal?: AbortSignal;
  /** The already-initialized primary agent config (starting point for BFS). */
  primaryConfig: InitializedAgent;
  /**
  * Optional legacy chain: agent IDs to append as sequential direct edges.
  * Used by the deprecated Agent Chain feature.
  */
  agent_ids?: string[];
  endpointOption?: Partial<TEndpointOption>;
  allowedProviders: Set<string>;
  modelsConfig: TModelsConfig;
  loadTools: InitializeAgentParams["loadTools"];
  requestFiles?: InitializeAgentParams["requestFiles"];
  conversationId?: string | null;
  parentMessageId?: string | null;
  /** Normalized runtime request metadata forwarded to MCP tool loading. */
  requestBody?: InitializeAgentParams["requestBody"];
  /**
  * ResourceType to check each sub-agent's access against. Defaults to
  * `AGENT` for the in-app chat flow. Callers whose entry-point gates on
  * a different resource type (e.g. the OpenAI-compat controllers gate on
  * `REMOTE_AGENT`) must pass the matching resource type so sub-agents
  * don't bypass the same sharing boundary enforced at the route.
  */
  resourceType?: string;
  /**
  * Optional per-sub-agent skill scoper. When provided, its return value
  * is forwarded to `initializeAgent` as `accessibleSkillIds` so each
  * handoff agent sees only the skills that match its own `skills`
  * allowlist (or the full accessible set when scoping is disabled).
  */
  computeAccessibleSkillIds?: (agent: Agent) => InitializeAgentParams["accessibleSkillIds"];
  /** Optional per-sub-agent skill authoring gate, paired with the scoped skill IDs. */
  computeSkillAuthoringAvailable?: (agent: Agent, accessibleSkillIds: InitializeAgentParams["accessibleSkillIds"]) => InitializeAgentParams["skillAuthoringAvailable"];
  /** Per-user skill active/inactive state, forwarded to each sub-agent. */
  skillStates?: InitializeAgentParams["skillStates"];
  /** Default active-on-share flag, forwarded to each sub-agent. */
  defaultActiveOnShare?: InitializeAgentParams["defaultActiveOnShare"];
  /**
  * Whether the `execute_code` capability is enabled for the run. Forwarded
  * verbatim to each handoff sub-agent so `registerCodeExecutionTools` can
  * expand `agent.tools: ['execute_code']` into the `bash_tool` + `read_file`
  * pair. Omitted (or `undefined`) → the expansion is skipped, matching the
  * primary-agent gate; callers that already resolved the capability set
  * for the primary SHOULD forward the same value here or sub-agents lose
  * code-execution tooling even though their parent had it.
  */
  codeEnvAvailable?: InitializeAgentParams["codeEnvAvailable"];
  /**
  * Sibling of `codeEnvAvailable` for the other role-gated tool — the
  * `file_search` capability AND the caller's `FILE_SEARCH` grant. Forwarded
  * verbatim so a handoff agent re-hydrates prior-turn search files on exactly
  * the terms its parent did.
  */
  fileSearchAvailable?: InitializeAgentParams["fileSearchAvailable"];
  /**
  * The caller's `WEB_SEARCH` grant resolver, forwarded so a handoff or subagent
  * whose provider config turns native web search on is authorized by the same
  * request-memoized read as its parent.
  */
  resolveWebSearchGrant?: InitializeAgentParams["resolveWebSearchGrant"];
  /** Sibling of `codeEnvAvailable` — the `stateful_code_sessions` capability flag, forwarded to every handoff `initializeAgent`. */
  statefulSessionsAvailable?: InitializeAgentParams["statefulSessionsAvailable"];
  /** Deployment policy for stateful workspace scopes, forwarded unchanged to every referenced agent. */
  allowedStatefulCodeEnvironments?: InitializeAgentParams["allowedStatefulCodeEnvironments"];
  /**
  * Run-level inline memory availability gate. Forwarded verbatim to every
  * handoff agent so sub-agents that list the `memory` capability expand the
  * `set_memory` + `delete_memory` pair only when the parent run permits it.
  */
  memoryAvailable?: InitializeAgentParams["memoryAvailable"];
  /**
  * Run-level `run_in_background` capability gate. Forwarded verbatim so a
  * handoff/connected agent's own event-driven tools with
  * `tool_options[tool].run_in_background` (and its background-native code
  * pair) get the injected param + poll tool, matching how the same agent
  * behaves when run as the primary.
  */
  backgroundToolsAvailable?: InitializeAgentParams["backgroundToolsAvailable"];
  /**
  * Run-level `tool_intents` capability gate. Forwarded verbatim so a
  * handoff/connected agent's opted-in tools get the injected `intent` param,
  * matching how the same agent behaves when run as the primary.
  */
  toolIntentsAvailable?: InitializeAgentParams["toolIntentsAvailable"];
}
interface DiscoverConnectedAgentsDeps {
  /** Fetch an agent by string id from the database. */
  getAgent: (filter: {
    id: string;
  }) => Promise<Agent | null>;
  /** Permission check (typically a wrapper around PermissionService.checkPermission). */
  checkPermission: CheckAgentPermission;
  /** Violation logger passed through to validateAgentModel. */
  logViolation: ValidateAgentModelParams["logViolation"];
  /** DB methods consumed by initializeAgent for each sub-agent. */
  db: InitializeAgentDbMethods;
  /** Optional callback invoked after each sub-agent is initialized. */
  onAgentInitialized?: OnAgentInitializedCallback;
  /** Optional callback invoked when an agent id is skipped (missing or no access). */
  onAgentSkipped?: (agentId: string) => void;
  /**
  * Optional override for `initializeAgent`. Exists primarily so JS callers
  * can inject their test doubles via `jest.mock('@librechat/api')` — since
  * this module's own direct import would otherwise bypass that mock.
  */
  initializeAgent?: typeof initializeAgent;
  /** Optional override for `validateAgentModel` (same DI rationale). */
  validateAgentModel?: typeof validateAgentModel;
}
interface DiscoverConnectedAgentsResult {
  /** Map of agentId -> initialized config for every discovered sub-agent. */
  agentConfigs: Map<string, InitializedAgent>;
  /** Deduplicated, orphan-filtered edges across the primary and sub-agents. */
  edges: GraphEdge[];
  /** Agent ids that were requested but could not be loaded (missing or no access). */
  skippedAgentIds: Set<string>;
  /** Merged MCP auth map from the primary and all sub-agents. */
  userMCPAuthMap?: Record<string, Record<string, string>>;
}
type GraphSubagentHostConfig = InitializedAgent & {
  subagentGraphConfigs?: Array<{
    definition: AgentSubagentGraph;
    memberConfigs: InitializedAgent[];
  }>;
};
interface ResolveSubagentGraphsParams extends DiscoverConnectedAgentsParams {
  /** Top-level primary/handoff configs whose saved graph spawn targets should be resolved. */
  rootConfigs: GraphSubagentHostConfig[];
}
/** Resolves saved graph spawn targets without promoting graph-only members to top-level nodes. */
declare function resolveSubagentGraphs(params: ResolveSubagentGraphsParams, deps: DiscoverConnectedAgentsDeps): Promise<Record<string, Record<string, string>> | undefined>;
/**
* Discovers and initializes all agents reachable from `primaryConfig.edges`
* via BFS. This is the shared graph-topology discovery logic that enables
* multi-agent handoffs (A -> B -> C) in both the primary chat flow and the
* OpenAI-compatible / Responses API controllers.
*
* Skips agents the caller cannot load (missing from DB or lacking VIEW
* permission) and filters out orphaned edges so `createRun` never sees an
* edge pointing at a missing node — which would otherwise trigger a
* `Found edge ending at unknown node` validation error from StateGraph.
*/
declare function discoverConnectedAgents(params: DiscoverConnectedAgentsParams, deps: DiscoverConnectedAgentsDeps): Promise<DiscoverConnectedAgentsResult>;
//#endregion
//#region src/agents/edges.d.ts
/**
* Rewrites an agent id wherever it appears as an edge source.
*
* Agent creation uses an empty source id until the server assigns the
* persisted id, while agent duplication needs to move the copied router's
* outgoing edges to the clone. Keeping both cases here makes the rewrite
* consistent for scalar and multi-source edges.
*/
declare function replaceEdgeSourceId(edges: GraphEdge[] | undefined, previousSourceId: string, nextSourceId: string): GraphEdge[] | undefined;
/**
* Creates a stable key for edge deduplication.
* Handles both single and array-based from/to values.
*/
declare function getEdgeKey(edge: GraphEdge): string;
/**
* Extracts all agent IDs referenced in an edge (both from and to).
*/
declare function getEdgeParticipants(edge: GraphEdge): string[];
/**
* Drops orphaned agents from edge endpoints, returning only the edges
* that still have at least one valid source and one valid destination.
*
* For multi-source / multi-destination edges, orphaned ids are stripped
* from the array rather than dropping the whole edge: `{ from: ['A','B'],
* to: 'C' }` with B skipped becomes `{ from: ['A'], to: 'C' }`. This
* matches the agents SDK's `MultiAgentGraph.createWorkflow`, which adds
* one LangGraph edge per source/destination pair (`builder.addEdge(src,
* dest)`), so losing one co-source (or one co-destination) doesn't
* invalidate the routes through the remaining members.
*
* An edge is dropped only when it has no surviving source OR no
* surviving destination — the SDK's per-source `addEdge` can't run if
* either side is empty, and any orphaned id left in place would still
* trigger `Found edge ending at unknown node` at compile time.
*/
declare function filterOrphanedEdges(edges: GraphEdge[], skippedAgentIds: Set<string>): GraphEdge[];
/** Applies the runtime graph's post-filter reachability rules to loaded agents and edges. */
declare function resolveReachableGraph(explicitSeeds: Iterable<string>, agentIds: Iterable<string>, preFilterEdges: GraphEdge[], skippedAgentIds: Set<string>): {
  reachable: Set<string>;
  edges: GraphEdge[];
};
/** Collects all unique agent IDs referenced across an array of edges. */
declare function collectEdgeAgentIds(edges: GraphEdge[] | undefined): Set<string>;
/**
* Result of discovering and aggregating edges from connected agents.
*/
interface EdgeDiscoveryResult {
  /** Deduplicated edges from all discovered agents */
  edges: GraphEdge[];
  /** Agent IDs that were not found (orphaned references) */
  skippedAgentIds: Set<string>;
}
/**
* Collects and deduplicates edges, tracking new agents to process.
* Used for BFS discovery of connected agents.
*/
declare function createEdgeCollector(checkAgentInit: (agentId: string) => boolean, skippedAgentIds: Set<string>): {
  edgeMap: Map<string, GraphEdge>;
  agentsToProcess: Set<string>;
  collectEdges: (edgeList: GraphEdge[] | undefined) => void;
};
//#endregion
//#region src/agents/eventRetention.d.ts
/** A missing deadline is durable; an invalid or elapsed deadline is never active. */
declare function isAgentEventRetentionActive(expiredAt: unknown, now?: number): boolean;
//#endregion
//#region src/agents/openai/types.d.ts
/**
* OpenAI-compatible types for the agent chat completions API.
* These types follow the OpenAI API spec for /v1/chat/completions.
*
* Note: This API uses agent_id as the "model" parameter per OpenAI spec.
* In the future, this will be extended to support the Responses API.
*/
/**
* Content part types for OpenAI format
*/
interface OpenAITextContentPart {
  type: "text";
  text: string;
}
interface OpenAIImageContentPart {
  type: "image_url";
  image_url: {
    url: string;
    detail?: "auto" | "low" | "high";
  };
}
type OpenAIContentPart = OpenAITextContentPart | OpenAIImageContentPart;
/**
* Tool call in OpenAI format
*/
interface ToolCall {
  id: string;
  type: "function";
  function: {
    name: string;
    arguments: string;
  };
}
/**
* OpenAI chat message format
*/
interface ChatMessage {
  role: "system" | "user" | "assistant" | "tool";
  content: string | OpenAIContentPart[] | null;
  name?: string;
  tool_calls?: ToolCall[];
  tool_call_id?: string;
}
/**
* OpenAI chat completion request
*/
interface ChatCompletionRequest {
  /** Agent ID to invoke (maps to model in OpenAI spec) */
  model: string;
  /** Conversation messages */
  messages: ChatMessage[];
  /** Whether to stream the response */
  stream?: boolean;
  /** Maximum tokens to generate */
  max_tokens?: number;
  /** Temperature for sampling */
  temperature?: number;
  /** Top-p sampling */
  top_p?: number;
  /** Frequency penalty */
  frequency_penalty?: number;
  /** Presence penalty */
  presence_penalty?: number;
  /** Stop sequences */
  stop?: string | string[];
  /** User identifier */
  user?: string;
  /** Conversation ID (TerraMind extension) */
  conversation_id?: string;
  /** Parent message ID (TerraMind extension) */
  parent_message_id?: string;
}
/**
* Token usage information
*/
interface CompletionUsageTotals {
  prompt_tokens: number;
  completion_tokens: number;
  total_tokens: number;
}
interface CompletionUsage {
  prompt_tokens: number;
  completion_tokens: number;
  total_tokens: number;
  /** Detailed breakdown of output tokens (OpenRouter/OpenAI convention) */
  completion_tokens_details?: {
    reasoning_tokens?: number;
  };
  /** TerraMind extension for parent, handoff, and summarization model calls. */
  primary?: CompletionUsageTotals;
  /** TerraMind extension for isolated subagent child model calls. */
  subagent?: CompletionUsageTotals;
}
/**
* Non-streaming choice
*/
interface ChatCompletionChoice {
  index: number;
  message: {
    role: "assistant";
    content: string | null; /** Reasoning/thinking content (OpenRouter convention) */
    reasoning?: string | null;
    tool_calls?: ToolCall[];
  };
  finish_reason: "stop" | "length" | "tool_calls" | "content_filter" | null;
}
/**
* Non-streaming response
*/
interface ChatCompletionResponse {
  id: string;
  object: "chat.completion";
  created: number;
  model: string;
  choices: ChatCompletionChoice[];
  usage?: CompletionUsage;
}
/**
* Streaming choice delta
* Note: `reasoning` field follows OpenRouter convention for streaming reasoning/thinking content
*/
interface ChatCompletionChunkChoice {
  index: number;
  delta: {
    role?: "assistant";
    content?: string | null; /** Reasoning/thinking content (OpenRouter convention) */
    reasoning?: string | null;
    tool_calls?: Array<{
      index: number;
      id?: string;
      type?: "function";
      function?: {
        name?: string;
        arguments?: string;
      };
    }>;
  };
  finish_reason: "stop" | "length" | "tool_calls" | "content_filter" | null;
}
/**
* Streaming response chunk
*/
interface ChatCompletionChunk {
  id: string;
  object: "chat.completion.chunk";
  created: number;
  model: string;
  choices: ChatCompletionChunkChoice[];
  /** Final chunk may include usage */
  usage?: CompletionUsage;
}
/**
* SSE event wrapper for streaming
*/
interface SSEEvent {
  data: ChatCompletionChunk | "[DONE]";
}
/**
* Context for building OpenAI responses
*/
interface OpenAIResponseContext {
  /** Request ID for the chat completion */
  requestId: string;
  /** Model/agent ID */
  model: string;
  /** Created timestamp */
  created: number;
}
/**
* Aggregated content for building final response
*/
interface AggregatedContent {
  text: string;
  toolCalls: ToolCall[];
}
/**
* Error response in OpenAI format
*/
interface OpenAIErrorResponse {
  error: {
    message: string;
    type: string;
    param: string | null;
    code: string | null;
  };
}
//#endregion
//#region src/agents/backgroundCompletion.d.ts
/** Deadline after which an invocation owner requests cancellation. */
declare const BACKGROUND_TASK_TIMEOUT_MS: number;
/** Gives a cooperative tool a short window to settle after cancellation before
* its automatic completion delivery is retired. */
declare const BACKGROUND_TASK_ABORT_GRACE_MS: number;
/** Three missed heartbeats prove the process-local executor has been lost. */
declare const BACKGROUND_TOOL_PRODUCER_LEASE_MS: number;
declare const BACKGROUND_TOOL_PRODUCER_HEARTBEAT_MS: number;
/** Host-owned identity recorded before ordinary background tool work begins. */
interface BackgroundToolWakeupRegistration {
  taskId: string;
  toolCallId: string;
  toolName: string;
  userId: string;
  tenantId?: string;
  conversationId: string;
  parentMessageId: string;
  parentAgentId?: string;
  createdAt: number;
}
interface BackgroundToolWakeupRetireOptions {
  /** Retire only before a resolver owns the delivery. */
  onlyIfUnclaimed?: boolean;
  /** Reconcile only after the delivery is irreversibly dead-lettered. */
  onlyIfDead?: boolean;
}
/** Process-local handle for the durable delivery admitted before launch. */
interface BackgroundToolWakeupAdmission {
  /** Renews durable proof that the process-local executor still owns work. */
  renew: () => Promise<boolean>;
  /** Retires a delivery whose terminal result can no longer be made durable.
  * Manual polling requires an atomic unclaimed-only transition: once a
  * resolver owns a lease, its prepared continuation cannot be cancelled. */
  retire: (reason: string, options?: BackgroundToolWakeupRetireOptions) => Promise<boolean>;
}
/** Durable ownership repair used by a manual poll after an automatic
* continuation delivery has irreversibly dead-lettered. `claimId` names the
* batch-root delivery that owns every claimed sibling, not necessarily the
* polled task's own pre-admitted delivery. */
interface BackgroundToolDeadClaimRecoveryInput {
  userId: string;
  conversationId: string;
  messageId: string;
  claimId: string;
  /** Omitted for the legacy automatic-wakeup recovery path. */
  kind?: "manual" | "wakeup";
  /** Required to prove that a manual claim's owning generation is no longer active. */
  generationId?: string;
}
type BackgroundToolDeadClaimRecovery = (input: BackgroundToolDeadClaimRecoveryInput) => Promise<boolean>;
//#endregion
//#region src/agents/codeFilesSession.d.ts
/**
* Minimal shape for an agent that may contribute primed code files to its
* execution-profile partition. Both `InitializedAgent` and `RunAgent` satisfy it,
* and the recursive walk in {@link buildInitialToolSessions} traverses
* legacy child configs and graph-member configs so agents pruned from the
* top-level `agentConfigs` map still contribute.
*/
interface CodeFilesAgent {
  id?: string;
  codeEnvAvailable?: boolean;
  codeExecutionContext?: CodeExecutionContext;
  codeSessionKey?: string;
  primedCodeFiles?: CodeEnvFile[];
  statefulCodeSessions?: boolean;
  statefulCodeEnvironment?: StatefulCodeEnvironment;
  subagentAgentConfigs?: CodeFilesAgent[];
  lazySubagentConfigs?: CodeFilesAgent[];
  subagentGraphConfigs?: Array<{
    memberConfigs: CodeFilesAgent[];
  }>;
}
interface CodeExecutionProfileRoute {
  codeExecutionContext: CodeExecutionContext;
  codeSessionKeys: string[];
}
/** Collects the distinct Code API deployments used by a run and every
* trusted session partition that must receive that deployment's immutable
* skill-file seed. */
declare function collectCodeExecutionProfileRoutes(agents: Iterable<CodeFilesAgent | undefined | null>, scope?: {
  userId: string;
  conversationId?: string | null;
}): CodeExecutionProfileRoute[];
/**
* Merges primed code-execution file references into a `ToolSessionMap` so the
* Graph's `ToolNode` can inject them into the very first `execute_code` tool
* call as `_injected_files`. Without this seed, `ToolNode.getCodeSessionContext`
* has no entry to read on call #1, and the agent-side `CodeExecutor` falls back
* to a `/files/{session_id}` fetch — but `session_id` itself is only populated
* after the first call returns one, so primed files were silently dropped.
*
* Files from `primeFiles` (api/server/services/Files/Code/process.js) carry
* per-file `storage_session_id`s (the long-lived storage bucket id). The
* map's representative top-level `session_id` is seeded from the first
* incoming file's `storage_session_id` (matching `primeInvokedSkills`),
* since no execution session exists yet at seed time. Per-file ids on the
* `files` array are what `ToolNode` actually uses
* (`file.storage_session_id ?? execSessionId`).
*
* When an entry already exists (e.g. seeded by `primeInvokedSkills` for skill
* files), incoming files are appended after the existing ones. The pre-existing
* representative `session_id` is preserved so a partial-cache/fresh-prime
* collision doesn't shift which session id `ToolNode` picks for the call.
*
* Files are deduplicated by `storage_session_id + id` as the stable
* identity key. Multiple agents in the same run commonly carry the same
* primed code-execution resources (shared conversation files), and without
* dedupe `_injected_files` would grow proportionally to agent count and
* inflate every `/exec` POST. First-seen wins so the original ordering /
* source is preserved.
*
* Identity dedupe alone cannot keep the seed valid: codeapi rejects the
* whole `/exec` request when two entries mount at one destination, and a
* file re-uploaded by one agent but cache-hit by another arrives twice
* under different `storage_session_id`s with a single `name`. Sources are
* merged in trust order — skill seed, then primary agent, then the rest —
* so first-seen also wins the destination, and the later copy of an
* already-mounted name is dropped rather than renamed to a path nothing
* told the model about.
*
* Each agent resolves destinations over its own candidate set, so this can
* still drop a genuinely distinct file: two agents whose *private*
* resources share a filename each claim the bare name locally, and only the
* first survives here while the second agent's tool context keeps
* advertising its own file at that path. `sortCodeFilesByDestinationPriority`
* ranks conversation-scoped files above private ones precisely so the shared
* majority cannot diverge that way; closing the private-versus-private case
* needs one assignment across contributors, which means resolving
* destinations before any agent renders its tool context. Until then this
* drop is the failure floor — before it, the pair reached codeapi together
* and took the whole run down with a rejected request.
*/
declare function seedCodeFilesIntoSessions(files: CodeEnvFile[] | undefined, existing: ToolSessionMap | undefined, sessionKey?: string): ToolSessionMap | undefined;
/**
* Adds primed or freshly provisioned file refs to one code-session context, keeping
* the ordering and identity rules the graph seed uses so both entry points agree on
* what the sandbox receives.
*
* Composes `(storage_session_id, id)` as the identity. `name` alone isn't sufficient:
* two distinct uploads can share a filename across different storage sessions and
* file_ids. First seen wins, so pre-existing refs keep their position.
*
* The representative top-level `session_id` is preserved when one already exists,
* otherwise it stands in from the first incoming file's `storage_session_id`, since
* no execution session exists until the first call returns one. ToolNode reads the
* per-file `storage_session_id` for actual injection, so the representative is
* informational. Mirrors the convention in `primeInvokedSkills`.
*
* Returns undefined when there is nothing usable to record.
*/
declare function mergeCodeFilesIntoContext(prior: {
  session_id?: string;
  files?: CodeEnvFile[] | FileRefs;
} | undefined, files: CodeEnvFile[] | undefined, sessionKey?: string): {
  session_id: string;
  files: CodeEnvFile[];
} | undefined;
/** Builds an isolated child-graph seed from the run's exact trusted partition
* plus files resolved specifically for that agent. Lazy subagents are resolved
* after the run-wide seed is built, so their attachments must be copied here
* when `AgentInputs` is created. */
declare function buildAgentInitialToolSessions(agent: CodeFilesAgent, runSessions: ToolSessionMap | undefined): ToolSessionMap | undefined;
/**
* Builds the run-wide `ToolSessionMap` for `Graph.sessions`, partitioned by
* each agent's trusted `codeSessionKey`. The legacy `execute_code` partition
* remains shared by stateless agents. Stateful agents share only when their
* configured environment resolves to the same key.
*
* Skill files are immutable input resources for the run, but their storage
* pointers are deployment-local. Callers therefore pre-seed each exact
* profile partition; this helper never copies a pointer across partitions.
*
* **Walk order:** primary first, then `agentConfigs` (handoff/addedConvo)
* in iteration order, then recurse through legacy children and graph members
* breadth-first. Order matters because when no
* skill sessions exist, the FIRST agent's first file supplies the
* representative `session_id` written to `Graph.sessions[EXECUTE_CODE]`.
* `ToolNode` ultimately uses per-file `session_id`s for injection so
* the representative is informational rather than load-bearing, but
* primary-first keeps it predictable and matches the existing
* `loadSubagentsFor` walk pattern in `Endpoints/agents/initialize.js`.
*
* The visited set is keyed by object identity (`Set<CodeFilesAgent>`)
* so cycles in a malformed agent graph (a subagent that points back at
* its parent) can't infinite-loop the seed.
*
* @param skillSessions - Output of `primeInvokedSkills` — already
*   contains an `EXECUTE_CODE` entry when skill files were primed; new
*   files from this walk merge into it (representative `session_id`
*   from the skill side is preserved).
* @param agents - The complete set of code-execution-capable agents in
*   the run. Caller passes `[primaryConfig, ...agentConfigs.values()]`;
*   this function recurses into every reachable subagent configuration.
*/
declare function buildInitialToolSessions(params: {
  skillSessions?: ToolSessionMap;
  agents: Iterable<CodeFilesAgent | undefined | null>;
}): ToolSessionMap | undefined;
//#endregion
//#region src/agents/skillFiles.d.ts
interface SkillFileRecord {
  relativePath: string;
  filename: string;
  filepath: string;
  source: string;
  bytes: number;
  codeEnvRef?: CodeEnvRef;
  codeEnvRefs?: CodeEnvRefMap;
}
interface PrimeSkillFilesParams {
  skill: {
    body: string;
    name: string;
    frontmatter?: Record<string, unknown>;
    _id: Types.ObjectId | string;
    /** Monotonic counter on the skill record. Bumped on every edit
    *  (frontmatter / body / file upsert). Threaded into `codeEnvRef.version`
    *  so codeapi's sessionKey scopes the cache per-revision. */
    version: number;
  };
  skillFiles: SkillFileRecord[];
  req: ServerRequest;
  /** Optional operation-wide wait allowance shared by sibling skill primes. */
  rateLimitBudget?: CodeApiRateLimitBudget;
  /** Effective foreground cancellation signal for acquisition, waits, and transport. */
  signal?: AbortSignal;
  getStrategyFunctions: (source: string) => {
    getDownloadStream?: (req: ServerRequest, filepath: string, options?: {
      signal?: AbortSignal;
    }) => Promise<NodeJS.ReadableStream>;
    [key: string]: unknown;
  };
  batchUploadCodeEnvFiles: (params: {
    req: ServerRequest;
    files: Array<{
      stream: NodeJS.ReadableStream;
      filename: string;
    }>;
    /** Resource kind that owns the batch's storage session. Drives codeapi's
    *  sessionKey derivation (`<tenant>:<kind>:<id>[:v:<version>]`). */
    kind: "skill" | "agent" | "user"; /** Resource id (skillId / agentId / userId). */
    id: string; /** Required when `kind === 'skill'`; forbidden otherwise. */
    version?: number;
    /** When true, codeapi tags every file in the batch as infrastructure
    *  (read-only inputs that must never surface as generated artifacts,
    *  even if sandboxed code mutates the bytes on disk). */
    read_only?: boolean;
    codeApiBaseUrl?: string;
    executionProfile?: CodeExecutionContext["executionProfile"];
    bridgeWorkerId?: string;
    signal?: AbortSignal;
  }) => Promise<{
    storage_session_id: string;
    files: Array<{
      fileId: string;
      filename: string;
    }>;
  }>;
  /** Checks if a code env file is still active. Returns lastModified timestamp or null. */
  getSessionInfo?: (ref: CodeEnvRef, req?: ServerRequest, route?: {
    baseUrl?: string;
    executionProfile?: CodeExecutionContext["executionProfile"];
    bridgeWorkerId?: string;
  }, signal?: AbortSignal) => Promise<string | null>;
  /** Trusted Code API route selected for the executing agent. */
  codeExecutionContext?: Pick<CodeExecutionContext, "baseUrl" | "executionProfile" | "executionRouteKey" | "bridgeWorkerId">;
  /** 23-hour freshness check */
  checkIfActive?: (dateString: string) => boolean;
  /** Persists `codeEnvRef` on skill files after upload. Implementations
  *  warn-log on partial writes (matchedCount/modifiedCount mismatch)
  *  internally — caller can fire-and-forget without losing visibility. */
  updateSkillFileCodeEnvIds?: (updates: Array<{
    skillId: Types.ObjectId | string;
    relativePath: string;
    codeEnvRef: CodeEnvRef;
  }>) => Promise<{
    matchedCount: number;
    modifiedCount: number;
  } | void>;
}
interface PrimeSkillFilesResult {
  /** Representative storage session id (first file's). */
  storage_session_id: string;
  files: Array<{
    /** Storage file id (the per-file uuid file_server returned at upload). */id: string;
    /** Resource id — the entity that owns the storage session. For skill
    *  files this is `skill._id.toString()`. Distinct from `id`; codeapi
    *  derives the sessionKey from `resource_id` (shared cache scope) but
    *  validates upload presence under `id` (per-file storage key). */
    resource_id: string;
    storage_session_id: string;
    name: string;
    kind: "skill" | "agent" | "user";
    version?: number;
  }>;
}
/**
* Uploads skill files to the code execution environment.
*
* Smart re-upload: if skill files have existing codeEnvIdentifiers,
* checks session freshness first. If the session is still active,
* returns cached references. Otherwise batch-uploads everything.
*
* After upload, persists new codeEnvIdentifiers on the SkillFile
* documents for future freshness checks.
*
* Rate-limit resilience: concurrent primes of the same (skill, version)
* share one flight, uploads are bounded per Code API route and principal,
* and 429 responses retry within a capped wait budget.
*/
declare function primeSkillFiles(params: PrimeSkillFilesParams): Promise<PrimeSkillFilesResult | null>;
interface PrimeInvokedSkillsDeps {
  req: ServerRequest;
  /** Effective cancellation signal for the historical priming operation. */
  signal?: AbortSignal;
  /** Raw message payload (before formatAgentMessages). Used to extract invoked skill names. */
  payload?: Array<Partial<{
    role: string;
    content: unknown;
  }>>;
  /** Explicit durable names used by a validated event-actor preflight. */
  skillNames?: readonly string[];
  accessibleSkillIds: Types.ObjectId[];
  /** `execute_code` capability flag for the run. When false, the batch-upload
  *  path is skipped entirely — skill bodies still reconstruct for history
  *  rebuilds, but no sandbox traffic is generated. */
  codeEnvAvailable: boolean;
  getSkillByName: (name: string, accessibleIds: Types.ObjectId[]) => Promise<{
    body: string;
    name: string;
    frontmatter?: Record<string, unknown>;
    _id: Types.ObjectId;
    version: number;
    fileCount: number;
  } | null>;
  listSkillFiles: (skillId: Types.ObjectId | string) => Promise<SkillFileRecord[]>;
  getStrategyFunctions: PrimeSkillFilesParams["getStrategyFunctions"];
  batchUploadCodeEnvFiles: PrimeSkillFilesParams["batchUploadCodeEnvFiles"];
  getSessionInfo?: PrimeSkillFilesParams["getSessionInfo"];
  checkIfActive?: PrimeSkillFilesParams["checkIfActive"];
  updateSkillFileCodeEnvIds?: PrimeSkillFilesParams["updateSkillFileCodeEnvIds"];
  codeExecutionContext?: PrimeSkillFilesParams["codeExecutionContext"];
  rateLimitBudget?: CodeApiRateLimitBudget;
}
interface PrimeInvokedSkillsResult {
  initialSessions?: ToolSessionMap;
  /** Pre-resolved skill bodies keyed by skill name. Passed to formatAgentMessages
  *  so it can reconstruct HumanMessages at the right position in the message sequence. */
  skills?: Map<string, string>;
  /** Exact records resolved under the current request's ACL. */
  skillManifest?: Array<{
    id: string;
    name: string;
    version: number;
    contentDigest: string;
  }>;
}
interface PrimeInvokedSkillsForProfilesDeps extends Omit<PrimeInvokedSkillsDeps, "codeEnvAvailable" | "codeExecutionContext"> {
  executionProfiles: CodeExecutionProfileRoute[];
}
/**
* Extracts previously invoked skills from message history, resolves their
* bodies from DB, and re-primes their files to the code env.
*
* Returns:
* - initialSessions: seeds Graph.sessions so ToolNode injects session_id into bash/code tools
* - skills: Map of skillName → body for formatAgentMessages to reconstruct HumanMessages
*/
declare function primeInvokedSkills(deps: PrimeInvokedSkillsDeps): Promise<PrimeInvokedSkillsResult>;
/** Primes historical skill files once per selected Code API deployment and
* seeds only the trusted session partitions that execute on that deployment. */
declare function primeInvokedSkillsForProfiles(deps: PrimeInvokedSkillsForProfilesDeps): Promise<PrimeInvokedSkillsResult>;
//#endregion
//#region src/files/code/classify.d.ts
type CodeArtifactCategory = "utf8-text" | "document" | "presentation" | "other";
/**
* Decide how to render a file produced by the code-execution sandbox.
* Extension wins over MIME for code/text files because content sniffing tends
* to label `.py`/`.json`/`.csv` as `application/octet-stream`.
*/
declare function classifyCodeArtifact(name: string, mimeType: string): CodeArtifactCategory;
//#endregion
//#region src/files/code/artifactDelivery.d.ts
type ArtifactDeliveryFailure = {
  code: "artifact_delivery_failed";
  status: "partial" | "failed";
  attempted: number;
  delivered: number;
  failed: number;
};
declare function normalizeArtifactDeliveryFailure(value: unknown): ArtifactDeliveryFailure | undefined;
//#endregion
//#region src/files/code/destinations.d.ts
/**
* Sandbox input files mount at a destination derived from their `name`, and
* codeapi rejects the whole `/exec` request when two entries resolve to the
* same destination — or when one destination is a directory prefix of
* another (`files contains duplicate destination "x"`, `files contains
* conflicting destinations "a" and "a/b"`). A rejection is fatal for the
* rest of the conversation: the request never reaches the sandbox, so no
* result comes back to collapse the colliding refs, and every later turn
* re-primes the same pair.
*
* Nothing about a TerraMind file record guarantees that uniqueness. Only
* code-generated outputs are covered by the `(filename, conversationId,
* context, tenantId)` partial unique index; user uploads carry
* `context: message_attachment`, so one conversation can hold several
* records sharing a `filename` — two uploads of `image.png`, or an upload
* whose name a later execution wrote back in place.
*
* This module owns the name → destination mapping so every contributor to
* an `/exec` file list agrees on it.
*/
interface CodeDestinationSet {
  readonly names: Set<string>;
  readonly ancestors: Set<string>;
}
declare function createCodeDestinationSet(): CodeDestinationSet;
/**
* Claims a destination for `name`, falling back to `<stem>-<identity hash>`
* when it is already spoken for. Callers that own what the model is told
* about the sandbox use this: both files stay reachable, at names the caller
* can echo into the tool context.
*
* The fallback hashes `identity` — a stable per-file value such as `file_id`
* — rather than counting collisions, so a displaced file keeps the same
* destination for the life of the conversation. A counter would be assigned
* from the current set: a third file arriving under the literal name a
* displaced file had been given would push that file along, and code the
* model wrote in an earlier turn would silently start reading the newcomer.
* The hash does not depend on what else is present, so a file that has been
* told to the model at one path stays there.
*
* One case still moves it, and deliberately: when a *later* file is itself
* named the alias, it takes it and the older file falls to a counter. In
* practice that later file is the model's own in-place rewrite of the
* displaced one — it was told `<stem>-<hash>.ext`, wrote back to it, and
* `processCodeOutput` registered an output under that literal name — so the
* path the model has been using keeps resolving to the newest content at
* that path, which is the whole point of the recency ordering. Reserving the
* alias against the newcomer instead would hand that path back to the
* superseded original. An *unrelated* file reaching this branch would have
* to be named for the hex digest of another file's `file_id`, which is not
* a name a user can construct.
*/
declare function claimCodeDestination(set: CodeDestinationSet, name: string, identity: string): string;
/**
* Reserves `name` only if it is free, reporting whether the caller may keep
* the file. Merge points use this rather than {@link claimCodeDestination}:
* a renamed destination there would be one the model was never told about,
* and a conflict at that layer means the same logical file arrived twice
* under different storage pointers, so dropping the later copy loses
* nothing.
*/
declare function reserveCodeDestination(set: CodeDestinationSet, name: string): boolean;
interface CodeDestinationCandidate {
  file_id?: string;
  createdAt?: Date | string | number;
  /** Last content write for a reused code-output record. `processCodeOutput`
  *  claims one row per `(filename, conversationId)` and rewrites it in
  *  place, so `createdAt` marks when the name was first produced, not when
  *  the bytes behind it last changed. */
  metadata?: {
    sourceDispatchedAt?: number;
  } | null;
}
/**
* Orders files so the one holding the newest content claims the bare name and
* the rest take an identity suffix. Newest-wins matches
* `ToolNode.updateCodeSession`, which collapses by name the same way once
* results come back, and it is the right answer when an execution rewrote an
* uploaded file in place: the model's next read of that path gets its own
* edit, not the superseded original.
*
* Recency is `max(metadata.sourceDispatchedAt, createdAt)`, never `updatedAt`.
* `updatedAt` — the default `getFiles` sort — is bumped by usage accounting
* and by re-upload, so destinations keyed on it would shuffle between turns
* and silently repoint paths that code written in an earlier turn still
* reads. `sourceDispatchedAt` moves only when a generated output's bytes are
* rewritten, which is exactly when the ranking should change.
*
* `privateFileIds` names files that belong to a single contributor rather
* than to the conversation, and sinks them below the shared ones. Each agent
* in a run claims destinations over its own set — the conversation's files
* plus its own — so without this a private file could take a bare name from a
* shared file in one agent and not in another, leaving two agents advertising
* different paths for the same file into one shared mount namespace.
*
* Holes are tolerated and preserved rather than filtered: callers hand this
* a raw query result and keep their own per-entry guard.
*/
declare function sortCodeFilesByDestinationPriority<T extends CodeDestinationCandidate>(files: Array<T | null | undefined>, privateFileIds?: ReadonlySet<string>): Array<T | null | undefined>;
//#endregion
//#region src/files/code/errors.d.ts
/**
* True for the failures a code-execution sandbox reports about the requested
* PATH, and only those. Deliberately narrow: a bare "not found" is also what
* the sandbox emits for a missing interpreter (`python3: not found`), and
* treating that as an absent file would hide a runner dependency the operator
* needs to see.
*
* Reads that miss are an ordinary outcome — `create_file` reads the target
* before writing so it can detect an overwrite — so callers use this to keep
* an expected miss out of error-level logging and off the model's error path.
*/
declare function isMissingSandboxPathError(reason: string): boolean;
//#endregion
//#region src/files/code/extract.d.ts
declare const MAX_TEXT_CACHE_BYTES: number;
/**
* Resolve the inline-preview extraction ceiling from
* `FILE_PREVIEW_MAX_EXTRACT_BYTES`, falling back to the 2 MB default when
* the value is missing, non-numeric, or non-positive. Raising it lets
* larger documents render an inline preview; the rendered HTML is still
* independently capped at {@link MAX_TEXT_CACHE_BYTES} (512 KB), so
* image-heavy files over that show the "too large" banner instead.
*/
declare function resolveMaxTextExtractBytes(value: string | undefined): number;
declare const MAX_TEXT_EXTRACT_BYTES: number;
/**
* Decide whether a buffer is a candidate for rich HTML preview. Wraps the
* shared `officeHtmlBucket` predicate from `~/files/documents/html` so the
* gate here stays in lock-step with what the dispatcher will actually
* route — including extensionless office files identified solely by MIME
* (e.g. a tool emitting `data` with `text/csv`, which would otherwise
* classify as `utf8-text`, skip the office gate, and ship raw CSV text
* to the client's SPREADSHEET bucket that expects full HTML).
*
* Exported so `processCodeOutput`'s deferred-preview flow can decide
* whether to mark a freshly-persisted file record as `status:
* 'pending'` (preview expected later) or skip the status field
* entirely (no preview ever expected).
*/
declare const hasOfficeHtmlPath: (name: string, mimeType: string) => boolean;
/**
* Classify the format of a string returned by `extractCodeArtifactText`
* so callers can persist it alongside the text and the client can gate
* "inject into iframe as HTML" on a trusted signal. Returns:
*   - `'html'` when the file went down the office-HTML producer path
*     (output is a complete sanitized HTML document)
*   - `'text'` for everything else (utf8 plain text, parseDocument
*     output for PDF/ODT, etc.)
*   - `null` when there's no text to format (caller should also skip
*     setting `textFormat` on the record)
*
* Inference is by extension/MIME via `officeHtmlBucket`, mirroring the
* dispatch logic inside `extractCodeArtifactText` exactly. Keeping the
* inference at the caller (rather than baking format into the
* extractor's return type) avoids breaking the existing function
* signature and its 30+ tests, while still giving downstream consumers
* a definitive trust signal.
*/
declare function getExtractedTextFormat(name: string, mimeType: string, text: string | null): "html" | "text" | null;
declare function extractCodeArtifactRawText(buffer: Buffer, category: CodeArtifactCategory): string | null;
/**
* Extract a string representation of a code-execution artifact for inline
* rendering. Returns `null` for binary, oversized, or unsupported files; the
* caller should fall back to the standard download UI in that case.
*
* Office types (docx, xlsx/xls/ods, csv, pptx/potx) are rendered as sanitized
* HTML by the producers in `~/files/documents/html`. The frontend feeds the
* HTML into the Sandpack `static` template via `index.html`. CSV is special-
* cased here — its category is `utf8-text` (raw CSV is text), but we want
* the styled-table preview when the file extension says CSV.
*
* - office (docx/xlsx/xls/ods/csv/pptx/potx): sanitized HTML preview
* - utf8-text: decodes the buffer (with a binary safety net)
* - document: dispatches to the existing PDF/ODT parser
* - other: returns null (binary file, no inline preview)
*/
declare function extractCodeArtifactText(buffer: Buffer, name: string, mimeType: string, category: CodeArtifactCategory): Promise<string | null>;
interface CodeArtifactInspectionText {
  readonly text: string | null;
  readonly complete: boolean;
}
/**
* Extracts text for content inspection independently of the persisted preview
* cache. Plain text and parsed documents are complete only when their full
* derived text fits inside the inspection ceiling. Parseable office documents
* use their full semantic extraction; office preview-only formats retain the
* available HTML for compatibility-mode inspection but stay marked partial
* because their producers have independent row, slide, and output caps.
*/
declare function extractCodeArtifactInspectionText(buffer: Buffer, name: string, mimeType: string, category: CodeArtifactCategory): Promise<CodeArtifactInspectionText>;
//#endregion
//#region src/files/code/form.d.ts
interface CodeEnvFileOptions {
  filename: string;
  filepath?: string;
}
/**
* Uses `filepath` for nested names because `form-data` strips directories from
* the bare string filename overload before codeapi can preserve safe paths.
*/
declare function getCodeEnvFileOptions(filename: string): CodeEnvFileOptions;
/** The destination encoded by the multipart adapter, including its safe-path fallback. */
declare function getCodeEnvUploadFilename(filename: string): string;
/** Older upload strategies may omit the receipt name; use their shared multipart normalization. */
declare function getUploadedCodeEnvFilename(uploaded: {
  filename?: string;
}, requestedFilename: string): string;
declare function appendCodeEnvFile(form: FormData, stream: NodeJS.ReadableStream, filename: string): void;
//#endregion
//#region src/files/code/identity.d.ts
/**
* Resource identity required by codeapi's sessionKey resolver. The
* shape is symmetric across the upload form fields and the URL query
* params on download/freshness routes — codeapi normalizes both into
* the same `parseUploadSessionKeyInput`. Validation rules (closed
* kind set, `version` required for `'skill'` and forbidden otherwise)
* mirror codeapi's validator so a bad caller fails fast on the client
* instead of round-tripping a 400. See codeapi #1455 / Phase C.
*
* `id` is sessionKey-meaningful for `'skill'` / `'agent'` and
* informational-only for `'user'` (codeapi resolves user identity
* from auth context). It's required uniformly here for shape
* consistency with the runtime validator.
*/
interface CodeEnvIdentity {
  kind: CodeEnvKind;
  id: string;
  /** Required when `kind === 'skill'`; forbidden otherwise. */
  version?: number;
}
/**
* Append `kind`/`id`/`version?` form fields to a multipart upload.
* Used by `uploadCodeEnvFile` and `batchUploadCodeEnvFiles` so codeapi
* can route the upload to the correct sessionKey bucket
* (`<tenant>:<kind>:<id>[:v:<version>]` for shared kinds,
* `<tenant>:user:<authContext.userId>` for `'user'`).
*
* Mutates the form in place and throws synchronously on bad input.
*/
declare function appendCodeEnvFileIdentity(form: FormData, identity: CodeEnvIdentity): void;
/**
* Build the `?kind=...&id=...&version=...` query string codeapi's
* `sessionAuth` middleware requires on `/download/<session>/<file>`
* and `/sessions/<session>/objects/<file>` URLs. Without these,
* codeapi 400s with "kind must be one of: skill, agent, user" before
* serving the file.
*
* Returns a string with leading `?`; concatenate onto a path.
*/
/**
* The same `kind`/`id`/`version?` identity as an axios `params` object, for callers that
* build their query that way rather than concatenating a string. Both forms exist so the
* identity codeapi's `sessionAuth` needs is stated once.
*/
declare function buildCodeEnvIdentityParams(identity: CodeEnvIdentity): Record<string, string>;
declare function buildCodeEnvDownloadQuery(identity: CodeEnvIdentity): string;
//#endregion
//#region src/files/code/image.d.ts
/**
* Hard round-trip ceiling for one image read, matched to a single window of
* the Code API's per-user execution limiter (20 requests per 30s in the
* reference deployment). Beyond it a read would have to wait out limiter
* windows to finish, stalling a chat turn; a file that needs more windows
* than this degrades to the same "too large to inline" result as one over
* the byte cap — and {@link readWindowedSandboxImage} detects that from the
* first window rather than spending the whole budget discovering it.
*/
declare const MAX_SANDBOX_IMAGE_EXEC_CALLS = 20;
/**
* One `/exec` window's outcome, as the transport observed it. Fields are
* optional because the body is whatever the in-sandbox reader printed:
* `error` when it refused the path, `too_large` when the file is over the
* caller's cap, `total`/`n`/`b64` for a window of bytes, `outputOverflow`
* when the runner truncated the response, and nothing at all when it
* produced no output.
*/
interface SandboxImageChunk {
  outputOverflow?: boolean;
  /** Bytes of stdout the runner did emit before truncating — its actual
  *  cap, and a far better next window than another blind halving. */
  observedStdoutBytes?: number;
  error?: string;
  too_large?: boolean;
  bytes?: number;
  total?: number;
  n?: number;
  b64?: string;
}
type SandboxImageChunkReader = (params: {
  code: string;
}) => Promise<SandboxImageChunk>;
type SandboxImageReadResult = {
  base64: string;
  bytes: number;
} | {
  tooLarge: true;
  reason: "size" | "round_trips";
  bytes: number;
  inlineCeiling?: number;
} | null;
/**
* Raw bytes to pull per `/exec` round-trip. Each window is base64-encoded
* (~1.33x) into the response's stdout, so the auto-derived size is the
* largest multiple of 3 whose encoding plus envelope fits the runner's
* stdout budget. `LIBRECHAT_CODE_IMAGE_CHUNK_BYTES` overrides it outright
* and is used verbatim, multiple of 3 or not.
*/
declare function getSandboxImageChunkBytes(baseUrl?: string): number;
/**
* Returns the window to retry a truncated read with, or `null` when no
* smaller one exists.
*
* A truncated response reveals the runner's real cap — it emitted exactly
* as much as it allows — so `observedStdoutBytes` sizes the retry directly
* and one round-trip is usually enough to land on a size that fits. Blind
* halving is the fallback, and bounds the result either way so every retry
* is strictly smaller than the window that just failed.
*
* The reduction is computed from the caller's own failing window, never
* from the shared learned value: reads run concurrently (one per tool
* call), so two reads failing at the same size would otherwise reduce the
* shared value twice and skip a size the runner would have accepted. For
* the same reason the learned value only ever moves down to the narrowest
* size any read has needed.
*/
declare function narrowSandboxImageChunkBytes(failedChunkBytes: number, baseUrl?: string, observedStdoutBytes?: number): number | null;
/**
* The in-sandbox reader for one window. Stats the file, refuses (without
* transferring) anything over `limit` or any non-regular file, and prints
* a single JSON line the transport hands back as a {@link SandboxImageChunk}.
* The parameters travel base64-encoded so neither the path nor the numbers
* are interpolated into shell syntax.
*/
declare function buildSandboxImageReaderCode(params: {
  filePath: string;
  limit: number;
  offset: number;
  chunkBytes: number;
}): string;
/**
* Turns one `/exec` response into a {@link SandboxImageChunk}. Throws only
* for output the reader could not have produced, so the caller can tell a
* broken transport from a file the sandbox declined to serve.
*/
declare function parseSandboxImageChunk(response: {
  stdout?: unknown;
  stderr?: unknown;
  status?: unknown;
}): SandboxImageChunk;
/**
* Pulls a sandbox image through as many windows as it takes, narrowing the
* window if the runner truncates and stopping at the round-trip ceiling.
* Returns `null` when the runner produced no output at all; throws what
* the transport throws.
*/
declare function readWindowedSandboxImage(params: {
  filePath: string;
  limit: number;
  baseUrl?: string;
  readChunk: SandboxImageChunkReader;
}): Promise<SandboxImageReadResult>;
//#endregion
//#region src/files/code/preflight.d.ts
declare const CODE_OUTPUT_PREFLIGHT_MAX_COUNT: number;
declare const CODE_OUTPUT_PREFLIGHT_MAX_BYTES: number;
interface CodeOutputArtifactFile extends FileContentInput {
  readonly id: string;
  readonly name: string;
  readonly storage_session_id?: string;
  readonly inherited?: boolean;
}
interface CodeOutputArtifact {
  readonly session_id?: string;
  readonly files?: readonly CodeOutputArtifactFile[];
}
interface PreparedCodeOutput {
  readonly buffer: Buffer;
  readonly extractedTextComplete?: boolean;
  readonly file: FileContentInput;
}
interface PreparedCodeOutputEntry {
  readonly file: CodeOutputArtifactFile;
  readonly sessionId?: string;
  readonly preparedBuffer?: Buffer;
  /** Skip a second download and use the existing code-output URL fallback. */
  readonly downloadFallback?: boolean;
}
interface CodeOutputPreflightLimits {
  readonly fileLimit: number;
  readonly fileSizeLimit: number;
  readonly totalSizeLimit: number;
}
interface PrepareCodeOutputInput {
  readonly file: CodeOutputArtifactFile;
  readonly sessionId?: string;
  readonly maxBytes: number;
  readonly inspectContent: boolean;
}
interface PreflightCodeOutputBatchInput {
  readonly filters?: FiltersConfig;
  readonly artifact?: CodeOutputArtifact | null;
  readonly limits: CodeOutputPreflightLimits;
  readonly prepare: (input: PrepareCodeOutputInput) => Promise<PreparedCodeOutput>;
  /** Reports an artifact whose bytes could not be fetched for inspection.
  *  The batch degrades that entry to the download fallback and the turn
  *  still succeeds, so this callback is the only trace the failure leaves;
  *  it carries the cause so the reason is not lost with it. `index` is the
  *  position within the batch, not within the artifact's own file list —
  *  inherited files and files past the configured count never enter it. */
  readonly onInspectionUnavailable?: (index: number, cause: unknown) => void;
}
/**
* TerraMind file-size configuration uses zero to mean unlimited. Generated
* output inspection preserves that contract while still applying its
* process-wide safety ceiling.
*/
declare function getBoundedCodeOutputByteLimit(configured: number | null | undefined, hardLimit?: number): number;
/**
* Prepares an all-or-nothing inspection batch. No persistence callback is
* accepted here by design: callers cannot write one artifact before every
* active content check has passed.
*/
declare function preflightCodeOutputBatch(input: PreflightCodeOutputBatchInput): Promise<PreparedCodeOutputEntry[]>;
//#endregion
//#region src/files/code/inspection.d.ts
type CodeOutputTypeDetector = (buffer: Buffer, returnFileType: true) => Promise<{
  mime: string;
} | undefined | null>;
/** Inspects supplied bytes without downloading them or adding them to a request cache. */
declare function prepareCodeOutputBufferForInspection({
  buffer,
  name,
  fileSizeLimit,
  inspectContent,
  determineFileType,
  classify,
  extractRawText,
  extractInspectionText
}: {
  buffer: Buffer;
  name: string;
  fileSizeLimit: number;
  inspectContent?: boolean;
  determineFileType: CodeOutputTypeDetector;
  classify?: typeof classifyCodeArtifact;
  extractRawText?: typeof extractCodeArtifactRawText;
  extractInspectionText?: typeof extractCodeArtifactInspectionText;
}): Promise<PreparedCodeOutput>;
//#endregion
//#region src/files/provision/service.d.ts
/** Storage strategy lookup, injected so this module stays free of the api workspace. */
interface ProvisionDeps {
  getStrategyFunctions: (source: string) => {
    getDownloadStream?: (req: ServerRequest, filepath: string, options?: {
      signal?: AbortSignal;
    }) => Promise<Readable$1>;
    handleFileUpload?: (params: Record<string, unknown>) => Promise<{
      storage_session_id: string;
      file_id: string;
      filename?: string;
    }>;
  };
  uploadVectors: (params: {
    req: ServerRequest;
    file: {
      path: string;
      originalname: string;
      mimetype?: string;
      size?: number;
    };
    file_id: string;
    entity_id?: string;
    signal?: AbortSignal;
  }) => Promise<{
    embedded?: boolean;
  } | undefined>;
  loadAuthValues: (params: {
    userId: string;
    authFields: string[];
    throwError?: boolean;
  }) => Promise<Record<string, string | undefined>>;
}
/** The Code API deployment an agent resolved for this turn. */
interface CodeExecutionRoute {
  baseUrl?: string;
  executionProfile?: "default" | "stateful";
  executionRouteKey?: string;
}
/** Deferred database write produced by a successful provisioning call. */
interface ProvisionFileUpdate {
  file_id: string;
  metadata?: Record<string, unknown>;
  embedded?: boolean;
}
/** One route pointer, written on its own so concurrent routes do not overwrite each other. */
interface CodeEnvRefUpdate {
  file_id: string;
  routeKey: string;
  ref: CodeEnvRef;
  legacyRef?: CodeEnvRef;
}
/** Merged code-environment pointers written after a successful sandbox upload. */
interface CodeEnvReferenceSetResult {
  codeEnvRef?: CodeEnvRef;
  codeEnvRefs?: CodeEnvRefMap;
}
interface ProvisionService {
  loadCodeApiKey: (userId: string) => Promise<string | undefined>;
  provisionToCodeEnv: (params: {
    req: ServerRequest;
    file: TFile;
    entity_id?: string;
    route?: CodeExecutionRoute;
    sandboxFilename?: string;
    signal?: AbortSignal;
    rateLimitBudget?: CodeApiRateLimitBudget;
  }) => Promise<{
    referenceSet: CodeEnvReferenceSetResult;
    refUpdate: CodeEnvRefUpdate; /** The name the sandbox stored it under, which a converted image renames. */
    sandboxFilename: string;
  }>;
  provisionToVectorDB: (params: {
    req: ServerRequest;
    file: TFile;
    entity_id?: string;
    existingStream?: Readable$1;
    signal?: AbortSignal;
  }) => Promise<{
    embedded: boolean;
    fileUpdate: ProvisionFileUpdate | null;
  }>;
  checkCodeEnvFileAlive: (params: {
    file: TFile;
    apiKey?: string;
    req?: ServerRequest;
  }) => Promise<boolean>;
  checkSessionsAlive: (params: {
    files: TFile[];
    apiKey?: string;
    req?: ServerRequest;
    staleSafeWindowMs?: number;
    /** Deployment this turn runs on. Refs are deployment-local, so a ref is probed
    *  against the Code API that issued it, not against the default one. */
    baseURL?: string;
    routeKey?: string;
  }) => Promise<Set<string>>;
}
declare function createProvisionService({
  getStrategyFunctions,
  uploadVectors,
  loadAuthValues
}: ProvisionDeps): ProvisionService;
//#endregion
//#region src/files/code/priming.d.ts
interface CodeFileInfo {
  lastModified?: string;
  originalFilename?: string;
}
/** The object metadata endpoint reports the same original filename as the download header. */
declare function getCodeFileInfo({
  ref,
  req,
  route,
  signal,
  request,
  getBaseURL,
  getAuthHeaders
}: {
  ref: CodeEnvRef;
  req: ServerRequest;
  route: CodeExecutionRoute & {
    bridgeWorkerId?: string;
  };
  signal?: AbortSignal;
  request: AxiosInstance;
  getBaseURL: () => string;
  getAuthHeaders: (req: ServerRequest, bridgeWorkerId?: string) => Promise<Record<string, string>>;
}): Promise<CodeFileInfo | null>;
/** Matches the existing 23-hour code-storage freshness window. */
declare function checkCodeFileActive(dateString: string | undefined): boolean;
interface PrimedCodeFile {
  file: TFile;
  ref: CodeEnvRef | undefined;
  sourceRef: CodeEnvRef;
  sandboxName: string;
  isActive: boolean;
  getUploadTime: () => Promise<string | undefined>;
}
/** Reserve actual storage destinations before recovery so a failed winner never revives stale content. */
declare function selectCodeFiles({
  files,
  privateFileIds,
  routeKey,
  getFileInfo,
  concurrency,
  signal
}: {
  files: Array<TFile | null | undefined>;
  privateFileIds?: ReadonlySet<string>;
  routeKey: string;
  getFileInfo: (ref: CodeEnvRef) => Promise<CodeFileInfo | null>;
  concurrency?: number;
  signal?: AbortSignal;
}): Promise<{
  selected: PrimedCodeFile[];
  skippedNoRef: number;
  skippedSuperseded: number;
}>;
//#endregion
//#region src/files/code/publication.d.ts
type CodeOutputStoredFile = CodeFileCommitData;
type CodeOutputDownloadFallback = Pick<TFile, "filename" | "filepath" | "conversationId"> & {
  expiresAt: number;
  messageId?: string;
  toolCallId?: string;
  agentId?: string;
};
interface CodeOutputClaimInput {
  filename: string;
  conversationId: string;
  file_id: string;
  user: string;
  tenantId?: string | null;
  sourceDispatchedAt?: number;
}
interface CodeOutputClaim {
  file_id: string;
  usage?: number;
  messageId?: string;
  createdAt?: Date | string;
  updatedAt?: Date | string;
  metadata?: TFile["metadata"];
}
interface CodeOutputPublication {
  scope: RunArtifactScope;
  provenance: RunFileProvenance;
  publish: (input: PublishRunArtifactInput) => Promise<RunArtifactFile>;
  find: (scope: RunArtifactScope) => Promise<RunArtifactFile | null>;
  discard: (file: TFile) => Promise<void>;
  /** Includes the shared run lifetime as well as request cancellation. */
  signal?: AbortSignal;
  /** Attempt-local ownership notifications; no database row exists for this object yet. */
  trackStored?: (file: TFile) => void;
  releaseStored?: (file: TFile) => void;
}
interface CodeOutputPersistence {
  context: FileContext;
  claim: (input: CodeOutputClaimInput) => Promise<CodeOutputClaim>;
  commit: (file: CodeOutputStoredFile) => Promise<boolean>;
  trackStored: (file: CodeOutputStoredFile) => void;
  finalize: <T>(file: CodeOutputStoredFile, finalize: () => Promise<T>) => (() => Promise<T>) | undefined;
}
/** Reuses code-output storage while giving every publication attempt a separate object key. */
declare function createCodeOutputPersistence({
  publication,
  claim,
  commit
}: {
  publication?: CodeOutputPublication;
  claim: (input: CodeOutputClaimInput) => Promise<CodeOutputClaim>;
  commit: (file: CodeOutputStoredFile) => Promise<boolean>;
}): CodeOutputPersistence;
interface RunArtifactDescriptor {
  id: string;
  name: string;
  sessionId: string;
  /** Opaque host-owned copy; never an agent-supplied filesystem path. */
  snapshotId?: string;
  codeExecutionContext?: Pick<CodeExecutionContext, "baseUrl" | "executionProfile" | "executionRouteKey" | "bridgeWorkerId">;
}
interface ProcessPublishedCodeOutputInput {
  id: string;
  name: string;
  session_id: string;
  conversationId: string;
  messageId: string;
  agentId: string;
  publication: CodeOutputPublication;
  preparedBuffer?: Buffer;
  codeApiBaseUrl?: string;
  executionProfile?: CodeExecutionContext["executionProfile"];
  executionRouteKey?: string;
  bridgeWorkerId?: string;
}
interface CodeOutputResult {
  file: CodeOutputStoredFile | CodeOutputDownloadFallback | null;
  finalize?: () => Promise<CodeOutputStoredFile | null>;
  previewRevision?: string;
}
interface PublishedCodeOutputResult extends CodeOutputResult {
  file: CodeOutputStoredFile;
}
interface PublishGeneratedRunArtifactInput {
  scope: RunArtifactScope;
  provenance: RunFileProvenance;
  artifact: RunArtifactDescriptor;
  signal?: AbortSignal;
}
declare function createRunArtifactPublisher({
  claimRunArtifactFile,
  publishRunArtifactFile,
  findRunArtifactFile,
  processCodeOutput,
  prepare,
  discard,
  finalize
}: {
  claimRunArtifactFile: (scope: RunArtifactScope) => Promise<RunArtifactClaim>;
  publishRunArtifactFile: CodeOutputPublication["publish"];
  findRunArtifactFile: CodeOutputPublication["find"];
  processCodeOutput: (input: ProcessPublishedCodeOutputInput) => Promise<CodeOutputResult | null>;
  prepare: (artifact: RunArtifactDescriptor, signal?: AbortSignal) => Promise<Buffer>;
  discard: CodeOutputPublication["discard"];
  finalize: (result: PublishedCodeOutputResult) => void;
}): (input: PublishGeneratedRunArtifactInput) => Promise<RunArtifactFile>;
//#endregion
//#region src/files/retention.d.ts
type InterfaceConfig = AppConfig["interfaceConfig"];
type RetentionConversation = {
  isTemporary?: boolean | null;
  expiredAt?: Date | string | number | null;
};
type RetentionRequest = {
  /** Owner-authenticated source row for operations attached to an existing message. */fileRetentionSource?: RetentionConversation;
  user?: {
    id?: string;
    tenantId?: string;
  };
  body?: {
    conversationId?: string;
    isTemporary?: boolean | string | null;
  };
  config?: {
    interfaceConfig?: InterfaceConfig;
  };
};
type RetentionExpiry = {
  expiredAt?: Date | null;
};
type AgentFileRetentionRequest = {
  req: RetentionRequest | null | undefined;
  messageAttachment?: boolean | null;
  toolResource?: string | null;
};
type RetentionLogger = {
  error: (message: string, error?: unknown) => void;
};
type RetentionDependencies = {
  getConvo: (userId: string, conversationId: string) => Promise<RetentionConversation | null | undefined>;
  createExpirationDate: (interfaceConfig?: InterfaceConfig, isTemporary?: boolean) => Date;
  logger?: RetentionLogger;
};
type SharedLinkRetentionDependencies = {
  getConvo: (userId: string, conversationId: string) => Promise<RetentionConversation | null | undefined>;
  createExpirationDate: (interfaceConfig?: InterfaceConfig, isTemporary?: boolean) => Date;
  logger?: RetentionLogger;
};
declare const isBooleanOrStringTrue: (value: unknown) => boolean;
declare const getConversationExpirationDate: (convo?: RetentionConversation | null) => Date | null;
declare const isActiveExpirationDate: (expiredAt: Date, now?: Date) => boolean;
declare function getRetentionExpiry$1(req: RetentionRequest | null | undefined, dependencies: RetentionDependencies): Promise<RetentionExpiry>;
declare function getAgentFileRetentionExpiry(params: AgentFileRetentionRequest, dependencies: RetentionDependencies): Promise<RetentionExpiry>;
/**
* Resolves the retention deadline for a shared link derived from a conversation.
*
* Return values are intentionally tri-state:
* - `undefined`: no decision can be made because the conversation id or row is missing.
* - `null`: the share should be stored without an expiration.
* - `Date`: the share should expire at that date; callers reject already-expired dates.
*/
declare function getSharedLinkExpiration({
  req,
  conversationId
}: {
  req: RetentionRequest | null | undefined;
  conversationId?: string | null;
}, dependencies: SharedLinkRetentionDependencies): Promise<Date | null | undefined>;
declare const createMinimalRetentionRequest: (req?: RetentionRequest | null) => RetentionRequest | undefined;
//#endregion
//#region src/files/code/process.d.ts
type CodeOutputProcessorInput = Omit<ProcessPublishedCodeOutputInput, "publication" | "agentId"> & {
  req: ServerRequest & Required<Pick<ServerRequest, "user" | "config">>;
  agentId?: string;
  toolCallId?: string;
  freshClaimAfter?: number;
  downloadFallback?: boolean;
  publication?: CodeOutputPublication;
};
type DownloadInput = Pick<CodeOutputProcessorInput, "req" | "id" | "session_id" | "codeApiBaseUrl" | "executionProfile" | "bridgeWorkerId"> & {
  maxBytes: number;
};
type DownloadFallbackInput = Pick<CodeOutputProcessorInput, "id" | "name" | "agentId" | "messageId" | "toolCallId" | "session_id" | "conversationId" | "executionProfile" | "executionRouteKey"> & {
  expiresAt: number;
};
interface CodeOutputProcessorDeps {
  getCodeOutputFileSettings: (req: ServerRequest) => {
    endpointFileConfig: ReturnType<typeof getEndpointFileConfig>;
    fileSizeLimit: number;
  };
  downloadCodeOutputBuffer: (input: DownloadInput) => Promise<Buffer>;
  createDownloadFallback: (input: DownloadFallbackInput) => CodeOutputDownloadFallback;
  getRetentionExpiry: (req: ServerRequest) => Promise<RetentionExpiry>;
  convertImage: (req: ServerRequest, buffer: Buffer, detail: "high", filename: string) => Promise<Pick<CodeOutputStoredFile, "filepath" | "bytes" | "width" | "height" | "storageKey" | "storageRegion">>;
  getStrategyFunctions: (source: FileSources) => {
    saveBuffer?: (input: {
      userId: string;
      buffer: Buffer;
      fileName: string;
      basePath: string;
      tenantId?: string;
    }) => Promise<string>;
  };
  determineFileType: (buffer: Buffer, reportErrors: boolean) => Promise<{
    mime: string;
  } | undefined>;
  claimCodeFile: CodeOutputPersistence["claim"];
  commitCodeFile: (data: CodeFileCommitData, sourceDispatchedAt?: number) => Promise<boolean>;
  finalizePreview: (input: {
    buffer: Buffer;
    leafName: string;
    mimeType: string;
    category: CodeArtifactCategory;
    file_id: string;
    previewRevision?: string | null;
  }) => Promise<CodeOutputStoredFile | null>;
  hasOfficeHtmlPath: typeof hasOfficeHtmlPath;
  sanitizeArtifactPath: typeof sanitizeArtifactPath;
  flattenArtifactPath: typeof flattenArtifactPath;
  classifyCodeArtifact: typeof classifyCodeArtifact;
  extractCodeArtifactText: typeof extractCodeArtifactText;
  getExtractedTextFormat: typeof getExtractedTextFormat;
  getStorageMetadata: (file: Pick<CodeOutputStoredFile, "filepath" | "source" | "storageKey" | "storageRegion">) => Pick<CodeOutputStoredFile, "storageKey" | "storageRegion">;
  logAxiosError: typeof logAxiosError;
  logger: {
    warn: (message: string) => void;
    debug: (message: string) => void;
  };
}
/** Persists ordinary outputs or explicit publications, returning owned preview work separately. */
declare function processCodeOutput$1({
  req,
  id,
  name,
  toolCallId,
  conversationId,
  messageId,
  session_id,
  agentId,
  freshClaimAfter,
  codeApiBaseUrl,
  executionProfile,
  executionRouteKey,
  bridgeWorkerId,
  preparedBuffer,
  downloadFallback,
  publication
}: CodeOutputProcessorInput, {
  getCodeOutputFileSettings,
  downloadCodeOutputBuffer,
  createDownloadFallback,
  getRetentionExpiry,
  convertImage,
  getStrategyFunctions,
  determineFileType,
  claimCodeFile,
  commitCodeFile: commitOutput,
  finalizePreview,
  hasOfficeHtmlPath,
  sanitizeArtifactPath,
  flattenArtifactPath,
  classifyCodeArtifact,
  extractCodeArtifactText,
  getExtractedTextFormat,
  getStorageMetadata,
  logAxiosError,
  logger
}: CodeOutputProcessorDeps): Promise<CodeOutputResult | null>;
//#endregion
//#region src/files/code/snapshot.d.ts
interface RunArtifactSnapshotAdapter {
  open: (artifact: RunArtifactDescriptor, signal?: AbortSignal) => Promise<Readable$1>;
  prepare: (artifact: RunArtifactDescriptor, buffer: Buffer, signal?: AbortSignal) => Promise<Buffer>;
}
/** Downloads privately once; publication inspects that immutable copy with the normal file policy. */
declare function createRunArtifactSnapshotAdapter({
  req,
  request,
  getAuthHeaders,
  getBaseURL,
  determineFileType
}: {
  req: ServerRequest;
  request: AxiosInstance;
  getAuthHeaders: (req: ServerRequest, bridgeWorkerId?: string) => Promise<Record<string, string>>;
  getBaseURL: (profile: "default" | "stateful") => string;
  determineFileType: CodeOutputTypeDetector;
}): RunArtifactSnapshotAdapter;
//#endregion
//#region src/agents/harvest.d.ts
interface HarvestFileRef {
  id: string;
  name: string;
  storage_session_id?: string;
  inherited?: boolean;
}
interface HarvestArtifact {
  session_id?: string;
  files?: HarvestFileRef[];
}
interface ProcessedCodeOutput {
  file?: {
    file_id: string;
  } & Record<string, unknown>;
  finalize?: () => Promise<unknown>;
  previewRevision?: number;
}
interface BackgroundToolResultState {
  taskId: string;
  toolName: string;
  /** Cancelled executions retain the pre-existing durable `error` state so
  * older replicas can still claim them during a rolling deploy. */
  status: "completed" | "error";
  cancelled?: true;
  settledAt: Date;
  /** This exact task owns a pre-registered automatic continuation delivery. */
  completionWakeup?: true;
  resultClaim?: {
    kind: "manual" | "wakeup";
    claimId: string;
    claimedAt: Date;
    generationId?: string;
  };
}
interface CodeHarvestDeps {
  req: ServerRequest;
  /** Data-schemas method: idempotent tool-call part patch + attachment append. */
  updateToolCallResult: (params: {
    userId: string;
    messageId: string;
    conversationId: string;
    toolCallId: string;
    stepId?: string;
    agentId?: string;
    output?: string;
    attachments?: unknown[];
    markBackgrounded?: boolean;
    backgroundTask?: BackgroundToolResultState;
  }) => Promise<{
    matched: boolean;
    unfinished: boolean;
  }>;
  /** Host preflight: inspects the entire generated-file batch before any write. */
  preflightCodeOutputBatch: (params: {
    req: ServerRequest;
    artifact: HarvestArtifact;
    codeExecutionContext?: CodeExecutionContext;
  }) => Promise<PreparedCodeOutputEntry[]>;
  /** Host file service: downloads and persists one code output file. */
  processCodeOutput: (params: {
    req: ServerRequest;
    id: string;
    name: string;
    messageId: string;
    toolCallId: string;
    conversationId: string;
    agentId?: string;
    session_id?: string;
    freshClaimAfter?: number;
    codeApiBaseUrl?: string;
    executionProfile?: CodeExecutionContext["executionProfile"];
    executionRouteKey?: string;
    bridgeWorkerId?: string;
    preparedBuffer?: Buffer;
    downloadFallback?: boolean;
  }) => Promise<ProcessedCodeOutput | null>;
  /** Host file service: runs the deferred office-preview extraction. */
  runPreviewFinalize: (params: {
    finalize?: () => Promise<unknown>;
    fileId: string;
    previewRevision?: number;
  }) => void;
}
interface CodeHarvestParams {
  toolName: string;
  toolCallId: string;
  stepId?: string;
  messageId?: string;
  conversationId?: string;
  /** Dispatching agent — scopes the part patch when provider tool-call ids
  *  repeat across agents in one response message. */
  agentId?: string;
  /** When the background task was DISPATCHED — the ordering anchor for the
  *  stale-output guard. A slow task settling after a newer run wrote the
  *  same filename must not overwrite it, so harvest wall-clock is wrong. */
  dispatchedAt?: number;
  output?: string;
  artifact?: unknown;
  codeExecutionContext?: CodeExecutionContext;
  attachments?: unknown[];
  reapply?: boolean;
  backgroundTask?: BackgroundToolResultState;
  /** Re-reads local claim ownership on every retry so a same-generation
  * manual poll cannot be overwritten by a later automatic continuation. */
  resolveBackgroundTask?: () => BackgroundToolResultState;
}
type CodeHarvestHandler = (params: CodeHarvestParams) => Promise<{
  attachments: unknown[];
  deliveryReady?: boolean;
} | null>;
/** Persists an ordinary detached tool result without invoking code-artifact processing. */
declare function createBackgroundToolResultHandler(deps: Pick<CodeHarvestDeps, "req" | "updateToolCallResult">): (params: Omit<CodeHarvestParams, "artifact" | "codeExecutionContext">) => Promise<boolean>;
/**
* Handles a backgrounded code-execution result once the detached call settles:
* persists generated files (same `processCodeOutput` path as the foreground
* callback, anchored to the ORIGINAL messageId/toolCallId), then patches the
* dispatch turn's tool-call part output and appends the attachments to that
* message row — so the backgrounded call reads like a foreground one on reload
* and in later model turns, and next-turn file priming picks the outputs up.
*
* The dispatch turn may still be streaming when a fast task settles (its
* response message is only saved at turn end), so the row patch retries on a
* backoff schedule before giving up; files are already persisted either way,
* and the poll turn still delivers content/attachments live. With
* `reapply: true` it only re-applies the (idempotent) row patch using the
* provided attachments — no file processing — to heal a full-row save that
* reverted the anchor.
*/
declare function createBackgroundCodeResultHandler(deps: CodeHarvestDeps): CodeHarvestHandler;
//#endregion
//#region src/agents/files/manifest.d.ts
interface RunFileScope {
  userId: string;
  tenantId?: string;
  conversationId: string;
  runId: string;
}
interface RunFilePolicy {
  allowSiblingSharing: boolean;
  maxFiles: number;
  ttlMs: number;
}
interface RunFileExecution {
  id: string;
  parentId?: string;
  parentAgentId?: string;
  agentIds: readonly string[];
  allowedAgentIds: readonly string[];
}
interface RunFileActor {
  executionId: string;
  agentId: string;
}
interface RunArtifact {
  id: string;
  filename: string;
}
interface RunFileEntry {
  file_id: string;
  filename: string;
  type: string;
  source?: FileSources;
  llmDeliveryPath?: TFile["llmDeliveryPath"];
  paths: Array<"provider" | "text" | EToolResources.execute_code | EToolResources.file_search>;
  provenance: RunFileProvenance | {
    kind: "user_attachment";
    runId: string;
  };
}
/** Describes an already authorized snapshot without another storage read. */
declare function describeRunFiles(files: readonly TFile[], runId: string): RunFileEntry[];
interface RunFileStore<TArtifact extends RunArtifact> {
  list: (scope: RunFileScope) => Promise<TFile[]>;
  publish: (params: {
    scope: RunFileScope;
    artifact: TArtifact;
    provenance: RunFileProvenance;
    signal?: AbortSignal;
  }) => Promise<TFile>;
}
interface RunFileAudit {
  action: "inherit" | "publish" | "expire" | "cleanup_failed";
  scope: RunFileScope;
  executionId: string;
  agentId?: string;
  fileIds: string[];
  recipientAgentIds?: string[];
}
type RunFileReadMode = "refresh" | "snapshot";
interface RunFileManifest<TArtifact extends RunArtifact = RunArtifact> {
  register: (execution: RunFileExecution) => void;
  getFiles: (actor: RunFileActor, signal?: AbortSignal, mode?: RunFileReadMode) => Promise<TFile[]>;
  list: (actor: RunFileActor, signal?: AbortSignal) => Promise<RunFileEntry[]>;
  stage: (actor: RunFileActor, artifact: TArtifact) => void;
  discardArtifacts: (actor: RunFileActor) => void;
  publish: (actor: RunFileActor, artifactId: string, recipientAgentIds?: string[], signal?: AbortSignal) => Promise<TFile>;
  close: () => void;
}
/** Metadata and grants are run-local; canonical file bytes remain in the unified file store. */
declare function createRunFileManifest<TArtifact extends RunArtifact>({
  scope,
  policy,
  root,
  inputFiles,
  inputFileIds,
  createdAt,
  store,
  now,
  audit
}: {
  scope: RunFileScope;
  policy: RunFilePolicy;
  root: RunFileExecution;
  inputFiles: readonly TFile[];
  inputFileIds: ReadonlySet<string>;
  createdAt: number;
  store: RunFileStore<TArtifact>;
  now?: () => number;
  audit?: (event: RunFileAudit) => void;
}): RunFileManifest<TArtifact>;
//#endregion
//#region src/agents/files/snapshots.d.ts
interface RunFileSnapshot {
  snapshotId: string;
  size: number;
  sha256: string;
}
interface RunFileSnapshotStore {
  capture: (source: RunArtifactDescriptor, signal?: AbortSignal) => Promise<RunFileSnapshot>;
  read: (snapshotId: string, signal?: AbortSignal) => Promise<Buffer>;
  discard: (snapshotId: string) => Promise<void>;
  close: () => Promise<void>;
}
//#endregion
//#region src/agents/files/encode.d.ts
type ContentBlock = Exclude<BaseMessage$1["content"], string>[number];
/** The already loaded child configuration; storage documents never cross this boundary. */
interface RunFileEncodingAgent {
  provider: string;
  model?: string | null;
  model_parameters?: {
    model?: string;
  };
  imageDetail?: ImageDetail;
  agentContextAttachments?: readonly TFile[];
  /** How the child receives attachments, settled when its configuration was initialized. */
  deliveryRouting: TurnDeliveryRouting;
  fileConsumers?: TurnFileConsumers;
}
interface RunFileEncodingParams {
  provider: string;
  endpoint: string;
  model?: string;
  useResponsesApi?: boolean;
  imageDetail?: ImageDetail;
}
type MediaEncoder<T> = (req: ServerRequest, files: TFile[], params: RunFileEncodingParams, getStrategyFunctions: (source: string) => StrategyFunctions) => Promise<T>;
/** Adapters bind the existing attachment encoders to the host's storage strategies. */
interface RunFileMessageEncoderDeps {
  req: ServerRequest;
  getAgent: (agentId: string) => RunFileEncodingAgent | undefined;
  encodeImages: MediaEncoder<{
    image_urls: ContentBlock[];
  }>;
  encodeDocuments: MediaEncoder<{
    documents: ContentBlock[];
  }>;
  encodeAudios: MediaEncoder<{
    audios: ContentBlock[];
  }>;
  encodeVideos: MediaEncoder<{
    videos: ContentBlock[];
  }>;
  getStrategyFunctions: (source: string) => StrategyFunctions;
  extractText: (params: {
    attachments: TFile[];
    req: ServerRequest;
    tokenCountFn: TokenCountFn;
  }) => Promise<string | undefined>;
}
interface RunFileMessageEncoder {
  validate: (files: TFile[], agentId: string) => void;
  encode: (files: TFile[], agentId: string) => Promise<BaseMessage$1[]>;
}
/** Encodes authorized run files for the receiving child, without provisioning new resources. */
declare function createRunFileMessageEncoder(deps: RunFileMessageEncoderDeps): RunFileMessageEncoder;
//#endregion
//#region src/agents/files/session.d.ts
interface SharedRunArtifact {
  id: string;
  filename: string;
  sha256: string;
  source: RunArtifactDescriptor;
}
declare function getAuthorizedRunFileSnapshot({
  policy,
  agent,
  files
}: {
  policy?: TAgentsEndpoint["fileSharing"];
  agent: Pick<Agent, "subagents">;
  files: readonly TFile[];
}): readonly TFile[] | undefined;
interface RunFilePreparation {
  executionContext: SubagentExecutionContext;
  memberAgentIds: readonly string[];
  signal: AbortSignal;
  resumed: boolean;
}
interface RunFileSessionDeps {
  policy?: TAgentsEndpoint["fileSharing"];
  userId: string;
  tenantId?: string;
  createdAt: number;
  audit?: (event: RunFileAudit) => void;
  getAgent: (id: string) => Pick<Agent, "id" | "subagents"> | undefined;
  getInputs: () => readonly TFile[];
  inputFileIds: ReadonlySet<string>;
  listPublications: (scope: RunFileScope) => Promise<TFile[]>;
  snapshots: Pick<RunFileSnapshotStore, "capture" | "discard" | "close">;
  publish: (params: {
    scope: RunFileScope;
    artifact: RunArtifactDescriptor;
    provenance: RunFileProvenance;
    signal?: AbortSignal;
  }) => Promise<TFile>;
  prepareAgent: (params: {
    actor: RunFileActor;
    files: TFile[];
    sessionKey: string;
    revision: number;
    signal: AbortSignal;
  }) => Promise<void>;
  validateMessages: RunFileMessageEncoder["validate"];
  encodeMessages: RunFileMessageEncoder["encode"];
  emit: (file: TFile & {
    messageId: string;
    toolCallId?: string;
    agentId: string;
  }) => Promise<void>;
}
interface CapturedRunArtifact {
  session_id?: string;
  files?: Array<{
    id: string;
    name: string;
    session_id?: string;
    storage_session_id?: string;
    inherited?: boolean;
  }>;
}
interface PreparedRunFiles {
  messages?: BaseMessage$1[];
  agentSessions?: Record<string, {
    codeSessionKey: string;
  }>;
}
interface RunFileSession {
  activate: (runId: string, conversationId: string, agentIds: string[], signal?: AbortSignal) => boolean;
  prepare: (input: RunFilePreparation) => Promise<PreparedRunFiles>;
  complete: (input: RunFilePreparation, result: {
    content: string;
  }) => Promise<{
    content: string;
  }>;
  list: (agentId: string, context?: SubagentExecutionContext, signal?: AbortSignal) => Promise<{
    files: RunFileEntry[];
    artifacts: Array<{
      artifact_id: string;
      filename: string;
    }>;
    private_artifact_recovery?: string;
  }>;
  capture: (agentId: string, context: SubagentExecutionContext | undefined, toolCallId: string, artifact: CapturedRunArtifact, codeExecutionContext?: RunArtifactDescriptor["codeExecutionContext"]) => Promise<boolean>;
  withCodeExecution: <T>(agentId: string, context: SubagentExecutionContext | undefined, signal: AbortSignal, execute: () => Promise<T>) => Promise<T>;
  publish: (agentId: string, context: SubagentExecutionContext | undefined, artifactId: string, recipients?: string[], signal?: AbortSignal) => Promise<TFile>;
  prepareTools: (agentId: string, context: SubagentExecutionContext | undefined, signal: AbortSignal, mode?: RunFileReadMode) => Promise<void>;
  actorFor: (agentId: string, context?: SubagentExecutionContext) => RunFileActor;
  close: () => Promise<void>;
  isActive: () => boolean;
}
/** Coordinates the SDK's execution identities with file grants and host-owned tool contexts. */
declare function createRunFileSession(deps: RunFileSessionDeps): RunFileSession;
//#endregion
//#region src/agents/handlers.d.ts
interface ToolEndCallbackData {
  /** The executed call's arguments. The stream-consumer tool-end path cannot
  * reconstruct these, so the execution handler — which owns both halves —
  * must supply them for consumers that fence on the input (the event-actor
  * action recorder validates its declared argument subset against this). */
  input?: unknown;
  /** True when this callback delivers the harvested completion of a
  * previously dispatched background task on a poll turn. `output.name` then
  * reports the ORIGINAL tool for artifact attribution while `input` carries
  * the poll call's arguments — consumers that fence on execution identity
  * (the event-actor action recorder) must ignore these deliveries, or a
  * name-only expected action could be impersonated by work another turn
  * dispatched. */
  backgroundDelivery?: boolean;
  /** True when the tool executed successfully but its returned content was
  * withheld by post-execution output policy. `output.content` is blank and
  * no artifact rides the callback — this delivery exists solely so
  * execution-identity consumers (the event-actor action recorder) can prove
  * the side effect occurred; a retry of an "actionless" turn would otherwise
  * repeat an external action whose output was merely filtered. */
  outputFiltered?: boolean;
  output: {
    name: string;
    tool_call_id: string;
    content: string | unknown;
    artifact?: unknown;
  };
}
interface EventActorDetachedActionLifecycle {
  reserve(input: {
    toolName: string;
    toolCallId: string;
    turnId: string;
    arguments: unknown;
  }): Promise<{
    status: "ignored";
  } | {
    status: "conflict";
    error?: string;
  } | {
    status: "terminal";
    taskId: string;
    idempotencyKey: string;
    outcome: "succeeded" | "failed" | "cancelled";
    result?: string;
    error?: string;
  } | {
    status: "reserved" | "replay";
    taskId: string;
    idempotencyKey: string;
  }>;
  markRunning(input: {
    taskId: string;
    idempotencyKey: string;
  }): Promise<boolean>;
  settle(input: {
    taskId: string;
    idempotencyKey: string;
    status: "succeeded" | "failed" | "cancelled";
    result?: unknown;
    error?: string;
  }): Promise<boolean>;
  wake(input: {
    taskId: string;
    idempotencyKey: string;
  }): Promise<void>;
}
interface ToolEndCallbackMetadata {
  /** SDK-authored lineage for artifacts generated inside a child execution. */
  executionContext?: SubagentExecutionContext;
  run_id?: string;
  thread_id?: string;
  [key: string]: unknown;
}
type ToolEndCallback = (data: ToolEndCallbackData, metadata: ToolEndCallbackMetadata) => Promise<void>;
/**
* Preserve the SDK's event-handler contract while attaching the graph-owned
* step identity to legacy artifact callbacks. `toolCallStepIds` is populated
* by ToolNode for the actual provider tool call; this wrapper never invents a
* fallback identity.
*/
declare function createOwnedToolEndHandler(callback: ToolEndCallback$1, loggerArg?: typeof logger$1): EventHandler$1;
interface ToolExecuteOptions {
  /**
  * Host-owned signal for the foreground run. This is authoritative across
  * graph reconstruction (including approval resume); the SDK event signal is
  * composed with it below so circuit-breaker cancellation is preserved too.
  */
  runSignal?: AbortSignal;
  /** Run id owned by `runSignal`; detached child runs carry a different id. */
  foregroundRunId?: string;
  /** Loads tools by name, using agentId to look up agent-specific context */
  loadTools: (toolNames: string[], agentId?: string, configurable?: Record<string, unknown>, callerCapabilityProjection?: CallerCapabilityProjectionSnapshot, signal?: AbortSignal, executionContext?: SubagentExecutionContext) => Promise<{
    loadedTools: StructuredToolInterface[]; /** Additional configurable properties to merge (e.g., userMCPAuthMap) */
    configurable?: Record<string, unknown>;
  }>;
  /** Trusted detached-subagent task scope for polling and parent controls. */
  subagentTasks?: SubagentTaskConfig;
  /** Shared-file grants and tool contexts scoped to the executing run instance. */
  runFiles?: Pick<RunFileSession, "isActive" | "prepareTools" | "withCodeExecution">;
  /** Trusted deployment gate for cooperative ordinary-tool cancellation. */
  ordinaryToolCancellation?: boolean;
  /** Callback to process tool artifacts (code output files, file citations, etc.) */
  toolEndCallback?: ToolEndCallback;
  /** Durable internal-completion adapter, present only for an Event Actor invocation. */
  eventActorDetachedAction?: EventActorDetachedActionLifecycle;
  /** Called once per batch before tool execution to lazily provision files to tool
  *  environments. Resolves to the code-env refs it uploaded, which the caller folds
  *  into this batch's code-session context. */
  provisionFiles?: (toolNames: string[], agentId?: string, signal?: AbortSignal, executionContext?: SubagentExecutionContext) => Promise<CodeEnvFile[] | void>;
  /**
  * Persists a backgrounded code-execution result onto the dispatch turn once
  * the detached call settles: downloads/persists generated files, patches the
  * original tool-call part's `output`, and appends the attachments to the
  * dispatch turn's message row. Returns the persisted attachments so the poll
  * turn can re-emit them on its live stream. With `reapply: true` it only
  * re-applies the (idempotent) row patch using the provided attachments — no
  * file processing — to heal a full-row save that reverted the anchor.
  */
  persistBackgroundCodeResult?: (params: {
    toolName: string;
    toolCallId: string;
    stepId?: string;
    messageId?: string;
    conversationId?: string;
    agentId?: string;
    dispatchedAt?: number;
    output?: string;
    artifact?: unknown;
    codeExecutionContext?: CodeExecutionContext;
    attachments?: unknown[];
    reapply?: boolean;
    backgroundTask?: BackgroundToolResultState;
    resolveBackgroundTask?: () => BackgroundToolResultState;
  }) => Promise<{
    attachments?: unknown[];
    deliveryReady?: boolean;
  } | null>;
  /** Shared ordinary-tool completion lifecycle. The delivery is registered
  * before invoke; settlement is persisted onto the original response row. */
  backgroundToolCompletion?: {
    preregister?: (registration: BackgroundToolWakeupRegistration) => Promise<BackgroundToolWakeupAdmission | false>;
    persist: (params: {
      toolName: string;
      toolCallId: string;
      stepId?: string;
      messageId?: string;
      conversationId?: string;
      agentId?: string;
      output?: string;
      backgroundTask: BackgroundToolResultState;
      resolveBackgroundTask?: () => BackgroundToolResultState;
    }) => Promise<boolean>;
    claim: (params: {
      userId: string;
      conversationId: string;
      messageId?: string;
      taskId: string;
      agentId?: string;
      kind: "manual";
      claimId: string;
      generationId?: string;
      allowUnfinished?: boolean;
    }) => Promise<BackgroundToolResultClaim>;
    recoverDeadClaim?: BackgroundToolDeadClaimRecovery;
  };
  /** Emits an `attachment` SSE event on the current request's live stream. */
  emitAttachment?: (attachment: unknown) => void;
  /**
  * Emits an `on_ptc_tool_call` SSE event for one inner tool invocation made
  * by a programmatic tool-calling program. Absent on transports that don't
  * carry the TerraMind step stream (Open Responses), which simply skips the
  * instrumentation.
  */
  emitPtcProgress?: (event: PtcToolCallEvent) => void;
  /**
  * Loads a skill by name with ACL constraint (returns full body for injection).
  *
  * `options.preferModelInvocable` (Phase 6): on a same-name collision,
  * prefer the newest `disableModelInvocation !== true` doc. Avoids a
  * newer disabled duplicate shadowing the cataloged model-invocable
  * skill the model actually targeted; falls back to newest match so
  * the explicit-rejection gate can still fire in the disabled-only case.
  */
  getSkillByName?: (name: string, accessibleIds: Types.ObjectId[], options?: {
    preferUserInvocable?: boolean;
    preferModelInvocable?: boolean;
  }) => Promise<{
    body: string;
    name: string;
    description?: string;
    frontmatter?: Record<string, unknown>;
    _id: Types.ObjectId;
    /** Monotonic counter on the skill record. Threaded into
    *  `codeEnvRef.version` so codeapi's sessionKey scopes the cache
    *  per-revision; bumping the version on edit invalidates the
    *  prior cache entry. */
    version: number;
    fileCount: number; /** True for deployment-directory skills that are loaded in memory. */
    deployment?: boolean;
    /**
    * Set when the skill author opted out of model invocation. The handler
    * rejects the call and returns an instructive error so the model knows
    * it can't reach the skill via the `skill` tool — manual `$` invocation
    * is still allowed and goes through `resolveManualSkills` instead.
    */
    disableModelInvocation?: boolean;
  } | null>;
  /** Captures a successfully resolved model-invoked Skill for durable continuation context. */
  onSkillResolved?: (skill: {
    id: string;
    name: string;
    version: number;
    contentDigest: string;
  }, context: {
    agentId?: string;
  }) => void;
  /**
  * Loads a skill by name when the current user is the author. This is a
  * narrow recovery path for freshly-authored skills whose runtime catalog
  * snapshot has not caught up yet; normal skill resolution still goes
  * through `accessibleSkillIds`.
  */
  getAuthorSkillByName?: (params: {
    req: ServerRequest;
    name: string;
  }) => Promise<{
    body: string;
    name: string;
    description?: string;
    frontmatter?: Record<string, unknown>;
    _id: Types.ObjectId;
    version: number;
    fileCount: number;
    disableModelInvocation?: boolean;
  } | null>;
  /** Creates a skill from a tool-authored SKILL.md body. */
  createSkill?: (data: {
    name: string;
    description: string;
    body: string;
    frontmatter?: Record<string, unknown>;
    author: Types.ObjectId;
    authorName: string;
    alwaysApply?: boolean;
    tenantId?: string;
  }) => Promise<{
    skill: {
      _id: Types.ObjectId;
      name: string;
      body: string;
      version: number;
    };
    warnings: ValidationIssue[];
  }>;
  /** Updates a skill body and derived metadata from a tool-authored SKILL.md body. */
  updateSkill?: (params: {
    id: string;
    expectedVersion: number;
    update: {
      body?: string;
      description?: string;
      frontmatter?: Record<string, unknown>;
      alwaysApply?: boolean;
    };
  }) => Promise<{
    status: "updated";
    skill: {
      _id: Types.ObjectId;
      name: string;
      body: string;
      version: number;
    };
    warnings: ValidationIssue[];
  } | {
    status: "conflict";
    current: {
      _id: Types.ObjectId;
      name: string;
      version: number;
    };
  } | {
    status: "not_found";
  }>;
  /** Checks role-level skill creation permission for the current user. */
  canCreateSkill?: (params: {
    req: ServerRequest;
  }) => Promise<boolean>;
  /** Checks resource-level edit permission for an existing skill. */
  canEditSkill?: (params: {
    req: ServerRequest;
    skillId: Types.ObjectId | string;
  }) => Promise<boolean>;
  /** Grants SKILL_OWNER to the current user after a tool-created skill is inserted. */
  grantSkillOwner?: (params: {
    req: ServerRequest;
    skillId: Types.ObjectId | string;
  }) => Promise<void>;
  /** Deletes a freshly-created skill if owner-permission setup fails. */
  deleteSkill?: (id: string) => Promise<{
    deleted: boolean;
  }>;
  /** Saves or replaces a bundled skill file in configured storage and metadata. */
  saveSkillFileContent?: (params: {
    req: ServerRequest;
    skillId: Types.ObjectId | string;
    relativePath: string;
    content: string;
    mimeType: string;
  }) => Promise<{
    bytes: number;
    relativePath: string;
  }>;
  /** Lists files bundled with a skill (for code env priming) */
  listSkillFiles?: (skillId: Types.ObjectId | string) => Promise<SkillFileRecord[]>;
  /** Storage strategy resolver for skill file streaming */
  getStrategyFunctions?: (source: string) => {
    getDownloadStream?: (req: ServerRequest, filepath: string) => Promise<NodeJS.ReadableStream>;
    [key: string]: unknown;
  };
  /** Batch uploads files to the code execution environment. `kind`/`id`/
  *  `version?` carry the resource identity codeapi uses to derive the
  *  sessionKey for the batch's storage bucket. */
  batchUploadCodeEnvFiles?: (params: {
    req: ServerRequest;
    files: Array<{
      stream: NodeJS.ReadableStream;
      filename: string;
    }>;
    kind: "skill" | "agent" | "user";
    id: string;
    version?: number;
    read_only?: boolean;
    codeApiBaseUrl?: string;
    executionProfile?: CodeExecutionContext["executionProfile"];
    bridgeWorkerId?: string;
    signal?: AbortSignal;
  }) => Promise<{
    storage_session_id: string;
    files: Array<{
      fileId: string;
      filename: string;
    }>;
  }>;
  /** Checks if a code env file is still active. Returns lastModified or null. */
  getSessionInfo?: (ref: CodeEnvRef, req?: ServerRequest, route?: {
    baseUrl?: string;
    executionProfile?: CodeExecutionContext["executionProfile"];
    bridgeWorkerId?: string;
  }) => Promise<string | null>;
  /** 23-hour freshness check */
  checkIfActive?: (dateString: string) => boolean;
  /** Persists `codeEnvRef` on skill files after upload */
  updateSkillFileCodeEnvIds?: (updates: Array<{
    skillId: Types.ObjectId | string;
    relativePath: string;
    codeEnvRef: CodeEnvRef;
  }>) => Promise<void>;
  /** Loads a skill file by path (for read_file tool) */
  getSkillFileByPath?: (skillId: Types.ObjectId | string, relativePath: string) => Promise<{
    content?: string;
    isBinary?: boolean;
    mimeType: string;
    bytes: number;
    filepath: string;
    source: string;
    relativePath: string;
  } | null>;
  /** Updates cached content on a skill file (lazy caching after first read) */
  updateSkillFileContent?: (skillId: Types.ObjectId | string, relativePath: string, update: {
    content?: string;
    isBinary?: boolean;
  }) => Promise<void>;
  /** Reads a bounded text range from an attached worker's logical workspace. */
  readWorkspaceFile?: (params: {
    file_path: string;
    workspace_id: string;
    start_line: number;
    max_lines: number;
    codeApiBaseUrl: string;
    executionProfile: CodeExecutionContext["executionProfile"];
    bridgeWorkerId?: string;
    req?: ServerRequest;
    signal?: AbortSignal;
  }) => Promise<WorkspaceReadResult>;
  /** Searches literal text within an attached worker's logical workspace. */
  searchWorkspace?: (params: {
    query: string;
    workspace_id: string;
    path?: string;
    max_results: number;
    codeApiBaseUrl: string;
    executionProfile: CodeExecutionContext["executionProfile"];
    bridgeWorkerId?: string;
    req?: ServerRequest;
    signal?: AbortSignal;
  }) => Promise<WorkspaceSearchResult>;
  /** Lists relative file paths within an attached worker's logical workspace. */
  listWorkspaceFiles?: (params: {
    workspace_id: string;
    path?: string;
    after_path?: string;
    max_results: number;
    codeApiBaseUrl: string;
    executionProfile: CodeExecutionContext["executionProfile"];
    bridgeWorkerId?: string;
    req?: ServerRequest;
    signal?: AbortSignal;
  }) => Promise<WorkspaceListResult>;
  /** Writes a UTF-8 file within an attached worker's logical workspace. */
  writeWorkspaceFile?: (params: {
    file_path: string;
    content: string;
    overwrite: boolean;
    workspace_id: string;
    codeApiBaseUrl: string;
    executionProfile: CodeExecutionContext["executionProfile"];
    bridgeWorkerId?: string;
    req?: ServerRequest;
    signal?: AbortSignal;
  }) => Promise<WorkspaceWriteResult>;
  /** Previews exact replacements without mutating an attached worker workspace. */
  previewWorkspaceEdit?: (params: {
    file_path: string;
    edits: Array<{
      oldText: string;
      newText: string;
    }>;
    workspace_id: string;
    codeApiBaseUrl: string;
    executionProfile: CodeExecutionContext["executionProfile"];
    bridgeWorkerId?: string;
    req?: ServerRequest;
    signal?: AbortSignal;
  }) => Promise<WorkspacePreviewEditResult>;
  /** Applies exact replacements atomically within an attached worker workspace. */
  editWorkspaceFile?: (params: {
    file_path: string;
    edits: Array<{
      oldText: string;
      newText: string;
    }>;
    expected_base_sha256?: string;
    workspace_id: string;
    codeApiBaseUrl: string;
    executionProfile: CodeExecutionContext["executionProfile"];
    bridgeWorkerId?: string;
    req?: ServerRequest;
    signal?: AbortSignal;
  }) => Promise<WorkspaceEditResult>;
  /**
  * Reads a code-execution sandbox file by shelling `cat` through the
  * sandbox `/exec` endpoint. The host implementation supplies the
  * codeapi base URL + auth and forwards the seeded `session_id` and
  * `files` so the read lands in the same sandbox session that holds
  * the agent's prior-turn artifacts. Returns `null` when codeapi is
  * unavailable; throws on transport errors so the handler can surface
  * a meaningful error message to the model.
  */
  readSandboxFile?: (params: {
    file_path: string;
    session_id?: string;
    files?: SandboxFileRef[];
    /** Per-conversation stateful runtime-session hint (thread_id); forwarded so a
    *  host file op that is the first sandbox call joins the same runtime session
    *  as bash_tool instead of the Code API's default session. */
    runtime_session_hint?: string;
    codeApiBaseUrl?: string;
    executionProfile?: CodeExecutionContext["executionProfile"];
    bridgeWorkerId?: string;
    executionRouteKey?: string;
    req?: ServerRequest;
  }) => Promise<{
    content: string;
  } | null>;
  /**
  * Reads a small image file out of the code-execution sandbox as base64 so
  * `read_file` can surface it to vision-capable models. The `readSandboxFile`
  * `cat` path round-trips stdout through codeapi's JSON transport, which
  * lossily replaces non-UTF-8 bytes and mangles image data — this reader
  * base64-encodes the bytes IN the sandbox (ASCII-safe over JSON) after an
  * in-sandbox size guard so an oversize image never crosses the wire.
  * Returns `null` when codeapi is unavailable; throws on transport / read
  * errors so the handler can fall back to an instructive message.
  */
  readSandboxImage?: (params: {
    file_path: string;
    session_id?: string;
    files?: SandboxFileRef[]; /** @see readSandboxFile.runtime_session_hint */
    runtime_session_hint?: string;
    codeApiBaseUrl?: string;
    executionProfile?: CodeExecutionContext["executionProfile"];
    bridgeWorkerId?: string;
    executionRouteKey?: string; /** In-sandbox size cap; files larger than this return `tooLarge` without transferring bytes. */
    maxBytes?: number;
    req?: ServerRequest;
    signal?: AbortSignal;
  }) => Promise<{
    base64: string;
    bytes: number;
  } | {
    tooLarge: true;
    reason?: "size" | "round_trips";
    bytes: number;
    inlineCeiling?: number;
  } | null>;
  /**
  * Writes a UTF-8 text file into the code-execution sandbox via the
  * sandbox `/exec` endpoint. Mirrors `readSandboxFile` session forwarding
  * so host-side file-authoring tools can operate in the same sandbox
  * session as `bash_tool` / `read_file`.
  */
  writeSandboxFile?: (params: {
    file_path: string;
    content: string;
    session_id?: string;
    files?: SandboxFileRef[]; /** @see readSandboxFile.runtime_session_hint */
    runtime_session_hint?: string;
    codeApiBaseUrl?: string;
    executionProfile?: CodeExecutionContext["executionProfile"];
    req?: ServerRequest;
  }) => Promise<{
    stdout?: string;
    stderr?: string;
    session_id?: string;
    files?: SandboxFileRef[];
    artifact_delivery?: ArtifactDeliveryFailure;
  } | null>;
}
/**
* A code-session file ref as it crosses the host boundary: the SDK's wire
* shape (`kind` / `resource_id` / `version` / `inherited`) plus the legacy
* per-file `session_id` older Code API responses carry, which
* `getPreparedCodeOutputBuffer` still reads as a storage-session fallback.
* Every field is load-bearing on the wire — `version` is required for
* `kind: 'skill'` refs and `resource_id` names the resource that owns the
* file's storage session — so refs must be carried whole, never rebuilt
* from a subset.
*/
type SandboxFileRef = FileRefs[number] & {
  session_id?: string;
};
declare function createToolExecuteHandler(options: ToolExecuteOptions): EventHandler$1;
/**
* Creates a handlers object that includes ON_TOOL_EXECUTE.
* Can be merged with other handler objects.
*/
declare function createToolExecuteHandlers(options: ToolExecuteOptions): Record<string, EventHandler$1>;
//#endregion
//#region src/agents/openai/handlers.d.ts
/**
* Create a chat completion chunk in OpenAI format
*/
declare function createChunk(context: OpenAIResponseContext, delta: ChatCompletionChunkChoice["delta"], finishReason?: ChatCompletionChunkChoice["finish_reason"], usage?: CompletionUsage): ChatCompletionChunk;
/**
* Write an SSE event to the response
*/
declare function writeSSE(res: Response$2, data: ChatCompletionChunk | string): void;
/**
* Lightweight tracker for streaming responses.
* Only tracks what's needed for finish_reason and usage - doesn't store content.
*/
interface OpenAIStreamTracker {
  /** Whether any text content was emitted */
  hasText: boolean;
  /** Whether any reasoning content was emitted */
  hasReasoning: boolean;
  /** Accumulated tool calls by index */
  toolCalls: Map<number, ToolCall>;
  /** Accumulated usage metadata */
  usage: {
    promptTokens: number;
    completionTokens: number;
    reasoningTokens: number;
  };
  /** Mark that text was emitted */
  addText: () => void;
  /** Mark that reasoning was emitted */
  addReasoning: () => void;
}
/**
* Create a lightweight stream tracker (doesn't store content)
*/
declare function createOpenAIStreamTracker(): OpenAIStreamTracker;
/**
* Content aggregator for non-streaming responses.
* Accumulates full text content, reasoning, and tool calls.
* Uses arrays for O(n) text accumulation instead of O(n²) string concatenation.
*/
interface OpenAIContentAggregator {
  /** Accumulated text chunks */
  textChunks: string[];
  /** Accumulated reasoning/thinking chunks */
  reasoningChunks: string[];
  /** Accumulated tool calls by index */
  toolCalls: Map<number, ToolCall>;
  /** Accumulated usage metadata */
  usage: {
    promptTokens: number;
    completionTokens: number;
    reasoningTokens: number;
  };
  /** Get accumulated text (joins chunks) */
  getText: () => string;
  /** Get accumulated reasoning (joins chunks) */
  getReasoning: () => string;
  /** Add text chunk */
  addText: (text: string) => void;
  /** Add reasoning chunk */
  addReasoning: (text: string) => void;
}
/**
* Create a content aggregator for non-streaming responses
*/
declare function createOpenAIContentAggregator(): OpenAIContentAggregator;
/**
* Handler configuration for OpenAI streaming
*/
interface OpenAIStreamHandlerConfig {
  res: Response$2;
  context: OpenAIResponseContext;
  tracker: OpenAIStreamTracker;
}
/**
* Graph event types from @librechat/agents
*/
declare const GraphEvents: {
  readonly CHAT_MODEL_END: "on_chat_model_end";
  readonly TOOL_END: "on_tool_end";
  readonly CHAT_MODEL_STREAM: "on_chat_model_stream";
  readonly ON_RUN_STEP: "on_run_step";
  readonly ON_RUN_STEP_DELTA: "on_run_step_delta";
  readonly ON_RUN_STEP_COMPLETED: "on_run_step_completed";
  readonly ON_MESSAGE_DELTA: "on_message_delta";
  readonly ON_REASONING_DELTA: "on_reasoning_delta";
  readonly ON_TOOL_EXECUTE: "on_tool_execute";
};
/**
* Step types from librechat-data-provider
*/
declare const StepTypes: {
  readonly MESSAGE_CREATION: "message_creation";
  readonly TOOL_CALLS: "tool_calls";
};
/**
* Event data interfaces
*/
interface MessageDeltaData {
  id?: string;
  content?: Array<{
    type: string;
    text?: string;
  }>;
}
interface RunStepDeltaData {
  id?: string;
  delta?: {
    type?: string;
    tool_calls?: Array<{
      index?: number;
      id?: string;
      type?: string;
      function?: {
        name?: string;
        arguments?: string;
      };
    }>;
  };
}
interface ToolEndData {
  output?: {
    name?: string;
    tool_call_id?: string;
    content?: string;
  };
}
interface ModelEndData {
  output?: {
    usage_metadata?: {
      input_tokens?: number;
      output_tokens?: number;
      model?: string;
      output_token_details?: {
        reasoning?: number;
        reasoning_tokens?: number;
      };
    };
  };
}
/**
* Event handler interface
*/
interface EventHandler {
  handle(event: string, data: unknown, metadata?: Record<string, unknown>, graph?: unknown): void | Promise<void>;
}
/**
* Handler for message delta events - streams text content
*/
declare class OpenAIMessageDeltaHandler implements EventHandler {
  private config;
  constructor(config: OpenAIStreamHandlerConfig);
  handle(_event: string, data: MessageDeltaData): void;
}
/**
* Handler for run step delta events - streams tool calls
*/
declare class OpenAIRunStepDeltaHandler implements EventHandler {
  private config;
  constructor(config: OpenAIStreamHandlerConfig);
  handle(_event: string, data: RunStepDeltaData): void;
}
/**
* Handler for run step events - sends initial tool call info
*/
declare class OpenAIRunStepHandler implements EventHandler {
  private config;
  constructor(config: OpenAIStreamHandlerConfig);
  handle(_event: string, data: {
    stepDetails?: {
      type?: string;
    };
  }): void;
}
/**
* Handler for model end events - captures usage
*/
declare class OpenAIModelEndHandler implements EventHandler {
  private config;
  constructor(config: OpenAIStreamHandlerConfig);
  handle(_event: string, data: ModelEndData): void;
}
/**
* Handler for chat model stream events
*/
declare class OpenAIChatModelStreamHandler implements EventHandler {
  handle(): void;
}
/**
* Handler for tool end events
*/
declare class OpenAIToolEndHandler implements EventHandler {
  handle(): void;
}
/**
* Handler for reasoning delta events.
* Streams reasoning/thinking content using the `delta.reasoning` field (OpenRouter convention).
*/
declare class OpenAIReasoningDeltaHandler implements EventHandler {
  private config;
  constructor(config: OpenAIStreamHandlerConfig);
  handle(_event: string, data: MessageDeltaData): void;
}
/**
* Create all handlers for OpenAI streaming format
*/
declare function createOpenAIHandlers(config: OpenAIStreamHandlerConfig, toolExecuteOptions?: ToolExecuteOptions): Record<string, EventHandler>;
/**
* Send the final chunk with finish_reason and optional usage
*/
declare function sendFinalChunk(config: OpenAIStreamHandlerConfig, finishReason?: ChatCompletionChunkChoice["finish_reason"], usageOverride?: CompletionUsage): void;
/** Build provider-normalized chat-completion usage from every billed call. */
declare function buildCompletionUsage(collectedUsage: ReadonlyArray<UsageMetadata | null | undefined>): CompletionUsage;
//#endregion
//#region src/langfuse/identity.d.ts
/** Request-scoped values a run may expose as trace metadata when allowlisted. */
type LangfuseTraceContext = Partial<Record<LangfuseTraceConversationMetadataField, string | null>>;
//#endregion
//#region src/tools/rolePermissions.d.ts
/**
* Role permission that gates a built-in agent tool. The matching
* `AgentCapabilities` entry is the instance-wide deployment switch; this is the
* per-role grant, so a tool has to clear both.
*/
declare const toolRolePermissions: Partial<Record<string, PermissionTypes>>;
/**
* Role permission required to upload for a given tool resource — the other half
* of the same door as {@link toolRolePermissions}. `code_interpreter` is the
* Assistants-side name for the same capability `execute_code` covers on agents.
*/
declare const toolResourceRolePermissions: Partial<Record<string, PermissionTypes>>;
/**
* Role permission that gates a native Assistants tool by its provider-side
* `type`. These run inside the provider, never through the agent tool loaders,
* so they have to be gated where the assistant is configured.
*/
declare const assistantToolRolePermissions: Partial<Record<string, PermissionTypes>>;
interface CheckToolRolePermissionParams {
  req?: Request;
  user?: CheckAccessParams["user"] | null;
  permissionType: PermissionTypes;
  getRoleByName: CheckAccessParams["getRoleByName"];
  /** Prefix for the denial log line, e.g. `loadAgentTools`. */
  context?: string;
  /**
  * Rethrow when the role lookup itself fails, instead of reporting a denial.
  *
  * Denying on error is right where a denial blocks the operation. It is wrong
  * where a denial instead *filters* a payload the caller then persists: there,
  * a transient outage would silently strip an assistant's native tools and save
  * the result. Callers that mutate pass this so the write aborts instead.
  */
  throwOnError?: boolean;
}
/**
* Resolves a single `USE` grant through the per-request permission cache, so
* repeat checks within one request cost no extra role read.
*
* Fails closed: a missing user or a check that throws denies the tool.
*/
declare function checkToolRolePermission({
  req,
  user,
  permissionType,
  getRoleByName,
  context,
  throwOnError
}: CheckToolRolePermissionParams): Promise<boolean>;
interface ResolveToolRolePermissionsParams {
  req?: Request;
  /** Tool names as configured on the agent. */
  tools?: string[] | null;
  getRoleByName: CheckAccessParams["getRoleByName"];
  /**
  * Optional pre-filter. Tools this rejects are never checked, so a tool already
  * turned off by its `AgentCapabilities` switch costs no role read and logs no
  * denial.
  */
  isEligible?: (tool: string) => boolean;
  context?: string;
}
/**
* Resolves role permissions for the gated tools an agent actually requests and
* returns a synchronous predicate, so a capability filter can consult the result
* inline. Tools carrying no role permission always pass.
*/
declare function resolveToolRolePermissions({
  req,
  tools,
  getRoleByName,
  isEligible,
  context
}: ResolveToolRolePermissionsParams): Promise<(tool: string) => boolean>;
interface CheckToolResourceUploadParams {
  req?: Request;
  /** `tool_resource` as posted by the client. */
  toolResource?: string | null;
  getRoleByName: CheckAccessParams["getRoleByName"];
}
/**
* Authorizes an upload destined for a role-gated tool resource. Shared by every
* upload handler that accepts `tool_resource` — the ordinary `/files` route and
* `/files/images`, which routes agent uploads of its own.
*
* Resolves to `allowed` for a resource that carries no role permission.
*/
declare function checkToolResourceUploadPermission({
  req,
  toolResource,
  getRoleByName
}: CheckToolResourceUploadParams): Promise<boolean>;
interface ResolveAssistantToolPermissionsParams {
  req?: Request;
  /** Tools as posted: names, or provider definitions carrying a `type`. */
  tools?: Array<string | {
    type?: string;
  } | null | undefined> | null;
  getRoleByName: CheckAccessParams["getRoleByName"];
}
/**
* Resolves which native Assistants tools the role may configure, and returns a
* synchronous predicate for filtering a tool payload.
*
* Native tools run inside the provider and never reach the agent tool loaders,
* so the assistant writers are where they have to be gated.
*
* Every caller either persists the filtered payload or rejects the request, so a
* role lookup that fails propagates rather than reporting a denial: treating an
* outage as "not permitted" here would quietly strip an assistant's native tools
* and save that as its new configuration.
*/
declare function resolveAssistantToolPermissions({
  req,
  tools,
  getRoleByName
}: ResolveAssistantToolPermissionsParams): Promise<(tool: string | {
  type?: string;
} | null | undefined) => boolean>;
interface ToolRoleGrants {
  /** `RUN_CODE.USE` — the sandbox, its file tools, and PTC. */
  runCode: boolean;
  /** `FILE_SEARCH.USE` — the search tool and its uploads. */
  fileSearch: boolean;
  /** `WEB_SEARCH.USE` — the `web_search` tool and provider-native web search. */
  webSearch: boolean;
}
interface ResolveToolRoleGrantsParams {
  req?: Request;
  /**
  * Whose grants to resolve. Defaults to `req.user`, which is all a route-backed
  * caller needs. Callers that carry the user outside the request — the agent
  * execution context, whose `runtime` is the canonical source and whose `req` is
  * a compatibility adapter some routes no longer pass — supply it here, or the
  * absent `req.user` would read as a denial.
  */
  user?: CheckAccessParams["user"] | null;
  getRoleByName: CheckAccessParams["getRoleByName"];
  context?: string;
}
/**
* Resolves all three tool grants for a request, once.
*
* Every gate on `AgentCapabilities.execute_code` / `file_search` / `web_search`
* reads its role half from here, so a boundary is authorized by pairing the
* capability with a field of this object rather than by repeating a permission
* check. The three lookups run together and the result is memoized on the
* request, so a startup that consults several gates — the tool loader, the
* agent initializer, an upload handler — pays one role read between them.
*
* Callers that want the read off their critical path can start it early without
* awaiting; the memoized promise is what later callers join.
*
* Fails closed: a missing user or a check that throws denies all three.
*/
declare function resolveToolRoleGrants({
  req,
  user,
  getRoleByName,
  context
}: ResolveToolRoleGrantsParams): Promise<ToolRoleGrants>;
//#endregion
//#region src/agents/openai/service.d.ts
/**
* Dependencies for the chat completion service
*/
interface ChatCompletionDependencies {
  readonly onTraversalFailure?: LocatorTraversalReporter;
  /** Get agent by ID */
  getAgent: (params: {
    id: string;
  }) => Promise<Agent$3 | null>;
  /** Initialize agent for use */
  initializeAgent: (params: InitializeAgentParams$1) => Promise<InitializedAgent$1>;
  /** Load agent tools */
  loadAgentTools?: LoadToolsFn;
  /** Get models config */
  getModelsConfig?: (req: Request) => Promise<unknown>;
  /** Validate agent model */
  validateAgentModel?: (params: unknown) => Promise<{
    isValid: boolean;
    error?: {
      message: string;
    };
  }>;
  /** Log violation */
  logViolation?: (req: Request, res: Response$2, type: string, info: unknown, score: number) => Promise<void>;
  /** Create agent run */
  createRun?: CreateRunFn;
  /**
  * App config. Optional for basic chat, but required for source-aware content
  * protection, tenant-scoped Langfuse fanout, and agents with `execute_code`
  * in their tools. Filter policy and tenant Langfuse keys are forwarded to
  * `createRun`, and the helper derives `codeEnvAvailable` from
  * `appConfig?.endpoints?.agents?.capabilities` and forwards it into
  * `deps.initializeAgent`. When `appConfig` is omitted, the resolved
  * `codeEnvAvailable` is `undefined`, so `initializeAgent` skips the
  * `execute_code` → `bash_tool` + `read_file` expansion entirely and
  * code-requesting agents silently lose sandbox tools. Pass `appConfig`
  * (even a minimal shape with just `endpoints.agents.capabilities`) to
  * keep tenant tracing and code execution working.
  */
  appConfig?: AppConfig$1;
  /**
  * Supply to have `codeEnvAvailable` and `fileSearchAvailable` respect the
  * caller's `RUN_CODE` / `FILE_SEARCH` grants as well as the deployment
  * capabilities. Optional so existing embedders keep their current behavior;
  * without it this route is gated by capability alone.
  */
  getRoleByName?: Parameters<typeof resolveToolRoleGrants>[0]["getRoleByName"];
  /** Tool execute options for event-driven tool execution */
  toolExecuteOptions?: ToolExecuteOptions;
}
/**
* Agent type from librechat-data-provider
*/
interface Agent$3 {
  id: string;
  name?: string;
  model?: string;
  provider: string;
  tools?: string[];
  instructions?: string;
  model_parameters?: Record<string, unknown>;
  tool_resources?: Record<string, unknown>;
  tool_options?: Record<string, unknown>;
  [key: string]: unknown;
}
interface InitializedFile extends FileContentInput {
  file_id?: string;
}
/**
* Initialized agent type - note: after initialization, tools become structured tool objects
*/
interface InitializedAgent$1 {
  id: string;
  name?: string;
  model?: string;
  provider: string;
  /** After initialization, tools are structured tool objects, not strings */
  tools: unknown[];
  instructions?: string;
  model_parameters?: Record<string, unknown>;
  tool_resources?: Record<string, unknown>;
  tool_options?: Record<string, unknown>;
  attachments: InitializedFile[];
  requestAttachments?: InitializedFile[];
  agentContextAttachments?: InitializedFile[];
  toolContextMap: Record<string, unknown>;
  dynamicToolContextMap?: Record<string, unknown>;
  maxContextTokens: number;
  userMCPAuthMap?: Record<string, Record<string, string>>;
  subagentAgentConfigs?: InitializedAgent$1[];
  /** Names of tools with the host-injected `intent` label param (see `agents/intent.ts`). */
  intentToolNames?: string[];
  recursion_limit?: number;
  [key: string]: unknown;
}
/**
* Initialize agent params
*/
interface InitializeAgentParams$1 {
  req: Request;
  res: Response$2;
  /** Cancellation signal owned by this chat-completion request. */
  signal?: InitializeAgentParams["signal"];
  agent: Agent$3;
  conversationId?: string | null;
  parentMessageId?: string | null;
  requestBody?: MCPRuntimeRequestBody;
  requestFiles?: unknown[];
  loadTools?: NonNullable<InitializeAgentParams["loadTools"]>;
  endpointOption?: Record<string, unknown>;
  allowedProviders: Set<string>;
  isInitialAgent?: boolean;
  /**
  * Whether the `execute_code` capability is enabled for the run.
  * `initializeAgent` uses this to expand `agent.tools: ['execute_code']`
  * into the `bash_tool` + `read_file` pair — if the caller's injected
  * `initializeAgent` implementation consults this flag, agents configured
  * for code execution will keep working post-Phase-8. Absent / `undefined`
  * skips the expansion (same semantics as the in-repo controllers).
  */
  codeEnvAvailable?: boolean;
  /**
  * Whether `file_search` is available to this caller — the capability AND, when
  * the embedder wires `getRoleByName`, the `FILE_SEARCH` grant. Read only when
  * re-hydrating a conversation's prior-turn files; absent / `undefined` leaves
  * that priming unconditional.
  */
  fileSearchAvailable?: boolean;
  /**
  * Resolves the `WEB_SEARCH` role grant. `initializeAgent` calls it only when an
  * agent's built provider config turns native web search on, and strips that
  * search when it resolves `false`.
  */
  resolveWebSearchGrant?: () => Promise<boolean>;
  /**
  * Whether the admin-level `stateful_code_sessions` capability is enabled.
  * Threaded to `initializeAgent` alongside `codeEnvAvailable` so this
  * OpenAI-compatible route resolves stateful sessions identically to the
  * in-repo controllers; absent / `undefined` disables the feature.
  */
  statefulSessionsAvailable?: boolean;
  /** Deployment allowlist carried explicitly because this route's Request has no req.config. */
  allowedStatefulCodeEnvironments?: readonly StatefulCodeEnvironment[];
  /**
  * Whether the admin-level `run_in_background` capability is enabled.
  * Gates `applyBackgroundToolCalls` in `initializeAgent` (the injected
  * `run_in_background` param + the `check_background_task` poll tool);
  * absent / `undefined` disables background tool calls on this route.
  */
  backgroundToolsAvailable?: boolean;
  /**
  * Whether the admin-level `tool_intents` capability is enabled. Gates
  * `applyIntentLabels` in `initializeAgent` (the injected `intent` label
  * param); absent / `undefined` disables intent labels on this route.
  *
  * Boundary: injection and the capability-off sanitize operate on the
  * `toolDefinitions`/`toolRegistry` surfaces. A custom `LoadToolsFn` that
  * returns only structured tool INSTANCES bypasses both — such loaders
  * must provide definition/registry surfaces to participate in intent
  * labels (matching how the in-repo tool loader behaves).
  */
  toolIntentsAvailable?: boolean;
}
/**
* Tool loading function type
*/
type LoadToolsFn = (params: {
  req: Request;
  res: Response$2;
  provider: string;
  agentId: string;
  tools: string[];
  model: string | null;
  tool_options: unknown;
  tool_resources: unknown;
  requestBody?: MCPRuntimeRequestBody;
}) => Promise<{
  tools: unknown[];
  toolContextMap: Record<string, unknown>;
  userMCPAuthMap?: Record<string, Record<string, string>>;
} | null>;
/**
* Create run function type
*/
type CreateRunAppConfig = Pick<AppConfig$1, "endpoints" | "filters" | "langfuse" | "messageFilter">;
type CreateRunFn = (params: {
  agents: unknown[];
  messages: unknown[];
  runId: string;
  signal: AbortSignal;
  customHandlers: Record<string, EventHandler>;
  requestBody: Record<string, unknown>;
  user: Record<string, unknown>;
  traceContext?: LangfuseTraceContext;
  tenantId?: string;
  appConfig?: CreateRunAppConfig;
  tokenCounter?: (message: unknown) => number;
}) => Promise<{
  Graph?: unknown;
  processStream: (input: {
    messages: unknown[];
  }, config: Record<string, unknown>, options: Record<string, unknown>) => Promise<void>;
} | null>;
/**
* App config type
*/
interface AppConfig$1 {
  endpoints?: Record<string, unknown>;
  filters?: FiltersConfig;
  langfuse?: Record<string, unknown>;
  messageFilter?: MessageFilterConfig;
  [key: string]: unknown;
}
/**
* Convert OpenAI messages to TerraMind format
*/
declare function convertMessages(messages: ChatMessage[]): unknown[];
/**
* Create an error response in OpenAI format
*/
declare function createErrorResponse(message: string, type?: string, code?: string | null): OpenAIErrorResponse;
/**
* Send an error response
*/
declare function sendErrorResponse(res: Response$2, statusCode: number, message: string, type?: string, code?: string | null): void;
/**
* Validation result types for chat completion requests
*/
type ChatCompletionValidationSuccess = {
  valid: true;
  request: ChatCompletionRequest;
};
type ChatCompletionValidationFailure = {
  valid: false;
  error: string;
};
type ChatCompletionValidationResult = ChatCompletionValidationSuccess | ChatCompletionValidationFailure;
/**
* Type guard for validation failure
*/
declare function isChatCompletionValidationFailure(result: ChatCompletionValidationResult): result is ChatCompletionValidationFailure;
/**
* Validate the chat completion request
*/
declare function validateRequest(body: unknown): ChatCompletionValidationResult;
/**
* Build a non-streaming response from aggregated content
*/
declare function buildNonStreamingResponse(context: OpenAIResponseContext, text: string, reasoning: string, toolCalls: Map<number, ToolCall>, usage: CompletionUsage): ChatCompletionResponse;
/**
* Main handler for OpenAI-compatible chat completions with agents.
*
* This function:
* 1. Validates the request
* 2. Looks up the agent by ID (model parameter)
* 3. Initializes the agent with tools
* 4. Runs the agent and streams/returns the response
*
* @param req - Express request object
* @param res - Express response object
* @param deps - Dependencies for the service
*/
declare function createAgentChatCompletion(req: Request, res: Response$2, deps: ChatCompletionDependencies): Promise<void>;
/**
* List available agents/models
*
* This provides a /v1/models compatible endpoint that lists available agents.
*/
declare function listAgentModels(_req: Request, res: Response$2, deps: {
  getAgents: (params: Record<string, unknown>) => Promise<Agent$3[]>;
}): Promise<void>;
//#endregion
//#region src/agents/responses/types.d.ts
/**
* Open Responses API Types
*
* Types following the Open Responses specification for building multi-provider,
* interoperable LLM interfaces. Items are the fundamental unit of context,
* and streaming uses semantic events rather than simple deltas.
*
* @see https://openresponses.org/specification
*/
/** Item status lifecycle */
type ItemStatus = "in_progress" | "incomplete" | "completed";
/** Response status lifecycle */
type ResponseStatus = "in_progress" | "completed" | "failed" | "incomplete";
/** Message roles */
type MessageRole = "user" | "assistant" | "system" | "developer";
/** Tool choice options */
type ToolChoiceValue = "none" | "auto" | "required";
/** Truncation options */
type TruncationValue = "auto" | "disabled";
/** Service tier options */
type ServiceTier = "auto" | "default" | "flex" | "priority";
/** Reasoning effort levels */
type ReasoningEffort = "none" | "low" | "medium" | "high" | "xhigh";
/** Reasoning summary options */
type ReasoningSummary = "concise" | "detailed" | "auto";
/** Text input content */
interface InputTextContent {
  type: "input_text";
  text: string;
}
/** Image input content */
interface InputImageContent {
  type: "input_image";
  image_url?: string;
  file_id?: string;
  detail?: "auto" | "low" | "high";
}
/** File input content */
interface InputFileContent {
  type: "input_file";
  file_id?: string;
  file_data?: string;
  filename?: string;
}
/** Union of all input content types */
type InputContent = InputTextContent | InputImageContent | InputFileContent;
/** Log probability for a token */
interface LogProb {
  token: string;
  logprob: number;
  bytes?: number[];
  top_logprobs?: TopLogProb[];
}
/** Top log probability entry */
interface TopLogProb {
  token: string;
  logprob: number;
  bytes?: number[];
}
/** Text output content */
interface OutputTextContent {
  type: "output_text";
  text: string;
  annotations: Annotation[];
  logprobs: LogProb[];
}
/** Refusal content */
interface RefusalContent {
  type: "refusal";
  refusal: string;
}
/** Union of model output content types */
type ModelContent = OutputTextContent | RefusalContent;
/** URL citation annotation */
interface UrlCitationAnnotation {
  type: "url_citation";
  url: string;
  title?: string;
  start_index: number;
  end_index: number;
}
/** File citation annotation */
interface FileCitationAnnotation {
  type: "file_citation";
  file_id: string;
  start_index: number;
  end_index: number;
}
/** Union of annotation types */
type Annotation = UrlCitationAnnotation | FileCitationAnnotation;
/** Reasoning text content */
interface ReasoningTextContent {
  type: "reasoning_text";
  text: string;
}
/** Summary text content */
interface SummaryTextContent {
  type: "summary_text";
  text: string;
}
/** Reasoning content union */
type ReasoningContent = ReasoningTextContent;
/** System message input item */
interface SystemMessageItemParam {
  type: "message";
  role: "system";
  content: string | InputContent[];
}
/** Developer message input item */
interface DeveloperMessageItemParam {
  type: "message";
  role: "developer";
  content: string | InputContent[];
}
/** User message input item */
interface UserMessageItemParam {
  type: "message";
  role: "user";
  content: string | InputContent[];
}
/** Assistant message input item */
interface AssistantMessageItemParam {
  type: "message";
  role: "assistant";
  content: string | ModelContent[];
}
/** Function call input item (for providing context) */
interface FunctionCallItemParam {
  type: "function_call";
  id: string;
  call_id: string;
  name: string;
  arguments: string;
  status?: ItemStatus;
}
/** Function call output input item (for providing tool results) */
interface FunctionCallOutputItemParam {
  type: "function_call_output";
  call_id: string;
  output: string;
  status?: ItemStatus;
}
/** Reasoning input item */
interface ReasoningItemParam {
  type: "reasoning";
  id?: string;
  content?: ReasoningContent[];
  encrypted_content?: string;
  summary?: SummaryTextContent[];
  status?: ItemStatus;
}
/** Item reference (for referencing existing items) */
interface ItemReferenceParam {
  type: "item_reference";
  id: string;
}
/** Union of all input item types */
type InputItem = SystemMessageItemParam | DeveloperMessageItemParam | UserMessageItemParam | AssistantMessageItemParam | FunctionCallItemParam | FunctionCallOutputItemParam | ReasoningItemParam | ItemReferenceParam;
/** Message output item */
interface MessageItem {
  type: "message";
  id: string;
  role: "assistant";
  status: ItemStatus;
  content: ModelContent[];
}
/** Function call output item */
interface FunctionCallItem {
  type: "function_call";
  id: string;
  call_id: string;
  name: string;
  arguments: string;
  status: ItemStatus;
}
/** Function call output result item (internal tool execution result) */
interface FunctionCallOutputItem {
  type: "function_call_output";
  id: string;
  call_id: string;
  output: string;
  status: ItemStatus;
}
/** Reasoning output item */
interface ReasoningItem {
  type: "reasoning";
  id: string;
  status?: ItemStatus;
  content?: ReasoningContent[];
  encrypted_content?: string;
  /** Required per Open Responses spec - summary content parts */
  summary: SummaryTextContent[];
}
/** Union of all output item types */
type OutputItem = MessageItem | FunctionCallItem | FunctionCallOutputItem | ReasoningItem;
/** Function tool definition */
interface FunctionTool {
  type: "function";
  name: string;
  description?: string;
  parameters?: Record<string, unknown>;
  strict?: boolean;
}
/** Hosted tool (provider-specific) */
interface HostedTool {
  type: string;
  [key: string]: unknown;
}
/** Union of tool types */
type Tool = FunctionTool | HostedTool;
/** Specific function tool choice */
interface FunctionToolChoice {
  type: "function";
  name: string;
}
/** Tool choice parameter */
type ToolChoice = ToolChoiceValue | FunctionToolChoice;
/** Reasoning configuration */
interface ReasoningConfig {
  effort?: ReasoningEffort;
  summary?: ReasoningSummary;
}
/** Text output configuration */
interface TextConfig {
  format?: {
    type: "text" | "json_object" | "json_schema";
    json_schema?: Record<string, unknown>;
  };
}
/** Stream options */
interface StreamOptions {
  include_usage?: boolean;
}
/** Metadata (key-value pairs) */
type Metadata = Record<string, string>;
/** Open Responses API Request */
interface ResponseRequest {
  /** Immutable attached-environment choice (TerraMind extension). */
  code_environment_mode?: import("librechat-data-provider").CodeEnvironmentMode;
  /** Explicit registered workspaces for attached agents (TerraMind extension). */
  code_workspaces?: import("librechat-data-provider").CodeWorkspaceSelection[];
  /** Model/agent ID to use */
  model: string;
  /** Input context - string or array of items */
  input: string | InputItem[];
  /** Previous response ID for conversation continuation */
  previous_response_id?: string;
  /** Tools available to the model */
  tools?: Tool[];
  /** Tool choice configuration */
  tool_choice?: ToolChoice;
  /** Whether to stream the response */
  stream?: boolean;
  /** Stream options */
  stream_options?: StreamOptions;
  /** Additional instructions */
  instructions?: string;
  /** Maximum output tokens */
  max_output_tokens?: number;
  /** Maximum tool calls */
  max_tool_calls?: number;
  /** Sampling temperature */
  temperature?: number;
  /** Top-p sampling */
  top_p?: number;
  /** Presence penalty */
  presence_penalty?: number;
  /** Frequency penalty */
  frequency_penalty?: number;
  /** Reasoning configuration */
  reasoning?: ReasoningConfig;
  /** Text output configuration */
  text?: TextConfig;
  /** Truncation behavior */
  truncation?: TruncationValue;
  /** Service tier */
  service_tier?: ServiceTier;
  /** Whether to store the response */
  store?: boolean;
  /** Metadata */
  metadata?: Metadata;
  /** Whether model can call multiple tools in parallel */
  parallel_tool_calls?: boolean;
  /** User identifier for safety */
  user?: string;
}
/** Token usage details */
interface InputTokensDetails {
  cached_tokens: number;
}
/** Output tokens details */
interface OutputTokensDetails {
  reasoning_tokens: number;
}
interface UsageTotals {
  input_tokens: number;
  output_tokens: number;
  total_tokens: number;
}
/** Token usage statistics */
interface Usage {
  input_tokens: number;
  output_tokens: number;
  total_tokens: number;
  input_tokens_details: InputTokensDetails;
  output_tokens_details: OutputTokensDetails;
  /** TerraMind extension for parent, handoff, and summarization model calls. */
  primary?: UsageTotals;
  /** TerraMind extension for isolated subagent child model calls. */
  subagent?: UsageTotals;
}
/** Incomplete details */
interface IncompleteDetails {
  reason: "max_output_tokens" | "max_tool_calls" | "content_filter" | "other";
}
/** Error object */
interface ResponseError {
  type: "server_error" | "invalid_request" | "not_found" | "model_error" | "too_many_requests";
  code?: string;
  message: string;
  param?: string;
}
/** Text field configuration */
interface TextField {
  format?: {
    type: "text" | "json_object" | "json_schema";
    json_schema?: Record<string, unknown>;
  };
}
/** Open Responses API Response - All required fields per spec */
interface Response$1 {
  /** Response ID */
  id: string;
  /** Object type - always "response" */
  object: "response";
  /** Creation timestamp (Unix seconds) */
  created_at: number;
  /** Completion timestamp (Unix seconds) - null if not completed */
  completed_at: number | null;
  /** Response status */
  status: ResponseStatus;
  /** Incomplete details - null if not incomplete */
  incomplete_details: IncompleteDetails | null;
  /** Model that generated the response */
  model: string;
  /** Previous response ID - null if not a continuation */
  previous_response_id: string | null;
  /** Instructions used - null if none */
  instructions: string | null;
  /** Output items */
  output: OutputItem[];
  /** Error - null if no error */
  error: ResponseError | null;
  /** Tools available */
  tools: Tool[];
  /** Tool choice setting */
  tool_choice: ToolChoice;
  /** Truncation setting used */
  truncation: TruncationValue;
  /** Whether parallel tool calls were allowed */
  parallel_tool_calls: boolean;
  /** Text configuration used */
  text: TextField;
  /** Temperature used */
  temperature: number;
  /** Top-p used */
  top_p: number;
  /** Presence penalty used */
  presence_penalty: number;
  /** Frequency penalty used */
  frequency_penalty: number;
  /** Top logprobs - number of most likely tokens to return */
  top_logprobs: number;
  /** Reasoning configuration - null if none */
  reasoning: ReasoningConfig | null;
  /** User identifier - null if none */
  user: string | null;
  /** Token usage - null if not available */
  usage: Usage | null;
  /** Max output tokens - null if not set */
  max_output_tokens: number | null;
  /** Max tool calls - null if not set */
  max_tool_calls: number | null;
  /** Whether response was stored */
  store: boolean;
  /** Whether request was run in background */
  background: boolean;
  /** Service tier used */
  service_tier: string;
  /** Metadata */
  metadata: Metadata;
  /** Safety identifier - null if none */
  safety_identifier: string | null;
  /** Prompt cache key - null if none */
  prompt_cache_key: string | null;
}
/** Base event structure */
interface BaseEvent {
  type: string;
  sequence_number: number;
}
/** Response created event (first event in stream) */
interface ResponseCreatedEvent extends BaseEvent {
  type: "response.created";
  response: Response$1;
}
/** Response in_progress event */
interface ResponseInProgressEvent extends BaseEvent {
  type: "response.in_progress";
  response: Response$1;
}
/** Response completed event */
interface ResponseCompletedEvent extends BaseEvent {
  type: "response.completed";
  response: Response$1;
}
/** Response failed event */
interface ResponseFailedEvent extends BaseEvent {
  type: "response.failed";
  response: Response$1;
}
/** Response incomplete event */
interface ResponseIncompleteEvent extends BaseEvent {
  type: "response.incomplete";
  response: Response$1;
}
/** Output item added event */
interface OutputItemAddedEvent extends BaseEvent {
  type: "response.output_item.added";
  output_index: number;
  item: OutputItem;
}
/** Output item done event */
interface OutputItemDoneEvent extends BaseEvent {
  type: "response.output_item.done";
  output_index: number;
  item: OutputItem;
}
/** Content part added event */
interface ContentPartAddedEvent extends BaseEvent {
  type: "response.content_part.added";
  item_id: string;
  output_index: number;
  content_index: number;
  part: ModelContent | ReasoningContent;
}
/** Content part done event */
interface ContentPartDoneEvent extends BaseEvent {
  type: "response.content_part.done";
  item_id: string;
  output_index: number;
  content_index: number;
  part: ModelContent | ReasoningContent;
}
/** Output text delta event */
interface OutputTextDeltaEvent extends BaseEvent {
  type: "response.output_text.delta";
  item_id: string;
  output_index: number;
  content_index: number;
  delta: string;
  logprobs: LogProb[];
}
/** Output text done event */
interface OutputTextDoneEvent extends BaseEvent {
  type: "response.output_text.done";
  item_id: string;
  output_index: number;
  content_index: number;
  text: string;
  logprobs: LogProb[];
}
/** Refusal delta event */
interface RefusalDeltaEvent extends BaseEvent {
  type: "response.refusal.delta";
  item_id: string;
  output_index: number;
  content_index: number;
  delta: string;
}
/** Refusal done event */
interface RefusalDoneEvent extends BaseEvent {
  type: "response.refusal.done";
  item_id: string;
  output_index: number;
  content_index: number;
  refusal: string;
}
/** Function call arguments delta event */
interface FunctionCallArgumentsDeltaEvent extends BaseEvent {
  type: "response.function_call_arguments.delta";
  item_id: string;
  output_index: number;
  call_id: string;
  delta: string;
}
/** Function call arguments done event */
interface FunctionCallArgumentsDoneEvent extends BaseEvent {
  type: "response.function_call_arguments.done";
  item_id: string;
  output_index: number;
  call_id: string;
  arguments: string;
}
/** Reasoning delta event */
interface ReasoningDeltaEvent extends BaseEvent {
  type: "response.reasoning.delta";
  item_id: string;
  output_index: number;
  content_index: number;
  delta: string;
}
/** Reasoning done event */
interface ReasoningDoneEvent extends BaseEvent {
  type: "response.reasoning.done";
  item_id: string;
  output_index: number;
  content_index: number;
  text: string;
}
/** Error event */
interface ErrorEvent extends BaseEvent {
  type: "error";
  error: ResponseError;
}
/** Attachment content types for TerraMind extensions */
interface TerraMindAttachmentContent {
  /** File ID in TerraMind storage */
  file_id?: string;
  /** Original filename */
  filename?: string;
  /** MIME type */
  type?: string;
  /** URL to access the file */
  url?: string;
  /** Base64-encoded image data (for inline images) */
  image_url?: string;
  /** Width for images */
  width?: number;
  /** Height for images */
  height?: number;
  /** Associated tool call ID */
  tool_call_id?: string;
  /** Additional metadata */
  [key: string]: unknown;
}
/**
* TerraMind attachment event - custom streaming event for file/image attachments
* Follows Open Responses extension pattern with librechat: prefix
*/
interface TerraMindAttachmentEvent extends BaseEvent {
  type: "librechat:attachment";
  /** The attachment data */
  attachment: TerraMindAttachmentContent;
  /** Associated message ID */
  message_id?: string;
  /** Associated conversation ID */
  conversation_id?: string;
}
/** Union of all streaming events (including TerraMind extensions) */
type ResponseEvent = ResponseCreatedEvent | ResponseInProgressEvent | ResponseCompletedEvent | ResponseFailedEvent | ResponseIncompleteEvent | OutputItemAddedEvent | OutputItemDoneEvent | ContentPartAddedEvent | ContentPartDoneEvent | OutputTextDeltaEvent | OutputTextDoneEvent | RefusalDeltaEvent | RefusalDoneEvent | FunctionCallArgumentsDeltaEvent | FunctionCallArgumentsDoneEvent | ReasoningDeltaEvent | ReasoningDoneEvent | ErrorEvent | TerraMindAttachmentEvent;
/** Context for building responses */
interface ResponseContext {
  /** Response ID */
  responseId: string;
  /** Model/agent ID */
  model: string;
  /** Creation timestamp */
  createdAt: number;
  /** Previous response ID */
  previousResponseId?: string;
  /** Instructions */
  instructions?: string;
}
/** Validation result for requests */
interface RequestValidationResult {
  valid: boolean;
  request?: ResponseRequest;
  error?: string;
}
//#endregion
//#region src/agents/responses/handlers.d.ts
/**
* Tracks the state of a response during streaming.
* Manages items, sequence numbers, and accumulated content.
*/
interface ResponseTracker {
  /** Current sequence number (monotonically increasing) */
  sequenceNumber: number;
  /** Output items being built */
  items: OutputItem[];
  /** Current message item (if any) */
  currentMessage: MessageItem | null;
  /** Current message content index */
  currentContentIndex: number;
  /** Current reasoning item (if any) */
  currentReasoning: ReasoningItem | null;
  /** Current reasoning content index */
  currentReasoningContentIndex: number;
  /** Map of function call items by call_id */
  functionCalls: Map<string, FunctionCallItem>;
  /** Map of function call outputs by call_id */
  functionCallOutputs: Map<string, FunctionCallOutputItem>;
  /** Accumulated text for current message */
  accumulatedText: string;
  /** Accumulated reasoning text */
  accumulatedReasoningText: string;
  /** Accumulated function call arguments by call_id */
  accumulatedArguments: Map<string, string>;
  /** Token usage */
  usage: {
    inputTokens: number;
    outputTokens: number;
    reasoningTokens: number;
    cachedTokens: number;
  };
  /** Response status */
  status: ResponseStatus;
  /** Get next sequence number */
  nextSequence: () => number;
}
/**
* Create a new response tracker
*/
declare function createResponseTracker(): ResponseTracker;
/**
* Write a semantic SSE event to the response.
* The `event:` field matches the `type` in the data payload.
*/
declare function writeEvent(res: Response$2, event: ResponseEvent): void;
/**
* Write the terminal [DONE] event
*/
declare function writeDone(res: Response$2): void;
/**
* Build a Response object from context and tracker
* Includes all required fields per Open Responses spec
*/
declare function buildResponse(context: ResponseContext, tracker: ResponseTracker, status?: ResponseStatus, usageOverride?: Usage): Response$1;
/**
* Generate a unique item ID
*/
declare function generateItemId(prefix: string): string;
/**
* Create a new message item
*/
declare function createMessageItem(status?: ItemStatus): MessageItem;
/**
* Create a new function call item
*/
declare function createFunctionCallItem(callId: string, name: string, status?: ItemStatus): FunctionCallItem;
/**
* Create a new function call output item
*/
declare function createFunctionCallOutputItem(callId: string, output: string, status?: ItemStatus): FunctionCallOutputItem;
/**
* Create a new reasoning item
*/
declare function createReasoningItem(status?: ItemStatus): ReasoningItem;
/**
* Create output text content
*/
declare function createOutputTextContent(text?: string): OutputTextContent;
/**
* Create reasoning text content
*/
declare function createReasoningTextContent(text?: string): ReasoningTextContent;
interface StreamHandlerConfig {
  res: Response$2;
  context: ResponseContext;
  tracker: ResponseTracker;
}
/**
* Emit response.created event
* This is the first event emitted per the Open Responses spec
*/
declare function emitResponseCreated(config: StreamHandlerConfig): void;
/**
* Emit response.in_progress event
*/
declare function emitResponseInProgress(config: StreamHandlerConfig): void;
/**
* Emit response.completed event
*/
declare function emitResponseCompleted(config: StreamHandlerConfig, usage?: Usage): void;
/**
* Emit response.failed event
*/
declare function emitResponseFailed(config: StreamHandlerConfig, error: {
  type: string;
  message: string;
  code?: string;
}): void;
/**
* Emit response.output_item.added event for a message
*/
declare function emitMessageItemAdded(config: StreamHandlerConfig): MessageItem;
/**
* Emit response.output_item.done event for a message
*/
declare function emitMessageItemDone(config: StreamHandlerConfig): void;
/**
* Emit response.content_part.added for text content
*/
declare function emitTextContentPartAdded(config: StreamHandlerConfig): void;
/**
* Emit response.output_text.delta event
*/
declare function emitOutputTextDelta(config: StreamHandlerConfig, delta: string): void;
/**
* Emit response.output_text.done event
*/
declare function emitOutputTextDone(config: StreamHandlerConfig): void;
/**
* Emit response.content_part.done for text content
*/
declare function emitTextContentPartDone(config: StreamHandlerConfig): void;
/**
* Emit response.output_item.added for a function call
*/
declare function emitFunctionCallItemAdded(config: StreamHandlerConfig, callId: string, name: string): FunctionCallItem;
/**
* Emit response.function_call_arguments.delta event
*/
declare function emitFunctionCallArgumentsDelta(config: StreamHandlerConfig, callId: string, delta: string): void;
/**
* Emit response.function_call_arguments.done event
*/
declare function emitFunctionCallArgumentsDone(config: StreamHandlerConfig, callId: string): void;
/**
* Emit response.output_item.done for a function call
*/
declare function emitFunctionCallItemDone(config: StreamHandlerConfig, callId: string): void;
/**
* Emit function call output item (internal tool result)
*/
declare function emitFunctionCallOutputItem(config: StreamHandlerConfig, callId: string, output: string): void;
/**
* Emit response.output_item.added for reasoning
*/
declare function emitReasoningItemAdded(config: StreamHandlerConfig): ReasoningItem;
/**
* Emit response.content_part.added for reasoning
*/
declare function emitReasoningContentPartAdded(config: StreamHandlerConfig): void;
/**
* Emit response.reasoning.delta event
*/
declare function emitReasoningDelta(config: StreamHandlerConfig, delta: string): void;
/**
* Emit response.reasoning.done event
*/
declare function emitReasoningDone(config: StreamHandlerConfig): void;
/**
* Emit response.content_part.done for reasoning
*/
declare function emitReasoningContentPartDone(config: StreamHandlerConfig): void;
/**
* Emit response.output_item.done for reasoning
*/
declare function emitReasoningItemDone(config: StreamHandlerConfig): void;
/**
* Emit error event
*/
declare function emitError(config: StreamHandlerConfig, error: {
  type: string;
  message: string;
  code?: string;
}): void;
/**
* Attachment data for librechat:attachment events
*/
interface AttachmentData {
  /** File ID in TerraMind storage */
  file_id?: string;
  /** Original filename */
  filename?: string;
  /** MIME type */
  type?: string;
  /** URL to access the file */
  url?: string;
  /** Base64-encoded image data (for inline images) */
  image_url?: string;
  /** Width for images */
  width?: number;
  /** Height for images */
  height?: number;
  /** Associated tool call ID */
  tool_call_id?: string;
  /**
  * Inline text or sanitized HTML preview (sized to MAX_TEXT_CACHE_BYTES).
  * Populated by `extractCodeArtifactText` for tool-output files: raw text
  * for plain-text artifacts, sanitized rich HTML for office formats
  * (DOCX/XLSX/CSV/PPTX). The frontend feeds HTML through the Sandpack
  * `static` template via `index.html`. Null if extraction was unavailable
  * (binary, oversized, or unsupported).
  */
  text?: string | null;
  /**
  * Format of the `text` field — `'html'` if `text` is a complete
  * sanitized HTML document the client is permitted to inject into the
  * iframe via `index.html`, `'text'` if it's plain text. Clients MUST
  * gate office-bucket routing on `textFormat === 'html'`; legacy
  * attachments and RAG-extracted plain text don't have this set and
  * must default to safe (markdown-escaped) rendering. Codex P1 review
  * on PR #12934.
  */
  textFormat?: "html" | "text" | null;
  /** Additional metadata */
  [key: string]: unknown;
}
/**
* Emit librechat:attachment event for file/image attachments
* This is a TerraMind extension to the Open Responses streaming protocol.
* External clients can safely ignore these events.
*/
declare function emitAttachment$1(config: StreamHandlerConfig, attachment: AttachmentData, options?: {
  messageId?: string;
  conversationId?: string;
}): void;
/**
* Write attachment event directly to response (for use outside streaming context)
* Useful when attachment processing happens asynchronously
*/
declare function writeAttachmentEvent(res: Response$2, sequenceNumber: number, attachment: AttachmentData, options?: {
  messageId?: string;
  conversationId?: string;
}): void;
/**
* Build a complete non-streaming response
*/
declare function buildResponsesNonStreamingResponse(context: ResponseContext, tracker: ResponseTracker): Response$1;
/**
* Update tracker usage from collected data
*/
declare function updateTrackerUsage(tracker: ResponseTracker, usage: {
  promptTokens?: number;
  completionTokens?: number;
  reasoningTokens?: number;
  cachedTokens?: number;
}): void;
//#endregion
//#region src/agents/responses/service.d.ts
/**
* Validate a request body
*/
declare function validateResponseRequest(body: unknown): RequestValidationResult;
/**
* Check if validation failed
*/
declare function isValidationFailure(result: RequestValidationResult): result is {
  valid: false;
  error: string;
};
/** Internal message format (TerraMind-compatible) */
interface InternalMessage {
  role: "system" | "user" | "assistant" | "tool";
  content: string | Array<{
    type: string;
    text?: string;
    image_url?: unknown;
  }>;
  name?: string;
  tool_call_id?: string;
  tool_calls?: Array<{
    id: string;
    type: "function";
    function: {
      name: string;
      arguments: string;
    };
  }>;
}
/**
* Convert Open Responses input to internal message format.
* Handles both string input and array of items.
*/
declare function convertInputToMessages(input: string | InputItem[]): InternalMessage[];
/**
* Merge previous conversation messages with new input
*/
declare function mergeMessagesWithInput(previousMessages: InternalMessage[], newInput: InternalMessage[]): InternalMessage[];
/**
* Send an error response in Open Responses format
*/
declare function sendResponsesErrorResponse(res: Response$2, statusCode: number, message: string, type?: string, code?: string): void;
/**
* Generate a unique response ID
*/
declare function generateResponseId(): string;
/**
* Create a response context from request
*/
declare function createResponseContext(request: ResponseRequest, responseId?: string): ResponseContext;
/**
* Set up streaming response headers
*/
declare function setupStreamingResponse(res: Response$2): void;
/**
* State for tracking streaming progress
*/
interface StreamState {
  messageStarted: boolean;
  messageContentStarted: boolean;
  reasoningStarted: boolean;
  reasoningContentStarted: boolean;
  activeToolCalls: Set<string>;
  completedToolCalls: Set<string>;
}
/**
* Create TerraMind event handlers that emit Open Responses events
*/
declare function createResponsesEventHandlers(config: StreamHandlerConfig): {
  handlers: Record<string, {
    handle: (event: string, data: unknown) => void;
  }>;
  state: StreamState;
  finalizeStream: (usage?: Usage) => void;
};
/**
* Aggregator for non-streaming responses
*/
interface ResponseAggregator {
  textChunks: string[];
  reasoningChunks: string[];
  toolCalls: Map<string, {
    id: string;
    name: string;
    arguments: string;
  }>;
  toolOutputs: Map<string, string>;
  usage: {
    inputTokens: number;
    outputTokens: number;
    reasoningTokens: number;
    cachedTokens: number;
  };
  addText: (text: string) => void;
  addReasoning: (text: string) => void;
  getText: () => string;
  getReasoning: () => string;
}
/**
* Create an aggregator for non-streaming responses
*/
declare function createResponseAggregator(): ResponseAggregator;
/**
* Build a non-streaming response from aggregator
* Includes all required fields per Open Responses spec
*/
declare function buildAggregatedResponse(context: ResponseContext, aggregator: ResponseAggregator, usageOverride?: Usage): Response$1;
/** Build provider-normalized Responses API usage from every billed call. */
declare function buildResponsesUsage(collectedUsage: ReadonlyArray<UsageMetadata | null | undefined>): Usage;
/**
* Create event handlers for non-streaming aggregation
*/
declare function createAggregatorEventHandlers(aggregator: ResponseAggregator): Record<string, {
  handle: (event: string, data: unknown) => void;
}>;
//#endregion
//#region src/agents/envelope.d.ts
declare const AGENT_RUN_ENVELOPE_VERSION: 1;
declare const AGENT_RUN_ENVELOPE_MAX_NESTING_DEPTH: number;
type AgentRunProtocol = "chat.completions" | "responses";
interface AgentRunPrincipal {
  userId: string;
  role?: string;
  tenantId?: string;
}
interface AgentRunPrincipalInput {
  id?: string;
  role?: string;
  tenantId?: string;
}
/** TerraMind request fields consumed by execution but not declared by the public protocols. */
interface AgentRunPayloadExtensions {
  ephemeralAgent?: TEphemeralAgent | null;
  manualSkills?: string[];
  timezone?: string;
  isTemporary?: boolean;
}
type ChatCompletionRunPayload = ChatCompletionRequest & AgentRunPayloadExtensions;
type ResponsesRunPayload = ResponseRequest & AgentRunPayloadExtensions;
interface AgentRunEnvelopeBase {
  version: typeof AGENT_RUN_ENVELOPE_VERSION;
  requestId: string;
  receivedAt: number;
  principal: AgentRunPrincipal;
}
interface ChatCompletionRunEnvelope extends AgentRunEnvelopeBase {
  protocol: "chat.completions";
  payload: ChatCompletionRunPayload;
}
interface ResponsesRunEnvelope extends AgentRunEnvelopeBase {
  protocol: "responses";
  payload: ResponsesRunPayload;
}
type AgentRunEnvelope = ChatCompletionRunEnvelope | ResponsesRunEnvelope;
type CreateAgentRunEnvelopeInput = {
  protocol: "chat.completions";
  requestId: string;
  receivedAt: number;
  principal: AgentRunPrincipalInput | null | undefined;
  payload: ChatCompletionRunPayload;
} | {
  protocol: "responses";
  requestId: string;
  receivedAt: number;
  principal: AgentRunPrincipalInput | null | undefined;
  payload: ResponsesRunPayload;
};
type CreateChatCompletionRunEnvelopeInput = Extract<CreateAgentRunEnvelopeInput, {
  protocol: "chat.completions";
}>;
type CreateResponsesRunEnvelopeInput = Extract<CreateAgentRunEnvelopeInput, {
  protocol: "responses";
}>;
declare class AgentRunEnvelopeError extends TypeError {
  constructor(message: string);
}
/**
* Creates the versioned, transport-safe request that crosses the agent execution seam.
* Runtime objects, provider clients, callbacks, credentials, and Express state belong to
* the execution host and must never be added to this envelope.
*/
declare function createAgentRunEnvelope(input: CreateChatCompletionRunEnvelopeInput): ChatCompletionRunEnvelope;
declare function createAgentRunEnvelope(input: CreateResponsesRunEnvelopeInput): ResponsesRunEnvelope;
//#endregion
//#region src/agents/files.d.ts
type AgentUploadPurpose = EToolResources.context | EToolResources.file_search | EToolResources.execute_code;
type AgentFilePurpose = AgentUploadPurpose | EToolResources.image_edit | EToolResources.ocr;
type AgentManagementFileRecord = {
  file_id: string;
  filename: string;
  bytes: number;
  type: string;
  createdAt?: Date;
};
type AgentManagementFileAgent = AgentManagementProjectionSource & {
  _id: Types.ObjectId;
  provider?: string;
  tool_resources?: Partial<Record<AgentFilePurpose, {
    file_ids?: string[];
  }>>;
};
type AgentUploadConfig = {
  endpoint: string;
  endpointType?: string;
  disabled?: boolean;
  fileSizeLimit?: number;
  fileLimit?: number;
  totalSizeLimit?: number;
};
type AgentUploadLockRedisClient = {
  set: (key: string, value: string, expiryMode: "PX", ttlMs: number, condition: "NX") => Promise<unknown>;
  eval: (script: string, numberOfKeys: number, key: string, ...args: Array<string | number>) => Promise<unknown>;
};
interface AgentManagementFileDeps {
  getRoleByName: (roleName: string, fieldsToSelect?: string | string[]) => Promise<IRole | null>;
  getAgentWithVersionCount: (search: {
    id: string;
    tenantId: string;
  }) => Promise<AgentManagementFileAgent | null>;
  getFiles: (filter: {
    file_id: {
      $in: string[];
    };
    tenantId: string;
  }, sort?: null, projection?: Record<string, 0 | 1>) => Promise<AgentManagementFileRecord[] | null>;
  checkPermission: (params: {
    userId: string;
    role?: string;
    resourceType: ResourceType;
    resourceId: Types.ObjectId;
    requiredPermission: PermissionBits;
  }) => Promise<boolean>;
  hasCapability: (user: IUser, capability: SystemCapability) => Promise<boolean>;
  removeAgentResourceFiles: (params: {
    agent_id: string;
    files: Array<{
      tool_resource: AgentFilePurpose;
      file_id: string;
    }>;
  }) => Promise<AgentManagementFileAgent>;
  processUpload: (req: Request, res: Response$2) => Promise<Response$2 | void>;
  deleteTempFile: (path: string) => Promise<void>;
  getUploadConfig: (req: Request, agent: AgentManagementFileAgent) => Promise<AgentUploadConfig>;
  isUploadPurposeEnabled: (req: Request, purpose: AgentUploadPurpose) => Promise<boolean>;
  runUploadExclusive: <T>(key: string, task: () => Promise<T>) => Promise<T>;
}
/** Serialize an Agent purpose's aggregate-limit check and upload across API replicas. */
declare function createAgentUploadLock({
  redisClient
}: {
  redisClient: AgentUploadLockRedisClient | null;
}): AgentManagementFileDeps["runUploadExclusive"];
/** Restrict the shared browser uploader's response to the management file contract. */
declare function createAgentManagementUploadResponse(res: Response$2, file: Express.Multer.File, purpose: AgentUploadPurpose): Response$2;
/** Machine-authenticated Agent file listing and unlink handlers. */
declare function createAgentManagementFileHandlers(deps: AgentManagementFileDeps): {
  authorizeUpload: (req: Request, res: Response$2, next: NextFunction) => Promise<Response$2 | void>;
  getUploadConfig: (req: Request) => Pick<AgentUploadConfig, "endpoint" | "endpointType"> | undefined;
  upload: (req: Request, res: Response$2) => Promise<Response$2>;
  list: (req: Request, res: Response$2) => Promise<Response$2>;
  remove: (req: Request, res: Response$2) => Promise<Response$2>;
};
//#endregion
//#region src/agents/subagentActivity.d.ts
type SubagentActivityTerminalStatus = "completed" | "failed" | "cancelled";
type SubagentActivityUpdateEvent = SubagentUpdateEvent$1 & {
  /** Host-assigned identity shared by parent and detached delivery paths. */activityEventId?: string; /** Host-assigned monotonic sequence shared by parent and detached delivery paths. */
  activitySequence?: number;
};
type SubagentActivityEnvelope = {
  event: "on_subagent_update";
  data: SubagentActivityUpdateEvent;
};
type SubagentActivitySubscription = {
  unsubscribe: () => void;
  ready?: Promise<void>;
};
type SubagentActivitySubscriber = {
  onEvent: (event: SubagentActivityEnvelope) => void;
  onDone?: (event: {
    final: true;
    subagentActivity: true;
    status: SubagentActivityTerminalStatus;
  }) => void;
  onError?: (error: string) => void;
};
type SubagentActivityStreamDependencies = Pick<ConversationMethods, "getConvoOwnership" | "getSubagentThreadForParent"> & Pick<MessageMethods, "getMessages">;
declare const boundSubagentActivityUpdate: (event: SubagentActivityUpdateEvent) => SubagentActivityUpdateEvent;
declare const subagentActivityStreamId: (threadId: string, taskId: string) => string;
/** Task-scoped live activity over the same in-memory/Redis transports used by generation SSE. */
declare class SubagentActivityStream {
  private readonly transport;
  private readonly demandCache;
  constructor(transport: IEventTransport);
  private isDemanded;
  private renewDemand;
  publish(threadId: string, taskId: string, event: SubagentActivityUpdateEvent): Promise<void>;
  subscribe(threadId: string, taskId: string, subscriber: SubagentActivitySubscriber): SubagentActivitySubscription;
  complete(threadId: string, taskId: string, status: SubagentActivityTerminalStatus): Promise<void>;
  /** Close this process's SSE responses before HTTP drain. Durable child execution and
  * cross-replica activity remain untouched; clients reconnect to another live owner. */
  prepareForShutdown(): void;
  destroy(): void;
}
/** Streams one active child task after the same parent/tenant authorization as its durable view. */
declare function createSubagentActivityStreamHandler(deps: SubagentActivityStreamDependencies, stream: Pick<SubagentActivityStream, "subscribe">): (req: ServerRequest, res: Response$2) => Promise<void>;
declare const SUBAGENT_ACTIVITY_STREAM_LIMITS: Readonly<{
  eventBytes: number;
  labelBytes: number;
}>;
//#endregion
//#region src/agents/subagentTaskRouting.d.ts
/** Bounds the model-facing task list, per owner reply and across the merged result. */
declare const MAX_TASK_SNAPSHOTS = 200;
type RedisClient$1 = Redis | Cluster;
interface SubagentTaskControlHandler {
  claim(scopeId: string, taskId: string): SubagentTaskClaim;
  control(scopeId: string, taskId: string, command: SubagentTaskControlCommand, invocationId: string): Promise<SubagentTaskControlResult> | SubagentTaskControlResult;
  list(scopeId: string): SubagentTaskSnapshot[];
  /** Receipt retry work can outlive the SDK task/result buckets. Keep its owner
  * addressable so deletion can revoke work whose durable target was removed. */
  retainsTaskOwnership(scopeId: string, taskId: string): boolean;
  cancelScope(scopeId: string, threadIds: string[] | null, removedConversationIds?: string[]): number;
}
/** Optional host transport for reaching the process that owns a live child task. */
interface SubagentTaskControlTransport {
  bind(handler: SubagentTaskControlHandler): Promise<void>;
  registerTask(scopeId: string, taskId: string, ttlMs: number): Promise<void>;
  hasTasks(scopeId: string): Promise<boolean>;
  claim(scopeId: string, taskId: string): Promise<SubagentTaskClaim | undefined>;
  control(scopeId: string, taskId: string, command: SubagentTaskControlCommand, invocationId: string): Promise<SubagentTaskControlResult | undefined>;
  list(scopeId: string): Promise<SubagentTaskSnapshot[]>;
  cancelScope(scopeId: string, threadIds: string[] | null, removedConversationIds?: string[]): Promise<number>;
  destroy(): Promise<void>;
}
declare class SubagentTaskOwnerUnavailableError extends Error {
  constructor();
}
interface RedisSubagentTaskControlTransportOptions {
  /** Separates pub/sub channels for deployments sharing one Redis service. */
  namespace?: string;
  instanceId?: string;
  requestTimeoutMs?: number;
  retryDelayMs?: number;
  registrationHeartbeatMs?: number;
}
/**
* Bounds a model-facing task list, keeping what a caller can still act on: running
* children first, then the most recent settled results. A plain oldest-first slice
* would drop the newest tasks, hiding a child that just started from the only tool
* able to poll it.
*/
declare function boundedTaskList(tasks: SubagentTaskSnapshot[]): SubagentTaskSnapshot[];
/** Applies the shared model-facing bound to a durable child result. */
declare function boundedSubagentTaskResult(result: string): string;
/** Applies the routed result and snapshot bounds to a claim from any source. */
declare function boundedClaim(claim: SubagentTaskClaim): SubagentTaskClaim;
/**
* Canonical identity of one control's content. Property order cannot vary it, so the
* transport and the owning task store agree on when two commands are the same, and it
* is hashed so retaining one costs a fixed few bytes rather than a whole message.
*/
declare function controlFingerprint(command: SubagentTaskControlCommand): string;
/**
* Routes bounded live-task operations to their owning API replica. Redis keeps
* only an expiring owner directory and request/reply envelopes; the executor,
* transcript, and checkpoint never move between processes.
*/
declare class RedisSubagentTaskControlTransport implements SubagentTaskControlTransport {
  private readonly publisher;
  private readonly subscriber;
  private readonly instanceId;
  private readonly namespaceHash;
  private readonly requestTimeoutMs;
  private readonly retryDelayMs;
  private readonly registrationHeartbeatMs;
  private readonly pending;
  private readonly claimReplays;
  private readonly controlReplays;
  private readonly ownedTasks;
  private handler?;
  private ready?;
  private registrationHeartbeat?;
  private registrationRefresh?;
  private destroyed;
  constructor(publisher: RedisClient$1, subscriber: RedisClient$1, options?: RedisSubagentTaskControlTransportOptions);
  bind(handler: SubagentTaskControlHandler): Promise<void>;
  registerTask(scopeId: string, taskId: string, ttlMs: number): Promise<void>;
  hasTasks(scopeId: string): Promise<boolean>;
  claim(scopeId: string, taskId: string): Promise<SubagentTaskClaim | undefined>;
  control(scopeId: string, taskId: string, command: SubagentTaskControlCommand, invocationId: string): Promise<SubagentTaskControlResult | undefined>;
  list(scopeId: string): Promise<SubagentTaskSnapshot[]>;
  /**
  * Cancels live children on every other owner of this scope. The owner applies the
  * predicate to its complete local task set, so deletion never depends on the
  * bounded model-facing list and cannot miss a task beyond that cap.
  */
  cancelScope(scopeId: string, threadIds: string[] | null, removedConversationIds?: string[]): Promise<number>;
  destroy(): Promise<void>;
  private readonly onMessage;
  private handleResponse;
  private handleRequest;
  /**
  * Locates a destructive operation's replay slot. A claim consumes the one-shot
  * result, so it is keyed by operation—stable across callers and replicas, so a
  * later poll still resolves to the response the owner produced. Lists are
  * idempotent and recomputable, so their large bodies are never retained.
  */
  private replayFor;
  private claimReplayKey;
  /**
  * Releases a retained result once a caller confirms holding it, so a delivered
  * result frees its slot immediately instead of waiting out the replay window.
  */
  private releaseClaimReplay;
  /**
  * Tells the owner it may release a delivered result. Delivery to zero subscribers is
  * not an acknowledgement, so this retries inside the ordinary request window.
  */
  private acknowledgeClaim;
  private requestTaskOwner;
  private sendRequest;
  private pruneExpiredReplays;
  private retainReplay;
  private publish;
  private ensureRegistrationHeartbeat;
  private refreshRegistrations;
  private publishRegistration;
  private readActiveRegistrations;
  private removeRegistrations;
  private registryKey;
  private registrationKey;
  private channel;
  private assertScope;
  private assertTaskAddress;
  private requireReady;
}
//#endregion
//#region src/agents/subagentDelivery.d.ts
declare const SUBAGENT_COMPLETION_DELIVERY = "wakeup";
/** Host-owned detached-subagent scope with its model-facing result-delivery contract. */
interface HostSubagentTaskConfig extends SubagentTaskConfig {
  completionDelivery?: typeof SUBAGENT_COMPLETION_DELIVERY;
}
declare const SUBAGENT_WAKEUP_GUIDANCE = "Automatic completion delivery is enabled for this subagent task. Continue independent work if available; otherwise end this turn and the host will resume you when the task finishes. Do not repeatedly poll an unchanged running task. Use check_background_task only for explicit status or control, or as a fallback if automatic delivery is unavailable.";
/** Automatic conversational completion is default-on and has one administrator opt-out. */
declare function backgroundCompletionWakeupsEnabled(config: TAgentsEndpoint | undefined): boolean;
declare function usesSubagentCompletionWakeups(config: SubagentTaskConfig | undefined): config is HostSubagentTaskConfig;
/** Automatic delivery is agent-specific even though the SDK task scope is run-wide. */
declare function agentUsesSubagentCompletionWakeups(config: SubagentTaskConfig | undefined, agentId: string | undefined): config is HostSubagentTaskConfig;
/** Replaces the SDK's legacy poll-first handle with the host's durable delivery contract. */
declare function createSubagentWakeupHandleHook(supportsWakeup?: (agentId: string | undefined) => boolean): HookCallback<"PostToolUse">;
//#endregion
//#region src/agents/subagentThreads.d.ts
declare const SCOPE_VERSION = 1;
/** A cancellation target set resolved before the conversations are removed. */
interface SubagentCancellationPlan {
  userId: string;
  tenantId?: string;
  conversationIds: string[];
  scopes: Array<{
    scopeId: string;
    threadIds: string[] | null;
  }>;
  leases: IActiveSubagentThreadLease[];
}
type SubagentThreadMethods = Pick<AllMethods, "acquireSubagentThreadLease" | "claimSubagentTaskResult" | "countActiveSubagentThreadLeases" | "deleteConvos" | "deleteMessages" | "getConvo" | "getSubagentTaskControlReplay" | "getMessages" | "listActiveSubagentThreadLeases" | "reserveSubagentThread" | "releaseSubagentThreadLease" | "recordSubagentTaskControlReceipt" | "renewSubagentThreadLease" | "saveConvo" | "saveMessage">;
interface SubagentThreadScope {
  version: typeof SCOPE_VERSION;
  userId: string;
  parentConversationId: string;
  tenantId?: string;
}
type SdkControlReceipt = {
  controlId: string;
  action: "steer" | "queue" | "interrupt";
  status: "accepted" | "applied" | "rejected" | "failed";
  createdAt: number;
  updatedAt: number;
  boundary?: "preempt" | "tool" | "turn";
  reason?: "withdrawn" | "task_completed" | "task_cancelled" | "task_failed";
};
interface SubagentThreadTaskStoreOptions extends InMemorySubagentTaskStoreOptions {
  maxThreadDepth?: number;
  leaseTtlMs?: number;
  leaseHeartbeatMs?: number;
  ownerDrainTimeoutMs?: number;
  ownerDrainPollMs?: number;
  taskRoutingTtlMs?: number;
  isOwnerActive?: (userId: string) => Promise<boolean>;
  maxControlInvocations?: number;
  controlReceiptRetryMs?: number;
  shutdownControlReceiptBackoffMs?: number;
  ownerFenceGraceMs?: number;
  fenceOwnerAdmission?: (userId: string, token: string, fencedUntil: Date) => Promise<void>;
  renewOwnerAdmission?: (userId: string, token: string, fencedUntil: Date) => Promise<boolean>;
  releaseOwnerAdmission?: (userId: string, token: string) => Promise<void>;
  /** Host-owned work may share the durable child lease protocol without living in
  * this in-memory task store. Return true only after that work is stopped. */
  cancelUnroutedTask?: (target: {
    userId: string;
    parentConversationId: string;
    taskId: string;
    tenantId?: string;
  }) => Promise<boolean>;
  onTaskPrepared?: (registration: SubagentTaskWakeupRegistration) => Promise<void> | void;
}
interface SubagentTaskWakeupRegistration {
  userId: string;
  parentConversationId: string;
  parentMessageId: string;
  parentAgentId?: string;
  tenantId?: string;
  taskId: string;
  threadId: string;
  subagentType: string;
  createdAt: number;
}
/** Builds the trusted live-owner routing scope after parent authorization. */
declare function createSubagentThreadScopeId(scope: Omit<SubagentThreadScope, "version">): string;
/** Persists view-only logical child threads with owner-routed controls and a shared execution fence. */
declare class SubagentThreadTaskStore extends InMemorySubagentTaskStore {
  private readonly methods;
  readonly supportsThreadContinuation = true;
  private readonly activeThreads;
  private readonly controlInvocations;
  private readonly terminalControlInvocations;
  private readonly controlInvocationByReceipt;
  private readonly pendingControlReceipts;
  private readonly controlPersistenceTails;
  private readonly controlPersistenceRetryTimers;
  private readonly controlReservationSlot;
  private controlPersistenceStopping;
  private controlCommandAdmissionClosed;
  private readonly parentPersistence;
  private readonly maxThreadDepth;
  private readonly leaseTtlMs;
  private readonly leaseHeartbeatMs;
  private readonly ownerDrainTimeoutMs;
  private readonly ownerDrainPollMs;
  private readonly taskRoutingTtlMs;
  private readonly maxControlInvocations;
  private readonly controlReceiptRetryMs;
  private readonly shutdownControlReceiptBackoffMs;
  private readonly ownerFenceGraceMs;
  private readonly isOwnerActive;
  private readonly fenceOwnerAdmission?;
  private readonly renewOwnerAdmission?;
  private readonly releaseOwnerAdmission?;
  private readonly cancelUnroutedTask?;
  private readonly onTaskPrepared?;
  private taskControlTransport?;
  private activityStream;
  constructor(methods: SubagentThreadMethods, options?: SubagentThreadTaskStoreOptions);
  /** Receives payload-free authoritative transitions from the SDK task store. */
  protected onControlReceipt(scopeId: string, taskId: string, receipt: SdkControlReceipt): void;
  private queueAuthoritativeControlReceipt;
  /** Keeps same-process retries aligned with the durable receipt ledger. The SDK
  * can replace an accepted control with a terminal transition after the child
  * settles, so the originally returned result is no longer authoritative. */
  private controlResultFromReceipt;
  private durableReceipt;
  private controlResultReceipt;
  private replayDurableControl;
  private queueControlReceipt;
  private flushControlReceipts;
  /** A terminal child may have no later caller to retrigger persistence. Keep a
  * single bounded retry timer per task so transient storage failures converge
  * even after result collection expires its in-memory task; restart durability
  * remains AI-1737. */
  private scheduleControlReceiptRetry;
  private flushControlReceiptsForSettlement;
  /** Enables optional cross-replica lookup after the host's Redis service is ready. */
  configureTaskControlTransport(transport: SubagentTaskControlTransport): Promise<void>;
  destroyTaskControlTransport(): Promise<void>;
  /** Replaces the process-local activity bus after the host's Redis service is ready. */
  configureActivityStream(stream: SubagentActivityStream): void;
  destroyActivityStream(): void;
  prepareActivityForShutdown(): void;
  subscribeActivity(threadId: string, taskId: string, subscriber: SubagentActivitySubscriber): SubagentActivitySubscription;
  /** Publishes activity produced by a host-owned event child. Event children
  * use the same bounded, demand-aware transport as detached tool children,
  * but their generation lease is owned by the trigger controller instead of
  * this task store. */
  publishTaskActivity(threadId: string, taskId: string, event: SubagentUpdateEvent$1): Promise<void>;
  private publishActivity;
  private completeActivity;
  /** Gates child creation on the ordinary parent write without retaining request state. */
  registerParentPersistence(scopeId: string, persistence: Promise<unknown>): void;
  override start(request: SubagentTaskStartRequest): SubagentTaskStartResult;
  /** Detached tasks intentionally outlive the HTTP request that admitted them.
  * Reconstruct only the trusted owner identity carried by the opaque task scope
  * so tenant-isolated database reads and lazy child initialization do not depend
  * on request AsyncLocalStorage remaining alive after the parent turn returns. */
  private runWithOwnerContext;
  /**
  * Claims locally when possible, otherwise asks the registered owning replica.
  *
  * A child's terminal result is durable in its own thread, so collection is recorded
  * there against the polling invocation rather than kept alive in the owner's memory.
  * The invocation that lost a response re-acquires its own result on the next poll;
  * a different invocation is told the result was already collected. Owner-side
  * retention stays a fast path, free to expire, instead of the only copy.
  */
  claimTask(scopeId: string, taskId: string, invocationId?: string): Promise<SubagentTaskClaim>;
  /**
  * Recovers a terminal task after its owning process and Redis registration are gone.
  * The task id locates only a candidate; durable child lineage re-establishes the
  * trusted parent scope before the one-shot result is claimed.
  */
  private claimDurableTaskResult;
  /**
  * Assigns one durable terminal result to the invocation collecting it. A failed
  * write is not an absent record: handing the result over without recording its
  * claimant would let another invocation acquire the same one-shot output once the
  * database recovers, so this reports the retryable path and leaves the result
  * unclaimed for a later poll.
  */
  private assignResultClaim;
  /**
  * Controls locally when possible, otherwise asks the registered owning replica.
  * `invocationId` identifies one caller invocation: a routed retransmission of that
  * invocation replays the owner's result, while a fresh invocation applies again even
  * when its action and message are identical.
  */
  controlTask(scopeId: string, taskId: string, command: SubagentTaskControlCommand, invocationId?: string): Promise<SubagentTaskControlResult>;
  /** Durable receipt reads are part of the owner boundary. Storage ambiguity must
  * remain retryable instead of escaping as an unrelated tool execution failure. */
  private replayDurableControlAtBoundary;
  private retainedControlInvocation;
  private replayRetainedControl;
  private retainedControlResult;
  /**
  * Applies one logical control exactly once for its owning task. Idempotency lives
  * here rather than in the transport so a local and a routed caller of the same
  * invocation agree, and it is keyed by task as well as invocation because provider
  * tool-call ids repeat across runs and agents.
  */
  controlInvocation(scopeId: string, taskId: string, command: SubagentTaskControlCommand, invocationId: string): SubagentTaskControlResult;
  private controlInvocationAndPersist;
  private awaitCurrentControlReceipt;
  /**
  * Frees invocation slots by dropping records whose task the store no longer holds:
  * a settled task cannot be controlled again, so its record is worthless, while a
  * live one is exactly what a caller retry needs to replay instead of applying its
  * command twice. The sweep runs only when the window is full and clears every dead
  * record at once, so it is amortized rather than repeated per control.
  */
  private makeRoomForInvocation;
  /** Returns this process's tasks plus tasks reported by registered remote owners. */
  listTasks(scopeId: string): Promise<SubagentTaskSnapshot[]>;
  /** Fast capability probe used while deciding whether a later turn needs the poll tool. */
  hasTasks(scopeId: string): Promise<boolean>;
  override control(scopeId: string, taskId: string, command: SubagentTaskControlCommand): SubagentTaskControlResult;
  /** Finds a provisional child lease before its conversation is durable. */
  isThreadActiveForOwner(userId: string, threadId: string, tenantId?: string): boolean;
  /** Cancels active descendants before their owning conversations are removed. */
  cancelForConversations(userId: string, conversationIds: Iterable<string>, tenantId?: string): number;
  /**
  * Resolves every cancellation target while the conversations still exist. The plan is
  * replayed after deletion, when those rows can no longer be read back, so the second
  * pass only has to reach registered owners through Redis.
  */
  planCancellationForConversations(userId: string, conversationIds: Iterable<string>, tenantId?: string): Promise<SubagentCancellationPlan>;
  /**
  * Cancels local children and replays a plan against registered remote owners.
  * `removedConversationIds` extends it with the cascade a deletion reported, matched
  * against leases captured before those rows were removed.
  */
  cancelPlan(plan: SubagentCancellationPlan, removedConversationIds?: Iterable<string>): Promise<number>;
  /** A successful deletion makes false receipt writes permanent, not retryable.
  * Remove only work whose authorized parent or child was actually deleted. */
  private dropDeletedControlReceiptWork;
  /** Cancels this process's live children for one scope, optionally narrowed to threads. */
  private cancelForScope;
  /** Cancels every active child owned by a user before a delete-all operation. */
  cancelForOwner(userId: string, tenantId?: string): number;
  /**
  * Deletes an owner's conversations behind a durable admission fence. Draining alone
  * cannot close the race: a child admitted on another replica after the drain read
  * its leases would begin provider work against a parent that is about to disappear.
  * Fencing first inverts that — the fence is written before any lease is read, and a
  * child validates the fence after its own lease is written, so one of the two always
  * observes the other. The fence expires by itself, so a process lost mid-deletion
  * cannot leave the account unable to run subagents.
  */
  withOwnerDeletionFence<T>(userId: string, tenantId: string | undefined, deletion: () => Promise<T>, recoverAdditionalOwnerWork?: () => Promise<void>): Promise<T>;
  /**
  * Cancels local work and waits for every replica's durable lease to drain. Each task
  * is cancelled under one invocation held for the whole drain and only while its
  * owner has not answered: a fresh invocation per poll would retain a replay entry on
  * the owner for every pass, and a task already reported cancelled needs no second
  * command, only its lease to disappear.
  */
  cancelAndDrainForOwner(userId: string, tenantId?: string): Promise<void>;
  /** Sends one drained task's cancellation, retrying only unconfirmed deliveries. */
  private cancelDrainedTask;
  private startSharedLeaseHeartbeat;
  private renewSharedLease;
  private renewSharedLeaseFence;
  private stopAndReleaseSharedLease;
  /** Whether a durable child may be created below the supplied conversation depth. */
  canCreateChildThread(parentDepth: number): boolean;
  private prepareThread;
  private assertContinuation;
  private isContinuationAllowed;
  private persistResult;
  private persistFailure;
  private registerTaskWakeup;
  private persistCancellation;
  private currentConversation;
  private cancelMatchingThreads;
  private requireCurrentConversation;
  private taskInputExists;
  private rollbackPreparation;
  private deleteTaskMessages;
  private touchAfterMessage;
  private aggregateDetachedUsage;
}
declare function createSubagentThreadTaskStore(methods: Pick<ConversationMethods, "acquireSubagentThreadLease" | "countActiveSubagentThreadLeases" | "deleteConvos" | "getConvo" | "listActiveSubagentThreadLeases" | "releaseSubagentThreadLease" | "reserveSubagentThread" | "renewSubagentThreadLease" | "saveConvo"> & Pick<MessageMethods, "claimSubagentTaskResult" | "deleteMessages" | "getSubagentTaskControlReplay" | "getMessages" | "recordSubagentTaskControlReceipt" | "saveMessage">, options?: SubagentThreadTaskStoreOptions): SubagentThreadTaskStore;
declare function buildSubagentThreadTaskConfig(store: SubagentThreadTaskStore, scope: Omit<SubagentThreadScope, "version">, options?: {
  completionWakeups?: boolean;
}): HostSubagentTaskConfig;
//#endregion
//#region src/agents/guard.d.ts
declare const CHILD_THREAD_READ_ONLY_ERROR = "This subagent thread is view-only. Continue it from its parent agent or create a separate chat.";
interface SubagentThreadWriteGuardDeps {
  getConvo: ConversationMethods["getConvo"];
  getEventBinding?: ConversationMethods["getAgentEventBinding"];
  isHumanResumeAllowed?: (target: SubagentThreadWriteTarget) => Promise<boolean>;
  store: SubagentThreadTaskStore;
}
interface SubagentThreadWriteTarget {
  userId: string;
  conversationId: string;
  tenantId?: string;
  /** Conversation already read earlier in the request (`null` = looked up, absent). */
  conversation?: IConversation | null;
}
/**
* Brands the lineage-only conversation `isBoundEventContinuation` synthesizes: it stands in
* for binding checks but carries none of the stored document's optional fields, so readers
* of `req.resolvedConversation` must not treat its absent fields as authoritative.
*/
declare const PARTIAL_RESOLVED_CONVERSATION: unique symbol;
/** Applies the same immutable-child policy to every server write adapter. */
declare function isSubagentThreadWriteBlocked(deps: SubagentThreadWriteGuardDeps, target: SubagentThreadWriteTarget): Promise<boolean>;
/** Rejects model-bound turns for durable or provisionally-created child threads. */
declare function createSubagentThreadTurnGuard(deps: SubagentThreadWriteGuardDeps): RequestHandler;
//#endregion
//#region src/agents/triggers/envelope.d.ts
declare const AGENT_TRIGGER_ENVELOPE_VERSION: 1;
declare const AGENT_TRIGGER_IDEMPOTENCY_PREFIX = "trigger_";
type AgentTriggerMode = "continue" | "fire" | "steer";
interface AgentTriggerSource {
  /** Stable identity of the configured source, such as a webhook or schedule id. */
  id: string;
  /** Source adapter kind. Core dispatch does not interpret this value. */
  type: string;
}
interface AgentTriggerEvent {
  /** Stable identity assigned by the source for this event occurrence. */
  id: string;
  type: string;
  /** Unix time in milliseconds recorded by the source. */
  occurredAt: number;
  /** Sanitized source data; adapters must omit credentials and transport secrets. */
  payload?: JsonValue;
  source: AgentTriggerSource;
}
interface AgentTriggerTarget {
  agentId: string;
}
/**
* Trusted host controls for a new fire. These values shape TerraMind's own
* generation request; they are never sourced directly from model output.
* `metadata` is opaque to the trigger core and lets an authenticated adapter
* identify its delivery to host-side lifecycle hooks without widening the
* public chat request surface.
*/
interface AgentFireRunContext {
  conversationId?: string;
  timezone?: string;
  /** Chat project the new conversation is filed under. Host-controlled like the
  *  rest of this context — never sourced from the event payload. */
  chatProjectId?: string;
  files?: JsonValue[];
  metadata?: JsonValue;
}
/**
* One fire delivery represents one new conversation for this agent; retries
* reuse the delivery id instead of starting another conversation.
*/
type AgentFireTarget = AgentTriggerTarget;
interface AgentContinueTarget extends AgentTriggerTarget {
  /** Existing conversation that receives a new host-authored turn. */
  conversationId: string;
  /** Persisted branch leaf below which the new turn is appended. */
  parentMessageId: string;
  /** Present only after an authenticated source binding resolved the target. */
  bindingId?: string;
  /** API-key identity captured by the ingress adapter and rechecked at dispatch. */
  sourceKeyId?: string;
}
interface AgentSteerTarget extends AgentTriggerTarget {
  /** Existing conversation whose active generation receives the input. */
  conversationId: string;
  /** Fences delivery to the generation observed by the event adapter. */
  generationCreatedAt: number;
  /** Request an interrupt when supported; otherwise delivery stays queued. */
  preempt?: boolean;
}
interface AgentTriggerEnvelopeBase {
  version: typeof AGENT_TRIGGER_ENVELOPE_VERSION;
  requestId: string;
  /** Stable across retries of one source-event-to-target delivery. */
  deliveryId: string;
  /** Unix time in milliseconds recorded by the receiving host. */
  receivedAt: number;
  principal: AgentRunPrincipal;
  event: AgentTriggerEvent;
  /** Host-rendered agent input; source payload remains available in `event.payload`. */
  input: string;
  expectedAction?: AgentTriggerExpectedAction;
}
interface AgentFireTriggerEnvelope extends AgentTriggerEnvelopeBase {
  mode: "fire";
  target: AgentFireTarget;
  run?: AgentFireRunContext;
}
interface AgentContinueTriggerEnvelope extends AgentTriggerEnvelopeBase {
  mode: "continue";
  target: AgentContinueTarget;
}
interface AgentSteerTriggerEnvelope extends AgentTriggerEnvelopeBase {
  mode: "steer";
  target: AgentSteerTarget;
}
type AgentTriggerEnvelope = AgentContinueTriggerEnvelope | AgentFireTriggerEnvelope | AgentSteerTriggerEnvelope;
interface CreateAgentTriggerEnvelopeBase {
  requestId: string;
  deliveryId: string;
  receivedAt: number;
  principal: AgentRunPrincipalInput | null | undefined;
  event: AgentTriggerEvent;
  input: string;
  expectedAction?: AgentTriggerExpectedAction;
}
type CreateAgentTriggerEnvelopeInput = (CreateAgentTriggerEnvelopeBase & {
  mode: "fire";
  target: AgentFireTarget;
  run?: AgentFireRunContext;
}) | (CreateAgentTriggerEnvelopeBase & {
  mode: "continue";
  target: AgentContinueTarget;
}) | (CreateAgentTriggerEnvelopeBase & {
  mode: "steer";
  target: AgentSteerTarget;
});
declare class AgentTriggerEnvelopeError extends TypeError {
  constructor(message: string);
}
declare function createAgentTriggerEnvelope(input: CreateAgentTriggerEnvelopeInput): AgentTriggerEnvelope;
/**
* Validates and detaches an envelope received from a queue or another process.
* Unknown versions, modes, and malformed v1 fields fail closed before dispatch.
*/
declare function parseAgentTriggerEnvelope(input: unknown): AgentTriggerEnvelope;
/**
* Stable idempotency key for one target delivery. Its character set and length
* are accepted by both generation `clientRequestId` and steering `clientSteerId`.
*/
declare function getAgentTriggerIdempotencyKey(envelope: AgentTriggerEnvelope): string;
//#endregion
//#region src/agents/triggers/dispatch.d.ts
interface AgentTriggerDispatchContext {
  idempotencyKey: string;
  /** Durable delivery attempt metadata, when dispatched by the queue engine. */
  attempt?: number;
  maxAttempts?: number;
  signal?: AbortSignal;
}
declare class AgentTriggerDispatchError extends TypeError {
  constructor(message: string);
}
/**
* Host-owned execution adapters. Each handler must enforce current authorization,
* limits, persistence, and the supplied idempotency identity before accepting work.
*/
interface AgentTriggerDispatchHandlers<FireResult, ContinueResult, SteerResult> {
  fire: (envelope: AgentFireTriggerEnvelope, context: AgentTriggerDispatchContext) => Promise<FireResult>;
  continue: (envelope: AgentContinueTriggerEnvelope, context: AgentTriggerDispatchContext) => Promise<ContinueResult>;
  steer: (envelope: AgentSteerTriggerEnvelope, context: AgentTriggerDispatchContext) => Promise<SteerResult>;
}
/** Routes a normalized trigger without coupling its source to an execution transport. */
declare function dispatchAgentTrigger<FireResult, ContinueResult, SteerResult>(envelope: unknown, handlers: AgentTriggerDispatchHandlers<FireResult, ContinueResult, SteerResult>, options?: {
  signal?: AbortSignal;
  attempt?: number;
  maxAttempts?: number;
}): Promise<ContinueResult | FireResult | SteerResult>;
//#endregion
//#region src/agents/triggers/host.d.ts
type MaybePromise<T> = T | Promise<T>;
type FireStatus = "started" | "resumed" | "replaced" | "settled";
type AgentTriggerFetch = (input: string | URL, init?: RequestInit) => Promise<Response>;
interface AgentContinuationAdmissionSource {
  source: string;
  sourceId: string;
  claimId: string;
  claimBy: string;
  effectivePredecessorCreatedAt?: number;
  lineagePredecessorId?: string;
}
type AgentTriggerContinuePreparation = {
  status: "ready";
  input: string;
  parentMessageId: string;
  expectedPredecessorCreatedAt?: number;
  /** Complete user-turn context for internal continuations that must start
  * a fresh ordinary Agent turn rather than inject into an existing run. */
  files?: Partial<TFile>[];
  quotes?: string[];
  manualSkills?: string[];
  /** Trusted source identity committed by execution enrollment before the
  * provider-start fence opens. */
  admissionSource?: AgentContinuationAdmissionSource;
  /** Compensates a durable pre-admission claim only when the host knows
  * that no generation was admitted. Ambiguous outcomes retain the claim. */
  releaseOnDefiniteFailure?: (error?: AgentTriggerExecutionError) => MaybePromise<void>;
  /** Commits the source handoff after generation admission. Failure is
  * outcome-ambiguous: the same delivery retries with the same request id. */
  settleOnAdmission?: (result: AgentTriggerContinueResult) => MaybePromise<void>;
} | {
  status: "settled";
};
type AgentTriggerFailureCertainty = "definite" | "ambiguous";
interface AgentTriggerExecutionErrorOptions {
  mode: AgentTriggerMode;
  certainty: AgentTriggerFailureCertainty;
  retryable: boolean;
  /** Release the delivery lease without consuming its logical retry budget. */
  deferWithoutAttempt?: boolean;
  code?: string;
  status?: number;
  retryAfter?: string;
}
/**
* A typed delivery failure. `ambiguous` means the action may already be
* committed; callers may retry only the same envelope so its idempotency key
* remains unchanged.
*/
declare class AgentTriggerExecutionError extends Error {
  readonly mode: AgentTriggerMode;
  readonly certainty: AgentTriggerFailureCertainty;
  readonly retryable: boolean;
  readonly deferWithoutAttempt: boolean;
  readonly code?: string;
  readonly status?: number;
  readonly retryAfter?: string;
  constructor(message: string, options: AgentTriggerExecutionErrorOptions);
}
interface AgentTriggerFireResult {
  mode: "fire";
  status: FireStatus;
  conversationId: string;
  streamId?: string;
  generationCreatedAt?: number;
}
interface AgentTriggerSteerResult {
  mode: "steer";
  status: "queued";
  conversationId: string;
  steerId: string;
  position: number;
  preempt: boolean;
  preemptRevision?: number;
  replayed?: boolean;
  settled?: boolean;
  leftover?: boolean;
}
interface AgentTriggerContinueResult {
  mode: "continue";
  status: FireStatus;
  conversationId: string;
  streamId?: string;
  generationCreatedAt?: number;
}
type AgentTriggerExecutionResult = AgentTriggerContinueResult | AgentTriggerFireResult | AgentTriggerSteerResult;
interface AgentTriggerExecutionHostDeps {
  /** Trusted root URL for this TerraMind server. */
  getBaseUrl: (options?: {
    localOnly?: boolean;
  }) => string;
  /** Mint a short-lived token for the envelope's already-authenticated principal. */
  mintToken: (principal: AgentRunPrincipal, envelope: AgentTriggerEnvelope) => MaybePromise<string>;
  /** Optional user-timezone resolver for dynamic date variables in a new run. */
  getTimezone?: (principal: AgentRunPrincipal, envelope: AgentContinueTriggerEnvelope | AgentFireTriggerEnvelope) => MaybePromise<string | undefined>;
  /** Optional server-owned resolver for durable internal continuation inputs.
  * External/source-neutral envelopes remain unchanged when this returns undefined. */
  prepareContinue?: (envelope: AgentContinueTriggerEnvelope, context: AgentTriggerDispatchContext) => MaybePromise<AgentTriggerContinuePreparation | undefined>;
  fetch?: AgentTriggerFetch;
  /** Total bound for setup, admission, and the bounded response read. */
  timeoutMs?: number;
}
interface AgentTriggerExecutionHost {
  dispatch: (envelope: unknown, options?: {
    signal?: AbortSignal;
    attempt?: number;
    maxAttempts?: number;
  }) => Promise<AgentTriggerExecutionResult>;
}
/**
* Creates the trusted, source-neutral execution host. Schedules, webhooks,
* queues, MCP adapters, and internal events all call the same `dispatch` and
* therefore share authorization, idempotency, tenant, and failure semantics.
*/
declare function createAgentTriggerExecutionHost(deps: AgentTriggerExecutionHostDeps): AgentTriggerExecutionHost;
//#endregion
//#region src/agents/triggers/delivery.d.ts
declare const MAX_AGENT_TRIGGER_ENVELOPE_BYTES: number;
declare const AGENT_TRIGGER_COALESCE_WINDOW_MS = 750;
declare const MAX_AGENT_TRIGGER_BATCH_SIZE = 8;
declare const MAX_AGENT_TRIGGER_BATCH_BYTES: number;
interface AgentTriggerCoalesceOptions {
  /** Source-defined compatibility class. The host additionally scopes this to
  *  the authenticated binding, source, principal, and ordering lane. */
  key: string;
}
interface AgentTriggerEnqueueOptions {
  /** Trusted lane override. Matching lanes dispatch strictly in enqueue order. */
  orderingKey?: string;
  /** Delays first eligibility without changing source occurrence time. */
  availableAt?: Date;
  /** Opt-in only for observational bound-child continuations. Actionable,
  *  fenced, approval, HITL, and control deliveries must remain individual. */
  coalesce?: AgentTriggerCoalesceOptions;
  /** Server-owned capability fence. Deliveries carrying this marker remain
  * invisible to older workers during a rolling deployment. */
  requiredWorkerCapability?: string;
  /** Private liveness lease for process-owned work behind a capability fence. */
  producerLeaseUntil?: Date;
}
interface PreparedAgentTriggerDelivery {
  deliveryKey: string;
  fingerprint: string;
  orderingKey: string;
  envelope: AgentTriggerEnvelope;
  user: string;
  tenantId?: string;
  availableAt: Date;
  envelopeBytes: number;
  coalesceKey?: string;
  coalesceFrom?: Date;
  coalesceUntil?: Date;
  /** Persisted rollout marker: keep this bound actor lane queued until the
  *  admitted child turn records an authoritative terminal outcome. */
  awaitTerminalHandling?: boolean;
  requiredWorkerCapability?: string;
  producerLeaseUntil?: Date;
}
declare class AgentTriggerDeliveryError extends TypeError {
  constructor(message: string);
}
/** Validates, bounds, detaches, and fingerprints one durable source delivery. */
declare function prepareAgentTriggerDelivery(value: unknown, options?: AgentTriggerEnqueueOptions): PreparedAgentTriggerDelivery;
//#endregion
//#region src/agents/backgroundCompletionWakeup.d.ts
declare const BACKGROUND_TOOL_WAKEUP_INPUT_MAX_CHARS: number;
declare const BACKGROUND_TOOL_COMPLETION_SOURCE = "background-tool-completion";
type EnqueueBackgroundToolCompletion = (envelope: unknown, options?: AgentTriggerEnqueueOptions) => Promise<{
  deliveryKey: string;
}>;
type RetireBackgroundToolCompletion = (deliveryKey: string, sourceId: string, reason: string, options?: BackgroundToolWakeupRetireOptions) => Promise<boolean>;
type RenewBackgroundToolCompletionProducerLease = (deliveryKey: string, sourceId: string, leaseUntil: Date) => Promise<boolean>;
type WakeupMethods$1 = Pick<ConversationMethods, "getConvo"> & Pick<MessageMethods, "getMessages"> & {
  claimBackgroundToolResults(params: {
    userId: string;
    conversationId: string;
    messageId: string;
    taskId: string;
    agentId?: string;
    kind: "manual" | "wakeup";
    claimId: string;
    limit?: number;
  }): Promise<BackgroundToolResultClaim>;
  releaseBackgroundToolResultClaims(params: {
    userId: string;
    conversationId: string;
    messageId: string;
    taskIds?: string[];
    kind: "manual" | "wakeup";
    claimId: string;
  }): Promise<boolean>;
  getAgentTriggerDeliveryProducerLease(params: {
    deliveryKey: string;
    sourceId: string;
    now: Date;
  }): Promise<AgentTriggerProducerLeaseStatus>;
};
interface GenerationState$2 {
  status?: unknown;
  metadata?: {
    idempotencyClientRequestId?: unknown;
    responseMessageId?: unknown;
    terminalPersistencePending?: unknown;
  };
}
interface BackgroundToolCompletionWakeupResolverDeps {
  methods: WakeupMethods$1;
  getGenerationJob: (conversationId: string) => Promise<GenerationState$2 | null>;
}
/** Resolves a pre-registered delivery only after its result is durably
* readable. The message claim elects automatic delivery against manual polls
* and returns a bounded sibling batch for the continuation input. */
declare function createBackgroundToolCompletionWakeupResolver({
  methods,
  getGenerationJob
}: BackgroundToolCompletionWakeupResolverDeps): NonNullable<AgentTriggerExecutionHostDeps["prepareContinue"]>;
/** Pre-registers the ordered completion delivery before external tool work starts. */
declare function createBackgroundToolCompletionWakeupHandler(enqueue: EnqueueBackgroundToolCompletion, retire: RetireBackgroundToolCompletion, renewProducerLease: RenewBackgroundToolCompletionProducerLease): (registration: BackgroundToolWakeupRegistration) => Promise<BackgroundToolWakeupAdmission | false>;
/** Reopens every result owned by one dead automatic batch. Generation state
* first fences an admitted continuation that is still running/finalizing;
* delivery retirement then proves no retry remains, and releasing by the
* batch-root claim identity makes sibling recovery independent of which task
* originally admitted that delivery. */
declare function createBackgroundToolDeadClaimRecovery(retire: RetireBackgroundToolCompletion, releaseClaims: WakeupMethods$1["releaseBackgroundToolResultClaims"], getGenerationJob: (conversationId: string) => Promise<GenerationState$2 | null | undefined>, fenceGenerationClaim: (input: {
  userId: string;
  conversationId: string;
  claimId: string;
}) => Promise<"fenced" | "started" | "unavailable">): BackgroundToolDeadClaimRecovery;
//#endregion
//#region src/agents/legacy.d.ts
/**
* Converts OCR tool resource to context tool resource in place.
* This modifies the input object directly (used for updateData in the handler).
*
* @param data - Object containing tool_resources and/or tools to convert
* @returns void - modifies the input object directly
*/
declare function convertOcrToContextInPlace(data: {
  tool_resources?: AgentToolResources;
  tools?: string[];
}): void;
/**
* Merges tool resources from existing agent with incoming update data,
* converting OCR to context and handling deduplication.
* Used when existing agent has OCR that needs to be converted and merged with updateData.
*
* @param existingAgent - The existing agent data
* @param updateData - The incoming update data
* @returns Object with merged tool_resources and tools
*/
declare function mergeAgentOcrConversion(existingAgent: {
  tool_resources?: AgentToolResources;
  tools?: string[];
}, updateData: {
  tool_resources?: AgentToolResources;
  tools?: string[];
}): {
  tool_resources?: AgentToolResources;
  tools?: string[];
};
//#endregion
//#region src/agents/lazySubagents.d.ts
type VersionedAgent = Pick<Agent, "id" | "name" | "description" | "instructions" | "additional_instructions" | "endpoint" | "provider" | "model" | "model_parameters" | "tools" | "tool_kwargs" | "tool_options" | "tool_resources" | "skills" | "skills_enabled" | "skill_authoring_enabled" | "skills_scope" | "stateful_code_sessions" | "stateful_code_environment" | "code_environment_id" | "git_identity" | "artifacts" | "recursion_limit" | "agent_ids" | "edges" | "end_after_tools" | "hide_sequential_outputs" | "subagents" | "memory_scope"> & {
  version?: number;
  actions?: string[];
  mcpServerNames?: string[];
};
/**
* Returns only persisted fields that can change the initialized child graph.
* `name` and `description` are intentionally included because they are
* advertised to the parent model; request state, ACL-only display fields, and
* secret values are excluded.
*/
declare function selectLazySubagentConfig(agent: VersionedAgent): Omit<VersionedAgent, "version">;
/** Deterministic descriptor identity for a persisted lazy subagent. */
declare function getLazySubagentConfigId(agent: VersionedAgent): string;
//#endregion
//#region src/agents/lazyHistory.d.ts
type LazyHistoryAgent = Pick<Agent, "id" | "skills" | "skills_enabled" | "tools" | "tool_options">;
interface LazyAgentHistoryCapabilities {
  deferredToolsAvailable: boolean;
  programmaticToolsAvailable: boolean;
  backgroundToolsAvailable: boolean;
}
interface CreateLazyAgentHistoryResolverParams extends LazyAgentHistoryCapabilities {
  accessibleSkillIds: Types.ObjectId[];
  editableSkillIds: Types.ObjectId[];
  skillsCapabilityEnabled: boolean;
  ephemeralSkillsToggle: boolean;
  userId?: string;
  userRole?: string;
  skillStates?: Record<string, boolean>;
  defaultActiveOnShare?: boolean;
  maxCatalogSkills?: number;
  listSkillsByAccess?: TListSkillsByAccess;
  listAlwaysApplySkills?: ResolveAlwaysApplySkillsParams["listAlwaysApplySkills"];
  getAccessibleMcpServerNames?: (userId?: string, role?: string) => Promise<string[]>;
  configuredMcpServerNames?: readonly string[];
  canAuthorSkillFiles?: (params: {
    agent: LazyHistoryAgent;
    scopedEditableSkillIds: Types.ObjectId[];
  }) => boolean;
}
interface LazyAgentHistoryMetadata {
  alwaysApplySkillPrimes: ResolvedAlwaysApplySkill[];
  historicalToolNames: string[];
  historicalMcpServerNames: string[];
  skillAuthoringAvailable: boolean;
}
interface LazyAgentHistoryResolver {
  resolve(params: {
    agent: LazyHistoryAgent;
    codeExecutionAvailable: boolean;
    memoryAvailable: boolean;
  }): Promise<LazyAgentHistoryMetadata>;
}
/**
* Creates one request-scoped lazy-history policy resolver.
*
* The resolver owns Skill activation/catalog policy, per-scope query caching,
* MCP name auditing, and capability-to-tool expansion so the legacy host only
* wires request dependencies and consumes typed metadata.
*/
declare function createLazyAgentHistoryResolver(params: CreateLazyAgentHistoryResolverParams): LazyAgentHistoryResolver;
//#endregion
//#region src/agents/memory.d.ts
type RequiredMemoryMethods = Pick<MemoryMethods, "setMemory" | "deleteMemory" | "getFormattedMemories" | "getUserMemories">;
interface MemoryConfig {
  validKeys?: string[];
  instructions?: string;
  llmConfig?: Partial<LLMConfig>;
  tokenLimit?: number;
}
declare const memoryInstructions = "The system automatically stores important user information and can update or delete memories based on user requests, enabling dynamic memory management.";
declare const SET_MEMORY_TOOL_NAME = "set_memory";
declare const DELETE_MEMORY_TOOL_NAME = "delete_memory";
/**
* Creates a memory tool instance with user context
*/
declare const createMemoryTool: ({
  userId,
  agentId,
  setMemory,
  validKeys,
  charLimit,
  tokenLimit,
  totalTokens,
  tokenCountsByKey,
  filters,
  onWrite
}: {
  userId: string | ObjectId; /** Agent partition to write to; omit for the shared personal pool */
  agentId?: string;
  setMemory: MemoryMethods["setMemory"];
  validKeys?: string[];
  charLimit?: number;
  tokenLimit?: number;
  totalTokens?: number;
  tokenCountsByKey?: ReadonlyMap<string, number>;
  filters?: FiltersConfig;
  onWrite?: () => void;
}) => DynamicStructuredTool;
/**
* Creates a delete memory tool instance with user context
*/
declare const createDeleteMemoryTool: ({
  userId,
  agentId,
  deleteMemory,
  validKeys,
  onWrite
}: {
  userId: string | ObjectId; /** Agent partition to delete from; omit for the shared personal pool */
  agentId?: string;
  deleteMemory: MemoryMethods["deleteMemory"];
  validKeys?: string[];
  onWrite?: () => void;
}) => DynamicStructuredTool;
/**
* Strict usage guard appended to the agent's instructions when the inline
* memory tools are registered, preserving the memory-agent's explicit-request
* behavior so the model never stores facts it merely observed.
*/
declare const memoryToolUsageGuard = "Only use the `set_memory` and `delete_memory` tools when the user explicitly asks you to remember, update, or forget something (e.g. \"remember that...\", \"don't forget...\", \"forget...\"). Never store information merely because the user mentioned it in conversation.";
/**
* LLM-facing definitions for the inline memory tool pair, used by the
* event-driven (definitions-only) loader. The `memory` capability string on
* an agent's `tools` array expands into this pair at initialize time via
* {@link registerMemoryTools}; the runtime instances created in the tool
* service enforce `validKeys`/`tokenLimit` and emit memory artifacts.
* `validKeys` is surfaced in the key descriptions so the model is told the
* allowed keys up front, matching the runtime `createMemoryTool` schema.
*/
declare function getMemoryToolDefinitions(validKeys?: string[]): LCTool$1[];
/**
* Idempotently registers the inline memory tool pair (`set_memory` +
* `delete_memory`) into the run's tool registry and tool-definition list.
* Mirrors `registerCodeExecutionTools`: the `memory` capability string stays
* as the `agent.tools` trigger marker and expands into this pair here so the
* definitions-only loader surfaces both tools to the LLM.
*/
declare function registerMemoryTools({
  toolRegistry,
  toolDefinitions,
  validKeys
}: {
  toolRegistry?: LCToolRegistry$1;
  toolDefinitions?: LCTool$1[];
  validKeys?: string[];
}): {
  toolDefinitions: LCTool$1[];
  registered: string[];
  toolNames: string[];
};
type GetRoleByName = (roleName: string, fieldsToSelect?: string | string[]) => Promise<IRole | null>;
type InlineMemoryAgent = {
  id?: string;
  tools?: unknown[];
  memoryToolsRegistered?: boolean;
  memory_scope?: MemoryScope;
} | null | undefined;
/**
* Resolves the memory partition an agent reads/writes: its persisted id when
* the agent opted into isolated memory (`memory_scope: 'agent'`), otherwise
* `undefined` for the shared personal pool. Runtime ids carry a `____N`
* suffix in added-conversation paths, so the suffix is stripped to keep the
* partition stable across single- and multi-agent runs. Ephemeral agents
* never carry `memory_scope`, so they always resolve to the shared pool.
*/
declare function getMemoryAgentId(agent: InlineMemoryAgent): string | undefined;
/**
* Whether an agent carries the inline memory tools. Prefers the TerraMind-only
* `memoryToolsRegistered` flag set by `initializeAgent`, falling back to the raw
* `memory` capability marker on `tools`. This works for both the raw agent and
* the initialized config that may be held in the tool-execution context, and
* never matches an MCP tool that merely shares the `set_memory`/`delete_memory`
* name (that name only collides at the tool level, not the capability marker).
*/
declare function agentHasInlineMemoryTools(agent: InlineMemoryAgent): boolean;
/** Builds the existing-memory system context for an inline-memory agent. */
declare function buildInlineMemoryContext({
  agent,
  req,
  userId,
  memoryAvailable,
  getFormattedMemories
}: {
  agent: InlineMemoryAgent;
  req: ServerRequest;
  userId: string | ObjectId;
  memoryAvailable: boolean;
  getFormattedMemories: MemoryMethods["getFormattedMemories"];
}): Promise<string>;
declare function getRequestMemories({
  req,
  userId,
  agentId,
  getFormattedMemories
}: {
  req: object;
  userId: string | ObjectId; /** Agent partition; omit for the shared personal pool */
  agentId?: string;
  getFormattedMemories: MemoryMethods["getFormattedMemories"];
}): Promise<FormattedMemoriesResult>;
/**
* Drops the cached memories for a request partition so the next
* {@link getRequestMemories} re-fetches. Inline `set_memory`/`delete_memory`
* writes call this on success so a later tool round in the same response is
* seeded with the post-write usage total instead of a stale pre-write one.
*/
declare function invalidateRequestMemories(req: object, agentId?: string): void;
/**
* Re-checks the run-level memory gate at tool-execution time: the agents
* `memory` capability is enabled, memory is configured, the user hasn't opted
* out, and the user holds the required (write) permissions. The event-driven
* executor loads tools by requested name, so this must be re-verified rather
* than trusted from registration time.
*/
declare function isMemoryToolAllowed({
  req,
  writePermissions,
  getRoleByName
}: {
  req: ServerRequest;
  writePermissions?: Permissions[];
  getRoleByName: GetRoleByName;
}): Promise<boolean>;
/**
* Builds an inline memory tool instance for the event-driven executor, applying
* the full opt-in + permission + config gate. Returns `null` when the call is
* not permitted (e.g. a hallucinated/undeclared call, missing write permission,
* or a disabled capability), so the executor drops the tool.
*/
declare function buildInlineMemoryTool({
  toolName,
  req,
  agent,
  userId,
  memoryMethods,
  getRoleByName
}: {
  toolName: string;
  req: ServerRequest;
  agent: InlineMemoryAgent;
  userId: string | ObjectId;
  memoryMethods: Pick<MemoryMethods, "setMemory" | "deleteMemory" | "getFormattedMemories">;
  getRoleByName: GetRoleByName;
}): Promise<DynamicStructuredTool | null>;
declare class BasicToolEndHandler implements EventHandler$1 {
  private callback?;
  constructor(callback?: ToolEndCallback$1);
  handle(event: string, data: StreamEventData | undefined, metadata?: Record<string, unknown>): void;
}
declare function processMemory({
  res,
  userId,
  agentId,
  setMemory,
  deleteMemory,
  messages,
  inspectionMessages,
  memory,
  memoryEntries,
  messageId,
  conversationId,
  validKeys,
  instructions,
  llmConfig,
  tokenLimit,
  totalTokens,
  tokenCountsByKey,
  filters,
  streamId,
  jobCreatedAt,
  user,
  tenantId
}: {
  res: Response$2;
  setMemory: MemoryMethods["setMemory"];
  deleteMemory: MemoryMethods["deleteMemory"];
  userId: string | ObjectId; /** Agent partition; omit for the shared personal pool */
  agentId?: string;
  memory: string;
  messageId: string;
  conversationId: string;
  messages: BaseMessage[];
  inspectionMessages?: BaseMessage[];
  validKeys?: string[];
  instructions: string; /** Canonical rows preserve key/value granularity for field-scoped policy. */
  memoryEntries?: readonly {
    key?: string;
    value?: string;
    summary?: string;
  }[];
  tokenLimit?: number;
  totalTokens?: number;
  tokenCountsByKey?: ReadonlyMap<string, number>;
  filters?: FiltersConfig;
  llmConfig?: Partial<LLMConfig>;
  streamId?: string | null;
  jobCreatedAt?: number;
  user?: IUser;
  tenantId?: string;
}): Promise<(TAttachment | null)[] | undefined>;
declare function createMemoryProcessor({
  res,
  userId,
  agentId,
  messageId,
  memoryMethods,
  conversationId,
  config,
  filters,
  streamId,
  jobCreatedAt,
  user,
  tenantId
}: {
  res: Response$2;
  messageId: string;
  conversationId: string;
  userId: string | ObjectId; /** Agent partition; omit for the shared personal pool */
  agentId?: string;
  memoryMethods: RequiredMemoryMethods;
  config?: MemoryConfig;
  filters?: FiltersConfig;
  streamId?: string | null;
  jobCreatedAt?: number;
  user?: IUser;
  tenantId?: string;
}): Promise<[string, (messages: BaseMessage[], inspectionMessages?: BaseMessage[]) => Promise<(TAttachment | null)[] | undefined>]>;
/**
* Creates a memory callback for handling memory artifacts
* @param params - The parameters object
* @param params.res - The server response object
* @param params.artifactPromises - Array to collect artifact promises
* @param params.streamId - The stream ID for resumable mode, or null for standard mode
* @param params.jobCreatedAt - The generation epoch that owns emitted artifacts
* @returns The memory callback function
*/
declare function createMemoryCallback({
  res,
  artifactPromises,
  streamId,
  jobCreatedAt
}: {
  res: Response$2;
  artifactPromises: Promise<Partial<TAttachment> | null>[];
  streamId?: string | null;
  jobCreatedAt?: number;
}): ToolEndCallback$1;
//#endregion
//#region src/agents/failures/tracker.d.ts
interface ModelErrorTrackerCallback {
  readonly name: "librechat-upstream-model-error-tracker";
  readonly awaitHandlers: true;
  readonly handleLLMError: (error: unknown) => void;
}
interface ModelErrorTracker {
  readonly callback: ModelErrorTrackerCallback;
  readonly getUpstreamModelError: (error: unknown) => object | null;
}
/**
* Tracks errors observed at the chat-model callback boundary. Hosts consult
* the tracker only after the complete graph rejects, so retries and fallbacks
* that recover never classify an otherwise successful run as upstream.
*/
declare function createModelErrorTracker(): ModelErrorTracker;
//#endregion
//#region src/agents/failures/terminal.d.ts
declare const UPSTREAM_MODEL_ERROR_CODE = "UPSTREAM_MODEL_ERROR";
declare const UPSTREAM_MODEL_ERROR_ORIGIN = "model_provider";
interface UpstreamModelErrorMetadata extends SafeErrorMetadata {
  readonly errorCode: typeof UPSTREAM_MODEL_ERROR_CODE;
  readonly errorOrigin: typeof UPSTREAM_MODEL_ERROR_ORIGIN;
  readonly errorType: string;
  readonly traceId?: string;
}
interface TerminalRunErrorLogger {
  error(message: string, metadata: SafeErrorMetadata | UpstreamModelErrorMetadata): void;
}
interface TerminalRunErrorObserver {
  readonly modelCallback: ModelErrorTrackerCallback;
  readonly log: (error: unknown, signal?: AbortSignal) => void;
  readonly getUserFacingError: (error: unknown, fallback: () => string) => string;
}
/** A run cancellation requires host-owned abort state plus its own reason or an abort shape. */
declare function isAgentRunCancellation(error: unknown, signal?: AbortSignal): boolean;
declare function getUpstreamModelErrorMetadata(error: unknown, responseMessageId?: string): UpstreamModelErrorMetadata;
/** Applies one terminal-run log taxonomy to every agent ingress. */
declare function createTerminalRunErrorObserver({
  logger,
  responseMessageId,
  source,
  genericMessage
}: {
  logger: TerminalRunErrorLogger;
  responseMessageId?: string;
  source: string;
  genericMessage?: string;
}): TerminalRunErrorObserver;
//#endregion
//#region src/agents/reads.d.ts
type AgentManagementRecord$1 = AgentManagementProjectionSource & {
  _id: Types.ObjectId;
};
interface AgentManagementReadDeps {
  getRoleByName: (roleName: string, fieldsToSelect?: string | string[]) => Promise<IRole | null>;
  getAgentWithVersionCount: (search: {
    id: string;
    tenantId: string;
  }) => Promise<AgentManagementRecord$1 | null>;
  getAgentManagementListByAccess: (params: {
    accessibleIds: Types.ObjectId[] | null;
    tenantId: string;
    limit: number;
    after?: string | null;
  }) => Promise<{
    data: AgentManagementRecord$1[];
    has_more: boolean;
    after: string | null;
  }>;
  findAccessibleResources: (params: {
    userId: string;
    role?: string;
    idOnTheSource?: string;
    resourceType: ResourceType;
    requiredPermissions: PermissionBits;
  }) => Promise<Types.ObjectId[]>;
  checkPermission: (params: {
    userId: string;
    role?: string;
    resourceType: ResourceType;
    resourceId: Types.ObjectId;
    requiredPermission: PermissionBits;
  }) => Promise<boolean>;
  hasCapability: (user: IUser, capability: SystemCapability) => Promise<boolean>;
}
/** Typed management read handlers; the Express route supplies concrete database and ACL dependencies. */
declare function createAgentManagementReadHandlers(deps: AgentManagementReadDeps): {
  list: (req: Request, res: Response$2) => Promise<Response$2>;
  get: (req: Request, res: Response$2) => Promise<Response$2>;
};
//#endregion
//#region src/agents/updates.d.ts
type AgentUpdateHandler = (req: Request, res: Response$2) => Promise<Response$2 | void> | Response$2 | void;
type AgentManagementRecord = AgentManagementProjectionSource & {
  _id: Types.ObjectId;
};
interface AgentManagementUpdateDeps {
  getRoleByName: (roleName: string, fieldsToSelect?: string | string[]) => Promise<IRole | null>;
  getAgentWithVersionCount: (search: {
    id: string;
    tenantId: string;
  }) => Promise<AgentManagementRecord | null>;
  checkPermission: (params: {
    userId: string;
    role?: string;
    resourceType: ResourceType;
    resourceId: Types.ObjectId;
    requiredPermission: PermissionBits;
  }) => Promise<boolean>;
  hasCapability: (user: IUser, capability: SystemCapability) => Promise<boolean>;
  updateAgent: AgentUpdateHandler;
}
/** Validate and authorize Agent Management updates before reusing the browser update flow. */
declare function createAgentManagementUpdateHandler(deps: AgentManagementUpdateDeps): (req: Request, res: Response$2) => Promise<Response$2>;
//#endregion
//#region src/agents/traversal.d.ts
interface ReachableAgent<TAgent> {
  readonly subagentAgentConfigs?: readonly (TAgent | null | undefined)[];
  readonly lazySubagentConfigs?: readonly (TAgent | null | undefined)[];
  readonly subagentGraphMemberMetadata?: readonly (TAgent | null | undefined)[];
  readonly subagentGraphConfigs?: readonly {
    readonly memberConfigs?: readonly (TAgent | null | undefined)[];
  }[];
}
/**
* Collects each agent object reachable from the supplied roots exactly once.
* Object identity preserves distinct initialized snapshots while preventing
* shared subagent nodes and cyclic graphs from repeating work.
*/
declare function collectReachableAgents<T extends ReachableAgent<T>>(roots: readonly (T | null | undefined)[]): T[];
//#endregion
//#region src/agents/mcpIdentity.d.ts
interface McpToolDefinitionLike {
  name?: string;
  serverName?: string;
}
interface McpRegisteredToolLike {
  mcpRawServerName?: string;
}
interface McpIdentityAgent extends ReachableAgent<McpIdentityAgent> {
  accessibleMcpServerNames?: readonly string[];
  historicalMcpServerNames?: readonly string[];
  toolDefinitions?: readonly McpToolDefinitionLike[];
  toolRegistry?: Iterable<readonly [string, McpRegisteredToolLike]>;
}
interface PersistedMcpToolCall {
  name?: string;
  mcpServerName?: string;
  subagent_content?: PersistedMcpContentPart[];
}
interface PersistedMcpContentPart {
  tool_call?: PersistedMcpToolCall;
}
interface StampMcpServerIdentitiesParams {
  contentParts?: PersistedMcpContentPart[];
  roots: readonly (McpIdentityAgent | null | undefined)[];
}
/**
* Stamps durable MCP server identities onto persisted native tool calls.
*
* Exact execution metadata wins. Tool registries and definitions provide a
* server-owned fallback, followed by boundary-aware parsing for legacy calls.
*/
declare function stampMcpServerIdentities({
  contentParts,
  roots
}: StampMcpServerIdentitiesParams): void;
//#endregion
//#region src/agents/orphans.d.ts
/**
* Every `EToolResources` member that can carry `file_ids` on an agent document.
* `code_interpreter` is intentionally omitted — it's part of `EToolResources`
* for the Assistants API but not a key of the agent-side `AgentToolResources`
* shape, so including it would be a type lie and generate dead MongoDB clauses.
*/
declare const TOOL_RESOURCE_KEYS: ReadonlyArray<keyof AgentToolResources>;
/**
* Collects every file_id referenced across all tool_resource categories.
* Duplicates are de-duplicated across categories.
*/
declare function collectToolResourceFileIds(tool_resources: AgentToolResources | undefined | null): string[];
/**
* Converts client-hydrated file objects into identifiers and removes the
* untrusted object payloads. Runtime file records must always be reloaded from
* canonical storage after the normal ownership checks.
*/
declare function normalizeToolResourceFiles(tool_resources: AgentToolResources | undefined | null): AgentToolResources | undefined | null;
/**
* Removes the given file_ids from every tool_resource category on the provided
* tool_resources object. Mutates in place and also returns the same reference
* for convenience. Returns the count of removed references.
*/
declare function stripFileIdsFromToolResources(tool_resources: AgentToolResources | undefined | null, idsToRemove: Iterable<string>): {
  tool_resources: AgentToolResources | undefined | null;
  removedCount: number;
};
//#endregion
//#region src/agents/migration.d.ts
interface MigrationCheckDbMethods {
  findRoleByIdentifier: AccessRoleMethods["findRoleByIdentifier"];
}
interface MigrationCheckParams {
  mongoose: Mongoose;
  methods: MigrationCheckDbMethods;
  AgentModel: Model<IAgent>;
}
interface MigrationCheckResult {
  totalToMigrate: number;
  globalEditAccess: number;
  globalViewAccess: number;
  privateAgents: number;
  details?: {
    globalEditAccess: Array<{
      name: string;
      id: string;
    }>;
    globalViewAccess: Array<{
      name: string;
      id: string;
    }>;
    privateAgents: Array<{
      name: string;
      id: string;
    }>;
  };
}
/**
* Check if agents need to be migrated to the new permission system
* This performs a dry-run check similar to the migration script
*/
declare function checkAgentPermissionsMigration({
  methods,
  mongoose,
  AgentModel
}: MigrationCheckParams): Promise<MigrationCheckResult>;
/**
* Log migration warning to console if agents need migration
*/
declare function logAgentMigrationWarning(result: MigrationCheckResult): void;
//#endregion
//#region src/agents/parameters.d.ts
/**
* Coerces known numeric model parameter keys to finite numbers, dropping values
* that cannot be represented as one (e.g. a stray placeholder string captured by
* the UI). Explicit `0` and negative values are preserved; other keys pass through.
*/
declare function sanitizeModelParameters(params: Record<string, unknown>): Record<string, unknown>;
//#endregion
//#region src/agents/plan.d.ts
type AgentTurnOrigin = "user" | "subagent" | "completion" | "schedule" | "event" | "resume";
type AgentTurnContinuationStrategy = "checkpoint" | "history" | "fresh";
interface AgentTurnBinding {
  bindingId: string;
  parentConversationId: string;
}
interface AgentTurnExecutionPlan {
  origin: AgentTurnOrigin;
  strategy: AgentTurnContinuationStrategy;
  conversationId: string;
  parentMessageId?: string;
  canPause: boolean;
  expectedAction?: AgentTriggerExpectedAction;
  binding?: AgentTurnBinding;
}
interface ResolveAgentTurnExecutionPlanInput {
  conversationId: string;
  parentMessageId?: string;
  isNewConversation: boolean;
  isResume?: boolean;
  isSchedule?: boolean;
  isSubagent?: boolean;
  isEvent?: boolean;
  event?: {
    type: string;
    binding?: AgentTurnBinding;
    expectedAction?: AgentTriggerExpectedAction;
  };
  canPause: boolean;
  /** Immutable request capability used only during mixed-version drain. This
  * is negotiated automatically and is never an operator-selected runtime. */
  durableEventActorSuspensions: boolean;
  checkpointerType?: TCheckpointerType;
}
/** Compiles already-loaded turn facts into one immutable state-loading decision. */
declare function resolveAgentTurnExecutionPlan(input: ResolveAgentTurnExecutionPlanInput): AgentTurnExecutionPlan;
//#endregion
//#region src/agents/prewarm.d.ts
type PrewarmAgent = {
  id: string;
  statefulCodeSessions?: boolean;
  statefulCodeEnvironment?: StatefulCodeEnvironment;
  codeExecutionContext?: CodeExecutionContext;
  subagentAgentConfigs?: PrewarmAgent[];
  lazySubagentConfigs?: PrewarmAgent[];
};
type PrewarmAgents = Array<PrewarmAgent | null | undefined>;
/**
* Record that the conversation's sandbox answered a request (prewarm or a
* real execute_code/bash call), refreshing the warm window and releasing
* any in-flight prewarm marker. Callers on hot paths should not await this;
* `void markSandboxReady(...)` is the expected usage.
*/
declare function markSandboxReady(conversationId: string, executionRouteKey?: string): Promise<void>;
/**
* Whether the UI should be told the sandbox is cold-booting for this
* conversation's code tool call: a prewarm is in flight and no completion
* (prewarm or real exec) has landed. Deployments that never prewarm —
* stateless setups or the `CODE_SANDBOX_PREWARM=false` kill switch — never
* have an in-flight marker and never signal, preserving existing behavior.
*/
declare function shouldSignalSandboxStart(conversationId?: string | null): Promise<boolean>;
/**
* Fire-and-forget boot of each selected stateful code environment so it
* comes up in parallel with model generation instead of on the first
* execute_code/bash call (~4s cold, worse on heavy first imports). No-op
* unless a reachable agent resolved `statefulCodeSessions` and neither a
* warm marker nor an in-flight prewarm exists. The existence check and
* marker write are not atomic, so concurrent turns (or replicas) can rarely
* double-fire — harmless, since the prewarm exec is a trivial idempotent
* `true` and the Code API serializes per-session work behind its own lock.
* Failures are logged at debug level and never affect the chat request; the
* in-flight marker's TTL then acts as the retry backoff.
*/
declare function maybePrewarmCodeSandbox(params: {
  req: ServerRequest;
  conversationId?: string | null;
  agents: PrewarmAgents;
}): void;
/** Test-only: clear tracked sandbox state between specs. */
declare function resetSandboxStateForTests(): Promise<void>;
//#endregion
//#region src/agents/ptc.d.ts
/**
* Collapses an inner call's input into a single `key=value, key=value` line
* for the CLI-style trace. Values are clipped individually and iteration stops
* as soon as the joined preview can no longer grow, so a call with a large
* body or many keys costs the same as a small one.
*/
declare function summarizePtcArgs(input: unknown): string;
interface InstrumentPtcToolMapParams {
  /** The tool map the PTC runner resolves inner calls against. */
  toolMap: Map<string, StructuredToolInterface>;
  /** The PTC run step's tool call id — the card the trace renders under. */
  toolCallId: string;
  runId?: string;
  /**
  * Whether argument and failure previews may ride the stream. False when the
  * deployment filters tool arguments for PII: inner calls never pass through
  * `filteredToolArgumentsResult` (the sandbox bridge invokes them directly),
  * so a preview would put values on the wire that the configured policy
  * exists to keep off it — and a failure message routinely quotes the very
  * argument that caused it. The trace still reports name, status and duration.
  */
  includePreviews?: boolean;
  /**
  * Inner tools whose *name* trips the deployment's PII policy. The event
  * carries the tool name unconditionally, so a name the `name` filter would
  * have blocked on a direct call cannot be allowed to ride the trace instead.
  * These tools still execute — they are simply left unwrapped, so no event
  * about them is ever emitted.
  */
  traceExclusions?: ReadonlySet<string>;
  emit: (event: PtcToolCallEvent) => void;
}
/**
* Wraps every tool the PTC sandbox can reach so each inner invocation reports
* its lifecycle on the live stream. The runner (`executeTools` in
* `@librechat/agents`) resolves a tool by name and calls `invoke` on it, so a
* `Proxy` intercepting only `invoke` is enough — `name`, `schema`, `mcp` and
* every other property the runner reads pass straight through to the real
* tool, and nothing about execution changes.
*/
declare function instrumentPtcToolMap({
  toolMap,
  toolCallId,
  runId,
  includePreviews,
  traceExclusions,
  emit
}: InstrumentPtcToolMapParams): Map<string, StructuredToolInterface>;
//#endregion
//#region src/agents/transactions.d.ts
type TokenType = "prompt" | "completion";
interface GetMultiplierParams {
  valueKey?: string;
  tokenType?: TokenType;
  model?: string;
  endpointTokenConfig?: EndpointTokenConfig;
  inputTokenCount?: number;
}
interface GetCacheMultiplierParams {
  cacheType: "write" | "read";
  model?: string;
  endpointTokenConfig?: EndpointTokenConfig;
  inputTokenCount?: number;
}
interface PricingFns {
  getMultiplier: (params: GetMultiplierParams) => number;
  getCacheMultiplier: (params: GetCacheMultiplierParams) => number | null;
}
interface PreparedEntry {
  doc: TransactionData;
  tokenValue: number;
  balance?: Partial<TCustomConfig["balance"]> | null;
}
interface TokenUsage {
  promptTokens?: number;
  completionTokens?: number;
}
interface StructuredPromptTokens {
  input?: number;
  write?: number;
  read?: number;
}
interface StructuredTokenUsage {
  promptTokens?: StructuredPromptTokens;
  completionTokens?: number;
}
interface TxMetadata {
  user: string;
  model?: string;
  context: string;
  messageId?: string;
  conversationId: string;
  balance?: Partial<TCustomConfig["balance"]> | null;
  transactions?: Partial<TTransactionsConfig>;
  endpointTokenConfig?: EndpointTokenConfig;
}
interface BulkWriteDeps {
  insertMany: (docs: TransactionData[]) => Promise<unknown>;
  updateBalance: (params: {
    user: string;
    incrementValue: number;
  }) => Promise<unknown>;
}
declare function prepareTokenSpend(txData: TxMetadata, tokenUsage: TokenUsage, pricing: PricingFns): PreparedEntry[];
declare function prepareStructuredTokenSpend(txData: TxMetadata, tokenUsage: StructuredTokenUsage, pricing: PricingFns): PreparedEntry[];
declare function bulkWriteTransactions({
  user,
  docs
}: {
  user: string;
  docs: PreparedEntry[];
}, dbOps: BulkWriteDeps): Promise<void>;
//#endregion
//#region src/agents/usage.d.ts
type SpendTokensFn = (txData: TxMetadata, tokenUsage: TokenUsage) => Promise<unknown>;
type SpendStructuredTokensFn = (txData: TxMetadata, tokenUsage: StructuredTokenUsage) => Promise<unknown>;
interface CollectedUsageTotals {
  inputTokens: number;
  outputTokens: number;
  totalTokens: number;
  cacheReadTokens: number;
  reasoningTokens: number;
}
interface CollectedUsageBreakdown {
  total: CollectedUsageTotals;
  primary: CollectedUsageTotals;
  subagent: CollectedUsageTotals;
}
/**
* Normalizes every billed model call before folding it into API response totals.
* The same provider-aware split used by billing keeps additive cache tokens and
* repaired provider output counts consistent without coupling billing to one
* external wire format.
*/
declare function aggregateCollectedUsage(collectedUsage: ReadonlyArray<UsageMetadata | null | undefined>): CollectedUsageBreakdown;
interface RecordUsageDeps {
  spendTokens: SpendTokensFn;
  spendStructuredTokens: SpendStructuredTokensFn;
  pricing?: PricingFns;
  bulkWriteOps?: BulkWriteDeps;
  isPrincipalActive?: (userId: string) => Promise<boolean>;
}
/**
* Authoritative USD cost of one model call. Reuses the exact billing
* functions (`prepareTokenSpend`/`prepareStructuredTokenSpend` → `getMultiplier`
* with `inputTokenCount` for premium tiers) so the figure emitted to the
* client matches what is charged against balance — the client must not
* re-derive pricing from base rates. `tokenValue` is credits (USD × 1e6).
*/
declare function computeUsageCostUSD(usage: UsageMetadata, pricing: PricingFns, endpointTokenConfig?: EndpointTokenConfig): number;
/**
* Aggregates the per-model-call `on_token_usage` payloads emitted for one
* response into a single rollup, persisted on `responseMessage.metadata.usage`.
*
* Each event is normalized into display units with the SAME logic the live
* client uses (`splitUsage`: input excludes cache, output is repaired) BEFORE
* summing, so the rollup reproduces the live branch/total usage exactly even
* when a turn mixes providers (e.g. a summarization or subagent call on a
* different provider than the primary). `cost` is the additive sum of the
* authoritative per-event cost, included only when at least one event carried
* it (i.e. `interface.contextCost` was on).
*/
declare function aggregateEmittedUsage(events: ReadonlyArray<TTokenUsageEvent>): TResponseUsage | null;
/**
* The counted tool results a save path attaches to its snapshot, or `undefined`
* when there are none to attach.
*
* Only a turn that stopped at the tool-call limit retains any: it keeps the
* results of the tools its final call requested, and the snapshot describing that
* call precedes them with no further call to produce a new one. Every other
* ending leaves nothing behind — a turn that finishes normally ends on model
* text, and a turn whose tools ran gets another call, hence another snapshot.
* A result that cannot be counted exactly withdraws the whole figure rather than
* contributing a guess (see `countRetainedToolTokens`).
*
* `countExact` is the run's own exact counter. It defaults to the shared
* tokenizer for the given encoding — the same one the SDK counted the snapshot
* with, which is the point — and is a parameter so a caller (or a test) can
* supply its own without reaching into module state. `maxCountChars` is the
* deployment's ceiling on that work (`endpoints.agents.maxRetainedToolCountChars`).
*/
declare function resolveRetainedToolTokens({
  stoppedAtToolLimit,
  contentParts,
  priorToolCallIds,
  encoding,
  maxCountChars,
  countExact
}: {
  stoppedAtToolLimit: boolean;
  contentParts: ReadonlyArray<unknown> | null | undefined;
  priorToolCallIds: ReadonlySet<string> | null | undefined;
  encoding: EncodingName;
  maxCountChars?: number;
  countExact?: (text: string) => number | undefined;
}): number | undefined;
/**
* Projects the latest live context snapshot into the blob persisted on
* `responseMessage.metadata.contextUsage`. Reconciles the calibrated estimate to
* the final call's ACTUAL prompt tokens (the SDK multiplier over-inflates
* `messageTokens`, badly so when a provider injects server-side content like web
* search), so a reloaded turn shows the real context — not a several×-too-high
* number. Sanitizes malformed optional token fields, bounds each result-message
* share by the parent total, and records the final call's output as
* `completedOutputTokens` so rehydration adds the same post-snapshot delta the
* live gauge did. The client re-anchors the blob to the response message id on
* load.
*
* `retainedToolTokens` is the second post-snapshot delta: the counted tool
* results a turn that stopped at the tool-call limit keeps beyond its last
* snapshot (see `countRetainedToolTokens`). It stays a separate field rather than
* being folded into `breakdown.messageTokens`, which is provider-reconciled — a
* locally counted figure added there would silently become part of the exact
* accounting. Zero and malformed values are dropped, so a normal turn carries
* nothing new.
*/
declare function buildPersistedContextUsage(snapshot: TContextUsageEvent, usageEvents?: ReadonlyArray<TTokenUsageEvent>, options?: {
  retainedToolTokens?: number;
}): TContextUsageEvent;
/**
* Sum of this response's output tokens already folded into a later snapshot's
* pre-invoke baseline that the response message's `tokenCount` ALSO carries — the
* overlap `computeSummaryUsedTokens` subtracts from the marker so the live-path
* client estimate (`summaryBaseline + responseTokenCount`) doesn't double-count:
*  - earlier tool-loop PRIMARY calls: a multi-call turn's first output sits in the
*    kept-message context of the next call's snapshot AND in `tokenCount`.
*  - the SUMMARIZATION call's generated summary: it sits in the snapshot baseline
*    as `summaryTokens` AND in `tokenCount` (`recordCollectedUsage` folds
*    summarization completion into the reported output total; subagent/sequential
*    are kept out of that total, so they are excluded here too).
*
* Both are matched by `runId` and bounded by `beforeIndex` to the calls that
* preceded the snapshot. The summarize detour inherits the graph run id
* (`traceConfig` spreads `config.metadata.run_id`), so it shares the snapshot's
* `runId`; a parallel sibling run's summary carries a DIFFERENT `runId` and must
* NOT be subtracted (its summary lives in the sibling's baseline, not this one).
* Untagged events (older lib / resume) match any run for back-compat.
*
* Only the live path (which builds `tokenCount` via `recordCollectedUsage`) calls
* this; the abort path subtracts nothing — see {@link buildAbortedResponseMetadata}.
*/
declare function priorRunOutputTokens(events: ReadonlyArray<TTokenUsageEvent>, beforeIndex: number, runId?: string): number;
/**
* Pre-invoke compacted context size for a summarized turn (instructions +
* summary + kept messages), or `undefined` when the turn did not summarize.
* Persisted as the lightweight `summaryUsedTokens` marker so the client estimate
* fallback caps the discarded pre-summary history instead of re-summing it (the
* gauge otherwise reads 100% in perpetuity after a compaction). Pre-invoke, so
* it carries none of the `completedOutputTokens` ambiguity that keeps the full
* snapshot off some save paths. `summaryTokens` is a SEPARATE breakdown field, so
* the non-`remainingContextTokens` fallback adds it explicitly.
*
* `priorOutputTokens` (this response's earlier tool-loop outputs, see
* {@link priorRunOutputTokens}) is subtracted: those tokens are inside the
* baseline's kept messages AND in the response message's `tokenCount` the client
* adds on top, so leaving them in the marker double-counts them on a tool-loop
* summarized turn. Single-call turns pass 0 and are unaffected.
*/
declare function computeSummaryUsedTokens(snapshot: TContextUsageEvent | null | undefined, priorOutputTokens?: number): number | undefined;
/**
* Builds the response `metadata` for a STOPPED generation from the job's
* persisted emitted usage, so a stopped reply keeps its accurate cost rollup on
* reload (finding: stopped responses otherwise lose cost). Shared by every abort
* save path (agents abort route + legacy abort middleware).
*
* Deliberately omits the full `contextUsage`: unlike the live path, the abort
* path can't tell whether the FINAL call (the one the latest snapshot precedes)
* emitted usage — the job stores only the latest snapshot, not the snapshot
* count. If the final call emitted none, `completedOutputTokens` would reuse an
* earlier call's output the snapshot already counts → reload over-reports. So a
* stopped response falls back to the per-message gauge estimate on reload.
*
* It DOES persist the `summaryUsedTokens` marker when the stopped turn had
* summarized: that marker is pre-invoke (no `completedOutputTokens` ambiguity),
* and without it the fallback estimate re-sums the history the compaction
* discarded — leaving a stopped summarized turn pinned at 100%. Unlike the live
* path, the abort `tokenCount` comes from `countTokens(text)` (abortMiddleware) or
* is absent (agents abort route) — it does NOT fold in the summarization or
* earlier-call output the way `recordCollectedUsage` does. So the marker subtracts
* NOTHING: the full pre-invoke baseline is correct, and the client adds only the
* partial answer text on top (no overlap to cancel).
*/
declare function buildAbortedResponseMetadata(job: {
  tokenUsage?: string | null;
  contextUsage?: string | null;
} | null | undefined): {
  usage?: TResponseUsage;
  summaryUsedTokens?: number;
} | undefined;
/**
* Resolves the endpoint token config for a usage item by its producing agent.
* Multi-endpoint graphs tag each call with `agentId`; that agent's resolved
* config is authoritative — including `undefined`, which means "no configured
* rates, use built-in pricing" (e.g. a non-custom agent in a custom-primary
* graph). Only an untagged or unknown agent falls back to `fallback` (the
* primary config), so single-endpoint graphs are unchanged. `byAgentId` must
* hold an entry for every known agent (value may be `undefined`) so `has`
* distinguishes "known, no rates" from "unknown".
*/
declare function resolveAgentTokenConfig({
  agentId,
  byAgentId,
  fallback
}: {
  agentId?: string | null;
  byAgentId?: Map<string, EndpointTokenConfig | undefined>;
  fallback?: EndpointTokenConfig;
}): EndpointTokenConfig | undefined;
/**
* The `context` a run stamps on the usage transactions it records on exit. A stopped run
* still owns what it consumed — the abort route only signals — so it records under
* `'abort'` rather than skipping, and a completed run under `'message'`.
*/
declare function resolveRunUsageContext(aborted: boolean): "abort" | "message";
/**
* Whether a run already recorded provider-reported consumption for this response.
* `BaseClient` falls back to text-count billing whenever the recorded usage has no
* positive output count, which would charge the prompt a second time after
* {@link recordCollectedUsage} debited it — a stopped call may report input tokens
* and no output. An all-zero report is treated as unreported so the estimate still applies.
*/
declare function hasRecordedProviderUsage(usage: Pick<UsageMetadata, "input_tokens" | "output_tokens"> | null | undefined): boolean;
/**
* Whether any primary (response) call in the collected usage reported consumption. The stream
* aggregate {@link recordCollectedUsage} returns takes its input from the first primary entry
* only, so a later cancelled call that reported input alone is billed yet invisible there.
*/
declare function hasRecordedPrimaryUsage(collectedUsage: ReadonlyArray<UsageMetadata | null | undefined> | null | undefined): boolean;
interface FallbackTokenUsageParams {
  /** Usage the run already recorded for this response, when it recorded any. */
  usage?: (Pick<UsageMetadata, "input_tokens" | "output_tokens"> & {
    reasoning_tokens?: number;
  }) | null;
  /** Every entry the run collected; a billed call the aggregate hides still suppresses the estimate. */
  collectedUsage?: ReadonlyArray<UsageMetadata | null | undefined> | null;
  promptTokens?: number;
  completionTokens?: number;
  /** Whether the run was stopped; labels the row when no explicit `context` is given. */
  aborted?: boolean;
  /** Explicit transaction label; otherwise derived from `aborted`. */
  context?: string;
  /** Transaction fields the caller owns: user, conversation, message, model, config. */
  txMetadata: Omit<TxMetadata, "context">;
}
/**
* Text-count billing for a response whose provider usage was never recorded — the
* fallback `BaseClient` takes when the recorded usage has no positive output count.
* Once provider usage was recorded it is already billed, so this records nothing
* (see {@link hasRecordedProviderUsage}). A reasoning count the estimate cannot see is
* billed as its own `'reasoning'` row. Failures are logged, never thrown, so a billing
* error cannot fail the response that was already produced.
*/
declare function recordFallbackTokenUsage(deps: Pick<RecordUsageDeps, "spendTokens">, {
  usage,
  collectedUsage,
  promptTokens,
  completionTokens,
  aborted,
  context,
  txMetadata
}: FallbackTokenUsageParams): Promise<void>;
interface RecordUsageParams {
  user: string;
  conversationId: string;
  collectedUsage: UsageMetadata[];
  model?: string;
  context?: string;
  messageId?: string;
  balance?: Partial<TCustomConfig["balance"]> | null;
  transactions?: Partial<TTransactionsConfig>;
  endpointTokenConfig?: EndpointTokenConfig;
  /**
  * Per-usage endpoint token config resolver for multi-endpoint graphs. Called
  * with each usage item; when provided it is authoritative — its result prices
  * that item, including `undefined` (built-in pricing for a known agent with no
  * configured rates). It owns its own fallback to the primary config for
  * untagged/unknown agents (see {@link resolveAgentTokenConfig}). Single-config
  * callers (responses.js / openai.js) omit it and use `endpointTokenConfig`.
  */
  resolveEndpointTokenConfig?: (usage: UsageMetadata) => EndpointTokenConfig | undefined;
}
interface RecordUsageResult {
  input_tokens: number;
  output_tokens: number;
}
interface DetachedSubagentUsageRecorderParams {
  user: string;
  conversationId: string;
  model?: string;
  messageId?: string;
  balance?: Partial<TCustomConfig["balance"]> | null;
  transactions?: Partial<TTransactionsConfig>;
  endpointTokenConfig?: EndpointTokenConfig;
  endpointTokenConfigByAgentId?: Map<string, EndpointTokenConfig | undefined>;
}
/**
* Records token usage for collected LLM calls and spends tokens against balance.
* This handles both sequential execution (tool calls) and parallel execution (multiple agents).
*
* When `pricing` and `bulkWriteOps` deps are provided, prepares all transaction documents
* in-memory first, then writes them in a single `insertMany` + one `updateBalance` call.
*/
declare function recordCollectedUsage(deps: RecordUsageDeps, params: RecordUsageParams): Promise<RecordUsageResult | undefined>;
/**
* Creates an immutable, request-independent billing adapter for detached child
* calls. The recorder owns pricing selection and failure isolation so legacy
* controllers only provide their database dependencies and request snapshot.
*/
declare function createDetachedSubagentUsageRecorder(deps: RecordUsageDeps, params: DetachedSubagentUsageRecorderParams): (usage: UsageMetadata) => Promise<void>;
/** SDK-owned usage envelope re-exported for host billing consumers. */
type SubagentUsageEvent = SubagentUsageEvent$1;
/**
* Builds the host-side `subagentUsageSink` for `Run.create`. Subagent child
* graphs execute outside the run's `streamEvents` loop, so their model calls
* never reach the `CHAT_MODEL_END` handler (`ModelEndHandler`) — the SDK
* reports them through this sink instead. Each event is tagged
* `usage_type: 'subagent'` with the child's model/provider. Foreground child
* calls join the parent `collectedUsage` batch. Detached calls are recognized
* through their task-local context, billed immediately through
* `recordDetachedUsage`, and persisted with the durable child result instead
* of depending on a parent turn that may already have closed.
*/
declare function createSubagentUsageSink(collectedUsage: UsageMetadata[], onUsage?: (usage: UsageMetadata) => void | Promise<void>, recordDetachedUsage?: (usage: UsageMetadata) => void | Promise<void>): (event: SubagentUsageEvent) => void | Promise<void>;
//#endregion
//#region src/agents/stepBudget.d.ts
/**
* Upper bound on the tool-calling rounds still affordable after `roundsUsed` have
* completed, leaving room for a final answer.
*
* An upper bound rather than an exact figure: summarization, handoff and multi-agent
* routing nodes also consume supersteps but are invisible at the tool boundary, so
* the real number can only be lower. The notice wording is hedged accordingly, because it
* exists to convey urgency, and {@link isStepLimitError} still catches the wall.
*/
declare function remainingToolRounds(recursionLimit: number, roundsUsed: number): number;
/**
* The context injected once the budget is nearly spent. Returned as
* `additionalContext`, which the SDK consolidates into a single system-flavored
* message placed immediately before the next model call, the last thing the model
* reads before deciding whether to call another tool.
*
* Graph-state only: nothing here is persisted as message content, so the pressure
* applies to this turn and does not accumulate across the conversation.
*/
declare function buildBudgetNotice(remaining: number): string;
interface StepBudgetHookOptions {
  /** The resolved graph `recursionLimit` this run was invoked with. */
  recursionLimit: number;
}
/**
* Builds the run-scoped `PostToolBatch` hook that keeps the model aware of its
* per-turn step budget.
*
* Hitting `recursionLimit` mid-turn is recoverable but never good: the user waits
* for a turn that ends without an answer. Models are poor at tracking how many
* calls they have made, so the fix is to tell them, counting down over the last
* few rounds rather than announcing a budget they will ignore while it is ample.
*
* Counts tool *rounds*, not tool calls: a parallel batch of six calls is one node
* execution and costs one step, so per-call counting would overstate consumption
* six-fold. Empty batches and subagent-scoped events are skipped so only a real
* root tool round advances the countdown.
*/
declare function createStepBudgetHook({
  recursionLimit
}: StepBudgetHookOptions): HookCallback<"PostToolBatch">;
//#endregion
//#region src/agents/startup.d.ts
interface AgentStartupTelemetry {
  mark: (milestone: AgentStartupMilestone) => void;
  setStreamId: (streamId: string) => void;
  recordGenerationEvent: (event: ServerSentEvent) => boolean;
  end: (result: AgentStartupResult, error?: Error) => void;
}
interface AgentStartupTelemetryOptions {
  now?: () => number;
  startedAt?: number;
  spanStartedAt?: number;
}
declare function createAgentStartupTelemetry(options?: AgentStartupTelemetryOptions): AgentStartupTelemetry | undefined;
declare function getAgentStartupTelemetry(req: ServerRequest): AgentStartupTelemetry | undefined;
declare function acceptAgentStartupTelemetry(req: ServerRequest, streamId: string): void;
/**
* Capture the outer request timestamp before body parsing and auth. The recorder is
* created later, after the HTTP tracing middleware has installed its active context.
*/
declare function agentStartupIngressMiddleware(req: ServerRequest, res: Response$2, next: NextFunction): void;
declare function agentStartupTelemetryMiddleware(req: ServerRequest, res: Response$2, next: NextFunction): void;
//#endregion
//#region src/agents/subagentIdentity.d.ts
/** Retain execution identity beside the existing bounded child-content buffer. */
declare function captureSubagentIdentity(target: {
  subagentIdentity?: SubagentIdentity;
}, event: SubagentUpdateEvent): void;
//#endregion
//#region src/agents/subagentCompletionWakeup.d.ts
declare const SUBAGENT_COMPLETION_SOURCE = "subagent-completion";
type EnqueueAgentTrigger = (envelope: unknown, options?: AgentTriggerEnqueueOptions) => Promise<unknown>;
type WakeupMethods = Pick<ConversationMethods, "getConvo"> & Pick<ConversationMethods, "listActiveSubagentThreadLeases"> & Pick<MessageMethods, "claimSubagentTaskResult" | "getMessages" | "releaseSubagentTaskResultClaim">;
interface GenerationState$1 {
  status?: unknown;
  metadata?: {
    idempotencyClientRequestId?: unknown;
    terminalPersistencePending?: unknown;
  };
}
interface SubagentCompletionWakeupResolverDeps {
  methods: WakeupMethods;
  getGenerationJob: (conversationId: string) => Promise<GenerationState$1 | null>;
  now?: () => number;
}
/** Resolves a pre-registered completion delivery immediately before dispatch.
* The durable result claim elects exactly one consumer (manual poll or this
* delivery), while the branch lookup chains ordered sibling completions. */
declare function createSubagentCompletionWakeupResolver({
  methods,
  getGenerationJob,
  now
}: SubagentCompletionWakeupResolverDeps): NonNullable<AgentTriggerExecutionHostDeps["prepareContinue"]>;
/** Pre-registers the idempotent delivery before child provider work starts.
* A process crash can therefore delay a wakeup but cannot lose it; dispatch
* simply defers until the terminal child message exists. */
declare function createSubagentCompletionWakeupHandler(enqueue: EnqueueAgentTrigger): (registration: SubagentTaskWakeupRegistration) => Promise<void>;
//#endregion
//#region src/agents/skillConfigurable.d.ts
/**
* Augments a loadTools result with skill-specific configurable properties.
*
* `codeEnvAvailable` is threaded as a boolean (true when the agent's
* `execute_code` capability is enabled). Downstream skill consumers —
* the skill-tool handler (for file priming) and `primeInvokedSkills`
* (for history re-priming) — gate sandbox uploads on this flag so no
* sandbox traffic occurs for agents that lack code-execution capability.
*
* `skillPrimedIdsByName` maps each primed skill name (manual `$` or
* always-apply) to the `_id` of the exact doc whose body was primed
* into the turn. Skill-tool handlers consult it to:
*   1. Relax the `disable-model-invocation` gate on `read_file` for
*      any primed skill (so a `disable-model-invocation: true` skill
*      whose body is in context can still load its `references/*` /
*      `scripts/*` files). Without this, a user manually invoking —
*      or auto-priming — a disabled skill would get a body that
*      references files the model is forbidden to open.
*   2. Constrain the lookup to the primed `_id` on same-name
*      collisions, so `read_file` reads from the same doc whose body
*      got primed (otherwise a newer same-name duplicate could shadow
*      the resolver's pick and cause body/file mismatch within a
*      single turn).
* Empty/missing → no exception, the gate applies as normal and the
* lookup uses the full ACL set.
*/
interface SkillConfigurableContext {
  req: {
    user?: {
      id?: string;
    };
  };
  accessibleSkillIds?: unknown[];
  codeEnvAvailable: boolean;
  /**
  * `{ [skillName]: skillIdString }` for every skill primed this turn
  * (manual or always-apply). The id pins same-name collision lookups to
  * the exact doc the resolver primed and relaxes the disable-model gate.
  */
  skillPrimedIdsByName?: Record<string, string>;
  /**
  * Names of skills the runtime can resolve, captured at agent init by
  * `injectSkillCatalog`. Lets `read_file` decide whether a
  * `{firstSegment}/...` path is a real skill reference vs. a code-env path
  * that should fall back to bash, without an extra `getSkillByName`
  * round-trip just to discover the name doesn't resolve. Empty/undefined
  * ⇒ skills aren't effectively in scope for this agent (admin capability
  * off, ephemeral badge off, or persisted `skills_enabled !== true`),
  * which is the same signal as `accessibleSkillIds.length === 0`.
  */
  activeSkillNames?: Set<string>;
  /** True when skills/{skillName}/... write paths are enabled for this run. */
  skillAuthoringAvailable?: boolean;
  /** Host file-authoring names registered by initializeAgent for this run. */
  fileAuthoringToolNames?: Set<string>;
}
interface SkillConfigurableLoadResult {
  loadedTools: unknown[];
  configurable?: Record<string, unknown>;
}
interface EnrichWithSkillConfigurableParams {
  result: SkillConfigurableLoadResult;
  context: SkillConfigurableContext;
}
declare function enrichWithSkillConfigurable(params: EnrichWithSkillConfigurableParams): {
  loadedTools: unknown[];
  configurable: Record<string, unknown>;
};
declare function enrichWithSkillConfigurable(result: SkillConfigurableLoadResult, req: {
  user?: {
    id?: string;
  };
}, accessibleSkillIds: unknown[] | undefined, codeEnvAvailable: boolean, skillPrimedIdsByName?: Record<string, string>, activeSkillNames?: Set<string>, skillAuthoringAvailable?: boolean, fileAuthoringToolNames?: Set<string>): {
  loadedTools: unknown[];
  configurable: Record<string, unknown>;
};
//#endregion
//#region src/agents/toolValidation.d.ts
interface CompletedToolCall {
  tool_call?: {
    name?: unknown;
    output?: unknown;
  };
}
type ToolInputValidationReason = "invalid_tool_input" | "option_label_too_long";
interface ToolInputValidationError {
  fieldPath?: string;
  isLengthLimit: boolean;
}
interface ToolInputValidationDetails {
  toolName: string;
  reason: ToolInputValidationReason;
  fieldPath?: string;
}
/**
* Parse a schema-validation exception at the tool error boundary. Calling this
* with the thrown error, rather than completed tool output, prevents successful
* user-authored text from being mistaken for an execution failure.
*/
declare function parseToolInputValidationError(error: unknown): ToolInputValidationError | null;
declare function recordToolInputValidationError(errorsByToolCallId: Map<string, ToolInputValidationError> | null | undefined, error: unknown, toolCallId: unknown): void;
/**
* Reduce a tool input validation failure to privacy-safe structured fields for
* observability. Tool arguments and the raw validation message can contain
* user/model content, so callers should log only the returned details.
*/
declare function getToolInputValidationDetails(result: CompletedToolCall | null | undefined, validationError: ToolInputValidationError | null | undefined): ToolInputValidationDetails | null;
//#endregion
//#region src/agents/hitl/hooks.d.ts
/**
* Per-run context handed to a {@link ToolApprovalHookFactory} when a run is built. It carries
* the request-scoped facts the SDK's `PreToolUse` hook input does NOT include — the user, the
* conversation, the tenant, and the resolved app config — so a process-wide hook can
* specialize its decision per request (e.g. "auto-approve for admins", "stricter for tenant
* X"). The SDK input already provides the per-call facts (tool name, args, agent, thread,
* turn); this fills the request-identity gap.
*/
interface ToolApprovalHookContext {
  /** The requesting user's id, when authenticated. */
  userId?: string;
  /** The conversation (== LangGraph `thread_id`) the run belongs to. */
  conversationId?: string;
  /** Tenant id, in multi-tenant deployments. */
  tenantId?: string;
  /** The resolved app config for the request. */
  appConfig?: AppConfig;
}
/**
* A programmatic tool-approval hook: a `PreToolUse` callback that decides `allow` / `ask` /
* `deny` (and may rewrite the tool args via `updatedInput` or restrict the offered decisions
* via `allowedDecisions`) from the FULL live call — tool name, args, executing agent, thread,
* turn — not just the static name lists in `endpoints.agents.toolApproval`. Return an empty
* object (`{}`) to abstain and fall through to the configured policy / other hooks.
*
* Hooks COMPOSE with the static policy through the SDK's `PreToolUse` fold, which resolves
* decisions `deny` > `ask` > `allow`. A hook can therefore only ever TIGHTEN a configured
* `ask` / `deny` — it can never silently auto-approve past policy. To loosen, change the
* static policy. (The SDK's own `createWorkspacePolicyHook` is this exact shape.)
*/
type ToolApprovalHook = HookCallback<"PreToolUse">;
/**
* Builds a {@link ToolApprovalHook} for one run from its {@link ToolApprovalHookContext}.
* Return `undefined` to opt out of this run entirely (e.g. the policy doesn't apply to this
* user, or the app config disables the hook) — cheaper and clearer than a hook that always
* abstains. Registered process-wide via {@link registerToolApprovalHook}.
*/
type ToolApprovalHookFactory = (context: ToolApprovalHookContext) => ToolApprovalHook | undefined;
interface ResolvedToolApprovalHook {
  hook: ToolApprovalHook;
  matcher?: string;
  /** Optional admission-only scope for hooks that inspect the executing agent at runtime. */
  agentIds?: ReadonlySet<string>;
}
/**
* Register a programmatic tool-approval hook (process-wide). Call once at startup. Returns an
* unregister function that removes exactly this registration.
*
* Inert unless tool approval is enabled AND the caller is HITL-capable — hooks only run inside
* the `PreToolUse` fold of an HITL run (see {@link buildToolApprovalHooks} /
* `buildHITLRunWiring`). They compose with, and can only tighten, the static
* `endpoints.agents.toolApproval` policy.
*
* @param factory Builds the per-run hook from its context; return `undefined` to opt out.
* @param options.matcher Optional regex string matched against the tool name — omit to run for
*   every tool. Patterns are compiled with `new RegExp` by the SDK without a sandbox, so only
*   register trusted / length-bounded patterns.
*/
declare function registerToolApprovalHook(factory: ToolApprovalHookFactory, options?: {
  matcher?: string;
}): () => void;
/** Number of currently-registered hook factories (diagnostics / tests). */
declare function getRegisteredToolApprovalHookCount(): number;
/** Remove every registered hook. Test/teardown helper. */
declare function clearToolApprovalHooks(): void;
/**
* Resolve the registered hook factories against a run's {@link ToolApprovalHookContext} into
* concrete `PreToolUse` hooks (each with its optional tool-name matcher). Factories that
* return `undefined` (opt out for this run) are dropped. Registration order is preserved.
*
* Consumed by `buildHITLRunWiring`, which registers these AFTER the static-policy hook so a
* host hook's `updatedInput` / `allowedDecisions` win the SDK's last-writer-wins precedence.
*/
declare function buildToolApprovalHooks(context: ToolApprovalHookContext): ResolvedToolApprovalHook[];
/** Whether any resolved hook matcher can run for one of the supplied tool names. */
declare function resolvedToolApprovalHooksCanMatch(hooks: readonly ResolvedToolApprovalHook[], toolNames: readonly string[], agentId?: string): boolean;
//#endregion
//#region src/agents/steering/runtime.d.ts
type TerminalSteerHookInput = Omit<HookInputByEvent["Stop"], "hook_event_name"> & {
  hook_event_name: "StopFinalize";
  continuationBudgetRemaining: number;
  continuationPlanned: boolean;
  continuationPrevented: boolean;
};
type TerminalSteerHook = (input: TerminalSteerHookInput, signal: AbortSignal) => HookOutputByEvent["Stop"] | Promise<HookOutputByEvent["Stop"]>;
/**
* Whether the installed `@librechat/agents` supports the FULL steering
* contract — both halves are required before any steer part may be created:
* 1. `HOOK_INJECTED_MESSAGES_CAPABLE`: hook-returned `injectedMessages`
*    convert into graph-state HumanMessages at the PostToolBatch boundary
*    (draining the queue without this silently drops the user's words).
* 2. `ContentTypes.STEER`: the SDK's `formatAgentMessages` replays persisted
*    steer parts as user turns on later prompts (the replay branch ships in
*    the same SDK commit as the enum member). Without it, a persisted steer
*    part would fall through the formatter's catch-all INTO the assistant's
*    provider content — so an SDK that can inject but not replay must still
*    501 the steer route.
* The pinned dependency (^3.2.62) carries both; the runtime probe stays (read
* via the namespace so an older install yields `undefined` → false, not a
* missing-binding failure) as the defensive gate for mismatched deployments.
*/
declare function isSteeringSupported(): boolean;
/** Encoded multimodal content for a steer that carried attachments. */
interface SteerMediaResult {
  /** Full ordered content array for graph injection (text part included). */
  content: Array<Record<string, unknown>>;
  /** Validated file refs (from the DB, not the client). */
  files?: SteerQueueItem["files"];
}
interface SteerDrainHookOptions {
  streamId: string;
  /**
  * The job's `createdAt` captured when this run was wired. A steer route can
  * only enqueue against the CURRENT job, so if the live job's `createdAt`
  * differs, this run was replaced — it must not consume the new job's steers.
  */
  jobCreatedAt?: number;
  /**
  * Applies one drained steer to host state — appends the steer content part
  * at the live content index, bumps the shared index offset, and emits the
  * `on_steer_applied` SSE event. Called FIFO, BEFORE the item's media encode:
  * once a steer leaves the durable queue, the part must be persisted before
  * any slow/abortable work, or an abort during the encode loses the user's
  * words (the terminal drain sees an empty queue and the content snapshot
  * lacks the part). Failures are logged per item and never block injection.
  */
  applySteer: (item: SteerQueueItem) => void | Promise<void>;
  /**
  * Resolves a steer's attachment refs into encoded model content (owner-scoped
  * fetch + provider encoding, host-side). Only consulted for items that carry
  * files. Any failure degrades that steer to text-only — the user's words are
  * never dropped because an attachment could not be encoded.
  */
  buildMedia?: (item: SteerQueueItem) => Promise<SteerMediaResult | undefined>;
  /** Repairs the durable steer part when media degrades to text-only. */
  onMediaError?: (item: SteerQueueItem, error: unknown) => void | Promise<void>;
}
/**
* Build the run-scoped `PostToolBatch` hook that drains the job's steer queue
* at each tool-batch boundary and injects each steer into graph state as its
* own user message (`role: 'user'`, `source: 'steer'` — never consolidated
* with hook context). Steers with attachments inject as multimodal content
* arrays via `buildMedia`.
*
* Subagent scopes are skipped (`input.agentId` set): a steer targets the
* top-level conversation, not a child agent's context. Hook errors are
* swallowed by the SDK's `executeHooks`, so a broken drain can never kill the
* run.
*/
declare function createSteerDrainHook(opts: SteerDrainHookOptions): HookCallback<"PostToolBatch">;
/**
* The PreemptBoundary twin of {@link createSteerDrainHook}: fires after the
* SDK seals a model stream mid-generation, drains the same durable queue
* through the same shared body, and injects the same message shapes. Because
* `noteSteersRemoved` lives in that shared body, a preempt satisfied at an
* ordinary tool boundary clears its request there and cannot cause a
* spurious seal later.
*/
declare function createSteerPreemptBoundaryHook(opts: SteerDrainHookOptions): HookCallback<"PreemptBoundary">;
/**
* Serialized terminal steering boundary. StopFinalize runs after ordinary
* Stop hooks have folded, so the store can atomically claim queued steers,
* keep admission open for a continuation another hook already planned, or
* seal admission so every later steer POST becomes an ordinary user turn.
*/
declare function createSteerTerminalContinuationHook(opts: SteerDrainHookOptions): TerminalSteerHook;
/**
* The run's `RunConfig.preemption` — a level-triggered O(1) poll over the
* job's armed preempt requests, exactly as the SDK contract requires: it
* keeps returning true until the boundary drain (either boundary) clears the
* request via `noteSteersRemoved`. Never consumes on read.
*
* `subscribe` adds the wake channel the poll alone cannot cover. The SDK only
* reads `shouldPreempt` per streamed chunk, so an interrupt armed while the
* model is silent — or while it streams reasoning that will never become
* sealable — would otherwise wait for the entire turn and land as a terminal
* continuation. Woken, the SDK discards the unstarted turn and re-issues it
* with the steer appended. The callback is a hint only; the poll above stays
* the authority, so the level-triggered contract is unchanged.
*
* Fenced on `jobCreatedAt` for the same reason every other preempt entry point
* is: a run wired to a generation that has since been replaced must not be
* woken by the replacement's arms.
*/
declare function createSteerPreemptPoll(streamId: string, jobCreatedAt?: number): StreamPreemption;
/**
* Whether the installed SDK can seal a generation mid-stream and inject at
* the resulting boundary. A SEPARATE probe from `isSteeringSupported()`:
* reusing that flag would arm an interrupt affordance that silently does
* nothing on an SDK that can only inject at tool boundaries.
*/
declare function isSteerPreemptSupported(): boolean;
/**
* Whether the installed SDK can honor a preempt on a turn that has produced
* nothing to keep — discarding the in-flight model call and re-issuing it
* rather than waiting for a sealable chunk that a silent or thinking turn
* never reaches.
*
* A THIRD probe, separate from `isSteerPreemptSupported()`, because the two
* differ in exactly the window users reach for an interrupt most. An SDK with
* only the seal path still accepts the request and still labels the chip
* "interrupting" — it simply cannot act until the model starts writing.
*/
declare function isSteerPreemptRestartSupported(): boolean;
declare function isSteerTerminalContinuationSupported(): boolean;
//#endregion
//#region src/agents/fading.d.ts
/** Latched tiers keyed by agent ID, the shape `RunConfig.fadingTiers` takes. */
type RunFadingTiers = Record<string, IAgentFadingTier>;
/**
* Normalizes the tier a run exposes for persistence, or undefined when there
* is none. `Run.getFadingTier()` already returns only tiers that carry
* information (masking active, a budget below the pruner's window, or a tier
* restored from host state), so the host validates the shape and strips
* anything else the SDK may attach, such as its `latched` provenance flag.
*/
declare function resolvePersistableFadingTier(tier: unknown): IAgentFadingTier | undefined;
/**
* Normalizes the per-agent tiers a run exposes (`Run.getFadingTiers()`) into
* persisted entries. Only own enumerable keys are read and every tier is
* validated and stripped, so an agent ID never reaches storage as a field name
* and nothing beyond the compact tier is persisted.
*/
declare function resolvePersistableFadingTiers(tiers: unknown): IAgentFadingTierEntry[] | undefined;
/**
* Rebuilds `RunConfig.fadingTiers` from persisted entries on a null-prototype
* record, so an agent ID such as `__proto__` stays an own key and can never
* touch the prototype chain.
*/
declare function resolveRunFadingTiers(entries: unknown): RunFadingTiers | undefined;
type RunContextMetaParams = {
  calibrationRatio: number;
  fadingTier: unknown; /** Per-agent tiers from `Run.getFadingTiers()` or the live graph. */
  fadingTiers?: unknown; /** Resolved lazily: only consulted when there is something to persist. */
  getEncoding: () => string;
};
/**
* Builds the compact `contextMeta` a response message persists from a run, or
* undefined when neither calibration nor fading carries information. Only the
* calibration ratio, its encoding and the latched tiers are kept; message
* content, canonical tool results and projection state never are. A latched
* tier is persisted even at a neutral calibration ratio, with the ratio
* recorded as 1 so the stored shape stays valid.
*/
declare function resolveRunContextMeta(params: RunContextMetaParams): IAgentEventActorContextMeta | undefined;
//#endregion
//#region src/agents/run.d.ts
/**
* Extracts discovered tool names from message history by parsing tool_search results.
* When the LLM calls tool_search, the result contains tool names that were discovered.
* These tools should have defer_loading overridden to false on subsequent turns.
*
* Supports both:
* - New JSON format: { "tools": [{ "name": "tool_name" }] }
* - Legacy text format: "- tool_name (score: X.XX)"
*
* @param messages - The conversation message history
* @returns Set of tool names that were discovered via tool_search
*/
declare function extractDiscoveredToolsFromHistory(messages: BaseMessage[]): Set<string>;
interface RunDiscoverySnapshot {
  getDiscoveredTools?: () => string[];
  getRunMessages?: () => BaseMessage[] | undefined;
}
/** Reads canonical run discovery state, with best-effort history parsing for older releases. */
declare function getRunDiscoveredTools(run: RunDiscoverySnapshot): string[];
/**
* Extracts skill names that were invoked in previous turns from raw message payload.
* Scans assistant messages for tool_call content parts where name === 'skill'.
* Works with TPayload (raw message objects) so it can run before formatAgentMessages.
*
* @param payload - The raw conversation message payload
* @returns Set of skill names that were previously invoked
*/
declare function extractInvokedSkillsFromPayload(payload: Array<Partial<{
  role: string;
  content: unknown;
}>>): Set<string>;
/**
* Overrides defer_loading to false for tools that were already discovered via tool_search.
* This prevents the LLM from having to re-discover tools on every turn.
*
* @param toolRegistry - The tool registry to modify (mutated in place)
* @param discoveredTools - Set of tool names that were previously discovered
* @returns Number of tools that had defer_loading overridden
*/
declare function overrideDeferLoadingForDiscoveredTools(toolRegistry: LCToolRegistry$1, discoveredTools: Set<string>): number;
type AgentReasoningKey = "reasoning_content" | "reasoning";
declare function getReasoningKey(provider: Providers, llmConfig: RunLLMConfig, agentEndpoint?: string | null, customReasoningKey?: ReasoningResponseKey): AgentReasoningKey;
/**
* Whether the (provider, model) pair targets DeepSeek's thinking-mode
* tool-calling contract, which requires `reasoning_content` to be replayed
* on every prior assistant message that emitted `tool_calls`.
* @see https://api-docs.deepseek.com/guides/thinking_mode#tool-calls
*/
declare function isDeepSeekReasoningProvider(provider: string | Providers | undefined | null, model?: string | null): boolean;
/**
* Whether prior assistant tool-call messages should have `reasoning_content`
* reconstructed when reformatting persisted history (cross-turn replay): either
* DeepSeek thinking-mode (#13366) or a custom OpenAI-compatible endpoint that
* opted in via `customParams.includeReasoningHistory` (e.g. Xiaomi MiMo, Kimi).
*/
declare function shouldReplayReasoningContent(agent?: {
  provider?: string | Providers | null;
  model?: string | null;
  model_parameters?: {
    model?: string | null;
  } | null;
  includeReasoningHistory?: boolean | null;
} | null): boolean;
type RunAgent = Omit<Agent, "tools"> & {
  azureOptions?: AzureOptions;
  tools?: GenericTool[];
  maxContextTokens?: number; /** Pre-ratio context budget from initializeAgent. */
  baseContextTokens?: number;
  useLegacyContent?: boolean;
  imageDetail?: ImageDetail;
  toolContextMap?: Record<string, unknown>;
  dynamicToolContextMap?: Record<string, unknown>;
  toolRegistry?: LCToolRegistry$1; /** Serializable tool definitions for event-driven execution */
  toolDefinitions?: LCTool$1[]; /** Precomputed flag indicating if any tools have defer_loading enabled */
  hasDeferredTools?: boolean; /** Both-direction identity aliases for MCP tools whose key spelling changed */
  mcpToolAliases?: MCPToolAlias[]; /** Names of tools injected with the `run_in_background` param (excluded from eager execution). */
  backgroundToolNames?: string[]; /** Names of tools with the host-injected `intent` param (stripped from self-spawn inputs). */
  intentToolNames?: string[]; /** Marker-verified tool names whose intent labels are safe compaction guidance. */
  semanticIntentToolNames?: string[];
  /**
  * Per-agent codeenv gate set by `initializeAgent`: admin-level
  * `execute_code` capability AND the agent actually requested
  * `execute_code` in its tools. Used here to enable
  * `RunConfig.toolOutputReferences` only on runs where the bash tool
  * is actually registered.
  */
  codeEnvAvailable?: boolean;
  /**
  * Per-agent stateful-session gate set by `initializeAgent`: the admin
  * `stateful_code_sessions` capability AND the agent's builder opt-in AND
  * `codeEnvAvailable`. Carried into per-agent tool loading and prewarming.
  */
  statefulCodeSessions?: boolean; /** Per-agent stateful workspace sharing scope. */
  statefulCodeEnvironment?: Agent["stateful_code_environment"]; /** Trusted partition for transient code session ids and file references. */
  codeSessionKey?: string; /** Trusted Code API route selected during initialization. */
  codeExecutionContext?: CodeExecutionContext; /** Whether this initialized agent can route skills/ writes to persistent skill storage. */
  skillAuthoringAvailable?: boolean; /** Optional per-agent summarization overrides */
  summarization?: SummarizationConfig; /** Response field to read model reasoning from for custom OpenAI-compatible endpoints. */
  reasoningKey?: ReasoningResponseKey; /** Whether to reconstruct `reasoning_content` from persisted history across turns. */
  includeReasoningHistory?: boolean;
  /**
  * Maximum characters allowed in a single tool result before truncation.
  * Overrides the default computed from maxContextTokens.
  */
  maxToolResultChars?: number; /** Initialized subagent configs (loaded by initialize.js from agent.subagents.agent_ids). */
  subagentAgentConfigs?: RunAgent[];
  /**
  * Inert, VIEW-checked descriptors for explicit children that are initialized
  * only after the SDK selects them. These resolvers are request-scoped: they
  * may use the active request's authorization and tool-loading context.
  */
  lazySubagentConfigs?: LazySubagentAgent[]; /** All-or-nothing saved-agent teams resolved by initialize.js. */
  subagentGraphConfigs?: Array<{
    definition: AgentSubagentGraph;
    memberConfigs: RunAgent[];
  }>; /** Member-scoped always-apply skills resolved during agent initialization. */
  alwaysApplySkillPrimes?: ResolvedAlwaysApplySkill[]; /** Source subagent spawning configuration (enabled / allowSelf / agent_ids). */
  subagents?: AgentSubagentsConfig;
};
type LazySubagentAgent = Pick<RunAgent, "id" | "name" | "description" | "provider" | "model" | "model_parameters" | "recursion_limit" | "subagents" | "codeEnvAvailable" | "statefulCodeSessions" | "statefulCodeEnvironment" | "codeExecutionContext" | "codeSessionKey" | "skillAuthoringAvailable" | "includeReasoningHistory" | "mcpToolAliases"> & {
  configId: string;
  subagentAgentConfigs?: RunAgent[];
  lazySubagentConfigs?: LazySubagentAgent[]; /** Lightweight graph-member metadata used only by run-wide capability gates. */
  subagentGraphMemberMetadata?: SubagentTreeNode[];
  resolve: (context: SubagentResolveContext) => Promise<RunAgent>;
};
type SubagentTreeNode = Pick<RunAgent, "id" | "provider" | "model" | "model_parameters" | "codeEnvAvailable" | "statefulCodeSessions" | "statefulCodeEnvironment" | "codeExecutionContext" | "codeSessionKey" | "skillAuthoringAvailable" | "includeReasoningHistory" | "mcpToolAliases"> & {
  subagentAgentConfigs?: SubagentTreeNode[];
  lazySubagentConfigs?: SubagentTreeNode[];
  subagentGraphMemberMetadata?: SubagentTreeNode[];
  subagentGraphConfigs?: Array<{
    memberConfigs: SubagentTreeNode[];
  }>;
};
type RunModelCallback = ModelBoundChatModelCallback | ModelErrorTrackerCallback;
/**
* Collect MCP key-spelling aliases from every eagerly known agent in the run.
* Lazy descriptors are revisited when they resolve, because initializing MCP
* tools solely to discover aliases would defeat lazy loading.
*/
declare function collectRunMCPToolAliases(agents: Array<RunAgent | SubagentTreeNode | null | undefined>): MCPToolAlias[];
/**
* Whether a single agent's tool surface includes the `ask_user_question` tool, in any
* of the three places a tool can live on a `RunAgent`: loaded instances (`tools`), the
* schema-only registry (`toolRegistry`), or serialized definitions (`toolDefinitions`).
* Checked against TOP-LEVEL agents only (not subagents — the tool is stripped from
* child configs in `buildAgentInput`, since a child graph executing outside the parent
* run's stream cannot pause the parent).
*
* Exported for AgentClient's pre-turn orphan-checkpoint prune gate: the prune must
* fire whenever THIS turn may attach a checkpointer, which since the ask tool is no
* longer coupled to `toolApproval.enabled` includes ask-capable runs.
*/
declare function agentRequestsAskUserQuestion(agent: {
  tools?: unknown[];
  toolRegistry?: Map<string, unknown>;
  toolDefinitions?: Array<{
    name: string;
  }>;
}): boolean;
/**
* Whether the admin tool filter (`includedTools` allowlist, else `filteredTools`
* exclude list — same precedence as `loadAndFormatTools`) disables
* `ask_user_question`. Enforced at RUN BUILD, not just in the tools-dialog listing:
* agents saved before an admin filtered the tool out would otherwise keep exposing
* it to the model, attaching checkpointers, and pausing runs — for a run-pausing
* tool the filter must be an actual kill switch.
*/
declare function isAskUserQuestionAdminDisabled(appConfig?: AppConfig): boolean;
/**
* Whether any agent reachable in the run — primary, handoff/parallel, or a
* nested subagent — opts into cross-turn `reasoning_content` reconstruction.
* Walks `subagentAgentConfigs` like {@link anyAgentHasCodeEnv}, since an
* opted-in custom endpoint may appear only as a (possibly pruned) subagent.
*/
declare function anyAgentReplaysReasoningContent(agents: Array<RunAgent | null | undefined>): boolean;
declare function createRun({
  runId,
  signal,
  conversationId,
  agents,
  messages,
  discoveredToolNames,
  requestBody,
  codeApprovalMode: requestedCodeApprovalMode,
  user,
  tenantId,
  centralTraceExportEnabled,
  traceContext,
  tokenCounter,
  customHandlers,
  indexTokenCountMap,
  initialSessions,
  summarizationConfig,
  summarizeOnly,
  compactionSemanticIndex,
  initialSummary,
  modelCallbacks,
  calibrationRatio,
  fadingTier,
  fadingTiers,
  appConfig,
  subagentUsageSink,
  subagentTasks,
  runFiles,
  steering,
  activityLabel,
  activityPhase,
  eventActorCheckpointing,
  hitlCapable,
  resolvedToolApprovalHooks,
  toolInputValidationErrors,
  sessionStartSource,
  streaming,
  streamUsage
}: {
  agents: RunAgent[];
  signal: AbortSignal;
  runId?: string;
  /** Conversation-stable identity, used by the e2e run hook to tell a resumed
  *  run apart from a fresh attempt (a resume carries no messages). */
  conversationId?: string;
  streaming?: boolean;
  streamUsage?: boolean;
  requestBody?: RequestBody;
  codeApprovalMode?: CodeApprovalMode;
  user?: IUser;
  tenantId?: string;
  /**
  * Defaults to true. Set false to suppress central Langfuse export for this
  * run. Tenant fanout can still export when tenant routing is available.
  */
  centralTraceExportEnabled?: boolean;
  /**
  * Request values the deployment may export as Langfuse trace metadata
  * (`langfuse.trace.conversationMetadataFields`). The conversation id,
  * provider, and model default from the run itself.
  */
  traceContext?: LangfuseTraceContext; /** Message history for extracting previously discovered tools */
  messages?: BaseMessage[];
  /**
  * Pre-discovered deferred-tool names to force-load directly, bypassing message
  * extraction. The HITL resume path rebuilds the graph with `messages: []` (state
  * comes from the durable checkpoint), so the in-turn `tool_search` results that
  * would normally mark a deferred tool discovered aren't present — without this the
  * paused tool's schema would be absent from the rebuilt model binding. Captured at
  * pause from canonical run state (with message parsing for older SDK releases) and
  * replayed here. Merged with (not replacing) names extracted from `messages`.
  */
  discoveredToolNames?: string[];
  summarizationConfig?: SummarizationConfig;
  /**
  * Manual compaction: the primary agent summarizes the history outright and
  * the run ends after the summary without a model call. Applies to the
  * primary agent only; a chained or delegated agent never runs.
  */
  summarizeOnly?: boolean; /** Bounded, source-addressed navigation guidance derived with provider messages. */
  compactionSemanticIndex?: CompactionSemanticIndex; /** Cross-run summary from formatAgentMessages, forwarded to AgentContext */
  initialSummary?: {
    text: string;
    tokenCount: number;
  }; /** Model callbacks inherited by root, summary, fallback, and subagent clients. */
  modelCallbacks?: readonly RunModelCallback[]; /** Calibration ratio from previous run's contextMeta, seeds the pruner EMA */
  calibrationRatio?: number;
  /**
  * Default agent's latched context-fading tier from the previous run's
  * contextMeta. It seeds the pruner so the provider-only projection of
  * historical tool results keeps the same bytes across runs; graph messages
  * stay canonical. Ships in `@librechat/agents` after 3.7.13; older SDK
  * versions ignore it.
  */
  fadingTier?: IAgentFadingTier | null;
  /**
  * Latched tiers keyed by agent ID from the previous run's contextMeta, so
  * every agent of a multi-agent run restores its own tier. Same SDK
  * availability as `fadingTier`.
  */
  fadingTiers?: RunFadingTiers | null;
  /**
  * Resolved app config. Used to translate custom-endpoint provider names
  * (e.g. "Ollama") in the summarization config to SDK-recognized providers.
  */
  appConfig?: AppConfig;
  /**
  * Receives per-model-call usage from subagent child runs so hosts can bill
  * them (child graphs execute outside the run's `streamEvents` loop, so
  * their usage never reaches `customHandlers`). Typed structurally — not as
  * `Pick<RunConfig, 'subagentUsageSink'>` — because the field ships in
  * `@librechat/agents` > 3.2.33; older SDK versions ignore it at runtime.
  * Switch to the `RunConfig` pick once the dependency is bumped.
  */
  subagentUsageSink?: (event: SubagentUsageEvent) => void; /** Host-owned detached-subagent task store and trusted parent-thread scope. */
  subagentTasks?: SubagentTaskConfig; /** Run-scoped file authorization and child context, supplied by the host. */
  runFiles?: RunFileSession;
  /**
  * The run-scoped steer-drain hook (a `PostToolBatch` callback built via
  * `createSteerDrainHook`). Registered on the run's hook registry independent
  * of the tool-approval policy — steering needs neither HITL nor a
  * checkpointer (injection merges via the messages reducer inside the tool
  * node). Only the resumable agents controller passes this; the
  * OpenAI-compatible and Responses controllers have no job/SSE surface.
  */
  steering?: {
    hook: HookCallback<"PostToolBatch">;
    /**
    * The PreemptBoundary twin of `hook`, built via
    * `createSteerPreemptBoundaryHook` from the same drain closures. Fires
    * when the SDK seals a model stream mid-generation on a preempt request.
    */
    preemptHook?: HookCallback<"PreemptBoundary">;
    /**
    * Atomically claims queued steers at the SDK's terminal Stop boundary or
    * seals admission so later messages become ordinary follow-up turns.
    */
    terminalHook?: TerminalSteerHook;
    /**
    * Level-triggered O(1) poll over the job's armed preempt requests
    * (`createSteerPreemptPoll`). Threaded into `RunConfig.preemption`, which
    * also makes the SDK reserve recursion-limit headroom for its seals.
    */
    preemption?: StreamPreemption;
  };
  /**
  * Run-scoped tool-batch summary hook (PostToolBatch). Like steering, it
  * registers independently of the approval policy and needs no checkpointer;
  * the hook returns immediately and generates off the critical path.
  */
  activityLabel?: {
    hook: HookCallback<"PostToolBatch">;
  }; /** Run-wide parent phase collector; registered after child batch labels. */
  activityPhase?: {
    hook: HookCallback<"PostToolBatch">;
  }; /** Persist clean terminal checkpoints for an isolated bound-event invocation. */
  eventActorCheckpointing?: boolean;
  /**
  * Whether the caller implements the HITL pause/resume lifecycle (inspects
  * `run.getInterrupt()`, persists a pending action, exposes a resume route). Gates the
  * tool-approval wiring: only AgentClient (chat + resume) sets this. The OpenAI-compatible
  * and Responses controllers leave it false, so an approval-gated tool can't pause on a
  * route that has no approval surface or resume endpoint (it would otherwise emit a normal
  * final response / `[DONE]` with the tool call left unresolved).
  */
  hitlCapable?: boolean;
  /**
  * Request-scoped approval hooks already resolved by the scheduled-run admission guard.
  * Reuse them here so a context-aware factory is evaluated exactly once for the run.
  */
  resolvedToolApprovalHooks?: readonly ResolvedToolApprovalHook[]; /** Plugin-hook SessionStart lifecycle source: 'startup' (default) or 'resume' on HITL-rebuild paths. */
  sessionStartSource?: string; /** Request-scoped tool input failures consumed by the completion handler. */
  toolInputValidationErrors?: Map<string, ToolInputValidationError>;
} & Pick<RunConfig, "tokenCounter" | "customHandlers" | "indexTokenCountMap" | "initialSessions">): Promise<Run<IState>>;
//#endregion
//#region src/agents/publication.d.ts
type ContextMetaWriter = (contextMeta: IAgentEventActorContextMeta) => Promise<void>;
interface ContextMetaPublisherOptions {
  /** Durable write of one record, e.g. the job metadata writer. */
  write: ContextMetaWriter;
  /** Called once per publication whose every attempt failed. */
  onFailure?: (error: unknown, contextMeta: IAgentEventActorContextMeta) => void;
  /** Write attempts per publication, including the first. */
  attempts?: number;
  /** Backoff before the second attempt, doubled for each later one. */
  retryDelayMs?: number;
  delay?: (ms: number) => Promise<void>;
}
interface ContextMetaPublisher {
  /**
  * Publishes a record unless it equals the latest publication, in which case
  * the caller shares that publication's promise. Distinct records are written
  * in call order, each after the previous publication settles, so the store's
  * last-writer-wins keeps the newest snapshot. Never rejects.
  */
  publish(contextMeta: IAgentEventActorContextMeta): Promise<void>;
  /** Whether the job may carry a record: a write has committed, or one is in flight. */
  readonly hasPublished: boolean;
}
/**
* Coordinates the durable publication of a run's compact context meta onto its
* job: one write per distinct record, shared by equal concurrent callers,
* ordered so the newest record wins, retried on transient failure. A caller
* that awaits `publish` before its model call therefore knows the job carries
* the record that describes that call. An exhausted publication is reported
* through `onFailure` and forgotten, so the next snapshot writes again, while
* a record committed earlier still counts as published so that a later neutral
* snapshot overwrites it instead of leaving it on the job.
*/
declare function createContextMetaPublisher(options: ContextMetaPublisherOptions): ContextMetaPublisher;
type RunContextMetaSelection = {
  /** True for a snapshot of the running graph; false for the pre-run seed publish. */live: boolean; /** The run's own compact state, when it has any. */
  captured: IAgentEventActorContextMeta | undefined; /** The state inherited from the parent response. */
  inherited: IAgentEventActorContextMeta | undefined;
  hasPublished: boolean;
  getEncoding: () => string;
};
/**
* Chooses what a publication should carry: the run's own state when it has
* any, the inherited seed before the run exists, and otherwise a neutral
* record (ratio 1, no tier) once something has been published, since a
* running job's fields cannot be deleted through the metadata writer and a
* neutral record seeds the next turn exactly as no record would. A fresh
* conversation's first neutral snapshot publishes nothing.
*/
declare function selectRunContextMetaToPublish(selection: RunContextMetaSelection): IAgentEventActorContextMeta | undefined;
//#endregion
//#region src/agents/testHook.d.ts
/**
* Context handed to a test run hook so it can shape fake-model behavior from
* the conversation and the agents' advertised tools.
*/
interface TestRunHookContext {
  messages?: BaseMessage[];
  /**
  * Identifies the conversation this run belongs to. A resumed run rebuilds
  * `createRun` with no messages because state is rehydrated from the
  * checkpoint, so message history alone cannot tell a resume apart from a
  * fresh attempt; this is the stable identity across both.
  */
  conversationId?: string;
  agents: ReadonlyArray<{
    tools?: ReadonlyArray<{
      name: string;
    }>;
  }>;
  /** Opaque model-client handlers forwarded to the fake model by the e2e harness. */
  modelCallbacks?: readonly object[];
}
type TestRunHook = (run: Run<IState>, context: TestRunHookContext) => void;
/**
* Env-gated extension point used only by the e2e harness. When
* `LIBRECHAT_TEST_RUN_HOOK` points at a module, it is loaded and invoked with
* the freshly created run so a test can swap in a fake model via
* `run.Graph.overrideTestModel(...)` instead of reaching a live provider. A
* no-op (returns immediately) in normal operation since the env var is unset.
*/
declare function applyTestRunHook(run: Run<IState>, context: TestRunHookContext): void;
//#endregion
//#region src/agents/tools.d.ts
declare const CREATE_FILE_TOOL_NAME = "create_file";
declare const EDIT_FILE_TOOL_NAME = "edit_file";
declare const SEARCH_WORKSPACE_TOOL_NAME = "search_workspace";
declare const LIST_WORKSPACE_FILES_TOOL_NAME = "list_workspace_files";
declare const HOST_FILE_AUTHORING_ARTIFACT_KEY = "__librechat_file_authoring";
declare const FILE_AUTHORING_TOOL_NAMES: ReadonlySet<string>;
/**
* Every tool that reads or writes the code environment. Eligibility and provisioning both
* consult this: a turn starting with any of them needs its files in the sandbox already,
* and an agent that has any of them wants code-file provisioning built even when it never
* names the `execute_code` marker itself.
*/
declare const CODE_FILE_TOOL_NAMES: ReadonlySet<string>;
declare function isCodeFileToolName(name: string): boolean;
declare function isCodeSessionToolName(name: string, hostFileAuthoringToolNames?: ReadonlySet<string>): boolean;
/** Tools that consume shared code, search, or image resources need current file records. */
declare function isFileResourceToolName(name: string): boolean;
/** File-authoring artifacts opt in explicitly; other tool artifacts keep their normal delivery. */
declare function isCodeArtifactToolOutput(output: {
  name: string;
  artifact?: unknown;
}): boolean;
interface ToolDefLike {
  name: string;
  [key: string]: unknown;
}
interface ToolInstanceLike {
  name: string;
  [key: string]: unknown;
}
interface BuildToolSetConfig {
  toolDefinitions?: ToolDefLike[];
  tools?: (ToolInstanceLike | null | undefined)[];
  /** Tool names retained on unresolved agent descriptors for history replay. */
  historicalToolNames?: readonly string[];
}
interface BuildHistoricalToolNamesConfig {
  configuredToolNames?: readonly string[];
  alwaysApplyToolNames?: readonly string[];
  toolOptions?: AgentToolOptions;
  rawMcpServerNames?: readonly string[];
  codeExecutionAvailable?: boolean;
  memoryAvailable?: boolean;
  skillsAvailable?: boolean;
  skillFileAccessAvailable?: boolean;
  skillAuthoringAvailable?: boolean;
  deferredToolsAvailable?: boolean;
  programmaticToolsAvailable?: boolean;
  backgroundToolsAvailable?: boolean;
}
/** Derives the model-facing names an unresolved lazy agent can expose without loading it. */
declare function buildHistoricalToolNames(config: BuildHistoricalToolNamesConfig): Set<string>;
/**
* Builds a Set of tool names for use with formatAgentMessages.
*
* In event-driven mode, tools are defined via toolDefinitions (which includes
* deferred tools like tool_search). In legacy mode, tools come from loaded
* tool instances.
*
* This ensures tool_search and other deferred tools are included in the toolSet,
* allowing their ToolMessages to be preserved in conversation history.
*/
declare function buildToolSet(agentConfig: BuildToolSetConfig | null | undefined): Set<string>;
interface RunToolSetConfig extends BuildToolSetConfig, ReachableAgent<RunToolSetConfig> {
  readonly edges?: readonly GraphEdge[];
  readonly accessibleMcpServerNames?: readonly string[];
  readonly historicalMcpServerNames?: readonly string[];
}
/** Builds the historical tool allowlist for the complete effective run topology. */
declare function buildRunToolSet(primaryConfig: RunToolSetConfig | null | undefined, additionalConfigs?: Iterable<RunToolSetConfig | null | undefined> | null, hostGeneratedToolNames?: Iterable<string> | null, historicalMessages?: Iterable<unknown> | null, allowAmbiguousMcpToolNamesWithoutIdentity?: boolean): Set<string>;
interface RegisterCodeExecutionToolsParams {
  toolRegistry: LCToolRegistry$1 | undefined;
  toolDefinitions: LCTool$1[] | undefined;
  /**
  * When `true`, register `bash_tool` alongside `read_file`. When `false`,
  * register `read_file` only — manually-primed skills still need it to
  * load `references/*` files from storage even without a sandbox.
  *
  * Callers:
  * - `initializeAgent` passes `true` iff the `execute_code` capability
  *   is enabled for the run.
  * - `injectSkillCatalog` passes whatever `codeEnvAvailable` resolved to
  *   for the run.
  *
  * Both callers reach this helper in the same `initializeAgent` run
  * sequentially; the registry `.has()` check keeps the second call a
  * no-op so there is exactly one copy of each tool in `toolDefinitions`.
  */
  includeBash: boolean;
  /**
  * When `true`, `read_file` tells the model about skill-file paths
  * (`{skillName}/{path}`). When `false`, it is described only as a
  * code-execution sandbox reader so execute-code-only agents don't get
  * prompted to discover skills that are not enabled for the run.
  */
  includeSkillFileInstructions?: boolean;
  /**
  * When `true`, `read_file` advertises the explicit `workspace/` namespace
  * backed by the selected attached worker, including bounded line pagination.
  */
  workspaceTools?: boolean;
  /** Live operation ceiling for the selected workspace. Omitted for managed runtimes. */
  workspaceOperations?: ReadonlySet<CodeWorkspaceOperation>;
  /** Deployment ceiling advertised on attached Bash tool definitions. */
  workspaceCommandTimeoutMaxMs?: number;
  workspaceEnvironment?: CodeWorkspaceDescriptor["environment"];
  /**
  * When `true`, the registered `bash_tool` description includes the
  * LLM-facing `{{tool<idx>turn<turn>}}` reference syntax guide so the
  * model knows it can substitute prior tool outputs in subsequent
  * commands. Paired with `RunConfig.toolOutputReferences` in `createRun`.
  */
  enableToolOutputReferences?: boolean;
  /**
  * When `true`, the registered `bash_tool` description is the hedged
  * stateful-session variant (workspace usually persists across calls, may
  * reset at any time). Transport routing is resolved independently from the
  * actually executing agent at tool-load time.
  */
  statefulSessions?: boolean;
}
interface RegisterCodeExecutionToolsResult {
  toolDefinitions: LCTool$1[];
  /** Tool names newly registered (skipped names that already existed). */
  registered: string[];
  /**
  * Every tool name this registration manages, whether newly registered or
  * already present. `initializeAgent` records these under the capability
  * marker that triggered the call, so a `tool_options` entry keyed by the
  * marker projects onto exactly the definitions the capability produced —
  * the registrar itself is the source of truth, not a hand-maintained map.
  */
  toolNames: string[];
}
type RegisterFileAuthoringToolsResult = RegisterCodeExecutionToolsResult;
interface RegisterFileAuthoringToolsParams {
  toolRegistry: LCToolRegistry$1 | undefined;
  toolDefinitions: LCTool$1[] | undefined;
  /**
  * When true, descriptions point the model at the skill file namespace
  * (`skills/{skillName}/...`) in addition to sandbox paths. When false,
  * descriptions stay focused on code-execution sandbox files.
  */
  includeSkillFileInstructions?: boolean;
  /** When true, non-skill paths use the attached worker's workspace/ namespace. */
  workspaceTools?: boolean;
  /** Live operation ceiling for the selected workspace. Omitted for managed runtimes. */
  workspaceOperations?: ReadonlySet<CodeWorkspaceOperation>;
}
declare function isFileAuthoringToolDefinition(def: LCTool$1 | undefined): boolean;
/**
* Idempotently registers the code-execution tool pair (`bash_tool` +
* `read_file`) into the run's tool registry and tool-definition list.
* `read_file` can be registered with a code-only description first and
* upgraded to the skill-aware description later in the same run if
* skills are actually injected.
*
* Replaces the legacy `CodeExecutionToolDefinition` / `execute_code`
* registration. `execute_code` as a capability name and as an
* `agent.tools` entry is preserved — it just expands into this tool
* pair at load time so there is only one code-execution tool path
* end-to-end (no same-run dedupe surprises for agents with both
* `execute_code` capability AND skills active).
*/
declare function registerCodeExecutionTools(params: RegisterCodeExecutionToolsParams): RegisterCodeExecutionToolsResult;
declare function registerFileAuthoringTools(params: RegisterFileAuthoringToolsParams): RegisterFileAuthoringToolsResult;
//#endregion
//#region src/agents/added.d.ts
declare const ADDED_AGENT_ID = "added_agent";
interface LoadAddedAgentDeps {
  getAgent: (searchParameter: {
    id: string;
  }) => Promise<Agent | null>;
  getMCPServerTools: (userId: string, serverName: string, serverConfig?: ParsedServerConfig) => Promise<Record<string, unknown> | null>;
  /** The MCP servers this user can reach, with the registry's tier precedence
  *  already applied — the resolution behind the client's catalog. Omitted, the
  *  chat selection is used as sent. */
  getAccessibleMCPServers?: (userId: string, role?: string) => Promise<Record<string, ParsedServerConfig>>;
}
interface LoadAddedAgentParams {
  req: {
    user?: {
      id?: string;
      role?: string;
    };
    config?: Record<string, unknown>;
  };
  conversation: TConversation | null;
  primaryAgent?: Agent | null;
}
/**
* Loads an agent from an added conversation (for multi-convo parallel agent execution).
* Returns the agent config as a plain object, or null if invalid.
*/
declare function loadAddedAgent({
  req,
  conversation,
  primaryAgent
}: LoadAddedAgentParams, deps: LoadAddedAgentDeps): Promise<Agent | null>;
//#endregion
//#region src/agents/load.d.ts
interface LoadAgentDeps {
  getAgent: (searchParameter: {
    id: string;
  }) => Promise<Agent | null>;
  getMCPServerTools: (userId: string, serverName: string, serverConfig?: ParsedServerConfig) => Promise<Record<string, unknown> | null>;
  /** The MCP servers this user can reach, with the registry's tier precedence
  *  already applied — the resolution behind the client's catalog. Omitted, the
  *  chat selection is used as sent. */
  getAccessibleMCPServers?: (userId: string, role?: string) => Promise<Record<string, ParsedServerConfig>>;
}
interface LoadAgentParams {
  req: {
    user?: {
      id?: string;
      role?: string;
    };
    config?: AppConfig;
    body?: {
      promptPrefix?: string;
      ephemeralAgent?: TEphemeralAgent;
    };
  };
  spec?: string;
  agent_id: string;
  endpoint: string;
  model_parameters?: AgentModelParameters & {
    model?: string;
  };
}
/**
* Load an ephemeral agent based on the request parameters.
*/
declare function loadEphemeralAgent({
  req,
  spec,
  endpoint,
  model_parameters: _m
}: Omit<LoadAgentParams, "agent_id">, deps: LoadAgentDeps): Promise<Agent | null>;
/**
* Load an agent based on the provided ID.
* For ephemeral agents, builds a synthetic agent from request parameters.
* For persistent agents, fetches from the database.
*/
declare function loadAgent(params: LoadAgentParams, deps: LoadAgentDeps): Promise<Agent | null>;
//#endregion
//#region src/agents/sender.d.ts
/**
* Resolves the display name persisted as `message.sender` for a response.
*
* Real agents use their own name. Ephemeral agents run the same label chain
* `loadEphemeralAgent` encodes into their id (`modelLabel` → spec label →
* endpoint `modelDisplayLabel`), recomputed here from the exact source values
* rather than decoded from the id — the id encoding is lossy for labels
* containing `__`/`___` sequences, and the persisted sender must match the
* configured label verbatim. `getResponseSender` remains the fallback for
* label-less agents (model-derived names such as `Claude` or `GPT-5`, family
* heuristics, `'AI'`).
*/
declare function resolveSender({
  agent,
  specLabel,
  endpointOption
}: {
  agent: Partial<Pick<Agent, "id" | "name">>;
  specLabel?: string | null;
  endpointOption: Partial<TEndpointOption>;
}): string;
//#endregion
//#region src/agents/hitl/policy.d.ts
/**
* Layered sources that combine into the effective tool-approval policy for a turn.
*
* Endpoint policy remains the administrative baseline. Attached code environments
* also activate TerraMind's built-in BYOM baseline; `agent` and `skills` remain
* reserved seams for future persisted overrides.
*/
interface ToolApprovalPolicyLayers {
  /**
  * App/endpoint policy — `endpoints.agents.toolApproval` from librechat.yaml.
  * The baseline, and the sole owner of the `enabled` kill switch.
  */
  endpoint?: TToolApprovalPolicy;
  /**
  * Per-agent override (not yet wired). Layered over `endpoint` to refine
  * `mode`/`allow`/`deny`/`ask`/`reason` for a specific agent. Must NOT flip
  * `enabled` — enablement stays endpoint-level by design.
  */
  agent?: TToolApprovalPolicy;
  /**
  * Skill-contributed policy (not yet wired). May only TIGHTEN — contribute
  * `ask`/`deny` entries — never grant `bypass` or widen `allow`, so a selected
  * skill can never silently auto-approve a tool.
  */
  skills?: TToolApprovalPolicy[];
  /**
  * At least one agent in this run executes in an attached, user-operated environment.
  * Attached environments get TerraMind's safe approval baseline without requiring
  * an administrator to opt the whole endpoint into prompts.
  */
  attachedCodeEnvironment?: boolean;
}
/**
* Resolve the effective tool-approval policy for a turn from its layered sources.
*
* This is the single seam where policy sources combine, kept out of the run call
* site so adding per-agent or per-skill policy later is a change to ONE function
* rather than to `createRun`. Intended precedence once those layers are wired:
*   - `endpoint` is the baseline and owns the `enabled` kill switch;
*   - `agent` overrides `mode`/`allow`/`deny`/`ask`/`reason`;
*   - `skills` may only tighten (add `ask`/`deny`), never loosen.
*
* When no endpoint policy is active, BYOM adds `enabled: true, mode: 'bypass'` and
* an agent-scoped hook supplies its coding decisions. An already-enabled endpoint
* policy remains the run-wide administrative baseline, including its unmatched-tool
* mode. An explicit endpoint `enabled: false` remains the administrator emergency
* override. `agent`/`skills` are accepted but not yet merged.
*/
declare function resolveToolApprovalPolicy(layers: ToolApprovalPolicyLayers): TToolApprovalPolicy | undefined;
/**
* Whether the HITL machinery should run for this policy.
*
* HITL remains default-off for the rollout; `enabled: true` is the explicit
* opt-in. Users wanting "stop asking me" after opting in should use
* `mode: 'bypass'` instead, which keeps the machinery in place but auto-approves.
*
* **Wiring caveat (Slice B):** when this returns `true` and the host passes
* `humanInTheLoop: { enabled: true }` to `Run.create`, the host MUST also
* supply `compileOptions.checkpointer` with a durable saver
* (`TerraMindCheckpointSaver`). Otherwise the SDK installs a process-local
* `MemorySaver` fallback, which silently breaks resume across worker hops in
* any multi-process deployment. Pair this predicate with the checkpointer
* assignment at the `Run.create` call site.
*/
declare function isHITLEnabled(policy: TToolApprovalPolicy | undefined): policy is NonNullable<TToolApprovalPolicy>;
/**
* Whether the configured policy can structurally return `ask` for any tool.
*
* `bypass` and `dontAsk` are non-pausing fallbacks; only an explicit ask rule
* or a programmatic hook can tighten them to `ask`. A catch-all deny remains
* non-pausing because deny wins over every hook/rule, while a catch-all allow
* removes the default mode's unmatched-tool ask fallback. More-specific pattern
* overlap is intentionally treated conservatively because the run's complete
* lazy tool surface is not known at admission time.
*/
declare function isToolApprovalPauseCapable(policy: TToolApprovalPolicy | undefined, hasProgrammaticHooks?: boolean, toolNames?: readonly string[]): boolean;
/** Whether an enabled static policy unconditionally denies one concrete tool name. */
declare function isToolDeniedByApprovalPolicy(policy: TToolApprovalPolicy | undefined, toolName: string): boolean;
/**
* Extends each `toolApproval` pattern list with the names of tools whose
* OTHER spelling matches, so admin YAML keeps applying when a tool's key
* spelling changed in either direction: patterns written against pre-strip
* upstream naming reach the stripped instances (a non-matching `deny` would
* otherwise FAIL OPEN), and patterns written against the current catalog
* naming reach legacy-named instances retained by unedited agents. Healing
* is list-level (literal names appended, patterns never rewritten), so
* `deny`/`ask`/`allow` precedence semantics are unchanged, and a name
* already matched by its own list is skipped.
*/
declare function healToolApprovalPolicy(policy: TToolApprovalPolicy | undefined, aliases: readonly MCPToolAlias[]): TToolApprovalPolicy | undefined;
/**
* Names whose OTHER spelling matches a programmatic hook's regex matcher
* while their own name does not — the hook must also fire for these or its
* argument-, user-, or tenant-specific deny/ask decisions are silently
* skipped for renamed tools. Mirrors the SDK's unanchored `new RegExp(pattern)`
* matcher semantics; an invalid pattern matches nothing there, so it aliases
* nothing here.
*/
declare function collectAliasMatcherNames(matcher: string | undefined, aliases: readonly MCPToolAlias[]): string[];
/** Anchored exact-name pattern for the alias-matched names of one hook matcher. */
declare function buildAliasMatcherPattern(names: readonly string[]): string;
declare function mapToolApprovalPolicy(policy: TToolApprovalPolicy | undefined): ToolPolicyConfig | undefined;
/** Tool-call shape consumed by {@link buildToolApprovalPayload}. */
interface ToolApprovalCallInput {
  name: string;
  arguments: string | Record<string, unknown>;
  tool_call_id: string;
  description?: string;
}
/**
* Build a tool-approval interrupt payload from one or more paused tool calls.
*
* Mirrors the SDK's `ToolApprovalInterruptPayload` shape so this can be used
* to synthesize payloads in tests, or by the host before the SDK upgrade ships.
*/
declare function buildToolApprovalPayload(toolCalls: ToolApprovalCallInput[], decisionsByToolName?: Record<string, Agents.ToolApprovalDecisionType[]>): Agents.ToolApprovalInterruptPayload;
/** Require one uniquely identified review policy for every paused tool call. */
declare function isToolApprovalPayloadValid(payload: Agents.ToolApprovalInterruptPayload): boolean;
/** Build an ask-user-question interrupt payload. */
declare function buildAskUserQuestionPayload(question: Agents.AskUserQuestionRequest): Agents.AskUserQuestionInterruptPayload;
/** Job-context fields wrapped around a {@link Agents.HumanInterruptPayload}. */
interface PendingActionContext {
  streamId: string;
  conversationId?: string;
  /** Stable per-turn identifier (e.g. responseMessageId or LangGraph checkpoint_ns). */
  runId?: string;
  responseMessageId?: string;
  /** Optional TTL (ms). When set, `expiresAt = createdAt + ttlMs`. */
  ttlMs?: number;
  /** Optional absolute upper bound inherited from an enclosing event binding. */
  expiresAt?: Date | string | number;
  /** Override actionId; defaults to a fresh uuid. */
  actionId?: string;
  /** SDK interrupt id (`RunInterruptResult.interruptId`) for cross-process resume. */
  interruptId?: string;
  /** LangGraph `thread_id` (`RunInterruptResult.threadId`) for cross-process resume. */
  threadId?: string;
  /** Fingerprint of the graph-determining request fields; see {@link computeAgentRequestFingerprint}. */
  requestFingerprint?: string;
  /** Current fingerprint; the legacy field remains populated for rolling-deploy compatibility. */
  requestFingerprintV2?: string;
  /** Graph-determining fields to replay on resume; see {@link RESUME_CONTEXT_KEYS}. */
  resumeContext?: Record<string, unknown>;
  /** Opaque server-only binding to the stateful code targets selected at pause time. */
  codeExecutionBinding?: Agents.CodeExecutionApprovalBinding;
}
/** Request fields that decide which agent/graph + tool set a turn runs. */
interface AgentRequestFingerprintFields {
  endpoint?: string | null;
  endpointType?: string | null;
  agent_id?: string | null;
  model?: string | null;
  spec?: string | null;
  /** Ephemeral agents derive their system instructions from this; pin it too. */
  promptPrefix?: string | null;
  ephemeralAgent?: Record<string, unknown> | null;
  codeApprovalMode?: string | null;
  codeEnvironmentMode?: string | null;
  codeWorkspaces?: unknown;
}
/**
* Fingerprint the request fields that determine which agent/graph + tool set a turn
* runs. Persisted on the pending action at pause time and recomputed on resume; a
* mismatch means the resume would rebuild a DIFFERENT graph. This is the guard that
* catches an ephemeral-agent config swap — those have an undefined `agent_id`, so the
* id check alone can't tell two ephemeral configs apart.
*/
/**
* Request fields that determine the agent/graph + tool set, persisted with the pending
* action so the resume can REPLAY them server-side. The client can't reliably re-send
* these after a reload (e.g. the ephemeralAgent state resets), so replaying them from
* the job guarantees the rebuilt run is the SAME graph the pause used — durable resume
* works across reloads/replicas, and a crafted resume can't swap the tool set.
*/
declare const RESUME_CONTEXT_KEYS: readonly ["endpoint", "endpointType", "agent_id", "spec", "model", "promptPrefix", "ephemeralAgent", "codeApprovalMode", "codeEnvironmentMode", "codeWorkspaces", "addedConvo", "timezone", "manualSkills", "modelLabel"];
type ResumeContext = Partial<Record<(typeof RESUME_CONTEXT_KEYS)[number], unknown>> & {
  /** Resolved model params captured at pause (sanitized); replayed by the resume route. */model_parameters?: Record<string, unknown>;
};
/**
* Strip credentials and server transport config from resolved model parameters before
* they are persisted for resume replay. The initialized agent's `model_parameters` are
* the resolved `llmConfig` — they carry provider secrets (`apiKey`, Azure key names,
* Google `authOptions`, Bedrock `credentials`) and gateway config (`configuration`,
* headers, base URLs). Resume re-resolves all of those server-side from env/config, so
* only the user-level generation params (temperature, max tokens, custom endpoint
* params, …) need to survive the round trip. Provider-format params that conflict with
* the request-body schema on replay are normalized back to the UI form (see
* {@link normalizeThinkingParam}).
*/
declare function sanitizeResumeModelParameters(params: unknown): Record<string, unknown> | undefined;
/**
* Capture the model parameters to replay on resume. The paused request body is the
* primary source — its fields are UI-form by construction (they already round-tripped
* `parseCompactConvo` on the original turn), so replaying them can't trip the schema.
* The resolved llmConfig only fills gaps: it's provider-format, where params are
* renamed (`maxOutputTokens` → `maxTokens`, `top_p` → `topP`), relocated
* (`effort` → `invocationKwargs`), or retyped (`thinking` → object) — the schema
* silently drops or, worse, fails on them (see the `normalize*` helpers, #14253).
*/
declare function captureResumeModelParameters(body: Record<string, unknown> | undefined | null, resolvedParams: unknown): Record<string, unknown> | undefined;
/** Extract the graph-determining fields from a request body for durable replay. */
declare function pickResumeContext(body: Record<string, unknown> | undefined | null): ResumeContext;
/**
* Replay a persisted resume context onto a request body so the rebuilt run matches the
* paused one. Every graph-determining field is forced to the persisted value: a key the
* context HAS overwrites whatever the client sent; a key it LACKS is deleted from the
* body. The delete is the security half — without it, a field the paused turn never
* carried (e.g. `addedConvo`, which {@link computeAgentRequestFingerprint} does NOT
* cover) could be injected by a crafted resume to rebuild the paused single-agent
* checkpoint as a different multi-agent graph/tool set.
*/
declare function applyResumeContext(body: Record<string, unknown> | undefined | null, ctx: ResumeContext | undefined | null): void;
/**
* Replay captured generation parameters without allowing provider configuration to
* replace routing, graph identity, or resume-action fields. This guard also makes
* pending actions captured by older versions safe to resume after an upgrade.
*/
declare function applyResumeModelParameters(body: Record<string, unknown> | undefined | null, params: unknown): void;
declare function computeAgentRequestFingerprint(fields: AgentRequestFingerprintFields): string;
/**
* Fingerprint understood by replicas predating conversation-owned code environments.
* Writers retain it in `requestFingerprint` while also storing the stricter current
* fingerprint, allowing either replica generation to resume safely during a rollout.
*/
declare function computeLegacyAgentRequestFingerprint(fields: AgentRequestFingerprintFields): string;
/**
* Wrap a HumanInterruptPayload (from the SDK or synthesized locally) as a
* {@link Agents.PendingAction} record persisted with the job.
*
* Accepts both interrupt categories (`tool_approval` and `ask_user_question`)
* via the discriminated union — the host doesn't need to branch.
*/
declare function buildPendingAction(payload: Agents.HumanInterruptPayload, ctx: PendingActionContext): Agents.PendingAction;
/**
* Client-facing projection of a pending action. `requestFingerprint`, `resumeContext`, and
* `codeExecutionBinding`
* are server-only replay state — `resumeContext` in particular carries the resolved
* model parameters — so every copy that leaves the server (SSE, status, resume state)
* must go through this. The full record stays in the job store for the resume route.
*/
declare function toClientPendingAction(pendingAction: Agents.PendingAction | undefined | null): Agents.PendingAction | undefined;
/**
* Exempt `ask_user_question` from the tool-approval prompt unless the admin
* mentioned it EXPLICITLY (in `allow`, `ask`, or `deny`). With approval enabled
* in its default prompt-everything mode, the ask tool would otherwise trigger
* an approval card for the act of asking a question — a double pause with no
* safety upside: the tool is side-effect-free by construction (it only asks;
* the payload is length-capped and rendered as text). An explicit admin entry
* still wins, in either direction.
*/
declare function exemptAskUserQuestionFromApproval(policy: TToolApprovalPolicy | undefined, toolName: string): TToolApprovalPolicy | undefined;
//#endregion
//#region src/agents/hooks/schema.d.ts
interface PluginHookHandler {
  type: string;
  command?: string;
  commandWindows?: string;
  args?: string[];
  shell?: "bash" | "powershell";
  url?: string;
  headers?: Record<string, string>;
  allowedEnvVars?: string[];
  server?: string;
  tool?: string;
  input?: Record<string, unknown>;
  prompt?: string;
  model?: string;
  timeout?: number;
  statusMessage?: string;
  if?: string;
  once?: boolean;
  continueOnBlock?: boolean;
  async?: boolean;
  asyncRewake?: boolean;
  rewakeMessage?: string;
  rewakeSummary?: string;
}
interface PluginHookGroup {
  matcher?: string;
  if?: string;
  hooks: PluginHookHandler[];
}
interface PluginHooksDocument {
  $schema?: string;
  description?: string;
  hooks: Record<string, PluginHookGroup[]>;
}
declare const pluginHookHandlerSchema: z.ZodType<PluginHookHandler>;
declare const pluginHookGroupSchema: z.ZodType<PluginHookGroup>;
declare const pluginHooksDocumentSchema: z.ZodType<PluginHooksDocument>;
interface PluginHookValidationIssue {
  path: string;
  message: string;
}
type PluginHooksParseResult = {
  success: true;
  document: PluginHooksDocument;
} | {
  success: false;
  issues: PluginHookValidationIssue[];
};
/** Parse Claude's plugin-specific `{"hooks": {...}}` wrapper. */
declare function parsePluginHooks(input: unknown): PluginHooksParseResult;
//#endregion
//#region src/agents/hooks/compatibility.d.ts
type PluginHookIssueSeverity = "warning" | "error";
type PluginHookIssueCode = "event_alias" | "unsupported_event" | "unsupported_handler" | "invalid_matcher" | "matcher_translated" | "unmapped_matcher" | "unmapped_tool_name" | "unsupported_matcher" | "unsupported_condition" | "conflicting_condition" | "unsupported_async" | "unsupported_async_rewake" | "unsupported_continue_on_block" | "unsupported_handler_event" | "unsupported_session_lifecycle" | "unsupported_session_source" | "unsupported_event_payload" | "unsupported_event_output" | "long_timeout";
interface PluginHookCompatibilityIssue {
  code: PluginHookIssueCode;
  severity: PluginHookIssueSeverity;
  message: string;
}
type PluginHookHandlerType = "command" | "prompt";
interface PluginHookMatcherTranslation {
  sourceEvent: string;
  targetEvent: HookEvent;
  /** Authored regex, or a canonical pipe-separated list when Claude applies exact matching. */
  matcher: string;
}
interface PluginHookMatcherTranslationResult {
  /** Runtime pattern; translations of Claude exact matchers are whole-string anchored. */
  matcher: string;
  requiresToolNameTranslation?: boolean;
  /**
  * Runtime tool names the translation produced. Reverse payload translation
  * applies per invocation to exactly these names, so a mixed matcher like
  * `Bash|create_file` keeps native payloads for its natively-authored
  * alternative. Omitted, translation applies to the whole declaration.
  */
  translatedToolNames?: string[];
}
interface PluginHookToolNameTranslation {
  sourceEvent: string;
  targetEvent: HookEvent;
  toolName: string;
}
interface PluginHookToolInputTranslation {
  sourceEvent: string;
  targetEvent: HookEvent;
  /** TerraMind runtime name of the invoked tool. */
  toolName: string;
  toolInput: Record<string, unknown>;
}
interface PluginHookHandlerSupport {
  sourceEvent: string;
  handler: PluginHookHandler;
}
interface PluginHookConditionMatch {
  sourceEvent: string;
  targetEvent: HookEvent;
  condition: string;
  toolName: string;
  toolInput: Record<string, unknown>;
}
interface PluginHookCapabilities {
  handlerTypes: ReadonlySet<PluginHookHandlerType>;
  translateMatcher?: (input: PluginHookMatcherTranslation) => string | PluginHookMatcherTranslationResult | undefined;
  /** Maps a TerraMind runtime tool name back into the plugin's source namespace. */
  toPluginToolName?: (input: PluginHookToolNameTranslation) => string;
  /** Presents a runtime tool input under the plugin's source field names. */
  toPluginToolInput?: (input: PluginHookToolInputTranslation) => Record<string, unknown>;
  /** Returns an error message when the executor cannot run this handler on the current host. */
  supportsHandler?: (input: PluginHookHandlerSupport) => string | undefined;
  /** Evaluates Claude permission-rule syntax before a conditional handler executes. */
  matchCondition?: (input: PluginHookConditionMatch) => boolean;
  async?: boolean;
  asyncRewake?: boolean;
  sessionLifecycle?: boolean;
}
interface PluginHookPlanEntry {
  sourceEvent: string;
  targetEvent?: HookEvent;
  groupIndex: number;
  handlerIndex: number;
  sourceMatcher?: string;
  matcher?: string;
  /**
  * Set when the matcher was authored against the plugin's alias namespace
  * and translated to runtime tool names; payload name/input reverse
  * translation applies only to such declarations — a native-authored
  * matcher keeps native payloads.
  */
  requiresToolNameTranslation?: boolean;
  /** Runtime tool names the translation produced; see the translation result type. */
  translatedToolNames?: string[];
  condition?: string;
  timeoutMs?: number;
  handler: PluginHookHandler;
  status: "ready" | "unsupported";
  issues: PluginHookCompatibilityIssue[];
}
interface PluginHookPlanSummary {
  declared: number;
  ready: number;
  unsupported: number;
}
interface PluginHookPlan {
  description?: string;
  entries: PluginHookPlanEntry[];
  summary: PluginHookPlanSummary;
}
declare function planPluginHooks(document: PluginHooksDocument, capabilities: PluginHookCapabilities): PluginHookPlan;
//#endregion
//#region src/agents/hooks/runtime.d.ts
interface PluginHookRuntimeContext {
  sessionId?: string;
  /** Authenticated principal owning the run; scopes cross-run dedup keys. */
  userId?: string;
  /**
  * Session working directory for the payload's `cwd`. TerraMind runs supply
  * none: tool paths address a remote code-execution sandbox, not the API
  * host where hook commands run, so no host directory describes the run.
  * Hook commands resolve their own paths from `PLUGIN_ROOT`, which is also
  * the process working directory.
  */
  cwd?: string;
  transcriptPath?: string | null;
  permissionMode?: string;
  sessionStartSource?: string;
  model?: string;
  agentType?: string;
}
interface PluginHookBatchToolCall {
  tool_name: string;
  tool_input: Record<string, unknown>;
  tool_use_id: string;
  tool_response?: unknown;
}
interface PluginHookPayload {
  hook_event_name: string;
  session_id: string;
  run_id: string;
  thread_id?: string;
  cwd?: string;
  transcript_path?: string | null;
  permission_mode?: string;
  source?: string;
  model?: string;
  agent_id?: string;
  executing_agent_id?: string;
  prompt?: string;
  tool_name?: string;
  tool_input?: Record<string, unknown>;
  tool_use_id?: string;
  tool_response?: unknown;
  error?: string;
  last_assistant_message?: string;
  reason?: string;
  agent_type?: string;
  stop_hook_active?: boolean;
  trigger?: string;
  compact_summary?: string;
  messages_before_count?: number;
  messages_after_count?: number;
  tool_calls?: PluginHookBatchToolCall[];
}
interface PluginHookExecutionRequest {
  pluginId: string;
  sourceEvent: string;
  targetEvent: HookEvent;
  handler: PluginHookHandler;
  /** Declaration position in the source document; distinguishes identical handlers under different matchers. */
  groupIndex: number;
  handlerIndex: number;
  condition?: string;
  input: HookInput;
  payload: PluginHookPayload;
}
/**
* Host-owned boundary for executing a planned hook.
* This compatibility layer never launches plugin code in the TerraMind API process.
*/
interface PluginHookExecutor {
  capabilities: PluginHookCapabilities;
  /**
  * Pre-execution gate consulted before the declaration claims its per-input
  * dedup slot. A suppressed declaration (for example, once-state already
  * recorded) must decline here rather than no-op inside `execute`, so an
  * identical handler under an overlapping matcher can still claim the slot
  * and fire independently.
  */
  shouldExecute?(request: PluginHookExecutionRequest): boolean | Promise<boolean>;
  execute(request: PluginHookExecutionRequest, signal: AbortSignal): HookOutput | Promise<HookOutput>;
}
interface RegisterPluginHooksOptions {
  pluginId: string;
  registry: HookRegistry;
  document: PluginHooksDocument;
  executor: PluginHookExecutor;
  /**
  * Plan already computed from `document` with the SAME executor capabilities
  * (e.g. at plugin load). Supplying it skips re-planning up to 512 handlers
  * on every run; omitted, the document is planned here.
  */
  plan?: PluginHookPlan;
  context?: PluginHookRuntimeContext;
}
interface PluginHookRegistration {
  plan: PluginHookPlan;
  registered: number;
  unregister: () => void;
}
interface PluginHookPayloadState {
  compactTrigger?: string;
  toPluginToolName?: (toolName: string) => string;
  toPluginToolInput?: (toolName: string, toolInput: Record<string, unknown>) => Record<string, unknown>;
}
declare function createPluginHookPayload(sourceEvent: string, input: HookInput, context?: PluginHookRuntimeContext, state?: PluginHookPayloadState): PluginHookPayload;
declare function registerPluginHooks(options: RegisterPluginHooksOptions): PluginHookRegistration;
//#endregion
//#region src/agents/hooks/source.d.ts
interface PluginHookSourceOptions {
  registry: HookRegistry;
  context?: PluginHookRuntimeContext;
  /** Whether the run can raise resumable `ask` interrupts (HITL wiring attached). */
  askDecisionSupported?: boolean;
}
/**
* Host-supplied provider of plugin hooks for agent runs. Keeps the dependency
* direction one-way: the run seam reads hooks through this seam while the
* plugins package (which imports agents code) registers the implementation at
* startup — mirroring the tool-approval hook registry pattern.
*/
interface PluginHookSource {
  hasHooks(): boolean;
  /** Whether a ready deployment hook can participate in tool-approval decisions. */
  hasToolApprovalHooks?(toolNames?: readonly string[]): boolean;
  register(options: PluginHookSourceOptions): number;
}
declare function setPluginHookSource(next: PluginHookSource | undefined): void;
declare function getPluginHookSource(): PluginHookSource | undefined;
//#endregion
//#region src/agents/hitl/admission.d.ts
interface ApprovalToolReference {
  readonly name?: string;
}
interface ApprovalToolRegistry {
  keys(): Iterable<string>;
  has(name: string): boolean;
}
interface ApprovalSubagentGraph {
  readonly memberConfigs?: readonly (ToolApprovalAdmissionAgent | null | undefined)[];
}
interface ToolApprovalAdmissionAgent {
  readonly id?: string;
  readonly tools?: readonly (string | ApprovalToolReference)[];
  readonly toolRegistry?: ApprovalToolRegistry;
  readonly toolDefinitions?: readonly ApprovalToolReference[];
  readonly mcpToolAliases?: readonly MCPToolAlias[];
  readonly subagentAgentConfigs?: readonly (ToolApprovalAdmissionAgent | null | undefined)[];
  readonly lazySubagentConfigs?: readonly (ToolApprovalAdmissionAgent | null | undefined)[];
  readonly subagentGraphMemberMetadata?: readonly (ToolApprovalAdmissionAgent | null | undefined)[];
  readonly subagentGraphConfigs?: readonly ApprovalSubagentGraph[];
}
interface ToolApprovalAdmissionInput {
  readonly policy: TToolApprovalPolicy | undefined;
  readonly agents: readonly (ToolApprovalAdmissionAgent | null | undefined)[];
  readonly hostGeneratedToolNames?: readonly string[];
  readonly resolvedProgrammaticHooks?: readonly ResolvedToolApprovalHook[];
  readonly pluginHookSource?: PluginHookSource;
  readonly askUserQuestionAdminDisabled?: boolean;
}
/**
* Whether an initialized run can pause through tool approval or a top-level
* `ask_user_question`. Eager tools are matched exactly across every subagent
* form; unresolved lazy surfaces are classified conservatively. The interrupt
* boundary remains the final fail-closed durability check.
*/
declare function canAgentGraphPause({
  policy,
  agents,
  hostGeneratedToolNames,
  resolvedProgrammaticHooks,
  pluginHookSource,
  askUserQuestionAdminDisabled
}: ToolApprovalAdmissionInput): boolean;
/**
* Whether `createRun` attaches a checkpointer for this initialization.
* Cleanup follows attachment, not current pause capability: a retry must not
* restore remnants written before a policy or request-hook change.
*/
declare function agentRunUsesCheckpointer({
  policy,
  agents,
  askUserQuestionAdminDisabled
}: Pick<ToolApprovalAdmissionInput, "policy" | "agents" | "askUserQuestionAdminDisabled">): boolean;
//#endregion
//#region src/agents/hitl/runtime.d.ts
/** Stable across resumes; the job epoch separates edits that reuse a response id. */
declare function buildToolApprovalExecutionConfig(responseMessageId: string, jobCreatedAt?: number): {
  [TOOL_APPROVAL_EXECUTION_SCOPE_CONFIG_KEY]: string;
};
/**
* The HITL fragment spread onto a `RunConfig` when tool approval is enabled.
*
* Kept as one object so the run seam attaches the opt-in switch and the policy
* hook together — they're meaningless apart. The checkpointer is resolved
* separately (it's an async, process-wide singleton) and merged into
* `graphConfig.compileOptions` at the call site.
*/
interface HITLRunWiring {
  humanInTheLoop: {
    enabled: true;
  };
  hooks: HookRegistry;
  /** Adds aliases discovered while a lazy subagent resolves. */
  addMCPToolAliases: (aliases: readonly MCPToolAlias[], policy: TToolApprovalPolicy | undefined) => void;
}
/**
* Assemble the run-level HITL wiring for a tool-approval policy, or `undefined`
* when HITL is disabled (the default) — in which case the run attaches nothing
* and behaves exactly as it did before this feature.
*
* The returned `hooks` registry carries the static-config `PreToolUse` policy hook built
* from {@link mapToolApprovalPolicy} (an enabled policy with no allow/deny/ask lists falls
* through to `mode: 'default'`, i.e. every tool prompts — the safe default for "HITL on,
* nothing else specified"), PLUS any host-registered programmatic hooks
* ({@link registerToolApprovalHook}) resolved against `context`. The static hook is
* registered first as the baseline; host hooks layer after it. Decisions fold in the SDK
* as `deny` > `ask` > `allow`, so a host hook can only TIGHTEN the configured policy.
*/
declare function buildHITLRunWiring(policy: TToolApprovalPolicy | undefined, context?: ToolApprovalHookContext, mcpToolAliases?: readonly MCPToolAlias[], resolvedProgrammaticHooks?: readonly ResolvedToolApprovalHook[]): HITLRunWiring | undefined;
//#endregion
//#region src/agents/hitl/inspection.d.ts
type ResumeMessage = StoredMessageContentInput & {
  id?: string;
  messageId?: string;
  conversationId?: string | null;
  parentMessageId?: string | null;
  isCreatedByUser?: boolean;
  isUserSubmitted?: boolean;
  userSubmittedPaths?: string[];
  userSubmittedMessageFieldPaths?: UserSubmittedMessageFieldPath[];
};
type ResumeSubmittedMessage = ExternalChatMessage & {
  readonly id?: string;
  readonly messageId?: string;
  readonly files?: readonly object[];
  readonly attachments?: readonly object[];
  readonly file_ids?: readonly string[];
};
type ResumeFile = CanonicalFileInspectionFile;
interface DynamicToolContextAgent {
  readonly dynamicToolContextMap?: Readonly<Record<string, unknown>>;
}
interface ResumeSnapshotAgent extends Agent {
  subagentAgentConfigs?: ResumeSnapshotAgent[];
}
interface ResumeAgentSnapshotInput {
  primaryAgent: Agent;
  additionalRoots?: readonly Agent[];
  primaryModelParameters?: Readonly<Record<string, unknown>>;
  subagentsEnabled: boolean;
  getAgent: (filter: {
    id: string;
  }) => Promise<Agent | null>;
  canAccessAgent?: (agent: Agent) => Promise<boolean>;
}
type GetResumeMessages = (filter: {
  conversationId: string;
  user?: string;
}) => Promise<ResumeMessage[] | null | undefined>;
interface ResumeContentInspectionInput {
  readonly onTraversalFailure?: LocatorTraversalReporter;
  appConfig?: AppConfig;
  conversationId: string;
  targetMessageId?: string | null;
  user?: Pick<IUser, "id" | "tenantId">;
  supplementalMessages: ResumeMessage[];
  submittedMessages: ResumeSubmittedMessage[];
  /** Additional checkpoint/runtime structures that may contain durable file references. */
  fileReferenceInputs?: readonly object[];
  liveFiles: ResumeFile[];
  /** True only for files assembled from server-side attachment hydration.
  * Request/job metadata must leave this false. */
  trustLiveFileContent?: boolean;
  isTemporary: boolean;
  getMessages: GetResumeMessages;
  getFiles: GetCanonicalFilesForInspection;
}
interface ResumeContentInspection {
  storedMessages: ResumeMessage[];
  submittedMessages: ResumeSubmittedMessage[];
  originalStoredMessages: ResumeMessage[];
  hydratedFiles: ResumeFile[];
  hydratedFilters?: FiltersConfig;
}
declare function getDynamicToolContexts(agents: readonly DynamicToolContextAgent[]): string[];
/**
* Reconstructs the current saved agent graph with owner-scoped reads only.
* It intentionally stops before initialization, so tool/provider setup and
* resource usage mutations remain behind the approval claim.
*/
declare function getResumeAgentSnapshot({
  primaryAgent,
  additionalRoots,
  primaryModelParameters,
  subagentsEnabled,
  getAgent,
  canAccessAgent
}: ResumeAgentSnapshotInput): Promise<ResumeSnapshotAgent[]>;
declare function getResumeContentInspection(input: ResumeContentInspectionInput): Promise<ResumeContentInspection>;
//#endregion
//#region src/agents/hitl/protection.d.ts
interface ResumeToolCall {
  readonly id?: string;
  readonly name?: unknown;
  readonly args?: unknown;
  readonly arguments?: unknown;
  readonly output?: unknown;
  readonly function?: {
    readonly name?: unknown;
    readonly arguments?: unknown;
    readonly output?: unknown;
  };
  readonly code_interpreter?: {
    readonly input?: unknown;
    readonly outputs?: unknown;
  };
}
interface ResumeContentPart {
  readonly type?: string;
  readonly text?: string | {
    readonly value?: string;
    readonly [key: string]: unknown;
  };
  readonly tool_call?: ResumeToolCall;
  readonly [key: string]: unknown;
}
interface ResumeCheckpointMessage {
  readonly role?: string;
  readonly name?: string;
  readonly text?: string;
  readonly content?: string | readonly ResumeContentPart[];
  readonly tool_calls?: readonly ResumeToolCall[];
  readonly files?: readonly object[];
  readonly attachments?: readonly object[];
  readonly file_ids?: readonly string[];
  readonly metadata?: object;
  readonly additional_kwargs?: {
    readonly skillName?: string;
    readonly sourceMessageId?: string;
    readonly sourceMessageIds?: readonly string[];
    readonly provenance?: {
      readonly parts?: readonly {
        readonly attribution?: string;
        readonly sourceMessageId?: string;
      }[];
    };
    readonly tool_calls?: readonly ResumeToolCall[];
  };
  readonly _getType?: () => string;
}
interface ResumeCheckpointer {
  getTuple(config: {
    configurable: Readonly<Record<string, string>>;
  }): Promise<{
    checkpoint?: {
      channel_values?: {
        messages?: readonly ResumeCheckpointMessage[];
      };
    };
  } | null | undefined>;
}
type ResumeAction = Action & AssistantActionContentInput;
interface ResumeEndpointOption extends Pick<Partial<TEndpointOption>, "agent" | "model_parameters"> {
  readonly addedConvo?: {
    readonly agent_id?: TConversation["agent_id"];
  };
}
type ResumeValue = ToolApprovalDecisionMap | AskUserQuestionResolution;
interface ResumeProtectionUser extends IUser {
  readonly id: IUser["id"];
  readonly role: IUser["role"];
}
interface ResumeProtectionConfig extends Omit<AppConfig, "messageFilter"> {
  readonly messageFilter?: {
    readonly pii?: MessageFilterPiiConfig;
  };
}
interface ResumeContentProtectionDependencies {
  readonly onTraversalFailure?: LocatorTraversalReporter;
  getAgentCheckpointer: (config: TCheckpointerConfig | undefined) => Promise<ResumeCheckpointer | undefined>;
  checkAccess: (params: CheckAccessParams) => Promise<boolean>;
  getMessages: ResumeContentInspectionInput["getMessages"];
  getFiles: ResumeContentInspectionInput["getFiles"];
  getAgent: (filter: {
    id: string;
  }) => Promise<Agent | null | undefined>;
  getActions: (query: {
    agentId: string[];
  }, includeSensitive: boolean) => Promise<ResumeAction[] | null | undefined>;
  getUserMemories: (params: {
    userId: string;
    agentId?: string;
  }) => Promise<MemoryContentInput[]>;
  getRoleByName: CheckAccessParams["getRoleByName"];
  decryptMetadata: (metadata: Action["metadata"]) => Promise<Action["metadata"]>;
  canAccessAgent: (agent: Agent, user: ResumeProtectionUser) => Promise<boolean>;
}
interface AssertResumeContentAllowedInput {
  readonly appConfig?: ResumeProtectionConfig;
  readonly endpointOption?: ResumeEndpointOption;
  readonly conversationId: string;
  readonly targetMessageId?: string | null;
  readonly user: ResumeProtectionUser;
  readonly storedMessages: ResumeContentInspectionInput["supplementalMessages"];
  readonly seedContent: readonly ResumeContentPart[];
  readonly resumeValue?: ResumeValue | null;
  readonly liveFiles: ResumeContentInspectionInput["liveFiles"];
  readonly isTemporary: boolean;
  readonly checkpointNamespace?: string;
  readonly resolvedAddedAgent?: Agent | null;
}
interface AssertResumeRuntimeContentAllowedInput extends Pick<AssertResumeContentAllowedInput, "appConfig" | "conversationId" | "targetMessageId" | "user" | "storedMessages" | "seedContent" | "resumeValue" | "liveFiles" | "isTemporary" | "checkpointNamespace"> {
  readonly agents: NonNullable<ModelBoundContentInput["agents"]>;
  readonly files: NonNullable<ModelBoundContentInput["files"]>;
}
type ResumeRuntimeContentProtectionDependencies = Pick<ResumeContentProtectionDependencies, "getAgentCheckpointer" | "getMessages" | "getFiles" | "onTraversalFailure">;
interface ResumeRuntimeContentProjection {
  readonly resolvedFiles: ResumeContentInspection["hydratedFiles"];
  readonly checkpointFiles: ResumeContentInspection["hydratedFiles"];
}
declare function hasResumeContentProtection(appConfig: ResumeProtectionConfig | undefined): boolean;
declare function assertResumeContentAllowed({
  appConfig,
  endpointOption,
  conversationId,
  targetMessageId,
  user,
  storedMessages,
  seedContent,
  resumeValue,
  liveFiles,
  isTemporary,
  checkpointNamespace,
  resolvedAddedAgent
}: AssertResumeContentAllowedInput, dependencies: ResumeContentProtectionDependencies): Promise<void>;
/**
* Rechecks the fully initialized runtime projection immediately before a resumed run.
* The controller-level preflight remains authoritative for approval claiming; this
* second boundary covers hydrated attachments and dynamic tool context assembled later.
*/
declare function assertResumeRuntimeContentAllowed(input: AssertResumeRuntimeContentAllowedInput, dependencies: ResumeRuntimeContentProtectionDependencies): Promise<ResumeRuntimeContentProjection>;
declare function getUserFacingResumeError(error: {
  readonly message?: string;
} | null | undefined, appConfig: ResumeProtectionConfig | undefined): string;
declare const mergeUserSubmittedPaths: (...pathLists: readonly (readonly (string | null | undefined)[] | null | undefined)[]) => string[];
declare const mergeUserSubmittedMessageFieldPaths: (...entryLists: readonly (readonly (UserSubmittedMessageFieldPath | null | undefined)[] | null | undefined)[]) => UserSubmittedMessageFieldPath[];
/** Map user decisions to the exact assistant fields they mutate during resume. */
declare function getResumeUserSubmittedPaths(content: readonly ResumeContentPart[] | null | undefined, pendingAction: Pick<Agents.PendingAction, "payload"> | null | undefined, body: {
  readonly decisions?: readonly Agents.ToolApprovalResolution[];
} | null | undefined): string[];
/** Preserve exact request-only message fields after they are embedded in tool outputs. */
declare function getResumeUserSubmittedMessageFieldPaths(content: readonly ResumeContentPart[] | null | undefined, pendingAction: Pick<Agents.PendingAction, "payload"> | null | undefined, body: {
  readonly answer?: string;
  readonly decisions?: readonly Agents.ToolApprovalResolution[];
} | null | undefined): UserSubmittedMessageFieldPath[];
//#endregion
//#region src/agents/hitl/preflight.d.ts
interface ResumePreflightBody {
  readonly answer?: string;
  readonly decisions?: readonly Agents.ToolApprovalResolution[];
}
interface ResumePreflightJobMetadata {
  readonly responseMessageId?: string;
  readonly userMessage?: Agents.UserMessageMeta;
  readonly userSubmittedPaths?: readonly string[];
  readonly userSubmittedMessageFieldPaths?: readonly UserSubmittedMessageFieldPath[];
}
interface ResumeUserSubmittedProvenance {
  readonly userSubmittedPaths: string[];
  readonly userSubmittedMessageFieldPaths: UserSubmittedMessageFieldPath[];
}
interface GetResumeProvenanceInput {
  readonly content: AssertResumeContentAllowedInput["seedContent"];
  readonly messageFieldContent?: AssertResumeContentAllowedInput["seedContent"];
  readonly pendingAction: Pick<Agents.PendingAction, "payload"> | null | undefined;
  readonly body: ResumePreflightBody | null | undefined;
  readonly existingPaths?: readonly string[];
  readonly existingMessageFieldPaths?: readonly UserSubmittedMessageFieldPath[];
}
interface PreflightResumeContentInput {
  readonly appConfig: AssertResumeContentAllowedInput["appConfig"];
  readonly endpointOption: AssertResumeContentAllowedInput["endpointOption"];
  readonly conversationId: string;
  readonly user: AssertResumeContentAllowedInput["user"];
  readonly jobMetadata: ResumePreflightJobMetadata;
  readonly pendingAction: Agents.PendingAction;
  readonly body: ResumePreflightBody;
  readonly resumeValue: AssertResumeContentAllowedInput["resumeValue"];
  readonly resumeState: Agents.ResumeState | null | undefined;
  readonly liveFiles: AssertResumeContentAllowedInput["liveFiles"];
  readonly isTemporary: boolean;
  readonly checkpointNamespace?: string;
  readonly resolvedAddedAgent: AssertResumeContentAllowedInput["resolvedAddedAgent"];
}
interface PreflightResumeContentResult extends ResumeUserSubmittedProvenance {
  readonly seedContent: AssertResumeContentAllowedInput["seedContent"];
  readonly storedMessages: AssertResumeContentAllowedInput["storedMessages"];
}
/** Compose the exact assistant paths authored by a user across resume segments. */
declare function getResumeProvenance({
  content,
  messageFieldContent,
  pendingAction,
  body,
  existingPaths,
  existingMessageFieldPaths
}: GetResumeProvenanceInput): ResumeUserSubmittedProvenance;
/**
* Build and inspect the exact model-bound projection for a HITL continuation.
* The legacy controller supplies lifecycle state and database adapters; all
* content/provenance policy decisions remain in typed code here.
*/
declare function preflightResumeContent({
  appConfig,
  endpointOption,
  conversationId,
  user,
  jobMetadata,
  pendingAction,
  body,
  resumeValue,
  resumeState,
  liveFiles,
  isTemporary,
  checkpointNamespace,
  resolvedAddedAgent
}: PreflightResumeContentInput, dependencies: ResumeContentProtectionDependencies): Promise<PreflightResumeContentResult>;
//#endregion
//#region src/agents/hitl/hookLoader.d.ts
/**
* The default-export contract a tool-approval hook MODULE must satisfy: a builder that takes
* the config entry's static `options` and returns a per-run {@link ToolApprovalHookFactory}.
*
*   // my-hook.js
*   module.exports = (options) => (context) => async (input) => ({ decision: 'ask' });
*/
type ToolApprovalHookModule = (options?: Record<string, unknown>) => ToolApprovalHookFactory;
interface LoadToolApprovalHooksOptions {
  /** Directory to resolve relative module paths against. Defaults to `process.cwd()`. */
  basePath?: string;
  /** Override the dynamic importer (used by tests to avoid touching the filesystem). */
  importModule?: (specifier: string) => Promise<unknown>;
}
/**
* Load + register the programmatic tool-approval hooks declared under
* `endpoints.agents.toolApproval.hooks`. Call once at startup (and again on a config reload —
* each call first unregisters the previous batch, so it is idempotent across reloads and
* never double-registers).
*
* Robust by design: a bad entry (unimportable module, non-function export, builder that
* throws or returns a non-function) is logged and skipped — one misconfigured hook never
* crashes startup or blocks the others. Returns the number of hooks successfully registered.
*
* SECURITY: each `module` is dynamically imported and executed in-process. This is
* admin-level config (librechat.yaml); only reference trusted code.
*/
declare function loadToolApprovalHooks(hooks: TToolApprovalHookConfig[] | undefined, options?: LoadToolApprovalHooksOptions): Promise<number>;
//#endregion
//#region src/agents/hitl/askUserQuestionTool.d.ts
/**
* Tool name. Deliberately identical to the SDK's interrupt discriminator
* (`AskUserQuestionInterruptPayload.type === 'ask_user_question'`) and the client
* content-part type the merged question card renders — the whole pipeline keys on
* this one string.
*/
declare const ASK_USER_QUESTION_TOOL_NAME = "ask_user_question";
/**
* Mirrors the SDK's `AskUserQuestionsRequest`; the validated input is passed to
* `askUserQuestions()` unchanged, so this schema is the wire shape inside the
* pending-action payload.
*/
declare const askUserQuestionItemSchema: z.ZodObject<{
  id: z.ZodString;
  header: z.ZodOptional<z.ZodString>;
  question: z.ZodString;
  description: z.ZodOptional<z.ZodString>;
  options: z.ZodOptional<z.ZodArray<z.ZodObject<{
    label: z.ZodString;
    value: z.ZodString;
  }, "strip">, "many">>;
  multiSelect: z.ZodOptional<z.ZodBoolean>;
}, "strip">;
declare const askUserQuestionsArraySchema: z.ZodEffects<z.ZodArray<typeof askUserQuestionItemSchema>>;
declare const askUserQuestionToolSchema: z.ZodObject<{
  questions: typeof askUserQuestionsArraySchema;
}, "strip">;
type AskUserQuestionToolInput = z.infer<typeof askUserQuestionToolSchema>;
/** Explicit shape of {@link AskUserQuestionToolDefinition} (isolatedDeclarations). */
interface AskUserQuestionToolDefinitionShape {
  name: string;
  description: string;
  schema: {
    type: "object";
    properties: {
      questions: {
        type: "array";
        minItems: number;
        maxItems: number;
        description: string;
        items: {
          type: "object";
          properties: {
            id: {
              type: "string";
              pattern: string;
              description: string;
            };
            header: {
              type: "string";
              minLength: number;
              maxLength: number;
              description: string;
            };
            question: {
              type: "string";
              minLength: number;
              maxLength: number;
              description: string;
            };
            description: {
              type: "string";
              maxLength: number;
              description: string;
            };
            options: {
              type: "array";
              maxItems: number;
              description: string;
              items: {
                type: "object";
                properties: {
                  label: {
                    type: "string";
                    minLength: number;
                    maxLength: number;
                    description: string;
                  };
                  value: {
                    type: "string";
                    minLength: number;
                    maxLength: number;
                    description: string;
                  };
                };
                required: string[];
              };
            };
            multiSelect: {
              type: "boolean";
              description: string;
            };
          };
          required: string[];
        };
      };
    };
    required: string[];
  };
}
/**
* JSON-schema twin of {@link askUserQuestionToolSchema} for the schema-only tool
* registry (`agentToolDefinitions` in `tools/registry/definitions.ts`), same shape the
* Calculator/WebSearch builtin definitions use.
*/
declare const AskUserQuestionToolDefinition: AskUserQuestionToolDefinitionShape;
/**
* Create the `ask_user_question` tool instance. The func calls the SDK's
* `askUserQuestions()` helper, which raises a LangGraph `interrupt()` — on the first
* pass execution unwinds (the run pauses; `run.getInterrupt().payload.type ===
* 'ask_user_question'`), and on the resume pass it returns the host-supplied
* `{ answers }`, which becomes the ToolMessage content the model sees.
*
* Requirements at the run level (wired in `agents/run.ts`): a checkpointer must be
* attached (the interrupt must be durable to be resumable) and the tool must be
* excluded from eager event execution (an interrupt raised outside the Pregel task
* frame cannot pause the run). It does NOT require the tool-approval policy
* (`humanInTheLoop`/hooks) — verified end-to-end in
* `api/server/controllers/agents/__tests__/askUserQuestion.e2e.spec.js`.
*
* LangGraph resume contract (documented on the SDK helper): the tool body re-runs
* from the top on the resume pass, and sibling tools in the same batch re-execute —
* which is why the description forbids parallel calls.
*/
declare function createAskUserQuestionTool(validationErrorsByToolCallId?: Map<string, ToolInputValidationError>): DynamicStructuredTool<typeof askUserQuestionToolSchema>;
//#endregion
//#region src/agents/hitl/answers.d.ts
/** The projection a stored-row loader needs; nothing else is read. */
declare const RETAINED_ANSWER_ROW_FIELDS = "messageId parentMessageId content isCreatedByUser isUserSubmitted userSubmittedPaths userSubmittedMessageFieldPaths";
/** Identifies the one retained-context message when relocating warm checkpoint history. */
declare const RETAINED_ANSWERS_MESSAGE_ID = "librechat:retained-answers";
/** The fields the scan reads from a stored row or an in-memory message. */
interface RetainedAnswerSource {
  messageId?: string | null;
  parentMessageId?: string | null;
  content?: unknown;
  isCreatedByUser?: boolean;
  isUserSubmitted?: boolean;
  userSubmittedPaths?: readonly string[];
  userSubmittedMessageFieldPaths?: readonly {
    path: string;
    field: string;
  }[];
}
interface RetainedAnswer {
  question: string;
  answer: string;
}
/** The answers one `ask_user_question` call received, in question order. */
interface RetainedAnswerSet {
  toolCallId?: string;
  answers: RetainedAnswer[];
}
interface RetainedAnswersConfig {
  enabled: boolean;
  maxTokens: number;
}
type RetainedAnswerTokenCounter = (text: string) => number | Promise<number>;
/** The stored-row query, in the shape the data layer's `getMessages` already has. */
type RetainedAnswerRowQuery = (filter: {
  conversationId: string;
  user: string;
}, select: string) => Promise<readonly RetainedAnswerSource[] | null | undefined>;
/**
* Every answered `ask_user_question` call in `sources`, in the order the sources
* are given. Rows are already unique by message id after the branch walk, and
* tool-call ids are not conversation-global (a provider may reuse `call_0` on
* every turn), so nothing is merged across rows.
*/
declare function collectRetainedAnswers(sources: readonly RetainedAnswerSource[]): RetainedAnswerSet[];
/**
* The branch that ends at `parentMessageId`, oldest first: the walk the prompt
* builder makes, without its stop at a checkpoint summary. Answers given before
* a compaction are exactly the ones the model no longer sees. The first row
* seen for an id wins, so a caller puts the rows it trusts most first.
*/
declare function orderConversationBranch<T extends RetainedAnswerSource>(messages: readonly T[], parentMessageId: string | null | undefined): T[];
/**
* Whether an ordered branch reaches its root. The rows a turn holds in memory
* stop early when a checkpoint summary bounded the history read, or when a
* warm event-actor turn holds only its new event message; both leave the oldest
* row pointing at a parent that is not in memory.
*/
declare function reachesBranchRoot(branch: readonly RetainedAnswerSource[]): boolean;
/**
* The block quoted into the current user turn. When every set fits `maxTokens`
* with the header and separators, all of them are rendered oldest first.
* Otherwise sets are kept newest first while the block, omission note
* included, still fits; the newest set is always kept, the way the
* summarizer's recency window always keeps the latest turn.
*/
declare function renderRetainedAnswers(sets: readonly RetainedAnswerSet[], maxTokens: number, countTokens: RetainedAnswerTokenCounter): Promise<string | undefined>;
/** Config as the run reads it: on unless disabled, and a positive integer budget. */
declare function resolveRetainedAnswersConfig(config: TAskUserQuestionConfig | null | undefined): RetainedAnswersConfig;
interface RetainedAnswersContextInput {
  /** The rows in memory, which may stop short of the branch root. */
  messages: readonly RetainedAnswerSource[];
  parentMessageId: string | null | undefined;
  /**
  * Every row the turn's history read already fetched, when it made one; used
  * to complete a branch whose rows in memory do not reach its root (a
  * checkpoint summary bounded the walk) without reading the conversation a
  * second time. Stored rows outrank the copies in memory, which may already be
  * prompt-shaped.
  */
  storedRows?: readonly RetainedAnswerSource[] | null;
  /**
  * The stored-row query, for a turn that made no history read (a warm
  * event-actor turn holds only its new event message). Called only when
  * retention is on, the branch is incomplete and no `storedRows` were given.
  */
  getMessages?: RetainedAnswerRowQuery;
  conversationId?: string | null;
  userId?: string | null;
  config: TAskUserQuestionConfig | null | undefined;
  countTokens: RetainedAnswerTokenCounter;
}
/**
* The retained-answers block for one turn, or `undefined` when there is nothing
* to carry or the block could not be built: a failure here costs the turn its
* carried answers, never the turn, and is logged once per occurrence.
*/
declare function buildRetainedAnswersContext(input: RetainedAnswersContextInput): Promise<string | undefined>;
/**
* The checkpoint carries the server-produced block separately from user text.
* Exact-count only that block; pasted/file context keeps the normal size guard.
* Do not mark this metadata-dependent counter content-cache-compatible: the SDK
* cache excludes additional_kwargs. The underlying tokenizer remains cached, and
* this closure retains only the most recently counted block across run recounts.
*/
declare function withRetainedAnswerTokenCounter(fallback: TokenCounter$1, encoding: EncodingName): TokenCounter$1;
/** The early admission count and the block share the final prompt's exact counter. */
declare function prepareRetainedAnswers(input: Omit<RetainedAnswersContextInput, "countTokens"> & {
  countTokens?: RetainedAnswerTokenCounter;
  encoding?: EncodingName;
}): Promise<{
  block?: string;
  tokenCount: number;
  tokenCounter?: TokenCounter$1;
}>;
/**
* Apply after SDK summary slicing and replay. Keep a dedicated ordinary human
* message so neither user text nor synthetic provenance is modified. Its stable
* ID lets warm invocation preparation relocate the block without accumulating
* another copy. The caller's transcript remains unchanged for memory extraction.
*/
declare function applyRetainedAnswers({
  block,
  messages,
  indexTokenCountMap,
  tokenCounter
}: {
  block: string | null | undefined;
  messages: BaseMessage[];
  indexTokenCountMap?: Record<number, number>;
  tokenCounter: TokenCounter$1;
}): {
  messages: BaseMessage[];
  indexTokenCountMap: Record<number, number>;
};
/**
* Replace the message list from the exact warm checkpoint in one invocation.
* The SDK's ordinary ID replacement preserves the old position, so it cannot
* refresh recency. Its remove-all reducer command preserves the supplied
* transcript while moving retained context beside the new event. No checkpoint
* write occurs here, and cold/no-answer turns need no additional read.
*/
declare function prepareRetainedAnswerInvocationMessages(messages: BaseMessage[], warm: boolean, readCheckpoint: () => Promise<{
  values: unknown;
}>): Promise<BaseMessage[]>;
//#endregion
//#region src/agents/hitl/byom.d.ts
/** Exact built-in tool names whose execution can touch a stateful code target. */
declare function isStatefulCodeEnvironmentToolName(name: string): boolean;
type AttachedCodeEnvironmentPolicySettings = {
  configSchema?: CodeEnvironmentUserConfigSchema;
  settings?: CodeEnvironmentUserSettings;
  skillAuthoringAvailable?: boolean;
};
type CodeEnvironmentPolicyAgent = {
  id?: string;
  skillAuthoringAvailable?: boolean;
  toolDefinitions?: readonly {
    name?: string;
    toolType?: string;
  }[];
  toolRegistry?: ReadonlyMap<string, {
    name?: string;
    toolType?: string;
  }>;
  codeExecutionContext?: {
    environmentType?: string;
    codeEnvironmentConfigSchema?: CodeEnvironmentUserConfigSchema;
    codeEnvironmentSettings?: CodeEnvironmentUserSettings;
  };
  subagentAgentConfigs?: readonly (CodeEnvironmentPolicyAgent | null | undefined)[];
  lazySubagentConfigs?: readonly (CodeEnvironmentPolicyAgent | null | undefined)[];
  subagentGraphMemberMetadata?: readonly (CodeEnvironmentPolicyAgent | null | undefined)[];
  subagentGraphConfigs?: readonly {
    memberConfigs?: readonly (CodeEnvironmentPolicyAgent | null | undefined)[];
  }[];
};
/**
* Mark code-specific approval previews only when the initialized server tool
* graph proves the effective name belongs exclusively to TerraMind's native
* code tools. A same-name user/MCP/action definition makes the name ambiguous
* and deliberately falls back to the generic argument preview.
*/
declare function markNativeCodeToolApprovalRequests(payload: Agents.ToolApprovalInterruptPayload, roots: readonly (CodeEnvironmentPolicyAgent | null | undefined)[]): Agents.ToolApprovalInterruptPayload;
declare class AttachedCodeEnvironmentApprovalError extends Error {
  readonly code = "BYOM_TOOL_APPROVAL_UNSUPPORTED";
  constructor();
}
/** Prevent an approval-gated BYOM tool from running on an ingress that cannot resume it. */
declare function assertAttachedCodeEnvironmentApprovalSupported({
  hasAttachedCodeEnvironment,
  hitlCapable,
  approvalExplicitlyDisabled
}: {
  hasAttachedCodeEnvironment: boolean;
  hitlCapable: boolean;
  approvalExplicitlyDisabled: boolean;
}): void;
/** Collect the SDK agent identities whose execution route targets an attached VM. */
declare function collectAttachedCodeEnvironmentAgentIds(roots: readonly (CodeEnvironmentPolicyAgent | null | undefined)[]): Set<string>;
declare function collectAttachedCodeEnvironmentPolicySettings(roots: readonly (CodeEnvironmentPolicyAgent | null | undefined)[]): Map<string, AttachedCodeEnvironmentPolicySettings>;
declare function resolveAttachedCodeApprovalMode(requested: unknown, settingsByAgentId: ReadonlyMap<string, AttachedCodeEnvironmentPolicySettings>, approvalsEnabled?: boolean): CodeApprovalMode | undefined;
/** Describe only the BYOM hook branches that can actually return `ask` during admission. */
declare function buildAttachedCodeEnvironmentAdmissionHooks(attachedAgentIds: ReadonlySet<string>, settingsByAgentId?: ReadonlyMap<string, AttachedCodeEnvironmentPolicySettings>, mode?: CodeApprovalMode): ResolvedToolApprovalHook[];
/**
* Safe default for user-operated code environments. Read-only file and search
* operations fall through to the run-wide policy; actions that can execute code
* or modify the workspace require approval for the agent that owns the BYOM route.
*/
declare function createAttachedCodeEnvironmentPolicyHook(attachedAgentIds: ReadonlySet<string>, settingsByAgentId?: ReadonlyMap<string, AttachedCodeEnvironmentPolicySettings>, mode?: CodeApprovalMode): HookCallback<"PreToolUse">;
//#endregion
//#region src/agents/hooks/executor.d.ts
/**
* Plan-time gate for handlers the executor cannot run on the current host.
* Windows has no portable `bash`, so a command handler must declare
* `commandWindows` or `shell: "powershell"` to be executable there; POSIX
* hosts run the portable `command` with bash, so a handler declaring
* `shell: "powershell"` without a separate `commandWindows` variant marks
* its only command as PowerShell syntax that bash would fail open on.
* Exported with an explicit platform parameter for direct testing.
*/
declare function getShellHandlerIssue(handler: PluginHookHandler, platform?: NodeJS.Platform): string | undefined;
/**
* Capabilities of the command executor, shared by plan time (plugin loading)
* and run time (hook registration) so a handler the loader marked `ready` is
* always executable. Alias tokens translate in exact matchers ("Bash|Write")
* and in regex matchers ("^(Write|Edit)$") via word-bounded substitution; a
* regex whose alias sits in a character class or escape — or any matcher
* naming a Claude built-in with no runtime equivalent — is rejected as
* unmapped, a loud plan-time diagnostic instead of a guard that never fires.
* Payloads are presented entirely in the plugin's namespace: `tool_name`
* maps back to the Claude alias and `tool_input` fields are renamed to
* Claude's schema, so one hook script works unchanged across both.
*/
declare const commandExecutorCapabilities: PluginHookCapabilities;
interface CommandExecutorOptions {
  /** Filesystem-resolved plugin root; becomes the command's cwd and `PLUGIN_ROOT`. */
  pluginRoot: string;
  /** Persistent per-plugin data directory; becomes `PLUGIN_DATA`. */
  pluginData: string;
  /** Environment source for the allowlist (defaults to `process.env`). */
  env?: NodeJS.ProcessEnv;
  /**
  * Whether `ask` decisions can raise a resumable approval interrupt. Off by
  * default: without a HITL surface an `ask` is tightened to `deny` so a
  * plugin's confirmation intent still blocks rather than stranding the run.
  */
  allowAskDecision?: boolean;
  /** SIGTERM-to-SIGKILL escalation delay after an abort (defaults to 5s). */
  killGraceMs?: number;
}
/**
* Runs `command` hook handlers as child processes outside the TerraMind API
* process, mirroring Claude Code's contract: the event payload arrives as
* JSON on stdin, exit 0 with JSON stdout returns a (sanitized) hook output,
* exit 2 blocks with stderr as the reason, and any other exit is logged and
* ignored. Commands run from the plugin root with a minimal allowlisted
* environment plus `PLUGIN_ROOT`/`PLUGIN_DATA`.
*
* SECURITY: deployment plugins are operator-installed code, the same trust
* level as `toolApproval.hooks` modules. Execution is additionally gated on
* the `DEPLOYMENT_PLUGIN_HOOKS` environment opt-in (see `plugins/runtime`).
*/
declare function createCommandExecutor(options: CommandExecutorOptions): PluginHookExecutor;
//#endregion
//#region src/agents/steering/refs.d.ts
interface SteerRequestUser {
  id?: string;
  tenantId?: string;
}
/**
* Copies the display-metadata fields a steer attachment ref may carry,
* dropping everything else. The single source of truth for the ref shape —
* used by the request sanitizer (untrusted client input) and the media
* builder (trusted DB docs) alike; the per-field type checks are the
* validation for the former and harmless for the latter. Only `file_id` is
* ever meaningful server-side: the drain re-fetches owner-scoped and
* re-derives everything else.
*
* Returns `null` when there is no usable `file_id`.
*/
declare function toSteerFileRef(raw: unknown): Partial<TFile> | null;
//#endregion
//#region src/agents/steering/request.d.ts
/** Attachment cap per steer, mirroring the composer's practical limits. */
declare const STEER_MAX_FILES = 10;
/** Character cap for a single steer message (env-overridable). */
declare function getSteerMaxLength(): number;
/** `db.getFiles`-shaped dependency (injected — this package has no DB access). */
type SteerFileFetcher = (filter: Record<string, unknown>, sortOptions: Record<string, unknown>, selectFields: Record<string, unknown>) => Promise<IMongoFile[] | null | undefined>;
interface SteerRequestBody {
  conversationId?: unknown;
  generationCreatedAt?: unknown;
  text?: unknown;
  clientSteerId?: unknown;
  files?: unknown;
  /** Quoted excerpts steered with the message ("Add to chat" selections);
  *  normalized like the chat route's quotes and merged into the model-bound
  *  turn at the injection boundary. */
  quotes?: unknown;
  /** Ask the generating replica to seal the live model stream at the next
  *  provider-safe boundary instead of waiting for a tool step. NEVER a
  *  rejection reason: on an SDK without the capability the steer still
  *  enqueues and the 202 echoes `preempt: false`. */
  preempt?: unknown;
}
interface SteerCancelBody {
  conversationId?: unknown;
  generationCreatedAt?: unknown;
  steerId?: unknown;
  clientSteerId?: unknown;
}
/** HTTP-shaped outcome the thin route wrapper serializes verbatim. */
interface SteerRequestResult {
  status: number;
  body: Record<string, unknown>;
}
/** Protocol-only options shared by cancel/arm, which do not need the steer
* request's file and agent-authorization dependencies. */
interface SteerProtocolOptions {
  generationProtocolVersion?: GenerationProtocolVersion;
}
/** The originating run's identity, from job metadata (never the request). */
interface SteerRunContext {
  agentId?: string;
  endpoint?: string;
}
/**
* Host-injected dependencies for the steer guard ladder. All optional — a
* host that omits them gets shape-level sanitization only (pre-round-13
* behavior); the TerraMind route wires all three.
*/
interface SteerRequestDeps {
  /** Upper protocol bound selected by the HTTP host from the request markers
  * and its rollout gate. Direct package callers that omit it retain the
  * current-package (v2) behavior. */
  generationProtocolVersion?: GenerationProtocolVersion;
  /** Refuse a legacy v1 enqueue that cannot persist `clientSteerId` receipts.
  * Background/event deliveries use this to make retrying an ambiguous
  * outcome safe instead of potentially injecting the same instruction twice. */
  requireIdempotentDelivery?: boolean;
  /** Best-effort cancellation observed immediately before durable mutation. */
  signal?: AbortSignal;
  /** Owner-scoped file fetch (`db.getFiles`-shaped). When present, every
  *  client-supplied ref must resolve to an owned DB doc at enqueue and the
  *  queued refs are replaced with DB-derived ones. */
  getFiles?: SteerFileFetcher;
  /** Marks resolved uploads used so the upload-window TTL cannot reap a file
  *  the persisted steer part references (parity with normal sends). */
  updateFilesUsage?: (files: Array<{
    file_id: string;
  }>, fileIds?: string[], options?: {
    user?: string;
    tenantId?: string | null;
  }) => Promise<unknown[]>;
  /** Agent authorization for the originating run (role + per-agent ACL),
  *  mirroring the chat route's middlewares. `false` → 403 FORBIDDEN. */
  checkAgentAccess?: (run: SteerRunContext) => Promise<boolean>;
}
/**
* The full steer-request guard ladder, typed and host-agnostic: validation,
* capability gate, job ownership/state checks, and the status-guarded enqueue.
* Returns the HTTP status + JSON body for the thin `/api` route wrapper.
* Rejection codes tell the client how to degrade:
* - 404 NO_ACTIVE_RUN → send as a normal message
* - 409 RUN_PAUSED    → run awaits human review; queue client-side instead
* - 429 STEER_QUEUE_FULL → too many undrained steers
* - 501 STEER_UNSUPPORTED → SDK cannot inject; queue client-side
*/
declare function handleSteerRequest(user: SteerRequestUser, body: SteerRequestBody, deps?: SteerRequestDeps): Promise<SteerRequestResult>;
/**
* Cancel a queued steer before injection. `removed: false` is advisory, not
* an error (200): the cancel lost its race — the steer already injected (the
* inline part is authoritative) or the run reached a terminal path that owns
* delivery — and the client should defer to the events it will receive. A
* missing job reads the same way: nothing is left to cancel from.
*
* No `checkAgentAccess` dep here: a cancel injects nothing model-bound, so
* job ownership/tenant checks suffice.
*/
declare function handleSteerCancel(user: SteerRequestUser, body: SteerCancelBody, options?: SteerProtocolOptions): Promise<SteerRequestResult>;
/**
* Escalates a still-queued steer to an interrupt IN PLACE — one atomic flag
* flip on the existing item, so its FIFO position, id, and timestamp all
* survive and no reclaim window ever exists. `armed: false` is not an error:
* the steer already injected, was cancelled, or belongs to a run that ended
* (the client defers to the events it will receive). Mirrors the steer POST's
* preempt contract exactly: the durable flag is gated on the OWNER's recorded
* capability, the store op is fenced to the validated generation, and the
* volatile arm publish is fire-and-forget because the durable flag is the
* truth `rearmQueuedPreempts` trusts on resume and handover.
*/
declare function handleSteerArm(user: SteerRequestUser, body: SteerCancelBody, options?: SteerProtocolOptions): Promise<SteerRequestResult>;
//#endregion
//#region src/agents/steering/media.d.ts
/** The BaseClient encode surface the steer media pipeline reuses. */
interface SteerMediaClient {
  /** The turn's view of stored records, which every check and encode below must share. */
  resolveTurnAttachments(files: IMongoFile[], consumers?: TurnFileConsumers): IMongoFile[];
  addFileContextToMessage(message: Record<string, unknown>, files: IMongoFile[], consumers?: TurnFileConsumers): Promise<void>;
  processAttachments(message: Record<string, unknown>, files: IMongoFile[], consumers?: TurnFileConsumers): Promise<IMongoFile[] | undefined>;
}
interface SteerPart {
  type?: string;
  steerId?: string;
  files?: Partial<TFile>[];
  quotes?: string[];
  media?: Array<Record<string, unknown>>;
  [key: string]: unknown;
}
/** One stamped steer part: payload index + the stamped content array, so the
*  caller can fold the re-encoded media into its token accounting. */
interface StampedSteerMedia {
  index: number;
  sourceMessageId?: string;
  fileIds: string[];
  media: Array<Record<string, unknown>>;
  /** The bare steer body, so token accounting can subtract what the
  *  assistant message already counted (file context must still count). */
  steerText: string;
}
/**
* Resolves a queued steer's attachment refs into encoded model content for
* live mid-run injection. Only `file_id`s are trusted from the item — the
* fetch is owner-scoped and every other field is re-derived from the DB docs,
* restored to the composer's ref order (a `$in` result comes back in database
* order). Returns `undefined` when nothing authorized remains (the drain hook
* then injects text only, so the user's words always land).
*/
declare function buildSteerMedia({
  client,
  user,
  item,
  getFiles,
  assertFilesAllowed
}: {
  client: SteerMediaClient;
  user: SteerRequestUser | undefined;
  item: SteerQueueItem;
  getFiles: SteerFileFetcher;
  assertFilesAllowed?: (files: IMongoFile[]) => void;
}): Promise<SteerMediaResult | undefined>;
interface SteerStampTarget {
  message: {
    id?: string;
    messageId?: string;
    content?: unknown;
  };
  part: SteerPart;
  index: number;
  quotes: string[] | null;
  encodeFiles: boolean;
}
type SteerStampPayload = Array<{
  id?: string;
  messageId?: string;
  role?: string;
  content?: unknown;
}>;
/** One pass over the payload for everything the stamp needs. Callers check
*  `.length` for the zero-await fast path and hand the result to
*  `stampSteerPartMedia`, so the history is never scanned twice. */
declare function collectSteerStampTargets(payload: SteerStampPayload, resendFiles: boolean): SteerStampTarget[];
/**
* Re-encodes attachments and re-merges quotes for persisted steer parts of
* PAST turns, stamping the assembled content array as a transient `media`
* field, which the SDK's `formatAgentMessages` prefers over the plain text
* when reconstructing the steer's HumanMessage. Refs are re-encoded per turn
* — encoded data is never persisted — and parts are replaced immutably so the
* stamp cannot leak into a message save. Encodes run in parallel after doc
* resolution.
*
* Quote-bearing parts are stamped UNCONDITIONALLY (a merged text part is the
* only way the excerpts reach the model on replay, mirroring `prependQuotes`
* for regular user turns), while file encoding remains gated on the
* conversation's `resendFiles` setting — a quote-bearing part whose files are
* not resent still replays its quotes, exactly like its text.
*
* `docsById` should be the owner-scoped doc map `addPreviousAttachments`
* already fetched this turn (its single historical-files query collects
* steer-part refs); when present, NO extra query is issued — an id missing
* from the map is unauthorized or deleted, exactly as if the fallback query
* had excluded it.
*/
declare function stampSteerPartMedia({
  client,
  user,
  payload,
  targets,
  docsById,
  getFiles,
  resendFiles
}: {
  client: SteerMediaClient;
  user: SteerRequestUser | undefined;
  payload: SteerStampPayload;
  /** Pre-collected via `collectSteerStampTargets` so the caller's zero-await
  *  probe and this stamp share one payload scan; collected here otherwise. */
  targets?: SteerStampTarget[];
  docsById?: Map<string, IMongoFile>;
  getFiles: SteerFileFetcher;
  resendFiles?: boolean;
}): Promise<StampedSteerMedia[]>;
//#endregion
//#region src/agents/steering/offset.d.ts
/**
* Shared mutable offset. The host increments `offset` each time a steer part
* is spliced into the live content array; the wrapped handlers read it at
* handle time so every SDK-emitted content index that arrives AFTER an
* injection lands past the inserted part.
*/
interface SteerOffsetState {
  offset: number;
}
/**
* Wrap a run's event handlers so content indices shift by the CURRENT steer
* offset. The mid-run analog of `createContentIndexOffsetHandlers` (HITL
* resume): that wrapper closes over a fixed seed length, while this one reads
* a mutable counter because steers arrive while the run is streaming.
*
* The index enters the pipeline at exactly one point: `ON_RUN_STEP`'s payload
* (whose `index` every subsequent delta resolves through the aggregator's
* step map). `ON_AGENT_UPDATE` carries its own inline index and is shifted
* likewise. All other handlers pass through untouched. Installed even at
* offset 0 — the first steer can arrive at any time. Steps the aggregator has
* ALREADY indexed are unaffected (deltas/completions resolve by step id), so
* injection at a tool-batch boundary never re-shifts the current batch.
*
* Composes with the resume wrapper: apply this OVER its output so a resumed
* run shifts by seed + live steer offset.
*/
declare function createSteerIndexOffsetHandlers(handlers: Record<string, EventHandler$1> | undefined, state: SteerOffsetState): Record<string, EventHandler$1> | undefined;
//#endregion
//#region src/agents/triggers/bindings.d.ts
interface AgentEventBindingDependencies {
  getAgent: AgentMethods["getAgent"];
  getConvo: ConversationMethods["getConvo"];
  getBinding: ConversationMethods["getAgentEventBinding"];
  getMessage: MessageMethods["getMessage"];
  deleteConvos: ConversationMethods["deleteConvos"];
  reserveThread: ConversationMethods["reserveSubagentThread"];
}
declare function createAgentEventBindingHandlers(deps: AgentEventBindingDependencies): {
  register: RequestHandler;
  resolve: RequestHandler;
};
//#endregion
//#region src/agents/triggers/actor.d.ts
interface EventActorResult extends Record<string, EventActorEvent> {
  action: AgentEventAppliedAction & EventActorEvent;
  checkpointCaptureError: string | null;
}
interface AgentEventActorInvocationContext {
  checkpointNamespace: string;
  checkpointId?: string;
  invocationId: string;
  continuation: "warm" | "cold";
  signal: AbortSignal;
}
interface ExecuteAgentEventActorInput<T> {
  user: string;
  tenantId?: string;
  conversationId: string;
  /** Authenticated binding that owns the delivery receipt. */
  bindingId?: string;
  invocationId: string;
  event: EventActorEvent;
  expectedAction?: AgentTriggerExpectedAction;
  signal: AbortSignal;
  checkpointer?: TCheckpointerConfig;
  contextFingerprint?: AgentContextFingerprint;
  resolveContext?(state: IAgentEventActorState): Promise<AgentEventActorContext | undefined>;
  readResultContext?(): Promise<AgentEventActorContext | undefined>;
  /** Deprecated compatibility input. Elapsed time never proves replay safety. */
  legacyTurnStaleMs?: number;
  invoke(context: AgentEventActorInvocationContext): Promise<T>;
  readAppliedAction(): AgentEventAppliedAction | undefined;
  readSuspension?(): {
    kind?: "human_decision" | "internal_completion";
    actionId: string;
    jobCreatedAt: number;
    interrupt: EventActorInterrupt;
  } | undefined;
}
interface AgentEventActorContext {
  fingerprint: AgentContextFingerprint;
  skillManifest: IAgentEventActorSkillIdentity[];
  discoveredToolNames?: string[];
  summary?: IAgentEventActorState["summary"];
  contextMeta?: IAgentEventActorState["contextMeta"];
  compactionSemanticIndex?: IAgentEventActorState["compactionSemanticIndex"];
  checkpointMessageOverlay?: AgentEventCheckpointMessageOverlay;
}
interface ExecuteAgentEventActorResult<T> {
  value: T;
  execution: EventActorExecutionResult<EventActorResult>;
}
interface AgentEventActorDependencies {
  getSnapshot: ConversationMethods["getAgentEventActorSnapshot"];
  commitState: ConversationMethods["commitAgentEventActorState"];
  storeSuspension?: ConversationMethods["storeAgentEventActorSuspension"];
  claimSuspension?: ConversationMethods["claimAgentEventActorSuspension"];
  settleSuspension?: ConversationMethods["settleAgentEventActorSuspension"];
  cancelSuspension?: ConversationMethods["cancelAgentEventActorSuspension"];
  recordReconciliation: ConversationMethods["recordAgentEventActorReconciliation"];
  resolveReconciliation: ConversationMethods["resolveAgentEventActorReconciliation"];
  admitAction?: AgentTriggerDeliveryMethods["admitAgentEventActorAction"];
  releaseAction?: AgentTriggerDeliveryMethods["releaseAgentEventActorAction"];
  hasActionAdmission?: AgentTriggerDeliveryMethods["hasAgentEventActorActionAdmission"];
  getReceipt?: AgentTriggerDeliveryMethods["getAgentEventActorReceipt"];
  clearReconciliation?: ConversationMethods["clearAgentEventActorReconciliation"];
}
interface ResumeAgentEventActorInput<T> {
  user: string;
  tenantId?: string;
  conversationId: string;
  bindingId?: string;
  suspension: EventActorSuspension;
  resumeAttemptId: string;
  resumeValue: EventActorEvent;
  signal: AbortSignal;
  checkpointer?: TCheckpointerConfig;
  expectedAction?: AgentTriggerExpectedAction;
  /** Projects the claimed Conversation fence into the exact job/action CAS. */
  claimProjection?(): Promise<boolean>;
  resume(context: AgentEventActorInvocationContext): Promise<T>;
  readAppliedAction(): AgentEventAppliedAction | undefined;
  readSuspension?(): {
    kind?: "human_decision" | "internal_completion";
    actionId: string;
    jobCreatedAt: number;
    interrupt: EventActorInterrupt;
  } | undefined;
  readResultContext?(): Promise<AgentEventActorContext | undefined>;
}
interface CancelAgentEventActorInput {
  user: string;
  tenantId?: string;
  conversationId: string;
  suspension: EventActorSuspension;
  cancelAttemptId: string;
  reason: "cancelled" | "expired";
  signal?: AbortSignal;
  checkpointer?: TCheckpointerConfig;
  /** Exact orphaned resume claim whose job never entered provider execution. */
  claimedResumeAttemptId?: string;
}
declare function createAgentEventActorActionAdmissionId(invocationId: string, checkpoint: {
  threadId: string;
  checkpointId?: string;
  checkpointNs: string;
}): string;
/**
* Executes one authenticated bound-child event through the SDK's checkpoint-fork lifecycle.
* The request controller still owns generation admission and terminal receipts; this adapter owns
* only checkpoint preparation, invocation isolation, CAS commit, and bounded cleanup.
*/
declare function executeAgentEventActor<T>(input: ExecuteAgentEventActorInput<T>, deps: AgentEventActorDependencies): Promise<ExecuteAgentEventActorResult<T>>;
/** Resumes one signed suspended fork on any replica using the Conversation as authority. */
declare function resumeAgentEventActor<T>(input: ResumeAgentEventActorInput<T>, deps: AgentEventActorDependencies): Promise<ExecuteAgentEventActorResult<T>>;
/** Cancels one exact current suspension through the SDK evidence validator.
* The Conversation CAS is the logical winner; checkpoint deletion follows
* idempotently so an ambiguous cleanup can safely retry the same proof. */
declare function cancelAgentEventActor(input: CancelAgentEventActorInput, deps: Pick<AgentEventActorDependencies, "cancelSuspension">): Promise<EventActorCancelSuspensionResult>;
//#endregion
//#region src/agents/triggers/bindingResolver.d.ts
type ContinueResolver$1 = NonNullable<AgentTriggerExecutionHostDeps["prepareContinue"]>;
interface AgentEventContinueResolverDeps {
  methods: Pick<ConversationMethods, "getAgentEventBinding" | "getConvo"> & Pick<MessageMethods, "getMessages">;
  getGenerationJob?: (conversationId: string) => Promise<{
    status?: string;
    createdAt?: number;
    metadata?: {
      terminalPersistencePending?: boolean;
    };
  } | null | undefined>;
}
/** Resolves the branch leaf at dispatch time so queued events never persist a stale parent. */
declare function createAgentEventContinueResolver({
  methods,
  getGenerationJob
}: AgentEventContinueResolverDeps): ContinueResolver$1;
//#endregion
//#region src/agents/triggers/continuation.d.ts
type ContinueResolver = NonNullable<AgentTriggerExecutionHostDeps["prepareContinue"]>;
interface AgentContinuationResolverDeps {
  eventActor: ContinueResolver;
  internalSources: ReadonlyMap<string, ContinueResolver>;
}
/** Selects the single continuation adapter authorized for this delivery. */
declare function createAgentContinuationResolver({
  eventActor,
  internalSources
}: AgentContinuationResolverDeps): ContinueResolver;
//#endregion
//#region src/stream/SteeringLifecycle.d.ts
/** Client-safe projection of a queued steer (drops the server-only userId). */
declare function toPendingSteer(item: SteerQueueItem): TPendingSteer;
/** Who a parked payload belongs to — the claim surface can outlive the job
*  record, so ownership must travel with the payload itself. */
interface SteerOwner {
  userId: string;
  tenantId?: string;
}
interface ParkedSteerRecovery {
  generationProtocolVersion: GenerationProtocolVersion;
  steers: TPendingSteer[];
}
/**
* The FIFO queue of mid-run steering messages for a generation job.
*
* A steer is a user message submitted while a run is generating. The steer
* route (any instance) enqueues it against the job; the owning process's
* run-scoped `PostToolBatch` hook drains the queue at the next tool-batch
* boundary and injects each entry into graph state as its own user message.
*
* Race-safety mirrors {@link ApprovalLifecycle}: enqueue is status-guarded in
* the store (a steer can never land on a completed/aborted job — the caller
* gets a rejection code instead), and drain is an atomic take-all so a steer
* is delivered to exactly one drain. Steers left in the queue when the run
* reaches a terminal state are drained by the finalization paths and reported
* to the client, which converts them to queued follow-up turns.
*/
declare class SteeringLifecycle {
  private readonly store;
  constructor(store: IJobStoreV2);
  /**
  * Append a steer, guarded on the job being `running`. Returns the new queue
  * depth, or a rejection code ({@link STEER_ENQUEUE_NOT_RUNNING} /
  * {@link STEER_ENQUEUE_QUEUE_FULL}).
  */
  enqueue(streamId: string, item: SteerQueueItem, expectedCreatedAt?: number): Promise<number>;
  enqueueVersioned(streamId: string, item: SteerQueueItem, wantsPreempt: boolean, expectedCreatedAt?: number): Promise<SteerEnqueueVersionedResult>;
  enqueueWithReceipt(streamId: string, item: SteerQueueItem, receipt: SteerReceiptInput, wantsPreempt: boolean, expectedCreatedAt?: number): Promise<SteerEnqueueReceiptResult>;
  getReceipt(streamId: string, clientSteerId: string): Promise<SteerReceipt | null>;
  /** Atomically take ALL queued steers, FIFO. `expectedCreatedAt` refuses the
  *  drain inside the store when the job was replaced. */
  drain(streamId: string, expectedCreatedAt?: number): Promise<SteerQueueItem[]>;
  restoreClaimed(streamId: string, items: SteerQueueItem[], expectedCreatedAt?: number): Promise<boolean>;
  admitTerminal(streamId: string, policy: TerminalSteerAdmissionPolicy, expectedCreatedAt?: number): Promise<TerminalSteerAdmissionResult>;
  /**
  * Terminal drain: atomically CLOSE the queue to new steers, then take all
  * queued items. Finalization paths use this so a steer POST racing the
  * final/abort event can never be ACKed after the last drain and then
  * silently cleared — once closed, enqueue rejects (the client falls back to
  * a normal send) until the next `createJob` reopens the stream id.
  * `expectedCreatedAt` keeps a stale run's finalization from closing or
  * stealing a replacement job's queue.
  */
  closeAndDrain(streamId: string, expectedCreatedAt?: number): Promise<SteerQueueItem[]>;
  /** Non-destructive FIFO read, optionally scoped to one generation. */
  peek(streamId: string, expectedCreatedAt?: number): Promise<SteerQueueItem[]>;
  /**
  * User-cancelled before injection. Races with a drain settle inside the
  * store's atomic removal — `false` means the steer already injected (the
  * inline part is authoritative) or the run ended (the terminal paths own
  * delivery), and the cancel is simply too late.
  */
  cancel(streamId: string, steerId: string, expectedCreatedAt?: number): Promise<boolean>;
  discardLeftover(streamId: string, clientSteerId: string, steerId: string, owner: SteerOwner, expectedGenerationCreatedAt?: number): Promise<boolean>;
  consumeRecovered(streamId: string, steerId: string, owner: SteerOwner, expectedCreatedAt: number): Promise<boolean>;
  /**
  * Escalate a still-queued steer to an interrupt IN PLACE — the durable
  * `preempt` flag flips on the existing item, so its FIFO position survives
  * (the whole queue drains at the seal, in order). Races with a drain,
  * cancel, or replacement run settle inside the store's atomic update as
  * `missing`, and the owner's LIVE capability is part of the same predicate
  * (`incapable`) — see {@link IJobStoreV2.armSteer}.
  */
  arm(streamId: string, steerId: string, expectedCreatedAt?: number): Promise<SteerArmOutcome>;
  armVersioned(streamId: string, steerId: string, expectedCreatedAt?: number): Promise<SteerArmResult>;
  /** Relabel durable interrupt requests as ordinary steers when HITL resume
  * moves ownership to a replica that cannot seal mid-stream. */
  downgradePreempts(streamId: string, expectedCreatedAt?: number): Promise<SteerQueueItem[] | null>;
  /** Drop any queued steers (terminal cleanup backstop). */
  clear(streamId: string): Promise<void>;
  /**
  * Parks terminally-drained leftovers under their own bounded-TTL store key
  * so a client with NO live subscriber (closed tab, reload racing the final
  * event) can still recover them via the status route — even after the
  * default `completeJob` path deletes the job record itself. The owner
  * identity travels WITH the payload for exactly that reason: the jobless
  * claim path has no job record left to authorize against. Live clients keep
  * using the final/abort event copy; recovery is idempotent (queued chips
  * dedupe by steer id).
  */
  park(streamId: string, steers: TPendingSteer[], owner: SteerOwner, expectedCreatedAt?: number): Promise<void>;
  /**
  * Owner-gated read of parked leftovers. The store keeps the payload until
  * a recovered item starts its deterministic next generation, so losing this
  * HTTP response cannot lose the user's words.
  */
  claim(streamId: string, requester: SteerOwner): Promise<TPendingSteer[]>;
  /** Protocol-aware recovery read used by the status route during the
  * rolling-upgrade bridge. Legacy stores fall back to destructive v1. */
  claimDetailed(streamId: string, requester: SteerOwner, requestedProtocolVersion?: GenerationProtocolVersion): Promise<ParkedSteerRecovery>;
}
//#endregion
//#region src/stream/ApprovalLifecycle.d.ts
interface ApprovalLifecycleCallbacks {
  onPaused?: (streamId: string, createdAt: number) => void;
  onResumed?: (streamId: string, createdAt: number) => void;
  onExpired?: (streamId: string, createdAt: number) => void;
  onPausePersistenceFailed?: (streamId: string, createdAt: number, error: string, drainedSteers: SteerQueueItem[]) => void | Promise<void>;
}
interface ApprovalPauseOptions {
  discoveredTools?: string[];
  activityPhaseSnapshot?: ActivityPhaseSnapshot;
  compactionSemanticIndex?: ICompactionSemanticIndexProjection;
  /** Calibration and fading state at the pause; seeds the resumed run's pruner. */
  contextMeta?: IAgentEventActorContextMeta;
  /** Generation identity observed by the interrupted run. */
  expectedCreatedAt?: number;
  /** Hold Stop/resume until the paused assistant row is durably unfinished. */
  persistencePending?: boolean;
  /** Versioned pointer to the canonical signed Conversation suspension. */
  agentEventSuspension?: AgentEventSuspensionProjection;
}
declare const PENDING_ACTION_EXPIRED_CODE = "HITL_ACTION_EXPIRED";
declare class PendingActionExpiredError extends Error {
  readonly code: typeof PENDING_ACTION_EXPIRED_CODE;
  constructor();
}
/**
* The guarded lifecycle of a run paused for human review (`requires_action`).
*
* Owns the legal transitions — pause, resolve, expire — behind one interface,
* on top of the store's atomic {@link IJobStore.transitionStatus}. Callers
* (approval routes, the status endpoint, the run seam) cross this seam instead
* of re-implementing the "is this transition legal from the current state, and
* is it safe under a concurrent second submit" logic at each site.
*
* Race-safety is the point. Two approval clicks racing to resume the same job
* must not both drive the run — a double-drive re-executes tools and
* double-bills. {@link resolve} returns `true` to exactly one caller; the loser
* gets `false`. The same guard protects {@link pause} (don't pause a job that
* was aborted between the interrupt firing and the mark) and {@link expire}.
*
* State machine:
* ```
*   running ──pause(pendingAction)──▶ requires_action
*   requires_action ──resolve()──────▶ running
*   requires_action ──expire()───────▶ aborted   (the edge that was undefined)
* ```
*/
declare class ApprovalLifecycle {
  private readonly store;
  private readonly callbacks;
  constructor(store: IJobStoreV2, callbacks?: ApprovalLifecycleCallbacks);
  /**
  * `running → requires_action`, attaching the pending review record.
  * Returns `false` when the job was not running (aborted mid-flight, gone),
  * so a late interrupt is dropped rather than pausing a dead job.
  */
  pause(streamId: string, pendingAction: Agents.PendingAction, options?: ApprovalPauseOptions): Promise<boolean>;
  /**
  * True only while this exact paused action still owns the durable response
  * persistence barrier installed by {@link pause}. Terminal and resume CASes
  * wait for the barrier, so a controller that observes `true` may safely write
  * its `unfinished:true` response before releasing it.
  */
  ownsPausePersistence(streamId: string, expectedActionId: string, expectedCreatedAt?: number): Promise<boolean>;
  /** Release the exact pause barrier after its required response write settles. */
  finishPausePersistence(streamId: string, expectedActionId: string, expectedCreatedAt?: number): Promise<boolean>;
  /**
  * Wait for a live pause writer, or recover its lock after the bounded owner
  * deadline. Every requires_action→running/terminal path crosses this helper,
  * making the pause response write linearize before either competing outcome.
  */
  waitForPausePersistence(streamId: string, expectedCreatedAt?: number): Promise<Awaited<ReturnType<IJobStoreV2["getJob"]>>>;
  /**
  * Fail a pause writer whose bounded persistence lease expired. The terminal
  * CAS is fenced to the exact action and generation and parks all accepted
  * steers in the same store transaction. Returning the ordinary action id to
  * callers here would be unsafe: the user, assistant, or conversation rows may
  * never have committed before the writer disappeared.
  */
  failStalePausePersistence(streamId: string, expectedCreatedAt?: number): Promise<boolean>;
  /**
  * The pending review record, or `null` when the job isn't awaiting review.
  * A past-`expiresAt` record reads as `null` (lazy expiry) so a stale prompt
  * is never surfaced to a UI or fed to a resume.
  */
  peek(streamId: string): Promise<Agents.PendingAction | null>;
  /**
  * `requires_action → running`, atomically. Returns `true` to the single
  * caller that won the transition; `false` if the job was not paused, was
  * already resumed by a racing submit, no longer matches `expectedActionId`,
  * or had expired — in which case it is moved to a terminal state instead of
  * resumed.
  *
  * Pass `expectedActionId` (the id the user actually decided on, from the
  * approval route) so a stale decision can't resume a job that has since
  * paused for a *different* action. Omit it only for callers with no specific
  * action in hand.
  *
  * The caller MUST treat `false` as "do not drive the run": only the `true`
  * winner may re-enter the agent.
  */
  resolve(streamId: string, expectedActionId?: string, resumePatch?: JobMetadataPatch, expectedCreatedAt?: number): Promise<boolean>;
  /**
  * `requires_action → aborted`: the edge that fires when no decision arrives
  * in time. Previously undefined; now an explicit, idempotent terminal
  * transition. Returns `true` to the single caller that expired it. Honors
  * `expectedActionId` for the same stale-decision protection as `resolve`.
  */
  expire(streamId: string, expectedActionId?: string, expectedCreatedAt?: number): Promise<boolean>;
  /**
  * Expires the observed approval and returns the winning terminal snapshot.
  * Callers that run host lifecycle hooks get the trusted pre-CAS metadata plus
  * the exact status/error/completion fields committed by this transition,
  * without a racy post-transition read (the terminal TTL may remove the hash).
  */
  expireWithIdentity(streamId: string, expectedActionId?: string, expectedCreatedAt?: number, options?: {
    markHostActionPending?: boolean;
  }): Promise<SerializableJobData | null>;
}
//#endregion
//#region src/stream/GenerationJobManager.d.ts
/** Internal transport signal: durable terminal state exists, but its DONE
* publication failed. HTTP subscribers must reconnect rather than treating
* this as an application-level generation error. */
declare const TERMINAL_PUBLICATION_RECONNECT_ERROR = "Terminal publication failed; reconnect to load the durable result";
declare const GENERATION_RECOVERY_FAILED_ERROR = "generation_recovery_failed";
/**
* Configuration options for GenerationJobManager
*/
interface GenerationJobManagerOptions {
  jobStore?: IJobStore;
  eventTransport?: IEventTransport;
  /**
  * If true, cleans up event transport immediately when job completes.
  * If false, keeps EventEmitters until periodic cleanup for late reconnections.
  * Default: true (immediate cleanup to save memory)
  */
  cleanupOnComplete?: boolean;
}
/** Host-owned lifecycle seam for durable work layered on agent generations.
* Delivery is at-least-once: implementations must be idempotent by generation. */
type ApprovalExpiredHandler = (streamId: string, job: SerializableJobData) => void | Promise<void>;
type TerminalHostActionHandler = (streamId: string, job: SerializableJobData, runSteps: Agents.RunStep[], content: Agents.MessageContentComplex[]) => void | Promise<void>;
interface CreateGenerationJobOptions {
  startupTelemetry?: AgentStartupTelemetry;
  initialMetadata?: Partial<GenerationJobMetadata>;
  /** A terminal steer being handed off as this new normal turn. The store
  * leases this exact parked item while the generation is active; persistence
  * commits its removal through `consumeRecovered`. */
  recoveredSteerId?: string;
  /** Exact user-visible source proof checked inside the store's atomic create.
  * Required whenever `recoveredSteerId` is present. */
  recoveredSteerPayload?: RecoveredSteerPayload;
  idempotencyClientRequestId?: string;
  idempotencyClaimToken?: string;
  /** Optional compare-and-set fence from the client's last authoritative
  * status result. Creation may proceed only if that exact epoch is still
  * current or the stream has no durable job. */
  expectedPredecessorCreatedAt?: number;
  /** Atomically refuse to replace a running/paused predecessor while allowing
  * an absent or terminal predecessor. Used by automatic continuations. */
  rejectActivePredecessor?: boolean;
}
/**
* Proof that this manager won the only legal `running -> terminal` transition
* for one generation. Controllers must publish their terminal event only after
* receiving this claim, then pass the same object to {@link finishTerminalJob}
* from a `finally` block.
*/
interface TerminalJobClaim {
  readonly streamId: string;
  readonly createdAt: number;
  readonly conversationId?: string;
  readonly status: "complete" | "error" | "aborted";
  readonly error?: string;
  /** The winner must durably publish either its normal FINAL or a
  * reconciliation payload before cleanup may remove the job. */
  readonly persistencePending?: true;
  /** Exact queued + claimed steers atomically parked by the winning CAS. */
  readonly drainedSteers: readonly SteerQueueItem[];
}
/**
* Manages generation jobs for resumable LLM streams.
*
* Architecture: Composes two pluggable services via dependency injection:
* - jobStore: Job metadata + content state (InMemory → Redis for horizontal scaling)
* - eventTransport: Pub/sub events (InMemory → Redis Pub/Sub for horizontal scaling)
*
* Content state is tied to jobs:
* - In-memory: jobStore holds WeakRef to graph for live content/run steps access
* - Redis: jobStore persists chunks, reconstructs content on demand
*
* All storage methods are async to support both in-memory and external stores (Redis, etc.).
*
* @example Redis injection:
* ```ts
* const manager = new GenerationJobManagerClass({
*   jobStore: new RedisJobStore(redisClient),
*   eventTransport: new RedisPubSubTransport(redisClient),
* });
* ```
*/
declare class GenerationJobManagerClass {
  /** Job metadata + content state storage - swappable for Redis, etc. */
  private jobStore;
  /** Guarded human-review lifecycle (pause / resolve / expire) over the store. */
  private _approvals;
  /** FIFO steering queue (enqueue / drain / peek / clear) over the store. */
  private _steering;
  /** Event pub/sub transport - swappable for Redis Pub/Sub, etc. */
  private eventTransport;
  /** Runtime state - always in-memory, not serializable */
  private runtimeState;
  /** Jobs actively owned by this process, pinned to their durable creation epoch. */
  private ownedJobs;
  /** Serializes replay-event read/modify/write updates per stream. */
  private replayEventWriteQueues;
  /** Serializes token-usage read/modify/write updates per stream. */
  private tokenUsageWriteQueues;
  /** Serializes whole-array run-step snapshots so an older save cannot overwrite completion. */
  private runStepWriteQueues;
  /** Coalesces reconnects waiting on the same owner-side overflow flush. */
  private earlyBufferOverflowWaiters;
  /** Partial-response and disconnect-state writes still draining during shutdown. */
  private subscriberCleanupPromises;
  /** Makes terminal cleanup idempotent while keeping claims opaque to callers. */
  private terminalFinishPromises;
  /** Exact local runtime observed when a claim won; never clean a later runtime. */
  private terminalClaimRuntimes;
  /** Claims whose durable final exists but whose DONE transport publication
  * failed. Retain their job record through normal finish so the forced
  * reconnect can replay that authoritative final payload. */
  private terminalPublicationFailures;
  /** Persistence-pending error claims whose terminal output was already
  * reconciled by a competing owner or stale-owner recovery. */
  private terminalErrorPublicationSuppressions;
  private cleanupInterval;
  /** Generation-scoped retirement callbacks must not outlive the configured
  * store/transport pair that created them. */
  private fencedRuntimeRetirements;
  /** Suppresses the stream-global disconnect callback while an exact fenced
  * predecessor detaches. The callback may otherwise target a same-stream
  * successor that never owned the departing SSE response. */
  private fencedSubscriberDetachments;
  /** Rejects new jobs once graceful shutdown has started. */
  private shuttingDown;
  /**
  * Provider executions this process has begun and not yet drained, awaited during shutdown so
  * each can record its own drain once its trailing writes settle. Opened in
  * `beginProviderExecution` and closed in `markProviderExecutionDrained` — the two chokepoints
  * every entry point (interactive, resume, remote API) already passes through — so no caller
  * has to remember to register, and a new entry point cannot silently opt out.
  */
  private readonly openProviderExecutions;
  /** Whether we're using Redis stores */
  private _isRedis;
  /** Whether streaming-delta publish/append coalescing is enabled (Redis only).
  * Off (the default) preserves the awaited per-event emitChunk contract exactly. */
  private _deltaCoalescingEnabled;
  /** Whether to cleanup event transport immediately on job completion */
  private _cleanupOnComplete;
  /** Optional host hook; the generic stream runtime does not know what external
  * durable work (scheduled chats, webhooks, etc.) a generation represents. */
  private approvalExpiredHandler;
  private terminalHostActionHandler;
  constructor(options?: GenerationJobManagerOptions);
  /**
  * Initialize the job manager with periodic cleanup.
  * Call this once at application startup.
  */
  initialize(): void;
  /**
  * Configure the manager with custom stores.
  * Call this BEFORE initialize() to use Redis or other stores.
  *
  * @example Using Redis
  * ```ts
  * import { createStreamServicesFromCache } from '~/stream/createStreamServices';
  * import { cacheConfig, ioredisClient } from '~/cache';
  *
  * const services = createStreamServicesFromCache({ cacheConfig, ioredisClient });
  * GenerationJobManager.configure(services);
  * GenerationJobManager.initialize();
  * ```
  */
  configure(services: {
    jobStore: IJobStore;
    eventTransport: IEventTransport;
    isRedis?: boolean;
    cleanupOnComplete?: boolean;
  }): void;
  /**
  * Check if using Redis stores.
  */
  get isRedis(): boolean;
  /**
  * Store-selected detached Event Actor execution guarantee. In-memory stores
  * support the same lifecycle within one process; Redis additionally makes it
  * recoverable across restarts and replica handoffs.
  */
  get detachedAgentEventActionStoreMode(): DetachedAgentEventActionStoreMode | undefined;
  get supportsDetachedAgentEventActions(): boolean;
  /** Installs the application-owned approval-expiry hook without coupling the
  * stream package to any particular trigger/scheduler implementation. */
  setApprovalExpiredHandler(handler?: ApprovalExpiredHandler): void;
  /** Installs a durable, generation-fenced terminal lifecycle adapter. */
  setTerminalHostActionHandler(handler?: TerminalHostActionHandler): void;
  /**
  * Event-driven retry for a durable terminal host action whose external
  * evidence arrived after the generation itself became terminal. The exact
  * generation fence prevents a delayed callback from touching a replacement;
  * the periodic recovery sweep remains the restart/replica fallback.
  */
  retryTerminalHostAction(streamId: string, expectedCreatedAt: number): Promise<boolean>;
  private get storeLabel();
  private syncRunningJobMetrics;
  private createApprovalLifecycle;
  /**
  * The approval lifecycle already won the exact barrier-to-error CAS. Reuse
  * the normal terminal finisher so attached clients receive the durable error
  * and every runtime/ownership resource is retired with the same epoch guards
  * as an ordinary generation failure.
  */
  private finishTimedOutPausePersistence;
  private acquireJobOwnership;
  private acquireResumedJobOwnership;
  private releaseJobOwnership;
  /**
  * Releases this generation's cross-replica listeners — abort AND preempt —
  * and drops any armed preempt requests. Every terminal site that retires a
  * runtime goes through here, so preempt state cannot outlive the
  * generation it belongs to.
  */
  private releaseAbortSubscription;
  private releasePreemptSubscription;
  private reconcileInactiveGeneration;
  private reconcileLostTerminalTransition;
  private reconcileFencedRuntimeHandoff;
  /** Promotes a terminal claim whose persistence owner disappeared to a conservative
  * terminal payload. The store CAS races safely with a slow owner: whichever
  * side finalizes first chooses the only payload subscribers may consume. */
  private recoverStaleTerminalPersistence;
  private registerAbortSubscription;
  private ensurePreemptState;
  /**
  * Arms ids that have not already been observed as removed. Returns how many
  * were actually accepted: a tombstoned id (its steer already drained at an
  * ordinary boundary while this request was in flight) or an over-cap id is
  * skipped, and the caller must not report those as armed.
  */
  private armPreemptIds;
  /**
  * Wakes the live model attempt after an accepted arm. Called for local and
  * cross-replica arms alike, since both land here through `armPreemptIds`.
  *
  * Errors are swallowed per listener: a wake is an optimization over the
  * per-chunk poll, and a throwing listener must not fail the arm that a
  * queued steer already depends on.
  */
  private notePreemptArmed;
  /**
  * Registers a wake listener for the SDK's `StreamPreemption.subscribe`.
  * Returns the unsubscribe the SDK calls when the model attempt ends.
  *
  * Fenced on `createdAt` like every other preempt entry point: a run wiring
  * itself to a replaced generation must not be woken by the replacement's
  * arms. A mismatch yields an inert unsubscribe rather than throwing, so the
  * run still starts — it simply falls back to the per-chunk poll.
  *
  * An arm that ALREADY happened is replayed on registration. Requests are
  * level-triggered, and the window between a job becoming steerable and the
  * SDK installing its listener is real: an interrupt landing there would be
  * recorded with no callbacks to notify, and on a silent or reasoning-only
  * turn no later chunk poll may ever run — the exact stall this channel
  * exists to remove. The replay makes the host correct on its own terms
  * rather than resting on when the SDK first reads the flag.
  */
  subscribePreempt(streamId: string, wake: () => void, jobCreatedAt?: number): () => void;
  /** Non-terminal disarm used by a capable→incapable owner handover. The
  * queue item remains valid, so this records a revision floor instead of the
  * permanent removal tombstone used by `clearPreemptIds`. */
  private downgradePreemptIds;
  /**
  * Disarms ids and tombstones them against a late-arriving arm. The
  * tombstone set is bounded by EVICTING its oldest entry rather than
  * refusing new ones: every drained or cancelled steer is tombstoned, not
  * just preempting ones, so a refusing cap would silently stop recording
  * after a long generation and let the late-arm race resurface. Set
  * iteration is insertion-ordered, so the first key is the oldest.
  */
  private clearPreemptIds;
  /**
  * Pub/sub delivery is not durable: an ARM can sit in a subscriber buffer
  * until after the steer drained and a handover reconciliation observed an
  * empty queue. Re-check the generation-fenced queue on receipt so that late
  * message cannot resurrect an interrupt with no payload behind it.
  *
  * The durable revision, rather than the publication's revision, is used for
  * a still-preempting item. This preserves liveness when an older duplicate
  * arrives after a later explicit arm, while `preempt: false` advances the
  * local floor and keeps a pre-downgrade publication from re-arming it.
  */
  private validateAndArmPreemptMessage;
  /**
  * Mirrors {@link registerAbortSubscription} for cooperative preempts. The
  * same double fence applies — runtime object identity plus the generation
  * `createdAt` carried by every {@link PreemptMessage} — so a stale
  * cross-replica publish can never arm a replacement job on the same
  * streamId. Arm requests cap at {@link STEER_QUEUE_MAX_DEPTH}, matching
  * the durable queue they mirror.
  *
  * Never rejects. Every caller fires this without awaiting (see the
  * createJob registration for why), so a propagating subscription error
  * would surface as an unhandled rejection: an alarming stack trace in
  * TerraMind's own server, which installs a global handler and keeps
  * serving, and a dead process for any other consumer of this package,
  * which under Node's default `--unhandled-rejections=throw` does not.
  * Neither is warranted — a failed subscription degrades this generation's
  * preemptive steers to the next tool boundary, the documented fallback.
  */
  private registerPreemptSubscription;
  private rejectSubscriptionDuringShutdown;
  private detachSubscriptionDuringShutdown;
  private registerAllSubscribersLeft;
  private stopSubscriberLease;
  private startSubscriberLease;
  private persistSubscriberCleanup;
  private drainSubscriberCleanups;
  /**
  * Get the job store instance (for advanced use cases).
  */
  getJobStore(): IJobStoreV2;
  /**
  * Create a new generation job.
  *
  * This sets up:
  * 1. Serializable job data in the job store
  * 2. Runtime state including the legacy, immediately-resolved readyPromise facade
  * 3. allSubscribersLeft callback for handling client disconnections
  *
  * Generation starts independently of SSE attachment. Early events are buffered locally and,
  * in Redis mode, persisted/published for replay when the client subscribes.
  *
  * @param streamId - Unique identifier for this stream
  * @param userId - User who initiated the request
  * @param conversationId - Optional conversation ID for lookup
  * @returns A facade object for the GenerationJob
  */
  private notifyReplacedGeneration;
  /** Finish local resource cleanup when a replacement cannot be exposed after
  * it already stopped the previous provider. The epoch and object checks keep
  * this from touching a newer runtime installed during any preceding await. */
  private retireStoppedPredecessorRuntime;
  /** Deliver every transaction-time predecessor receipt retained by the
  * current creation attempt, then epoch/attempt-fenced acknowledge only the
  * receipts whose provider/client handoff completed. A replacement racing the
  * acknowledgement inherits the still-unacknowledged chain and retries it. */
  private processDurableReplacementReceipts;
  /** A legacy started-mark EVAL can commit and lose its reply. Probe both
  * mirrors before failing closed: the same-slot primary is authoritative for
  * the full claim lineage, and the legacy key is safe only when it contains
  * that exact lineage plus this job's started epoch/protocol. SET-NX may
  * recreate an unexpectedly missing legacy mirror with the already-started
  * primary value, which establishes the rollout fence before exposure. */
  private confirmLegacyStartedFence;
  /**
  * A Redis script can commit a job and its started idempotency tombstone, then
  * lose the reply before the store repairs cross-slot membership. Recover only
  * when the durable job's opaque attempt id (and, when present, its started
  * claim) proves this invocation created this owner/tenant/epoch. The same-status transition is
  * intentionally idempotent: besides refreshing the live TTL, Redis uses it to
  * reconcile the running/user membership sets that live outside the stream's
  * cluster slot.
  */
  private recoverCommittedCreate;
  /** Persist a conservative FINAL for a job whose generation was never exposed
  * to its controller. This prevents both a provider-less running ghost and a
  * terminal hash that makes every duplicate retry wait until Redis TTL expiry. */
  private terminalizeUnexposedGeneration;
  createJob(streamId: string, userId: string, conversationId?: string, options?: CreateGenerationJobOptions): Promise<GenerationJob>;
  /**
  * Build a GenerationJob facade from composed services.
  *
  * This facade provides a unified API (job.emitter, job.abortController, etc.)
  * while internally delegating to the injected services (jobStore, eventTransport,
  * contentState). This allows swapping implementations (e.g., Redis) without
  * changing consumer code.
  *
  * IMPORTANT: The emitterProxy.on('allSubscribersLeft') handler registration
  * does NOT use eventTransport.subscribe(). This is intentional:
  *
  * If we used subscribe() for internal handlers, those handlers would count
  * as subscribers. When the real SSE client connects, isFirstSubscriber()
  * would return false (because internal handler was "first"), and readyPromise
  * would never resolve - causing a 5-second timeout delay before generation starts.
  *
  * Instead, allSubscribersLeft handlers are stored in runtime.allSubscribersLeftHandlers
  * and called directly from the onAllSubscribersLeft callback in createJob().
  *
  * @param streamId - The stream identifier
  * @param jobData - Serializable job metadata from job store
  * @param runtime - Non-serializable runtime state (abort controller, promises, etc.)
  * @returns A GenerationJob facade object
  */
  private buildJobFacade;
  /**
  * Get or create runtime state for a job.
  *
  * This enables cross-replica support in Redis mode:
  * - If runtime exists locally (same replica), return it
  * - If job exists in Redis but not locally (cross-replica), create minimal runtime
  *
  * The lazily-created runtime state is sufficient for:
  * - Subscribing to events (via Redis pub/sub)
  * - Getting resume state
  * - Handling reconnections
  * - Receiving cross-replica abort signals (via Redis pub/sub)
  *
  * @param streamId - The stream identifier
  * @returns Runtime state or null if job doesn't exist anywhere
  */
  private getOrCreateRuntimeState;
  /** Durable deletion evidence does not depend on a replica's runtime attachment. */
  getCleanupJob(streamId: string): Promise<Pick<GenerationJob, "createdAt" | "status" | "metadata"> | undefined>;
  getJob(streamId: string): Promise<GenerationJob | undefined>;
  /**
  * Check if a job exists.
  */
  hasJob(streamId: string): Promise<boolean>;
  /**
  * Atomically claim a start-generation request for `(userId, clientRequestId)`.
  * The first caller wins (`claimed: true`) and should create the job; a retried
  * request for the same submission loses and receives the original stream so it
  * can attach to it instead of starting a second billed generation.
  */
  claimGeneration(userId: string, clientRequestId: string, streamId: string, conversationId: string, generationProtocolVersion?: 1 | 2): Promise<IdempotencyClaimResult>;
  /** Checks the mixed-version admission key without creating or repairing a
  * claim. This is deliberately weaker than `claimGeneration`: callers may
  * use it only to exempt a confirmed retry from request rate limiting; the
  * controller must still perform the authoritative claim transition. */
  hasGenerationClaim(userId: string, clientRequestId: string): Promise<boolean>;
  /** Returns immutable proof that one exact request crossed generation
  * admission, even after the generation job itself has completed or been
  * replaced. Built-in stores retain this started receipt for the bounded
  * idempotency horizon; legacy custom stores fall back to the live job. */
  getGenerationAdmissionEvidence(userId: string, clientRequestId: string, streamId: string, conversationId?: string): Promise<{
    generationId: string;
    generationCreatedAt: number;
  } | null>;
  /** Atomically prevents an unpublished automatic continuation from starting
  * after its delivery has been retired for manual recovery. If recovery wins
  * the unstarted idempotency claim (directly or through the existing takeover
  * CAS), it converts that exact token into a started tombstone. A concurrent
  * `createJob` still holding the predecessor token then fails its atomic
  * create, while later duplicate POSTs take the settled/refetch path.
  *
  * `started` means job creation won the claim race; the caller must inspect
  * the generation itself before releasing any result ownership. */
  fenceGenerationClaimForRecovery(userId: string, clientRequestId: string, streamId: string, conversationId: string): Promise<"fenced" | "started" | "unavailable">;
  private generationClaimKey;
  /** Exact pre-bridge physical key through RedisJobStore.KEYS.idempotency:
  * `stream:idem:{userId:clientRequestId}`. The braces belong in the store key
  * argument because current stores no longer add a second hash tag. */
  private legacyGenerationClaimKey;
  /** Finish one proven cross-slot bridge step. A failed CAS may mean another
  * replica already completed the same value, so probe it before declaring an
  * ambiguous outcome. The probe's SET-NX behavior also safely recreates an
  * unexpectedly expired legacy mirror with the authoritative primary value. */
  private synchronizeLegacyGenerationClaim;
  /** Once an exact same-owner generation carrying this request id has been
  * observed, its later completion/deletion cannot erase the historical fact
  * that the request already started. Convert the freshly reacquired mirrors
  * into started tombstones even when the live-job adoption CAS loses that
  * race. The token CAS/probe never overwrites an unrelated claimant. */
  private tombstoneObservedGenerationClaim;
  takeoverGeneration(userId: string, clientRequestId: string, streamId: string, expected: IdempotencyClaimValue): Promise<IdempotencyClaimResult>;
  /** A fixed replay lease can expire while a long generation is still active.
  * If the identical client request reacquires that lease, atomically bind it
  * back to the matching live job instead of letting createJob replace/rebill
  * the generation. A different clientRequestId remains a genuinely new turn. */
  resumeClaimedGeneration(userId: string, clientRequestId: string, streamId: string, claim: IdempotencyClaimValue): Promise<IdempotencyClaimValue | null>;
  /**
  * Release a start-generation claim so the submission can be retried (e.g. the
  * start failed before generation began).
  */
  releaseGeneration(userId: string, clientRequestId: string, streamId: string, expected?: IdempotencyClaimValue): Promise<void>;
  /**
  * Get job status.
  */
  getJobStatus(streamId: string): Promise<GenerationJobStatus | undefined>;
  /**
  * Atomically win terminal ownership for a running generation and park the
  * exact steer leftovers in the same transaction. A controller must not emit
  * a terminal client event before this succeeds: abort, pause, and completion
  * can all race on the same generation epoch.
  */
  /**
  * Drain both delta coalescers ahead of a terminal status CAS. Coalesced
  * deltas still buffered for a stream must land under its live status:
  * flushed after the CAS they fence against the generation's own completion,
  * and the false receipts retire a healthy runtime and error-close its
  * subscribers ahead of the terminal frame. Every terminal transition that
  * can interrupt a live emitter (claim, abort, shutdown) must call this
  * before its CAS; no-op (two Map lookups) when coalescing is off or idle.
  */
  private flushCoalescedStreamBuffers;
  private persistAgentEventRunStepEvidence;
  claimTerminalJob(streamId: string, status: TerminalJobClaim["status"], error?: string, expectedCreatedAt?: number, options?: {
    persistencePending?: boolean;
    failedPauseActionId?: string;
  }): Promise<TerminalJobClaim | null>;
  /**
  * Durably settle and publish a persistence-owning terminal claim. Passing a
  * normal event means the controller's required writes succeeded; `null`
  * publishes conservative reconciliation instead. The store CAS also races a
  * stale-owner recovery, and whichever side wins supplies the only payload
  * this method will publish.
  */
  publishTerminalClaim(claim: TerminalJobClaim, finalEvent: ServerSentEvent | null): Promise<{
    finalEvent: ServerSentEvent;
    persistenceFailed: boolean;
    publicationFenced?: true;
  }>;
  /**
  * Release resources owned by a successful terminal claim. Reusing the same
  * claim is idempotent, and every local mutation is pinned to the runtime
  * object and generation epoch captured when the CAS won.
  */
  finishTerminalJob(claim: TerminalJobClaim): Promise<void>;
  private finishTerminalJobInternal;
  /**
  * Mark job as complete.
  * Returns true only when this caller won the terminal CAS and finished it;
  * controllers can safely gate generation-owned cleanup on that result.
  * If cleanupOnComplete is true (default), immediately cleans up job resources.
  * Exception: Jobs with errors are NOT immediately deleted to allow late-connecting
  * clients to receive the error (race condition where error occurs before client connects).
  * Note: eventTransport is NOT cleaned up here to allow the final event to be
  * fully transmitted. It will be cleaned up when subscribers disconnect or
  * by the periodic cleanup job.
  */
  completeJob(streamId: string, error?: string, expectedCreatedAt?: number, options?: {
    beforeErrorPublication?: () => Promise<void>;
  }): Promise<boolean>;
  /**
  * Atomically fail the exact paused action whose required unfinished-response
  * write did not succeed. Unlike `finishPausePersistence` followed by
  * `completeJob`, this never exposes the ordinary action id between those two
  * decisions, so a waiting/concurrent resume cannot drive an unpersisted turn.
  */
  failPausePersistence(streamId: string, actionId: string, error: string, expectedCreatedAt?: number): Promise<boolean>;
  /**
  * Abort a job (user-initiated).
  * Returns all data needed for token spending and message saving.
  *
  * Cross-replica support (Redis mode):
  * - Emits abort signal via Redis pub/sub
  * - The replica running generation receives signal and aborts its AbortController
  *
  * `options.transformAbortContent` rewrites the reconstructed content BEFORE
  * persistence filtering and the final SSE (and before it is returned for the DB save), so a
  * host-side stamp — e.g. re-attaching a paused `ask_user_question`'s args
  * that the Redis chunk-log reconstruction dropped — reaches the LIVE client
  * too, not just the saved message. Pure/optional; identity when omitted.
  */
  abortJob(streamId: string, options?: {
    transformAbortContent?: (content: TMessageContentParts[], jobData: SerializableJobData) => TMessageContentParts[];
    /** Required durable work (for example saving the partial response and
    * pruning the paused checkpoint) that must finish before the normal
    * FINAL lets the client submit against that history. Throwing emits a
    * conservative reconciliation frame instead of the normal payload. */
    beforePublish?: (result: AbortResult) => void | Promise<void>;
    /** Epoch observed by the authenticated route. Without this fence, a
    * same-stream replacement between authorization and the manager read
    * could be stopped by a stale request intended for its predecessor. */
    expectedCreatedAt?: number;
    /** Destructive user cleanup must wait until the provider owner has
    * completed every trailing persistence task, not merely received Stop. */
    awaitProviderDrain?: boolean;
  }): Promise<AbortResult>;
  /**
  * Subscribe to a job's event stream.
  *
  * This is called when an SSE client connects to /chat/stream/:streamId.
  * On first subscription:
  * - Resolves readyPromise (legacy, for API compatibility)
  * - Replays any buffered early events (e.g., 'created' event)
  *
  * Supports cross-replica reconnection in Redis mode:
  * - If job exists in Redis but not locally, creates minimal runtime state
  * - Events are delivered via Redis pub/sub, not in-memory EventEmitter
  *
  * @param streamId - The stream to subscribe to
  * @param onChunk - Handler for chunk events (streamed tokens, run steps, etc.)
  * @param onDone - Handler for completion event (includes final message)
  * @param onError - Handler for error events
  * @param options - Subscription configuration
  * @param options.skipBufferReplay - When true, skips replaying the earlyEventBuffer.
  *   Use this when a sync event was already sent (resume), since the sync's
  *   aggregatedContent already includes all buffered events.
  * @returns Subscription object with unsubscribe function, or null if job not found
  */
  subscribe(streamId: string, onChunk: ChunkHandler, onDone?: DoneHandler, onError?: ErrorHandler, options?: SubscribeOptions): Promise<StreamSubscription | null>;
  private attachSubscription;
  /**
  * Wait until every buffered publication has an authoritative Redis sequence before replay.
  * Replaying while a publication is unresolved can deliver the local copy and then deliver the
  * same event again when its late pub/sub message arrives.
  */
  private waitForEarlyEventPublications;
  private resetEarlyEventBuffer;
  /**
  * Buffers a pre-attachment event for local replay, enforcing hard bounds.
  *
  * A generation streaming with no attached subscriber can run for its entire
  * duration; unbounded buffering here retained every emitted event in memory,
  * with GC cost climbing alongside the heap. On overflow the whole buffer is
  * discarded and closed — the durable chunk log (Redis) or the resume
  * snapshot (in-memory) already owns recovery for late subscribers.
  *
  * @returns whether the event was accepted into the buffer.
  */
  private bufferEarlyEvent;
  private overflowEarlyEventBuffer;
  /**
  * If the subscriber that owns Redis attachment bootstrap disconnects, finish the
  * replay/sync for any concurrent subscriber. Otherwise the transport-wide reorder
  * fence remains closed forever because later subscribers observe hasSubscriber=true.
  */
  private continueEarlyEventBootstrap;
  private waitForFinalizedEarlyBufferOverflow;
  private settleEarlyBufferRecovery;
  /**
  * Snapshots resume state and attaches a paused live subscription.
  *
  * In-memory emissions during the snapshot-to-attachment interval are captured per resume,
  * so overlapping reconnects do not compete for the shared early-event buffer. Live delivery
  * remains paused until the caller writes its sync frame and activates the subscription.
  */
  subscribeWithResume(streamId: string, onChunk: ChunkHandler, onDone?: DoneHandler, onError?: ErrorHandler, options?: Pick<SubscribeOptions, "signal" | "expectedCreatedAt">): Promise<SubscribeWithResumeResult>;
  /**
  * Emit a chunk event to all subscribers.
  * Uses runtime state check for performance (avoids async job store lookup per token).
  *
  * If no subscriber has connected yet, buffers the event for replay when they do.
  * This ensures early events (like 'created') aren't lost due to race conditions.
  *
  * In Redis mode, awaits the publish to guarantee event ordering.
  * This is critical for streaming deltas (tool args, message content) to arrive in order.
  *
  * `options.durable` additionally awaits the Redis chunk append BEFORE the
  * transport publish (still best-effort on failure): events whose durable
  * record is the recovery source (e.g. `on_steer_applied`) must be in the
  * chunk log before any subscriber can observe the publish, or a
  * cross-replica reconnect can reconstruct content without them. The default
  * stays fire-and-forget — no added latency on the per-delta hot path.
  */
  /**
  * Flags a run as producing activity labels. Read back on resume so label
  * gap-reconciliation can be skipped for runs without the feature WITHOUT
  * inspecting content — the first label can be claimed inside the
  * snapshot->subscribe window, so content is not a reliable signal.
  * Best-effort: the flag is an optimization hint, never correctness.
  */
  markActivityLabels(streamId: string, expectedCreatedAt?: number): Promise<void>;
  /** Publish a durable non-streaming control event from any API replica.
  * `emitChunk` intentionally requires a local runtime, but steer/arm routes
  * may execute on a non-owner during horizontal deployment. When the owner
  * is local we retain the normal ordering/buffering path; otherwise the
  * generation-fenced chunk append commits before direct transport publish. */
  emitChunkFromAnyReplica(streamId: string, event: ServerSentEvent, expectedCreatedAt: number): Promise<boolean>;
  emitChunk(streamId: string, event: ServerSentEvent, options?: {
    durable?: boolean;
    expectedCreatedAt?: number; /** Atomically settles a claimed steer with its durable applied event. */
    deliveredSteer?: SteerQueueItem;
  }): Promise<void>;
  private emitChunkNow;
  private getReplacementHandoffState;
  private getReplacementHandoffStateBeforeDeadline;
  private cancelFencedRuntimeRetirements;
  private scheduleFencedRuntimeRetirement;
  private finishFencedRuntimeRetirement;
  private cleanupUnobservedFencedRuntime;
  private persistFencedRuntimeAbortProof;
  private recordFencedRuntimeAbortProof;
  private cleanupFencedRuntime;
  /** A generation-fenced append returning false is same-slot proof that this
  * epoch is no longer the durable owner. Stop its provider immediately, then
  * preserve captured subscribers while a durable successor still owns their
  * handoff receipt. A newer runtime is never touched. */
  private retireRuntimeAfterDurableFence;
  private preserveFencedRuntimeUntilHandoff;
  private releaseReplacementTransportHold;
  private isCurrentRuntime;
  private recordStartupEvent;
  private trackEvent;
  /**
  * Extract and save a run step from its wire envelope. Completed events wrap
  * the authoritative step under `result`; live and closed events carry their
  * payload directly. The terminal close is the authoritative execution status:
  * a tool-end payload may contain an error output even though the step closes
  * as failed.
  */
  private saveRunStepFromEvent;
  /**
  * Accumulate run steps for a stream.
  * Redis stores flush this buffer for cross-replica recovery; in-memory stores
  * retain it as a fallback after a paused run's live graph has been released.
  */
  private runStepBuffers;
  private accumulateRunStep;
  /**
  * Persist the last title event so resume sync can replay it. Content
  * aggregation only reconstructs message parts, so UI-only events need their
  * own metadata slot.
  */
  private trackTitleEvent;
  /**
  * Persist the latest context usage snapshot (one per model call) so a
  * resuming client can restore the context gauge without waiting for the
  * next model call.
  */
  private trackContextUsage;
  /**
  * Chains a read/modify/write job update onto the stream's queue so
  * concurrent writers can't clobber each other's merged state.
  */
  private queueJobWrite;
  /**
  * Persist replay-only stream events that are needed to reconstruct active
  * UI state on resume but are not represented by aggregated message content.
  */
  private trackReplayEvent;
  /**
  * Persist per-model-call token usage so resuming clients can rebuild
  * usage totals on any replica (the live collectedUsage array only exists
  * on the generating instance).
  */
  private trackTokenUsage;
  private persistTokenUsage;
  private persistReplayEvent;
  /**
  * Persist user message metadata from the created event.
  * Awaited in emitChunk so the HSET commits before the PUBLISH,
  * guaranteeing any cross-replica getJob() after the pub/sub window
  * finds userMessage in Redis.
  */
  private trackUserMessage;
  /**
  * Update job metadata.
  */
  updateMetadata(streamId: string, metadata: Partial<GenerationJobMetadata>, expectedCreatedAt?: number): Promise<void>;
  /** Stages exact detached terminal evidence in the generation-owned job outbox
  * and verifies the epoch-fenced write before the external result leaves memory. */
  persistAgentEventDetachedTerminalEvidence(streamId: string, expectedCreatedAt: number, evidence: NonNullable<GenerationJobMetadata["agentEventDetachedTerminalEvidence"]>): Promise<boolean>;
  /** Records that one exact provider segment has completed every trailing write.
  * The opaque segment id prevents a paused controller from acknowledging a
  * later HITL resume that reuses the same generation epoch. */
  /**
  * Wait for tracked generations to settle, bounded so shutdown still finishes inside a
  * container stop grace period. Anything still outstanding is left alone: asserting a drain
  * it has not reached is the unsafe direction, so we log and let the deployment's
  * hard-kill handling own that case.
  */
  private awaitGenerationSettlements;
  markProviderExecutionDrained(streamId: string, expectedCreatedAt: number, providerExecutionId: string): Promise<boolean>;
  private recordProviderExecutionDrain;
  /** Starts the initial provider owner only if account deletion, abort, or a
  * replacement has not already changed the exact running generation. */
  beginProviderExecution(streamId: string, expectedCreatedAt: number, providerExecutionId: string): Promise<boolean>;
  private providerExecutionKey;
  private openProviderExecution;
  private closeProviderExecution;
  /**
  * Release the tracker for an execution whose owner will never record its drain — a run
  * that settled its provider work but failed terminalization and deliberately publishes no
  * marker. Nothing is written: shutdown simply stops waiting on it, and the absent marker
  * keeps telling a successor the truth.
  */
  abandonProviderExecution(streamId: string, createdAt: number, providerExecutionId: string): void;
  /** Settle and drop every open tracker; used wherever the services they belong to go away. */
  private releaseOpenProviderExecutions;
  private waitForProviderExecutionDrain;
  private waitForProviderDrainIfRequired;
  /**
  * Set reference to the graph's contentParts array.
  */
  setContentParts(streamId: string, contentParts: Agents.MessageContentComplex[], expectedCreatedAt?: number): void;
  /**
  * Set reference to the collectedUsage array.
  * This array accumulates token usage from all models during generation.
  */
  setCollectedUsage(streamId: string, collectedUsage: UsageMetadata[], expectedCreatedAt?: number): void;
  /**
  * Set reference to the graph instance.
  */
  setGraph(streamId: string, graph: StandardGraph, expectedCreatedAt?: number): void;
  /**
  * The guarded human-review lifecycle for paused runs:
  * `approvals.pause()` / `peek()` / `resolve()` / `expire()`.
  *
  * This is the seam approval routes, the status endpoint, and the run wiring
  * cross — it owns the legal `requires_action` transitions and is race-safe
  * against concurrent resumes (a double-resolve would otherwise drive the run
  * twice). The job's chunks, run steps, and user-active-set membership are
  * preserved across a pause so the resume path can rebuild context; the store
  * refreshes the job-hash TTL to give the user the full window to respond.
  */
  get approvals(): ApprovalLifecycle;
  /**
  * The FIFO steering queue for mid-run user messages:
  * `steering.enqueue()` / `drain()` / `peek()` / `clear()`.
  *
  * The steer route enqueues from any instance; the owning process's
  * run-scoped PostToolBatch hook drains at the next tool-batch boundary.
  * Finalization paths drain leftovers into the final/abort events so the
  * client can convert them to queued follow-ups.
  */
  get steering(): SteeringLifecycle;
  /**
  * Arms a cooperative-seal request for one queued steer and reports whether
  * the arm actually reached an owner. Never touches job status.
  *
  * Returns true when this replica owns the generation (armed locally), or
  * when the fenced publish was received by at least one subscriber. Returns
  * FALSE when the publish failed or nobody was listening: the steer is still
  * durably queued and will inject at the next tool boundary, but nothing
  * will seal for it, so the caller must not acknowledge it as interrupting.
  * The owner's own `createdAt` fence still decides whether to honour it.
  */
  requestPreempt(streamId: string, steerId: string, jobCreatedAt: number, revision?: number): Promise<boolean>;
  /** O(1) level-triggered poll consumed by the run's `shouldPreempt`. */
  isPreemptRequested(streamId: string): boolean;
  /**
  * Clears preempt requests for steers that left the durable queue — drained
  * at a boundary (tool or preempt), cancelled, or dropped. The fenced
  * cross-replica `clear` keeps a request from outliving its steer on the
  * generating replica; without a fence identity the publish is skipped and
  * the empty-boundary path's self-clear bounds the damage to one seal.
  */
  noteSteersRemoved(streamId: string, steerIds: string[], jobCreatedAt?: number): Promise<boolean>;
  /**
  * Rebuilds the armed set from the DURABLE queue for a generation whose
  * ownership just moved (HITL resume landing on another replica).
  *
  * An arm lives only in the owning replica's runtime plus a transient
  * pub/sub message, while the steer's `preempt` flag is durable on the queue
  * item. A replica that never observed the original arm therefore starts
  * with an empty set and its poll stays false, so an interrupt the user
  * already had acknowledged would silently wait for an ordinary tool
  * boundary.
  *
  * REPLACES the armed set rather than adding to it. A replica that only ever
  * read this job still installed a facade runtime and subscribed, so it can
  * have accepted an arm and then missed the best-effort clear that followed
  * the drain. Promotion to owner makes that orphan live, and a union would
  * keep it: the first resumed stream seals on a steer no longer in the
  * queue, drains nothing, and truncates the resumed answer as
  * `preempt_incomplete`. The durable queue is the only authority at a
  * handover, so anything absent from it is disarmed and tombstoned here —
  * tombstoned because an arm that outlived its steer must not be able to
  * come back a second time.
  */
  rearmQueuedPreempts(streamId: string, jobCreatedAt: number): Promise<number>;
  /**
  * Snapshot of the ids armed right now, taken BEFORE a drain so the
  * empty-boundary disarm can scope itself to them.
  */
  getArmedPreemptIds(streamId: string, jobCreatedAt?: number): string[];
  /**
  * Disarms the requests a spent boundary was responsible for. Called when a
  * boundary drains nothing: the seal is already spent and those ids refer to
  * steers no longer in the queue, so leaving them armed would seal again on
  * the next chunk and truncate an unrelated answer.
  *
  * Scoped to an explicit id list rather than wiping the set: a second steer
  * can enqueue and arm between the atomic drain returning empty and this
  * call, and that arm is backed by a live queue item that has not been
  * injected yet.
  */
  clearPreemptRequests(streamId: string, steerIds: string[], jobCreatedAt?: number): void;
  /**
  * Get resume state for reconnecting clients.
  */
  getResumeState(streamId: string, expectedCreatedAt?: number, options?: {
    durableOnly?: boolean;
    validateEarlyBufferRecovery?: boolean;
    onContentSnapshot?: (snapshot: Awaited<ReturnType<IJobStore["getContentParts"]>>) => void;
  }): Promise<ResumeState | null>;
  /**
  * Mark that sync has been sent.
  * Persists to Redis for cross-replica consistency.
  */
  markSyncSent(streamId: string, expectedCreatedAt?: number): void;
  /**
  * Check if sync has been sent.
  * Checks local runtime first, then falls back to Redis for cross-replica scenarios.
  */
  wasSyncSent(streamId: string): Promise<boolean>;
  /**
  * Emit a done event.
  * Persists finalEvent to Redis for cross-replica access.
  */
  emitDone(streamId: string, event: ServerSentEvent, expectedCreatedAt?: number): Promise<void>;
  /**
  * Emit an error event.
  * Stores the error for late-connecting subscribers (race condition where error
  * occurs before client connects to SSE stream).
  */
  emitError(streamId: string, error: string, expectedCreatedAt?: number): Promise<void>;
  /**
  * Expire a single observed-stale pending approval NOW (immediate, not via the periodic
  * sweep): run the `requires_action → aborted` CAS — pinned to `actionId` so a concurrent
  * resolve + re-pause on a fresh action isn't aborted — and, on success, emit the terminal
  * `APPROVAL_EXPIRED_ERROR` so any attached SSE client gets a terminal event instead of a
  * hung stream. The durable checkpoint remains bounded by its Mongo TTL; deleting the
  * thread eagerly here could race a replacement generation. Used by the periodic sweeper
  * and by the resume route, which observes a just-expired action when the user submits a
  * decision after the TTL lapsed. Returns true if this call expired the action.
  */
  expireApproval(streamId: string, actionId?: string, expectedCreatedAt?: number): Promise<boolean>;
  private runApprovalExpiredHandler;
  private runTerminalHostActionHandler;
  private getTerminalRunSteps;
  private notifyApprovalExpiredRuntime;
  private expireStaleApprovals;
  /**
  * Let the manager (rather than store-only cleanup) win stale pause barriers
  * whenever this replica has the runtime. That preserves the same atomic
  * fail-closed store transition while also notifying an attached client and
  * releasing process-local graph/listener resources.
  */
  private failStalePausePersistenceBarriers;
  private cleanup;
  /**
  * Get stream info for status endpoint.
  */
  getStreamInfo(streamId: string): Promise<{
    active: boolean;
    status: GenerationJobStatus;
    aggregatedContent?: Agents.MessageContentComplex[];
    createdAt: number;
  } | null>;
  /**
  * Get total job count.
  */
  getJobCount(): Promise<number>;
  /** Returns sizes of internal runtime maps for diagnostics */
  getRuntimeStats(): {
    runtimeStateSize: number;
    fencedRuntimeRetirements: number;
    runStepBufferSize: number;
    eventTransportStreams: number;
    earlyBufferedEvents: number;
    earlyBufferedBytes: number;
  };
  /**
  * Get job count by status.
  */
  getJobCountByStatus(): Promise<Record<GenerationJobStatus, number>>;
  /**
  * Get active job IDs for a user.
  * Returns conversation IDs of running jobs belonging to the user.
  * Performs self-healing cleanup of stale entries.
  *
  * @param userId - The user ID to query
  * @returns Array of conversation IDs with active jobs
  */
  getActiveJobIdsForUser(userId: string, tenantId?: string): Promise<string[]>;
  /** Returns every generation whose provider can still mutate user-owned data,
  * including a terminal generation whose controller is finishing trailing writes. */
  getCleanupBlockingJobIdsForUser(userId: string, tenantId?: string): Promise<string[]>;
  /** Also includes retained paused jobs whose checkpoints belong to this account. */
  getAccountCleanupJobIdsForUser(userId: string, tenantId?: string): Promise<string[]>;
  /** Resolves every cleanup-blocking run attached to any target conversation.
  * Remote API runs use response IDs as stream identities, so conversation
  * deletion cannot assume one stream per conversation. */
  getCleanupBlockingJobIdsForConversations(userId: string, conversationIds: readonly string[], tenantId?: string): Promise<string[]>;
  private finalizeOwnedJobsForShutdown;
  /**
  * Stop accepting jobs and close only this process's attached SSE responses.
  *
  * This runs before HTTP drain. Durable finalization waits until post-drain, when only jobs
  * still owned by this process are atomically moved to a terminal state.
  */
  prepareForShutdown(): void;
  /**
  * Destroy the manager.
  * Cleans up all resources including runtime state, buffers, and stores.
  */
  destroy(options?: {
    settlementBudgetMs?: number;
  }): Promise<void>;
}
declare const GenerationJobManager: GenerationJobManagerClass;
//#endregion
//#region src/stream/jobStoreCapabilities.d.ts
/**
* Methods added by the v2 generation-store contract.
*
* Keep this list in lockstep with {@link IJobStoreV2}: it is the runtime
* boundary that lets the public {@link IJobStore} type remain source-compatible
* while rejecting an implementation that cannot provide the atomic guarantees
* required by the current generation manager.
*/
declare const JOB_STORE_V2_REQUIRED_METHODS: readonly ["acknowledgeReplacedJobs", "markProviderExecutionDrained", "beginProviderExecution", "getCleanupBlockingJobIdsByUser", "finalizeTerminalPersistence", "transitionStatusAndDrainSteers", "takeoverIdempotencyKey", "markIdempotencyKeyStarted", "adoptIdempotencyKeyForJob", "enqueueSteerVersioned", "enqueueSteerWithReceipt", "getSteerReceipt", "restoreClaimedSteers", "admitTerminalSteers", "peekClaimedSteers", "armSteer", "armSteerVersioned", "downgradeSteerPreempts", "claimParkedSteersDetailed", "consumeParkedSteer", "discardSteerLeftover", "settleEarlyBufferRecovery", "finalizeEarlyBufferOverflow", "hasSubscriberAttached", "claimFirstSubscriber", "detachSubscriber", "hasActiveSubscriber"];
type JobStoreV2RequiredMethod = (typeof JOB_STORE_V2_REQUIRED_METHODS)[number];
/** Return the v2 capabilities absent from a legacy-compatible store. */
declare function getMissingJobStoreV2Methods(store: IJobStore): JobStoreV2RequiredMethod[];
/**
* Narrow a public/legacy store to the contract the current runtime requires.
* Throws before the manager mutates any configuration when capabilities are
* missing, so a custom-store upgrade fails loudly and deterministically.
*/
declare function assertJobStoreV2(store: IJobStore): asserts store is IJobStoreV2;
//#endregion
//#region src/stream/createStreamServices.d.ts
/**
* Configuration for stream services (optional overrides)
*/
interface StreamServicesConfig {
  /**
  * Override Redis detection. If not provided, uses cacheConfig.USE_REDIS.
  */
  useRedis?: boolean;
  /**
  * Override Redis client. If not provided, uses ioredisClient from cache.
  */
  redisClient?: Redis | Cluster | null;
  /**
  * Dedicated Redis client for pub/sub subscribing.
  * If not provided, will duplicate the main client.
  */
  redisSubscriber?: Redis | Cluster | null;
  /**
  * Options for in-memory job store
  */
  inMemoryOptions?: {
    ttlAfterComplete?: number;
    maxJobs?: number;
    staleJobTimeout?: number;
  };
}
/**
* Stream services result
*/
interface StreamServices {
  jobStore: IJobStoreV2;
  eventTransport: IEventTransport;
  isRedis: boolean;
}
/**
* Create stream services (job store + event transport).
*
* Automatically detects Redis from cacheConfig.USE_REDIS_STREAMS and uses
* the existing ioredisClient. Falls back to in-memory if Redis
* is not configured or not available.
*
* USE_REDIS_STREAMS defaults to USE_REDIS if not explicitly set,
* allowing users to disable Redis for streams while keeping it for other caches.
*
* @example Auto-detect (uses cacheConfig)
* ```ts
* const services = createStreamServices();
* // Uses Redis if USE_REDIS_STREAMS=true (defaults to USE_REDIS), otherwise in-memory
* ```
*
* @example Force in-memory
* ```ts
* const services = createStreamServices({ useRedis: false });
* ```
*/
declare function createStreamServices(config?: StreamServicesConfig): StreamServices;
//#endregion
//#region src/stream/abortContent.d.ts
type PersistableAbortContentPart = Agents.MessageContentComplex | TMessageContentParts;
declare function hasPersistableAbortContent(content: unknown): boolean;
declare function filterPersistableAbortContent(content: unknown): PersistableAbortContentPart[];
//#endregion
//#region src/stream/elapsed.d.ts
/**
* Age of a generation computed entirely on this process's clock, for status
* responses whose clients rebuild a clock-local elapsed baseline instead of
* comparing timestamps across machines. Clamped: a job stamped by a replica
* whose clock ran ahead of this one must never report a negative age.
*/
declare function getGenerationElapsedMs(job: {
  createdAt: number;
}): number;
//#endregion
//#region src/stream/implementations/InMemoryJobStore.d.ts
/**
* In-memory implementation of IJobStoreV2.
* Suitable for single-instance deployments.
* For horizontal scaling, use RedisJobStore.
*
* Content state is tied to jobs:
* - Uses WeakRef to graph for live access to contentParts and contentData (run steps)
* - No chunk persistence needed - same instance handles generation and reconnects
*/
declare class InMemoryJobStore implements IJobStoreV2 {
  readonly detachedAgentEventActionStoreMode = "process_local";
  private jobs;
  private contentState;
  private cleanupInterval;
  /** Maps userId -> Set of streamIds (conversationIds) for active jobs */
  private userJobMap;
  /**
  * Maps streamId -> last generation-activity timestamp. Refreshed via
  * recordActivity() on each emitted chunk so the stale-job failsafe reaps on
  * inactivity (a hung generation) rather than age (a long but live stream).
  */
  private lastActivity;
  /** Maps streamId -> FIFO queue of pending steer messages. */
  private steerQueues;
  /** FIFO items removed from the pending queue but not yet durably applied.
  * Keeping this independently of receipts also protects legacy steers that
  * do not carry a clientSteerId. */
  private claimedSteers;
  /** Stream ids whose steer queue was closed by a terminal drain. Reopened by createJob. */
  private closedSteerQueues;
  /** Parked terminally-drained steers — lifecycle-independent of `jobs` (the
  *  default completeJob path deletes the job record immediately). */
  private parkedSteers;
  /** Conversation-scoped lost-ACK receipts; deliberately survives job replacement. */
  private steerReceipts;
  /** Maps idempotency key -> claimed stream + expiry, deduping retried start requests. */
  private idempotencyClaims;
  /** Time to keep completed jobs before cleanup (0 = immediate) */
  private ttlAfterComplete;
  /** Maximum number of concurrent jobs */
  private maxJobs;
  /** Store-lifetime generation clock. A completed job may be deleted before a
  * same-millisecond replacement starts, so the current jobs map alone cannot
  * guarantee a distinct checkpoint namespace. */
  private lastGenerationEpoch;
  /** Latest per-stream epoch retained briefly after its job disappears. This
  * is also the conditional-create fence: a delayed queue drain must not treat
  * an empty job slot as proof that no intervening generation ran. */
  private generationEpochs;
  /**
  * Failsafe timeout (ms) for jobs stuck in "running" status. Mirrors
  * RedisJobStore's running-job TTL: a crashed or hung generation that never
  * reaches a terminal state would otherwise retain its content state forever,
  * leaking the full message context until the process runs out of memory.
  * 0 disables the failsafe. Default: 20 minutes.
  */
  private staleJobTimeout;
  constructor(options?: {
    ttlAfterComplete?: number;
    maxJobs?: number;
    staleJobTimeout?: number;
  });
  initialize(): Promise<void>;
  private getRetainedGenerationEpoch;
  private retainGenerationEpoch;
  createJob(streamId: string, userId: string, conversationId?: string, tenantId?: string, initialMetadata?: JobMetadataPatch, recoveredSteerId?: string, idempotencyClaimKey?: string, idempotencyClaimToken?: string, idempotencyClientRequestId?: string, recoveredSteerPayload?: RecoveredSteerPayload, creationAttemptId?: string, expectedPredecessorCreatedAt?: number, rejectActivePredecessor?: boolean): Promise<CreatedJobData>;
  getJob(streamId: string): Promise<SerializableJobData | null>;
  acknowledgeReplacedJobs(streamId: string, creationAttemptId: string, replacedCreatedAts: readonly number[]): Promise<boolean>;
  updateJob(streamId: string, updates: Partial<SerializableJobData>, expectedCreatedAt?: number): Promise<void>;
  markProviderExecutionDrained(streamId: string, expectedCreatedAt: number, providerExecutionId: string): Promise<boolean>;
  beginProviderExecution(streamId: string, expectedCreatedAt: number, providerExecutionId: string): Promise<boolean>;
  finalizeTerminalPersistence(streamId: string, expectedCreatedAt: number, finalEvent: string): Promise<boolean>;
  /**
  * Atomic in-memory: the single-threaded event loop makes the
  * read-check-write sequence indivisible, so the status guard is exact.
  * Membership/counts derive from `job.status` directly, so there are no
  * sets to reconcile here.
  */
  transitionStatus(streamId: string, args: JobStatusTransition): Promise<boolean>;
  transitionStatusAndDrainSteers(streamId: string, args: JobStatusTransition): Promise<SteerQueueItem[] | null>;
  claimIdempotencyKey(key: string, value: IdempotencyClaimValue, ttlSeconds: number): Promise<IdempotencyClaimResult>;
  hasIdempotencyKey(key: string): Promise<boolean>;
  getIdempotencyClaim(key: string): Promise<IdempotencyClaimValue | null>;
  takeoverIdempotencyKey(key: string, expected: IdempotencyClaimValue, value: IdempotencyClaimValue, ttlSeconds: number): Promise<boolean>;
  markIdempotencyKeyStarted(key: string, claimToken: string, startedAt: number, ttlSeconds: number): Promise<boolean>;
  adoptIdempotencyKeyForJob(key: string, expected: IdempotencyClaimValue, streamId: string, userId: string, clientRequestId: string, tenantId: string | undefined, expectedCreatedAt: number, ttlSeconds: number, allowMissingClientRequestId?: boolean): Promise<boolean>;
  releaseIdempotencyKey(key: string, expected?: IdempotencyClaimValue): Promise<void>;
  deleteJob(streamId: string, expectedCreatedAt?: number): Promise<boolean>;
  /**
  * Refresh a job's last-activity timestamp (called on each emitted chunk) so the
  * stale-job failsafe in cleanup() reaps on inactivity rather than total age,
  * mirroring RedisJobStore refreshing the running TTL on each appendChunk.
  */
  recordActivity(streamId: string, expectedCreatedAt?: number): void;
  hasJob(streamId: string): Promise<boolean>;
  getRunningJobs(): Promise<SerializableJobData[]>;
  getRequiresActionJobs(): Promise<SerializableJobData[]>;
  getTerminalHostActionJobs(): Promise<SerializableJobData[]>;
  clearTerminalHostAction(streamId: string, expectedCreatedAt?: number): Promise<void>;
  cleanup(): Promise<number>;
  private evictOldest;
  /** Get job count (for monitoring) */
  getJobCount(): Promise<number>;
  /** Get job count by status (for monitoring) */
  getJobCountByStatus(status: JobStatus): Promise<number>;
  destroy(): Promise<void>;
  /**
  * Get active job IDs for a user.
  * Returns conversation IDs of running jobs belonging to the user.
  * Also performs self-healing cleanup: removes stale entries for jobs that no longer exist.
  */
  getActiveJobIdsByUser(userId: string, tenantId?: string): Promise<string[]>;
  getCleanupBlockingJobIdsByUser(userId: string, tenantId?: string): Promise<string[]>;
  getCleanupJobIdsByUser(userId: string, tenantId?: string): Promise<string[]>;
  getRetainedJobIdsByUser(userId: string, tenantId?: string): Promise<string[]>;
  private getJobIdsByUser;
  /**
  * Set the graph reference for a job.
  * Uses WeakRef to allow garbage collection when graph is no longer needed.
  */
  setGraph(streamId: string, graph: StandardGraph, expectedCreatedAt?: number): void;
  /**
  * Set content parts reference for a job.
  */
  setContentParts(streamId: string, contentParts: Agents.MessageContentComplex[], expectedCreatedAt?: number): void;
  /**
  * Set collected usage reference for a job.
  */
  setCollectedUsage(streamId: string, collectedUsage: UsageMetadata[], expectedCreatedAt?: number): void;
  /**
  * Get collected usage for a job.
  */
  getCollectedUsage(streamId: string, expectedCreatedAt?: number): UsageMetadata[];
  /**
  * Get content parts for a job.
  * Returns live content from stored reference.
  */
  getContentParts(streamId: string, expectedCreatedAt?: number, _options?: {
    durableOnly?: boolean;
  }): Promise<{
    content: Agents.MessageContentComplex[];
    reconstructedEventCount?: number;
    durableEventCount?: number;
  } | null>;
  settleEarlyBufferRecovery(streamId: string, expectedCreatedAt: number, overflowId: string, settlement: Pick<EarlyBufferOverflowState, "recoveryMethod" | "recoveryOutcome" | "recoveryCompletedAt" | "recoveryFailureReason">): Promise<boolean>;
  finalizeEarlyBufferOverflow(streamId: string, expectedCreatedAt: number, overflowId: string, finalizedOverflow: EarlyBufferOverflowState): Promise<boolean>;
  hasSubscriberAttached(streamId: string, expectedCreatedAt: number): Promise<boolean>;
  claimFirstSubscriber(streamId: string, expectedCreatedAt: number, attachedAt: number, subscriberId: string, leaseExpiresAt: number): Promise<boolean>;
  detachSubscriber(streamId: string, expectedCreatedAt: number, subscriberId: string): Promise<void>;
  hasActiveSubscriber(streamId: string, expectedCreatedAt: number, observedAt: number): Promise<boolean>;
  /**
  * Get run steps for a job from graph.contentData.
  * Uses WeakRef - may return empty if graph has been GC'd.
  */
  getRunSteps(streamId: string, expectedCreatedAt?: number): Promise<Agents.RunStep[]>;
  /**
  * No-op for in-memory - content available via graph reference.
  */
  appendChunk(streamId: string, _event: unknown, expectedCreatedAt?: number, deliveredSteer?: SteerQueueItem): Promise<boolean>;
  /**
  * Clear content state for a job.
  */
  clearContentState(streamId: string, expectedCreatedAt?: number): void;
  enqueueSteer(streamId: string, item: SteerQueueItem, expectedCreatedAt?: number): Promise<number>;
  enqueueSteerVersioned(streamId: string, item: SteerQueueItem, wantsPreempt: boolean, expectedCreatedAt?: number): Promise<SteerEnqueueVersionedResult>;
  private normalizeSteerReceipt;
  private claimedSteerItems;
  getSteerReceipt(streamId: string, clientSteerId: string): Promise<SteerReceipt | null>;
  enqueueSteerWithReceipt(streamId: string, item: SteerQueueItem, receiptInput: SteerReceiptInput, wantsPreempt: boolean, expectedCreatedAt?: number): Promise<SteerEnqueueReceiptResult>;
  private settleSteerReceipts;
  /** With `expectedCreatedAt`, refuses when the job was replaced — a stale
  *  run's drain must never consume (or close) a replacement job's queue. */
  drainSteers(streamId: string, expectedCreatedAt?: number): Promise<SteerQueueItem[]>;
  restoreClaimedSteers(streamId: string, items: SteerQueueItem[], expectedCreatedAt?: number): Promise<boolean>;
  admitTerminalSteers(streamId: string, policy: TerminalSteerAdmissionPolicy, expectedCreatedAt?: number): Promise<TerminalSteerAdmissionResult>;
  closeAndDrainSteers(streamId: string, expectedCreatedAt?: number): Promise<SteerQueueItem[]>;
  peekSteers(streamId: string, expectedCreatedAt?: number): Promise<SteerQueueItem[]>;
  peekClaimedSteers(streamId: string, expectedCreatedAt?: number): Promise<SteerQueueItem[]>;
  clearSteers(streamId: string): Promise<void>;
  removeSteer(streamId: string, steerId: string, expectedCreatedAt?: number): Promise<boolean>;
  armSteer(streamId: string, steerId: string, expectedCreatedAt?: number): Promise<SteerArmOutcome>;
  private queuedReceiptForMutation;
  armSteerVersioned(streamId: string, steerId: string, expectedCreatedAt?: number): Promise<SteerArmResult>;
  downgradeSteerPreempts(streamId: string, expectedCreatedAt?: number): Promise<SteerQueueItem[] | null>;
  parkSteers(streamId: string, payload: string, expectedCreatedAt?: number): Promise<void>;
  claimParkedSteers(streamId: string, ownerUserId: string, ownerTenantId?: string): Promise<string | undefined>;
  claimParkedSteersDetailed(streamId: string, ownerUserId: string, ownerTenantId?: string, requestedProtocolVersion?: 1 | 2): Promise<ParkedSteerClaim | undefined>;
  discardSteerLeftover(streamId: string, clientSteerId: string, steerId: string, ownerUserId: string, ownerTenantId?: string, expectedGenerationCreatedAt?: number): Promise<boolean>;
  private parkLeftovers;
  /** Lease without deleting: an initialization failure makes the item visible
  * again as soon as this generation becomes terminal. */
  private leaseParkedSteer;
  /** A recovery source must outlive the generation that hides it, plus one
  * normal parked-recovery window after a stale-job reap exposes it again. */
  private runningRecoveryLeaseExpiry;
  private extendRecoveryLease;
  consumeParkedSteer(streamId: string, steerId: string, ownerUserId: string, ownerTenantId: string | undefined, expectedCreatedAt: number): Promise<boolean>;
  /** Terminal cleanup (approval expiry, stale-running reap) must not drop
  *  202-accepted steers with the job. */
  private isParkedRecoveryCompatible;
  private parkQueuedSteers;
}
//#endregion
//#region src/stream/implementations/InMemoryEventTransport.d.ts
/**
* In-memory event transport using Node.js EventEmitter.
* For horizontal scaling, replace with RedisEventTransport.
*/
declare class InMemoryEventTransport implements IEventTransport {
  private streams;
  private providerDrainProofs;
  private providerDrainWrites;
  private static readonly PROVIDER_DRAIN_PROOF_TTL_MS;
  private providerDrainKey;
  private getOrCreateStream;
  subscribe(streamId: string, handlers: {
    onChunk: (event: unknown, generationId?: number) => void;
    onDone?: (event: unknown, generationId?: number) => void;
    onError?: (error: string, generationId?: number) => void;
  }): {
    unsubscribe: () => void;
    ready?: Promise<void>;
  };
  emitChunk(streamId: string, event: unknown, generationId?: number): void;
  emitDone(streamId: string, event: unknown, generationId?: number): void;
  emitError(streamId: string, error: string, generationId?: number): void;
  renewDemand(_streamId: string, _ttlMs: number): void;
  hasDemand(streamId: string): boolean;
  recordProviderDrain(streamId: string, generationId: number, providerExecutionId: string): Promise<boolean>;
  hasProviderDrain(streamId: string, generationId: number, providerExecutionId: string): Promise<boolean>;
  getSubscriberCount(streamId: string): number;
  onAllSubscribersLeft(streamId: string, callback: () => void): void;
  /**
  * Check if this is the first subscriber (for ready signaling)
  */
  isFirstSubscriber(streamId: string): boolean;
  closeLocalSubscribers(streamId: string, error: string): void;
  /**
  * Cleanup a stream's event emitter
  */
  cleanup(streamId: string): void;
  /**
  * Get count of tracked streams (for monitoring)
  */
  getStreamCount(): number;
  /**
  * Get all tracked stream IDs (for orphan cleanup)
  */
  getTrackedStreamIds(): string[];
  destroy(): void;
}
//#endregion
//#region src/stream/implementations/RedisJobStore.d.ts
/**
* Redis implementation of IJobStoreV2.
* Enables horizontal scaling with multi-instance deployments.
*
* Storage strategy:
* - Job metadata: Redis Hash (fast field access)
* - Chunks: Redis Streams (append-only, efficient for streaming)
* - Run steps: Redis String (JSON serialized)
*
* Note: streamId === conversationId, so getJob(conversationId) works directly.
*
* @example
* ```ts
* import { ioredisClient } from '~/cache';
* const store = new RedisJobStore(ioredisClient);
* await store.initialize();
* ```
*/
/**
* Configuration options for RedisJobStore
*/
interface RedisJobStoreOptions {
  /** TTL for completed jobs in seconds (default: 300 = 5 minutes) */
  completedTtl?: number;
  /** TTL for running jobs/chunks in seconds (default: 1200 = 20 minutes) */
  runningTtl?: number;
  /** TTL for chunks after completion in seconds (default: 0 = delete immediately) */
  chunksAfterCompleteTtl?: number;
  /** TTL for run steps after completion in seconds (default: 0 = delete immediately) */
  runStepsAfterCompleteTtl?: number;
  /** TTL for per-user job tracking sets in seconds (default: 86400 = 24 hours). 0 = no TTL. */
  userJobsSetTtl?: number;
  /** Backstop TTL for a paused (requires_action) job in seconds (default: 86400 = 24 hours). */
  requiresActionTtl?: number;
}
declare class RedisJobStore implements IJobStoreV2 {
  readonly detachedAgentEventActionStoreMode = "distributed";
  private redis;
  private cleanupInterval;
  private cleanupMembershipReady?;
  private ttl;
  /** Coalescable chunk appends awaiting their window flush, per stream */
  private pendingAppends;
  /** Durable-append coalescing window; 0 keeps every append on the per-event path */
  private readonly coalesceWindowMs;
  /** Whether Redis client is in cluster mode (affects pipeline usage) */
  private isCluster;
  /**
  * Local cache for graph references on THIS instance.
  * Enables fast reconnects when client returns to the same server.
  * Uses WeakRef to allow garbage collection when graph is no longer needed.
  */
  private localGraphCache;
  /**
  * Local cache for collectedUsage arrays.
  * Generation happens on a single instance, so collectedUsage is only available locally.
  * For cross-replica abort, the abort handler falls back to text-based token counting.
  */
  private localCollectedUsageCache;
  /** Same-instance HOST content view (includes host-authored parts like
  *  steers, which the SDK graph never sees). Preferred over the graph cache
  *  on local reads; cross-instance reads reconstruct from chunks. */
  private localContentParts;
  /** Cleanup interval in ms (1 minute) */
  private cleanupIntervalMs;
  constructor(redis: Redis | Cluster, options?: RedisJobStoreOptions);
  initialize(): Promise<void>;
  private getLocalEntry;
  private setLocalEntry;
  private deleteLocalEntry;
  private clearLocalState;
  private clearPredecessorLocalState;
  createJob(streamId: string, userId: string, conversationId?: string, tenantId?: string, initialMetadata?: JobMetadataPatch, recoveredSteerId?: string, idempotencyClaimKey?: string, idempotencyClaimToken?: string, idempotencyClientRequestId?: string, recoveredSteerPayload?: RecoveredSteerPayload, creationAttemptId?: string, expectedPredecessorCreatedAt?: number, rejectActivePredecessor?: boolean): Promise<CreatedJobData>;
  getJob(streamId: string): Promise<SerializableJobData | null>;
  acknowledgeReplacedJobs(streamId: string, creationAttemptId: string, replacedCreatedAts: readonly number[]): Promise<boolean>;
  updateJob(streamId: string, updates: Partial<SerializableJobData>, expectedCreatedAt?: number): Promise<void>;
  settleEarlyBufferRecovery(streamId: string, expectedCreatedAt: number, overflowId: string, settlement: Pick<EarlyBufferOverflowState, "recoveryMethod" | "recoveryOutcome" | "recoveryCompletedAt" | "recoveryFailureReason">): Promise<boolean>;
  finalizeEarlyBufferOverflow(streamId: string, expectedCreatedAt: number, overflowId: string, finalizedOverflow: EarlyBufferOverflowState): Promise<boolean>;
  hasSubscriberAttached(streamId: string, expectedCreatedAt: number): Promise<boolean>;
  claimFirstSubscriber(streamId: string, expectedCreatedAt: number, attachedAt: number, subscriberId: string, leaseExpiresAt: number): Promise<boolean>;
  detachSubscriber(streamId: string, expectedCreatedAt: number, subscriberId: string): Promise<void>;
  hasActiveSubscriber(streamId: string, expectedCreatedAt: number, observedAt: number): Promise<boolean>;
  markProviderExecutionDrained(streamId: string, expectedCreatedAt: number, providerExecutionId: string): Promise<boolean>;
  beginProviderExecution(streamId: string, expectedCreatedAt: number, providerExecutionId: string): Promise<boolean>;
  finalizeTerminalPersistence(streamId: string, expectedCreatedAt: number, finalEvent: string): Promise<boolean>;
  private sameMembershipSource;
  private addObservedUserKey;
  private ownerMembershipTtl;
  private retainCleanupOwner;
  /** Recover retained work from older stores once; owner requests await recovery. */
  private ensureCleanupMembership;
  private applyMembershipSnapshot;
  /**
  * Cross-slot sets are derived state, so every mutation writes the membership
  * implied by the durable job hash and then reads the hash again. If a status
  * change or replacement crossed that window, the loop repairs from the newer
  * source. Because every writer uses this path, the final writer always
  * converges even when an older reconciliation finishes later.
  */
  private reconcileJobMembership;
  private clearLocalStateUnlessActive;
  /**
  * Live-key TTL (seconds) for a paused job. A paused job isn't a hung
  * generation, so it uses the longer requires_action backstop rather than the
  * running TTL — otherwise a no-expiry approval (the buildPendingAction
  * default), which the API treats as "live", would be evicted after the 20m
  * running window. A pendingAction with an `expiresAt` farther out than the
  * backstop extends to cover it, plus a grace margin so a decision arriving
  * right at the deadline can still resume.
  */
  private pauseTtlSeconds;
  /** Same grace lets cleanup park accepted words before Redis expires the
  * live hash/list keys at the semantic stale cutoff. */
  private runningStorageTtlSeconds;
  private parkedRecoveryTtlSeconds;
  /** The membership set a status belongs to; terminal statuses have none. */
  private statusSetKey;
  transitionStatus(streamId: string, args: JobStatusTransition): Promise<boolean>;
  transitionStatusAndDrainSteers(streamId: string, args: JobStatusTransition): Promise<SteerQueueItem[] | null>;
  private transitionStatusInternal;
  claimIdempotencyKey(key: string, value: IdempotencyClaimValue, ttlSeconds: number): Promise<IdempotencyClaimResult>;
  hasIdempotencyKey(key: string): Promise<boolean>;
  getIdempotencyClaim(key: string): Promise<IdempotencyClaimValue | null>;
  takeoverIdempotencyKey(key: string, expected: IdempotencyClaimValue, value: IdempotencyClaimValue, ttlSeconds: number): Promise<boolean>;
  markIdempotencyKeyStarted(key: string, claimToken: string, startedAt: number, ttlSeconds: number): Promise<boolean>;
  adoptIdempotencyKeyForJob(key: string, expected: IdempotencyClaimValue, streamId: string, userId: string, clientRequestId: string, tenantId: string | undefined, expectedCreatedAt: number, ttlSeconds: number, allowMissingClientRequestId?: boolean): Promise<boolean>;
  releaseIdempotencyKey(key: string, expected?: IdempotencyClaimValue): Promise<void>;
  deleteJob(streamId: string, expectedCreatedAt?: number): Promise<boolean>;
  private deleteStaleRunningJob;
  hasJob(streamId: string): Promise<boolean>;
  getRunningJobs(): Promise<SerializableJobData[]>;
  getRequiresActionJobs(): Promise<SerializableJobData[]>;
  getTerminalHostActionJobs(): Promise<SerializableJobData[]>;
  getDetachedAgentEventTerminalHostActionJobs(): Promise<SerializableJobData[]>;
  private getIndexedTerminalHostActionJobs;
  clearTerminalHostAction(streamId: string, expectedCreatedAt?: number): Promise<void>;
  cleanup(): Promise<number>;
  private cleanupRequiresActionIndex;
  getJobCount(): Promise<number>;
  getJobCountByStatus(status: JobStatus): Promise<number>;
  private countJobsInStatusSet;
  /**
  * Get active job IDs for a user.
  * Returns conversation IDs of running jobs belonging to the user.
  * Also performs self-healing cleanup: removes stale entries for jobs that no longer exist.
  *
  * @param userId - The user ID to query
  * @returns Array of conversation IDs with active jobs
  */
  getActiveJobIdsByUser(userId: string, tenantId?: string): Promise<string[]>;
  getCleanupBlockingJobIdsByUser(userId: string, tenantId?: string): Promise<string[]>;
  getCleanupJobIdsByUser(userId: string, tenantId?: string): Promise<string[]>;
  getRetainedJobIdsByUser(userId: string, tenantId?: string): Promise<string[]>;
  private getJobIdsByUser;
  destroy(): Promise<void>;
  /**
  * Store graph reference in local cache.
  * This enables fast reconnects when client returns to the same instance.
  * Falls back to Redis chunk reconstruction for cross-instance reconnects.
  *
  * @param streamId - The stream identifier
  * @param graph - The graph instance (stored as WeakRef)
  */
  setGraph(streamId: string, graph: StandardGraph, expectedCreatedAt?: number): void;
  /** Splice-inserts host-authored parts (steers from `on_steer_applied`,
  *  activity labels from `on_activity_label` chunks) into an SDK-graph
  *  content view, ascending by recorded index so each host-view position
  *  lands exactly where live clients saw it. Label events fire twice per
  *  slot (placeholder, then filled); chronological last-wins keeps the
  *  resolved label. */
  private overlayHostSteerParts;
  /**
  * Cache the HOST-authored content array (WeakRef; owned by the run closure).
  * This is the authoritative same-instance view: host-only parts (steers)
  * live here but never inside the SDK graph, so preferring it over the graph
  * cache keeps same-instance reconnect/abort/status reads steer-complete.
  */
  setContentParts(streamId: string, contentParts: Agents.MessageContentComplex[], expectedCreatedAt?: number): void;
  /**
  * Store collectedUsage reference in local cache.
  * This is used for abort handling to spend tokens for all models.
  * Note: Only available on the generating instance; cross-replica abort uses fallback.
  */
  setCollectedUsage(streamId: string, collectedUsage: UsageMetadata[], expectedCreatedAt?: number): void;
  /**
  * Get collected usage for a job.
  * Only available if this is the generating instance.
  */
  getCollectedUsage(streamId: string, expectedCreatedAt?: number): UsageMetadata[];
  /**
  * Get aggregated content - tries local cache first, falls back to Redis reconstruction.
  *
  * Optimization: If this instance has the live graph (same-instance reconnect),
  * we return the content directly without Redis round-trip.
  * For cross-instance reconnects, we reconstruct from Redis Streams.
  *
  * @param streamId - The stream identifier
  * @returns Content parts array or null if not found
  */
  /**
  * Read from a cached {@link StandardGraph}, tolerating one disposed after a HITL
  * pause. When a paused turn's client is disposed, `disposeClient`
  * (api/server/cleanup.js `graphPropsToClean`) NULLS the graph's internal arrays
  * (`messages`, `contentData`) for GC — but this store still holds a WeakRef to
  * that object. Calling `getContentParts()` (`this.messages.slice()`) or
  * `getRunSteps()` (`[...this.contentData]`) on it then throws
  * ("Cannot read properties of null (reading 'slice')" / "not iterable"), which
  * aborts the resume (#14247). Swallow it, drop the stale entry, and let the
  * caller fall back to durable chunk reconstruction. (The SDK-side null guard in
  * `StandardGraph` is a separate agents fix.)
  */
  private readCachedGraph;
  getContentParts(streamId: string, expectedCreatedAt?: number, options?: {
    durableOnly?: boolean;
  }): Promise<{
    content: Agents.MessageContentComplex[];
    reconstructedEventCount?: number;
    durableEventCount?: number;
  } | null>;
  /**
  * Get run steps - tries local cache first, falls back to Redis.
  *
  * Optimization: If this instance has the live graph, we get run steps
  * directly without Redis round-trip.
  *
  * @param streamId - The stream identifier
  * @returns Run steps array
  */
  getRunSteps(streamId: string, expectedCreatedAt?: number): Promise<Agents.RunStep[]>;
  private getRunStepsData;
  /**
  * Clear content state for a job.
  * Removes both local cache and Redis data.
  */
  clearContentState(streamId: string, expectedCreatedAt?: number): void;
  /**
  * Clear content state async.
  */
  private clearContentStateAsync;
  enqueueSteer(streamId: string, item: SteerQueueItem, expectedCreatedAt?: number): Promise<number>;
  enqueueSteerVersioned(streamId: string, item: SteerQueueItem, wantsPreempt: boolean, expectedCreatedAt?: number): Promise<SteerEnqueueVersionedResult>;
  getSteerReceipt(streamId: string, clientSteerId: string): Promise<SteerReceipt | null>;
  enqueueSteerWithReceipt(streamId: string, item: SteerQueueItem, receipt: SteerReceiptInput, wantsPreempt: boolean, expectedCreatedAt?: number): Promise<SteerEnqueueReceiptResult>;
  drainSteers(streamId: string, expectedCreatedAt?: number): Promise<SteerQueueItem[]>;
  admitTerminalSteers(streamId: string, policy: TerminalSteerAdmissionPolicy, expectedCreatedAt?: number): Promise<TerminalSteerAdmissionResult>;
  restoreClaimedSteers(streamId: string, items: SteerQueueItem[], expectedCreatedAt?: number): Promise<boolean>;
  closeAndDrainSteers(streamId: string, expectedCreatedAt?: number): Promise<SteerQueueItem[]>;
  peekSteers(streamId: string, expectedCreatedAt?: number): Promise<SteerQueueItem[]>;
  peekClaimedSteers(streamId: string, expectedCreatedAt?: number): Promise<SteerQueueItem[]>;
  clearSteers(streamId: string): Promise<void>;
  removeSteer(streamId: string, steerId: string, expectedCreatedAt?: number): Promise<boolean>;
  armSteer(streamId: string, steerId: string, expectedCreatedAt?: number): Promise<SteerArmOutcome>;
  armSteerVersioned(streamId: string, steerId: string, expectedCreatedAt?: number): Promise<SteerArmResult>;
  downgradeSteerPreempts(streamId: string, expectedCreatedAt?: number): Promise<SteerQueueItem[] | null>;
  parkSteers(streamId: string, payload: string, expectedCreatedAt?: number): Promise<void>;
  claimParkedSteers(streamId: string, ownerUserId: string, ownerTenantId?: string): Promise<string | undefined>;
  claimParkedSteersDetailed(streamId: string, ownerUserId: string, ownerTenantId?: string, requestedProtocolVersion?: 1 | 2): Promise<ParkedSteerClaim | undefined>;
  consumeParkedSteer(streamId: string, steerId: string, ownerUserId: string, ownerTenantId: string | undefined, expectedCreatedAt: number): Promise<boolean>;
  discardSteerLeftover(streamId: string, clientSteerId: string, steerId: string, ownerUserId: string, ownerTenantId?: string, expectedGenerationCreatedAt?: number): Promise<boolean>;
  /** A malformed entry is dropped (logged) rather than poisoning the drain. */
  private parseSteerItems;
  /**
  * Append a streaming chunk to Redis Stream.
  * Uses XADD for efficient append-only storage.
  * Sets TTL on first chunk to ensure cleanup if job crashes.
  */
  appendChunk(streamId: string, event: unknown, expectedCreatedAt?: number, deliveredSteer?: SteerQueueItem, options?: {
    coalesce?: boolean;
  }): Promise<boolean>;
  /**
  * Buffer a coalescable durable append for the current window. The whole batch
  * settles together: `true` on commit, `false` under the generation/status
  * fence, and a rejection on operational failure — mirroring the per-event
  * appendChunk contract each caller's fence continuation already handles.
  */
  private enqueueCoalescedAppend;
  private flushCoalescedAppends;
  /** Persist a stream's pending coalesced appends now (pre-transition barrier). */
  flushPendingAppends(streamId: string): Promise<void>;
  /**
  * Get all chunks from Redis Stream.
  */
  private getChunks;
  private getChunkSnapshot;
  /**
  * Save run steps for resume state. Uses the paused-window TTL script so a run-step save
  * landing at/after a HITL pause extends to the approval window instead of resetting the
  * key to the short running TTL (which would drop the tool timeline on a reload of a
  * still-live approval — mirrors the chunk-stream no-shrink behavior).
  */
  saveRunSteps(streamId: string, runSteps: Agents.RunStep[], expectedCreatedAt?: number): Promise<void>;
  /**
  * Create a consumer group for a stream.
  * Used to track which chunks a client has already received.
  *
  * @param streamId - The stream identifier
  * @param groupName - Unique name for the consumer group (e.g., session ID)
  * @param startFrom - Where to start reading ('0' = from beginning, '$' = only new)
  */
  createConsumerGroup(streamId: string, groupName: string, startFrom?: "0" | "$"): Promise<void>;
  /**
  * Read chunks from a consumer group (only unseen chunks).
  * This is the key to the resumable stream pattern.
  *
  * @param streamId - The stream identifier
  * @param groupName - Consumer group name
  * @param consumerName - Name of the consumer within the group
  * @param count - Maximum number of chunks to read (default: all available)
  * @returns Array of { id, event } where id is the Redis stream entry ID
  */
  readChunksFromGroup(streamId: string, groupName: string, consumerName?: string, count?: number): Promise<Array<{
    id: string;
    event: unknown;
  }>>;
  /**
  * Acknowledge that chunks have been processed.
  * This tells Redis we've successfully delivered these chunks to the client.
  *
  * @param streamId - The stream identifier
  * @param groupName - Consumer group name
  * @param messageIds - Array of Redis stream entry IDs to acknowledge
  */
  acknowledgeChunks(streamId: string, groupName: string, messageIds: string[]): Promise<void>;
  /**
  * Delete a consumer group.
  * Called when a client disconnects and won't reconnect.
  *
  * @param streamId - The stream identifier
  * @param groupName - Consumer group name to delete
  */
  deleteConsumerGroup(streamId: string, groupName: string): Promise<void>;
  /**
  * Get pending chunks for a consumer (chunks delivered but not acknowledged).
  * Useful for recovering from crashes.
  *
  * @param streamId - The stream identifier
  * @param groupName - Consumer group name
  * @param consumerName - Consumer name
  */
  getPendingChunks(streamId: string, groupName: string, consumerName?: string): Promise<Array<{
    id: string;
    event: unknown;
  }>>;
  /**
  * Serialize job data for Redis hash storage.
  * Converts complex types to strings.
  */
  private serializeJob;
  /**
  * Deserialize job data from Redis hash.
  */
  private deserializeJob;
  /**
  * Parse a persisted `pendingAction`, defending the cold-resume path against
  * malformed or stale records: a corrupt JSON blob or a payload whose shape
  * predates the current SDK contract is dropped (logged) rather than crashing
  * the resume or feeding a bad record to an approval route. Returns undefined
  * when absent/invalid.
  */
  private parsePendingAction;
  /** Parse the accepted ask answer retained across resume ownership transfer. */
  private parseResolvedAskUserQuestions;
}
//#endregion
//#region src/stream/implementations/RedisEventTransport.d.ts
/**
* Redis Pub/Sub implementation of IEventTransport.
* Enables real-time event delivery across multiple instances.
*
* Architecture (inspired by https://upstash.com/blog/resumable-llm-streams):
* - Publisher: Emits events to Redis channel when chunks arrive
* - Subscriber: Listens to Redis channel and forwards to SSE clients
* - Decoupled: Generator and consumer don't need direct connection
* - Ordering: Every sequenced event for one stream must use PUBLISH_SEQ_LUA. Its hash-tagged
*   counter routes all publishers through one Redis slot owner, so sequence and publish order
*   share one authoritative FIFO origin.
*
* Note: Requires TWO Redis connections - one for publishing, one for subscribing.
* This is a Redis limitation: a client in subscribe mode can't publish.
*
* @example
* ```ts
* const transport = new RedisEventTransport(publisherClient, subscriberClient);
* transport.subscribe(streamId, { onChunk: (e) => res.write(e) });
* transport.emitChunk(streamId, { text: 'Hello' });
* ```
*/
declare class RedisEventTransport implements IEventTransport {
  /** Redis client for publishing events */
  private publisher;
  /** Redis client for subscribing to events (separate connection required) */
  private subscriber;
  /** Track subscribers per stream */
  private streams;
  /** Track channel subscription state: resolved promise = active, pending = in-flight */
  private channelSubscriptions;
  /** Counter for generating unique subscriber IDs */
  private subscriberIdCounter;
  /** Coalescable chunk publications awaiting their window flush, per stream */
  private pendingBatches;
  /** Local waiters for atomic sequence-frontier markers published after SUBSCRIBE. */
  private subscriptionFrontierWaiters;
  /** Delta-coalescing window; 0 keeps every publication on the per-event path */
  private readonly coalesceWindowMs;
  private createStreamState;
  private getOrCreateStreamState;
  private captureSubscriptionFrontier;
  private releaseSubscriptionFrontiers;
  /**
  * Create a new Redis event transport.
  *
  * @param publisher - Redis client for publishing (can be shared)
  * @param subscriber - Redis client for subscribing (must be dedicated)
  */
  constructor(publisher: Redis | Cluster, subscriber: Redis | Cluster);
  /** Minimum safety-net TTL in seconds; publishing and pause transitions may only extend it. */
  private static readonly SEQUENCE_TTL_SECONDS;
  /**
  * Split a seq-less message into the JSON fragments surrounding its `seq`, so the sequence
  * can be spliced in by {@link PUBLISH_SEQ_LUA} without re-encoding the payload.
  *
  * Omitting a field (e.g. `data: undefined`) yields an empty tail, matching what
  * `JSON.stringify` would have dropped from the whole-object encoding.
  */
  private static buildPayloadParts;
  /**
  * Allocate a sequence number and publish, in one Redis round trip.
  *
  * The shared counter survives local cleanup and expires after its sliding TTL once no
  * generation is publishing. This bounds storage without resetting another replica's
  * subscriber frontier between turns.
  */
  private publishWithSequence;
  private evalPublishSequenced;
  private publishChunkWithReceipt;
  /**
  * Buffer a coalescable chunk for the current window and settle its receipt when the
  * batch flushes. The receipt keeps per-event semantics: its own absolute sequence,
  * `false` under a generation/status fence, `undefined` on operational failure.
  */
  private enqueueCoalescedChunk;
  /**
  * Publish the stream's pending coalesced chunks as one CHUNK_BATCH frame.
  *
  * One INCRBY reserves a consecutive sequence per buffered event, so subscribers
  * unpack the frame into individually sequenced chunks and the reorder buffer is
  * none the wiser. The events array is spliced from pre-serialized payloads for
  * the same reason single frames are: no server-side re-encoding.
  */
  private flushCoalescedChunks;
  /** Publish a stream's pending coalesced chunks now (pre-transition barrier). */
  flushPendingChunks(streamId: string): Promise<void>;
  /** Drop a stream's pending coalesced chunks without publishing (teardown path). */
  private discardCoalescedChunks;
  private ensureChannelSubscription;
  private trackOrReleaseActiveChannel;
  /** Reset subscriber reorder buffer state to initial values */
  private resetReorderBuffer;
  /** Release a deferred attachment into the ordinary ordered-delivery fallback.
  * The state identity fence prevents a failed stale attachment from touching a
  * replacement lifecycle that happens to reuse the same stream ID. */
  private releaseDeferredDelivery;
  /**
  * Advance subscriber reorder buffer to the authoritative Redis sequence counter
  * (cross-replica safe).
  *
  * @param replayedNextSeq - Absolute Redis sequence immediately after the last event replayed
  *   from earlyEventBuffer. Pending entries below it were already delivered; entries at or
  *   above it are live chunks from the ongoing generation. Using the exact replay frontier
  *   (not the Redis counter) is critical: INCR can advance the counter past a live chunk's
  *   sequence during the GET window. Undefined means no local replay, so currentSeq is trusted.
  * @param preserveBufferedBeforeFrontier - A fresh post-SUBSCRIBE fence has no replay log;
  *   preserve every frame observed before its marker and begin at the earliest buffered seq.
  */
  syncReorderBuffer(streamId: string, replayedNextSeq?: number, preserveBufferedBeforeFrontier?: boolean): Promise<void>;
  /**
  * Handle incoming pub/sub message with reordering support for Redis Cluster
  */
  private handleMessage;
  /**
  * Handle terminal events (done/error) with sequence-based ordering.
  * Buffers the terminal event and delivers after all preceding chunks arrive.
  */
  private handleTerminalEvent;
  /**
  * Handle chunk messages with sequence-based reordering.
  * Buffers out-of-order messages and delivers them in sequence.
  */
  private handleOrderedChunk;
  /** Deliver consecutive pending messages */
  private flushPendingMessages;
  /** Force-flush all pending messages in order (used on timeout or overflow) */
  private forceFlushBuffer;
  /** Schedule a timeout to force-flush if gaps aren't filled */
  private scheduleFlushTimeout;
  /** Deliver a message to all handlers */
  private deliverMessage;
  private detachStreamSubscribers;
  private unsubscribeUnusedChannel;
  /**
  * Subscribe to events for a stream.
  *
  * Ensures the Redis channel is active and returns an SSE-specific unsubscribe function.
  */
  subscribe(streamId: string, handlers: {
    onChunk: (event: unknown, generationId?: number) => void;
    onDone?: (event: unknown, generationId?: number) => void;
    onError?: (error: string, generationId?: number) => void;
  }, options?: {
    deferSequenceDelivery?: boolean;
    captureSequenceFrontier?: boolean; /** @deprecated Use deferSequenceDelivery. */
    deferDeliveryUntilSynchronized?: boolean;
  }): {
    unsubscribe: () => void;
    ready?: Promise<void>;
    syncReorderBuffer?: () => void | Promise<void>;
  };
  /**
  * Publish a chunk event to all subscribers across all instances.
  * Includes sequence number for ordered delivery in Redis Cluster mode.
  *
  * Performance: sequence allocation and publish share one round trip. This runs per streamed
  * delta, so the saved round trip is multiplied by the token count of every response.
  */
  emitChunk(streamId: string, event: unknown, generationId?: number): Promise<void>;
  /**
  * Publish a done event to all subscribers.
  * Includes sequence number to ensure delivery after all chunks.
  */
  emitDone(streamId: string, event: unknown, generationId?: number): Promise<void>;
  renewDemand(streamId: string, ttlMs: number): Promise<void>;
  hasDemand(streamId: string): Promise<boolean>;
  emitReplacedDoneConfirmed(streamId: string, event: unknown, replacedGenerationId: number, creationAttemptId: string): Promise<void>;
  /**
  * Publish an error event to all subscribers.
  * Includes sequence number to ensure delivery after all chunks.
  */
  emitError(streamId: string, error: string, generationId?: number): Promise<void>;
  closeLocalSubscribers(streamId: string, error: string): void;
  /**
  * Get subscriber count for a stream (local instance only).
  *
  * Note: In a multi-instance setup, this only returns local subscriber count.
  * For global count, would need to track in Redis (e.g., with a counter key).
  */
  getSubscriberCount(streamId: string): number;
  /**
  * Check if this is the first subscriber (local instance only).
  */
  isFirstSubscriber(streamId: string): boolean;
  /**
  * Register callback for when all subscribers leave.
  */
  onAllSubscribersLeft(streamId: string, callback: () => void): void;
  /**
  * Publish an abort signal to all replicas.
  * This enables cross-replica abort: when a user aborts on Replica B,
  * the generating Replica A receives the signal and stops.
  */
  emitAbort(streamId: string, generationId?: number): void;
  private publishAbort;
  private settleAbortAck;
  private hasDurableAbortAck;
  recordAbortAcknowledgement(streamId: string, generationId: number): Promise<boolean>;
  recordProviderDrain(streamId: string, generationId: number, providerExecutionId: string): Promise<boolean>;
  hasProviderDrain(streamId: string, generationId: number, providerExecutionId: string): Promise<boolean>;
  private publishAbortAcknowledgement;
  /** Awaitable variant for replacement handoff receipts. Success requires an
  * acknowledgement from the callback that owns this exact generation; Redis
  * PUBLISH receiver counts are deliberately not treated as proof. */
  emitAbortConfirmed(streamId: string, generationId: number): Promise<boolean>;
  /**
  * Register callback for abort signals from any replica.
  * Called when abort is triggered on any replica (including this one).
  * Resolves once the Redis channel is active so callers can safely expose the stream.
  *
  * @param streamId - The stream identifier
  * @param callback - Called when abort signal is received
  */
  onAbort(streamId: string, callback: (generationId?: number) => void | boolean): Promise<() => void>;
  /**
  * Publish a preempt arm/clear to all replicas. Unlike abort this does NOT
  * stop the run — the generating replica seals its current model stream at
  * the next provider-safe boundary. Same channel and subscription as every
  * other stream event; fenced by `msg.createdAt` on the receiving side.
  */
  /**
  * Resolves to the number of replicas that received the message, so an ARM
  * can be acknowledged only once it actually reached someone. Unlike abort
  * (fire-and-forget, because a failed abort is retried by the user hitting
  * stop again) an unheard arm is invisible: the route would answer
  * `preempt: true` for a seal that never happens. Rejects on publish
  * failure; callers decide what to do.
  */
  emitPreempt(streamId: string, msg: PreemptMessage): Promise<number>;
  /**
  * Register callback for preempt signals from any replica.
  * Resolves once the Redis channel is active so callers can safely arm.
  * The returned function removes only this registration, so a terminal
  * generation releases its channel without touching a same-stream
  * replacement.
  */
  onPreempt(streamId: string, callback: (msg: PreemptMessage) => void): Promise<() => void>;
  /**
  * Get all tracked stream IDs (for orphan cleanup)
  */
  getTrackedStreamIds(): string[];
  /**
  * Cleanup local resources for a specific stream.
  *
  * The sequence counter is deliberately left in Redis. A stream ID is currently the
  * conversation ID, so later turns reuse the same ordering namespace. Another replica
  * may also still have a subscriber whose reorder buffer is positioned at this counter.
  * Deleting it here would restart the next producer at zero and make that subscriber
  * discard the entire next turn as duplicate traffic. The counter's sliding TTL bounds
  * orphan lifetime.
  */
  cleanup(streamId: string): void;
  /**
  * Destroy all resources.
  */
  destroy(): void;
}
//#endregion
//#region src/stream/persistence.d.ts
/** A settled status can precede final persistence and terminal host actions. */
declare function waitForGenerationPersistence(streamId: string, createdAt: number, readJob: (id: string) => Promise<Pick<GenerationJob, "createdAt" | "metadata"> | null | undefined>, {
  timeoutMs,
  pollMs
}?: {
  timeoutMs?: number;
  pollMs?: number;
}): Promise<void>;
//#endregion
//#region src/agents/triggers/detachedAction.d.ts
declare const EVENT_ACTOR_DETACHED_COMPLETION_TYPE = "librechat.event_actor.detached_completion";
declare const EVENT_ACTOR_DETACHED_COMPLETION_SOURCE = "librechat-event-actor";
declare function parseAgentEventDetachedTerminalEvidence(value: unknown): AgentEventDetachedTerminalEvidence | undefined;
interface AgentEventActorDetachedCompletionProjection {
  version: 1;
  invocationId: string;
  /** Generation that owns the original delivery's detached-action record. */
  generationCreatedAt: number;
  /** Generation whose terminal handling enqueued this wake. */
  wakeGenerationCreatedAt: number;
  taskId: string;
  idempotencyKey: string;
}
declare function parseAgentEventActorDetachedCompletion(value: unknown): AgentEventActorDetachedCompletionProjection | undefined;
interface DetachedActionOwner {
  user: string;
  tenantId?: string;
  bindingId: string;
  conversationId: string;
  generationCreatedAt: number;
  turnCreatedAt: number;
  invocationId: string;
  expectedAction: AgentTriggerExpectedAction;
}
interface DetachedActionDependencies {
  reserveAgentEventActorDetachedAction: AgentTriggerDeliveryMethods["reserveAgentEventActorDetachedAction"];
  markAgentEventActorDetachedActionRunning: AgentTriggerDeliveryMethods["markAgentEventActorDetachedActionRunning"];
  settleAgentEventActorDetachedAction: AgentTriggerDeliveryMethods["settleAgentEventActorDetachedAction"];
  persistTerminalEvidence(input: AgentEventDetachedTerminalEvidence): Promise<void>;
  onTerminal(input: {
    taskId: string;
    idempotencyKey: string;
  }): Promise<void>;
  waitForTerminalPersistenceRetry?(delayMs: number): Promise<void>;
  storeMode(): DetachedAgentEventActionStoreMode | undefined;
  now?(): Date;
}
interface AgentEventActorInternalSuspension {
  kind: "internal_completion";
  actionId: string;
  jobCreatedAt: number;
  interrupt: EventActorInterrupt;
}
interface AgentEventActorDetachedActionLifecycle extends EventActorDetachedActionLifecycle {
  readSuspension(): AgentEventActorInternalSuspension | undefined;
}
interface AgentEventActorDetachedResumeInput {
  streamId: string;
  job: SerializableJobData;
  handlingGenerationCreatedAt: number;
  suspension: IAgentEventActorSuspensionEvidence;
  action: AgentEventActorDetachedAction;
}
interface DetachedResumeDependencies {
  getAgentTriggerDelivery: AgentTriggerDeliveryMethods["getAgentTriggerDelivery"];
  enqueueAgentTrigger(envelope: AgentTriggerEnvelope, options?: {
    requiredWorkerCapability?: string;
  }): Promise<unknown>;
  requestId?(): string;
  now?(): number;
}
/** Builds the exact internal continuation in the typed trigger layer. The app
* server supplies only persistence and dispatch composition dependencies. */
declare function createAgentEventDetachedResumeHandler(deps: DetachedResumeDependencies): ({
  job,
  action,
  handlingGenerationCreatedAt
}: AgentEventActorDetachedResumeInput) => Promise<void>;
/**
* Binds the generic background executor to one delivery-owned expected action.
* The delivery row remains authoritative; request-local state only lets the
* actor adapter discover the exact launch that this segment already persisted.
*/
declare function createAgentEventActorDetachedActionLifecycle(owner: DetachedActionOwner, deps: DetachedActionDependencies): AgentEventActorDetachedActionLifecycle;
//#endregion
//#region src/agents/triggers/engine.d.ts
/** A pre-dispatch condition that must not consume the delivery's retry budget. */
declare class AgentTriggerDeliveryDeferredError extends Error {
  readonly delayMs: number;
  constructor(message: string, delayMs?: number);
}
type AgentTriggerDeliveryStatus = "staging" | "capability_staging" | "batched" | "pending" | "capability_pending" | "leased" | "capability_leased" | "succeeded" | "capability_dead" | "dead";
interface AgentTriggerDeliveryFailure {
  code: string;
  message: string;
  certainty: "definite" | "ambiguous";
  retryable: boolean;
  attemptedAt: Date;
  status?: number;
}
interface AgentTriggerDeliveryRecord {
  id: string;
  user: string;
  claimToken: string;
  deliveryKey: string;
  fingerprint: string;
  orderingKey: string;
  laneSequence: number;
  envelope: unknown;
  status: AgentTriggerDeliveryStatus;
  attempts: number;
  availableAt: Date;
  createdAt: Date;
  envelopeBytes?: number;
  coalesceKey?: string;
  coalesceFrom?: Date;
  coalesceUntil?: Date;
  batchSize?: number;
  batchBytes?: number;
  batchMemberIds?: Array<{
    toString(): string;
  } | string>;
  batchRootId?: {
    toString(): string;
  } | string;
  batchMembersSettledAt?: Date;
  awaitTerminalHandling?: boolean;
  leaseBy?: string;
  leaseUntil?: Date;
  lastError?: AgentTriggerDeliveryFailure;
  handling?: {
    status: "started" | "applied" | "completed_no_action" | "failed" | "cancelled";
    conversationId: string;
    streamId: string;
    generationCreatedAt: number;
    startedAt: Date;
    settledAt?: Date;
    error?: string;
    action?: {
      toolName: string;
      toolCallId?: string;
    };
  };
}
interface AgentTriggerOrderingBlock {
  availableAt: Date;
  leaseUntil?: Date;
  reason?: "active_handling";
}
interface AgentTriggerDeliveryStore {
  claimNext: (input: {
    workerId: string;
    claimToken: string;
    now: Date;
    leaseUntil: Date;
  }) => Promise<AgentTriggerDeliveryRecord | null>;
  findEarlierUnsettled: (delivery: AgentTriggerDeliveryRecord) => Promise<AgentTriggerOrderingBlock | null>;
  getBatch: (delivery: Pick<AgentTriggerDeliveryRecord, "id" | "batchMemberIds">) => Promise<Array<Pick<AgentTriggerDeliveryRecord, "id" | "deliveryKey" | "envelope">>>;
  release: (input: {
    id: string;
    workerId: string;
    claimToken: string;
    availableAt: Date;
  }) => Promise<boolean>;
  beginAttempt: (input: {
    id: string;
    workerId: string;
    claimToken: string;
    now: Date;
  }) => Promise<number | null>;
  defer: (input: {
    id: string;
    workerId: string;
    claimToken: string;
    attempt: number;
    availableAt: Date;
  }) => Promise<boolean>;
  complete: (input: {
    id: string;
    workerId: string;
    claimToken: string;
    attempt: number;
    result: AgentTriggerExecutionResult;
    settledAt: Date;
    handling?: AgentTriggerDeliveryRecord["handling"];
    awaitTerminalHandling?: true;
  }) => Promise<boolean>;
  retry: (input: {
    id: string;
    workerId: string;
    claimToken: string;
    attempt: number;
    error: AgentTriggerDeliveryFailure;
    availableAt: Date;
  }) => Promise<boolean>;
  dead: (input: {
    id: string;
    workerId: string;
    claimToken: string;
    attempt: number;
    error: AgentTriggerDeliveryFailure;
    settledAt: Date;
  }) => Promise<boolean>;
}
interface AgentTriggerDeliveryEngineOptions {
  concurrency?: number;
  leaseMs?: number;
  maxAttempts?: number;
  retryBaseMs?: number;
  retryCapMs?: number;
  tickMs?: number;
  /** Ceiling for the poll interval while the queue stays empty; any wake or claimed
  *  delivery snaps polling back to `tickMs`, so only true idleness ever waits this long. */
  maxIdleTickMs?: number;
}
interface AgentTriggerDeliveryEngineDeps {
  store: AgentTriggerDeliveryStore;
  dispatch: (envelope: unknown, options?: {
    signal?: AbortSignal;
    attempt?: number;
    maxAttempts?: number;
  }) => Promise<AgentTriggerExecutionResult>;
  /** Source-owned terminalization must commit before its delivery can become
  * dead, including recovery after a crash that exhausted the attempt budget. */
  settleSourceBeforeDeadLetter?: (envelope: unknown, failure: AgentTriggerDeliveryFailure) => Promise<void>;
  now?: () => Date;
  random?: () => number;
  workerId?: string;
}
interface AgentTriggerDeliveryEngine {
  start: () => void;
  /** Registers a future eligibility time so the idle poll never sleeps past it. */
  noteEligibleAt: (at: Date) => void;
  stop: () => Promise<void>;
  cancelUser: (userId: string) => Promise<void>;
  releaseUserCancellation: (userId: string) => void;
  wake: () => void;
  runTick: () => Promise<number>;
}
/** Durable, lease-fenced delivery runner shared by every trusted event source. */
declare function createAgentTriggerDeliveryEngine(deps: AgentTriggerDeliveryEngineDeps, options?: AgentTriggerDeliveryEngineOptions): AgentTriggerDeliveryEngine;
//#endregion
//#region src/agents/triggers/service.d.ts
declare const AGENT_TRIGGER_TOKEN_TTL = "60s";
interface AgentTriggerServiceOptions {
  address?: BoundAddress | string | null;
}
interface AgentTriggerServiceDeps {
  fetch?: AgentTriggerExecutionHostDeps["fetch"];
  getTimezone?: AgentTriggerExecutionHostDeps["getTimezone"];
  prepareContinue?: AgentTriggerExecutionHostDeps["prepareContinue"];
  mintToken?: AgentTriggerExecutionHostDeps["mintToken"];
  timeoutMs?: number;
  methods?: AgentTriggerDeliveryPersistence;
  deliveryOptions?: AgentTriggerDeliveryEngineOptions;
  isPrincipalActive?: (userId: string) => boolean | Promise<boolean>;
  userDrainTimeoutMs?: number;
  userDrainPollMs?: number;
  purgeRecoveryIntervalMs?: number;
  purgeRecoveryLimit?: number;
  reclaimCheckpointDeletions?: (limit: number) => Promise<number>;
  supportsDetachedActionCompletion?: () => boolean;
  settleSourceBeforeDeadLetter?: AgentTriggerDeliveryEngineDeps["settleSourceBeforeDeadLetter"];
}
interface AgentTriggerDeliveryReceipt {
  id: string;
  deliveryKey: string;
  status: Exclude<AgentTriggerStoredRecord["status"], "batched">;
  availableAt: Date;
  replayed: boolean;
}
declare class AgentTriggerServiceUnavailableError extends AgentTriggerDeliveryDeferredError {
  constructor(message: string);
}
interface AgentTriggerStoredHistoryEntry {
  attempt: number;
  outcome: "succeeded" | "retry" | "dead";
  at: Date;
  workerId: string;
  error?: AgentTriggerDeliveryFailure;
}
interface AgentTriggerStoredRecord extends Omit<AgentTriggerDeliveryRecord, "claimToken"> {
  claimToken?: string;
  tenantId?: string;
  result?: unknown;
  history?: AgentTriggerStoredHistoryEntry[];
  settledAt?: Date;
  expiresAt?: Date;
  requeueCount?: number;
}
/** Persistence contract implemented by the data-schemas method bundle. */
interface AgentTriggerDeliveryPersistence {
  ensureAgentTriggerDeliveryIndexes: () => Promise<void>;
  enqueueAgentTriggerDelivery: (input: PreparedAgentTriggerDelivery) => Promise<{
    delivery: AgentTriggerStoredRecord;
    replayed: boolean;
  }>;
  claimNextAgentTriggerDelivery: (input: Parameters<AgentTriggerDeliveryStore["claimNext"]>[0] & {
    workerCapabilities?: string[];
  }) => ReturnType<AgentTriggerDeliveryStore["claimNext"]>;
  findEarlierAgentTriggerDelivery: AgentTriggerDeliveryStore["findEarlierUnsettled"];
  getAgentTriggerDeliveryBatch: AgentTriggerDeliveryStore["getBatch"];
  releaseAgentTriggerDelivery: AgentTriggerDeliveryStore["release"];
  beginAgentTriggerDeliveryAttempt: AgentTriggerDeliveryStore["beginAttempt"];
  deferAgentTriggerDeliveryAttempt: AgentTriggerDeliveryStore["defer"];
  completeAgentTriggerDelivery: AgentTriggerDeliveryStore["complete"];
  retireAgentTriggerDelivery: AgentTriggerDeliveryMethods["retireAgentTriggerDelivery"];
  renewAgentTriggerDeliveryProducerLease: AgentTriggerDeliveryMethods["renewAgentTriggerDeliveryProducerLease"];
  retryAgentTriggerDelivery: AgentTriggerDeliveryStore["retry"];
  deadLetterAgentTriggerDelivery: AgentTriggerDeliveryStore["dead"];
  getAgentTriggerDelivery: (deliveryKey: string) => Promise<AgentTriggerStoredRecord | null>;
  getAgentTriggerDeliveryStatus: (deliveryKey: string, userId: string, sourceKeyId: string, tenantId?: string) => Promise<AgentTriggerDeliveryStatusRecord | null>;
  getAgentTriggerDeadLetters: (limit?: number) => Promise<AgentTriggerStoredRecord[]>;
  requeueAgentTriggerDelivery: (id: string, availableAt: Date) => Promise<AgentTriggerStoredRecord | null>;
  countActiveAgentTriggerDeliveriesByUser: (userId: string, now: Date) => Promise<number>;
  recoverAgentTriggerLanePublications: (limit?: number) => Promise<number>;
  recoverAgentTriggerBatchReceipts: (limit?: number) => Promise<number>;
  reclaimInactiveAgentTriggerLanes: (limit?: number) => Promise<number>;
  prepareAgentTriggerUserPurge: (userId: string, fenceStartedAt: Date, tenantId?: string) => Promise<void>;
  cancelAgentTriggerUserPurge: (userId: string, fenceStartedAt: Date) => Promise<boolean>;
  recoverAgentTriggerUserPurges: (limit?: number) => Promise<number>;
  expireLegacyAgentEventActorReceipts?: (now: Date, limit?: number) => Promise<number>;
  deleteAgentTriggerDeliveriesByUser: (userId: string) => Promise<void>;
}
interface AgentTriggerService {
  initialize: (options?: AgentTriggerServiceOptions) => Promise<void>;
  stop: () => Promise<void>;
  dispatch: (envelope: unknown, options?: {
    signal?: AbortSignal;
  }) => Promise<AgentTriggerExecutionResult>;
  enqueue: (envelope: unknown, options?: AgentTriggerEnqueueOptions) => Promise<AgentTriggerDeliveryReceipt>;
  getDelivery: (deliveryKey: string) => Promise<AgentTriggerStoredRecord | null>;
  getDeliveryStatus: (deliveryKey: string, userId: string, sourceKeyId: string, tenantId?: string) => Promise<AgentTriggerDeliveryStatusRecord | null>;
  getDeadLetters: (limit?: number) => Promise<AgentTriggerStoredRecord[]>;
  requeue: (id: string, availableAt?: Date) => Promise<AgentTriggerStoredRecord | null>;
  retire: (deliveryKey: string, sourceId: string, reason: string, options?: {
    onlyIfUnclaimed?: boolean;
    onlyIfDead?: boolean;
  }) => Promise<boolean>;
  renewProducerLease: (deliveryKey: string, sourceId: string, leaseUntil: Date) => Promise<boolean>;
  drainUser: (userId: string) => Promise<void>;
  prepareUserPurge: (userId: string, fenceStartedAt: Date, tenantId?: string) => Promise<void>;
  cancelUserPurge: (userId: string, fenceStartedAt: Date) => Promise<boolean>;
  purgeUser: (userId: string) => Promise<void>;
}
/** Production composition for trusted, in-process trigger producers. */
declare function createAgentTriggerService(deps?: AgentTriggerServiceDeps): AgentTriggerService;
//#endregion
//#region src/agents/triggers/ingress.d.ts
interface AgentTriggerIngressDependencies {
  enqueue: AgentTriggerService["enqueue"];
  getDeliveryStatus: AgentTriggerService["getDeliveryStatus"];
  now?: () => number;
  createRequestId?: () => string;
}
declare function createAgentTriggerIngressHandlers(deps: AgentTriggerIngressDependencies): {
  enqueueEvent: RequestHandler;
  getEvent: RequestHandler;
};
//#endregion
//#region src/agents/triggers/lease.d.ts
type EventChildLeaseMethods = Pick<ConversationMethods, "acquireSubagentThreadLease" | "renewSubagentThreadLease" | "releaseSubagentThreadLease">;
interface AbortGenerationOptions {
  expectedCreatedAt: number;
  awaitProviderDrain: true;
}
interface EventChildGenerationLeaseDependencies {
  methods: EventChildLeaseMethods;
  abortGeneration: (streamId: string, options: AbortGenerationOptions) => Promise<AbortResult>;
}
interface EventChildGenerationLeaseInput {
  userId: string;
  tenantId?: string;
  conversationId: string;
  streamId: string;
  /** Stable logical delivery identity exposed through the parent activity view. */
  taskId?: string;
  jobCreatedAt: number;
  retentionExpiresAt?: Date | string | number;
}
type ReleaseEventChildGenerationLease = () => Promise<void>;
/** Makes an event-driven child generation visible to the durable deletion protocol. */
declare function createEventChildGenerationLeaseAcquirer({
  methods,
  abortGeneration
}: EventChildGenerationLeaseDependencies): ({
  userId,
  tenantId,
  conversationId,
  streamId,
  taskId,
  jobCreatedAt,
  retentionExpiresAt
}: EventChildGenerationLeaseInput) => Promise<ReleaseEventChildGenerationLease | null>;
//#endregion
//#region src/agents/triggers/expectedAction.d.ts
interface CompletedToolEvidence {
  toolName: string;
  toolCallId?: string;
  arguments?: unknown;
}
declare function matchesExpectedAction(evidence: CompletedToolEvidence, expected: AgentTriggerExpectedAction): boolean;
//#endregion
//#region src/agents/triggers/outcome.d.ts
interface SettleAgentTriggerHandlingOutcomeInput {
  deliveryKey: string;
  conversationId: string;
  generationCreatedAt: number;
  status: "applied" | "completed_no_action" | "failed" | "cancelled";
  settledAt: Date;
  error?: string;
  action?: {
    toolName: string;
    toolCallId?: string;
  };
}
interface AgentEventRunOutcome {
  status: "applied" | "completed_no_action" | "failed" | "cancelled";
  action?: {
    toolName: string;
    toolCallId?: string;
  };
}
/** Classifies terminal run evidence once for both checkpoint commit and public receipt. */
declare function classifyAgentEventRunOutcome(job: SerializableJobData, runSteps: Agents.RunStep[], content?: Agents.MessageContentComplex[]): AgentEventRunOutcome;
/** Finds qualifying action evidence without requiring the generation to be terminal yet. */
declare function findAgentEventAppliedAction(expectedAction: AgentTriggerExpectedAction | undefined, runSteps: Agents.RunStep[], content?: Agents.MessageContentComplex[], provenance?: Pick<SerializableJobData, "userSubmittedMessageFieldPaths">): AgentEventAppliedAction | undefined;
interface AgentEventActionRecorder {
  observeToolEnd(data: {
    input?: unknown;
    backgroundDelivery?: boolean;
    outputFiltered?: boolean;
    output?: unknown;
  }): void;
  read(): AgentEventAppliedAction | undefined;
}
/**
* Captures qualifying applied-action evidence at tool-execution time, in graph
* context, instead of trusting the asynchronously populated run-step
* collection to be observable the instant `sendMessage` resolves. The recorder
* applies the SAME fences as run-step evidence — exact tool name (with the MCP
* suffix form), the declared argument subset, an error-free result, and the
* background non-execution receipt exclusion. HITL never reaches the fork path
* and non-executed approvals never emit a tool end, so the non-execution id
* set has no equivalent here. Only the first qualifying execution is retained;
* run-step evidence remains the fallback for paths that bypass the tool-end
* chain (e.g. programmatic tool calling).
*/
declare function createAgentEventActionRecorder(expectedAction: AgentTriggerExpectedAction | undefined): AgentEventActionRecorder;
declare function createAgentEventTerminalHandler(methods: {
  settleAgentTriggerHandlingOutcome: (input: SettleAgentTriggerHandlingOutcomeInput) => Promise<boolean>;
  getAgentEventActorSnapshot: ConversationMethods["getAgentEventActorSnapshot"];
  recordAgentEventActorReconciliation: ConversationMethods["recordAgentEventActorReconciliation"];
  resolveAgentEventActorReconciliation: ConversationMethods["resolveAgentEventActorReconciliation"];
  clearAgentEventActorReconciliation: ConversationMethods["clearAgentEventActorReconciliation"];
  settleAgentEventActorReceipt: AgentTriggerDeliveryMethods["settleAgentEventActorReceipt"];
  getAgentEventActorReceipt: AgentTriggerDeliveryMethods["getAgentEventActorReceipt"];
  backfillAgentEventActorReceipt: AgentTriggerDeliveryMethods["backfillAgentEventActorReceipt"];
  completeAgentEventActorLegacyTurn: ConversationMethods["completeAgentEventActorLegacyTurn"];
  cancelAgentEventActorSuspension: ConversationMethods["cancelAgentEventActorSuspension"];
  releaseAgentEventActorAction: AgentTriggerDeliveryMethods["releaseAgentEventActorAction"];
  getAgentEventActorActionAdmission: AgentTriggerDeliveryMethods["getAgentEventActorActionAdmission"];
  hasAgentEventActorActionAdmission: AgentTriggerDeliveryMethods["hasAgentEventActorActionAdmission"];
  getAgentEventActorDetachedAction: AgentTriggerDeliveryMethods["getAgentEventActorDetachedAction"];
  settleAgentEventActorDetachedAction: AgentTriggerDeliveryMethods["settleAgentEventActorDetachedAction"];
  markAgentEventActorDetachedActionLaunchIndeterminate: AgentTriggerDeliveryMethods["markAgentEventActorDetachedActionLaunchIndeterminate"];
  getMessage: MessageMethods["getMessage"];
}, options?: {
  resumeDetachedAction?(input: AgentEventActorDetachedResumeInput): Promise<void>;
}): (streamId: string, job: SerializableJobData, runSteps: Agents.RunStep[], content?: Agents.MessageContentComplex[]) => Promise<void>;
//#endregion
//#region src/agents/triggers/turn.d.ts
interface AgentEventActorTurnOwner {
  user: string;
  tenantId?: string;
  conversationId: string;
}
type AgentEventActorCheckpointTurn<T> = {
  kind: "execute";
  input: ExecuteAgentEventActorInput<T>;
} | {
  kind: "resume";
  input: ResumeAgentEventActorInput<T>;
};
interface AgentEventActorHistoryTurn<T> {
  owner: AgentEventActorTurnOwner;
  persistToken(token: string): Promise<void>;
  invoke(): Promise<T>;
}
interface CreateAgentEventActorTurnInput<T> {
  strategy: AgentTurnContinuationStrategy;
  checkpoint?: AgentEventActorCheckpointTurn<T>;
  history?: AgentEventActorHistoryTurn<T>;
}
interface AgentEventActorTurnDependencies {
  actor?: AgentEventActorDependencies;
  history?: {
    begin: ConversationMethods["beginAgentEventActorLegacyTurn"];
    complete: ConversationMethods["completeAgentEventActorLegacyTurn"];
  };
}
type AgentEventActorTurnResult<T> = ({
  adapter: "checkpoint";
} & ExecuteAgentEventActorResult<T>) | {
  adapter: "history";
  value: T;
};
interface AgentEventActorTurn<T> {
  run(): Promise<AgentEventActorTurnResult<T>>;
  historyPersisted(): Promise<void>;
}
/** One Event Actor turn interface with checkpoint and history as private state-loading adapters. */
declare function createAgentEventActorTurn<T>(input: CreateAgentEventActorTurnInput<T>, deps: AgentEventActorTurnDependencies): AgentEventActorTurn<T>;
/** Read-compatible settlement for a history turn created by an earlier generation. */
declare function settleAgentEventActorHistoryTurn(input: AgentEventActorTurnOwner & {
  token: string;
}, complete: ConversationMethods["completeAgentEventActorLegacyTurn"]): Promise<boolean>;
//#endregion
//#region src/agents/activityLabels/host.d.ts
/** Cache-token details in the LangChain-standard normalized shape. */
interface CacheTokenDetails {
  cache_read?: number;
  cache_creation?: number;
}
/** Aggregated LLM metadata entries (shape varies by provider SDK). */
interface CollectedMetadataEntry {
  usage?: {
    prompt_tokens?: number;
    input_tokens?: number;
    inputTokens?: number;
    completion_tokens?: number;
    output_tokens?: number;
    outputTokens?: number; /** Anthropic raw usage. */
    cache_creation_input_tokens?: number;
    cache_read_input_tokens?: number; /** OpenAI raw usage. */
    prompt_tokens_details?: {
      cached_tokens?: number;
    };
  };
  tokenUsage?: {
    promptTokens?: number;
    completionTokens?: number;
  };
  usage_metadata?: {
    input_tokens?: number;
    output_tokens?: number;
    input_token_details?: CacheTokenDetails;
  };
}
interface ActivityLabelUsage {
  input_tokens?: number;
  output_tokens?: number;
  /** Normalized cache tokens — `computeUsageCostUSD` and the transaction
  *  path read this shape first, so carrying it prices cached label calls
  *  at cache rates instead of the ordinary input rate (or not at all). */
  input_token_details?: CacheTokenDetails;
}
/**
* Normalizes provider-specific aggregated metadata into the usage shape
* `recordCollectedUsage` expects, cache-token details included — dropping
* them made Anthropic cache tokens vanish from billing and charged OpenAI
* cache reads at the full input rate. Mirrors the title path's inline
* mapping otherwise.
*/
declare function mapCollectedMetadataToUsage(collected: CollectedMetadataEntry[]): ActivityLabelUsage[];
/** The agent fields the label model resolution needs. */
interface ActivityLabelAgent {
  endpoint?: string;
  provider?: string;
  model?: string;
  model_parameters?: {
    model?: string;
  };
}
interface ResolveActivityLabelModelParams {
  req: ServerRequest;
  agent: ActivityLabelAgent;
  /** The PUBLIC endpoint the request came in on (e.g. `agents`) when it
  *  differs from the agent's rewritten provider endpoint — its config block
  *  wins per field over the provider's. */
  publicEndpoint?: string;
  /** Request-scoped ids for header placeholder resolution. */
  ids: {
    messageId?: string;
    conversationId?: string;
    parentMessageId?: string;
  };
  db: EndpointDbMethods;
}
/** Effective activity-label settings for one endpoint. */
interface ResolvedActivityConfig {
  enabled: boolean;
  model?: string;
  endpoint?: string;
  prompt?: string;
  maxPerRun?: number;
  charLimit?: number;
}
/** Effective parent activity-phase settings for one endpoint. */
interface ResolvedActivityPhaseConfig {
  enabled: boolean;
  model?: string;
  endpoint?: string;
  prompt?: string;
  maxPerRun?: number;
  charLimit?: number;
}
/** Effective live reasoning-label settings for one endpoint. */
interface ResolvedReasoningLabelConfig {
  enabled: boolean;
  model?: string;
  endpoint?: string;
  prompt?: string;
  minChars?: number;
  updateChars?: number;
  updateIntervalMs?: number;
  maxPerRun?: number;
}
declare function resolveActivityConfig(appConfig: AppConfig | undefined, endpoint: string, customEndpointConfig?: Partial<TEndpoint>, publicEndpoint?: string): ResolvedActivityConfig;
declare function resolveActivityPhaseConfig(appConfig: AppConfig | undefined, endpoint: string, customEndpointConfig?: Partial<TEndpoint>, publicEndpoint?: string): ResolvedActivityPhaseConfig;
declare function resolveReasoningLabelConfig(appConfig: AppConfig | undefined, endpoint: string, customEndpointConfig?: Partial<TEndpoint>, publicEndpoint?: string): ResolvedReasoningLabelConfig;
/**
* Resolves provider + client options for the label model, mirroring
* `titleConvo`'s resolution. Model precedence: the endpoint's
* `activityModel` > its `titleModel` > the agent's own model. When
* `activityEndpoint` names a different endpoint, the label runs on THAT
* endpoint's credentials (title parity); an unknown name falls back to the
* agent's endpoint with a warning rather than failing the run.
*/
declare function resolveActivityLabelModel({
  req,
  agent,
  publicEndpoint,
  ids,
  db,
  phase,
  reasoning
}: ResolveActivityLabelModelParams & {
  phase?: boolean;
  reasoning?: boolean;
}): Promise<ActivityLabelLLM>;
/** Phase-model resolution shares credentials and sanitization with child labels. */
declare function resolveActivityPhaseLabelModel(params: ResolveActivityLabelModelParams): Promise<ActivityLabelLLM>;
/** Reasoning-label model resolution shares credentials and sanitization with activity labels. */
declare function resolveReasoningLabelModel(params: ResolveActivityLabelModelParams): Promise<ActivityLabelLLM>;
/**
* Bounded wait for in-flight label fills so a label resolving during the
* final batch still reaches the durable log and the saved message before the
* job completes. Never delays finalization past the bound; fills that lose
* the race leave the counts-only placeholder, which renders fine.
*/
declare function settlePendingLabelFills(pending: Array<Promise<void>>, timeoutMs?: number, onTimeout?: () => void): Promise<void>;
//#endregion
//#region src/agents/view.d.ts
type SubagentThreadViewDependencies = Pick<ConversationMethods, "getConvoOwnership" | "getSubagentThreadForParent"> & Pick<MessageMethods, "getMessagesForSubagentThreadView">;
type ParentSubagentIndexDependencies = Pick<ConversationMethods, "getConvoOwnership" | "listSubagentThreadsForParent"> & Pick<MessageMethods, "listSubagentTasksForThreads">;
/**
* Discovers bounded child activity through a human parent. It performs one
* child read and one batched task read; detailed activity stays on the lazy
* thread Interface.
*/
declare function createParentSubagentIndexHandler(deps: ParentSubagentIndexDependencies): (req: ServerRequest, res: Response$2) => Promise<void>;
/** Reads one durable child through its parent without reopening ordinary conversation reads. */
declare function createSubagentThreadViewHandler(deps: SubagentThreadViewDependencies): (req: ServerRequest, res: Response$2) => Promise<void>;
declare const SUBAGENT_THREAD_VIEW_LIMITS: Readonly<{
  messages: number;
  messageTextBytes: number;
  responseTextBytes: number;
  responseBytes: number;
  activityItems: number;
  activityBytes: number;
  activitySourceBytes: number;
}>;
declare const PARENT_SUBAGENT_INDEX_LIMITS: Readonly<{
  children: number;
  tasksPerChild: number;
  responseBytes: number;
}>;
//#endregion
//#region src/agents/workspace.d.ts
interface AgentWorkspaceUpdate {
  code_environment_id?: string | null;
  code_workspace_id?: string;
  [key: string]: unknown;
}
interface AgentWorkspaceEnvironment {
  id: string;
  type?: string;
}
interface AgentWorkspaceConfiguration {
  stateful_code_sessions?: boolean;
  stateful_code_environment?: string | null;
  code_environment_id?: string | null;
  code_workspace_id?: string;
}
declare const AGENT_WORKSPACE_ATTACHED_ENVIRONMENT_ERROR = "Code workspace defaults require an explicit attached code environment";
declare function isActiveAgentWorkspaceConfiguration(configuration?: AgentWorkspaceConfiguration): boolean;
/** Resolve workspace fields according to the version restore persistence contract. */
declare function resolveAgentWorkspaceRestoreConfiguration({
  version,
  current
}: {
  version: AgentWorkspaceConfiguration;
  current: AgentWorkspaceConfiguration;
}): AgentWorkspaceConfiguration;
declare function shouldValidateAgentWorkspaceDefaultBinding({
  workspaceId,
  environmentId,
  currentWorkspaceId,
  currentEnvironmentId
}: {
  workspaceId?: string;
  environmentId?: string | null;
  currentWorkspaceId?: string;
  currentEnvironmentId?: string | null;
}): boolean;
/** Validate only a newly selected or rebound machine-scoped workspace default. */
declare function validateAgentWorkspaceDefaultBinding({
  workspaceId,
  environmentId,
  currentWorkspaceId,
  currentEnvironmentId,
  environments
}: {
  workspaceId?: string;
  environmentId?: string | null;
  currentWorkspaceId?: string;
  currentEnvironmentId?: string | null;
  environments?: readonly AgentWorkspaceEnvironment[];
}): {
  valid: true;
} | {
  valid: false;
  error: string;
};
/** Clear a machine-scoped default when its environment changes without a replacement default. */
declare function reconcileAgentWorkspaceDefault<T extends AgentWorkspaceUpdate>({
  update,
  request,
  currentEnvironmentId
}: {
  update: T;
  request: AgentWorkspaceUpdate;
  currentEnvironmentId?: string | null;
}): T;
//#endregion
//#region src/agents/reasoningLabels/runtime.d.ts
type ReasoningLabelStatus = "streaming" | "complete";
interface GenerateReasoningLabelPayload {
  visibleReasoning: string;
  reasoningStepId: string;
  revision: number;
  status: ReasoningLabelStatus;
  previousLabel?: string;
  agentId?: string;
  prompt?: string;
  charLimit: number;
  signal: AbortSignal;
}
interface GeneratedReasoningLabel {
  label?: string;
  /** Bills this provider call; receives the least-normalized completion text
  * available when metadata-based token counts are absent. */
  collectUsage?: (completionText?: string) => void | Promise<void>;
}
interface ReasoningLabelEvent {
  index: number;
  stepId: string;
  revision: number;
  label: string;
  status: ReasoningLabelStatus;
}
interface ReasoningLabelAttemptEvent {
  index: number;
  stepId: string;
  attempts: number;
  submittedChars: number;
}
interface ReasoningLabelHostDeps {
  minChars?: number;
  updateChars?: number;
  updateIntervalMs?: number;
  maxPerRun?: number;
  /** Explicit call-budget seed. Omit on HITL resume to derive it from content;
  *  pass zero for a new generation whose retained edit prefix is historical. */
  initialAttempts?: number;
  prompt?: string;
  abortSignal?: AbortSignal;
  getContentParts: () => Array<LooseContentPart | null | undefined>;
  getStepIndex: (stepId: string) => number | undefined;
  emitAttemptEvent: (event: ReasoningLabelAttemptEvent) => Promise<unknown>;
  emitLabelEvent: (event: ReasoningLabelEvent) => Promise<unknown>;
  trackPendingFill: (fillDone: Promise<void>) => void;
  isClosed?: () => boolean;
  generateLabel: (payload: GenerateReasoningLabelPayload) => Promise<GeneratedReasoningLabel>;
  now?: () => number;
}
interface ReasoningLabelWiring {
  handlers: (handlers: Record<string, EventHandler$1> | undefined) => Record<string, EventHandler$1> | undefined;
  /** Closes every reasoning step and schedules any meaningful trailing revision. */
  complete: () => void;
}
interface ReasoningLabelGapEvent {
  event: string;
  data: Record<string, unknown>;
}
/** Re-emits reasoning-title revisions or resets committed in the resume snapshot gap. */
declare function synthesizeReasoningLabelGapEvents(snapshotContent: ReadonlyArray<LooseContentPart | null | undefined>, freshContent: ReadonlyArray<LooseContentPart | null | undefined>, meta: {
  conversationId: string;
  responseMessageId?: string;
}): ReasoningLabelGapEvent[];
/**
* Adds a throttled, revision-safe title lifecycle to top-level reasoning run
* steps. Nested subagent-content envelopes have a separate persistence and UI
* lifecycle and are intentionally outside this wiring. The title lives on the
* THINK part itself, so updates never reserve or shift content indices.
*/
declare function createReasoningLabelWiring(deps: ReasoningLabelHostDeps): ReasoningLabelWiring;
//#endregion
//#region src/agents/reasoningLabels/host.d.ts
type ReasoningLabelUsageMetadata = Record<string, unknown>;
interface ReasoningLabelPromptMessage {
  content: unknown;
}
interface ReasoningLabelCaptureCallback {
  handleLLMStart: (_llm: unknown, prompts: string[]) => void;
  handleChatModelStart: (_llm: unknown, messages: ReasoningLabelPromptMessage[][]) => void;
  handleLLMEnd: HandleLLMEnd;
}
interface ReasoningLabelSDKOptions {
  provider: Providers;
  clientOptions: ActivityLabelLLM["clientOptions"];
  visibleReasoning: string;
  reasoningStepId: string;
  revision: number;
  status: ReasoningLabelStatus;
  previousLabel?: string;
  agentId?: string;
  charLimit: number;
  prompt?: string;
  sourceRunId: string;
  sourceTraceId: string;
  responseId: string;
  chainOptions: {
    signal: AbortSignal;
    callbacks: ReasoningLabelCaptureCallback[];
    configurable: {
      thread_id: string;
      user_id?: string;
      requestBody: {
        parentMessageId?: string;
      };
    };
  };
}
interface ReasoningLabelRun {
  generateReasoningLabel: (options: ReasoningLabelSDKOptions) => Promise<{
    label?: string;
    usage?: ReasoningLabelUsageMetadata;
  }>;
}
interface ReasoningLabelUsageRecord {
  collectedMetadata: Record<string, unknown>[];
  model?: string;
  endpointTokenConfig?: unknown;
  sameEndpoint?: boolean;
  provider: Providers;
  promptText: string;
  completionText: string;
}
interface GenerateReasoningLabelRevisionOptions {
  payload: GenerateReasoningLabelPayload;
  run?: ReasoningLabelRun;
  resolveModel: () => Promise<ActivityLabelLLM>;
  sourceRunId: string;
  sourceTraceId: string;
  responseId: string;
  sessionId: string;
  userId?: string;
  parentMessageId?: string;
  recordUsage: (usage: ReasoningLabelUsageRecord) => void | Promise<void>;
  onError?: (error: unknown) => void;
}
interface ReasoningLabelHostScope {
  closed: boolean;
  abort: AbortController;
  detach?: () => void;
}
type ReasoningLabelDurableEvent = ReasoningLabelAttemptEvent | ReasoningLabelEvent;
interface CreateReasoningLabelHostWiringOptions {
  config: ResolvedReasoningLabelConfig;
  seedFromContent?: boolean;
  abortSignal?: AbortSignal;
  markResumable?: () => Promise<unknown>;
  onMarkFailure?: () => void;
  getContentParts: ReasoningLabelHostDeps["getContentParts"];
  getStepIndex: ReasoningLabelHostDeps["getStepIndex"];
  emitEvent: (event: ReasoningLabelEvents, data: ReasoningLabelDurableEvent) => Promise<unknown>;
  trackPendingFill: ReasoningLabelHostDeps["trackPendingFill"];
  generateLabel: ReasoningLabelHostDeps["generateLabel"];
}
interface ReasoningLabelHostWiringResult {
  wiring: ReasoningLabelWiring;
  scope: ReasoningLabelHostScope;
  markedPromise?: Promise<void>;
}
/** Computes the shared negative usage sequence from durable activity and reasoning calls. */
declare function getLabelUsageSequenceSeed(parts: ReadonlyArray<LooseContentPart | null | undefined>, current?: number): number;
/** Owns the host-side lifecycle around the pure reasoning-label stream controller. */
declare function createReasoningLabelHostWiring({
  config,
  seedFromContent,
  abortSignal,
  markResumable,
  onMarkFailure,
  getContentParts,
  getStepIndex,
  emitEvent,
  trackPendingFill,
  generateLabel
}: CreateReasoningLabelHostWiringOptions): ReasoningLabelHostWiringResult;
/** Invokes one SDK reasoning-label revision and captures complete provider text for billing. */
declare function generateReasoningLabelRevision({
  payload,
  run,
  resolveModel,
  sourceRunId,
  sourceTraceId,
  responseId,
  sessionId,
  userId,
  parentMessageId,
  recordUsage,
  onError
}: GenerateReasoningLabelRevisionOptions): Promise<GeneratedReasoningLabel>;
//#endregion
//#region src/agents/refusal.d.ts
interface ModelResponseMetadata {
  messageStop?: {
    stopReason?: string;
  };
  stopReason?: string;
}
interface ModelOutputWithRefusal<TAdditional extends {
  stop_reason?: string;
}> {
  additional_kwargs?: TAdditional;
  response_metadata?: ModelResponseMetadata;
}
/** Normalizes provider-specific model refusal metadata for downstream handlers. */
declare function getModelRefusalInfo<TAdditional extends {
  stop_reason?: string;
}>(output?: ModelOutputWithRefusal<TAdditional>): TAdditional | {
  stop_reason: string;
} | undefined;
//#endregion
//#region src/agents/remote/lifecycle.d.ts
declare class AgentExecutionAdmissionError extends Error {
  readonly code: "ACCOUNT_DELETION_IN_PROGRESS" | "RUN_REPLACED";
  readonly status = 409;
  constructor(message: string, code: "ACCOUNT_DELETION_IN_PROGRESS" | "RUN_REPLACED");
}
interface AgentExecutionEnrollmentParams {
  runId: string;
  userId: string;
  conversationId: string;
  agentId: string;
  protocol: "chat.completions" | "responses";
  isPrincipalActive: (userId: string) => Promise<boolean>;
}
interface AgentExecutionEnrollmentDeps {
  manager: GenerationJobManagerClass;
}
declare function waitForAgentExecutionWrites<T>(writes: readonly Promise<T>[]): Promise<void>;
declare class AgentExecutionEnrollment {
  readonly runId: string;
  readonly createdAt: number;
  readonly signal: AbortSignal;
  private readonly manager;
  private readonly providerExecutionId;
  private readonly abortController;
  private readonly trailingWrites;
  private providerStarted;
  private settlement?;
  constructor(manager: GenerationJobManagerClass, job: GenerationJob);
  abort(reason?: unknown): void;
  track<T>(write: Promise<T>): Promise<T>;
  beginProviderExecution(): Promise<void>;
  settle(error?: unknown): Promise<void>;
  private settleInternal;
}
declare function enrollAgentExecution(params: AgentExecutionEnrollmentParams, deps?: AgentExecutionEnrollmentDeps): Promise<AgentExecutionEnrollment>;
//#endregion
//#region src/agents/remote/host.d.ts
/** Transport connection observed by the execution host for cancellation only. */
interface AgentExecutionConnection {
  isClosed: () => boolean;
  onClose: (listener: () => void) => () => void;
}
interface ExecuteAgentRunParams<Result> {
  envelope: AgentRunEnvelope;
  runId: string;
  conversationId: string;
  connection?: AgentExecutionConnection;
  isPrincipalActive: (userId: string) => Promise<boolean>;
  execute: (execution: AgentExecutionEnrollment) => Promise<Result>;
  handleExecutionError?: (error: unknown, signal?: AbortSignal) => Result | Promise<Result>;
  beforeSettle?: (execution: AgentExecutionEnrollment, executionError: unknown) => void | Promise<void>;
  onSettlementError?: (error: unknown) => void;
}
/**
* Owns admission, cancellation, provider-start fencing, and settlement for a
* validated Agent run. Protocol implementations own only execution semantics;
* ingress adapters own transport validation and final rendering.
*/
declare function executeAgentRun<Result>({
  envelope,
  runId,
  conversationId,
  connection,
  isPrincipalActive,
  execute,
  handleExecutionError,
  beforeSettle,
  onSettlementError
}: ExecuteAgentRunParams<Result>): Promise<Result>;
//#endregion
//#region src/agents/queuedTurns.d.ts
declare const AGENT_QUEUED_TURN_SOURCE = "agent-queued-turn";
interface GenerationState {
  streamId?: unknown;
  status?: unknown;
  createdAt?: unknown;
  error?: unknown;
  metadata?: {
    idempotencyClientRequestId?: unknown;
    terminalPersistencePending?: unknown;
  };
}
interface GenerationAdmissionEvidence {
  generationId: string;
  generationCreatedAt: number;
}
type GetGenerationAdmissionEvidence = (userId: string, clientRequestId: string, streamId: string, conversationId?: string) => Promise<GenerationAdmissionEvidence | null>;
type QueuedTurnResolverMethods = AgentQueuedTurnMethods & Pick<ConversationMethods, "getConvo"> & Pick<MessageMethods, "getMessages">;
interface AgentQueuedTurnResolverDeps {
  methods: QueuedTurnResolverMethods;
  getGenerationJob: (conversationId: string) => Promise<GenerationState | null>;
  now?: () => number;
  claimBy?: string;
}
type EnqueueAgentQueuedTurnDelivery = (envelope: unknown, options?: AgentTriggerEnqueueOptions) => Promise<{
  deliveryKey: string;
}>;
interface AgentQueuedTurnSchedulerDeps {
  methods: AgentQueuedTurnMethods;
  enqueue: EnqueueAgentQueuedTurnDelivery;
  getGenerationAdmissionEvidence: GetGenerationAdmissionEvidence;
  recoveryIntervalMs?: number;
  recoveryLimit?: number;
}
interface AgentQueuedTurnScheduler {
  initialize: () => Promise<void>;
  stop: () => Promise<void>;
  schedule: (turn: AgentQueuedTurnRecord) => Promise<string>;
  recover: () => Promise<number>;
}
interface AgentQueuedTurnLifecycle {
  prepareContinue: NonNullable<AgentTriggerExecutionHostDeps["prepareContinue"]>;
  settleBeforeDeadLetter: (rawEnvelope: unknown, failure: AgentTriggerDeliveryFailure) => Promise<void>;
  recordExecutionAdmission: (rawSource: unknown, input: AgentQueuedTurnExecutionAdmission) => Promise<boolean>;
  verifyExecutionAdmission: (rawSource: unknown, input: AgentQueuedTurnExecutionAdmission) => Promise<boolean>;
  initialize: () => Promise<void>;
  stop: () => Promise<void>;
  schedule: (turn: AgentQueuedTurnRecord) => Promise<string>;
  cancel: (input: Parameters<AgentQueuedTurnMethods["cancelAgentQueuedTurn"]>[0]) => ReturnType<AgentQueuedTurnMethods["cancelAgentQueuedTurn"]>;
  recover: () => Promise<number>;
}
interface AgentQueuedTurnLifecycleDeps {
  methods: QueuedTurnResolverMethods;
  getGenerationJob: AgentQueuedTurnResolverDeps["getGenerationJob"];
  getGenerationAdmissionEvidence: GetGenerationAdmissionEvidence;
  enqueue: EnqueueAgentQueuedTurnDelivery;
  retireDelivery?: (deliveryKey: string, sourceId: string, reason: string, options?: {
    onlyIfDead?: boolean;
  }) => Promise<boolean>;
  getDelivery?: (deliveryKey: string) => Promise<unknown | null>;
  now?: () => number;
  claimBy?: string;
  recoveryIntervalMs?: number;
  recoveryLimit?: number;
}
interface AgentQueuedTurnExecutionAdmission {
  userId: string;
  tenantId?: string;
  conversationId: string;
  clientRequestId: string;
  generationId: string;
  generationCreatedAt: number;
}
/** Owns the complete backend Agent queued-turn lifecycle while retaining
* Mongo, trigger delivery, and generation execution as internal adapters. */
declare function createAgentQueuedTurnLifecycle({
  methods,
  getGenerationJob,
  getGenerationAdmissionEvidence,
  enqueue,
  retireDelivery,
  getDelivery,
  now,
  claimBy,
  recoveryIntervalMs,
  recoveryLimit
}: AgentQueuedTurnLifecycleDeps): AgentQueuedTurnLifecycle;
//#endregion
//#region src/agents/queuedTurnHttp.d.ts
interface QueuedTurnConversation {
  agent_id?: string;
  endpoint?: string;
  tenantId?: string;
}
interface QueuedTurnHttpMethods extends AgentQueuedTurnMethods {
  getConvo: (userId: string, conversationId: string) => Promise<QueuedTurnConversation | null>;
}
interface AgentQueuedTurnHttpDeps {
  methods: QueuedTurnHttpMethods;
  lifecycle: Pick<AgentQueuedTurnLifecycle, "schedule" | "cancel">;
  getFiles?: SteerFileFetcher;
  updateFilesUsage?: (files: Array<{
    file_id: string;
  }>, fileIds?: string[], options?: {
    user?: string;
    tenantId?: string | null;
  }) => Promise<unknown[]>;
  checkAgentAccess?: (run: {
    agentId?: string;
    endpoint?: string;
  }) => Promise<boolean>;
  isPrincipalActive?: (userId: string) => boolean | Promise<boolean>;
}
interface AgentQueuedTurnHttpResult {
  status: number;
  body: Record<string, unknown>;
}
declare function handleAgentQueuedTurnEnqueue(user: SteerRequestUser, body: unknown, deps: AgentQueuedTurnHttpDeps): Promise<AgentQueuedTurnHttpResult>;
declare function handleAgentQueuedTurnList(user: SteerRequestUser, conversationId: unknown, deps: AgentQueuedTurnHttpDeps, rawClientRequestIds?: unknown): Promise<AgentQueuedTurnHttpResult>;
declare function handleAgentQueuedTurnCancel(user: SteerRequestUser, queuedTurnId: unknown, deps: AgentQueuedTurnHttpDeps): Promise<AgentQueuedTurnHttpResult>;
//#endregion
//#region src/files/provision/callback.d.ts
/** Deferred database write produced by a successful provisioning call. */
interface FileUpdate {
  file_id: string;
  metadata?: Record<string, unknown>;
  embedded?: boolean;
}
/** The slice of a per-agent tool context this callback reads and updates. */
interface ProvisionToolContext {
  provisionState?: ProvisionState;
  tool_resources?: AgentToolResources;
  /** Code API deployment this agent resolved, so uploads land where it will execute. */
  codeExecutionContext?: CodeExecutionRoute;
  /** Successful refs retained while another file in the same batch awaits retry. */
  pendingProvisionedCodeFiles?: CodeEnvFile[];
}
interface ProvisionCallbackDeps {
  req: ServerRequest;
  agentToolContexts: Map<string, ProvisionToolContext>;
  resolvePrimaryAgentId?: () => string | undefined;
  provisionToCodeEnv: ProvisionService["provisionToCodeEnv"];
  provisionToVectorDB: ProvisionService["provisionToVectorDB"];
  updateFile: (update: FileUpdate) => Promise<unknown>;
  updateCodeEnvRef: (update: CodeEnvRefUpdate) => Promise<unknown>;
  /** Records one vector namespace without disturbing the others already recorded. */
  addEmbeddedEntity: (update: {
    file_id: string;
    entityId: string;
  }) => Promise<unknown>;
}
/**
* Builds the ON_TOOL_EXECUTE provisioning callback. Shared by the chat path and the
* OpenAI-compatible controllers so every surface provisions queued attachments before
* the tool that needs them loads.
*/
declare function createProvisionFilesCallback({
  req,
  agentToolContexts,
  resolvePrimaryAgentId,
  provisionToCodeEnv,
  provisionToVectorDB,
  updateFile,
  updateCodeEnvRef,
  addEmbeddedEntity
}: ProvisionCallbackDeps): (toolNames: string[], agentId?: string, signal?: AbortSignal) => Promise<CodeEnvFile[]>;
//#endregion
//#region src/agents/files/host.d.ts
interface RunFileToolContext extends ProvisionToolContext {
  agent?: Agent;
  codeEnvAvailable?: boolean;
  codeExecutionContext?: CodeExecutionContext;
}
interface RunFileHost<TContext extends RunFileToolContext = RunFileToolContext> {
  session: RunFileSession;
  getContext: (agentId: string, executionContext?: SubagentExecutionContext) => TContext | undefined;
  provision: (toolNames: string[], agentId: string, signal: AbortSignal, executionContext?: SubagentExecutionContext) => Promise<CodeEnvFile[] | undefined>;
  /** Used immediately after the handler has authorized the current tool batch. */
  provisionPrepared: (toolNames: string[], agentId: string, signal: AbortSignal, executionContext?: SubagentExecutionContext) => Promise<CodeEnvFile[] | undefined>;
  deliverToolEnd: (callback: ToolEndCallback, data: Parameters<ToolEndCallback>[0], metadata: ToolEndCallbackMetadata) => Promise<void>;
}
/** Overrides only the session identity; deployments and credentials remain host-resolved. */
declare function resolveRunFileCodeExecutionContext(resolved: CodeExecutionContext, shared?: CodeExecutionContext): CodeExecutionContext;
declare function createRunFileHost<TContext extends RunFileToolContext>({
  req,
  contexts,
  createdAt,
  inputFileIds,
  getInputs,
  loadFiles,
  filterFiles,
  validateMessages,
  encodeMessages,
  listPublications,
  snapshots,
  publish,
  emit,
  audit,
  provisioning
}: {
  req: ServerRequest;
  contexts: Map<string, TContext>;
  createdAt: number;
  inputFileIds: ReadonlySet<string>;
  getInputs: () => readonly TFile[];
  loadFiles: (fileIds: string[]) => Promise<TFile[]>;
  filterFiles: TFilterFilesByAgentAccess;
  validateMessages: RunFileSessionDeps["validateMessages"];
  encodeMessages: RunFileSessionDeps["encodeMessages"];
  listPublications: RunFileSessionDeps["listPublications"];
  snapshots: RunFileSessionDeps["snapshots"];
  publish: RunFileSessionDeps["publish"];
  emit: RunFileSessionDeps["emit"];
  audit?: RunFileSessionDeps["audit"];
  provisioning: Pick<ProvisionCallbackDeps, "provisionToCodeEnv" | "provisionToVectorDB" | "updateFile" | "updateCodeEnvRef" | "addEmbeddedEntity">;
}): RunFileHost<TContext>;
//#endregion
//#region src/agents/files/binding.d.ts
type HostOptions = Parameters<typeof createRunFileHost>[0];
type PublisherOptions = Parameters<typeof createRunArtifactPublisher>[0];
type ProvisionCallback = (names: string[], agentId?: string, signal?: AbortSignal) => Promise<CodeEnvFile[] | void>;
interface ChatRunFileBindings extends RunFileHost {
  getCodeExecutionContext: (agentId: string, context?: SubagentExecutionContext) => CodeExecutionContext | undefined;
  wrapProvision: (fallback: ProvisionCallback) => (names: string[], agentId?: string, signal?: AbortSignal, context?: SubagentExecutionContext) => Promise<CodeEnvFile[] | void>;
  wrapToolEnd: (fallback: ToolEndCallback) => ToolEndCallback;
}
/** Binds the existing storage, preview, and SSE adapters without giving shared files another pipeline. */
declare function createChatRunFileBindings({
  req,
  contexts,
  createdAt,
  audit,
  requestFiles,
  getInputs,
  loadFiles,
  filterFiles,
  listPublications,
  provisioning,
  fileMethods,
  processCodeOutput,
  snapshotAdapter,
  finalize,
  encoder,
  getStrategyFunctions,
  artifactPromises,
  emitAttachment
}: {
  req: ServerRequest;
  contexts: HostOptions["contexts"];
  createdAt?: number;
  audit?: HostOptions["audit"];
  requestFiles: readonly {
    file_id?: string;
  }[];
  getInputs: HostOptions["getInputs"];
  loadFiles: HostOptions["loadFiles"];
  filterFiles: HostOptions["filterFiles"];
  listPublications: HostOptions["listPublications"];
  provisioning: HostOptions["provisioning"];
  fileMethods: Pick<PublisherOptions, "claimRunArtifactFile" | "publishRunArtifactFile" | "findRunArtifactFile">;
  processCodeOutput: (input: Parameters<PublisherOptions["processCodeOutput"]>[0] & {
    req: ServerRequest;
  }) => ReturnType<PublisherOptions["processCodeOutput"]>;
  snapshotAdapter: Omit<Parameters<typeof createRunArtifactSnapshotAdapter>[0], "req">;
  finalize: (input: {
    finalize?: PublishedCodeOutputResult["finalize"];
    fileId?: string;
    previewRevision?: string;
  }) => void;
  encoder: Omit<RunFileMessageEncoderDeps, "req" | "getStrategyFunctions">;
  getStrategyFunctions: (source: string) => StrategyFunctions;
  artifactPromises: Array<Promise<TFile | null>>;
  emitAttachment: (file: TFile) => void;
}): ChatRunFileBindings;
//#endregion
//#region src/agents/files/delivery.d.ts
/** The app config a turn's attachment routing reads. */
type TurnDeliveryConfig = Pick<AppConfig, "fileConfig" | "speech" | "endpoints">;
/** The fields of an agent that route its attachments. */
interface TurnDeliveryAgent {
  provider: string;
  /** The endpoint name initialization records before it swaps `provider` for the backing
  *  client; an agent loaded without one is routed by its provider. */
  endpoint?: string | null;
  /** The Responses API setting the turn runs on, once initialization has decided it. */
  model_parameters?: {
    useResponsesApi?: boolean;
  } | null;
}
/**
* Settles how the agent running a turn receives its attachments.
*
* Read once, after initialization has resolved the backing provider and the Responses API
* decision: the file policy is the one configured under the endpoint's own name, the media
* dialect is the one its config declares rather than the client family it runs as, and the
* Responses setting is the one the model call uses. Every reader of a turn route consumes the
* returned value, so delivery, steering and child run-file encoding cannot answer differently.
*/
declare function resolveTurnDeliveryRouting({
  agent,
  config
}: {
  agent: TurnDeliveryAgent;
  config?: TurnDeliveryConfig;
}): TurnDeliveryRouting;
/** Materialize the exact turn route before admission without mutating stored records. */
declare function applyTurnDelivery<T extends TurnDeliveryFile>(files: T[], {
  routing,
  consumers
}: {
  routing?: TurnDeliveryRouting;
  consumers?: TurnFileConsumers;
}): T[];
/**
* Checkpoints replay already encoded content; current tools and policy cannot remove it.
* A referenced inferred tool file carrying extracted text may have been delivered as fallback.
* Charge that text conservatively even if fallback was since disabled. Explicit destinations
* and records predating routing keep their existing accounting; no stored record is changed.
*/
declare function applyCheckpointDelivery<T extends TurnDeliveryFile>(files: T[]): T[];
/**
* A primary agent's tool route cannot decide whether a handoff receives text. Keep
* owner-hydrated candidates until each receiver resolves them, then add only text absent
* from the shared prompt to its scoped context. The existing scoped-context pipeline owns
* endpoint filtering, aggregate admission, inspection and extraction for these copies.
*/
declare function resolveScopedTurnAttachments<T extends TurnDeliveryFile & {
  file_id: string;
}>({
  agents,
  sharedConversationAgentIds,
  resendFiles,
  messages,
  historicalFiles,
  requestAttachments,
  sharedRunAttachmentIds,
  attachmentsByAgentId
}: {
  agents: readonly {
    agentId: string;
    agent: {
      deliveryRouting?: TurnDeliveryRouting;
      fileConsumers?: TurnFileConsumers;
    };
  }[]; /** Only the primary/handoff graph shares root conversation files. */
  sharedConversationAgentIds: readonly string[];
  resendFiles?: boolean;
  messages: Parameters<typeof collectModelBoundHistoricalFileIdState>[0];
  historicalFiles?: ReadonlyMap<string, T>;
  requestAttachments: readonly T[];
  sharedRunAttachmentIds: ReadonlySet<string>;
  attachmentsByAgentId?: Map<string, T[]> | Record<string, T[]>;
}): Map<string, T[]>;
//#endregion
//#region src/assistants/protection.d.ts
interface AssistantProtectionConfig {
  readonly filters?: FiltersConfig;
  readonly messageFilter?: {
    readonly pii?: MessageFilterPiiConfig;
  };
}
interface AssistantThreadMessage extends StoredMessageContentInput {
  readonly id?: string;
  readonly file_ids?: readonly string[];
}
interface AssistantThreadMessagePage {
  readonly data?: readonly AssistantThreadMessage[];
  readonly last_id?: string;
  readonly has_more?: boolean;
  readonly hasNextPage?: () => boolean;
  readonly getNextPage?: () => Promise<AssistantThreadMessagePage | null | undefined>;
}
interface AssistantOpenAIClient {
  readonly beta: {
    readonly assistants: {
      readonly retrieve: (assistantId: string) => Promise<AssistantContentInput | null | undefined>;
    };
    readonly threads: {
      readonly messages: {
        readonly list: (threadId: string, options: {
          readonly limit: number;
          readonly order: "asc";
          readonly after?: string;
        }) => Promise<AssistantThreadMessagePage | null | undefined>;
      };
    };
  };
}
interface AssistantUserMessage extends ExternalChatMessage {
  readonly file_ids?: readonly string[];
  readonly attachments?: readonly object[];
  readonly metadata?: object;
}
interface PreflightAssistantRunContentInput {
  readonly onTraversalFailure?: LocatorTraversalReporter;
  readonly config?: AssistantProtectionConfig;
  readonly openai: AssistantOpenAIClient;
  readonly user?: CanonicalFileInspectionUser;
  readonly assistantId: string;
  readonly threadId?: string;
  readonly getFiles: GetCanonicalFilesForInspection;
}
interface PreflightAssistantUserMessageContentInput {
  readonly onTraversalFailure?: LocatorTraversalReporter;
  readonly config?: AssistantProtectionConfig;
  readonly user?: CanonicalFileInspectionUser;
  readonly message: AssistantUserMessage;
  readonly fileIds?: readonly string[];
  readonly getFiles: GetCanonicalFilesForInspection;
}
declare function loadThreadUserMessages(openai: AssistantOpenAIClient, threadId?: string): Promise<AssistantThreadMessage[]>;
/**
* Re-applies current policy to remote Assistants state that can become
* model-bound without appearing in the current chat request.
*/
declare function preflightAssistantRunContent({
  config,
  openai,
  user,
  assistantId,
  threadId,
  getFiles,
  onTraversalFailure
}: PreflightAssistantRunContentInput): Promise<AssistantContentInput | undefined>;
/**
* Re-inspects the exact user message after persisted conversation file
* references have been attached and owner-resolved.
*/
declare function preflightAssistantUserMessageContent({
  config,
  user,
  message,
  fileIds,
  getFiles,
  onTraversalFailure
}: PreflightAssistantUserMessageContentInput): Promise<void>;
//#endregion
//#region src/actions/credentials.d.ts
type ActionMetadataUpdateResult = {
  metadata: ActionMetadata;
  targetChanged: boolean;
  requiresCredentialRefresh: boolean;
};
type ActionMetadataUpdateParams = {
  storedMetadata: ActionMetadata;
  incomingMetadata: ActionMetadata;
};
/**
* Preserves saved credentials only while an Action continues to point at the
* same request/auth boundary. A changed domain, OpenAPI server URL, or auth
* endpoint must come with fresh credentials or explicitly remove auth.
*/
declare function mergeActionMetadataForUpdate({
  storedMetadata,
  incomingMetadata
}: ActionMetadataUpdateParams): ActionMetadataUpdateResult;
//#endregion
//#region src/actions/crypto.d.ts
/**
* Encrypts an action credential, encoding it first so that reserved characters (`@`, `+`, `=`,
* `/`, `:`) survive storage.
*/
declare function encryptSensitiveValue(value: string): Promise<string>;
/**
* Decrypts an action credential, reversing the encoding applied by {@link encryptSensitiveValue}.
*
* Encoding before encryption was introduced in 299cabd6e (March 2025). Credentials stored earlier
* were encrypted raw and carry no marker saying so, so a legacy value containing a stray `%` would
* make `decodeURIComponent` throw `URIError`; those are returned as decrypted.
*
* Every reader of these fields must use this helper. Decoding in one read path but not another
* sends different credentials to the provider depending on which path ran.
*/
declare function decryptSensitiveValue(encryptedValue: string): Promise<string>;
//#endregion
//#region src/actions/protection.d.ts
/** Enforces the exact action projection before it is returned or persisted. */
declare function blockFilteredActionProjection(filters: FiltersConfig | undefined, res: Response$2, action: AssistantActionContentInput): boolean;
//#endregion
//#region src/actions/tools.d.ts
type ActionToolLike = {
  function?: {
    name?: string;
  };
};
type MergeAgentActionToolsParams = {
  existingTools: string[];
  incomingFunctions: ActionToolLike[];
  encodedDomain: string;
  actionId: string;
  requestedActionId?: string;
  legacyDomain?: string;
  previousEncodedDomain?: string;
  previousLegacyDomain?: string;
  previousRawSpec?: string;
};
declare function mergeAgentActionTools({
  existingTools,
  incomingFunctions,
  encodedDomain,
  actionId,
  requestedActionId,
  legacyDomain,
  previousEncodedDomain,
  previousLegacyDomain,
  previousRawSpec
}: MergeAgentActionToolsParams): string[];
//#endregion
//#region src/actions/update.d.ts
declare const ACTION_CREDENTIAL_REFRESH_MESSAGE = "Action credentials must be re-entered when changing the domain, OpenAPI server URL, or authentication settings";
type StoredActionForUpdate = {
  metadata?: ActionMetadata | null;
};
type PlanAgentActionUpdateParams = {
  agentActions: string[];
  agentTools: string[];
  incomingFunctions: ActionToolLike[];
  incomingMetadata: ActionMetadata;
  actionId: string;
  requestedActionId?: string;
  encodedDomain: string;
  legacyDomain?: string;
  previousLegacyDomain?: string;
  storedAction?: StoredActionForUpdate | null;
};
type PlannedAgentActionUpdate = {
  actionId: string;
  metadata: ActionMetadata;
  actions: string[];
  tools: string[];
  targetChanged: boolean;
  requiresCredentialRefresh: boolean;
  deleteOAuthTokens: boolean;
};
type ActionOAuthTokenDeleteQuery = {
  type: "oauth" | "oauth_refresh";
  identifier: RegExp;
};
declare function legacyActionDomainEncode(domain?: string): string;
declare function buildActionOAuthTokenDeleteQueries(actionId: string): ActionOAuthTokenDeleteQuery[];
declare function planAgentActionUpdate({
  agentActions,
  agentTools,
  incomingFunctions,
  incomingMetadata,
  actionId,
  requestedActionId,
  encodedDomain,
  legacyDomain,
  previousLegacyDomain,
  storedAction
}: PlanAgentActionUpdateParams): PlannedAgentActionUpdate;
//#endregion
//#region src/prompts/artifacts/index.d.ts
/**
* Generates an artifacts prompt based on the endpoint and artifact mode
* @param params - Configuration parameters
* @param params.endpoint - The current endpoint
* @param params.artifacts - The current artifact mode
* @returns The artifacts prompt, or null if mode is CUSTOM
*/
declare function generateArtifactsPrompt(params: {
  endpoint: EModelEndpoint | string;
  artifacts: ArtifactModes;
}): string | null;
//#endregion
//#region src/prompts/format.d.ts
/**
* Formats prompt groups for the paginated /groups endpoint response
*/
declare function formatPromptGroupsResponse({
  promptGroups,
  pageNumber,
  pageSize,
  actualLimit,
  hasMore,
  after
}: {
  promptGroups: IPromptGroupDocument[];
  pageNumber?: string;
  pageSize?: string;
  actualLimit?: string | number;
  hasMore?: boolean;
  after?: string | null;
}): PromptGroupsListResponse;
/**
* Creates an empty response for the paginated /groups endpoint
*/
declare function createEmptyPromptGroupsResponse({
  pageNumber,
  pageSize,
  actualLimit
}: {
  pageNumber?: string;
  pageSize?: string;
  actualLimit?: string | number;
}): PromptGroupsListResponse;
/**
* Marks prompt groups as public based on the publicly accessible IDs
*/
declare function markPublicPromptGroups(promptGroups: IPromptGroupDocument[], publiclyAccessibleIds: Types.ObjectId[]): IPromptGroupDocument[];
/**
* Builds filter object for prompt group queries
*/
declare function buildPromptGroupFilter({
  name,
  category
}: {
  name?: string;
  category?: string;
}): {
  filter: Record<string, string | number | boolean | RegExp | undefined>;
  searchShared: boolean;
  searchSharedOnly: boolean;
};
/**
* Filters accessible IDs based on shared/public prompts logic.
*
* @param ownedPromptGroupIds - IDs of prompt groups authored by the current user.
*   Required for correct MY_PROMPTS and SHARED_PROMPTS filtering. When omitted the
*   function falls back to the legacy behaviour (public-only filtering).
*/
declare function filterAccessibleIdsBySharedLogic({
  accessibleIds,
  searchShared,
  searchSharedOnly,
  publicPromptGroupIds,
  ownedPromptGroupIds
}: {
  accessibleIds: Types.ObjectId[];
  searchShared: boolean;
  searchSharedOnly: boolean;
  publicPromptGroupIds?: Types.ObjectId[];
  ownedPromptGroupIds?: Types.ObjectId[];
}): Promise<Types.ObjectId[]>;
//#endregion
//#region src/prompts/migration.d.ts
interface PromptMigrationCheckDbMethods {
  findRoleByIdentifier: AccessRoleMethods["findRoleByIdentifier"];
}
interface PromptMigrationCheckParams {
  mongoose: Mongoose;
  methods: PromptMigrationCheckDbMethods;
  PromptGroupModel: Model<IPromptGroupDocument>;
}
interface PromptMigrationCheckResult {
  totalToMigrate: number;
  globalViewAccess: number;
  privateGroups: number;
  details?: {
    globalViewAccess: Array<{
      name: string;
      _id: string;
      category: string;
    }>;
    privateGroups: Array<{
      name: string;
      _id: string;
      category: string;
    }>;
  };
}
/**
* Check if prompt groups need to be migrated to the new permission system
* This performs a dry-run check similar to the migration script
*/
declare function checkPromptPermissionsMigration({
  methods,
  mongoose,
  PromptGroupModel
}: PromptMigrationCheckParams): Promise<PromptMigrationCheckResult>;
/**
* Log migration warning to console if prompt groups need migration
*/
declare function logPromptMigrationWarning(result: PromptMigrationCheckResult): void;
//#endregion
//#region src/prompts/protection.d.ts
type PromptProjectionKey = "name" | "description" | "oneliner" | "category" | "command" | "prompt";
type PresetProjectionKey = PromptProjectionKey | "title" | "promptPrefix" | "system" | "context" | "instructions" | "additional_instructions" | "greeting" | "examples" | "stop" | "additionalModelRequestFields" | "additional_model_request_fields" | "response_format" | "responseFormat" | "metadata" | "model_parameters" | "options";
type ProjectedStoredPrompt<T extends PromptRecordInput> = T | (Omit<T, PromptProjectionKey> & {
  readonly prompt: "";
  readonly contentFilterBlocked: true;
});
type ProjectedStoredPreset<T extends PresetContentInput> = T | (Omit<T, PresetProjectionKey> & {
  readonly title: "";
  readonly contentFilterBlocked: true;
});
interface StoredPromptGroupInput extends PromptRecordInput {
  readonly productionPrompt?: PromptRecordInput | null;
}
type ProjectedStoredPromptGroup<T extends StoredPromptGroupInput> = T | (Omit<T, "productionPrompt"> & {
  readonly productionPrompt?: ProjectedStoredPrompt<PromptRecordInput> | null;
});
/** Inspects prompt submission content at the shared typed policy boundary. */
declare function inspectPromptContent(input: PromptContentInput | null | undefined, filters?: FiltersConfig): ProtectionFinding | null;
/** Reapplies current prompt policy while retaining structural management fields. */
declare function projectStoredPrompt<T extends PromptRecordInput>(prompt: T, filters?: FiltersConfig): ProjectedStoredPrompt<T>;
/** Projects stored prompts in one pass under the current prompt policy. */
declare function projectStoredPrompts<T extends PromptRecordInput>(prompts: readonly T[], filters?: FiltersConfig): readonly ProjectedStoredPrompt<T>[];
/** Omits blocked group metadata and redacts or rejects its production prompt. */
declare function projectStoredPromptGroup<T extends StoredPromptGroupInput>(group: T, filters?: FiltersConfig, options?: {
  readonly forReuse?: boolean;
}): ProjectedStoredPromptGroup<T> | null;
/** Projects prompt groups in one pass and omits groups unsafe for the requested use. */
declare function projectStoredPromptGroups<T extends StoredPromptGroupInput>(groups: readonly T[], filters?: FiltersConfig, options?: {
  readonly forReuse?: boolean;
}): readonly ProjectedStoredPromptGroup<T>[];
/** Reapplies prompt and model-parameter policy to stored presets. */
declare function projectStoredPreset<T extends PresetContentInput>(preset: T, filters?: FiltersConfig): ProjectedStoredPreset<T>;
/** Projects stored presets in one pass under the current prompt/model policy. */
declare function projectStoredPresets<T extends PresetContentInput>(presets: readonly T[], filters?: FiltersConfig): readonly ProjectedStoredPreset<T>[];
//#endregion
//#region src/prompts/schemas.d.ts
/**
* Schema for validating prompt group update payloads.
* Only allows fields that users should be able to modify.
* Sensitive fields like author, authorName, _id, productionId, etc. are excluded.
*/
declare const updatePromptGroupSchema: z.ZodObject<{
  /** The name of the prompt group */name: z.ZodOptional<z.ZodString>; /** Short description/oneliner for the prompt group */
  oneliner: z.ZodOptional<z.ZodString>; /** Category for organizing prompt groups */
  category: z.ZodOptional<z.ZodString>; /** Command shortcut for the prompt group */
  command: z.ZodNullable<z.ZodOptional<z.ZodString>>;
}, "strict">;
type TUpdatePromptGroupSchema = z.infer<typeof updatePromptGroupSchema>;
/**
* Validates and sanitizes a prompt group update payload.
* Returns only the allowed fields, stripping any sensitive fields.
* @param data - The raw request body to validate
* @returns The validated and sanitized payload
* @throws ZodError if validation fails
*/
declare function validatePromptGroupUpdate(data: unknown): TUpdatePromptGroupSchema;
/**
* Safely validates a prompt group update payload without throwing.
* @param data - The raw request body to validate
* @returns A SafeParseResult with either the validated data or validation errors
*/
declare function safeValidatePromptGroupUpdate(data: unknown): z.SafeParseReturnType<{
  name?: string | undefined;
  category?: string | undefined;
  command?: string | null | undefined;
  oneliner?: string | undefined;
}, {
  name?: string | undefined;
  category?: string | undefined;
  command?: string | null | undefined;
  oneliner?: string | undefined;
}>;
//#endregion
//#region src/projects/handlers.d.ts
interface ProjectUser {
  id: string;
  _id?: {
    toString(): string;
  };
}
interface ProjectRequest extends Request {
  user?: ProjectUser;
}
type ProjectHandlerDependencies = Pick<ChatProjectMethods, "listChatProjects" | "createChatProject" | "getChatProject" | "updateChatProject" | "deleteChatProject" | "assignConversationToProject">;
declare function createProjectHandlers(deps: ProjectHandlerDependencies): {
  listProjects: (req: ProjectRequest, res: Response$2) => Promise<Response$2>;
  createProject: (req: ProjectRequest, res: Response$2) => Promise<Response$2>;
  assignConversationToProject: (req: ProjectRequest, res: Response$2) => Promise<Response$2>;
  getProject: (req: ProjectRequest, res: Response$2) => Promise<Response$2>;
  updateProject: (req: ProjectRequest, res: Response$2) => Promise<Response$2>;
  deleteProject: (req: ProjectRequest, res: Response$2) => Promise<Response$2>;
};
//#endregion
//#region src/conversations/archive.d.ts
type ArchiveAllHandlerDependencies = Pick<ConversationMethods, "archiveAllConvos">;
declare function createArchiveAllHandler(deps: ArchiveAllHandlerDependencies): (req: ServerRequest, res: Response$2) => Promise<Response$2>;
//#endregion
//#region src/conversations/chatgpt.d.ts
interface ChatGptThought {
  content?: string | null;
  summary?: string | null;
}
interface ChatGptExportContent {
  content_type?: string | null;
  thoughts?: ChatGptThought[] | null;
}
interface ChatGptExportMessage {
  author?: {
    role?: string | null;
  } | null;
  content?: ChatGptExportContent | null;
}
interface ChatGptMappingNode {
  message?: ChatGptExportMessage | null;
  parent?: string | null;
}
type ChatGptMapping = Readonly<Record<string, ChatGptMappingNode | undefined>>;
interface ChatGptCitationMetadata {
  type?: string;
  title?: string;
  url?: string;
}
interface ChatGptCitation {
  start_ix?: number;
  end_ix?: number;
  metadata?: ChatGptCitationMetadata | null;
}
interface ChatGptLineage {
  /** Nearest imported ancestor, passing over system, reasoning-recap and thoughts nodes. */
  findValidParent: (startId: string | null | undefined) => string;
  /** Reasoning from the thoughts node behind a response, reached through any reasoning recaps. */
  findThinkingContent: (parentId: string | null | undefined) => Agents.ReasoningContentText[];
}
/**
* Resolves parent and reasoning ancestry for one exported ChatGPT conversation.
* Every node's answer is memoized when its walk finishes, so a long run of passed-over
* ancestors is traversed once for the whole conversation rather than once per descendant.
* `messageIds` maps export node ids to imported message ids and must not change afterward.
*/
declare function createChatGptLineage(mapping: ChatGptMapping, messageIds: ReadonlyMap<string, string>): ChatGptLineage;
/**
* Replaces webpage citation markers with Markdown links in a single pass over the text.
* Citations apply from the end of the text backward, with indices past the end clamped to it
* as `String.prototype.slice` would; a citation overlapping one already applied is left out.
*/
declare function linkChatGptCitations(text: string, citations: readonly (ChatGptCitation | null | undefined)[] | null | undefined): string;
//#endregion
//#region src/conversations/import.d.ts
declare const MAX_CONVERSATION_IMPORT_BSON_BYTES: number;
declare const CONVERSATION_IMPORT_BSON_HEADROOM_BYTES: number;
declare const MAX_CONVERSATION_IMPORT_DOCUMENT_BYTES: number;
interface ConversationImportWriteBatch {
  conversations: readonly Document[];
  messages: readonly Document[];
  tenantId?: string;
}
interface ConversationImportWriteOperations {
  saveConversations: () => Promise<void>;
  saveMessages: () => Promise<void>;
  updateTagCounts: () => Promise<void>;
  deleteMessages: () => Promise<void>;
  deleteConversations: () => Promise<void>;
  onTagCountError?: (error: Error) => void;
  onCleanupError?: (error: Error, resource: "messages" | "conversations") => void;
}
declare class ConversationImportError extends Error {
  readonly code = "invalid_request";
  readonly statusCode: number;
  readonly body: {
    error: "invalid_request";
    message: string;
  };
  constructor(message: string, statusCode: number, options?: ErrorOptions);
}
declare function isConversationImportError(error: unknown): error is ConversationImportError;
declare function assertConversationImportWriteSize(batch: ConversationImportWriteBatch): void;
declare function executeConversationImportWrites(operations: ConversationImportWriteOperations): Promise<void>;
//#endregion
//#region src/conversations/lineage.d.ts
/** The identity, parent link and timestamp of a message whose lineage is being ordered. */
interface LineageMessage {
  messageId: string;
  parentMessageId?: string | null;
  createdAt: Date;
}
/** A message as it is read before cloning, with the timestamp still in its stored form. */
interface LineageSource {
  messageId: string;
  parentMessageId?: string | null;
  createdAt?: Date | string | number | null;
}
/** A source message's clone identity: its new id, re-linked parent and ordered timestamp. */
interface ClonedLineageEntry<T extends LineageSource> {
  source: T;
  messageId: string;
  /** Undefined when the source's parent had not been cloned yet in clone order. */
  parentMessageId: string | undefined;
  createdAt: Date;
}
interface ClonedLineage<T extends LineageSource> {
  entries: ClonedLineageEntry<T>[];
  /** Source message id to cloned message id. */
  idMapping: Map<string, string>;
}
/**
* Severs the parent link that closes each cycle so the messages form a forest.
* Every message joins at most one walk, so the pass is linear in the message count.
* @returns Whether any cycle was found.
*/
declare function breakParentCycles<T extends LineageMessage>(messages: readonly T[]): boolean;
/**
* Moves each child's `createdAt` to one millisecond after its parent's whenever the child
* would otherwise sort at or before it. Each chain is resolved once, downward from its
* nearest settled ancestor, so the pass is linear in the message count. A cycle that
* remains is treated as rooted where the walk re-entered it.
*/
declare function orderParentTimestamps<T extends LineageMessage>(messages: readonly T[]): void;
/**
* Makes imported messages a forest whose children sort after their parents:
* cyclic parent links are severed first, then timestamps are ordered in one pass.
*/
declare function orderMessageLineage<T extends LineageMessage>(messages: readonly T[]): void;
/**
* Assigns each message a new id, re-links it to its parent's clone and moves its timestamp
* after that clone's. Root messages are cloned first; a message whose parent is cloned later
* keeps an undefined parent link, as the caller's persistence decides where it attaches.
*/
declare function cloneLineage<T extends LineageSource>(messages: readonly T[], createId: () => string): ClonedLineage<T>;
/**
* Retrieves the target message, its ancestors up to the root, and every sibling along that
* path, excluding the target's own children. The first message listed under an id wins.
*/
declare function getAllMessagesUpToParent<T extends Omit<LineageSource, "createdAt">>(messages: readonly T[], targetMessageId: string): T[];
//#endregion
//#region src/schedules/capacity.d.ts
/** A claim either succeeded (carrying the caller's own result) or lost the slot race. */
type SlotClaimResult<T> = {
  claimed: T;
} | "slot-taken";
//#endregion
//#region src/schedules/types.d.ts
interface ScheduleLimits {
  /** Feature-level switch: when false the engine claims/fires nothing. */
  enabled: boolean;
  maxPerUser: number;
  minIntervalMinutes: number;
  autoDisableAfterFailures: number;
  /** Maximum claimed occurrences concurrently passing readiness admission per replica. */
  admissionConcurrency: number;
  fireConcurrency: number;
  /** Maximum MCP readiness probes active during one schedule admission. */
  mcpPreflightConcurrency: number;
  /** Maximum wall-clock time for one unattended MCP readiness admission. */
  mcpPreflightTimeoutMs: number;
  /** Every schedule must be filed under a chat project. A pinned `projectId`
  *  implies this, so callers only ever have to read one flag. */
  requireProject: boolean;
  /** Operator-pinned destination project. When set it OVERRIDES whatever the row
  *  stores, at write time and at fire time alike — the pin is a policy about where
  *  scheduled runs land, and a stored id from before the pin must not outrank it. */
  projectId?: string;
}
declare const DEFAULT_SCHEDULE_LIMITS: ScheduleLimits;
/**
* The project a schedule's runs must land in under the CURRENT policy: an operator
* pin outranks the stored choice, otherwise the owner's own selection stands.
* Single source of truth for the write handlers, the fire path, and the wire
* projection, so the form, the precheck, and the dispatched conversation can never
* disagree about the destination.
*/
declare function resolveScheduleProjectId(limits: Pick<ScheduleLimits, "projectId">, stored?: string | null): string | undefined;
interface ScheduleUserContext {
  id: string;
  tenantId?: string;
  role?: string;
}
/**
* Outcome of an owner-initiated schedule delete.
* - `deleted`: drained and erased.
* - `draining`: every active run's abort was DELIVERED; erasure follows once the
*   generation records its terminal outcome (erase-on-settle), in any topology.
* - `unconfirmed`: at least one run could not be confirmed stopped (job store
*   unreachable, or the abort was not delivered). The schedule stays hidden and
*   fenced, but its generation may still be producing — callers must not report
*   the run as stopped.
*/
type ScheduleDeleteResult = "not_found" | "deleted" | "draining" | "unconfirmed";
/**
* How long an abort's OWNER is presumed alive and obliged to settle the run itself.
*
* The rule this enforces: **job state is never persistence acknowledgement.** A job
* reads `aborted` — or vanishes, or carries `completedAt` — the moment `abortJob` wins
* its status CAS, which is BEFORE the owner unwinds and writes anything. Both owner
* paths deliberately settle LAST, after `saveMessage`, so the ONLY acknowledgement that
* all persistence-producing work finished is the owner's own terminal outcome write
* (i.e. the run row leaving the active set). Reading post-abort job state as "nothing is
* generating" confirms a drain mid-write: account deletion destroys the user's data and
* the owner then writes a message back for the deleted account.
*
* So while an abort is in flight, nothing but the owner settles — deletion drains
* defer (503 + Retry-After) and the reconciler leaves the row alone. Past the window
* the owner is presumed dead, and the reconciler's orphan handling takes over.
*
* Deliberately the reconciler's ORPHAN cutoff rather than a shorter local grace: that
* presumption already exists and is already the thing that would eventually clear the
* row. One rule, not two.
*/
declare const ABORT_OWNER_PRESUMED_ALIVE_MS: number;
/**
* Whether an abort was requested for this run recently enough that its owner is still
* expected to persist and settle. `abortRequestedAt` is stamped BEFORE any abort is
* signalled (see `abortActiveRun` and the interactive abort route), which is what makes
* it usable as evidence that post-abort job state is not yet a settled generation.
*/
/** How long a resume-claim stamp fences quiesce settling before it is presumed
*  crashed. A resume's own generation shows `running` (already unsettleable);
*  this bound only covers the re-pause hand-off window plus crash recovery. */
declare const RESUME_HANDOFF_STALE_MS: number;
/** Whether a paused run's RESUME hand-off is still in flight: its approval was
*  consumed and the continuation's re-pause writes may still be landing, so the
*  paused job state is not settleable evidence yet. */
declare function hasResumeHandoffInFlight(run: {
  resumeClaimedAt?: Date | null;
}, now: number): boolean;
/**
* The outcome the generation owner intended for a RETAINED `complete` job, or `success`
* when it left none (a fire from before the stamp, or a store that never carried it).
* An unrecognized value is treated as `success` rather than forwarded: it crossed a
* serialization boundary, and `recordRunOutcome` would reject a status outside its
* union — losing a rare refinement beats failing the recovery write outright.
*/
declare function retainedOutcome(jobState: JobState | null, fallback: "success" | "error"): {
  status: "success" | "error" | "interrupted" | "skipped_balance";
  error?: string;
};
declare function hasAbortInFlight(run: {
  abortRequestedAt?: Date;
}, now: number): boolean;
/**
* Renewable upload hold for schedule attachments (extendFilesTTL-shaped), replacing
* the earlier permanent TTL removal, which leaked the upload forever when the schedule
* was deleted before its first run, its file_ids were replaced, or creation failed.
* Touched at create/edit and at every fire preflight; the FIRST fire that actually
* sends the file clears its TTL permanently through the ordinary consumption path, so
* the hold only has to bridge upload -> first consumption. `renewMs` covers the longest
* cadence gap (weekly) twice over; `maxLifetimeMs` bounds a schedule that never manages
* to consume (auto-disable stops its renewals long before this ceiling).
*/
declare const SCHEDULE_FILE_HOLD: {
  renewMs: number;
  maxLifetimeMs: number;
};
interface ScheduleFileRef {
  file_id: string;
  filepath?: string;
  filename?: string;
  type?: string;
  height?: number;
  width?: number;
  source?: string;
}
interface ScheduleEngineDeps {
  preflightMCP: ScheduleMCPPreflight;
  methods: ScheduleMethods;
  /** Resolves interface.schedules limits, per-principal when a user is given. */
  getLimits: (user?: ScheduleUserContext) => Promise<ScheduleLimits>;
  /** Loads the owning user (id + tenant) or null when deleted/disabled. */
  getUserContext: (userId: string | Types.ObjectId) => Promise<ScheduleUserContext | null>;
  /** Whether the balance feature gates this user and they are out of credits. */
  isOutOfBalance: (user: ScheduleUserContext) => Promise<boolean>;
  /** Live agent access for the owner: 'missing' (deleted) vs 'forbidden' (ACL revoked). */
  agentAccess: (agentId: string, user: ScheduleUserContext) => Promise<"ok" | "missing" | "forbidden">;
  /**
  * Whether the owner still owns the schedule's destination chat project. Projects
  * are user-owned, so 'missing' covers both deletion and a pinned id belonging to
  * someone else — the fire path treats them identically because the observable
  * outcome is the same: the conversation would be filed nowhere.
  */
  projectAccess: (projectId: string, user: ScheduleUserContext) => Promise<"ok" | "missing">;
  /** Whether the owning user's current role still grants SCHEDULES access. */
  hasScheduleAccess: (user: ScheduleUserContext) => Promise<boolean>;
  /** Re-resolves stored file_ids to attachment payloads; missing files are simply absent. */
  resolveFiles: (fileIds: string[], user: ScheduleUserContext) => Promise<ScheduleFileRef[]>;
  /** Persists a trusted trigger delivery before this occurrence releases its claim. */
  enqueueTrigger: (envelope: AgentTriggerEnvelope, options?: AgentTriggerEnqueueOptions) => Promise<unknown>;
  /**
  * Reads the durable trigger delivery for a reservation's `deliveryKey`, so
  * reconciliation can tell a still-live admission (`staging`/`batched`/`pending`/`leased`, which
  * a `Retry-After` can defer up to 24h) or a dead-letter (`dead`, with its `lastError`)
  * apart from a genuinely orphaned jobless run. Null when no delivery record exists.
  */
  getTriggerDelivery: (deliveryKey: string) => Promise<{
    status: AgentTriggerDeliveryStatus$1;
    lastError?: AgentTriggerDeliveryFailure$1;
  } | null>;
  /** Runs fn inside the owner's tenant ALS context. */
  runInTenantContext: <T>(user: ScheduleUserContext, fn: () => Promise<T>) => Promise<T>;
  /**
  * Job-store state for a run's conversation, or null when the job is gone. Carries
  * the job's scheduled identity so reconciliation can verify the job at this
  * conversationId is THIS occurrence's generation (a replacement user turn reuses
  * the conversationId but strips scheduleId/scheduledFor) before trusting the status.
  */
  getJobStatus: (conversationId: string) => Promise<JobState | null>;
  /** True once graceful shutdown began; fires step aside instead of dispatching. */
  isShuttingDown?: () => boolean;
  /**
  * Aborts the loopback generation for a scheduled occurrence, identity-guarded so
  * it never signals/clobbers a replacement turn that reused the conversationId.
  * Used by deletion quiescing to stop an in-flight run before its evidence is
  * erased. `preserve` keeps the terminal job for the reconciler (per-schedule
  * delete, whose run row survives to drive cleanup); account deletion passes false
  * since it hard-deletes the run rows, so a preserved job would leak in the store.
  */
  abortScheduledJob: (conversationId: string, identity: JobIdentity, options?: {
    preserve?: boolean;
  }) => Promise<boolean>;
  /**
  * Deletes a retained terminal job after the reconciler has finalized its run.
  * Identity-guarded: only deletes when the job still carries this run's
  * scheduleId/scheduledFor, so a replacement generation occupying the same
  * conversationId is never destroyed. Gives `preserveForReconcile` jobs (kept
  * without `completedAt` so the finished-job sweep can't reap them early) a
  * definitive cleanup path.
  */
  clearReconciledJob: (conversationId: string, identity: JobIdentity) => Promise<void>;
  /** Global in-flight scheduled-run count (system tenant scope) for the fire cap. */
  countActiveRunsGlobal: () => Promise<number>;
  /**
  * The GLOBAL kill switch, deliberately distinct from per-principal availability.
  * True when scheduling is stopped for the whole deployment: the SCHEDULES_DISABLED
  * env lever (works even when the config plane is unhealthy), or `interface.schedules:
  * false` in the BASE config — read base-only so no role/user/tenant override can
  * re-enable it. Checked once per engine tick, so the uncached read is negligible.
  */
  isGloballyDisabled: () => Promise<boolean>;
  /** Whether the run owner's account deletion has begun. Fail-closed (unknown == true). */
  isOwnerDeleting: (userId: string) => Promise<boolean>;
  /**
  * Runs `claim` against the lowest free GLOBAL capacity slot, retrying the next slot
  * when the unique partial index rejects a collision. Enforces fireConcurrency in the
  * database instead of via a read-then-compare count, so concurrent admissions of
  * different schedules cannot both pass a cap-1 check. Occupancy is read in system
  * scope so the cap stays global across tenants.
  */
  withGlobalCapacitySlot: <T>(cap: number, claim: (slot: number) => Promise<SlotClaimResult<T>>) => Promise<{
    claimed: T;
  } | "capacity">;
}
/** The immutable scheduled identity of a generation job, for reconcile/abort fencing. */
interface JobIdentity {
  scheduleId: string;
  scheduledFor: string | Date;
}
/** Job-store state plus the job's scheduled identity (absent on a replacement turn). */
interface JobState {
  status: string;
  checkpointNamespace?: string;
  createdAt?: number;
  scheduleId?: string;
  scheduledFor?: string;
  /** Whether the generation ever emitted its created event — false means a
  *  pre-start abort whose conversation never came to exist. */
  createdEventEmitted?: boolean;
  /** The outcome the generation owner intended to record, stamped on a retained
  *  terminal job. A generic `complete` cannot distinguish a clean run from a balance
  *  refusal or a swallowed provider failure, so reconciliation prefers this when the
  *  owner left it. See SerializableJobData.scheduleOutcome. */
  scheduleOutcome?: string;
  scheduleOutcomeError?: string;
  preserveForScheduleReconcile?: boolean;
}
interface FireResult {
  mcp?: ScheduleMCPOutcome[];
  mcpPreflightUnavailable?: boolean;
  fired: boolean;
  conversationId?: string;
  skipped?: "overlap" | "balance" | "capacity" | "duplicate" | "superseded" | "agent_deleted" | "user_missing" | "user_deleting" | "permission_revoked" | "project_deleted" | "project_required" | "rate_limited" | "disabled";
  error?: string;
}
type FireableSchedule = ISchedule;
type ScheduleMCPPreflight = (agentId: string, user: ScheduleUserContext, options: {
  concurrency: number;
  signal?: AbortSignal;
  deadlineMs?: number;
  scheduleId?: string;
}) => Promise<ScheduleMCPOutcome[]>;
//#endregion
//#region src/schedules/access.d.ts
type AgentAccess = "ok" | "missing" | "forbidden";
interface AgentFireAccessDeps {
  /** Resolves an agent's internal `_id` by its custom id, or null when it doesn't exist. */
  findAgentObjectId: (agentId: string) => Promise<{
    _id: Types.ObjectId;
  } | null>;
  /** Loads a role's permission map by name. */
  getRoleByName: (role?: string) => Promise<{
    permissions?: Record<string, Record<string, boolean | undefined>>;
  } | null>;
  /** Whether the user's role grants a system capability (the manage:agents bypass). */
  hasCapability: (user: ScheduleUserContext, capability: SystemCapability) => Promise<boolean>;
  /** Resource ACL check for a specific permission bit. */
  checkPermission: (params: {
    userId: string;
    role?: string;
    resourceType: ResourceType;
    resourceId: Types.ObjectId;
    requiredPermission: PermissionBits;
  }) => Promise<boolean>;
}
/**
* Resolves a user's live access to a schedule's target agent, mirroring the loopback
* chat route's authorization EXACTLY so the create/update precheck and the fire-time
* precheck accept a schedule iff the actual fire would be accepted:
*   1) role-level AGENTS:USE (checkAccess on the route; admins do NOT bypass)
*   2) resource VIEW with the manage:agents capability bypass
* The two prechecks must never diverge — a create-time VIEW-only check would let a
* role without AGENTS:USE schedule runs that every fire then rejects, burning failures.
*/
declare function createResolveAgentFireAccess(deps: AgentFireAccessDeps): (agentId: string, user: ScheduleUserContext) => Promise<AgentAccess>;
//#endregion
//#region src/schedules/cadence.d.ts
declare const SCHEDULE_JITTER_WINDOW_MS = 12e4;
declare function isValidTimezone(timezone: string): boolean;
/**
* Deterministic per-schedule jitter so fleet-wide fire spikes (everyone at
* 9:00) spread across a window while each schedule's displayed next-run time
* stays stable across recomputations.
*/
declare function scheduleJitterMs(scheduleId: string, windowMs?: number): number;
interface ComputeNextRunParams {
  cadence: TScheduleCadence;
  timezone: string;
  scheduleId: string;
  /** Occurrences at or before this instant are skipped (misfire skip-forward). */
  after?: Date;
  disableJitter?: boolean;
}
/**
* Next fire instant strictly after `after` (default now), jitter applied.
* DST semantics are croner's behavior (locked by cadence.spec.ts): a
* spring-forward gap occurrence fires shifted to the first valid instant
* (02:30 → 03:30); fall-back ambiguity fires the first occurrence only.
*/
declare function computeNextRunAt(params: ComputeNextRunParams): Date | null;
//#endregion
//#region src/schedules/context.d.ts
/** Root actor for an owner-authorized schedule, preserved across subagents and handoffs. */
interface ScheduledTokenContext {
  readonly scheduleId: string;
  readonly ownerId: string;
  readonly tenantId?: string;
  readonly agentId: string;
  readonly invocationMode: "delegated";
}
interface ScheduleJobIdentity {
  userId?: string;
  tenantId?: string;
  scheduleId?: string;
  agent_id?: string;
}
/** Called by the resume host after job ownership, tenant, agent, and schedule checks. */
declare function restoreScheduledTokenContext(req: {
  user: {
    id: string;
    tenantId?: string;
  };
}, metadata?: ScheduleJobIdentity): ScheduledTokenContext | undefined;
//#endregion
//#region src/schedules/engine.d.ts
type ScheduleEngine = {
  stop: () => void; /** Exposed for tests and the run-now handler: one full claim/fire pass. */
  runTick: () => Promise<number>; /** Exposed for tests: one awaitable reconciliation pass (the tick loop's is fire-and-forget). */
  reconcile: () => Promise<void>;
};
declare function startScheduleEngine(deps: ScheduleEngineDeps): ScheduleEngine;
//#endregion
//#region src/schedules/erasure.d.ts
interface ScheduleErasureSweep {
  stop: () => void;
}
interface ScheduleErasureDeps {
  methods: Pick<ScheduleMethods, "getDeletingSchedules" | "eraseScheduleIfDrained" | "markEraseAttempted" | "getActiveRunsForSchedule" | "getRunsForReconciliation" | "recordRunOutcome">;
  /** Job state at a run's conversationId; null = confirmed absent, throw = unknown. */
  getJobStatus: (conversationId: string) => Promise<JobState | null>;
  /** Durable trigger delivery for a reservation's `deliveryKey`. A `dead` delivery is
  *  POSITIVE shared evidence that no generation owns the reservation, so settling on it
  *  is topology-safe (unlike absence). */
  getTriggerDelivery: ScheduleEngineDeps["getTriggerDelivery"];
  /** Deletes a run's retained terminal job once its outcome is durable. Identity-guarded,
  *  so a replacement generation reusing the conversationId is never destroyed. */
  clearReconciledJob: ScheduleEngineDeps["clearReconciledJob"];
  /** Whether absence in this process's job store proves absence deployment-wide.
  * False for the process-local fallback whose scheduler refused unsafe topology. */
  canInferOwnerDeathFromMissingJob: boolean;
}
/**
* Erases soft-deleted schedules once they drain — and NOTHING else.
*
* A `deleting` row is normally erased by whichever actor first observes it drained: the
* delete request, or the terminal outcome write (erase-on-settle). Both are single
* best-effort attempts, so one transient failure — or a lease that outlived the delete —
* leaves a hidden row holding the user's prompt, with no TTL and no way for the owner to
* retry it (the row is hidden from their list). In the standard entrypoint the
* reconciler retries it; the clustered entrypoint runs no engine, so nothing does.
*
* This is deliberately NOT the engine: it never claims, leases, fires, advances, or
* reconciles a run, so running it in every replica of a clustered deployment is safe and
* changes nothing about v1's single-process scheduling. It only re-drives
* `eraseScheduleIfDrained`, which re-checks drained-ness itself (no active run, no live
* lease) and is idempotent — concurrent sweepers race harmlessly.
*/
declare function startScheduleErasureSweep(deps: ScheduleErasureDeps): ScheduleErasureSweep;
//#endregion
//#region src/schedules/fire.d.ts
/** Consecutive balance skips (pre-fire or mid-generation) before auto-disable. */
declare const BALANCE_SKIP_DISABLE_THRESHOLD: number;
/**
* Stable source delivery identity for one scheduled occurrence. The trigger
* service hashes this into the generation-compatible idempotency key.
*/
declare function buildFireClientRequestId(scheduleId: string, scheduledFor: Date): string;
/**
* Fires one claimed occurrence. The caller owns the lease; this function owns
* the run-doc idempotency insert, the skip checks, durable trigger enqueue, and
* advancing `nextRunAt` (every path advances so a schedule can never wedge).
*/
declare function fireSchedule(deps: ScheduleEngineDeps, schedule: FireableSchedule, limits: ScheduleLimits, scheduledFor: Date, options?: {
  manual?: boolean;
  dbNow?: Date;
  signal?: AbortSignal;
}): Promise<FireResult>;
//#endregion
//#region src/schedules/handlers.d.ts
interface SchedulesHandlersDeps {
  preflightMCP: ScheduleMCPPreflight;
  methods: ScheduleMethods;
  getLimits: (user?: ScheduleUserContext) => Promise<ScheduleLimits>;
  /** Agent existence + VIEW access for the requesting user. */
  canViewAgent: (agentId: string, req: ServerRequest) => Promise<boolean>;
  /** Whether the requesting user owns this chat project. Projects are user-owned,
  *  so existence and authorization are the same question. */
  canUseProject: (projectId: string, userId: string) => Promise<boolean>;
  /** Filters to file ids owned by the user. */
  filterOwnedFileIds: (fileIds: string[], userId: string) => Promise<string[]>;
  /** Extends a bounded renewable upload hold on attached files so they survive to the
  *  first fire, which consumes them permanently; a schedule that dies first lets the
  *  hold lapse instead of retaining the upload forever. Throws when any file is gone. */
  markFilesUsed: (fileIds: string[], userId: string) => Promise<void>;
  /** Serialized manual fire (acquires the schedule lease); null if already leased. */
  fireNow: (schedule: FireableSchedule, limits: ScheduleLimits, options?: {
    signal?: AbortSignal;
  }) => Promise<FireResult | null>;
  /**
  * Soft-deletes a schedule with quiescing: stops new claims, aborts in-flight
  * runs, and erases once drained. See ScheduleDeleteResult for the honest states.
  */
  deleteSchedule: (id: string, userId: string) => Promise<ScheduleDeleteResult>;
  /** Whether this user's account deletion has begun. Fail-closed (unknown == true). */
  isUserDeleting: (userId: string) => Promise<boolean>;
}
/**
* Digest of a create payload's FULL intent, stamped on the row at insert and never
* edited. Replay matching compares this immutable record instead of mutable schedule
* state, so a PATCH (or a policy auto-disable flipping `enabled`) landing between the
* first attempt and its retry cannot make a genuine replay read as key reuse.
*/
declare function computeCreateDigest(payload: TCreateSchedule): string;
/** Public projection of a schedule — an allowlist of the `TSchedule` fields, so internal
*  bookkeeping (_id, tenantId, __v, claimToken, lease*, slot, deleting, countedFor,
*  balanceSkipCount, bookkept, ...) never reaches the browser. */
type WireSchedule = Pick<ISchedule, "id" | "user" | "name" | "prompt" | "agent_id" | "cadence" | "timezone" | "target" | "chatProjectId" | "file_ids" | "enabled" | "disabledReason" | "nextRunAt" | "lastRun" | "runCount" | "failureCount" | "configRevision" | "createdAt" | "updatedAt"> & {
  /** See `TSchedule.inFlight`: the generating occurrences, from their own run rows. */inFlight?: Array<{
    conversationId: string;
  }>;
};
/** Only generating occurrences are read for the list. `ScheduleRun` is indexed by
*  status, not by user, and `started` rows are bounded globally by the capacity
*  slots; `requires_action` rows accumulate for as long as their approvals wait. */
declare const LISTED_RUN_STATUSES: readonly IScheduleRun["status"][];
/**
* The chats a schedule's generating occurrences are producing. A reservation that
* has not been dispatched yet carries no conversation id, and there is nothing to
* look for until it does.
*/
declare function toWireInFlight(runs: readonly IScheduleRun[]): Array<{
  conversationId: string;
}> | undefined;
/**
* Public projection. `limits` is optional only for callers that have none to hand;
* pass it wherever one is available so the card shows the destination a fire would
* ACTUALLY use — an operator pin added after a row was written outranks its stored
* id at fire time, and a projection reading the raw field would name the wrong
* project until the owner next edits the schedule.
*/
declare function toWireSchedule(schedule: ISchedule, limits?: Pick<ScheduleLimits, "projectId">, inFlight?: readonly IScheduleRun[]): WireSchedule;
type ScheduleHandler = (req: ServerRequest, res: Response$2) => Promise<void>;
interface SchedulesHandlers {
  listSchedules: ScheduleHandler;
  getSchedule: ScheduleHandler;
  createSchedule: ScheduleHandler;
  updateSchedule: ScheduleHandler;
  deleteSchedule: ScheduleHandler;
  runScheduleNow: ScheduleHandler;
}
declare function createSchedulesHandlers(deps: SchedulesHandlersDeps): SchedulesHandlers;
//#endregion
//#region src/schedules/readiness.d.ts
declare const SCHEDULES_NOT_READY_CODE = "SCHEDULES_NOT_READY";
declare const SCHEDULES_UNAVAILABLE_CODE = "SCHEDULES_UNAVAILABLE";
/**
* Whether the schedule engine has been armed for this process.
*
* `starting` and `unavailable` both refuse writes, but they are not the same condition:
* arming is attempted EXACTLY ONCE at boot, so a failed arm is terminal for the life of
* the process. Collapsing the two into a single flag is what let a permanent outage be
* advertised with `Retry-After`.
*/
type ScheduleEngineState = "starting" | "armed" | "unavailable";
interface ScheduleWriteGateOptions {
  getState: () => ScheduleEngineState;
  /** `Retry-After` for the genuinely transient window only. */
  retryAfterSeconds: string;
}
type ScheduleWriteGate = (req: {
  method: string;
}, res: Response$2, next: NextFunction) => Response$2 | void;
/**
* Guards schedule writes on engine readiness, answering with the retry contract that
* matches the real state: retry while arming is still pending, and a terminal refusal once
* it has definitively failed — nothing re-attempts arming, so a client obeying
* `Retry-After` there would poll a condition that cannot change without operator action.
*/
declare function createScheduleWriteGate({
  getState,
  retryAfterSeconds
}: ScheduleWriteGateOptions): ScheduleWriteGate;
//#endregion
//#region src/schedules/trigger.d.ts
interface ScheduleTriggerRequest {
  _isAgentTrigger?: boolean;
  _isScheduledFire?: boolean;
  _isManualScheduledFire?: boolean;
  body?: Record<string, unknown>;
}
interface ScheduleFireContext {
  scheduleId: string;
  scheduledFor: string;
  manual: boolean;
  configRevision?: number;
}
/**
* Reads the schedule identity carried by the trusted trigger host. The generic
* trigger token is the trust boundary; ordinary chat requests cannot opt into
* schedule bookkeeping or limiter exemptions by copying these body fields.
*/
declare function readScheduleFireContext(req?: ScheduleTriggerRequest): ScheduleFireContext | undefined;
/**
* Captures the verified trigger classification once, then projects the minimum
* schedule fields expected by the existing generation lifecycle hooks.
*/
declare function captureScheduleFireContext(req: ScheduleTriggerRequest): ScheduleFireContext | undefined;
declare function isScheduleFireRequest(req?: ScheduleTriggerRequest): boolean;
/** Automatic occurrences are bounded by schedule cadence and global capacity. */
declare function exemptFromUserLimiter(req?: ScheduleTriggerRequest): boolean;
/** Acquire/release sites share this exact predicate to keep counters balanced. */
declare function exemptFromConcurrencyLimiter(req?: ScheduleTriggerRequest): boolean;
//#endregion
//#region src/schedules/mcp.d.ts
interface ScheduledTokenIdentity {
  readonly id: string;
  readonly tenantId?: string;
  readonly role?: string;
  readonly provider?: string;
  readonly openidId?: string;
  readonly openidIssuer?: string;
}
type HostUpstreamTokenProviderResolver = (user: ScheduledTokenIdentity, options: {
  signal?: AbortSignal;
  context?: ScheduledTokenContext;
  target?: UpstreamTokenTarget;
}) => ReturnType<UpstreamTokenProviderResolver>;
/** Bind a credential lookup to the trusted principal and owning run's cancellation. */
declare function bindUpstreamTokenProviderResolver(user: ScheduledTokenIdentity, resolve: HostUpstreamTokenProviderResolver | undefined, signal?: AbortSignal, context?: ScheduledTokenContext): UpstreamTokenProviderResolver | undefined;
declare function createScheduleUpstreamTokenProviderResolver(req: Parameters<typeof isScheduleFireRequest>[0] & {
  user: ScheduledTokenIdentity;
}, resolve: HostUpstreamTokenProviderResolver | undefined, signal?: AbortSignal, restoredContext?: ScheduledTokenContext): UpstreamTokenProviderResolver | undefined;
declare class ScheduleMCPError extends Error {
  readonly outcomes: ScheduleMCPOutcome[];
  readonly code: Exclude<ScheduleMCPStatus, "ready">;
  constructor(outcomes: ScheduleMCPOutcome[]);
}
/** One response discriminator for every schedule admission surface. */
declare function getScheduleMCPFailureCode(outcomes: ScheduleMCPOutcome[]): Exclude<ScheduleMCPStatus, "ready">;
interface ScheduleMCPDeps {
  resolveAgentGraphAccess: (access: {
    userId: string;
    role?: string | null;
    idOnTheSource?: string | null;
  }) => Promise<AgentGraphAccessContext>;
  getAgentGraphNodes: (ids: string[], access?: AgentGraphAccessContext) => Promise<AgentGraphNode[]>;
  getModelsConfig: (user: IUser) => Promise<TModelsConfig>;
  getRoleByName: CheckAccessParams["getRoleByName"];
  getUser: (id: string) => Promise<IUser | null>;
  getAppConfig: (options: GetAppConfigOptions) => Promise<AppConfig | undefined>;
  ensureConfigServers: (config: NonNullable<AppConfig["mcpConfig"]>, limit?: <T>(task: () => Promise<T>) => Promise<T>) => Promise<Record<string, ParsedServerConfig>>;
  getServerConfigs: (userId: string, config: Record<string, ParsedServerConfig>, role?: string) => Promise<Record<string, ParsedServerConfig>>;
  findPluginAuthsByKeys: PluginAuthMethods["findPluginAuthsByKeys"];
  resolveUpstreamTokenProvider?: HostUpstreamTokenProviderResolver;
  connect: (options: UserMCPConnectionOptions) => Promise<{
    fetchToolsSnapshot: (deadlineMs?: number, signal?: AbortSignal) => Promise<MCPToolsSnapshot>;
  }>;
}
/** Probes MCP readiness with isolated user connections and no interactive OAuth wait. */
declare function createScheduleMCPPreflight(deps: ScheduleMCPDeps): ScheduleMCPPreflight;
//#endregion
//#region src/schedules/service.d.ts
/** Recordable terminal/paused run outcome, as accepted by `recordRunOutcome`. */
type ScheduleRunOutcomeStatus = Parameters<ScheduleMethods["recordRunOutcome"]>[0]["status"];
interface RecordScheduleOutcomeInput {
  scheduleId?: string;
  scheduledFor?: string | Date;
  /** Exact generation whose terminal evidence is being persisted. */
  streamId?: string;
  jobCreatedAt?: number;
  status: ScheduleRunOutcomeStatus;
  conversationId?: string;
  checkpointNamespace?: string;
  /** Erase the row's reserved conversationId (pre-start abort: no conversation exists). */
  clearConversationId?: boolean;
  error?: string;
}
type ScheduleResumeClaimResult = {
  capacitySlot: number;
  claimToken: string;
  leaseBy: string;
} | {
  conflict: "capacity" | "overlap" | "not-paused" | "inactive";
};
/**
* Api-side dependencies the schedules service needs injected: model methods,
* config/balance access, and the owner-scoped agent access check. Everything
* else (job store and tenant context) lives in `@librechat/api` and is imported
* directly.
*/
interface SchedulesServiceDeps {
  preflightMCP: ScheduleMCPPreflight;
  methods: ScheduleMethods & {
    getRoleByName: (role?: string) => Promise<{
      permissions?: Record<string, Record<string, boolean | undefined>>;
    } | null>;
    getFiles: (filter: unknown, sort: unknown, select: unknown) => Promise<Array<{
      file_id: string;
      filepath: string;
      filename: string;
      type: string;
      height?: number;
      width?: number;
      source: string;
    }> | null>; /** Owner-scoped bounded TTL hold (`db.extendFilesTTL`-shaped). */
    extendFilesTTL: (fileIds: string[], hold: {
      renewMs: number;
      maxLifetimeMs: number;
    }, owner: {
      user: string;
      tenantId?: string | null;
    }) => Promise<number>;
  };
  getAppConfig: (options?: GetAppConfigOptions) => Promise<AppConfig | undefined>;
  findUserById: (userId: string | Types.ObjectId) => Promise<{
    _id: Types.ObjectId;
    tenantId?: string;
    role?: string;
  } | null>;
  /** Reads the balance record together with the credits unexpired in-flight reservations hold. */
  findBalance: (userId: string) => Promise<IBalance | null>;
  /**
  * Upserts a balance record. `setOnInsert` carries fields that must ONLY apply to a
  * document this call creates — chiefly the starting credit — so a record created by a
  * concurrent charge is never overwritten with a fresh balance.
  */
  upsertBalance: (userId: string, update: {
    set: Partial<BalanceUpdateFields>;
    setOnInsert: Partial<BalanceUpdateFields>;
  }) => Promise<IBalance | null>;
  /**
  * Compare-and-set initialization for an EXISTING balance whose `tokenCredits` is still
  * null. Writes the starting credit (and any refill-config sync) only while
  * `{ user, tokenCredits: null }` still matches — never an upsert — so a concurrent
  * initializer/charge that already set credits between the read and this write is not
  * clobbered. Returns the winning document, or `null` when the CAS did not match (the
  * caller re-reads the winner rather than restoring credits).
  */
  initializeNullBalance: (userId: string, update: {
    tokenCredits: number;
    sync: Partial<BalanceUpdateFields>;
  }) => Promise<IBalance | null>;
  resolveAgentFireAccess: (agentId: string, user: ScheduleUserContext) => Promise<"ok" | "missing" | "forbidden">;
  /** Loads a chat project scoped to its owner, or null when it does not exist for
  *  them. Chat projects are user-owned, so this is both the existence check and the
  *  authorization check. */
  getChatProject: (userId: string, projectId: string) => Promise<IChatProject | null>;
  /** Whether this user's account deletion has begun. Fail-closed (unknown == true). */
  isUserDeleting: (userId: string) => Promise<boolean>;
  /** Shared durable trigger admission from the merged agent-trigger service. */
  enqueueAgentTrigger: ScheduleEngineDeps["enqueueTrigger"];
  /** Reads a durable trigger delivery by key from the merged agent-trigger service, for
  *  reconciliation's live-vs-dead delivery check. */
  getTriggerDelivery: ScheduleEngineDeps["getTriggerDelivery"];
}
interface SchedulesService {
  getLimits: (user?: ScheduleUserContext) => Promise<ScheduleLimits>;
  engineDeps: ScheduleEngineDeps;
  fireScheduleNow: (schedule: FireableSchedule, limits: ScheduleLimits, options?: {
    signal?: AbortSignal;
  }) => Promise<FireResult | null>;
  recordScheduleOutcome: (input: RecordScheduleOutcomeInput) => Promise<boolean>;
  /**
  * Stamps a scheduled run's interactive Stop BEFORE the abort is signalled, so the owner
  * settlement barrier, reconciliation, and schedule/account deletion hold off settling or
  * erasing until {@link acknowledgeScheduledStopPersistence}. Serialized: `'in_progress'`
  * means a fresh Stop already owns the stamp and the caller must not signal a second abort;
  * `false` means there is no active run to stop.
  */
  beginScheduledStop: (input: {
    scheduleId: string;
    scheduledFor: string | Date;
  }) => Promise<boolean | "in_progress">;
  /**
  * Releases the Stop settlement barrier once the route's partial-message/checkpoint writes
  * have landed. Call ONLY after persistence succeeds — a failed persistence must leave the
  * barrier unresolved so the run stays preserved and recovers via the stale-owner timeout.
  */
  acknowledgeScheduledStopPersistence: (input: {
    scheduleId: string;
    scheduledFor: string | Date;
    /** Optional terminal outcome to re-drive once the barrier clears, so a settlement the
    *  owner deferred past its poll budget still converges where no reconciler is armed. */
    settle?: {
      status: ScheduleRunOutcomeStatus;
      conversationId?: string;
      error?: string;
    };
  }) => Promise<void>;
  /** Re-enters a paused occurrence into the DB-enforced global/same-schedule
  * capacity set before its approval job is allowed to resume. */
  claimScheduleResume: (scheduleId: string, scheduledFor: string | Date, options?: {
    expectedConfigRevision?: number;
    automatic?: boolean;
  }) => Promise<ScheduleResumeClaimResult>;
  /** Guarded rollback when the approval CAS did not consume the paused action. */
  releaseScheduleResumeClaim: (scheduleId: string, scheduledFor: string | Date, capacitySlot: number) => Promise<boolean>;
  /** Atomically validates the live schedule generation after the approval CAS and
  * releases its short-lived document fence. This is the resume linearization point. */
  finalizeScheduleResumeClaim: (scheduleId: string, claimToken: string, leaseBy: string, options?: {
    expectedConfigRevision?: number;
    automatic?: boolean;
  }) => Promise<boolean>;
  /** Releases only the schedule-document fence after an unconsumed/ambiguous approval CAS. */
  releaseScheduleResumeFence: (scheduleId: string, leaseBy: string) => Promise<void>;
  /**
  * Whether a schedule is still live (exists and not soft-deleted). The loopback
  * chat controller calls this right after creating the generation job to re-fence
  * a fire against a delete/quiesce that landed in the claim -> POST window (when
  * the reservation row exists but the job did not yet, so the deletion's abort
  * missed it) — aborting before any messages are persisted.
  */
  isScheduleLive: (scheduleId: string, expectedConfigRevision?: number, options?: {
    automatic?: boolean;
    policy?: boolean;
    scheduledFor?: string | Date;
  }) => Promise<boolean>;
  /** Soft-deletes an owner's schedule: stop claims, abort active runs, drain, erase. */
  deleteScheduleForOwner: (scheduleId: string, userId: string) => Promise<ScheduleDeleteResult>;
  /**
  * Quiesces all of a user's schedules ahead of account deletion (reversible suspension +
  * abort + drain). `token` identifies this deletion attempt and is what a later
  * {@link restoreUserSchedulesFromDeletion} restores against. Returns whether the drain was
  * CONFIRMED: false means at least one run could not be confirmed settled, and the caller
  * must NOT proceed to destructive deletion — the durable barrier keeps refusing new work
  * while a later pass finishes the cascade.
  */
  quiesceUserSchedules: (userId: string, token: string) => Promise<boolean>;
  /**
  * Reverses a quiesce whose account deletion was cancelled, re-enabling and re-arming only
  * the rows this exact attempt suspended. Safe to call even if quiesce never suspended a
  * row (no matching token → no-op).
  */
  restoreUserSchedulesFromDeletion: (userId: string, token: string) => Promise<void>;
  /** Arms the scheduler for THIS process. v1 is single-process only; the clustered
  *  entrypoint does not start it. Returns undefined when index creation failed, or when
  *  the topology cannot be shown safe (process-local job store with no single-process
  *  assertion) — see isTopologySafeToArm. */
  initializeScheduleEngine: () => Promise<ReturnType<typeof startScheduleEngine> | undefined>;
  /** Starts erasure-ONLY maintenance for an entrypoint that never arms the engine (the
  *  clustered worker). Idempotent per process and a no-op once the full engine is armed.
  *  Arms nothing else — no claims, firing, cadence advancement, or absence-based
  *  reconciliation — and refuses to infer owner death from a process-local missing job
  *  (isTopologySafeToArm gates that). See startScheduleErasureSweep. */
  initializeScheduleErasureSweep: () => void;
}
/** Test-only overrides for the service's bounded waits (drains, barriers). */
interface ScheduleServiceTimings {
  drainTimeoutMs?: number;
  drainPollMs?: number;
  stopBarrierTimeoutMs?: number;
  stopBarrierPollMs?: number;
}
/**
* Builds the scheduler service around api-side dependencies. Each call owns its
* own engine singleton and job-store-shared flag, so state never leaks between
* instances.
*/
declare function createSchedulesService(deps: SchedulesServiceDeps, timings?: ScheduleServiceTimings): SchedulesService;
//#endregion
//#region src/skills/binary.d.ts
/**
* Determine whether a buffer contains binary (non-text) content.
* Scans the complete bounded buffer so a printable prefix cannot hide
* opaque bytes later in the submitted content.
*/
declare function isBinaryBuffer(buffer: Buffer): boolean;
//#endregion
//#region src/skills/handlers.d.ts
/**
* All dependencies required to serve skill HTTP requests. Every dep is resolved
* from the legacy api layer (`~/models`, `PermissionService`) so the TS handlers
* stay pure — no direct imports of mongoose, no direct filesystem I/O.
*/
interface SkillsHandlersDeps {
  /** Skill CRUD — from `@librechat/data-schemas` `createMethods` output. */
  createSkill: (data: CreateSkillInput) => Promise<CreateSkillResult>;
  getSkillById: (id: string | Types.ObjectId) => Promise<(ISkill & {
    _id: Types.ObjectId;
  }) | null>;
  listSkillsByAccess: (params: ListSkillsByAccessParams) => Promise<ListSkillsByAccessResult>;
  updateSkill: (params: {
    id: string;
    expectedVersion: number;
    update: UpdateSkillInput;
  }) => Promise<UpdateSkillResult>;
  deleteSkill: (id: string) => Promise<{
    deleted: boolean;
  }>;
  listSkillFiles: (skillId: string | Types.ObjectId) => Promise<Array<ISkillFile & {
    _id: Types.ObjectId;
  }>>;
  deleteSkillFile: (skillId: string | Types.ObjectId, relativePath: string) => Promise<{
    deleted: boolean;
  }>;
  /** Access-control primitives from PermissionService. */
  findAccessibleResources: (params: {
    userId: string;
    role?: string | null;
    resourceType: string;
    requiredPermissions: number;
  }) => Promise<Types.ObjectId[]>;
  findPubliclyAccessibleResources: (params: {
    resourceType: string;
    requiredPermissions: number;
  }) => Promise<Types.ObjectId[]>;
  hasPublicPermission: (params: {
    resourceType: string;
    resourceId: string | Types.ObjectId;
    requiredPermissions: number;
  }) => Promise<boolean>;
  grantPermission: (params: {
    principalType: string;
    principalId: string | Types.ObjectId;
    resourceType: string;
    resourceId: string | Types.ObjectId;
    accessRoleId: string;
    grantedBy: string | Types.ObjectId;
  }) => Promise<unknown>;
  /** Single-file lookup + cache update for the download handler. */
  getSkillFileByPath: (skillId: string | Types.ObjectId, relativePath: string) => Promise<(ISkillFile & {
    _id: Types.ObjectId;
    content?: string;
    isBinary?: boolean;
  }) | null>;
  updateSkillFileContent: (skillId: string | Types.ObjectId, relativePath: string, update: {
    content?: string;
    isBinary?: boolean;
  }) => Promise<void>;
  /** Storage strategy resolver — returns stream/URL helpers keyed by source. */
  getStrategyFunctions: (source: string) => Partial<StrategyFunctions>;
  /** ObjectId validation helper from data-schemas. */
  isValidObjectIdString: (value: unknown) => boolean;
}
type SkillResponseOptions = {
  includePublicStatus?: boolean;
};
type SkillListOptions = Pick<ListSkillsByAccessParams, "manageTenantId" | "limit" | "cursor">;
/**
* Factory for the typed Express handlers served at `/api/skills`.
* The legacy `api/server/routes/skills.js` imports this, passes in concrete
* deps from `~/models` + `PermissionService`, and wires the returned handlers
* onto the Express router.
*/
declare function createSkillsHandlers(deps: SkillsHandlersDeps): {
  list: (req: ServerRequest, res: Response$2, options?: SkillListOptions) => Promise<Response$2>;
  create: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  get: (req: ServerRequest, res: Response$2, options?: SkillResponseOptions) => Promise<Response$2>;
  patch: (req: ServerRequest, res: Response$2, options?: SkillResponseOptions) => Promise<Response$2>;
  delete: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  listFiles: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  downloadFile: (req: ServerRequest, res: Response$2) => Promise<Response$2 | undefined>;
  deleteFile: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
};
type SkillsHandlers = ReturnType<typeof createSkillsHandlers>;
//#endregion
//#region src/skills/limits.d.ts
type ImportLimits = {
  maxZipBytes: number;
  maxDecompressedBytes: number; /** Caps aggregate decompression and synchronous pattern scans during filtered preflight. */
  maxContentInspectionBytes: number;
  maxEntries: number;
  maxSingleFileBytes: number;
};
declare const DEFAULT_SKILL_IMPORT_LIMITS: ImportLimits;
//#endregion
//#region src/skills/import.d.ts
/**
* YAML frontmatter parser — extracts the first-class fields TerraMind
* persists as columns (`name`, `description`, `alwaysApply`) out of a
* SKILL.md file. Intentionally narrow: the full frontmatter validator in
* `packages/data-schemas/src/methods/skill.ts` covers the wire contract;
* this parser only needs to hand `createSkill` the columns it populates.
*
* When a known boolean field (currently `always-apply` plus the accepted
* `alwaysApply` alias) is present
* with a value that isn't recognizable as `true`/`false`, the parser
* records it on `invalidBooleans[]` so the import handler can surface
* a 400 instead of silently dropping the flag. Without this signal,
* authoring mistakes like `alwaysApply: yes` would be lossy-converted
* to "not always-applied" and the user would never learn their
* frontmatter was malformed.
*
* Exported for unit testing only — prefer `createImportHandler` at runtime.
*/
declare function parseFrontmatter(raw: string): {
  name: string;
  description: string;
  alwaysApply?: boolean; /** Keys that carried non-boolean values for fields that must be boolean. */
  invalidBooleans: string[];
  parseError?: string;
};
interface ImportSkillDeps {
  limits?: Partial<ImportLimits> | ((req: ServerRequest$1) => Partial<ImportLimits> | undefined);
  createSkill: (data: CreateSkillInput) => Promise<CreateSkillResult>;
  getSkillById: (id: string | Types.ObjectId) => Promise<(ISkill & {
    _id: Types.ObjectId;
  }) | null>;
  deleteSkill: (id: string) => Promise<{
    deleted: boolean;
  }>;
  upsertSkillFile: (row: UpsertSkillFileInput) => Promise<ISkillFile & {
    _id: Types.ObjectId;
  }>;
  saveBuffer: (req: Request, params: {
    userId: string;
    buffer: Buffer;
    fileName: string;
    basePath?: string;
    isImage?: boolean;
    tenantId?: string;
  }) => Promise<{
    filepath: string;
    source: string;
    storageKey?: string;
    storageRegion?: string;
  }>;
  deleteFile?: (req: Request, file: {
    filepath: string;
    source: string;
    [key: string]: unknown;
  }) => Promise<void>;
  grantPermission: (params: {
    principalType: string;
    principalId: string;
    resourceType: string;
    resourceId: Types.ObjectId;
    accessRoleId: string;
    grantedBy: string;
  }) => Promise<unknown>;
}
interface ServerRequest$1 extends Request {
  tenantId?: string;
  config?: AppConfig;
  user: {
    id: string;
    _id: Types.ObjectId;
    name?: string;
    username?: string;
    tenantId?: string;
  };
  file?: Express.Multer.File;
}
/**
* `POST /api/skills/import`
*
* Accepts a single multipart file (.md, .zip, or .skill).
* Creates the skill, then processes additional files individually.
* Grants SKILL_OWNER to the uploader.
*/
declare function createImportHandler(deps: ImportSkillDeps): (req: ServerRequest$1, res: Response$2) => Promise<Response$2>;
declare function isTextLikeSkillFile(filename: string, mimeType?: string): boolean;
declare function guessMimeType(filename: string): string;
//#endregion
//#region src/skills/path.d.ts
/**
* Skill file paths are stored and matched verbatim (e.g. `references/guide.md`).
* A path is safe when it has no absolute prefix, no `.`/`..`/empty segments, and
* uses only the restricted character set skills are imported with. Single source
* of truth shared by import, upload, and the file-serving routes so read, write,
* and delete stay in lockstep.
*/
declare function isSafeSkillFilePath(p: string): boolean;
/**
* Reconstruct a skill file path from an Express route param. Express 5 splat
* params (`*relativePath`) arrive as an array of already-decoded segments, while
* a single named param arrives as a string. Wildcard routing lets nested files
* resolve whether the client sends an encoded `%2F` or a proxy has already
* decoded it to a literal slash before the request reaches Node. Returns `null`
* for empty or traversal-unsafe paths.
*/
declare function resolveSkillFilePathParam(param: string | string[] | undefined): string | null;
//#endregion
//#region src/skills/protection.d.ts
interface SkillFileContentPolicyInput {
  readonly buffer: Buffer;
  readonly originalName: string;
  readonly relativePath: string;
}
/** Enforces both skill-file and canonical file policy over one uploaded buffer. */
declare function assertSkillFileContentAllowed(filters: FiltersConfig | undefined, input: SkillFileContentPolicyInput): void;
/** Express adapter shared by live skill-file mutation routes. */
declare function blockFilteredSkillFile(filters: FiltersConfig | undefined, res: Response$2, input: SkillFileContentPolicyInput): boolean;
//#endregion
//#region src/skills/parse.d.ts
type ParsedSkillMarkdown = {
  name: string;
  description: string;
  alwaysApply?: boolean;
  frontmatter?: Record<string, unknown>;
  invalidBooleans: string[];
  parseError?: string;
};
declare function parseSkillMarkdown(raw: string): ParsedSkillMarkdown;
//#endregion
//#region src/skills/skillStates.d.ts
/** Hard cap on explicit override entries stored on a user document. */
declare const MAX_SKILL_STATES = 200;
/** Max length of a skill-ID map key (matches ObjectId hex length with slack). */
declare const MAX_KEY_LENGTH = 64;
/**
* Generous upper bound on raw payload size to reject abusive inputs before
* we spend cycles validating or querying the DB for orphan cleanup.
*/
declare const MAX_RAW_PAYLOAD: number;
/** Map of skillId → explicit active state override. */
type SkillStatesRecord = Record<string, boolean>;
/**
* Converts a Mongoose Map (non-lean) or plain object (lean) into a
* `SkillStatesRecord`. Returns `{}` for any other shape.
*/
declare function toSkillStatesRecord(raw: Map<string, boolean> | Record<string, boolean> | null | undefined): SkillStatesRecord;
/**
* Reads `defaultActiveOnShare` out of the `interface.skills` config shape.
* The shape is a Zod union (boolean | object) so we handle both gracefully.
*/
declare function resolveDefaultActiveOnShare(skillsConfig: unknown): boolean;
/** Return shape from `loadSkillStates`. */
interface LoadedSkillStates {
  skillStates: SkillStatesRecord;
  defaultActiveOnShare: boolean;
}
interface LoadSkillStatesParams {
  userId: string;
  appConfig?: {
    interfaceConfig?: {
      skills?: unknown;
    };
  } | null;
  getUserById: (id: string, select: string) => Promise<{
    skillStates?: Map<string, boolean> | Record<string, boolean>;
  } | null | undefined>;
  /**
  * When provided and empty, the helper short-circuits to defaults without
  * issuing the user query. Lets every agent entry point skip the DB round
  * trip when no skills are accessible, without re-implementing the guard.
  */
  accessibleSkillIds?: ArrayLike<unknown>;
}
/**
* Loads a user's `skillStates` overrides and the admin-configured
* `defaultActiveOnShare` in one call. Used by every agent entry point so the
* same loading block is not duplicated across controllers.
*/
declare function loadSkillStates(params: LoadSkillStatesParams): Promise<LoadedSkillStates>;
interface SkillStatesValidationError {
  code?: string;
  message: string;
  limit?: number;
}
/**
* Validates a raw skill-states update payload. Returns `null` on success or
* a structured `SkillStatesValidationError` describing the first rejection
* reason. Caller maps the error to an HTTP 400 response.
*
* Rejects: non-object payloads, oversize payloads (sanity bound for abuse),
* non-string/empty/oversize keys, keys that are not valid ObjectIds, and
* non-boolean values. The strict 200-entry cap is enforced *after* orphan
* pruning, not here, so stale-client payloads near the cap do not get a
* false-positive rejection.
*/
declare function validateSkillStatesPayload(payload: unknown): SkillStatesValidationError | null;
interface PruneOrphansDeps {
  /** Returns the subset of the given IDs that exist as Skill documents. */
  findExistingSkillIds: (validIds: string[]) => Promise<string[]>;
  /** Returns IDs the current user has VIEW access to via ACL. */
  findAccessibleSkillIds: () => Promise<Array<Types.ObjectId | string>>;
}
/**
* Returns a copy of `skillStates` containing only entries that: are valid
* ObjectIds, point to a Skill that currently exists, AND the user still has
* VIEW access to. Self-heals three classes of orphan without cascade logic:
* malformed keys, deleted skills, and revoked shares.
*
* Deps are injected so the pure logic can be tested without Mongoose or ACL
* wiring. Callers in `/api` adapt their live model + permission service.
*/
declare function pruneOrphanSkillStates(skillStates: SkillStatesRecord, deps: PruneOrphansDeps): Promise<SkillStatesRecord>;
//#endregion
//#region src/skills/deployment.d.ts
declare const DEPLOYMENT_SKILLS_DIR_ENV = "DEPLOYMENT_SKILLS_DIR";
declare const DEFAULT_DEPLOYMENT_SKILLS_DIR = "skill";
declare const DEPLOYMENT_SKILL_SOURCE = "deployment";
declare const DEPLOYMENT_SKILL_FILE_SOURCE = "deployment";
type SkillId = Types.ObjectId | string;
type DeploymentSkillFile = {
  _id: Types.ObjectId;
  skillId: Types.ObjectId;
  relativePath: string;
  file_id: string;
  filename: string;
  filepath: string;
  source: typeof DEPLOYMENT_SKILL_FILE_SOURCE;
  mimeType: string;
  bytes: number;
  category: "script" | "reference" | "asset" | "other";
  isExecutable: boolean;
  author: Types.ObjectId;
  content?: string;
  isBinary?: boolean;
  codeEnvRef?: CodeEnvRef;
  codeEnvRefs?: CodeEnvRefMap;
  createdAt: Date;
  updatedAt: Date;
};
type DeploymentSkill = {
  _id: Types.ObjectId;
  name: string;
  displayTitle?: string;
  description: string;
  body: string;
  frontmatter: Record<string, unknown>;
  category: string;
  disableModelInvocation?: boolean;
  userInvocable?: boolean;
  allowedTools?: string[];
  author: Types.ObjectId;
  authorName: string;
  version: number;
  source: typeof DEPLOYMENT_SKILL_SOURCE;
  sourceMetadata: {
    deployment: true;
    directory: string;
    plugin?: string;
  };
  fileCount: number;
  alwaysApply: boolean;
  isPublic: true;
  deployment: true;
  files: DeploymentSkillFile[];
  createdAt: Date;
  updatedAt: Date;
};
type SkillLookupOptions = {
  preferUserInvocable?: boolean;
  preferModelInvocable?: boolean;
};
type SkillSummaryRow = {
  _id: Types.ObjectId;
  name: string;
  displayTitle?: string;
  description: string;
  category?: string;
  disableModelInvocation?: boolean;
  userInvocable?: boolean;
  allowedTools?: string[];
  author: Types.ObjectId;
  authorName?: string;
  version?: number;
  source?: string;
  sourceMetadata?: Record<string, unknown>;
  fileCount?: number;
  alwaysApply?: boolean;
  isPublic?: boolean;
  tenantId?: string;
  deployment?: boolean;
  createdAt?: Date;
  updatedAt?: Date;
};
type SkillDetailRow = SkillSummaryRow & {
  body: string;
  frontmatter?: Record<string, unknown>;
  version: number;
  fileCount: number;
};
type AlwaysApplySkillRow = {
  _id: Types.ObjectId;
  name: string;
  body: string;
  author: Types.ObjectId | string;
  allowedTools?: string[];
  deployment?: boolean;
  updatedAt?: Date;
};
type ListSkillsByAccessParams$1 = {
  accessibleIds: Types.ObjectId[];
  category?: string;
  search?: string;
  limit: number;
  cursor?: string | null;
};
type ListSkillsByAccessResult$1 = {
  skills: SkillSummaryRow[];
  has_more?: boolean;
  after?: string | null;
};
type ListAlwaysApplyParams = {
  accessibleIds: Types.ObjectId[];
  limit: number;
  cursor?: string | null;
};
type ListAlwaysApplyResult = {
  skills: AlwaysApplySkillRow[];
  has_more?: boolean;
  after?: string | null;
};
type SkillFileRow = Omit<DeploymentSkillFile, "codeEnvRef" | "codeEnvRefs" | "content" | "isBinary"> & {
  storageKey?: string;
  storageRegion?: string;
  tenantId?: string;
};
type SkillFileContentRow = SkillFileRow & {
  codeEnvRef?: CodeEnvRef;
  codeEnvRefs?: CodeEnvRefMap;
  content?: string;
  isBinary?: boolean;
};
type DeploymentSkillBaseMethods = {
  getSkillById?: (id: SkillId) => Promise<SkillDetailRow | null>;
  getSkillByName?: (name: string, accessibleIds: Types.ObjectId[], options?: SkillLookupOptions) => Promise<SkillDetailRow | null>;
  listSkillsByAccess?: (params: ListSkillsByAccessParams$1) => Promise<ListSkillsByAccessResult$1>;
  listAlwaysApplySkills?: (params: ListAlwaysApplyParams) => Promise<ListAlwaysApplyResult>;
  listSkillFiles?: (skillId: SkillId) => Promise<SkillFileRow[]>;
  getSkillFileByPath?: (skillId: SkillId, relativePath: string) => Promise<SkillFileContentRow | null>;
  updateSkillFileContent?: (skillId: SkillId, relativePath: string, update: {
    content?: string;
    isBinary?: boolean;
  }) => Promise<void>;
  updateSkillFileCodeEnvIds?: (updates: Array<{
    skillId: SkillId;
    relativePath: string;
    codeEnvRef: CodeEnvRef;
  }>) => Promise<{
    matchedCount: number;
    modifiedCount: number;
  } | void>;
};
type LoadDeploymentSkillsOptions = {
  projectRoot?: string;
  env?: NodeJS.ProcessEnv; /** Skills contributed by Agent Plugins packages, which yield to the deployment directory on a name conflict. */
  additionalSkills?: DeploymentSkill[];
};
type DirectoryResolution$1 = {
  directory: string;
  explicitlyConfigured: boolean;
};
type LoadedSkillDirectory = {
  directory: string;
  relativeDirectory: string;
};
type SkillIdentity = {
  /** Namespace for the deterministic skill id; keeps same-named skills from different sources distinct. */idNamespace?: string; /** Agent Plugins package that contributed the skill, when it came from one. */
  plugin?: string;
};
declare class DeploymentSkillRegistry {
  private readonly directory;
  private readonly skillsById;
  private readonly skillsByName;
  private readonly filesByPath;
  constructor(directory: string | null, skills: DeploymentSkill[]);
  getDirectory(): string | null;
  list(): DeploymentSkill[];
  ids(): Types.ObjectId[];
  hasId(id: SkillId | undefined): boolean;
  getById(id: SkillId): DeploymentSkill | null;
  getByName(name: string, accessibleIds: Types.ObjectId[], _options?: SkillLookupOptions): DeploymentSkill | null;
  namesByAccess(accessibleIds: Types.ObjectId[]): Set<string>;
  listByAccess(params: ListSkillsByAccessParams$1): DeploymentSkill[];
  listAlwaysApply(params: ListAlwaysApplyParams): DeploymentSkill[];
  listFiles(skillId: SkillId): DeploymentSkillFile[] | null;
  getFileByPath(skillId: SkillId, relativePath: string): DeploymentSkillFile | null;
  hasFilePath(filepath: string): boolean;
  updateFileCodeEnvRefs(updates: Array<{
    skillId: SkillId;
    relativePath: string;
    codeEnvRef: CodeEnvRef;
  }>): Array<{
    skillId: SkillId;
    relativePath: string;
    codeEnvRef: CodeEnvRef;
  }>;
}
declare function getDeploymentSkillRegistry(): DeploymentSkillRegistry;
declare function getDeploymentSkillIds(): Types.ObjectId[];
declare function mergeDeploymentSkillIds(ids: Array<SkillId>): Types.ObjectId[];
declare function isDeploymentSkillId(id: SkillId | undefined): boolean;
declare function isDeploymentSkillFileSource(source: unknown): boolean;
declare function getDeploymentSkillById(id: SkillId): DeploymentSkill | null;
declare function isDeploymentSkillFilePath(filepath: string): boolean;
declare function getDeploymentSkillDownloadStream(filepath: string): Promise<NodeJS.ReadableStream>;
declare function updateDeploymentSkillFileCodeEnvRefs(updates: Array<{
  skillId: SkillId;
  relativePath: string;
  codeEnvRef: CodeEnvRef;
}>): Array<{
  skillId: SkillId;
  relativePath: string;
  codeEnvRef: CodeEnvRef;
}>;
declare function resolveDeploymentSkillDirectory(options?: LoadDeploymentSkillsOptions): DirectoryResolution$1;
declare function initializeDeploymentSkills(options?: LoadDeploymentSkillsOptions): Promise<DeploymentSkillRegistry>;
declare function loadDeploymentSkillsFromDirectory(directory: string, options?: {
  projectRoot?: string;
  explicitlyConfigured?: boolean;
  additionalSkills?: DeploymentSkill[];
}): Promise<DeploymentSkillRegistry>;
declare function createDeploymentSkillMethods<T extends DeploymentSkillBaseMethods>(base: T): T & Required<Pick<DeploymentSkillBaseMethods, "getSkillById">>;
/**
* Loads one `SKILL.md` directory into a deployment skill. Shared by the
* deployment skills directory and Agent Plugins packages, which differ only in
* how the skill is identified and attributed.
*/
declare function loadSkillFromDirectory(skillDirectory: LoadedSkillDirectory, rootDirectory: string, identity?: SkillIdentity): Promise<DeploymentSkill>;
//#endregion
//#region src/skills/management.d.ts
type SkillManagementUpdate = TUpdateSkillPayload & {
  expectedVersion: number;
};
declare const skillManagementUpdateSchema: z.ZodType<SkillManagementUpdate>;
type SkillManagementResponse = Pick<TSkill, "name" | "displayTitle" | "description" | "category" | "alwaysApply" | "version" | "fileCount" | "disableModelInvocation" | "userInvocable" | "allowedTools" | "createdAt" | "updatedAt" | "body" | "frontmatter"> & {
  id: string;
};
declare const skillManagementResponseSchema: z.ZodType<SkillManagementResponse>;
interface SkillManagementDeps {
  handlers: SkillsHandlers;
  beforeList?: (req: ServerRequest) => Promise<void>;
  fileWriteLimiters?: RequestHandler[];
  getSkillById: SkillsHandlersDeps["getSkillById"];
  getRoleByName: AgentManagementReadDeps["getRoleByName"];
  checkPermission: AgentManagementReadDeps["checkPermission"];
  hasCapability: AgentManagementReadDeps["hasCapability"];
  saveFile: (input: {
    req: ServerRequest;
    skillId: string;
    relativePath: string;
    content: string;
    mimeType: string;
  }) => Promise<{
    relativePath: string;
    bytes: number;
  }>;
}
/** Machine API adapters reuse the browser Skills behavior after principal and resource checks. */
declare function createSkillManagementHandlers(deps: SkillManagementDeps): Record<"list" | "get" | "update" | "listFiles" | "getFile" | "updateFile", (request: Request, res: Response$2) => Promise<Response$2>>;
//#endregion
//#region src/favorites/handlers.d.ts
interface ToolFavoritesHandlersDeps {
  /** Tool favorite CRUD — from `@librechat/data-schemas` `createMethods` output. */
  getToolFavorites: (userId: string) => Promise<IToolFavoriteLean[]>;
  addToolFavorite: (params: {
    userId: string;
    itemType: FavoriteItemType;
    itemId: string;
  }) => Promise<AddToolFavoriteResult>;
  removeToolFavorite: (params: {
    userId: string;
    itemType: FavoriteItemType;
    itemId: string;
  }) => Promise<RemoveToolFavoriteResult>;
}
declare function createToolFavoritesHandlers(deps: ToolFavoritesHandlersDeps): {
  listToolFavorites: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  addToolFavorite: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  removeToolFavorite: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
};
//#endregion
//#region src/favorites/pinned.d.ts
interface PinnedOrderHandlersDeps {
  /** User read/write — from `@librechat/data-schemas` `createMethods` output. */
  getUserById: (userId: string, fieldsToSelect?: string) => Promise<IUser | null>;
  updateUser: (userId: string, updateData: Partial<IUser>) => Promise<IUser | null>;
  invalidateCachedAuthUserDoc: (userId: string) => Promise<void>;
}
/** Combined display order for the sidebar's Pinned section. Keys interleave
*  favorites and pinned chats (`agent:`, `spec:`, `model:`, `convo:`), so the
*  two systems keep their own membership while sharing one ordering. */
declare function createPinnedOrderHandlers(deps: PinnedOrderHandlersDeps): {
  getPinnedOrder: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
  updatePinnedOrder: (req: ServerRequest, res: Response$2) => Promise<Response$2>;
};
//#endregion
//#region src/user/preferences.d.ts
interface UserPreferencesBody {
  statefulCodeEnvironment?: string;
}
type UserPreferencesRequest = Omit<ServerRequest, "body" | "user"> & {
  body: UserPreferencesBody;
  user?: IUser;
};
interface UserPreferencesHandlerDeps {
  updateStatefulCodeEnvironment: (userId: string, environment: StatefulCodeEnvironment) => Promise<IUser | null>;
}
declare function createUserPreferencesHandler(deps: UserPreferencesHandlerDeps): (req: UserPreferencesRequest, res: Response$2) => Promise<Response$2>;
//#endregion
//#region src/plugins/constants.d.ts
/** Agent Plugins specification version implemented by this client. */
declare const AGENT_PLUGINS_VERSION = "1.0.0";
declare const PLUGIN_MANIFEST_SCHEMA_ID = "https://agent-plugins.org/schemas/1.0.0/plugin.schema.json";
declare const PLUGIN_MCP_SCHEMA_ID = "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json";
declare const PLUGIN_MANIFEST_FILE = "plugin.json";
declare const PLUGIN_MCP_FILE = "mcp.json";
declare const PLUGIN_SKILLS_DIR = "skills";
declare const SKILL_MANIFEST_FILE = "SKILL.md";
/**
* TerraMind's reverse-domain extension namespace. Owns both the `extensions`
* manifest key and the top-level extension directory of the same name.
*/
declare const LIBRECHAT_EXTENSION_NAMESPACE = "ai.librechat";
declare const EXTENSION_HOOKS_FILE = "hooks/hooks.json";
declare const PLUGIN_ROOT_VAR = "PLUGIN_ROOT";
declare const PLUGIN_DATA_VAR = "PLUGIN_DATA";
declare const DEPLOYMENT_PLUGIN_HOOKS_ENV = "DEPLOYMENT_PLUGIN_HOOKS";
declare const DEPLOYMENT_PLUGINS_DIR_ENV = "DEPLOYMENT_PLUGINS_DIR";
declare const DEFAULT_DEPLOYMENT_PLUGINS_DIR = "plugin";
declare const DEPLOYMENT_PLUGIN_DATA_DIR_ENV = "DEPLOYMENT_PLUGIN_DATA_DIR";
declare const DEFAULT_DEPLOYMENT_PLUGIN_DATA_DIR = "data/plugins";
declare const MAX_PLUGIN_NAME_LENGTH = 64;
//#endregion
//#region src/plugins/types.d.ts
type PluginDiagnosticSeverity = "error" | "warning";
type PluginDiagnosticCode = "manifest_missing" | "manifest_unreadable" | "manifest_invalid_json" | "manifest_invalid" | "manifest_unknown_field" | "manifest_unsupported_version" | "manifest_name_conflict" | "data_directory_unavailable" | "extensions_invalid" | "path_escape" | "component_location_invalid" | "skill_invalid" | "mcp_unreadable" | "mcp_invalid_json" | "mcp_invalid" | "mcp_version_mismatch" | "mcp_server_invalid" | "mcp_transport_unsupported" | "hooks_invalid" | "hooks_unsupported";
interface PluginDiagnostic {
  code: PluginDiagnosticCode;
  severity: PluginDiagnosticSeverity;
  message: string;
  /** Plugin-relative location the diagnostic refers to, when one applies. */
  location?: string;
}
/** Contents of one client extension namespace; JSON that this client does not validate (§8.1). */
type PluginExtensionData = Record<string, JsonValue>;
interface PluginAuthor {
  name?: string;
  email?: string;
  url?: string;
}
interface PluginManifest {
  $schema: string;
  name: string;
  version?: string;
  description?: string;
  author?: PluginAuthor;
  homepage?: string;
  repository?: string;
  license?: string;
  keywords?: string[];
  extensions?: Record<string, PluginExtensionData>;
}
/**
* Plugin MCP options always carry their provenance so the connection layer can
* tell them apart from operator-authored config and leave every placeholder the
* plugin declared literal.
*/
type PluginMcpOptions = MCPOptions$1 & {
  source: typeof MCP_PLUGIN_SOURCE;
};
interface PluginMcpServer {
  /** Server name as declared in the plugin's `mcpServers` object. */
  name: string;
  options: PluginMcpOptions;
}
/** Hooks contributed through the `ai.librechat` extension directory. */
interface PluginHookContribution {
  plan: PluginHookPlan;
  /** Parsed source document, re-planned at registration so plan and runtime never drift. */
  document: PluginHooksDocument;
  location: string;
}
interface LoadedPlugin {
  /** Filesystem-resolved plugin root. */
  root: string;
  /** Client-managed persistent data directory supplied to plugin subprocesses. */
  dataDirectory: string;
  manifest: PluginManifest;
  skills: DeploymentSkill[];
  mcpServers: PluginMcpServer[];
  hooks?: PluginHookContribution;
  diagnostics: PluginDiagnostic[];
}
type PluginLoadResult = {
  status: "loaded";
  plugin: LoadedPlugin;
} | {
  status: "rejected";
  root: string;
  diagnostics: PluginDiagnostic[];
};
//#endregion
//#region src/plugins/paths.d.ts
/**
* Agent Plugins §4.1: a configuration field defined as a plugin-relative path
* MUST begin with `./`. Bare and parent-relative forms are rejected verbatim
* rather than normalized, so `data` and `../bin` never resolve.
*/
declare function isPluginRelativePath(value: string): boolean;
/**
* Resolves `target` through any symlinked ancestors, tolerating a path whose
* leaf does not exist yet. Containment must be judged against the realpath of
* the deepest existing ancestor so a symlinked parent cannot smuggle a
* not-yet-created child outside the plugin root.
*/
declare function realpathAllowingMissing(target: string): Promise<string>;
/** True when `target` is the root itself or sits beneath it. */
declare function isWithinRoot(root: string, target: string): boolean;
/**
* Resolves a package path against the filesystem-resolved plugin root and
* enforces Agent Plugins §4.1 containment. Returns `null` when the resolved
* path escapes the root; callers map that to the narrowest applicable failure
* boundary for the component they are loading.
*/
declare function resolveWithinRoot(realRoot: string, relativePath: string): Promise<string | null>;
//#endregion
//#region src/plugins/manifest.d.ts
type ManifestResult = {
  status: "ok";
  manifest: PluginManifest;
  diagnostics: PluginDiagnostic[];
} | {
  status: "rejected";
  diagnostics: PluginDiagnostic[];
};
/**
* Validates a parsed `plugin.json` against the closed Agent Plugins manifest
* schema. Unknown top-level fields and a non-object `extensions` field are
* reported and ignored; every other violation rejects the plugin.
*/
declare function validateManifest(document: unknown): ManifestResult;
//#endregion
//#region src/plugins/mcp.d.ts
interface PluginMcpContext {
  /** Filesystem-resolved plugin root. */
  realRoot: string;
  /** Filesystem-resolved persistent data directory for this plugin instance. */
  dataDirectory: string;
  /** Agent Plugins version declared by `plugin.json`. */
  declaredVersion: string;
}
interface PluginMcpResult {
  servers: PluginMcpServer[];
  diagnostics: PluginDiagnostic[];
}
/**
* Agent Plugins §9.2: one non-recursive pass replacing every exact occurrence.
* A function replacement keeps `$`-bearing paths literal and prevents text
* introduced by a replacement from being rescanned.
*/
declare function expandPluginVariables(value: string, root: string, data: string): string;
declare function schemaVersion(schemaId: string): string | undefined;
/**
* Validates a parsed `mcp.json` and maps each conforming server onto TerraMind
* MCP options. A malformed document disables MCP for the plugin; a malformed
* entry skips only that server (§7.2.2).
*/
declare function readMcpConfig(document: unknown, context: PluginMcpContext): Promise<PluginMcpResult>;
//#endregion
//#region src/plugins/skills.d.ts
interface PluginSkillsResult {
  skills: DeploymentSkill[];
  diagnostics: PluginDiagnostic[];
}
/**
* Agent Plugins §7.1: each immediate child of `skills/` holding a regular
* `SKILL.md` is one skill. Deeper descendants are never searched, and a skill
* that fails validation is skipped rather than failing the plugin.
*/
declare function loadPluginSkills(realRoot: string, pluginName: string): Promise<PluginSkillsResult>;
//#endregion
//#region src/plugins/hooks.d.ts
interface PluginHooksResult {
  hooks?: PluginHookContribution;
  diagnostics: PluginDiagnostic[];
}
/**
* Reports a package that declares hooks when the host has registered no hook
* capabilities (execution not opted into via `DEPLOYMENT_PLUGIN_HOOKS`).
* Silently ignoring the document would leave an operator believing it runs.
*/
declare function reportUnexecutedHooks(realRoot: string): Promise<PluginDiagnostic[]>;
/**
* Hooks are outside the portable Agent Plugins v1 format, so TerraMind reads
* them from its own extension directory (§8.2). Absence is normal; a malformed
* document disables only this plugin's hooks.
*/
declare function loadPluginHooks(realRoot: string, capabilities: PluginHookCapabilities): Promise<PluginHooksResult>;
//#endregion
//#region src/plugins/load.d.ts
interface LoadPluginOptions {
  /** Root under which each plugin's persistent `PLUGIN_DATA` directory is created. */
  dataRoot: string;
  hookCapabilities?: PluginHookCapabilities;
}
/**
* Loads one Agent Plugins package. A manifest failure rejects the plugin;
* every component failure below it is isolated so independently valid
* components still load (§11.3).
*/
declare function loadPlugin(root: string, options: LoadPluginOptions): Promise<PluginLoadResult>;
//#endregion
//#region src/plugins/once.d.ts
/**
* Owner of cross-run "fired once" state for SessionStart and `once: true`
* plugin hooks, keyed by conversation scope. Naming the owner as a seam lets
* `/api` substitute a shared conversation-scoped store (e.g. the Redis/keyv
* cache layer) so once-state survives replicas; the default in-memory store
* is per-process by design — a multi-replica deployment over-fires rather
* than ever dropping a hook.
*/
interface PluginHookOnceStore {
  /** Marks the scope active, refreshing its retention. */
  touch(scope: string): void | Promise<void>;
  /** Records the key within the scope; resolves true only the first time. */
  markOnce(scope: string, key: string): boolean | Promise<boolean>;
}
/**
* Bounded in-memory store that retains and evicts whole conversation scopes,
* least-recently-active first. Hook registration touches its scope on every
* run, so an active conversation keeps all of its once-state — including
* keys of handlers that match only rarely — and eviction under the capacity
* bound (which counts conversations, not keys) only reaches the
* conversations idle longest.
*/
declare function createMemoryOnceStore(capacity?: number): PluginHookOnceStore;
declare function setPluginHookOnceStore(next: PluginHookOnceStore | undefined): void;
declare function getPluginHookOnceStore(): PluginHookOnceStore;
//#endregion
//#region src/plugins/runtime.d.ts
/**
* Capabilities handed to plugin loading when the operator has opted in to
* hook execution via `DEPLOYMENT_PLUGIN_HOOKS`. Undefined (the default) keeps
* hook documents parsed-but-inert, with the existing "not executed" warning.
*/
declare function getDeploymentPluginHookCapabilities(env?: NodeJS.ProcessEnv): PluginHookCapabilities | undefined;
declare function hasDeploymentPluginHooks(): boolean;
/** Whether any executable deployment-plugin hook can return a tool decision. */
declare function hasDeploymentPluginToolApprovalHooks(toolNames?: readonly string[]): boolean;
interface RegisterDeploymentPluginHooksOptions {
  registry: HookRegistry;
  context?: PluginHookRuntimeContext;
  /**
  * Whether the run has a HITL approval surface (checkpointer + resume route).
  * Off by default: without it a plugin's `ask` decision is tightened to
  * `deny`, since an un-resumable interrupt would strand the run.
  */
  askDecisionSupported?: boolean;
}
/**
* Registers every loaded deployment plugin's ready hooks onto a run's hook
* registry. Called once per run from the run-construction seam; the registry
* (and with it every registration) is garbage-collected with the run.
* Returns the number of handlers registered.
*/
declare function registerDeploymentPluginHooks(options: RegisterDeploymentPluginHooksOptions): number;
//#endregion
//#region src/plugins/deployment.d.ts
interface DeploymentPluginOptions {
  projectRoot?: string;
  env?: NodeJS.ProcessEnv;
  hookCapabilities?: PluginHookCapabilities;
}
interface DirectoryResolution {
  directory: string;
  explicitlyConfigured: boolean;
}
declare function resolveDeploymentPluginDirectory(options?: DeploymentPluginOptions): DirectoryResolution;
declare function resolveDeploymentPluginDataDirectory(options?: DeploymentPluginOptions): string;
/**
* Holds the plugins installed by the operator. Names are unique across the
* registry: a later plugin declaring an already-claimed plugin name, skill
* name, or MCP server name loses and the conflict is reported.
*/
declare class DeploymentPluginRegistry {
  private readonly directory;
  private readonly plugins;
  private readonly pluginsByName;
  private readonly skillsByName;
  private readonly serversByName;
  private readonly rejections;
  constructor(directory: string | null, loaded?: LoadedPlugin[]);
  private add;
  addRejection(diagnostics: PluginDiagnostic[]): void;
  getDirectory(): string | null;
  list(): LoadedPlugin[];
  skills(): DeploymentSkill[];
  /** Plugin MCP servers keyed by server name, shaped for `appConfig.mcpConfig`. */
  mcpServers(): Record<string, MCPOptions$1>;
  diagnostics(): PluginDiagnostic[];
}
declare function getDeploymentPluginRegistry(): DeploymentPluginRegistry;
declare function getDeploymentPluginSkills(): DeploymentSkill[];
declare function getDeploymentPluginMcpServers(): Record<string, MCPOptions$1>;
/**
* Scans a directory of Agent Plugins packages. Each immediate child directory
* is one plugin; a rejected plugin is reported and skipped so the remaining
* packages still load.
*/
declare function loadPluginsFromDirectory(directory: string, options?: DeploymentPluginOptions & {
  explicitlyConfigured?: boolean;
}): Promise<DeploymentPluginRegistry>;
declare function initializeDeploymentPlugins(options?: DeploymentPluginOptions): Promise<DeploymentPluginRegistry>;
//#endregion
//#region src/endpoints/anthropic/helpers.d.ts
declare const FINE_GRAINED_TOOL_STREAMING_BETA = "fine-grained-tool-streaming-2025-05-14";
declare function appendAnthropicBetaHeader(headers: Record<string, string> | undefined, beta: string): Record<string, string>;
/**
* @param {string} modelName
* @returns {boolean}
*/
declare function checkPromptCacheSupport(modelName: string): boolean;
/**
* Gets the appropriate headers for Claude models with cache control
* @param {string} model The model name
* @param {boolean} supportsCacheControl Whether the model supports cache control
* @returns {AnthropicClientOptions['extendedOptions']['defaultHeaders']|undefined} The headers object or undefined if not applicable
*/
declare function getClaudeHeaders(model: string, supportsCacheControl: boolean): Record<string, string> | undefined;
/**
* Configures reasoning-related options for Claude models.
* Models supporting adaptive thinking (Opus 4.6+, Sonnet 4.6+) use effort control instead of manual budget_tokens.
*/
declare function configureReasoning(anthropicInput: AnthropicClientOptions & {
  max_tokens?: number;
}, extendedOptions?: {
  thinking?: boolean;
  thinkingBudget?: number | null;
  effort?: AnthropicEffort | string | null;
  thinkingDisplay?: ThinkingDisplay | string | null;
}): AnthropicClientOptions & {
  max_tokens?: number;
};
//#endregion
//#region src/endpoints/anthropic/llm.d.ts
/** Known Anthropic parameters that map directly to the client config */
declare const knownAnthropicParams: Set<string>;
/**
* Generates configuration options for creating an Anthropic language model (LLM) instance.
* @param credentials - The API key for authentication with Anthropic, or credentials object for Vertex AI.
* @param options={} - Additional options for configuring the LLM.
* @returns Configuration options for creating an Anthropic LLM instance, with null and undefined values removed.
*/
declare function getLLMConfig(credentials: string | AnthropicCredentials | undefined, options?: AnthropicConfigOptions): AnthropicLLMConfigResult;
//#endregion
//#region src/endpoints/anthropic/vertex.d.ts
/**
* Options for loading Vertex AI credentials
*/
interface VertexCredentialOptions {
  /** Path to service account key file (overrides env var) */
  serviceKeyFile?: string;
  /** Project ID for Vertex AI */
  projectId?: string;
  /** Region for Vertex AI */
  region?: string;
}
/**
* Interface for Vertex AI configuration from YAML config.
* This matches the TVertexAISchema from librechat-data-provider.
*/
interface VertexAIConfigInput {
  enabled?: boolean;
  projectId?: string;
  region?: string;
  serviceKeyFile?: string;
  deploymentName?: string;
  models?: string[] | Record<string, boolean | {
    deploymentName?: string;
  }>;
}
/**
* Loads Google service account configuration for Vertex AI.
* Supports both YAML configuration and environment variables.
* @param options - Optional configuration from YAML or other sources
*/
declare function loadAnthropicVertexCredentials(options?: VertexCredentialOptions): Promise<AnthropicCredentials>;
/**
* Creates Vertex credential options from a Vertex AI configuration object.
* @param config - The Vertex AI configuration (from YAML config or other sources)
*/
declare function getVertexCredentialOptions(config?: VertexAIConfigInput): VertexCredentialOptions;
/**
* Checks if credentials are for Vertex AI (has service account key but no API key)
*/
declare function isAnthropicVertexCredentials(credentials: AnthropicCredentials): boolean;
/**
* Gets the deployment name for a given model name from the Vertex AI configuration.
* Maps visible model names to actual deployment names (model IDs).
* @param modelName - The visible model name (e.g., "Claude Opus 4.5")
* @param vertexConfig - The Vertex AI configuration with model mappings
* @returns The deployment name to use with the API (e.g., "claude-opus-4-5@20251101")
*/
declare function getVertexDeploymentName(modelName: string, vertexConfig?: VertexAIConfigInput): string;
/**
* Creates and configures a Vertex AI client for Anthropic.
* Supports both YAML configuration and environment variables for region/projectId.
* The projectId is automatically extracted from the service key if not explicitly provided.
* @param credentials - The Google service account credentials
* @param options - SDK client options
* @param vertexOptions - Vertex AI specific options (region, projectId) from YAML config
*/
declare function createAnthropicVertexClient(credentials: AnthropicCredentials, options?: ClientOptions$1, vertexOptions?: VertexAIClientOptions): AnthropicVertex;
//#endregion
//#region src/endpoints/anthropic/initialize.d.ts
/**
* Initializes Anthropic endpoint configuration.
* Supports both direct API key authentication and Google Cloud Vertex AI.
*
* @param params - Configuration parameters
* @returns Promise resolving to Anthropic configuration options
* @throws Error if API key is not provided (when not using Vertex AI)
*/
declare function initializeAnthropic(params: ProviderInitializeParams): Promise<InitializeResultBase>;
//#endregion
//#region src/endpoints/bedrock/initialize.d.ts
/**
* Initializes Bedrock endpoint configuration.
*
* This module handles configuration for AWS Bedrock endpoints, including support for
* HTTP/HTTPS proxies and reverse proxies.
*
* Proxy Support:
* - When proxy env vars are set, creates a custom BedrockRuntimeClient
*   with an HttpsProxyAgent to route Bedrock API calls through the resolved proxy
* - The custom client is fully configured with credentials, region, and endpoint,
*   and is passed directly to ChatBedrockConverse via the 'client' parameter
*
* Reverse Proxy Support:
* - When BEDROCK_REVERSE_PROXY is set, routes Bedrock API calls through a custom endpoint
* - Works with or without the PROXY setting
*
* Without Proxy:
* - Credentials and endpoint configuration are passed separately to ChatBedrockConverse,
*   which creates its own BedrockRuntimeClient internally
*
* @param params - Configuration parameters
* @returns Promise resolving to Bedrock configuration options
* @throws Error if credentials are not provided when required
*/
declare function initializeBedrock(params: ProviderInitializeParams): Promise<InitializeResultBase>;
//#endregion
//#region src/endpoints/config/endpoints.d.ts
type PartialEndpointEntry = Partial<TConfig> & Record<string, unknown>;
type DefaultEndpointsResult = Record<string, PartialEndpointEntry | false | null>;
interface EndpointsConfigDeps {
  getAppConfig: (params: GetAppConfigOptions) => Promise<AppConfig>;
  loadDefaultEndpointsConfig: (appConfig: AppConfig) => Promise<DefaultEndpointsResult>;
  loadCustomEndpointsConfig?: (custom: unknown) => TCustomEndpointsConfig | undefined;
}
declare function createEndpointsConfigService(deps: EndpointsConfigDeps): {
  getEndpointsConfig: (req: ServerRequest) => Promise<TEndpointsConfig>;
  checkCapability: (req: ServerRequest, capability: AgentCapabilities) => Promise<boolean>;
};
//#endregion
//#region src/endpoints/models.d.ts
interface FetchModelsParams {
  /** User ID for API requests */
  user?: string;
  /** API key for authentication */
  apiKey: string;
  /** Base URL for the API */
  baseURL?: string;
  /** Whether the base URL came from a user-stored credential */
  baseURLIsUserProvided?: boolean;
  /** Admin-approved internal host:port exemptions for user-provided base URLs */
  allowedAddresses?: string[] | null;
  /** Endpoint name (defaults to 'openAI') */
  name?: string;
  /** Whether directEndpoint was configured */
  direct?: boolean;
  /** Whether to fetch from Azure */
  azure?: boolean;
  /** Whether to send user ID as query parameter */
  userIdQuery?: boolean;
  /** Whether to create token configuration from API response */
  createTokenConfig?: boolean;
  /** Cache key for token configuration (uses name if omitted) */
  tokenKey?: string;
  /** Optional headers for the request */
  headers?: Record<string, string> | null;
  /** Optional user object for header resolution */
  userObject?: Partial<IUser>;
  /** Skip MODEL_QUERIES cache (e.g., for user-provided keys) */
  skipCache?: boolean;
}
/**
* Splits a string by commas and trims each resulting value.
* @param input - The input string to split.
* @returns An array of trimmed values.
*/
declare function splitAndTrim(input: string | null | undefined): string[];
/**
* Fetches models from the specified base API path or Azure, based on the provided configuration.
*
* @param params - The parameters for fetching the models.
* @returns A promise that resolves to an array of model identifiers.
*/
declare function fetchModels({
  user,
  apiKey,
  baseURL: _baseURL,
  baseURLIsUserProvided,
  allowedAddresses,
  name,
  direct,
  azure,
  userIdQuery,
  createTokenConfig,
  tokenKey,
  headers,
  userObject,
  skipCache
}: FetchModelsParams): Promise<string[]>;
/** Options for fetching OpenAI models */
interface GetOpenAIModelsOptions {
  /** User ID for API requests */
  user?: string;
  /** Whether to fetch from Azure */
  azure?: boolean;
  /** Whether to fetch models for the Assistants endpoint */
  assistants?: boolean;
  /** OpenAI API key (if not using environment variable) */
  openAIApiKey?: string;
  /** Skip MODEL_QUERIES cache (e.g., for user-provided keys) */
  skipCache?: boolean;
  /** Configured custom headers forwarded to the (gateway-fronted) provider */
  headers?: Record<string, string> | null;
  /** User object for resolving header placeholders */
  userObject?: Partial<IUser>;
}
/**
* Fetches models from OpenAI or Azure based on the provided options.
* @param opts - Options for fetching models
* @param _models - Fallback models array
* @returns Promise resolving to array of model IDs
*/
declare function fetchOpenAIModels(opts: GetOpenAIModelsOptions, _models?: string[]): Promise<string[]>;
/**
* Loads the default models for OpenAI or Azure.
* @param opts - Options for getting models
* @returns Promise resolving to array of model IDs
*/
declare function getOpenAIModels(opts?: GetOpenAIModelsOptions): Promise<string[]>;
/**
* Fetches models from the Anthropic API.
* @param opts - Options for fetching models
* @param _models - Fallback models array
* @returns Promise resolving to array of model IDs
*/
declare function fetchAnthropicModels(opts?: {
  user?: string;
  skipCache?: boolean;
  headers?: Record<string, string> | null;
  userObject?: Partial<IUser>;
}, _models?: string[]): Promise<string[]>;
/**
* Gets Anthropic models from environment or API.
* @param opts - Options for fetching models
* @returns Promise resolving to array of model IDs
*/
declare function getAnthropicModels(opts?: {
  user?: string;
  vertexModels?: string[];
  headers?: Record<string, string> | null;
  userObject?: Partial<IUser>;
}): Promise<string[]>;
/**
* Gets Google models from environment or defaults.
* @returns Array of model IDs
*/
declare function getGoogleModels(): string[];
/**
* Gets Bedrock models from environment or defaults.
* @returns Array of model IDs
*/
declare function getBedrockModels(): string[];
//#endregion
//#region src/endpoints/config/models.d.ts
interface LoadConfigModelsDeps {
  getAppConfig: (params: GetAppConfigOptions) => Promise<AppConfig>;
  getUserKeyValues: GetUserKeyValuesFunction;
  fetchModels?: (params: FetchModelsParams) => Promise<string[]>;
}
declare function createLoadConfigModels(deps: LoadConfigModelsDeps): (req: ServerRequest) => Promise<TModelsConfig>;
//#endregion
//#region src/endpoints/config/providers.d.ts
/**
* Type for initialize functions
*/
type InitializeFn = (params: ProviderInitializeParams) => Promise<InitializeResultBase>;
/**
* Check if the provider is a known custom provider
* @param provider - The provider string
* @returns True if the provider is a known custom provider, false otherwise
*/
declare function isKnownCustomProvider(provider?: string): boolean;
/**
* Provider configuration map mapping providers to their initialization functions.
*
* `Providers.VERTEXAI` shares `initializeGoogle` because the runtime distinction
* is auth-only — the agent flow may resolve `agent.provider` to `vertexai` when
* a service account is configured, but summarization (and other downstream
* resolvers) get the same lowercase enum value passed back. Without this
* mapping `getProviderConfig` throws "Provider vertexai not supported" and
* summarization falls back to the raw provider, dropping client overrides.
*/
declare const providerConfigMap: Record<string, InitializeFn>;
type TitleTiming = "immediate" | "final";
/**
* Resolves when conversation titles are generated for a given endpoint.
*
* `endpoints.all.titleTiming`, when present, is the global override. Otherwise,
* endpoint candidates are checked in order so the public endpoint (for example
* `agents`) can override the backing provider, with provider/custom config used
* as a fallback. Resolving custom providers via `getProviderConfig` picks up its
* case-insensitive fallback for normalized provider names (e.g. `openrouter` →
* `OpenRouter`). Defaults to `immediate`.
*/
declare function resolveTitleTiming({
  appConfig,
  endpoint
}: {
  appConfig?: AppConfig;
  endpoint?: string | Array<string | undefined>;
}): TitleTiming;
/**
* Result from getProviderConfig
*/
interface ProviderConfigResult {
  /** The initialization function for this provider */
  getOptions: InitializeFn;
  /** The resolved provider name (may be different from input if normalized) */
  overrideProvider: string;
  /** Custom endpoint configuration (if applicable) */
  customEndpointConfig?: Partial<TEndpoint>;
}
/**
* Get the provider configuration and override endpoint based on the provider string
*
* @param params - Configuration parameters
* @param params.provider - The provider string
* @param params.appConfig - The application configuration
* @returns Provider configuration including getOptions function, override provider, and custom config
* @throws Error if provider is not supported
*/
declare function getProviderConfig({
  provider,
  appConfig
}: {
  provider: string;
  appConfig?: AppConfig;
}): ProviderConfigResult;
//#endregion
//#region src/endpoints/custom/config.d.ts
/**
* Load config endpoints from the cached configuration object
* @param customEndpointsConfig - The configuration object
*/
declare function loadCustomEndpointsConfig(customEndpoints?: TCustomEndpoints): TCustomEndpointsConfig | undefined;
//#endregion
//#region src/endpoints/custom/initialize.d.ts
/**
* Cache key for an endpoint's fetched token config. User-scoped when the
* model fetch can resolve per-user: user-provided key/URL, or header
* templates forwarded against an admin-trusted base URL — making the
* response, and therefore the derived token config, user-specific. Otherwise
* tenant-scoped when a tenant id is available because custom endpoint config
* is resolved per tenant.
*/
declare function getTokenConfigKey(endpointConfig: Partial<TEndpoint>, endpoint: string, userId: string, tenantId?: string | null): string;
/**
* Initializes a custom endpoint client configuration.
* This function handles custom endpoints defined in librechat.yaml, including
* user-provided API keys and URLs.
*
* @param params - Configuration parameters
* @returns Promise resolving to endpoint configuration options
* @throws Error if config is missing, API key is not provided, or base URL is missing
*/
declare function initializeCustom(params: ProviderInitializeParams): Promise<InitializeResultBase>;
//#endregion
//#region src/endpoints/google/llm.d.ts
/** Known Google/Vertex AI parameters that map directly to the client config */
declare const knownGoogleParams: Set<string>;
/**
* Removes the parameters a Gemini Flash model rejects (see
* {@link geminiFlashLegacyParams}) from a params object. Used by the
* Google-compatible custom-endpoint path (`getOpenAIConfig`), where `addParams`
* is re-applied by `transformToOpenAIConfig` after `getGoogleConfig` has already
* stripped `llmConfig` — without this the strip is undone and the deprecated
* sampling / rejected penalty params reach the provider again. No-op for
* non-Flash models and when there is nothing to strip.
*/
declare function stripGeminiFlashBlockedParams<T extends Record<string, unknown> | undefined>(params: T, model: string | undefined): T;
declare function getSafetySettings(model?: string): Array<{
  category: string;
  threshold: string;
}> | undefined;
/**
* Replicates core logic from GoogleClient's constructor and setOptions, plus client determination.
* Returns an object with the provider label and the final options that would be passed to createLLM.
*
* @param credentials - Either a JSON string or an object containing Google keys
* @param options - The same shape as the "GoogleClient" constructor options
*/
declare function getGoogleConfig(credentials: string | GoogleCredentials | undefined, options?: GoogleConfigOptions, acceptRawApiKey?: boolean): {
  /** @type {GoogleAIToolType[]} */tools: GoogleAIToolType[]; /** @type {Providers.GOOGLE | Providers.VERTEXAI} */
  provider: Providers.VERTEXAI | Providers.GOOGLE; /** @type {GoogleClientOptions | VertexAIClientOptions} */
  llmConfig: VertexAIClientOptions$1 | GoogleClientOptions;
};
//#endregion
//#region src/endpoints/google/errors.d.ts
/**
* True for Google's generic `400 INVALID_ARGUMENT`. Both the Gemini Developer API and Vertex use
* the same wording, so the HTTP status is accepted from either the error object or the message text
* that the `@google/generative-ai` SDK bakes its status into.
*/
declare function isGoogleInvalidArgumentError(error: unknown): boolean;
/**
* Maps a failed Google request back to the YouTube video that most likely caused it, returning the
* typed error payload the client localizes (or `undefined` to leave the original error alone).
*
* Attribution rests on context rather than the response body: when the turn carried an injected
* YouTube video and Google answers with its generic `INVALID_ARGUMENT`, the video is the cause we
* can act on. Verified against the live API — a public 9h15m video is refused this way on every
* Gemini model tested, including at `MEDIA_RESOLUTION_LOW`, while a short video with an otherwise
* identical payload succeeds. Duration is the common trigger; region and access restrictions
* produce the same response, so the localized copy names length first without claiming certainty.
*/
declare function resolveGoogleVideoError(params: {
  error: unknown;
  provider?: string;
  hasYouTubeVideo?: boolean;
}): string | undefined;
//#endregion
//#region src/endpoints/google/initialize.d.ts
/**
* Initializes Google/Vertex AI endpoint configuration.
* Supports both API key authentication and service account credentials.
*
* @param params - Configuration parameters
* @returns Promise resolving to Google configuration options
* @throws Error if no valid credentials are provided
*/
declare function initializeGoogle(params: ProviderInitializeParams): Promise<InitializeResultBase>;
//#endregion
//#region src/endpoints/google/youtube.d.ts
/**
* Native YouTube -> Gemini video-understanding support for the Google `url_context` param.
*
* The Gemini URL Context tool does NOT support YouTube as a content type; YouTube must be
* passed as a video part (`fileData.fileUri`) via the video-understanding path. When
* `url_context` is enabled we detect YouTube URLs in the user's message and inject them as
* `{ type: 'media', fileUri }` content blocks, which the `@librechat/agents` Google content
* formatter converts into `fileData` parts. All other URLs keep flowing through the native
* `urlContext` tool.
*/
/** Per-message cap on auto-injected YouTube video parts for Gemini 2.5+ (the API allows up to 10). */
declare const DEFAULT_MAX_YOUTUBE_PARTS = 5;
/** A Gemini video-understanding content block (becomes a `fileData` part downstream). */
interface YouTubeVideoPart {
  type: "media";
  /**
  * Always present as a key so the agents `messageContentMedia` `'mimeType' in content` guard
  * passes. The Gemini Developer API omits it for YouTube (value `undefined`, dropped on JSON
  * serialization); Vertex samples set `video/mp4`.
  */
  mimeType?: string;
  fileUri: string;
}
/**
* Resolves provider/model-aware YouTube injection limits.
* - Vertex: every Vertex sample sets a `video/mp4` mimeType on YouTube fileData, and Vertex is
*   capped conservatively at a single YouTube URL per request.
* - Gemini Developer API: omits the mimeType; 2.5+ models accept multiple videos, earlier ones one.
*/
declare function resolveYouTubeInjectionConfig(params: {
  provider?: string;
  model?: string;
}): {
  max: number;
  mimeType?: string;
};
/**
* Extracts YouTube URLs from text, normalized to canonical `watch?v=<id>` form, de-duplicated
* by video id (first occurrence wins), and capped at `max` (default {@link DEFAULT_MAX_YOUTUBE_PARTS}).
*/
declare function extractYouTubeUrls(text?: string | null, max?: number): string[];
/** True when the resolved provider tool list contains the native Google `urlContext` tool. */
declare function hasUrlContextTool(tools: unknown): boolean;
/**
* True when a formatted message carries a YouTube video part produced by
* `appendYouTubeVideoParts`. Read after injection so a provider rejection can be attributed to the
* video rather than reported as a generic failure — Google returns the same opaque
* `INVALID_ARGUMENT` for an over-length or otherwise unreadable video as for unrelated bad input.
*/
declare function hasYouTubeVideoParts(content: string | MessageContentComplex[]): boolean;
/**
* Appends YouTube video parts to a message's content when `url_context` is enabled, and removes
* the matched YouTube URLs from the prompt text so the `urlContext` tool (which reads URLs from
* the text and cannot fetch YouTube) does not spend its URL budget on them. Non-YouTube URLs are
* left intact for the tool. No-ops when disabled, when no YouTube URLs are found, or when every
* URL is already present as a media part. A string content is upgraded to a parts array only when
* something is added.
*/
declare function appendYouTubeVideoParts(params: {
  enabled: boolean;
  text?: string | null;
  content: string | MessageContentComplex[];
  max?: number;
  mimeType?: string;
}): string | MessageContentComplex[];
//#endregion
//#region src/endpoints/openai/llm.d.ts
declare const knownOpenAIParams: Set<string>;
/**
* Translates a scalar `reasoning_effort` parameter into the reasoning fields an
* already-resolved OpenAI-compatible client honors, for callers that layer
* their own parameters on top of a client configuration built elsewhere
* (summarization reusing the agent's client options).
*
* The override has to land in *top-level* fields. A nested `modelKwargs`
* fragment would replace the inherited `modelKwargs` wholesale, and a scalar
* `reasoning_effort` is dropped outright: LangChain reads only `reasoning` from
* constructor fields — `reasoning_effort` is a call-time option. `reasoning` is
* the one shape every OpenAI-compatible client honors, since Chat Completions
* re-emits it as `reasoning_effort`, the Responses API sends it as-is, and
* `ChatOpenRouter` merges it over an inherited `modelKwargs.reasoning`.
*
* Mirrors {@link applyOpenRouterReasoningConfig} for OpenRouter's adaptive
* Anthropic models, where effort is expressed as `verbosity` rather than
* `reasoning.effort`. Non-OpenAI-compatible providers are left untouched:
* they have no `reasoning_effort` concept to translate into.
*/
declare function resolveReasoningParams({
  provider,
  model,
  parameters
}: {
  provider?: string | null;
  model?: string | null;
  parameters?: Record<string, unknown>;
}): Record<string, unknown> | undefined;
/**
* Extracts default parameters from customParams.paramDefinitions
* @param paramDefinitions - Array of parameter definitions with key and default values
* @returns Record of default parameters
*/
declare function extractDefaultParams(paramDefinitions?: Partial<SettingDefinition>[]): Record<string, unknown> | undefined;
/**
* Applies default parameters to the target object only if the field is undefined
* @param target - The target object to apply defaults to
* @param defaults - Record of default parameter values
*/
declare function applyDefaultParams(target: Record<string, unknown>, defaults: Record<string, unknown>): void;
declare function getOpenAILLMConfig({
  azure,
  apiKey,
  baseURL,
  endpoint,
  streaming,
  addParams,
  dropParams,
  defaultParams,
  useOpenRouter,
  reasoningFormat,
  modelOptions: _modelOptions
}: {
  apiKey: string;
  streaming: boolean;
  baseURL?: string | null;
  endpoint?: EModelEndpoint | string | null;
  modelOptions: Partial<OpenAIParameters>;
  addParams?: Record<string, unknown>;
  dropParams?: string[];
  defaultParams?: Record<string, unknown>;
  useOpenRouter?: boolean;
  reasoningFormat?: ReasoningParameterFormat;
  azure?: false | AzureOptions;
}): Pick<LLMConfigResult, "llmConfig" | "tools"> & {
  azure?: AzureOptions;
};
//#endregion
//#region src/endpoints/openai/config.d.ts
/**
* Generates configuration options for creating a language model (LLM) instance.
* @param apiKey - The API key for authentication.
* @param options - Additional options for configuring the LLM.
* @param endpoint - The endpoint name
* @returns Configuration options for creating an LLM instance.
*/
declare function getOpenAIConfig(apiKey: string, options?: OpenAIConfigOptions, endpoint?: string | null): OpenAIConfigResult;
//#endregion
//#region src/endpoints/openai/initialize.d.ts
/**
* Admin-configured base URL for a built-in OpenAI-family endpoint, or `undefined`
* when the endpoint is served by OpenAI/Azure directly.
*
* Read at call time rather than module load so environment changes apply, and
* shared with callers that must know the endpoint a built-in provider actually
* talks to before claiming its contract. May be the `user_provided` sentinel,
* which resolves to a per-user value only after a database read.
*/
declare function getBuiltInBaseURL(endpoint?: string | null): string | undefined;
/**
* Initializes OpenAI options for agent usage. This function always returns configuration
* options and never creates a client instance (equivalent to optionsOnly=true behavior).
*
* @param params - Configuration parameters
* @returns Promise resolving to OpenAI configuration options
* @throws Error if API key is missing or user key has expired
*/
declare function initializeOpenAI(params: ProviderInitializeParams): Promise<InitializeResultBase>;
//#endregion
//#region src/endpoints/pricing.d.ts
interface TokenomicsDeps {
  getValueKey: TxMethods["getValueKey"];
  getMultiplier: TxMethods["getMultiplier"];
  getCacheMultiplier: TxMethods["getCacheMultiplier"];
}
interface TokenConfigParams {
  /** endpoint → model list, from the resolved models config */
  modelsConfig: TModelsConfig;
  /** Per-endpoint overrides: fetched (e.g. OpenRouter) or yaml `tokenConfig` */
  endpointTokenConfigs?: Record<string, EndpointTokenConfig | undefined>;
  /** Include USD-per-1M rates; gated by `interface.contextCost` */
  includePricing?: boolean;
}
/**
* Resolves context windows (and optionally pricing) for every configured
* model, server-side, so the client never reimplements pattern matching.
*/
declare function buildTokenConfigMap(params: TokenConfigParams, deps: TokenomicsDeps): TTokenConfigMap;
//#endregion
//#region src/endpoints/tokenConfig.d.ts
interface ResolveTokenConfigParams {
  appConfig?: AppConfig;
  /** endpoint → model list, from the resolved models config */
  modelsConfig: TModelsConfig;
  userId: string;
  tenantId?: string | null;
}
/**
* Server-side token-config resolution for the `/endpoints/token-config`
* route. Gathers each custom endpoint's override — a static yaml `tokenConfig`
* or the cached fetched config — then resolves context windows (and pricing,
* when `interface.contextCost` is enabled) via {@link buildTokenConfigMap}.
* Lives in TypeScript so the `/api` controller stays a thin wrapper.
*/
declare function resolveTokenConfigMap({
  appConfig,
  modelsConfig,
  userId,
  tenantId
}: ResolveTokenConfigParams, deps: TokenomicsDeps): Promise<TTokenConfigMap>;
//#endregion
//#region src/files/agents/auth.d.ts
type AgentUploadAuthResult = {
  allowed: true;
} | {
  allowed: false;
  status: number;
  error: string;
  message: string;
};
interface AgentUploadAuthParams {
  userId: string;
  userRole: string;
  agentId?: string;
  toolResource?: string | null;
  messageFile?: boolean | string;
}
interface AgentUploadAuthDeps {
  getAgent: (params: {
    id: string;
  }) => Promise<{
    _id: string | Types.ObjectId;
    author?: string | Types.ObjectId | null;
  } | null>;
  checkPermission: (params: {
    userId: string;
    role: string;
    resourceType: ResourceType;
    resourceId: string | Types.ObjectId;
    requiredPermission: number;
  }) => Promise<boolean>;
}
declare function checkAgentUploadAuth(params: AgentUploadAuthParams, deps: AgentUploadAuthDeps): Promise<AgentUploadAuthResult>;
/** @returns true if denied (response already sent), false if allowed */
declare function verifyAgentUploadPermission({
  req,
  res,
  metadata,
  getAgent,
  checkPermission,
  hasUploadBypass
}: {
  req: ServerRequest;
  res: Response$2;
  metadata: {
    agent_id?: string;
    tool_resource?: string | null;
    message_file?: boolean | string;
  };
  getAgent: AgentUploadAuthDeps["getAgent"];
  checkPermission: AgentUploadAuthDeps["checkPermission"];
  /** Global capability that permits agent writes regardless of the per-agent grant. Held
  *  here rather than at each route so the two upload routes cannot answer differently. */
  hasUploadBypass?: () => Promise<boolean>;
}): Promise<boolean>;
//#endregion
//#region src/files/audio.d.ts
/**
* Processes audio files using Speech-to-Text (STT) service.
* @returns A promise that resolves to an object containing text and bytes.
*/
declare function processAudioFile({
  req,
  file,
  sttService
}: {
  req: ServerRequest;
  file: FileObject;
  sttService: STTService;
}): Promise<AudioProcessingResult>;
//#endregion
//#region src/files/citations.d.ts
interface FileCitationSource {
  fileId: string;
  relevance: number;
}
interface FileCitationSelectionConfig {
  maxCitations?: number;
  maxCitationsPerFile?: number;
  minRelevanceScore?: number;
}
declare function applyCitationLimits<TSource extends FileCitationSource>(sources: readonly TSource[], maxCitations: number, maxCitationsPerFile: number): TSource[];
/** Selects the passages shared by model anchors and browser citation attachments. */
declare function selectFileCitationSources<TSource extends FileCitationSource>(sources: readonly TSource[] | null | undefined, config?: FileCitationSelectionConfig): TSource[];
//#endregion
//#region src/files/context.d.ts
/**
* Stand-in text for a user turn that carries attachments but no typed message.
* Anthropic and the Assistants API both reject empty user content, and files
* that reach the model out-of-band (RAG, code environment) leave nothing else
* in the turn, so the payload needs this minimal note. The stored message keeps
* its empty text so the UI still renders the attachment on its own.
*/
declare const ATTACHMENT_ONLY_TEXT = "Please refer to the attached file(s).";
/**
* Title-generation input for a turn the user sent without typing anything.
* Immediate title timing runs before any response exists, so the attachment
* filenames are the only conversation-specific signal available; without them
* the title model is prompted with an empty string and invents a topic.
*/
declare function getAttachmentTitleText(files?: TFile[] | null): string;
/**
* Extracts text context from attachments and returns formatted text.
* This handles text that was already extracted from files (OCR, transcriptions, document text, etc.)
* @param params - The parameters object
* @param params.attachments - Array of file attachments
* @param params.req - Express request object for config access
* @param params.tokenCountFn - Function to count tokens in text
* @returns The formatted file context text, or undefined if no text found
*/
declare function extractFileContext({
  attachments,
  req,
  tokenCountFn
}: {
  attachments: readonly (Pick<TFile, "text" | "filename"> & {
    source?: string;
    llmDeliveryPath?: string;
  })[];
  req?: ServerRequest;
  tokenCountFn: TokenCountFn;
}): Promise<string | undefined>;
//#endregion
//#region src/files/deletion.d.ts
/** What a delete pass reports back about the records it was given. */
type FileDeletionOutcome = {
  deletedFileIds?: string[];
  failedFileIds?: string[];
};
declare const PARTIAL_FILE_DELETION_MESSAGE = "Some files could not be deleted";
/**
* Shapes the body of a delete response. A record whose storage delete failed is not an error for
* the request as a whole, so the outcome travels in the body rather than the status: clients read
* `failedFileIds` to know what is still on disk, and treat everything else they asked for as gone.
*/
declare const buildDeleteFilesResponse: (result: FileDeletionOutcome | null | undefined, successMessage: string) => DeleteFilesResponse;
//#endregion
//#region src/files/extract.d.ts
/**
* The most extracted text a file document can hold. MongoDB refuses a document past 16 MiB and
* the text shares it with the file's other fields, so this bounds storage rather than policy:
* what reaches a model is governed by `fileContextCharLimit` and `fileTokenLimit`.
*/
declare const MAX_STORED_EXTRACTED_TEXT_BYTES: number;
interface ExtractedFileText {
  readonly text?: string | null;
}
/** Keeps submitted filenames and provider/parser details out of protected extraction logs. */
declare function getFileExtractionLogDetails(input: {
  readonly filters?: FiltersConfig;
  readonly filename: string;
  readonly fileId: string;
  readonly error: unknown;
}): {
  readonly contentProtected: boolean;
  readonly fileLabel: string;
  readonly errorMetadata: unknown;
};
/** Requires a produced value when strict extracted-text coverage is configured. */
declare function assertExtractedTextInspectable(input: {
  readonly filters?: FiltersConfig;
  readonly text?: string | null;
}): void;
/** Converts extraction failures and empty results into the stable fail-close policy error. */
declare function extractInspectableFileText<T extends ExtractedFileText>(input: {
  readonly filters?: FiltersConfig;
  readonly extract: () => Promise<T | null | undefined>;
}): Promise<T | null | undefined>;
//#endregion
//#region src/files/documents/crud.d.ts
/**
* Parses an uploaded document and extracts its text content and metadata.
* Handled types must stay in sync with `documentParserMimeTypes` from data-provider.
*
* @throws {Error} if `file.mimetype` is not handled, file exceeds size limit, or no text is found.
*/
declare function parseDocument({
  file
}: {
  file: Express.Multer.File;
}): Promise<MistralOCRUploadResult>;
//#endregion
//#region src/files/encode/audio.d.ts
/**
* Encodes and formats audio files for different providers
* @param req - The request object
* @param files - Array of audio files
* @param params - Object containing provider and optional endpoint
* @param params.provider - The provider to format for
* @param params.endpoint - Optional endpoint name for file config lookup
* @param getStrategyFunctions - Function to get strategy functions
* @returns Promise that resolves to audio and file metadata
*/
declare function encodeAndFormatAudios(req: ServerRequest, files: IMongoFile[], params: {
  provider: Providers;
  endpoint?: string;
}, getStrategyFunctions: (source: string) => StrategyFunctions): Promise<AudioResult>;
//#endregion
//#region src/files/encode/document.d.ts
/**
* Encodes and formats document files for various providers.
*
* Callers are responsible for pre-filtering `files` to types the endpoint accepts
* (e.g., via `supportedMimeTypes` in `processAttachments`). This function processes
* every file it receives and dispatches to the appropriate provider format:
* - **Bedrock**: Only encodes types in `bedrockDocumentFormats`; all others are skipped.
* - **Anthropic**: Only encodes PDFs (base64 source) and textual types (plain-text source);
*   all others are skipped.
* - **PDF**: Validated via `validatePdf` before encoding.
* - **Generic types**: Encoded with a provider-specific size check.
*/
declare function encodeAndFormatDocuments(req: ServerRequest, files: IMongoFile[], params: {
  provider: Providers;
  endpoint?: string;
  useResponsesApi?: boolean;
  model?: string;
}, getStrategyFunctions: (source: string) => StrategyFunctions): Promise<DocumentResult>;
//#endregion
//#region src/files/encode/image.d.ts
type ImageEncodingFile = Pick<IMongoFile, "file_id" | "temp_file_id" | "filepath" | "storageKey" | "filename" | "type" | "height" | "width" | "embedded" | "metadata"> & Partial<Pick<IMongoFile, "source" | "bytes">>;
interface ImageStrategy extends StrategyFunctions {
  prepareImagePayload?: (req: ServerRequest, file: ImageEncodingFile) => Promise<[ImageEncodingFile, string]>;
}
interface ImageEncodingDependencies {
  getStrategyFunctions: (source: string) => ImageStrategy;
  httpClient: Pick<AxiosInstance, "get">;
}
interface ImageResult {
  files: Array<Pick<ImageEncodingFile, "file_id" | "filepath" | "filename" | "type" | "height" | "width" | "embedded" | "metadata">>;
  image_urls: Array<Agents.MessageContentImageUrl | {
    type: ContentTypes.IMAGE_URL;
    inlineData: {
      mimeType: string;
      data: string;
    };
  } | {
    type: "image";
    source: {
      type: "base64";
      media_type: string;
      data: string;
    };
  }>;
}
declare class AttachmentStorageError extends Error {
  readonly code = "ATTACHMENT_STORAGE_ERROR";
  constructor();
}
/** Owns source selection, guarded reads, validation and provider-specific image formatting. */
declare function encodeAndFormatImages(req: ServerRequest, files: ImageEncodingFile[] | null | undefined, params: {
  provider?: string;
  endpoint?: string;
  imageDetail?: Agents.ImageDetail;
}, {
  getStrategyFunctions,
  httpClient
}: ImageEncodingDependencies, mode?: string): Promise<ImageResult>;
//#endregion
//#region src/files/encode/video.d.ts
/**
* Encodes and formats video files for different providers
* @param req - The request object
* @param files - Array of video files
* @param params - Object containing provider and optional endpoint
* @param params.provider - The provider to format for
* @param params.endpoint - Optional endpoint name for file config lookup
* @param getStrategyFunctions - Function to get strategy functions
* @returns Promise that resolves to videos and file metadata
*/
declare function encodeAndFormatVideos(req: ServerRequest, files: IMongoFile[], params: {
  provider: Providers;
  endpoint?: string;
}, getStrategyFunctions: (source: string) => StrategyFunctions): Promise<VideoResult>;
//#endregion
//#region src/files/encode/memoryGuard.d.ts
declare class InFlightBytesSemaphore {
  private readonly ceiling;
  private inFlight;
  private readonly queue;
  constructor(ceiling: number);
  acquire(bytes: number): Promise<() => void>;
  private canAdmit;
  private grant;
  private drain;
}
declare function runGuardedEncode<T>(estimatedBytes: number, task: () => Promise<T>): Promise<T>;
//#endregion
//#region src/storage/path.d.ts
/** A stored file as a download needs it: the recorded object key when one exists, else the URL or path. */
type StoredFileRef = Pick<TFile, "filepath"> & {
  storageKey?: string | null;
};
/**
* Resolves the argument for a strategy's `getDownloadStream`. S3 and CloudFront records carry
* the object key since the region-aware storage keys landed; handing it over directly skips
* re-deriving it from a presigned or CDN URL, which is only sound while that URL still parses.
* Records without a key (local, Firebase, Azure, code output) fall through to `filepath` as before.
*/
declare function resolveDownloadPath(file: StoredFileRef): string;
//#endregion
//#region src/files/encode/utils.d.ts
declare class AttachmentObjectNotFoundError extends Error {
  readonly fileId: string | undefined;
  readonly code = "ATTACHMENT_OBJECT_NOT_FOUND";
  constructor(fileId: string | undefined);
}
declare function isAttachmentObjectNotFoundError(error: unknown): error is AttachmentObjectNotFoundError;
/**
* Extracts the configured file size limit for a specific provider from fileConfig
* @param req - The server request object containing config
* @param params - Object containing provider and optional endpoint
* @param params.provider - The provider to get the limit for
* @param params.endpoint - Optional endpoint name for lookup
* @returns The configured file size limit in bytes, or undefined if not configured
*/
declare const getConfiguredFileSizeLimit: (req: ServerRequest, params: {
  provider: Providers;
  endpoint?: string;
}) => number | undefined;
/**
* Whether the admin explicitly allowed `mimeType` for this endpoint via
* `fileConfig.endpoints.<name>.supportedMimeTypes`. The inherited default list does
* not count as opting in, mirroring the client's picker and drag-drop logic, so an
* OpenAI-compatible endpoint only receives video/audio parts when configured for them.
* @param req - The server request object containing config
* @param params - Object containing provider and optional endpoint
* @param params.provider - The provider to look up
* @param params.endpoint - Optional endpoint name for lookup
* @param mimeType - The MIME type of the file being attached
* @returns True when the endpoint config explicitly matches the MIME type
*/
declare const isConfiguredProviderMediaType: (req: ServerRequest, params: {
  provider: Providers;
  endpoint?: string;
}, mimeType: string) => boolean;
/**
* Resolves the `input_audio.format` value for a file.
*
* Prefers the MIME type, which is validated upstream against `audioMimeTypes`, because
* the filename extension is user-controlled: it may be absent (`recording`), an alias
* that is not a valid format (`clip.wave`), or disagree with the actual content. Falls
* back to the extension only when it is itself a supported format.
* @param mimeType - The file's MIME type
* @param filename - The original filename
* @returns The canonical format, or undefined when neither source yields a supported one
*/
declare const getAudioFormat: (mimeType: string, filename: string) => string | undefined;
/**
* Processes a file by downloading and encoding it to base64
* @param req - Express request object
* @param file - File object to process
* @param encodingMethods - Cache of encoding methods by source
* @param getStrategyFunctions - Function to get strategy functions for a source
* @returns Processed file with content and metadata, or null if no download reference exists
*/
declare function getFileStream<T extends ProcessedFile["metadata"] & StoredFileRef>(req: ServerRequest, file: T, encodingMethods: Record<string, StrategyFunctions>, getStrategyFunctions: (source: string) => StrategyFunctions): Promise<ProcessedFile<T> | null>;
//#endregion
//#region src/files/filter.d.ts
/**
* Filters out files based on endpoint configuration including:
* - Disabled status
* - File size limits
* - MIME type restrictions
* - Total size limits
* @param req - The server request object containing config
* @param params - Object containing files, endpoint, and endpointType
* @param params.files - Array of processed file documents from MongoDB
* @param params.endpoint - The endpoint name to check configuration for
* @param params.endpointType - The endpoint type to check configuration for
* @returns Filtered array of files
*/
declare function filterFilesByEndpointConfig(req: ServerRequest, params: {
  files: IMongoFile[] | undefined;
  endpoint?: string | null;
  endpointType?: string | null;
  skipTotalSizeLimit?: boolean;
  preserveTextSources?: boolean;
}): IMongoFile[];
/** Request-free endpoint file-policy adapter used by Agent execution hosts. */
/**
* Whether this endpoint still presents the explicit upload-destination chooser. In that
* mode the destination is the user's choice and the upload path acts on it immediately,
* so nothing may be provisioned to a service they did not select.
*/
declare function isLegacyFileUploadUX(appConfig: AppConfig | undefined, params: {
  endpoint?: string | null;
  endpointType?: string | null;
}): boolean;
type EndpointPolicyFile = Pick<TFile, "bytes" | "type"> & {
  source?: string;
  metadata?: {
    routingMimeType?: string;
  };
};
declare function filterFilesByEndpointRuntimeConfig<T extends EndpointPolicyFile>(appConfig: AppConfig | undefined, params: {
  files: T[] | undefined;
  endpoint?: string | null;
  endpointType?: string | null;
  /** Bytes already committed by an earlier call, so a request split across several
  *  sets spends one shared `totalSizeLimit` instead of restarting it per set. */
  consumedBytes?: number;
  skipTotalSizeLimit?: boolean;
  preserveTextSources?: boolean;
}): T[];
//#endregion
//#region src/files/mime.d.ts
type EncodedImageMetadata = Pick<Metadata$1, "format" | "compression">;
/**
* Resolves the media type of an encoded image from what sharp read back out of it.
*
* Sharp re-encodes whatever it resizes, and the format that comes out need not be the one that
* went in — an SVG is rasterized to PNG. Recording a caller's declared type against re-encoded
* bytes leaves a file whose `type` misdescribes its own contents, and that type is later handed
* to providers verbatim as `media_type`/`mimeType`, so it has to describe the bytes on disk.
*
* Returns `undefined` when the bytes cannot be named confidently — a format with no media type of
* its own, or a heif container whose compression sharp did not report — leaving the caller to fall
* back to what it already knows. Naming one of those anyway would be the guess this exists to stop.
*/
declare function resolveImageMimeType(metadata: EncodedImageMetadata): string | undefined;
//#endregion
//#region src/files/mistral/crud.d.ts
/** Helper type for OCR request context */
interface OCRContext {
  req: ServerRequest;
  file: Express.Multer.File;
  loadAuthValues: (params: {
    userId: string;
    authFields: string[];
    optional?: Set<string>;
  }) => Promise<Record<string, string | undefined>>;
}
/**
* Uploads a document to Mistral API using file streaming to avoid loading the entire file into memory
* @param params Upload parameters
* @param params.filePath The path to the file on disk
* @param params.fileName Optional filename to use (defaults to the name from filePath)
* @param params.apiKey Mistral API key
* @param params.baseURL Mistral API base URL
* @returns The response from Mistral API
*/
declare function uploadDocumentToMistral({
  apiKey,
  filePath,
  baseURL,
  fileName,
  allowedAddresses
}: {
  apiKey: string;
  filePath: string;
  baseURL?: string;
  fileName?: string;
  allowedAddresses?: string[] | null;
}): Promise<MistralFileUploadResponse>;
declare function getSignedUrl({
  apiKey,
  fileId,
  expiry,
  baseURL,
  allowedAddresses,
  contentProtected
}: {
  apiKey: string;
  fileId: string;
  expiry?: number;
  baseURL?: string;
  allowedAddresses?: string[] | null;
  contentProtected?: boolean;
}): Promise<MistralSignedUrlResponse>;
/**
* @param {Object} params
* @param {string} params.apiKey
* @param {string} params.url - The document or image URL
* @param {string} [params.documentType='document_url'] - 'document_url' or 'image_url'
* @param {string} [params.model]
* @param {string} [params.baseURL]
* @returns {Promise<OCRResult>}
*/
declare function performOCR({
  url,
  apiKey,
  model,
  baseURL,
  documentType,
  allowedAddresses,
  contentProtected
}: {
  url: string;
  apiKey: string;
  model?: string;
  baseURL?: string;
  documentType?: "document_url" | "image_url";
  allowedAddresses?: string[] | null;
  contentProtected?: boolean;
}): Promise<OCRResult>;
/**
* Deletes a file from Mistral API
* @param params Delete parameters
* @param params.fileId The file ID to delete
* @param params.apiKey Mistral API key
* @param params.baseURL Mistral API base URL
* @returns Promise that resolves when the file is deleted
*/
declare function deleteMistralFile({
  fileId,
  apiKey,
  baseURL,
  allowedAddresses,
  contentProtected
}: {
  fileId: string;
  apiKey: string;
  baseURL?: string;
  allowedAddresses?: string[] | null;
  contentProtected?: boolean;
}): Promise<void>;
/**
* Uploads a file to the Mistral OCR API and processes the OCR result.
*
* @param params - The params object.
* @param params.req - The request object from Express. It should have a `user` property with an `id`
*                       representing the user
* @param params.file - The file object, which is part of the request. The file object should
*                                     have a `mimetype` property that tells us the file type
* @param params.loadAuthValues - Function to load authentication values
* @returns - The result object containing the processed `text` and `images` (not currently used),
*                       along with the `filename` and `bytes` properties.
*/
declare const uploadMistralOCR: (context: OCRContext) => Promise<MistralOCRUploadResult>;
/**
* Use Azure Mistral OCR API to processe the OCR result.
*
* @param params - The params object.
* @param params.req - The request object from Express. It should have a `user` property with an `id`
*                       representing the user
* @param params.appConfig - Application configuration object
* @param params.file - The file object, which is part of the request. The file object should
*                                     have a `mimetype` property that tells us the file type
* @param params.loadAuthValues - Function to load authentication values
* @returns - The result object containing the processed `text` and `images` (not currently used),
*                       along with the `filename` and `bytes` properties.
*/
declare const uploadAzureMistralOCR: (context: OCRContext) => Promise<MistralOCRUploadResult>;
/**
* Use Google Vertex AI Mistral OCR API to process the OCR result.
*
* @param params - The params object.
* @param params.req - The request object from Express. It should have a `user` property with an `id`
*                       representing the user
* @param params.appConfig - Application configuration object
* @param params.file - The file object, which is part of the request. The file object should
*                                     have a `mimetype` property that tells us the file type
* @param params.loadAuthValues - Function to load authentication values
* @returns - The result object containing the processed `text` and `images` (not currently used),
*                       along with the `filename` and `bytes` properties.
*/
declare const uploadGoogleVertexMistralOCR: (context: OCRContext) => Promise<MistralOCRUploadResult>;
//#endregion
//#region src/files/ocr.d.ts
declare function loadOCRConfig(config?: TCustomConfig["ocr"]): TCustomConfig["ocr"] | undefined;
//#endregion
//#region src/files/parse.d.ts
/**
* Extracts the image basename from a given URL.
*
* @param urlString - The URL string from which the image basename is to be extracted.
* @returns The basename of the image file from the URL.
* Returns an empty string if the URL does not contain a valid image basename.
*/
declare function getImageBasename(urlString: string): string;
/**
* Extracts the basename of a file from a given URL.
*
* @param urlString - The URL string from which the file basename is to be extracted.
* @returns The basename of the file from the URL.
* Returns an empty string if the URL parsing fails.
*/
declare function getFileBasename(urlString: string): string;
//#endregion
//#region src/files/preflight.d.ts
declare const MAX_FILTERABLE_TEXT_BYTES: number;
interface UploadPolicyFile {
  readonly originalname?: string;
  readonly mimetype?: string;
  readonly path?: string;
  readonly size?: number;
}
interface UploadContentPolicyInput {
  readonly filters?: FiltersConfig;
  readonly file?: UploadPolicyFile;
  readonly endpoint?: string;
  readonly toolResource?: string;
  readonly fileConfig: FileConfig;
  readonly ocrConfigured: boolean;
  readonly ragConfigured: boolean;
  readonly rawFileMode?: "materialize" | "opaque";
  readonly readFile?: (path: string) => Promise<Buffer>;
}
/** Applies upload policy before any provider, parser, or persistence work starts. */
declare function assertUploadContentAllowed(input: UploadContentPolicyInput): Promise<void>;
//#endregion
//#region src/files/upload/routing.d.ts
/** The subset of an agent record this module reads. */
interface UploadAgent {
  provider?: string;
}
/** The subset of upload metadata that determines where a file is routed. */
interface UploadMetadata {
  endpoint?: string;
  agent_id?: string;
  tool_resource?: string | null;
  /** Azure carries native documents only through the Responses API. Multipart form data
  *  carries it as a string, so it arrives as "true" rather than a boolean. */
  useResponsesApi?: boolean | string;
}
/** The uploaded file fields routing depends on. */
interface UploadFile {
  mimetype: string;
  originalname?: string;
}
type GetUploadAgent = (params: {
  id: string;
}) => Promise<UploadAgent | null>;
/**
* Reads the upload's agent once per request. Routing, authorization and processing each
* need it, and this runs before any bytes are handled, so repeating the read adds a round
* trip to every upload.
*/
declare function resolveUploadAgent(req: ServerRequest, agentId: string | undefined, getAgent: GetUploadAgent): Promise<UploadAgent | null>;
/**
* Agent uploads carry endpoint `agents`; the agent's own provider governs both the file
* configuration used for validation and the delivery-path routing.
*
* An assistants upload has its own pipeline and never files anything against an agent, so
* an `agent_id` on one names a record the request has no business reading: the agent
* authorization gate does not run for assistants, and the provider it resolved would
* still shape the validation errors the caller sees.
*/
declare function resolveUploadEndpoint({
  req,
  metadata,
  getAgent
}: {
  req: ServerRequest;
  metadata: UploadMetadata;
  getAgent: GetUploadAgent;
}): Promise<string | undefined>;
/**
* The destination this upload will actually be processed under. A unified upload names no
* tool resource but is promoted to a text context when routing sends it there, and both
* the content preflight and the image route have to agree with the processing path about
* where the file is going.
*/
declare function resolveEffectiveToolResource({
  req,
  metadata,
  getAgent
}: {
  req: ServerRequest;
  metadata: UploadMetadata;
  getAgent: GetUploadAgent;
}): Promise<string | undefined>;
//#endregion
//#region src/files/upload/diagnostics.d.ts
/**
* Warns about delivery settings that keep a file off the model path. Routing a type to
* `none` is a legitimate choice, but a silent one: uploads still succeed and only a file
* tool can reach them, so an operator who set it by accident has nothing to notice. A
* `none` fallback is the louder case, since it covers every type no override names.
*/
declare function warnOnUnreachableDeliveryPaths(appConfig?: Pick<AppConfig, "fileConfig">): void;
//#endregion
//#region src/files/upload/fallback.d.ts
declare const UPLOAD_FALLBACK_TEXT_PLANS: {
  readonly documentParser: "document_parser";
  readonly nativeText: "native_text";
};
type UploadFallbackTextPlan = (typeof UPLOAD_FALLBACK_TEXT_PLANS)[keyof typeof UPLOAD_FALLBACK_TEXT_PLANS];
interface UploadFallbackTextRoute {
  /** The route the upload resolved to. */
  deliveryPath: TDefaultLLMDeliveryPath;
  /** Whether the user chose the destination, by tool resource or the legacy chooser. */
  destinationChosen: boolean;
  /** Whether the upload attaches to a message; an agent's own tool files never reach a prompt. */
  isMessageAttachment: boolean;
  mimeType: string;
  /** The upload endpoint's file config, which opts in through `textFallbackWithoutTools`. */
  endpointConfig?: Pick<EndpointFileConfig, "textFallbackWithoutTools">;
}
interface ExtractedText {
  readonly text?: string | null;
}
/** The built-in readers fallback text comes from. */
interface UploadFallbackTextExtractors {
  parseDocument: (params: {
    file: Express.Multer.File;
  }) => Promise<ExtractedText>;
  parseTextNative: (file: Express.Multer.File) => Promise<ExtractedText>;
}
/**
* Which built-in extractor stores text for an upload left to tools, or `null` when none runs.
*
* Where the endpoint enables `textFallbackWithoutTools`, a turn that runs no tool able to read a
* `none`-routed file delivers this text instead (`resolveTurnLLMDeliveryPath`). Only an inferred
* route on a message attachment qualifies: a turn never re-resolves a destination the user chose,
* and files kept on an agent's tool resources never reach a prompt. A file filed
* under a tool that reads it still gets text, because a later turn may run without that tool: a
* handoff agent, or the same agent after its tools or grants change. Only built-in extractors
* run, so a file meant for a tool never costs a RAG or OCR call.
*/
declare function getUploadFallbackTextPlan(route: UploadFallbackTextRoute): UploadFallbackTextPlan | null;
/**
* Text a turn without a reading tool can fall back to for this upload, when one applies.
*
* Reads the upload before storage can move it, with the extractor its plan names. Best effort by
* design: an extractor failure, text a policy cannot inspect, an overrun of the storage cap, or a
* content finding leaves the upload without fallback text rather than refusing a file whose
* primary reader is a tool. Text that is delivered later is inspected again as model-bound
* content.
*/
declare function resolveUploadFallbackText({
  file,
  fileId,
  filters,
  extractors,
  ...route
}: Omit<UploadFallbackTextRoute, "mimeType"> & {
  file: Express.Multer.File;
  fileId: string;
  filters?: FiltersConfig;
  extractors?: UploadFallbackTextExtractors;
}): Promise<string | undefined>;
//#endregion
//#region src/files/rag.d.ts
interface DeleteRagFileParams {
  /** The user ID. Required for authentication. If not provided, the function returns false and logs an error. */
  userId: string;
  /** The file object. Must have `embedded` and `file_id` properties. */
  file: {
    file_id: string;
    embedded?: boolean;
  };
}
/**
* Deletes embedded document(s) from the RAG API.
* This is a shared utility function used by all file storage strategies
* (S3, Azure, Firebase, Local) to delete RAG embeddings when a file is deleted.
*
* @param params - The parameters object.
* @param params.userId - The user ID for authentication.
* @param params.file - The file object. Must have `embedded` and `file_id` properties.
* @returns Returns true if deletion was successful or skipped, false if there was an error.
*/
declare function deleteRagFile({
  userId,
  file
}: DeleteRagFileParams): Promise<boolean>;
//#endregion
//#region src/files/regexEngine.d.ts
/**
* Route admin-configured file-config MIME patterns through a linear-time engine (RE2) on the
* server so a catastrophic-backtracking pattern cannot ReDoS the event loop when tested against
* an uploaded file's Content-Type. Call once at server startup, before any upload is handled.
* Browser builds keep the native compiler, so no regex engine is added to the client bundle.
*/
declare function configureFileConfigRegexEngine(): void;
//#endregion
//#region src/files/sse.d.ts
declare function shouldUseUploadSse(req: Request): boolean;
interface UploadSseStream {
  /** Emits the successful upload payload as an `event:data` message. */
  sendData: <T>(data: T) => void;
  /** Emits a failure payload as an `event:error` message. */
  sendError: <T>(data: T) => void;
  /** Emits the terminal `event:close` message, stops the heartbeat, and ends the response. */
  close: () => void;
}
interface UploadPolicyErrorContext {
  readonly tempFileId?: string | null;
  readonly toolResource?: string | null;
}
declare function sendUploadPolicyError(res: Response$2, sseStream: UploadSseStream | null | undefined, error: unknown, context?: UploadPolicyErrorContext): boolean;
/**
* Opens a keep-alive SSE stream for a file-upload response: sends the SSE headers, starts a
* heartbeat interval so proxies/clients don't time out the connection during long-running
* uploads, and stops the heartbeat when the client disconnects early.
*
* Callers must only invoke this once all synchronous validation and permission checks that
* might still need to send a normal (non-SSE) response have already passed — once the headers
* are flushed here, the HTTP status is committed to 200 and can no longer be changed.
*/
declare function startUploadSseStream(res: Response$2): UploadSseStream;
/**
* Sends a successful upload response, either as an SSE `event:data` message (when an upload
* stream is active) or as a plain JSON response.
*/
declare function sendUploadSuccess<T extends object>(res: Response$2, sseStream: UploadSseStream | null | undefined, message: string, result: T): void;
//#endregion
//#region src/files/sweep.d.ts
type ExpiredFile = {
  file_id: string;
  source?: string;
  user?: string | {
    toString?: () => string;
  };
  tenantId?: string;
};
type SweepRequest = {
  baseUrl: string;
  originalUrl: string;
  path: string;
  method: string;
  headers: Record<string, unknown>;
  query: Record<string, unknown>;
  params: Record<string, unknown>;
  config?: AppConfig;
  body: {
    endpoint: string;
    version: string;
  };
  user: {
    id: string;
    tenantId?: string;
  };
};
type SweepLogger = {
  info: (message: string) => void;
  warn: (message: string) => void;
  error: (message: string, error?: unknown) => void;
};
type SweepDependencies = {
  getExpiredFiles: (limit: number) => Promise<ExpiredFile[] | null | undefined>;
  processDeleteRequest: (params: {
    req: SweepRequest;
    files: ExpiredFile[];
  }) => Promise<{
    deletedFileIds: string[];
    failedFileIds: string[];
  }>;
  incrementFileDeletionAttempts: (file_id: string) => Promise<number>;
  deferExpiredFile: (file_id: string, deletionRetryAt: Date) => Promise<void>;
  logger: SweepLogger;
};
type StartSweepDependencies = {
  sweepExpiredFiles: (options?: ExpiredFileSweepOptions) => Promise<ExpiredFileSweepResult>;
  runAsSystem: <T>(fn: () => Promise<T>) => Promise<T>;
  isLeader: () => Promise<boolean>;
  logger: SweepLogger;
};
type ExpiredFileSweepOptions = {
  appConfig?: AppConfig;
  limit?: number;
  loadAppConfig?: () => Promise<AppConfig | undefined>;
};
type ExpiredFileSweepResult = {
  scanned: number;
  deleted: number;
  failed: number;
};
declare function getFileRetentionSweepInterval(interval?: string | undefined): number;
/**
* Consecutive failures after which a file is parked rather than retried on
* the ordinary ladder.
*
* Lower it when working through a large backlog: every attempt costs a slot
* in the bounded batch, so N stranded files take N × this many passes to
* settle before the queue frees up for files expiring behind them.
*/
declare function getFileRetentionMaxAttempts(maxAttempts?: string | undefined): number;
/**
* Backoff before the next attempt, doubling from one sweep interval up to a
* day. `attempts` is the file's failure count including the one just
* recorded, so the first retry lands on the following sweep.
*
* A file that exhausts `maxAttempts` is parked for a month instead. Parking
* rather than excluding is what keeps this safe against record reuse: a
* code-output row is repurposed for a repeated `(filename, conversationId)`
* and keeps fields it was not asked to change, so state that survived into
* a new lifecycle would strand a different object than the one it was
* recorded against. A deadline can only delay that object; a flag would
* lose it.
*
* The base tracks the configured interval so the schedule stays meaningful
* at any cadence — a fixed hour would be inert for the first three attempts
* of a six-hour sweep, and would skip twelve passes of a five-minute one.
* The floor keeps a pathologically short interval from spending the whole
* budget on a transient outage.
*/
declare function getExpiredFileRetryDelay(attempts: number, maxAttempts: number): number;
declare function getExpiredFileEndpoint(source?: string): string;
declare function hasExpiredFileEndpointConfig(appConfig: AppConfig | undefined, source?: string): boolean;
declare function getConfiguredExpiredFileAssistantVersion({
  appConfig,
  source,
  endpoint
}: {
  appConfig?: AppConfig;
  source?: string;
  endpoint: string;
}): unknown;
declare function getExpiredFileAssistantVersion({
  appConfig,
  source,
  endpoint
}: {
  appConfig?: AppConfig;
  source?: string;
  endpoint: string;
}): string;
declare function createExpiredFileSweepRequest({
  appConfig,
  file,
  userId
}: {
  appConfig?: AppConfig;
  file: ExpiredFile;
  userId: string;
}): SweepRequest;
declare function resolveExpiredFileSweepConfig({
  appConfig,
  file,
  loadAppConfig
}: {
  appConfig?: AppConfig;
  file: ExpiredFile;
  loadAppConfig?: () => Promise<AppConfig | undefined>;
}): Promise<AppConfig | undefined>;
declare function sweepExpiredFiles({
  appConfig,
  limit,
  loadAppConfig
}: ExpiredFileSweepOptions | undefined, {
  getExpiredFiles,
  processDeleteRequest,
  incrementFileDeletionAttempts,
  deferExpiredFile,
  logger
}: SweepDependencies): Promise<ExpiredFileSweepResult>;
/** Joins configuration readiness with distributed or primary-assigned ownership. */
declare function createClusteredFileSweep(distributed: boolean, start: (options: ExpiredFileSweepOptions) => NodeJS.Timeout | null): {
  configure: (options: ExpiredFileSweepOptions) => void;
  assign: () => void;
};
declare function startExpiredFileSweep(options: ExpiredFileSweepOptions | undefined, {
  sweepExpiredFiles,
  runAsSystem,
  isLeader,
  logger
}: StartSweepDependencies): NodeJS.Timeout | null;
//#endregion
//#region src/files/usage.d.ts
/** Cap per usage touch, mirroring the composer's practical attachment limit. */
declare const FILES_USAGE_MAX_IDS: number;
/**
* Baseline lifetime a held upload gets, measured from its upload time. Covers
* the span a queued attachment spends before a run can even pause: upload,
* enqueue, and the run itself reaching an approval point.
*/
declare const FILES_USAGE_BASE_HOLD_MS: number;
/**
* Queued runs a held attachment is assumed to wait behind. The queue is
* unbounded, so no multiple is provably sufficient; this is the depth the
* ceiling covers before a still-queued attachment can lapse.
*/
declare const FILES_USAGE_QUEUED_RUN_ALLOWANCE: number;
interface FilesUsageHold {
  /** Granted from now, so an actively draining queue keeps renewing. */
  renewMs: number;
  /** Hard ceiling from upload time, so renewals converge instead of walking. */
  maxLifetimeMs: number;
}
/**
* Resolves the hold window for one touch.
*
* A queued message legitimately outlives the upload window when a run pauses
* for approval, and that pause is bounded by the deployment's configured
* approval window (`endpoints.agents.checkpointer.ttl`), which has no upper
* limit. Deeper queues wait behind several such runs, since the drain sends
* one item per run completion.
*
* Hence two numbers rather than one. `renewMs` covers a single run's wait and
* is granted from now, so a queue that is still draining re-asserts it at each
* transition. `maxLifetimeMs` is measured from the immutable upload time and
* caps every renewal, so repeated touches converge on a fixed ceiling instead
* of advancing a window per call. An abandoned queue therefore lapses one
* `renewMs` after its last touch rather than surviving to the ceiling.
*
* @param approvalTtlMs - Configured approval window; falsy means none configured
*/
declare function resolveFilesUsageHold(approvalTtlMs?: number): FilesUsageHold;
interface FilesUsageUser {
  id?: string;
  tenantId?: string;
}
interface FilesUsageBody {
  file_ids?: unknown;
}
/** HTTP-shaped outcome the thin route wrapper serializes verbatim. */
interface FilesUsageResult {
  status: number;
  body: Record<string, unknown>;
}
interface FilesUsageDeps {
  /** Owner-scoped TTL hold (`db.extendFilesTTL`-shaped). */
  extendFilesTTL: (fileIds: string[], hold: FilesUsageHold, owner: {
    user: string;
    tenantId?: string | null;
  }) => Promise<number>;
  /** Configured approval window, so the hold outlasts a paused run. */
  approvalTtlMs?: number;
}
/**
* Owner-scoped TTL hold for attachments entering a client-side queue: a
* queued message can outlive the upload window (long run, approval pause),
* so holding at queue time stops the TTL from reaping files the drain will
* send.
*
* A hold, not a release. The client queue is ephemeral browser state, so a
* closed tab or a cleared queue leaves nothing referencing these files, and
* clearing the TTL outright would strand them in storage permanently. The
* hold only ever widens, and every renewal is capped against the file's
* upload time, so replaying it converges on a ceiling instead of advancing
* indefinitely; the real release happens at send, where `updateFilesUsage`
* marks the files used against an actual message.
*
* The window tracks the deployment's configured approval TTL so a queue
* waiting on a paused run cannot be reaped while that approval is live.
*
* Best-effort 200: ids that do not resolve to a held file are not errors
* (they may be already-sent files, or not owned).
*/
declare function handleFilesUsageRequest(user: FilesUsageUser, body: FilesUsageBody, deps: FilesUsageDeps): Promise<FilesUsageResult>;
//#endregion
//#region src/files/validation.d.ts
interface ValidationResult {
  isValid: boolean;
  error?: string;
}
interface PDFValidationResult {
  isValid: boolean;
  error?: string;
}
interface VideoValidationResult {
  isValid: boolean;
  error?: string;
}
interface AudioValidationResult {
  isValid: boolean;
  error?: string;
}
interface ImageValidationResult {
  isValid: boolean;
  error?: string;
}
declare function validatePdf(pdfBuffer: Buffer, fileSize: number, provider: Providers, configuredFileSizeLimit?: number, model?: string): Promise<PDFValidationResult>;
/**
* Validates a document against Bedrock size limits. The default limit is 4.5 MB,
* but Claude 4+ (PDF) and Nova (PDF/DOCX) models are exempt per AWS docs.
* When exempt, falls back to a 32 MB request-level limit as a reasonable upper bound.
* @param fileSize - The file size in bytes
* @param mimeType - The MIME type of the document
* @param fileBuffer - The file buffer (used for PDF header validation)
* @param configuredFileSizeLimit - Optional configured file size limit from fileConfig (in bytes)
* @param model - Optional Bedrock model identifier for model-specific limit exceptions
* @returns Promise that resolves to validation result
*/
declare function validateBedrockDocument(fileSize: number, mimeType: string, fileBuffer?: Buffer, configuredFileSizeLimit?: number, model?: string): Promise<ValidationResult>;
/**
* Validates video files for different providers
* @param videoBuffer - The video file as a buffer
* @param fileSize - The file size in bytes
* @param provider - The provider to validate for
* @param configuredFileSizeLimit - Optional configured file size limit from fileConfig (in bytes)
* @returns Promise that resolves to validation result
*/
declare function validateVideo(videoBuffer: Buffer, fileSize: number, provider: Providers, configuredFileSizeLimit?: number): Promise<VideoValidationResult>;
/**
* Validates audio files for different providers
* @param audioBuffer - The audio file as a buffer
* @param fileSize - The file size in bytes
* @param provider - The provider to validate for
* @param configuredFileSizeLimit - Optional configured file size limit from fileConfig (in bytes)
* @returns Promise that resolves to validation result
*/
declare function validateAudio(audioBuffer: Buffer, fileSize: number, provider: Providers, configuredFileSizeLimit?: number): Promise<AudioValidationResult>;
/**
* Validates image files for different providers
* @param imageBuffer - The image file as a buffer
* @param fileSize - The file size in bytes
* @param provider - The provider to validate for
* @param configuredFileSizeLimit - Optional configured file size limit from fileConfig (in bytes)
* @returns Promise that resolves to validation result
*/
declare function validateImage(imageBuffer: Buffer, fileSize: number, provider: Providers | string, configuredFileSizeLimit?: number): Promise<ImageValidationResult>;
//#endregion
//#region src/files/text.d.ts
/**
* Attempts to parse text using RAG API, falls back to native text parsing.
* @param params - The parameters object
* @param params.req - The Express request object
* @param params.file - The uploaded file
* @param params.file_id - The file ID
* @param params.allowNativeFallback - When false, throw instead of falling back to native parsing
*   if the RAG API is unavailable. Callers handling document types (docx/pdf/etc.) set this so a
*   RAG outage can be routed to the built-in document parser rather than degraded to raw bytes.
* @returns
*/
declare function parseText({
  req,
  file,
  file_id,
  allowNativeFallback
}: {
  req: ServerRequest;
  file: Express.Multer.File;
  file_id: string;
  allowNativeFallback?: boolean;
}): Promise<{
  text: string;
  bytes: number;
  source: string;
}>;
/**
* Native JavaScript text parsing fallback
* Simple text file reading - complex formats handled by RAG API
* @param file - The uploaded file
* @returns
*/
declare function parseTextNative(file: Express.Multer.File): Promise<{
  text: string;
  bytes: number;
  source: string;
}>;
//#endregion
//#region src/images/authorization.d.ts
type Principal$2 = {
  principalType: PrincipalType;
  principalId?: string | Types.ObjectId;
};
type ImageUser = {
  role?: string | null;
  tenantId?: string;
  idOnTheSource?: string | null;
  avatar?: string | null;
};
type AssistantConfig = {
  endpoint: string;
  privateAssistants?: boolean;
  supportedIds?: string[];
  excludedIds?: string[];
};
type AssistantImageRecord = Pick<IAssistant, "_id" | "assistant_id"> & {
  endpoint?: string;
};
interface ImageAuthorizationDeps {
  parseCookies: (cookieHeader: string) => Record<string, string | undefined>;
  isOpenIdReuseEnabled: () => boolean;
  getBasePath: () => string;
  findSession: (query: {
    userId: string;
    refreshToken: string;
  }) => Promise<unknown | null>;
  getUserById: (userId: string, select: string) => Promise<ImageUser | null>;
  getAgent: (query: FilterQuery<IAgent>, projection?: ProjectionType<IAgent>) => Promise<Pick<IAgent, "_id"> | null>;
  getAssistant: (query: AssistantQuery, projection?: ProjectionType<IAssistant>) => Promise<AssistantImageRecord | null>;
  getImageConfig?: (params: {
    userId: string;
    user: ImageUser;
  }) => Promise<{
    secureImageLinks?: boolean;
    assistantEndpoints?: AssistantConfig[];
  }>;
  getUserPrincipals: (params: {
    userId: string;
    role?: string | null;
    idOnTheSource?: string | null;
  }) => Promise<Principal$2[]>;
  hasCapabilityForPrincipals: (params: {
    principals: Principal$2[];
    capability: SystemCapability;
    tenantId?: string;
  }) => Promise<boolean>;
  hasPermission: (principals: Principal$2[], resourceType: ResourceType, resourceId: string | Types.ObjectId, permissionBit: number) => Promise<boolean>;
}
interface ImageAuthorizationOptions {
  secureImageLinks?: boolean;
  assistantEndpoints?: AssistantConfig[];
}
declare function createImageAuthorizationMiddleware(options: ImageAuthorizationOptions, deps: ImageAuthorizationDeps): RequestHandler;
//#endregion
//#region src/storage/cloudfront/crud.d.ts
interface CloudFrontURLParams extends GetURLParams {
  sign?: boolean;
}
/**
* Get CloudFront URL for a file.
* @param sign - If true, returns a signed URL. Caller (strategy) decides based on config.
*/
declare function getCloudFrontURL({
  userId,
  fileName,
  basePath,
  tenantId,
  storageRegion,
  includeRegionInPath,
  useInlinePath,
  sign
}: CloudFrontURLParams): Promise<string>;
/** Save buffer to S3 and return CloudFront URL. */
declare function saveBufferToCloudFront(params: SaveBufferParams & {
  sign?: boolean;
}): Promise<string>;
/** Save file from URL to S3 and return CloudFront URL. */
declare function saveURLToCloudFront(params: SaveURLParams & {
  sign?: boolean;
}): Promise<string>;
/** Save file from URL to S3 and return CloudFront URL with fetched metadata. */
declare function saveURLToCloudFrontWithMetadata(params: SaveURLParams & {
  sign?: boolean;
}): Promise<SaveURLResult>;
/** Upload file to S3 and return CloudFront URL. */
declare function uploadFileToCloudFront(params: UploadFileParams & {
  sign?: boolean;
}): Promise<UploadResult>;
/** Delete file from S3 and optionally invalidate CloudFront cache. */
declare function deleteFileFromCloudFront(req: ServerRequest, file: TFile): Promise<void>;
/** Get file stream from S3 storage. */
declare function getCloudFrontFileStream(req: ServerRequest, filePath: string, options?: {
  signal?: AbortSignal;
}): Promise<Readable>;
/** Get a signed CloudFront URL for an authorized file download. */
declare function getCloudFrontDownloadURL({
  file,
  customFilename,
  contentType
}: DownloadURLParams): Promise<string>;
//#endregion
//#region src/storage/s3/crud.d.ts
interface S3KeyBase {
  basePath: string;
  userId: string;
  fileName: string;
  includeRegionInPath?: boolean;
  useInlinePath?: boolean;
}
interface S3KeyParts extends S3KeyBase {
  storageRegion?: string;
  tenantId?: string;
  inlinePathPrefix?: string;
}
interface S3KeyOptions extends S3KeyBase {
  tenantId?: string | null;
  storageRegion?: string | null;
}
declare function getS3Key(options: S3KeyOptions): string;
declare function getS3Key(basePath: string, userId: string, fileName: string, tenantId?: string | null): string;
declare const parseS3Key: (key: string) => S3KeyParts | null;
declare function getStorageMetadataForKey(key: string): Pick<SaveURLResult, "storageKey" | "storageRegion">;
declare function resolveStoredS3Key(file: StoredFileRef): string;
declare function getS3URL({
  userId,
  fileName,
  basePath,
  customFilename,
  contentType,
  tenantId,
  storageRegion,
  includeRegionInPath,
  useInlinePath
}: GetURLParams): Promise<string>;
declare function saveBufferToS3({
  userId,
  buffer,
  fileName,
  basePath,
  tenantId,
  storageRegion,
  includeRegionInPath,
  useInlinePath,
  urlBuilder
}: SaveBufferParams & {
  urlBuilder?: UrlBuilder;
}): Promise<string>;
declare function saveURLToS3WithMetadata({
  userId,
  URL,
  fileName,
  basePath,
  tenantId,
  storageRegion,
  includeRegionInPath,
  useInlinePath,
  urlBuilder
}: SaveURLParams & {
  urlBuilder?: UrlBuilder;
}): Promise<SaveURLResult>;
declare function saveURLToS3(params: SaveURLParams & {
  urlBuilder?: UrlBuilder;
}): Promise<string>;
declare function extractKeyFromS3Url(fileUrlOrKey: string): string;
declare function deleteFileFromS3(req: ServerRequest, file: TFile): Promise<void>;
declare function uploadFileToS3({
  req,
  file,
  file_id,
  basePath,
  tenantId,
  storageRegion,
  includeRegionInPath,
  useInlinePath,
  urlBuilder
}: UploadFileParams & {
  urlBuilder?: UrlBuilder;
}): Promise<UploadResult>;
declare function getS3FileStream(_req: ServerRequest, filePath: string, {
  signal
}?: {
  signal?: AbortSignal;
}): Promise<Readable>;
declare function getS3DownloadURL({
  file,
  customFilename,
  contentType
}: DownloadURLParams): Promise<string>;
declare function needsRefresh(signedUrl: string, bufferSeconds: number): boolean;
declare function getNewS3URL(currentURL: string, storageKey?: string | null): Promise<string | undefined>;
declare function refreshS3FileUrls(files: TFile[] | null | undefined, batchUpdateFiles: BatchUpdateFn, bufferSeconds?: number): Promise<TFile[]>;
declare function refreshS3Url$1(fileObj: S3FileRef, bufferSeconds?: number): Promise<string>;
//#endregion
//#region src/storage/images.d.ts
interface ImageServiceDeps {
  resizeImageBuffer: (buffer: Buffer, resolution: string, endpoint: string) => Promise<{
    buffer: Buffer;
    width: number;
    height: number;
  }>;
  updateUser: (userId: string, update: {
    avatar: string;
  }) => Promise<IUser | null>;
  updateFile: (params: {
    file_id: string;
  }) => Promise<TFile>;
}
interface ImageServiceConfig {
  /** If true, appends ?manual=... to avatar URLs (Firebase/Azure behavior) */
  appendManualParam?: boolean;
}
/**
* Unified image service for cloud storage strategies.
* Handles image uploads, URL preparation, and avatar processing
* via an injected `saveBuffer` function, enabling any storage backend
* (S3, CloudFront, Azure, Firebase, etc.) without subclassing.
*/
declare class ImageService {
  private saveBuffer;
  private deps;
  private config;
  /**
  * @param saveBuffer - Strategy-specific function that persists a buffer and returns a download URL.
  * @param deps - External dependencies (resize, user/file update callbacks).
  * @param config - Optional per-strategy configuration.
  */
  constructor(saveBuffer: SaveBufferFn, deps: ImageServiceDeps, config?: ImageServiceConfig);
  /**
  * Resizes, converts, and uploads an image file to cloud storage.
  * Deletes the local temp file after a successful upload.
  */
  uploadImage({
    req,
    file,
    file_id,
    endpoint,
    resolution,
    basePath
  }: UploadImageParams): Promise<ImageUploadResult>;
  prepareImageURL(file: {
    file_id: string;
    filepath: string;
  }): Promise<[TFile, string]>;
  /**
  * Processes and uploads an avatar image.
  * Detects GIF vs PNG, generates a timestamped filename, and optionally
  * persists the URL to the user record when `manual` is `'true'`.
  */
  processAvatar({
    buffer,
    userId,
    manual,
    agentId,
    basePath,
    tenantId
  }: ProcessAvatarParams): Promise<string>;
}
//#endregion
//#region src/storage/avatar.d.ts
type AvatarConfig = {
  fileStrategy?: string | null;
  fileStrategies?: {
    avatar?: string | null;
    default?: string | null;
  } | null;
} | null | undefined;
/**
* Resolves the storage strategy used for avatars. `fallbackStrategy` is usually
* `process.env.CDN_PROVIDER`; undefined is valid and falls back to local storage.
*/
declare function getAvatarFileStrategy(appConfig: AvatarConfig, fallbackStrategy?: string | null): string;
declare function getAvatarSaveParams<T extends SaveBufferParams>(fileStrategy: string, params: T): T;
//#endregion
//#region src/storage/metadata.d.ts
type StorageMetadataInput = {
  filepath?: string | null;
  source?: string | null;
  storageKey?: string | null;
  storageRegion?: string | null;
};
declare function getStorageMetadata$1({
  filepath,
  source,
  storageKey,
  storageRegion
}: StorageMetadataInput): Pick<SaveURLResult, "storageKey" | "storageRegion">;
//#endregion
//#region src/storage/url.d.ts
declare const DEFAULT_REMOTE_FILE_FETCH_TIMEOUT_MS: number;
declare const DEFAULT_REMOTE_FILE_FETCH_MAX_BYTES: number;
type HeaderGetter = {
  get: (name: string) => string | null;
};
type HeaderRecord = Record<string, string | string[] | number | undefined>;
type RemoteFileHeaders = HeaderGetter | HeaderRecord;
declare function getRemoteFileFetchTimeoutMs(): number;
declare function getRemoteFileFetchMaxBytes(): number;
declare function assertRemoteFileURL(input: string): string;
declare function assertRemoteFileContentLength(headers: RemoteFileHeaders, maxBytes?: number): void;
declare function createRemoteFileByteLimitTransform(maxBytes?: number): Transform;
//#endregion
//#region src/tools/format.d.ts
/**
* Filters out duplicate plugins from the list of plugins.
*
* @param plugins The list of plugins to filter.
* @returns The list of plugins with duplicates removed.
*/
declare const filterUniquePlugins: (plugins?: TPlugin[]) => TPlugin[];
/**
* Determines if a plugin is authenticated by checking if all required authentication fields have non-empty values.
* Supports alternate authentication fields, allowing validation against multiple possible environment variables.
*
* @param plugin The plugin object containing the authentication configuration.
* @returns True if the plugin is authenticated for all required fields, false otherwise.
*/
declare const checkPluginAuth: (plugin?: TPlugin) => boolean;
/**
* @param toolkits
* @param toolName
* @returns toolKey
*/
declare function getToolkitKey({
  toolkits,
  toolName
}: {
  toolkits: TPlugin[];
  toolName?: string;
}): string | undefined;
//#endregion
//#region src/tools/discovery.d.ts
declare function isToolModuleFile(filename: string): boolean;
//#endregion
//#region src/tools/protection.d.ts
/** Enforces direct-call tool output before persistence or HTTP disclosure. */
declare function assertDirectToolOutputAllowed(filters: FiltersConfig | undefined, toolId: string, output: unknown): void;
//#endregion
//#region src/tools/registry/schema.d.ts
/** Extended JSON Schema type that includes standard validation keywords */
type ExtendedJsonSchema = {
  type?: "string" | "number" | "integer" | "float" | "boolean" | "array" | "object" | "null";
  enum?: (string | number | boolean | null)[];
  items?: ExtendedJsonSchema;
  properties?: Record<string, ExtendedJsonSchema>;
  required?: string[];
  description?: string;
  additionalProperties?: boolean | ExtendedJsonSchema;
  minLength?: number;
  maxLength?: number;
  minimum?: number;
  maximum?: number;
  minItems?: number;
  maxItems?: number;
  pattern?: string;
  format?: string;
  default?: unknown;
  const?: unknown;
  oneOf?: ExtendedJsonSchema[];
  anyOf?: ExtendedJsonSchema[];
  allOf?: ExtendedJsonSchema[];
  $ref?: string;
  $defs?: Record<string, ExtendedJsonSchema>;
  definitions?: Record<string, ExtendedJsonSchema>;
};
//#endregion
//#region src/tools/registry/definitions.d.ts
interface ToolRegistryDefinition {
  name: string;
  description: string;
  schema: ExtendedJsonSchema;
  description_for_model?: string;
  responseFormat?: "content_and_artifact" | "content";
  toolType: "builtin" | "mcp" | "action" | "custom";
}
/** Google Search tool JSON schema */
declare const googleSearchSchema: ExtendedJsonSchema;
/** DALL-E 3 tool JSON schema */
declare const dalle3Schema: ExtendedJsonSchema;
/** Flux API tool JSON schema */
declare const fluxApiSchema: ExtendedJsonSchema;
/** OpenWeather tool JSON schema */
declare const openWeatherSchema: ExtendedJsonSchema;
/** Wolfram Alpha tool JSON schema */
declare const wolframSchema: ExtendedJsonSchema;
/** Stable Diffusion tool JSON schema */
declare const stableDiffusionSchema: ExtendedJsonSchema;
/** Azure AI Search tool JSON schema */
declare const azureAISearchSchema: ExtendedJsonSchema;
/** Traversaal Search tool JSON schema */
declare const traversaalSearchSchema: ExtendedJsonSchema;
/** Tavily Search Results tool JSON schema */
declare const tavilySearchSchema: ExtendedJsonSchema;
/** File Search tool JSON schema */
declare const fileSearchSchema: ExtendedJsonSchema;
/** Tool definitions registry - maps tool names to their definitions */
declare const toolDefinitions$1: Record<string, ToolRegistryDefinition>;
declare function getToolDefinition(toolName: string): ToolRegistryDefinition | undefined;
declare function getAllToolDefinitions(): ToolRegistryDefinition[];
declare function getToolSchema(toolName: string): ExtendedJsonSchema | undefined;
//#endregion
//#region src/tools/toolkits/gemini.d.ts
declare const geminiToolkit: {
  readonly gemini_image_gen: {
    readonly name: "gemini_image_gen";
    readonly description: string;
    readonly description_for_model: "Use this tool to generate images from text descriptions using Vertex AI Gemini.\n1. Prompts should be detailed and specific for best results.\n2. One image per function call. Create only 1 image per request.\n3. IMPORTANT: When user asks to \"edit\", \"modify\", \"change\", or \"swap\" elements in an existing image:\n   - ALWAYS include the original image ID in the image_ids array\n   - Describe the desired changes clearly in the prompt\n   - The tool will generate a new image based on the original image context + your prompt\n4. IMPORTANT: For editing requests, use DIRECT editing instructions:\n   - User says \"remove the gun\" → prompt should be \"remove the gun from this image\"\n   - User says \"make it blue\" → prompt should be \"make this image blue\"\n   - User says \"add sunglasses\" → prompt should be \"add sunglasses to this image\"\n   - DO NOT reconstruct or modify the original prompt - use the user's editing instruction directly\n   - ALWAYS include the image being edited in image_ids array\n5. OPTIONAL: Use image_ids to provide context images that will influence the generation:\n   - Include any relevant image IDs from the conversation in the image_ids array\n   - These images will be used as visual context/inspiration for the new generation\n   - For \"editing\" requests, always include the image being \"edited\"\n6. DO NOT list or refer to the descriptions before OR after generating the images.\n7. Always mention the image type (photo, oil painting, watercolor painting, illustration, cartoon, drawing, vector, render, etc.) at the beginning of the prompt.\n8. Use aspectRatio to control the shape of the image:\n   - 16:9 or 3:2 for landscape/wide images\n   - 9:16 or 2:3 for portrait/tall images\n   - 21:9 for ultra-wide/cinematic images\n   - 1:1 for square images (default)\n9. Use imageSize to control the resolution: 1K (standard), 2K (high), 4K (maximum quality).\n\nThe prompt should be a detailed paragraph describing every part of the image in concrete, objective detail.";
    readonly schema: ExtendedJsonSchema;
    readonly responseFormat: "content_and_artifact";
  };
};
type GeminiToolkit = typeof geminiToolkit;
//#endregion
//#region src/tools/toolkits/imageContext.d.ts
/**
* Builds tool context string for image generation tools based on available image files.
* @param params - The parameters for building image context
* @param params.imageFiles - Array of image file objects with file_id property
* @param params.toolName - The name of the tool (e.g., 'gemini_image_gen', 'image_edit_oai')
* @param params.contextDescription - Description of what the images are for (e.g., 'image context', 'image editing')
* @returns The tool context string or empty string if no images
*/
declare function buildImageToolContext({
  imageFiles,
  toolName,
  contextDescription
}: {
  imageFiles: Array<{
    file_id: string;
  }>;
  toolName: string;
  contextDescription?: string;
}): string;
//#endregion
//#region src/tools/toolkits/mapping.d.ts
/**
* Maps toolkit keys to additional tool names they contain.
* When a toolkit key appears in an agent's tool list,
* these extra tools should also be included.
*/
declare const toolkitExpansion: {
  readonly image_gen_oai: readonly ["image_edit_oai"];
};
/** Reverse mapping: maps child tool names to their parent toolkit key */
declare const toolkitParent: Readonly<Record<string, string>>;
//#endregion
//#region src/tools/toolkits/oai.d.ts
/** `auto` or `WIDTHxHEIGHT`; the image API enforces which dimensions the configured model supports. */
declare const IMAGE_SIZE_PATTERN = "^(auto|[1-9][0-9]*x[1-9][0-9]*)$";
declare const oaiToolkit: {
  readonly image_gen_oai: {
    readonly name: "image_gen_oai";
    readonly description: string;
    readonly schema: ExtendedJsonSchema;
    readonly responseFormat: "content_and_artifact";
  };
  readonly image_edit_oai: {
    readonly name: "image_edit_oai";
    readonly description: string;
    readonly schema: ExtendedJsonSchema;
    readonly responseFormat: "content_and_artifact";
  };
};
//#endregion
//#region src/tools/toolkits/web.d.ts
/** Builds the web search tool context with citation format instructions. */
declare function buildWebSearchContext(): string;
/** Builds dynamic web search context scoped to the logical turn start time. */
declare function buildWebSearchDynamicContext(now?: string | number | Date): string;
//#endregion
//#region src/tools/definitions.d.ts
interface MCPServerTool {
  function?: {
    name?: string;
    description?: string;
    parameters?: JsonSchemaType$2;
  };
  serverToolName?: string;
}
type MCPServerTools = Record<string, MCPServerTool>;
interface LoadToolDefinitionsParams {
  /** User ID for MCP server tool lookup */
  userId: string;
  /** Agent ID for tool classification */
  agentId: string;
  /** Agent's tool list (tool names/identifiers) */
  tools: string[];
  /** Agent-specific tool options */
  toolOptions?: AgentToolOptions;
  /** Whether deferred tools feature is enabled */
  deferredToolsEnabled?: boolean;
  /** Whether programmatic tool calling is enabled */
  programmaticToolsEnabled?: boolean;
  /** Whether code execution is enabled and requested by this agent */
  codeExecutionEnabled?: boolean;
  codeExecutionContext?: CodeExecutionContext;
  codeEnvironments?: readonly CodeEnvironmentConfig[];
  getAppConfig?: CodeCapabilityConfigLoader;
  /** Agent provider — Gemini/Vertex tool schemas get union-flattened for compatibility */
  provider?: Providers;
  /** Configured server names, used to resolve the tool-key boundary exactly */
  mcpServerNames?: readonly string[];
  /**
  * Configured server names in raw config form. A parsed (normalized) server
  * name resolves back to its raw name for config lookups AND for the
  * `serverName`/`mcpRawServerName` metadata stored on definitions — server
  * instructions and other config-keyed consumers read that field raw.
  */
  rawServerNames?: readonly string[];
  /**
  * Every server the user can reach (operator + user DB), from a COMPLETE
  * collision audit. When a parsed name appears here, it IS a real server —
  * a null tool fetch then means "currently unavailable" (OAuth pending,
  * missing user variables, disconnected) and must NOT fall back to the raw
  * alias, or the aliased server's definitions would be emitted under the
  * unavailable server's names.
  */
  accessibleServerNames?: readonly string[];
}
interface ActionToolDefinition {
  name: string;
  description?: string;
  parameters?: JsonSchemaType$2;
  /** True when the action authenticates via OAuth — its calls may block on an
  *  interactive login prompt, so it must never be dispatched in the background. */
  oauth?: boolean;
}
interface LoadToolDefinitionsDeps {
  /** Gets MCP server tools - first checks cache, then initializes server if needed */
  getOrFetchMCPServerTools: (userId: string, serverName: string) => Promise<MCPServerTools | null>;
  /** Bypasses a non-empty stale catalog when it does not contain a selected tool. */
  refreshMCPServerTools?: (userId: string, serverName: string) => Promise<MCPServerTools | null>;
  /** Checks if a tool name is a known built-in tool */
  isBuiltInTool: (toolName: string) => boolean;
  /** Loads action tool definitions (schemas) from OpenAPI specs */
  getActionToolDefinitions?: (agentId: string, actionToolNames: string[]) => Promise<ActionToolDefinition[]>;
}
interface LoadToolDefinitionsResult {
  toolDefinitions: (ToolDefinition | LCTool$1)[];
  toolRegistry: LCToolRegistry$1;
  hasDeferredTools: boolean;
  /** Both-direction identity aliases for MCP tools whose key spelling changed */
  mcpToolAliases: MCPToolAlias[];
  mcpResolution: {
    expectedToolCount: number;
    resolvedToolCount: number;
  };
  /** Action tool names backed by OAuth — excluded from background dispatch. */
  oauthActionToolNames: string[];
}
/**
* Loads tool definitions without creating tool instances.
* This is the efficient path for event-driven mode where tools are loaded on-demand.
*/
declare function loadToolDefinitions(params: LoadToolDefinitionsParams, deps: LoadToolDefinitionsDeps): Promise<LoadToolDefinitionsResult>;
//#endregion
//#region src/web/web.d.ts
declare function extractWebSearchEnvVars({
  keys,
  config
}: {
  keys: TWebSearchKeys[];
  config: TCustomConfig["webSearch"] | undefined;
}): string[];
declare function getWebSearchInstallEntries({
  auth,
  config
}: {
  auth: Partial<Record<string, string | null>>;
  config: TCustomConfig["webSearch"];
}): [string, string][];
declare function getWebSearchUninstallFields(config: TCustomConfig["webSearch"]): string[];
/**
* Type for web search authentication result
*/
interface WebSearchAuthResult {
  /** Whether all required categories have at least one authenticated service */
  authenticated: boolean;
  /** Authentication type (user_provided or system_defined) by category */
  authTypes: [TWebSearchCategories, AuthType][];
  /** Original authentication values mapped to their respective keys */
  authResult: Partial<TWebSearchConfig>;
}
/**
* Loads and verifies web search authentication values
* @param params - Authentication parameters
* @returns Authentication result
*/
declare function loadWebSearchAuth({
  userId,
  webSearchConfig,
  loadAuthValues,
  throwError
}: {
  userId: string;
  webSearchConfig: TCustomConfig["webSearch"];
  loadAuthValues: (params: {
    userId: string;
    authFields: string[];
    optional?: Set<string>;
    throwError?: boolean;
    failOnOptionalError?: boolean;
  }) => Promise<Record<string, string>>;
  throwError?: boolean;
}): Promise<WebSearchAuthResult>;
//#endregion
//#region src/web/agent.d.ts
interface WebSearchSSRFAgents {
  httpAgent: http$1.Agent;
  httpsAgent: https.Agent;
}
/**
* Connect-time SSRF agents for the web-search tool, which issues its own requests and accepts
* only a shared agent pair.
*
* Redirect hops traverse these agents, so `blockLiteralHosts` covers a redirect to a private
* address whether it is named or a literal, which is what `maxRedirects` would otherwise be
* needed for. A blocked redirect surfaces as `ERR_FR_REDIRECTION_FAILURE` with the `ESSRF`
* message attached, because `follow-redirects` wraps the agent's error.
*
* When a proxy carries the request the proxy resolves the destination, so enforcement there
* belongs to the proxy's egress policy. The proxy endpoint is exempted for the whole pair rather
* than per destination, since one shared pair cannot discriminate: a destination that `NO_PROXY`
* sends direct therefore also carries that exemption.
*/
declare function resolveWebSearchSSRFAgents(authResult: Partial<TWebSearchConfig>, allowedAddresses?: string[] | null): WebSearchSSRFAgents;
//#endregion
//#region src/langfuse/destinations.d.ts
type LangfuseScoreDestination = {
  id?: string;
  name: "central" | "tenant" | "connection";
  baseUrl: string;
  authorization: string; /** Deployment proxy/gateway headers, merged beneath `Authorization`. */
  headers?: Record<string, string>;
};
type LangfuseScoreDestinationOptions = {
  waitForCentralProjectId?: boolean;
  /**
  * Deployments that route traces per tenant may want a turn's spans to reach
  * only the tenant destination. Setting this false drops the central project
  * from resolution without disturbing tenant or connection destinations.
  */
  centralTraceExportEnabled?: boolean;
};
/** Drops the deployment's headers unless this destination is the one configured
*  Langfuse origin — a run can resolve to several destinations, and only the
*  intended one may receive the gateway credential. */
declare function scopeHeadersToDestination(headers: Record<string, string> | undefined, baseUrl: string): Record<string, string> | undefined;
declare function getLangfuseDestinationId(baseUrl: string, projectId: string): string;
/**
* Scores use Langfuse's direct REST API. Multi-tenant score fanout follows the
* collector availability gate used by traces; single-tenant connections send
* directly to their configured destination.
*/
declare function getScoreDestinations(appConfig: AppConfig | undefined, traceId: string, sampled?: boolean, {
  waitForCentralProjectId,
  centralTraceExportEnabled
}?: LangfuseScoreDestinationOptions): Promise<LangfuseScoreDestination[]>;
/**
* Captures the concrete Langfuse projects eligible to receive a generated
* trace. The opaque IDs let later feedback avoid newly configured or replaced
* destinations without persisting credentials on the message.
*/
declare function getLangfuseTraceDestinationIds(appConfig: AppConfig | undefined, traceId: string, sampled?: boolean, {
  centralTraceExportEnabled
}?: Pick<LangfuseScoreDestinationOptions, "centralTraceExportEnabled">): Promise<string[] | undefined>;
/** The trace sampling record a response message stores. */
type LangfuseTraceMessageFields = {
  langfuseSampled?: boolean;
  langfuseDestinationIds?: string[];
  langfuseRunId?: string;
};
/**
* The sampling record a response stores for its run's trace. `runId` names the
* run when it is not the response's own id, as for a failed turn's error row;
* it is then stored too, so feedback and the trace viewer follow that run.
*/
declare function getLangfuseTraceMessageFields(appConfig: AppConfig | undefined, messageId: string, {
  centralTraceExportEnabled,
  runId
}?: Pick<LangfuseScoreDestinationOptions, "centralTraceExportEnabled"> & {
  runId?: string;
}): Promise<{
  langfuseSampled: boolean;
  langfuseDestinationIds?: string[];
  langfuseRunId?: string;
}>;
/**
* The sampling record for a failed turn's error row, which keeps its own id and
* so names the run that failed. A turn that failed before its run was created
* has no trace to name, and a destination lookup that fails leaves the row
* without one rather than failing the error write.
*/
declare function getFailedTurnTraceFields(appConfig: AppConfig | undefined, {
  messageId,
  runId,
  runCreated
}: {
  messageId: string;
  runId?: string | null;
  runCreated: boolean;
}): Promise<LangfuseTraceMessageFields>;
//#endregion
//#region src/langfuse/feedback.d.ts
type LangfuseFeedback = {
  rating?: "thumbsUp" | "thumbsDown";
  tag?: string;
  text?: string;
};
type LangfuseFeedbackMetadata = Record<string, string | number | boolean | null | undefined>;
type SendFeedbackScoreParams = {
  traceId: string;
  sampled?: boolean;
  destinationIds?: string[];
  feedback?: LangfuseFeedback | null;
  metadata?: LangfuseFeedbackMetadata;
  observationId?: string;
  appConfig?: AppConfig;
  /**
  * Must mirror the value used when the trace was generated. `destinationIds`
  * only restricts feedback to destinations that carry a stable id, so a caller
  * that opts out of central export has to re-assert it here for destinations
  * whose project identity is unknown.
  */
  centralTraceExportEnabled?: boolean;
};
declare function sendFeedbackScore({
  traceId,
  sampled,
  destinationIds,
  feedback,
  metadata,
  observationId,
  appConfig,
  centralTraceExportEnabled
}: SendFeedbackScoreParams): Promise<void>;
//#endregion
//#region src/langfuse/policy.d.ts
declare function isLangfuseTenantExportEnabled(): boolean;
declare function isLangfuseCentralMediaUploadDisabled(): boolean;
declare function isLangfuseFanoutEnabled(): boolean;
declare function hasLangfuseEnvCredentials(): boolean;
declare function usesLangfuseMultiTenantRouting(): boolean;
declare function getLangfuseSampleRate(): number;
declare function isLangfuseTracingEnabled(): boolean;
declare function isLangfuseTraceSampled(traceId: string): boolean;
declare function isLangfuseConnectionAvailable(): boolean;
//#endregion
//#region src/traces/types.d.ts
/** One authorized read: the caller has already proven `userId` owns `conversationId`. */
interface TraceQuery {
  userId: string;
  /** The requester's tenant; `undefined` is the tenantless scope, never "any tenant". */
  tenantId?: string;
  conversationId: string;
  appConfig?: AppConfig;
  settings: TResolvedTraceViewerConfig;
  /** Aborts when the client goes away, so an abandoned read stops spending backend quota. */
  signal?: AbortSignal;
}
/**
* The seam between TerraMind's trace routes and a tracing backend. An
* implementation maps its storage into the provider-neutral records, so adding
* a backend is a new reader rather than a branch in the routes or the client.
*/
interface TraceReader {
  /**
  * Whether a trace may exist. Runs alongside the ownership check, so it may
  * read only the requesting user's own data and never the backend's records.
  */
  isAvailable(query: TraceQuery): Promise<TTraceAvailability>;
  listRecords(query: TraceQuery & {
    cursor?: string;
  }): Promise<TTracePage>;
  /** `null` when the record is absent or outside the conversation's traces. */
  /** `messageId` is the turn the list attributed the record to, so only that turn's traces are
  *  looked up; `sourceId` pins the read to the page that listed the record when still readable. */
  getRecord(query: TraceQuery & {
    recordId: string;
    messageId: string;
    sourceId?: string;
  }): Promise<TTraceRecordDetail | null>;
}
declare class TraceReadError extends Error {
  readonly code: TTraceErrorCode;
  constructor(code: TTraceErrorCode, message: string);
}
//#endregion
//#region src/langfuse/reader.d.ts
interface LangfuseTraceReaderDeps {
  getConversationTraceRefs: (input: {
    user: string;
    conversationId: string;
    tenantId?: string;
    messageId?: string;
    through?: {
      messageId: string;
      orderKey: string;
    };
    limit?: number;
  }) => Promise<ConversationTraceRefs>;
  hasSampledTraceMessage: (input: {
    user: string;
    conversationId: string;
    tenantId?: string;
    destinationIds: string[];
  }) => Promise<boolean>;
  /** The projects a read may use; production passes {@link resolveLangfuseReadDestinations}. */
  resolveDestinations: (appConfig?: AppConfig) => Promise<LangfuseScoreDestination[]>;
  fetch: (url: string, init: RequestInit) => Promise<Response>;
  now?: () => number;
}
/**
* The destinations feedback scores use, which carry the read credentials. Never
* waits on the central project lookup: the process starts it at boot, and a read
* must not stall behind a `/api/public/projects` call that `requestTimeoutMs`
* does not govern. Until it resolves, central has no id; availability then asks
* the client to check again rather than answering a definitive no.
*/
declare function resolveLangfuseReadDestinations(appConfig?: AppConfig): Promise<LangfuseScoreDestination[]>;
declare function createLangfuseTraceReader({
  getConversationTraceRefs,
  hasSampledTraceMessage,
  resolveDestinations,
  fetch: fetchImpl,
  now
}: LangfuseTraceReaderDeps): TraceReader;
//#endregion
//#region src/langfuse/trace.d.ts
type TraceRefFields = {
  langfuseSampled?: boolean | null;
  langfuseDestinationIds?: string[] | null;
  langfuseRunId?: string | null;
};
declare function traceIdForMessage(messageId: string): string;
/**
* Replaces a message's trace sampling record with an explicit "never traced".
* A copied or client-authored row carries an id no run was traced under, so the
* source's record would claim a trace the row never produced — for feedback
* scores and the trace viewer alike — while a missing record would let a
* feedback score recompute sampling for a trace that does not exist.
*/
declare function withoutTraceRefs<T extends TraceRefFields>(message: T): Omit<T, keyof TraceRefFields> & {
  langfuseSampled: false;
};
//#endregion
//#region src/langfuse/session.d.ts
interface LangfuseSessionLinkParams {
  config: TCustomConfig["langfuse"];
  conversationId: string;
  userId: string;
  getMessages: MessageMethods["getMessages"];
}
interface LangfuseSession {
  url: string;
  /** Identity of the project `url` opens (see `getLangfuseDestinationId`). */
  destinationId: string;
}
declare function resolveLangfuseSession({
  config,
  conversationId,
  userId,
  getMessages
}: LangfuseSessionLinkParams): Promise<LangfuseSession | null>;
declare function resolveLangfuseSessionUrl(params: LangfuseSessionLinkParams): Promise<string | null>;
//#endregion
//#region src/traces/limiter.d.ts
/**
* Per-user limit on trace reads, which spend the tracing backend's own API
* quota. The ceiling is read per request from `interface.traceViewer`, so a
* config override applies without a restart; must run after config middleware.
*/
declare function createTraceReadLimiter({
  store
}?: {
  /** Shared counter store (e.g. Redis) from the caller; omitted, counts stay in this process. */store?: Store;
}): RequestHandler;
//#endregion
//#region src/traces/handlers.d.ts
type TraceRequest = ServerRequest & Request<{
  conversationId?: string;
  recordId?: string;
}>;
type ConversationOwnership = {
  user?: string | null;
  tenantId?: string | null;
  subagentThread?: object | null;
} | null;
/** Resolves once the response is sent; Express route handlers return nothing. */
type TraceRouteHandler = (req: TraceRequest, res: Response$2) => Promise<void>;
interface TraceHandlers {
  availability: TraceRouteHandler;
  records: TraceRouteHandler;
  record: TraceRouteHandler;
}
interface TraceHandlerDeps {
  reader: TraceReader;
  /** `tenantId: null` is the tenantless scope, as the subagent thread views pass it. */
  getConvoOwnership: (userId: string, conversationId: string, tenantId: string | null) => Promise<ConversationOwnership>;
}
/**
* Trace routes for a user's own conversations. Every response is gated on
* ownership of the conversation; the reader then narrows the backend's session
* to the traces that user's responses produced.
*/
declare function createTraceHandlers({
  reader,
  getConvoOwnership
}: TraceHandlerDeps): TraceHandlers;
//#endregion
//#region src/insights/handlers.d.ts
type InsightsHandlerDeps = {
  isInsightsEnabled: () => boolean;
  getInsights: InsightsMethods["getInsights"];
  getAccessibleAgents: (user: NonNullable<ServerRequest["user"]>) => Promise<TInsightsAgent[]>;
};
type InsightsAccessHandlerDeps = Pick<InsightsHandlerDeps, "isInsightsEnabled" | "getAccessibleAgents">;
declare function createInsightsAccessHandler({
  isInsightsEnabled,
  getAccessibleAgents
}: InsightsAccessHandlerDeps): (req: ServerRequest, res: Response$2) => Promise<void>;
declare function createInsightsHandler({
  isInsightsEnabled,
  getInsights,
  getAccessibleAgents
}: InsightsHandlerDeps): (req: ServerRequest, res: Response$2) => Promise<void>;
//#endregion
//#region src/insights/access.d.ts
type Principal$1 = {
  principalType: PrincipalType;
  principalId?: string | Types.ObjectId;
};
type AgentRecord = {
  _id: Types.ObjectId;
  id: string;
  name?: string;
  tenantId?: string;
};
type InsightsAccessUser = {
  id: string;
  role?: string;
  tenantId?: string;
  idOnTheSource?: string | null;
};
type InsightsAgentAccessDeps = {
  getAgents: (filter: Record<string, unknown>, select?: string | Record<string, number>) => Promise<AgentRecord[]>;
  getUserPrincipals: (input: {
    userId: string;
    role?: string | null;
    idOnTheSource?: string | null;
  }) => Promise<Principal$1[]>;
  hasCapabilityForPrincipals: (input: {
    principals: Principal$1[];
    capability: typeof SystemCapabilities.READ_INSIGHTS;
    tenantId?: string;
  }) => Promise<boolean>;
  findAccessibleResources: (principals: Principal$1[], resourceType: ResourceType, requiredBits: number, resourceIds: Types.ObjectId[]) => Promise<Types.ObjectId[]>;
};
declare function createInsightsAgentAccessResolver(deps: InsightsAgentAccessDeps): (user: InsightsAccessUser) => Promise<TInsightsAgent[]>;
//#endregion
//#region src/cache/cacheConfig.d.ts
declare const cacheConfig: {
  FORCED_IN_MEMORY_CACHE_NAMESPACES: string[];
  USE_REDIS: boolean;
  USE_REDIS_STREAMS: boolean;
  REDIS_URI: string | undefined;
  REDIS_USERNAME: string | undefined;
  REDIS_PASSWORD: string | undefined;
  REDIS_CA: string | null;
  REDIS_KEY_PREFIX: string;
  GLOBAL_PREFIX_SEPARATOR: string;
  REDIS_MAX_LISTENERS: number;
  REDIS_PING_INTERVAL: number; /** Milliseconds a heartbeat PING may go unanswered before the socket is presumed dead */
  REDIS_PING_TIMEOUT: number; /** Heartbeat interval in seconds for dedicated pub/sub subscriber connections (0 = disabled) */
  REDIS_SUBSCRIBER_PING_INTERVAL: number; /** TCP keepalive idle delay in ms for ioredis sockets (0 = kernel default idle time) */
  REDIS_KEEP_ALIVE: number; /** Max delay between reconnection attempts in ms */
  REDIS_RETRY_MAX_DELAY: number; /** Max number of reconnection attempts (0 = infinite) */
  REDIS_RETRY_MAX_ATTEMPTS: number; /** Connection timeout in ms */
  REDIS_CONNECT_TIMEOUT: number; /** Min spacing in ms between reconnects forced by READONLY replies after a failover */
  REDIS_READONLY_RECOVERY_INTERVAL: number; /** Queue commands when disconnected */
  REDIS_ENABLE_OFFLINE_QUEUE: boolean;
  /** flag to modify redis connection by adding dnsLookup this is required when connecting to elasticache for ioredis
  * see "Special Note: Aws Elasticache Clusters with TLS" on this webpage:  https://www.npmjs.com/package/ioredis **/
  REDIS_USE_ALTERNATIVE_DNS_LOOKUP: boolean; /** Enable redis cluster without the need of multiple URIs */
  USE_REDIS_CLUSTER: boolean;
  /**
  * Force cluster-safe (key-by-key) deletion even when connecting as a single-node Redis instance.
  * Needed for managed services like ElastiCache Serverless that present a single endpoint
  * but shard keys internally, causing CROSSSLOT errors on multi-key DEL commands.
  * Has no effect when USE_REDIS_CLUSTER is already true.
  */
  REDIS_CLUSTER_SAFE_DELETE: boolean;
  CI: boolean;
  DEBUG_MEMORY_CACHE: boolean;
  BAN_DURATION: number;
  /**
  * TTL in ms for violation scores: a score expires after this long without new violations
  * (each violation write restarts the countdown). `undefined` — from a non-positive
  * setting — disables expiry so scores accumulate forever.
  * @default 3600000 (1 hour)
  */
  VIOLATION_SCORE_TTL: number | undefined;
  /**
  * Number of keys to delete in each batch during Redis DEL operations.
  * In cluster mode, keys are deleted individually in parallel chunks to avoid CROSSSLOT errors.
  * In single-node mode, keys are deleted in batches using DEL with arrays.
  * Lower values reduce memory usage but increase number of Redis calls.
  * @default 1000
  */
  REDIS_DELETE_CHUNK_SIZE: number;
  /**
  * Number of keys to update in each batch during Redis SET operations.
  * In cluster mode, keys are updated individually in parallel chunks to avoid CROSSSLOT errors.
  * In single-node mode, keys are updated in batches using transactions (multi/exec).
  * Lower values reduce memory usage but increase number of Redis calls.
  * @default 1000
  */
  REDIS_UPDATE_CHUNK_SIZE: number;
  /**
  * COUNT hint for Redis SCAN operations when scanning keys by pattern.
  * This is a hint to Redis about how many keys to scan in each iteration.
  * Higher values can reduce round trips but increase memory usage and latency per call.
  * Note: Redis may return more or fewer keys than this count depending on internal heuristics.
  * @default 1000
  */
  REDIS_SCAN_COUNT: number;
  /**
  * TTL in milliseconds for MCP registry caches. Used by both:
  * - `MCPServersRegistry` read-through caches (`readThroughCache`/`readThroughCacheAll`)
  * - `ServerConfigsCacheRedisAggregateKey` local snapshot (avoids redundant Redis GETs)
  *
  * Both layers use this value, so the effective max cross-instance staleness is up
  * to 2× this value in multi-instance deployments. Set to 0 to disable the local
  * snapshot entirely (every `getAll()` hits Redis directly).
  * @default 5000 (5 seconds)
  */
  MCP_REGISTRY_CACHE_TTL: number;
};
//#endregion
//#region src/cache/redisClients.d.ts
declare const keyvRedisClientReady: Promise<void> | null;
/** Waits for the stable shared Keyv Redis readiness gate. */
declare function waitForKeyvRedisClient(): Promise<void>;
declare let ioredisClient: Redis | Cluster | null;
declare let keyvRedisClient: RedisClientType | RedisClusterType | null;
/**
* Routes a Keyv Redis client error through READONLY failover recovery.
* Cluster clients follow MOVED to the owning master on their own, so the hook
* only exists for the standalone client. Returns whether a reconnect was started.
*/
declare function handleKeyvRedisError(error: unknown): boolean;
type RedisEvalOptions = {
  keys: string[];
  arguments: string[];
};
/**
* Runs a Lua script on the master that owns its keys. Node Redis can execute a
* cluster EVAL through an arbitrary node while the slot map is settling, which
* leaks a MOVED reply instead of following it. Catalog scripts are deliberately
* single-slot, so selecting the owning master also makes that invariant explicit.
*
* Script failures reject straight to the caller without passing through any
* client error event, so READONLY replies are routed to failover recovery here.
*/
declare function evalKeyvRedisScript(script: string, options: RedisEvalOptions): Promise<unknown>;
//#endregion
//#region src/cache/keyvFiles.d.ts
declare const logFile: KeyvFile;
declare const violationFile: KeyvFile;
//#endregion
//#region src/cache/keyvMongo.d.ts
interface KeyvMongoOptions {
  url?: string;
  collection?: string;
  useGridFS?: boolean;
  readPreference?: ReadPreference;
}
declare class KeyvMongoCustom extends EventEmitter {
  private opts;
  ttlSupport: boolean;
  namespace?: string;
  constructor(options?: KeyvMongoOptions);
  private _getClient;
  get(key: string): Promise<unknown>;
  getMany(keys: string[]): Promise<unknown[]>;
  set(key: string, value: string, ttl?: number): Promise<unknown>;
  delete(key: string): Promise<boolean>;
  deleteMany(keys: string[]): Promise<boolean>;
  clear(): Promise<void>;
  has(key: string): Promise<boolean>;
  disconnect(): Promise<boolean>;
  private isGridFSClient;
}
declare const keyvMongo: KeyvMongoCustom;
//#endregion
//#region src/cache/cacheFactory.d.ts
/**
* Creates a cache instance using Redis or a fallback store. Suitable for general caching needs.
*
* **In-memory mode** (no Redis, no custom fallbackStore): instances are memoized by
* namespace so that every call-site shares the same underlying `Map`. The first
* caller's TTL wins for a given namespace.
*
* @param namespace - The cache namespace.
* @param ttl - Time to live for cache entries.
* @param fallbackStore - Optional fallback store if Redis is not used.
* @returns Cache instance.
*/
declare const standardCache: (namespace: string, ttl?: number, fallbackStore?: object) => Keyv;
/** Convenience accessor for the TOKEN_CONFIG cache namespace. */
declare const tokenConfigCache: () => Keyv;
/**
* Creates a cache instance for storing violation data.
* Uses a file-based fallback store if Redis is not enabled.
* @param namespace - The cache namespace for violations.
* @param ttl - Time to live for cache entries. Defaults to `cacheConfig.VIOLATION_SCORE_TTL`
* so violation scores decay instead of accumulating forever; each write restarts the countdown.
* @returns Cache instance for violations.
*/
declare const violationCache: (namespace: string, ttl?: number | undefined) => Keyv;
/**
* Creates a session cache instance using Redis or in-memory store.
* @param namespace - The session namespace.
* @param ttl - Time to live for session entries.
* @returns Session store instance.
*/
declare const sessionCache: (namespace: string, ttl?: number) => MemoryStore | RedisStore$1;
/**
* Creates a rate limiter cache using Redis.
* @param prefix - The key prefix for rate limiting.
* @returns RedisStore instance or undefined if Redis is not used.
*/
declare const limiterCache: (prefix: string) => RedisStore | undefined;
//#endregion
//#region src/cache/principals.d.ts
type UserPrincipalsCache = Keyv & {
  crossProcess?: boolean;
  lockWaitMs?: number;
  staleEvictionDelayMs?: number;
  acquireLock?: (key: string) => Promise<string | null>;
  releaseLock?: (key: string, token: string) => Promise<void>;
};
/**
* Cache for resolved group memberships used by ACL principal resolution
* (`getUserPrincipals`). When the namespace is Redis-backed, lock helpers are
* attached so concurrent cold-key builds are deduplicated across containers.
* Returns undefined when disabled via USER_PRINCIPALS_CACHE_TTL_MS=0.
*/
declare function userPrincipalsCache(): UserPrincipalsCache | undefined;
//#endregion
//#region src/cache/redisUtils.d.ts
/**
* Duplicates an ioredis connection with option overrides. `Cluster.duplicate` reads its
* first argument as an optional startup-node list and its second as the overrides,
* unlike `Redis.duplicate`, so options passed positionally to a cluster are silently
* dropped and the duplicate quietly inherits the original's behaviour.
*/
declare function duplicateIoRedisClient(client: Redis | Cluster, options?: RedisOptions & ClusterOptions): Redis | Cluster;
/**
* Duplicates a client for a dedicated pub/sub subscriber. `duplicate()` copies options but
* not listeners, so a bare duplicate reports socket errors only through ioredis's
* "Unhandled error event" and, between generations, carries no traffic at all: a peer
* that vanishes without closing the socket stays undetected until the kernel gives up.
* The heartbeat is the traffic the shared client gets for free.
*/
declare function createIoRedisSubscriber(client: Redis | Cluster, label: string): Redis | Cluster;
/**
* Efficiently deletes multiple Redis keys with support for both cluster and single-node modes.
*
* - Cluster mode: Deletes keys in parallel chunks to avoid CROSSSLOT errors
* - Single-node mode: Uses batch DEL commands for efficiency
*
* @param client - Redis client (node or cluster)
* @param keys - Array of keys to delete
* @param chunkSize - Optional chunk size (defaults to REDIS_DELETE_CHUNK_SIZE config)
* @returns Number of keys deleted
*
* @example
* ```typescript
* const deletedCount = await batchDeleteKeys(keyvRedisClient, ['key1', 'key2', 'key3']);
* console.log(`Deleted ${deletedCount} keys`);
* ```
*/
declare function batchDeleteKeys(client: RedisClientType | RedisClusterType, keys: string[], chunkSize?: number): Promise<number>;
/**
* Scans Redis for keys matching a pattern and collects them into an array.
* Uses Redis SCAN to avoid blocking the server.
*
* @param client - Redis client (node or cluster) with scanIterator support
* @param pattern - Pattern to match keys (e.g., 'user:*', 'session:*:active')
* @param count - Optional SCAN COUNT hint (defaults to REDIS_SCAN_COUNT config)
* @returns Array of matching keys
*
* @example
* ```typescript
* const userKeys = await scanKeys(keyvRedisClient, 'user:*');
* const sessionKeys = await scanKeys(keyvRedisClient, 'session:*:active', 500);
* ```
*/
declare function scanKeys(client: RedisClientType | RedisClusterType, pattern: string, count?: number): Promise<string[]>;
//#endregion
//#region src/cache/redisTelemetry.d.ts
declare const RedisUseCases: {
  readonly GENERATION_STREAM: "generation_stream";
  readonly LEADER_ELECTION: "leader_election";
  readonly MCP_REGISTRY: "mcp_registry";
  readonly RATE_LIMIT: "rate_limit";
  readonly VIOLATIONS: "violations";
};
type RedisUseCaseSummary = {
  calls: number;
  durationMs: number;
  errors: number;
  maxCallMs: number;
};
interface RedisRequestTelemetry {
  calls: number;
  durationMs: number;
  ended: boolean;
  errors: number;
  maxCallMs: number;
  operations: Set<string>;
  span: Span;
  useCases: Map<string, RedisUseCaseSummary>;
}
declare function normalizeRedisUseCase(namespace: string): string;
declare function createRedisRequestTelemetry(span: Span): RedisRequestTelemetry;
declare function runWithRedisRequestTelemetry<T>(telemetry: RedisRequestTelemetry, callback: () => T): T;
declare function finishRedisRequestTelemetry(telemetry: RedisRequestTelemetry): void;
declare function observeRedisOperation<T>(client: RedisClient, namespace: string, operationName: string, operation: () => T | PromiseLike<T>, isErrorResult?: (result: T) => boolean): Promise<T>;
declare function instrumentRedisCache<T extends object>(cache: T, namespace: string, client?: RedisClient): T;
declare function instrumentIORedisClient<T extends object>(client: T, namespace: string): T;
//#endregion
//#region src/cluster/LeaderElection.d.ts
declare const isLeader$1: () => Promise<boolean>;
//#endregion
//#region src/shared-links/access.d.ts
interface SharedLinkAccessDeps {
  mongoose: typeof import("mongoose");
  aclService?: AccessControlService;
}
declare function createSharedLinkAccessMiddleware(deps: SharedLinkAccessDeps): (req: Request, res: Response$2, next: NextFunction) => Promise<void>;
//#endregion
//#region src/shared-links/service.d.ts
interface RawSharedLink {
  _id?: Types.ObjectId;
  conversationId: string;
  title?: string;
  user?: string;
  shareId?: string;
  tenantId?: string;
  isPublic?: boolean;
  expiredAt?: Date;
}
declare function autoMigrateLegacyLink(share: RawSharedLink): Promise<void>;
declare function grantCreationPermissions(sharedLinkId: string | Types.ObjectId, userId: string, grantPublic?: boolean, expiredAt?: Date | null): Promise<void>;
declare function ensureLinkPermissions(sharedLinkId: string | Types.ObjectId, userId: string): Promise<void>;
declare function cleanupSharedLinkPermissions(resourceId: string | Types.ObjectId): Promise<DeleteResult>;
declare function cleanupBulkSharedLinkPermissions(resourceIds: (string | Types.ObjectId)[]): Promise<DeleteResult>;
declare function updateSharedLinkPermissionsExpiration(resourceId: string | Types.ObjectId, expiredAt: Date | null): Promise<void>;
declare function deleteSharedLinkWithCleanup(user: string, shareId: string): Promise<{
  _id?: string;
  success: boolean;
  shareId: string;
  message: string;
} | null>;
declare function deleteConvoSharedLinksWithCleanup(user: string, conversationId: string): Promise<{
  message: string;
  deletedCount: number;
}>;
declare function deleteAllSharedLinksWithCleanup(user: string): Promise<{
  message: string;
  deletedCount: number;
}>;
//#endregion
//#region src/shared-links/config.d.ts
type SharedLinkStartupEnv = NodeJS.ProcessEnv;
interface SharedLinkConfigRequest extends Request {
  config?: AppConfig;
  shareTenantId?: string;
}
interface SharedLinkConfigMiddlewareDeps {
  getAppConfig: (options?: GetAppConfigOptions) => Promise<AppConfig>;
}
/** Resolve shared-link policy independently of the authenticated viewer. */
declare function resolveSharedLinkConfig(getAppConfig: SharedLinkConfigMiddlewareDeps["getAppConfig"], tenantId?: string): Promise<AppConfig>;
declare function createSharedLinkConfigMiddleware({
  getAppConfig
}: SharedLinkConfigMiddlewareDeps): (req: SharedLinkConfigRequest, _res: Response$2, next: NextFunction) => Promise<void>;
/**
* Whether shared links should snapshot the files referenced by the shared chat
* snapshot. The `SHARED_LINKS_SNAPSHOT_FILES` env var overrides the yaml
* `interface.sharedLinks.snapshotFiles` value; both default to enabled.
*/
declare function isFileSnapshotEnabled(appConfig?: AppConfig): boolean;
/**
* Viewer-independent global kill switch for serving shared-link files. Reading
* and serving must NOT depend on the viewer's resolved config (per-role/user
* overrides) — only on the link's own stored choice plus this global env switch.
* Active only when `SHARED_LINKS_SNAPSHOT_FILES` is explicitly set to a disabled
* value; the creator's yaml choice is already captured per-link at share time.
*/
declare function isFileSnapshotKillSwitchActive(): boolean;
declare function buildSharedLinkStartupPayload(appConfig?: AppConfig | null, env?: SharedLinkStartupEnv): TSharedLinkStartupConfig;
//#endregion
//#region src/shared-links/http.d.ts
declare const DEFAULT_SHARED_LINKS_PAGE_SIZE: number;
declare const MAX_SHARED_LINKS_PAGE_SIZE: number;
declare const MAX_SHARED_LINK_SEARCH_LENGTH: number;
declare const MAX_SHARED_LINK_CURSOR_LENGTH: number;
/** Clamp a requested page size, falling back to the default for anything unparseable. */
declare function parseSharedLinksPageSize(value: unknown): number;
/**
* The list cursor is opaque: base64 `{ primary, id }`, where `id` is the `_id` that
* breaks ties on a repeated title or timestamp and `primary` is null for a row with no
* title. Cursors issued before that encoding carry the bare sort value, so a plain date
* still passes for a `createdAt` page.
*/
declare function isValidSharedLinksCursor(cursor: string, sortBy: string): boolean;
interface ShareFileEtagSource {
  file_id: string;
  previewRevision?: string | number | null;
  bytes?: number | null;
  storageKey?: string | null;
  filepath?: string | null;
}
/**
* Cache validator for a snapshotted file. Beyond the fields the share routes pin the
* snapshot on (`previewRevision`, `bytes`), it folds in the stored location: a re-publish
* that swaps the object without changing size or revision still moves
* `storageKey`/`filepath` (regenerated code outputs carry a `?v=` cache-buster), so the
* viewer revalidates instead of keeping its stale copy. A same-path, same-size,
* same-revision replacement stays the best-effort gap inherent to the no-byte-copy
* snapshot design.
*/
declare function buildShareFileEtag(file: ShareFileEtagSource): string;
//#endregion
//#region src/shared-links/protection.d.ts
interface SerializedSharedFile extends FileContentInput {
  readonly preview?: string;
  readonly [key: string]: unknown;
}
interface SerializedSharedMessagePart {
  readonly type?: string;
  readonly audio_url?: string | SerializedSharedFile | null;
  readonly data?: unknown;
  readonly document?: Readonly<Record<string, unknown>> | null;
  readonly document_url?: string | SerializedSharedFile | null;
  readonly file?: SerializedSharedFile | null;
  readonly fileData?: SerializedSharedFile | null;
  readonly file_data?: unknown;
  readonly file_id?: string;
  readonly fileUri?: string;
  readonly file_uri?: string;
  readonly filename?: string;
  readonly image_file?: SerializedSharedFile | null;
  readonly image_url?: string | SerializedSharedFile | null;
  readonly inlineData?: SerializedSharedFile | null;
  readonly input_audio?: SerializedSharedFile | null;
  readonly source?: string | Readonly<Record<string, unknown>> | null;
  readonly video_url?: string | SerializedSharedFile | null;
  readonly files?: readonly (SerializedSharedFile | null | undefined)[];
  readonly [key: string]: unknown;
}
interface SerializedSharedMessage {
  readonly isCreatedByUser?: boolean;
  readonly isUserSubmitted?: boolean;
  readonly userSubmittedPaths?: readonly string[];
  readonly userSubmittedMessageFieldPaths?: readonly UserSubmittedMessageFieldPath[];
  readonly role?: string;
  readonly iconURL?: string;
  readonly finish_reason?: string;
  readonly manualSkills?: readonly (string | null | undefined)[];
  readonly alwaysAppliedSkills?: readonly (string | null | undefined)[];
  readonly files?: readonly (SerializedSharedFile | null | undefined)[];
  readonly attachments?: readonly (SerializedSharedFile | null | undefined)[];
  readonly content?: readonly (SerializedSharedMessagePart | null | undefined)[];
}
interface SharedFileMetadataPolicyInput {
  readonly filters?: FiltersConfig;
  readonly messages: readonly SerializedSharedMessage[];
  readonly shareId?: string;
  /** Creation/update preflights use `false` because their file objects have
  * not yet been projected onto share-scoped locators; canonical file policy
  * is enforced separately on that exact durable snapshot. */
  readonly includeFiles?: boolean;
}
type ShareContentPreflightMessage = ConversationImportMessage & SerializedSharedMessage;
interface ShareContentPreflightInput {
  readonly title?: string;
  readonly messages: readonly ShareContentPreflightMessage[];
  readonly shareId?: string;
}
interface ShareContentPreflightOptions {
  readonly onTraversalFailure?: LocatorTraversalReporter;
  readonly legacyPii?: MessageFilterPiiConfig | null;
  readonly snapshotFiles?: boolean;
  readonly user?: CanonicalFileInspectionUser;
  readonly getFiles?: GetCanonicalFilesForInspection;
  readonly sharedFileMetadata?: boolean;
  readonly sharedFileMetadataFiles?: boolean;
}
type ShareContentPreflight = (input: ShareContentPreflightInput) => Promise<void>;
/** Builds the exact read/publish preflight shared by every shared-link route. */
declare function createShareContentPreflight(filters: FiltersConfig | null | undefined, options?: ShareContentPreflightOptions): ShareContentPreflight | undefined;
/**
* Reapplies current policy to metadata serialized by a public share. Pattern
* inspection covers user-submitted message metadata and every returned file
* reference, while fail-close ignores only server-issued shared-file locators.
*/
declare function assertSharedFileMetadataAllowed({
  filters,
  messages,
  shareId,
  includeFiles
}: SharedFileMetadataPolicyInput): void;
//#endregion
//#region src/shared-links/session.d.ts
interface SharedSessionViewer {
  id?: string;
  _id?: {
    toString(): string;
  };
  role?: string;
  tenantId?: string;
  idOnTheSource?: string | null;
}
interface SharedLangfuseSessionParams {
  viewer?: SharedSessionViewer;
  shareTenantId?: string;
  shareConversationId?: string;
  shareOwnerId?: string;
  config: TCustomConfig["langfuse"];
}
interface SharedLangfuseSessionDeps {
  getHeldCapabilities: GetHeldCapabilitiesFn;
  getMessages: LangfuseSessionLinkParams["getMessages"];
  resolveSessionUrl?: (params: LangfuseSessionLinkParams) => Promise<string | null>;
}
declare function createSharedLangfuseSessionResolver(deps: SharedLangfuseSessionDeps): ({
  viewer,
  shareTenantId,
  shareConversationId,
  shareOwnerId,
  config
}: SharedLangfuseSessionParams) => Promise<string | null>;
//#endregion
//#region src/utils/memory.d.ts
type ConnectionStats = ReturnType<InstanceType<typeof MCPManager>["getConnectionStats"]>;
type TrackerStats = ReturnType<InstanceType<typeof OAuthReconnectionManager>["getTrackerStats"]>;
type RuntimeStats = ReturnType<(typeof GenerationJobManager)["getRuntimeStats"]>;
interface MemorySnapshot {
  ts: number;
  rss: number;
  heapUsed: number;
  heapTotal: number;
  external: number;
  arrayBuffers: number;
  mcpConnections: ConnectionStats | null;
  oauthTracker: TrackerStats | null;
  generationJobs: RuntimeStats | null;
}
declare function collectSnapshot(): void;
declare function forceGC(): boolean;
declare function getSnapshots(): readonly MemorySnapshot[];
declare function start(): void;
declare function stop(): void;
declare const memoryDiagnostics: {
  start: typeof start;
  stop: typeof stop;
  forceGC: typeof forceGC;
  getSnapshots: typeof getSnapshots;
  collectSnapshot: typeof collectSnapshot;
};
//#endregion
//#region src/rum/proxy.d.ts
declare function getRumProxyClientUrl(): string;
declare function getRumProxyBodyLimit(): string;
declare function getRumProxyTimeoutMs(): number;
declare function getRumProxyTargetBaseUrl(): URL | undefined;
declare function isRumProxyEnabled(): boolean;
declare function resolveRumProxyTarget(path: string): string | undefined;
declare function proxyRumRequest(req: Request, res: Response$2): Promise<void>;
//#endregion
export { ABORT_OWNER_PRESUMED_ALIVE_MS, ACTION_CREDENTIAL_REFRESH_MESSAGE, ACTIVITY_INSTRUCTION, ACTIVITY_PHASE_INSTRUCTION, ADDED_AGENT_ID, AGENT_ATTACHMENT_LIMIT_EXCEEDED, AGENT_CHECKPOINT_FORMAT_VERSION, AGENT_CONTEXT_FINGERPRINT_VERSION, AGENT_EXPECTED_MCP_TOOLS_UNAVAILABLE, AGENT_GRAPH_SCHEMA_VERSION, AGENT_PLUGINS_VERSION, AGENT_QUEUED_TURN_SOURCE, AGENT_RUN_ENVELOPE_MAX_NESTING_DEPTH, AGENT_RUN_ENVELOPE_VERSION, AGENT_TRIGGER_COALESCE_WINDOW_MS, AGENT_TRIGGER_ENVELOPE_VERSION, AGENT_TRIGGER_IDEMPOTENCY_PREFIX, AGENT_TRIGGER_SCOPE, AGENT_TRIGGER_TOKEN_TTL, AGENT_WORKSPACE_ATTACHED_ENVIRONMENT_ERROR, ALLOWED_BODY_FIELDS, ARTIFACT_END, ARTIFACT_START, ASK_USER_QUESTION_TOOL_NAME, ATTACHED_WORKSPACE_BASH_DESCRIPTION, ATTACHED_WORKSPACE_BASH_SCHEMA, ATTACHMENT_ONLY_TEXT, AUTH_USER_DOC_CACHE_TTL_MS, AVATAR_REFRESH_BATCH_SIZE, type AbortResult, AccessControlService, AccessTokenAudiences, AccessibleCodeEnvironmentConfiguration, AccessibleCodeEnvironmentDetails, type AccessiblePromptGroupsResult, ActionMetadataUpdateParams, ActionMetadataUpdateResult, ActionOAuthTokenDeleteQuery, ActionToolDefinition, ActionToolLike, type ActivityLabelAgent, type ActivityLabelBatchMeta, type ActivityLabelBlockContext, type ActivityLabelHookOptions, type ActivityLabelHostDeps, type ActivityLabelInvokeCallbacks, type ActivityLabelLLM, type ActivityLabelSlot, type ActivityLabelUsage, ActivityPhaseEntry, ActivityPhaseHostDeps, ActivityPhaseSnapshot, ActivityPhaseWiring, type AddServerResult, type AdminAuditLogDeps, type AdminCodeEnvironmentDeps, type AdminConfigDeps, AdminExchangeData, AdminExchangeResponse, AdminExchangeUser, type AdminGrantsDeps, type AdminGroupsDeps, type AdminLangfuseDeps, AdminRefreshDeps, AdminRefreshError, AdminRefreshOptions, type AdminRolesDeps, type AdminSkillSyncAccessDeps, type AdminSkillSyncDeps, type AdminUsersDeps, AgentApiKeyService, AgentAttachmentEndpointsByAgentId, AgentAttachmentLimit, AgentAttachmentLimitError, AgentAttachmentPolicyError, AgentAttachmentStats, AgentCheckpointGeneration, AgentContactSource, AgentContentInput, AgentContextAttachmentCarrier, AgentContextAttachmentsByAgentId, AgentContextDefinition, AgentContextFingerprint, AgentContextMemorySnapshot, AgentContextSkillIdentity, AgentContinuationAdmissionSource, AgentContinuationResolverDeps, AgentContinueTarget, AgentContinueTriggerEnvelope, AgentEventActionRecorder, AgentEventActorCheckpointTurn, AgentEventActorContext, AgentEventActorDependencies, AgentEventActorDetachedActionLifecycle, AgentEventActorDetachedCompletionProjection, AgentEventActorDetachedResumeInput, AgentEventActorHistoryTurn, AgentEventActorInternalSuspension, AgentEventActorInvocationContext, AgentEventActorStorageMetricsSnapshot, AgentEventActorTurn, AgentEventActorTurnDependencies, AgentEventActorTurnOwner, AgentEventActorTurnResult, type AgentEventAppliedAction, AgentEventBindingDependencies, AgentEventCheckpointMessageOverlay, AgentEventCheckpointReference, AgentEventContinueResolverDeps, AgentEventRunOutcome, AgentExecutionAdmissionError, AgentExecutionConnection, AgentExecutionContext, AgentExecutionEnrollment, AgentExecutionEnrollmentParams, AgentFileRetentionRequest, AgentFireAccessDeps, AgentFireRunContext, AgentFireTarget, AgentFireTriggerEnvelope, AgentManagementAuthDeps, AgentManagementCreate, AgentManagementCreateDeps, AgentManagementDeleteDeps, AgentManagementDeleteResponse, AgentManagementError, AgentManagementErrorCode, AgentManagementFileDeps, AgentManagementList, AgentManagementListProjectionSource, AgentManagementListResponse, AgentManagementProjectionSource, AgentManagementReadDeps, AgentManagementResponse, AgentManagementUpdate, AgentManagementUpdateDeps, AgentMemoryPartitionAccess, AgentOwnerContactSource, AgentPermissionsRequest, AgentQueuedTurnExecutionAdmission, AgentQueuedTurnHttpDeps, AgentQueuedTurnHttpResult, AgentQueuedTurnLifecycle, AgentQueuedTurnLifecycleDeps, AgentQueuedTurnResolverDeps, AgentQueuedTurnScheduler, AgentQueuedTurnSchedulerDeps, AgentRequestFingerprintFields, AgentRunEnvelope, AgentRunEnvelopeError, AgentRunPayloadExtensions, AgentRunPrincipal, AgentRunPrincipalInput, AgentRunProtocol, AgentStartupMilestone, AgentStartupResult, AgentStartupTelemetry, AgentSteerTarget, AgentSteerTriggerEnvelope, AgentTriggerCoalesceOptions, AgentTriggerContinuePreparation, AgentTriggerContinueResult, AgentTriggerDeliveryDeferredError, AgentTriggerDeliveryEngine, AgentTriggerDeliveryEngineDeps, AgentTriggerDeliveryEngineOptions, AgentTriggerDeliveryError, AgentTriggerDeliveryFailure, AgentTriggerDeliveryPersistence, AgentTriggerDeliveryReceipt, AgentTriggerDeliveryRecord, AgentTriggerDeliveryStatus, AgentTriggerDeliveryStore, AgentTriggerDispatchContext, AgentTriggerDispatchError, AgentTriggerDispatchHandlers, AgentTriggerEnqueueOptions, AgentTriggerEnvelope, AgentTriggerEnvelopeError, AgentTriggerEvent, AgentTriggerExecutionError, AgentTriggerExecutionErrorOptions, AgentTriggerExecutionHost, AgentTriggerExecutionHostDeps, AgentTriggerExecutionResult, type AgentTriggerExpectedAction, AgentTriggerFailureCertainty, AgentTriggerFetch, AgentTriggerFireResult, AgentTriggerIngressDependencies, AgentTriggerMode, AgentTriggerOrderingBlock, AgentTriggerService, AgentTriggerServiceDeps, AgentTriggerServiceOptions, AgentTriggerServiceUnavailableError, AgentTriggerSource, AgentTriggerSteerResult, AgentTriggerStoredHistoryEntry, AgentTriggerStoredRecord, AgentTurnBinding, AgentTurnContinuationStrategy, AgentTurnExecutionPlan, AgentTurnOrigin, AgentTurnSemanticContext, AgentUploadAuthDeps, AgentUploadAuthParams, AgentUploadAuthResult, AgentWithTools, AggregatedContent, type AllowedCaller, type Annotation, type AnthropicConfigOptions, type AnthropicCredentials, type AnthropicDocumentBlock, type AnthropicLLMConfigResult, type AnthropicModelOptions, type AnthropicParameters, ApiKeyAuthDependencies, ApiKeyAuthRequest, ApiKeyHandlerDependencies, ApiKeyServiceDependencies, AppConfigPrincipal, AppConfigService, AppConfigServiceDeps, AppConfigUserLike, ApplyModelSpecPresetParams, ApplyModelSpecPresetResult, ApprovalLifecycle, type ApprovalLifecycleCallbacks, type ApprovalPauseOptions, ArtifactBoundary, ArtifactDeliveryFailure, type Artifacts, AskUserQuestionToolDefinition, AskUserQuestionToolDefinitionShape, AskUserQuestionToolInput, AssertNotCancelled, AssertResumeContentAllowedInput, AssertResumeRuntimeContentAllowedInput, AssistantActionContentInput, AssistantContentInput, AssistantMCPUser, type AssistantMessageItemParam, AssistantTextPhase, AssistantToolCatalogSnapshot, AssistantToolDefinitionsDeps, AssistantToolDefinitionsParams, AssistantToolDefinitionsResult, AssistantToolReference, AttachedCodeEnvironmentApprovalError, AttachedCodeEnvironmentPolicySettings, type AttachmentData, AttachmentObjectNotFoundError, AttachmentStorageError, type AudioBlock, type AudioContent, type AudioFileInfo, type AudioProcessingResult, type AudioResult, AudioValidationResult, AuthFailureReasonCategory, AuthFailureRedirectOptions, AuthIdentityContext, AuthIdentitySource, AuthIdentityTuple, AuthLogContext, AuthLogRequest, AuthLogState, AuthUserDocCacheKeyInput, AuthUserDocCacheMode, AuthUserDocCacheStore, type AzureOptions, BACKGROUND_TASK_ABORT_GRACE_MS, BACKGROUND_TASK_TIMEOUT_MS, BACKGROUND_TOOL_COMPLETION_SOURCE, BACKGROUND_TOOL_PRODUCER_HEARTBEAT_MS, BACKGROUND_TOOL_PRODUCER_LEASE_MS, BACKGROUND_TOOL_WAKEUP_INPUT_MAX_CHARS, BALANCE_SKIP_DISABLE_THRESHOLD, BackgroundToolCompletionWakeupResolverDeps, BackgroundToolDeadClaimRecovery, BackgroundToolDeadClaimRecoveryInput, BackgroundToolResultState, BackgroundToolWakeupAdmission, BackgroundToolWakeupRegistration, BackgroundToolWakeupRetireOptions, BalanceMiddlewareOptions, BalanceReservation, BalanceReservations, type BalanceUpdateFields, type BaseEvent, type BaseInitializeParams, type BasicConnectionOptions, BasicToolEndHandler, BatchUpdateFn, type BedrockConfigOptions, type BedrockCredentials, type BedrockDocumentBlock, type BedrockLLMConfigResult, type BedrockUserCredentials, BootstrapInjection, BoundAddress, BuildHistoricalToolNamesConfig, type BuildInfo, BuildOAuthFailureLogParams, BuildSkillPrimeContentPartsParams, BuildToolClassificationParams, BuildToolClassificationResult, BuildToolSetConfig, BulkPermissionUpdateResult, BulkWriteDeps, CHECKPOINT_HARD_LIMIT_BYTES, CHECKPOINT_WARN_BYTES, CHILD_THREAD_READ_ONLY_ERROR, CLAUDE_TOKEN_CORRECTION, CLOUDFRONT_SCOPE_COOKIE, CODE_API_BRIDGE_WORKER_HEADER, CODE_API_EXPECTED_PROFILE_HEADER, CODE_API_UPLOAD_CONCURRENCY_DEFAULT, CODE_FILE_TOOL_NAMES, CODE_OUTPUT_PREFLIGHT_MAX_BYTES, CODE_OUTPUT_PREFLIGHT_MAX_COUNT, CONTENT_MATERIALIZATION_MAX_CHARACTERS, CONTENT_TRAVERSAL_MAX_DEPTH, CONTENT_TRAVERSAL_MAX_NODES, CONVERSATION_IMPORT_BSON_HEADROOM_BYTES, CONVERSATION_SORT_FIELDS, CREATE_FILE_TOOL_NAME, CachedToolsOptions, CancelAgentEventActorInput, CanonicalFileInspectionCoverage, CanonicalFileInspectionFile, CanonicalFileInspectionUser, CanonicalFileReferenceInspection, CanonicalFileReferenceInspectionInput, CapabilityUser, CapturedRunArtifact, CapturedUserSubmittedPathMetadata, CatalogCache, CatalogStoreDeps, ChatCompletionChoice, ChatCompletionChunk, ChatCompletionChunkChoice, type ChatCompletionDependencies, ChatCompletionRequest, ChatCompletionResponse, ChatCompletionRunEnvelope, ChatCompletionRunPayload, type ChatCompletionValidationFailure, type ChatCompletionValidationResult, type ChatCompletionValidationSuccess, ChatGptCitation, ChatGptCitationMetadata, ChatGptExportContent, ChatGptExportMessage, ChatGptLineage, ChatGptMapping, ChatGptMappingNode, ChatGptThought, ChatMessage, ChatRunFileBindings, ChatSubmissionBody, ChatSubmissionDecision, CheckAccessParams, CheckAccessWithRequestCacheParams, CheckAgentPermission, CheckBalanceDeps, CheckToolResourceUploadParams, CheckToolRolePermissionParams, CheckpointDeletion, CheckpointTooLargeError, type ChunkHandler, ClonedLineage, ClonedLineageEntry, CloudFrontAuthCookieRefreshResult, CloudFrontCookieScope, CloudFrontFullConfig, CloudFrontURLParams, CodeApiRateLimitBudget, CodeApiUploadRegistry, CodeArtifactCategory, CodeArtifactInspectionText, CodeBridgeFetch, CodeBridgeLifecycleError, CodeBridgePairing, CodeBridgePairingError, CodeBridgePrincipalType, CodeBridgeStatusError, CodeBridgeWorkerBinding, CodeBridgeWorkerStatus, CodeCapabilityConfigLoader, CodeDestinationSet, CodeEnvFileOptions, CodeEnvIdentity, CodeEnvRefUpdate, CodeEnvReferenceSetResult, CodeEnvironmentConfig, CodeEnvironmentConversationDeps, CodeEnvironmentGenerationDeps, CodeEnvironmentGenerationJob, CodeEnvironmentHttpDeps, CodeEnvironmentInUseError, CodeEnvironmentLifecycleTarget, CodeEnvironmentLimitError, CodeEnvironmentPrincipalContext, CodeEnvironmentRegistration, CodeEnvironmentSummary, CodeEnvironmentValidationError, CodeExecutionApprovalTargetChangedError, CodeExecutionContext, CodeExecutionProfile, CodeExecutionProfileRoute, CodeExecutionRoute, CodeFileInfo, CodeFilesAgent, CodeHarvestDeps, CodeHarvestHandler, CodeHarvestParams, CodeOutputArtifact, CodeOutputArtifactFile, CodeOutputDownloadFallback, CodeOutputPersistence, CodeOutputPreflightLimits, CodeOutputProcessorDeps, CodeOutputProcessorInput, CodeOutputPublication, CodeOutputResult, CodeOutputStoredFile, CodeOutputTypeDetector, CodeWorkspaceSelectionError, type CodeWorkspaceSelectionErrorReason, type CollectedMetadataEntry, CollectedUsageBreakdown, CollectedUsageTotals, CommandExecutorOptions, ComparePasswordDeps, CompleteMCPAuthorizationWithTokenWaitersDeps, CompleteMCPAuthorizationWithTokenWaitersParams, type CompletedToolEvidence, CompletionUsage, CompletionUsageTotals, ComputeNextRunParams, ConfiguredContentInspectionSession, ConfiguredContentInspector, ConfiguredFooterSource, type ConnectionState, ContentFieldMap, ContentFilterBlockResponse, ContentFilterError, ContentFilterInputTooLargeError, ContentFilterInputTooLargeResponse, ContentFormat, ContentInspectionConfig, type ContentPart, type ContentPartAddedEvent, type ContentPartDoneEvent, ContentPolicyError, ContentProvenance, ContentSource, ContentTraversalDiagnostics, ContentTraversalLimitError, ContentTraversalLimitReason, ContentTraversalScope, ContentTreatment, ContextMetaPublisher, ContextMetaPublisherOptions, ContextMetaWriter, ConversationAnchor, ConversationAnchorSource, ConversationCodeEnvironmentDecision, ConversationCodeEnvironmentMove, ConversationImportContentInput, ConversationImportError, ConversationImportMessage, ConversationImportProtectionContext, ConversationImportSnapshot, ConversationImportWriteBatch, ConversationImportWriteOperations, ConversationSortField, CreateAgentEventActorTurnInput, CreateAgentRunEnvelopeInput, CreateAgentTriggerEnvelopeInput, CreateContentFilterOptions, type CreateGenerationJobOptions, CreateLazyAgentHistoryResolverParams, CreateMessageFilterPiiOptions, type CreateReasoningLabelHostWiringOptions, type CreatedEvent, CredentialFingerprintRecord, CredentialName, CredentialRuntimeState, CredentialSource, CspPolicy, CspResponse, type CustomError, DEFAULT_CHECKPOINT_TTL_SECONDS, DEFAULT_DEPLOYMENT_PLUGINS_DIR, DEFAULT_DEPLOYMENT_PLUGIN_DATA_DIR, DEFAULT_DEPLOYMENT_SKILLS_DIR, DEFAULT_GRAPH_SCOPES, DEFAULT_IMPORT_MAX_FILE_SIZE, DEFAULT_MAX_CONTEXT_TOKENS, DEFAULT_MAX_YOUTUBE_PARTS, DEFAULT_OAUTH_TOKEN_TTL_SECONDS, DEFAULT_OVERRIDE_CACHE_TTL, DEFAULT_PAGE_LIMIT, DEFAULT_REMOTE_FILE_FETCH_MAX_BYTES, DEFAULT_REMOTE_FILE_FETCH_TIMEOUT_MS, DEFAULT_SCHEDULE_LIMITS, DEFAULT_SHARED_LINKS_PAGE_SIZE, DEFAULT_SKILL_IMPORT_LIMITS, DEFAULT_TITLE_FALLBACK, DELETE_MEMORY_TOOL_NAME, DEPLOYMENT_PLUGINS_DIR_ENV, DEPLOYMENT_PLUGIN_DATA_DIR_ENV, DEPLOYMENT_PLUGIN_HOOKS_ENV, DEPLOYMENT_SKILLS_DIR_ENV, DEPLOYMENT_SKILL_FILE_SOURCE, DEPLOYMENT_SKILL_SOURCE, DeadlineWaitResult, DeploymentPluginOptions, DeploymentPluginRegistry, DeploymentSkill, DeploymentSkillBaseMethods, DeploymentSkillFile, DeploymentSkillRegistry, type DetachedAgentEventActionStoreMode, DetachedSubagentUsageRecorderParams, type DeveloperMessageItemParam, type DirectBearerRecoveryState, DirectoryPrincipalUser, DirectoryPrincipalUserData, DirectoryPrincipalUserMethods, DiscoverConnectedAgentsDeps, DiscoverConnectedAgentsParams, DiscoverConnectedAgentsResult, type DocumentBlock, type DocumentResult, type DoneHandler, DownloadURLParams, EDIT_FILE_TOOL_NAME, ENFORCED_MODEL_SPEC_REQUEST_FIELDS, EVENT_ACTOR_DETACHED_COMPLETION_SOURCE, EVENT_ACTOR_DETACHED_COMPLETION_TYPE, EXTENSION_HOOKS_FILE, EdgeDiscoveryResult, type EmbeddedResource, EncodedImageMetadata, type EncodingName, type EndpointDbMethods, type EndpointRuntimeContext, type EndpointTokenConfig, type EndpointsConfigDeps, EnqueueAgentQueuedTurnDelivery, EnqueueAgentTrigger, EnqueueBackgroundToolCompletion, EnrichResult, EnrichWithSkillConfigurableParams, EnricherDependencies, EntraPrincipalSearchType, ErrorController, type ErrorEvent, type ErrorHandler, EventActorDetachedActionLifecycle, EventChildGenerationLeaseDependencies, EventChildGenerationLeaseInput, EventHandler, ExecuteAgentEventActorInput, ExecuteAgentEventActorResult, ExecuteAgentRunParams, ExpiredFileSweepOptions, ExpiredFileSweepResult, type ExtendedJsonSchema, ExtendedOAuthTokens, ExternalChatMessage, ExternalMessagePart, ExternalToolCall, FILENAME_SEGMENT_MAX_BYTES, FILES_USAGE_BASE_HOLD_MS, FILES_USAGE_MAX_IDS, FILES_USAGE_QUEUED_RUN_ALLOWANCE, FILE_AUTHORING_TOOL_NAMES, FINE_GRAINED_TOOL_STREAMING_BETA, FailedMessageValidationResult, FallbackTokenUsageParams, FatalAgentInitializationOptions, FeedbackContentInput, FetchModelsParams, FetchTreeEntriesParams, type FileCitationAnnotation, FileCitationSelectionConfig, FileCitationSource, FileContentInput, FileDeletionOutcome, type FileObject, type FileSearchSource, FilesUsageBody, FilesUsageDeps, FilesUsageHold, FilesUsageResult, FilesUsageUser, type FinalEvent, type FinalMessageFields, FinalizeMCPAuthorizationMutationDeps, FinalizeMCPAuthorizationMutationParams, FireResult, FireableSchedule, FlowLease, type FlowManagerOptions, type FlowMetadata, type FlowState, FlowStateManager, FlowStateNotFoundError, type FlowStatus, type FormattedContent, type FormattedContentResult, FormattedMessageContentPart, FormattedMessageWithContent, type FormattedToolResponse, FrameOptionsAction, type FunctionCallArgumentsDeltaEvent, type FunctionCallArgumentsDoneEvent, type FunctionCallItem, type FunctionCallItemParam, type FunctionCallOutputItem, type FunctionCallOutputItemParam, type FunctionTool, type FunctionToolChoice, GENERATION_RECOVERY_FAILED_ERROR, GENERATION_RETRY_MAX, GENERATION_RETRY_PROBE_MAX, GENERATION_RETRY_WINDOW_MS, GENERIC_PROVIDER_ERROR, GITHUB_FINE_GRAINED_TOKEN_RECOMMENDATION, GRAPH_TOKEN_PLACEHOLDER, GeminiToolkit, GenerateActivityPhasePayload, type GenerateLabelPayload, type GenerateReasoningLabelPayload, type GenerateReasoningLabelRevisionOptions, GeneratedActivityPhase, type GeneratedReasoningLabel, type GenerationJob, GenerationJobManager, GenerationJobManagerClass, type GenerationJobManagerOptions, type GenerationJobMetadata, GenerationJobResult, type GenerationJobStatus, GenerationJobStore, type GenerationPredecessorState, type GenerationProtocolVersion, GenerationPublicationFencedError, GenerationStreamAttachmentOutcome, GenerationStreamRecoveryMethod, GenerationStreamRecoveryOutcome, GenerationStreamSubscriptionResult, GenerationStreamSubscriptionType, type GenericClient, GetAppConfigOptions, GetCanonicalFilesForInspection, GetHeldCapabilitiesFn, GetOpenAIModelsOptions, GetReadableConfigSectionsFn, GetRemoteAgentPermissionsDeps, GetResumeProvenanceInput, GetRolePermissions, GetURLParams, GetUploadAgent, type GetUserKeyFunction, type GetUserKeyValuesFunction, GetUserRoleByAuthorId, type GitHubRepoAdapterConfig, GitHubSkillSyncDeps, GitHubSkillSyncRunResult, GitHubSkillSyncRunner, GitHubSkillSyncScheduler, GitHubSkillSyncStatus, GitRepoAdapter, GoogleAdminRefreshDeps, GoogleAdminRefreshOptions, type GoogleAudioBlock, type GoogleConfigOptions, type GoogleCredentials, type GoogleDocumentBlock, type GoogleParameters, GoogleServiceKey, type GoogleVideoBlock, type GrantPrincipalType, GraphEvents, GraphSubagentHostConfig, GraphTokenOptions, GraphTokenResolver, GraphTokenResponse, GuardedToolsOptions, type GuardrailConfiguration, HITLRunWiring, HOST_FILE_AUTHORING_ARTIFACT_KEY, HasCapabilityFn, HasConfigCapabilityFn, HostSubagentTaskConfig, HostUpstreamTokenProviderResolver, type HostedTool, HstsOptions, type IEventTransport, type IJobStore, type IJobStoreV2, IMAGE_SIZE_PATTERN, ImageAuthorizationDeps, ImageAuthorizationOptions, type ImageContent, type ImageFormatter, ImageService, ImageServiceConfig, ImageServiceDeps, ImageUploadResult, ImageValidationResult, ImportLimits, ImportSkillDeps, InFlightBytesSemaphore, InMemoryEventTransport, InMemoryJobStore, type IncompleteDetails, type InferenceProfileConfig, InitialModelBoundAdmission, InitialModelBoundAdmissionCallback, InitializeAgentDbMethods, InitializeAgentParams, InitializeFn, type InitializeResultBase, InitializedAgent, InitializedAgentContextSource, InjectManualSkillPrimesParams, InjectManualSkillPrimesResult, InjectSkillCatalogParams, InjectSkillCatalogResult, InjectSkillPrimesParams, InjectSkillPrimesResult, type InputContent, type InputFileContent, type InputImageContent, type InputItem, type InputTextContent, type InputTokensDetails, InsightsAccessUser, InsightsAgentAccessDeps, InsightsPermissionChange, InsightsPermissionPrincipal, InsightsWrittenEntry, InstrumentPtcToolMapParams, type InternalMessage, InviteDeps, type ItemReferenceParam, type ItemStatus, JOB_STORE_V2_REQUIRED_METHODS, JobIdentity, type JobMetadataPatch, JobPredecessorMismatchError, JobState, type JobStatus, type JobStoreV2RequiredMethod, JsonPointer, type JsonPrimitive, type JsonSchemaType, type JsonValue, JwtTypeClaims, type LCAvailableTools, type LCFunctionTool, type LCManifestTool, type LCTool, type LCToolManifest, type LCToolRegistry, LIBRECHAT_CHECKPOINT_NAMESPACE_KEY, LIBRECHAT_CHECKPOINT_OWNER_KEY, LIBRECHAT_CHECKPOINT_STORAGE_OWNER_KEY, LIBRECHAT_EVENT_ACTOR_INVOCATION_KEY, LIBRECHAT_EXTENSION_NAMESPACE, LIBRECHAT_LEGACY_CHECKPOINT_KEY, LISTED_RUN_STATUSES, LIST_WORKSPACE_FILES_TOOL_NAME, type LLMConfigResult, LangfuseFeedback, LangfuseFeedbackMetadata, LangfuseScoreDestination, LangfuseScoreDestinationOptions, LangfuseSession, LangfuseSessionLinkParams, LangfuseTraceMessageFields, LangfuseTraceReaderDeps, LazyAgentHistoryCapabilities, LazyAgentHistoryMetadata, LazyAgentHistoryResolver, LazyMongoSaver, LazyMongoSaverOptions, LegacyPiiInspector, LegacyPiiMatch, LineageMessage, LineageSource, LoadAddedAgentDeps, LoadAgentDeps, LoadAgentParams, type LoadConfigModelsDeps, LoadPluginOptions, LoadSkillStatesParams, LoadToolApprovalHooksOptions, LoadToolDefinitionsDeps, LoadToolDefinitionsParams, LoadToolDefinitionsResult, LoadedPlugin, LoadedSkillDirectory, LoadedSkillStates, LogOpenIDCallbackFailureOptions, type LogProb, type LooseContentPart, MAX_AGENT_CONTEXT_SKILLS, MAX_AGENT_TRIGGER_BATCH_BYTES, MAX_AGENT_TRIGGER_BATCH_SIZE, MAX_AGENT_TRIGGER_ENVELOPE_BYTES, MAX_ALWAYS_APPLY_SKILLS, MAX_AVATAR_REFRESH_AGENTS, MAX_CODE_API_RATE_LIMIT_WAIT_MS, MAX_CONVERSATION_IMPORT_BSON_BYTES, MAX_CONVERSATION_IMPORT_DOCUMENT_BYTES, MAX_FILTERABLE_TEXT_BYTES, MAX_KEY_LENGTH, MAX_MANUAL_SKILLS, MAX_PAGE_LIMIT, MAX_PLUGIN_NAME_LENGTH, MAX_PRIMED_SKILLS_PER_TURN, MAX_RAW_PAYLOAD, MAX_RESOLVED_SCHEMA_NODES, MAX_SANDBOX_IMAGE_EXEC_CALLS, MAX_SHARED_LINKS_PAGE_SIZE, MAX_SHARED_LINK_CURSOR_LENGTH, MAX_SHARED_LINK_SEARCH_LENGTH, MAX_SKILL_NAME_LENGTH, MAX_SKILL_STATES, MAX_STORED_EXTRACTED_TEXT_BYTES, MAX_TASK_SNAPSHOTS, MAX_TEXT_CACHE_BYTES, MAX_TEXT_EXTRACT_BYTES, MAX_TITLE_LENGTH, MAX_USER_SUBMITTED_PATHS, MAX_USER_SUBMITTED_PATH_LENGTH, MCPAllowlistContext, MCPAllowlistResolver, MCPAuthenticationRefreshError, MCPAuthenticationRejectedError, MCPAuthorityProofResolver, MCPAuthorityProofResolverOptions, MCPAuthorityResolution, MCPAuthorityResolutionInput, MCPAuthorizationFenceRetryRecord, MCPAuthorizationFenceRetryService, MCPAuthorizationFenceRetryServiceDeps, MCPAuthorizationFenceRetryStorage, MCPAuthorizationFenceRetryWorkerPolicy, MCPAuthorizationPublicationDeps, MCPCatalogCapacityError, MCPCatalogStore, MCPConfigInitializationCanceledError, MCPConnection, MCPDomainNotAllowedError, MCPErrorCode, MCPErrorCodes, MCPInspectionFailedError, MCPManager, MCPOAuthCleanupDependencies, MCPOAuthFlowMetadata, MCPOAuthHandler, MCPOAuthSecretReentryRequiredError, MCPOAuthState, MCPOAuthTokens, type MCPOptions, type MCPPrompt, MCPRecoveryGenerationReader, MCPRecoveryGenerationScope, MCPReinitializeConfigResolution, MCPReinitializeStopResult, MCPRequestContext, MCPRequestContextOptions, MCPRequestScope, type MCPResource, type MCPRuntimeRequestBody, MCPServerCatalogLoaderDeps, MCPServerCatalogLoaderResult, MCPServerCatalogRecoveryDeps, MCPServerCatalogRecoveryInput, MCPServerCatalogRecoveryPolicy, MCPServerCatalogRecoveryTracker, MCPServerCatalogSnapshot, MCPServerContext, MCPServerOAuthDeletionSnapshot, type MCPServerSource, MCPServerTool, MCPServerTools, type MCPServers, MCPServersRegistry, MCPTokenRefreshUnavailableError, MCPTokenStorage, MCPTokenStorageUnavailableError, type MCPTool, MCPToolAlias, MCPToolCacheDeps, MCPToolCacheService, type MCPToolCallResponse, MCPToolInput, type MCPToolListResponse, MCPToolsChangedEvent, MCPToolsChangedGenerationHandler, MCPToolsChangedGenerationRenewalHandler, MCPToolsChangedHandler, MCPToolsChangedRevisionHandler, MCPToolsSnapshot, MCPTransportAuthenticationError, MCP_ALL_PLACEHOLDER_PREFIX, MCP_PLUGIN_SOURCE, ManifestResult, McpIdentityAgent, MemoryConfig, MemoryContentInput, MemoryManagementHandlersDeps, MergeAgentActionToolsParams, MessageDeltaData, type MessageItem, MessageRequestMiddleware, MessageRequestValidation, type MessageRole, MessageValidationDeps, MessageValidationRequest, MessageValidationResult, type Metadata, MetricsOptions, MigrationCheckDbMethods, MigrationCheckParams, MigrationCheckResult, MintedGoogleAdminToken, MintedToken, type MistralFileUploadResponse, type MistralOCRError, type MistralOCRRequest, type MistralOCRUploadResult, type MistralSignedUrlResponse, ModelBoundChatModelCallback, ModelBoundContentInput, ModelBoundHistoricalFileIdState, ModelBoundProviderAttribution, ModelBoundProviderContentInput, ModelBoundProviderMessage, ModelBoundProviderProvenance, ModelBoundProviderProvenancePart, ModelBoundSourceFileInput, ModelBoundSourceFileProjection, type ModelContent, ModelEndData, ModelErrorTracker, ModelErrorTrackerCallback, ModelOutputWithRefusal, ModelParameterContentInput, ModelResponseMetadata, ModelSpecEndpointResolution, ModelSpecParsedBody, type MongoServerError, NestedStringContext, type OAIClientOptions, OAUTH_CSRF_COOKIE, OAUTH_CSRF_MAX_AGE, OAUTH_SESSION_COOKIE, OAUTH_SESSION_COOKIE_PATH, OAUTH_SESSION_MAX_AGE, OAUTH_STATE_COOKIE_PREFIX, OAuthClientInformation, OAuthClientSource, type OAuthConnectionOptions, OAuthDetectionResult, OAuthFailureLog, OAuthFailureRequest, type OAuthHandledSource, OAuthLifecycleRelay, OAuthMetadata, OAuthPromptOptions, OAuthProtectedResourceMetadata, OAuthReconnectionManager, type OAuthStartHandler, type OAuthStartOptions, OAuthStateStore, OAuthStateStoreOptions, OAuthStoredClientMetadata, OAuthToolCall, type OCRImage, type OCRResult, type OCRResultPage, type OCRUsageInfo, OPENID_EXPIRY_BUFFER_SECONDS, OPENID_REFRESH_CANCELLED_BEFORE_GRANT, OPENID_REFRESH_OWNERSHIP_LOST, OPENID_TOKEN_FIELDS, OPENID_USER_ID_COOKIE, OboConfig, OboTokenResolutionError, OboTokenResolutionReason, OboTokenResolver, OboTrustChecker, OnAgentInitializedCallback, OpenAIChatModelStreamHandler, type OpenAIConfigOptions, type OpenAIConfigResult, type OpenAIConfiguration, OpenAIContentAggregator, OpenAIContentPart, OpenAIErrorResponse, type OpenAIFileBlock, OpenAIImageContentPart, type OpenAIInputFileBlock, OpenAIMessageDeltaHandler, OpenAIModelEndHandler, type OpenAIModelOptions, type OpenAIParameters, OpenAIReasoningDeltaHandler, OpenAIResponseContext, OpenAIRunStepDeltaHandler, OpenAIRunStepHandler, OpenAIStreamHandlerConfig, OpenAIStreamTracker, OpenAITextContentPart, OpenAIToolEndHandler, OpenIDCallbackAuthenticatorOptions, OpenIDCallbackRequest, OpenIDMarkerCookieOptions, OpenIDReauthRequiredError, OpenIDRefreshFlightDeps, OpenIDRefreshFlightService, OpenIDRefreshParams, OpenIDRefreshRecoveryDeps, OpenIDRefreshRecoveryService, OpenIDSessionIdentitySource, OpenIDSessionRefreshService, OpenIDTokenField, OpenIDTokenInfo, OpenIDUserLookupResult, OpenIdDiscoveryEnv, OpenIdEmailClaims, OpenIdIssuerSource, OpenIdRegistrationOptions, OpenIdRoleSyncClaimSource, OpenIdRoleSyncOptions, OpenIdRoleSyncSelectionResult, OpenIdSessionDeps, OpenIdSessionParams, type OpenRouterAudioBlock, type OpenRouterVideoBlock, OpenerPolicy, type OutputItem, type OutputItemAddedEvent, type OutputItemDoneEvent, type OutputTextContent, type OutputTextDeltaEvent, type OutputTextDoneEvent, type OutputTokensDetails, PARENT_SUBAGENT_INDEX_LIMITS, PARTIAL_FILE_DELETION_MESSAGE, PARTIAL_RESOLVED_CONVERSATION, PDFValidationResult, PENDING_ACTION_EXPIRED_CODE, PENDING_STALE_MS, PKCE_CHALLENGE_PATTERN, PKCE_CHALLENGE_TTL, PLUGIN_DATA_VAR, PLUGIN_MANIFEST_FILE, PLUGIN_MANIFEST_SCHEMA_ID, PLUGIN_MCP_FILE, PLUGIN_MCP_SCHEMA_ID, PLUGIN_ROOT_VAR, PLUGIN_SKILLS_DIR, PRIVATE_MODEL_SPEC_PRESET_FIELDS, type PageDimensions, type ParkedSteerClaim, type ParsedServerConfig, ParsedSkillMarkdown, PendingActionContext, PendingActionExpiredError, PendingOAuthStart, PendingRequestResult, PeoplePickerAccess, PeoplePickerRole, PersistMCPAuthorizationTransactionDeps, PersistMCPAuthorizationTransactionParams, PersistedMcpContentPart, PersistedMcpToolCall, PiiMatch, PinnedOrderHandlersDeps, PkceStrippableRequest, PlanAgentActionUpdateParams, PlannedAgentActionUpdate, PluginAuthor, PluginDiagnostic, PluginDiagnosticCode, PluginDiagnosticSeverity, PluginExtensionData, PluginHookBatchToolCall, PluginHookCapabilities, PluginHookCompatibilityIssue, PluginHookConditionMatch, PluginHookContribution, PluginHookExecutionRequest, PluginHookExecutor, PluginHookGroup, PluginHookHandler, PluginHookHandlerSupport, PluginHookHandlerType, PluginHookIssueCode, PluginHookIssueSeverity, PluginHookMatcherTranslation, PluginHookMatcherTranslationResult, PluginHookOnceStore, PluginHookPayload, PluginHookPlan, PluginHookPlanEntry, PluginHookPlanSummary, PluginHookRegistration, PluginHookRuntimeContext, PluginHookSource, PluginHookSourceOptions, PluginHookToolInputTranslation, PluginHookToolNameTranslation, PluginHookValidationIssue, PluginHooksDocument, PluginHooksParseResult, PluginHooksResult, PluginLoadResult, PluginManifest, PluginMcpContext, PluginMcpOptions, PluginMcpResult, PluginMcpServer, PluginSkillsResult, PreflightCodeOutputBatchInput, PreflightResumeContentInput, PreflightResumeContentResult, PrepareCodeOutputInput, PreparedAgentTriggerDelivery, PreparedCodeOutput, PreparedCodeOutputEntry, PreparedEntry, PreparedExternalMessages, PreparedRunFiles, PresetContentInput, PresetStateStrategy, PricingFns, PrimeInvokedSkillsDeps, PrimeInvokedSkillsForProfilesDeps, PrimeInvokedSkillsResult, PrimeSkillFilesParams, PrimeSkillFilesResult, Principal, PrincipalSearch, PrincipalSearchDeps, PrincipalSearchRequest, PrincipalSearchUser, PrivateModelSpecPresetField, ProcessAvatarParams, ProcessPublishedCodeOutputInput, ProcessedCodeOutput, type ProcessedFile, ProjectedStoredMemory, ProjectedStoredPreset, ProjectedStoredPrompt, ProjectedStoredPromptGroup, PrometheusMetrics, PromptContentInput, type PromptGroupsAllResponse, type PromptGroupsListResponse, PromptMigrationCheckDbMethods, PromptMigrationCheckParams, PromptMigrationCheckResult, PromptRecordInput, ProtectionFinding, type Provider, ProviderConfigResult, type ProviderInitializeParams, ProvisionCallbackDeps, ProvisionDeps, ProvisionFileUpdate, ProvisionService, ProvisionState, ProvisionToolContext, ProxyEnvConfig, PruneOrphansDeps, PublishGeneratedRunArtifactInput, PublishedCodeOutputResult, QUERY_DEVTOOLS_HEADER, QUOTE_MAX_COUNT, QUOTE_MAX_LENGTH, QueryDevtoolsRequest, REFRESH_TOKEN_COOKIE, RESUME_CONTEXT_KEYS, RESUME_HANDOFF_STALE_MS, RETAINED_ANSWERS_MESSAGE_ID, RETAINED_ANSWER_ROW_FIELDS, RateLimitReset, ReachableAgent, ReadFileOptions, ReadFileResult, type ReasoningConfig, type ReasoningContent, type ReasoningDeltaEvent, type ReasoningDoneEvent, type ReasoningEffort, type ReasoningItem, type ReasoningItemParam, type ReasoningLabelAttemptEvent, type ReasoningLabelEvent, type ReasoningLabelHostDeps, type ReasoningLabelHostScope, type ReasoningLabelHostWiringResult, type ReasoningLabelRun, type ReasoningLabelStatus, type ReasoningLabelUsageRecord, type ReasoningLabelWiring, type ReasoningSummary, type ReasoningTextContent, ReauthenticationRequiredError, RecordScheduleOutcomeInput, RecordUsageDeps, RecordUsageParams, RecordUsageResult, type RecoveredSteerPayload, RecoveredSteerPayloadMismatchError, RedactedServerConfig, RedisClient, RedisEventTransport, RedisJobStore, RedisOperationStatus, RedisRequestTelemetry, RedisSubagentTaskControlTransport, RedisSubagentTaskControlTransportOptions, RedisUseCases, ReferencedQuoteEntry, ReferrerPolicyToken, RefreshListAvatarsParams, RefreshS3UrlFn, RefreshStats, RefreshTokenBridgeDeps, RefreshTokenBridgeIdentity, RefreshTokenBridgeService, RefreshTokenset, type RefusalContent, type RefusalDeltaEvent, type RefusalDoneEvent, RegisterCodeExecutionToolsParams, RegisterCodeExecutionToolsResult, RegisterDeploymentPluginHooksOptions, RegisterFileAuthoringToolsParams, RegisterFileAuthoringToolsResult, RegisterPluginHooksOptions, ReleaseEventChildGenerationLease, RemoteAgentAccessDependencies, RemoteAgentAccessRequest, RemoteAgentAccessResult, RemoteAgentAuthDeps, RemoteFileHeaders, RenewBackgroundToolCompletionProducerLease, ReplayablePendingMCPOAuthStartOptions, RepoCommit, RepoTreeEntry, RepoTreeEntryType, type RequestBody, RequestLogContext, type RequestScopedMCPConnectionStore, type RequestValidationResult, RequireCapabilityFn, type ResolveActivityLabelModelParams, ResolveAgentScopedSkillIdsParams, ResolveAgentTurnExecutionPlanInput, ResolveAlwaysApplySkillsParams, ResolveAssistantToolPermissionsParams, ResolveConversationTitleOptions, ResolveMCPServerContextParams, ResolveManualSkillsParams, ResolveModelSpecSkillIdsParams, ResolveResendToolResourcesParams, ResolveSkillActiveParams, ResolveSkillCatalogParams, ResolveSubagentGraphsParams, ResolveTokenConfigParams, ResolveToolRoleGrantsParams, ResolveToolRolePermissionsParams, type ResolvedActivityConfig, type ResolvedActivityPhaseConfig, ResolvedAlwaysApplySkill, ResolvedAskUserQuestion, type ResolvedCheckpointerConfig, ResolvedManualSkill, type ResolvedPrincipal, type ResolvedReasoningLabelConfig, ResolvedSkillCatalog, ResolvedSkillPrime, ResolvedToolApprovalHook, type ResourceBody, type ResourceContents, type ResourceLink, ResourcePolicy, type Response$1 as Response, type ResponseAggregator, type ResponseCompletedEvent, type ResponseContext, type ResponseCreatedEvent, type ResponseError, type ResponseEvent, type ResponseFailedEvent, type ResponseInProgressEvent, type ResponseIncompleteEvent, type ResponseRequest, type ResponseStatus, type ResponseTracker, ResponsesRunEnvelope, ResponsesRunPayload, ResumeAgentEventActorInput, ResumeContentInspection, ResumeContentInspectionInput, ResumeContentProtectionDependencies, ResumeContext, ResumeRuntimeContentProjection, ResumeRuntimeContentProtectionDependencies, ResumeSnapshotAgent, type ResumeState, type ResumeSubscription, ResumeUserSubmittedProvenance, RetainedAnswer, RetainedAnswerRowQuery, RetainedAnswerSet, RetainedAnswerSource, RetainedAnswerTokenCounter, RetainedAnswersConfig, RetainedAnswersContextInput, RetentionConversation, RetentionDependencies, RetentionExpiry, RetentionLogger, RetentionRequest, RetireBackgroundToolCompletion, RumProxyEndpoint, RumProxyResult, RunArtifact, RunArtifactDescriptor, RunArtifactSnapshotAdapter, RunContextMetaParams, RunContextMetaSelection, RunDiscoverySnapshot, RunFadingTiers, RunFileActor, RunFileAudit, RunFileEncodingAgent, RunFileEncodingParams, RunFileEntry, RunFileExecution, RunFileHost, RunFileManifest, RunFileMessageEncoder, RunFileMessageEncoderDeps, RunFilePolicy, RunFilePreparation, RunFileReadMode, RunFileScope, RunFileSession, RunFileSessionDeps, RunFileStore, RunFileToolContext, type RunLLMConfig, RunStepDeltaData, RunToolSetConfig, type RuntimeInitializeParams, S3FileRef, S3KeyOptions, S3KeyParts, SAFE_CONVERSATION_TITLE, SCHEDULES_NOT_READY_CODE, SCHEDULES_UNAVAILABLE_CODE, SCHEDULE_FILE_HOLD, SCHEDULE_JITTER_WINDOW_MS, SEARCH_WORKSPACE_TOOL_NAME, SET_MEMORY_TOOL_NAME, SKILL_FILE_PREFIX, SKILL_MANIFEST_FILE, SKILL_MESSAGE_SOURCE, SKILL_SYNC_MAX_TIMER_INTERVAL_MINUTES, SKILL_TRIGGER_ALWAYS_APPLY, SKILL_TRIGGER_MANUAL, SKILL_TRIGGER_MODEL, SSEErrorDetails, SSEEvent, type SSEOptions, SSRFProtectionOptions, STEER_ENQUEUE_NOT_RUNNING, STEER_ENQUEUE_QUEUE_FULL, STEER_ENQUEUE_RECEIPT_FULL, STEER_MAX_FILES, STEER_QUEUE_MAX_DEPTH, type STTService, SUBAGENT_ACTIVITY_STREAM_LIMITS, SUBAGENT_COMPLETION_DELIVERY, SUBAGENT_COMPLETION_SOURCE, SUBAGENT_THREAD_VIEW_LIMITS, SUBAGENT_WAKEUP_GUIDANCE, SafeErrorMetadata, SameOriginGuardOptions, SamlSubjectError, SamlSubjectProfile, SamlSubjectResolution, SandboxImageChunk, SandboxImageChunkReader, SandboxImageReadResult, SaveBufferFn, SaveBufferParams, SaveURLParams, SaveURLResult, ScheduleDeleteResult, ScheduleEngine, ScheduleEngineDeps, ScheduleEngineState, ScheduleErasureDeps, ScheduleErasureSweep, ScheduleFileRef, ScheduleFireContext, ScheduleLimits, ScheduleMCPError, ScheduleMCPPreflight, ScheduleResumeClaimResult, ScheduleServiceTimings, ScheduleUserContext, ScheduleWriteGate, ScheduleWriteGateOptions, ScheduledTokenContext, ScheduledTokenIdentity, SchedulesHandlers, SchedulesHandlersDeps, SchedulesService, SchedulesServiceDeps, SearchablePrincipalType, SecurityHeaderOptions, SelectSkillPrimesForTurnResult, SendFeedbackScoreParams, type SerializableJobData, SerializedSharedFile, SerializedSharedMessage, type ServerRequest, type ServerSentEvent, type ServiceTier, ShareContentPreflight, ShareContentPreflightInput, ShareContentPreflightMessage, ShareContentPreflightOptions, ShareFileEtagSource, ShareLinkOperation, ShareLinkRejectionCode, SharePolicyDeps, SharedFileMetadataPolicyInput, SharedLangfuseSessionDeps, SharedLangfuseSessionParams, SharedLinkAccessDeps, SharedLinkRetentionDependencies, SharedRunArtifact, ShellCacheHeaders, ShutdownPhase, ShutdownTaskOptions, SkillCatalogSummary, SkillConfigurableContext, SkillConfigurableLoadResult, SkillContentInput, SkillFileContentPolicyInput, SkillFileRecord, SkillIdentity, SkillManagementDeps, SkillManagementResponse, SkillManagementUpdate, SkillPrimeContentPart, SkillPrimeWithTools, SkillStatesRecord, SkillStatesValidationError, SkillSyncAppConfigLike, SkillSyncRequestLike, SkillSyncRequestUser, SkillSyncTriggerOrchestrator, SkillSyncTriggerOrchestratorDeps, SkillSyncTriggerRunnerFactoryInput, SkillTrigger, SkillsHandlers, SkillsHandlersDeps, SortDirection, StampMcpServerIdentitiesParams, type StampedSteerMedia, type StdioOptions, type SteerCancelBody, type SteerDrainHookOptions, type SteerFileFetcher, type SteerMediaClient, type SteerMediaResult, type SteerOffsetState, type SteerQueueItem, type SteerRequestBody, type SteerRequestDeps, type SteerRequestResult, type SteerRequestUser, type SteerRunContext, type SteerStampTarget, SteeringLifecycle, StepBudgetHookOptions, StepTypes, StoredConversationDecision, StoredFileRef, StoredMessageBranchPolicyInput, StoredMessageContentInput, StoredMessagePartInput, StoredPromptGroupInput, type StrategyFunctions, type StreamEvent, type StreamHandlerConfig, StreamLimitsConfig, type StreamOptions, type StreamServices, type StreamServicesConfig, type StreamSubscription, type StreamableHTTPOptions, StructuredPromptTokens, StructuredTokenUsage, SubagentActivityEnvelope, SubagentActivityStream, SubagentActivitySubscriber, SubagentActivitySubscription, SubagentActivityTerminalStatus, SubagentActivityUpdateEvent, SubagentCancellationPlan, SubagentCompletionWakeupResolverDeps, SubagentTaskControlHandler, SubagentTaskControlTransport, SubagentTaskOwnerUnavailableError, SubagentTaskWakeupRegistration, SubagentThreadTaskStore, SubagentThreadTaskStoreOptions, SubagentThreadWriteGuardDeps, SubagentThreadWriteTarget, SubagentUsageEvent, type SubscribeOptions, type SubscribeWithResumeResult, type SummaryTextContent, type SystemMessageItemParam, TCheckSessionsAlive, type TCustomEndpointsConfig, TERMINAL_PUBLICATION_RECONNECT_ERROR, TFileUpdate, TFilterFilesByAgentAccess, TGetFiles, TGetSkillByName, TListSkillsByAccess, TLoadCodeApiKey, TOKEN_PROVIDER_COOKIE, TOOL_RESOURCE_KEYS, TProvisionToCodeEnv, TProvisionToVectorDB, TRANSIENT_SAML_NAME_ID_FORMAT, TUpdatePromptGroupSchema, type TerminalJobClaim, TerminalRunErrorLogger, TerminalRunErrorObserver, type TerminalSteerHook, type TerminalSteerHookInput, type TerraMindAttachmentContent, type TerraMindAttachmentEvent, TestRunHook, TestRunHookContext, type TextConfig, type TextContent, TextContentFragment, type TextField, type ThinkingConfigAdaptive, type ThinkingConfigDisabled, type ThinkingConfigEnabled, type ThinkingConfigParam, ThreadData, TitleTiming, type TokenConfig, TokenConfigParams, TokenCountFn, TokenCounter, TokenResult, TokenUsage, TokenizerSingleton as Tokenizer, TokenomicsDeps, type Tool, ToolApprovalAdmissionAgent, ToolApprovalAdmissionInput, ToolApprovalCallInput, ToolApprovalHook, ToolApprovalHookContext, ToolApprovalHookFactory, ToolApprovalHookModule, ToolApprovalPolicyLayers, ToolArgumentContentInput, ToolCacheKeys, ToolCall, type ToolChoice, type ToolChoiceValue, type ToolContentPart, ToolDefinition, type ToolDiscoveryOptions, type ToolDiscoveryResult, ToolEndCallback, ToolEndCallbackData, ToolEndCallbackMetadata, ToolEndData, ToolExecuteOptions, ToolFavoritesHandlersDeps, ToolInputValidationDetails, ToolInputValidationError, ToolInputValidationReason, ToolRegistryDefinition, ToolRoleGrants, type TopLogProb, TraceHandlerDeps, TraceHandlers, TraceQuery, TraceReadError, TraceReader, TraceRequest, TraceRouteHandler, TrackedActivity, TraversalAwareContentInspectionConfig, TraversalAwareContentInspectionResult, type TruncationValue, TurnDeliveryAgent, TurnDeliveryConfig, TxMetadata, UPLOAD_EXTRACTED_TEXT_PLANS, UPLOAD_FALLBACK_TEXT_PLANS, UninspectableFileBlockResponse, UninspectableFileError, UninspectableNestedContentResponse, UnionPrimeAllowedToolsParams, UnionPrimeAllowedToolsResult, type UnsubscribeFn, UpdateAgentFn, UploadAgent, UploadContentPolicyInput, UploadExtractedTextPlan, UploadFallbackTextExtractors, UploadFallbackTextPlan, UploadFallbackTextRoute, UploadFile, UploadFileParams, UploadImageParams, UploadMetadata, UploadPolicyErrorContext, UploadPolicyFile, UploadResult, UploadSseStream, UpstreamModelErrorMetadata, UpstreamTokenProvider, UpstreamTokenProviderResolver, UpstreamTokenTarget, UrlBuilder, type UrlCitationAnnotation, type Usage, type UsageMetadata, type UserConnectionContext, type UserKeyValues, type UserMCPConnectionOptions, type UserMessageItemParam, UserPreferencesHandlerDeps, UserPrincipalsCache, UserSubmittedMessageFieldPathState, UserSubmittedPathOptions, UserSubmittedPathState, UserSubmittedProvenanceWorkBudget, ValidateAgentModelParams, type ValidationError, ValidationResult, type VertexAIClientOptions, VertexAIConfigInput, VertexCredentialOptions, type VideoBlock, type VideoResult, VideoValidationResult, ViolationInfo, VisitNestedStringsBudget, VisitNestedStringsOptions, WORKSPACE_COMMAND_DEFAULT_TIMEOUT_MS, WORKSPACE_COMMAND_MAX_TIMEOUT_MS, WORKSPACE_EDIT_MAX_COUNT, WORKSPACE_WRITE_MAX_BYTES, WebSearchAuthResult, WebSearchSSRFAgents, type WebSocketOptions, WireSchedule, WorkspaceEditRequest, WorkspaceEditResult, WorkspaceExecuteCommandRequest, WorkspaceExecuteCommandResult, WorkspaceListRequest, WorkspaceListResult, WorkspacePreviewEditRequest, WorkspacePreviewEditResult, WorkspaceReadRequest, WorkspaceReadResult, WorkspaceSearchRequest, WorkspaceSearchResult, WorkspaceTextEdit, WorkspaceToolHttpError, WorkspaceToolRequest, WorkspaceToolResult, WorkspaceWriteRequest, WorkspaceWriteResult, YouTubeVideoPart, __resetCheckpointerForTests, __resetShutdownStateForTests, _resetOverrideStrictCache, acceptAgentStartupTelemetry, addFileToResource, agentAvatarSchema, agentBaseResourceSchema, agentBaseSchema, agentContextFingerprintsMatch, agentCreateSchema, agentFileResourceSchema, agentHasDeferredTools, agentHasInlineMemoryTools, agentHasProgrammaticTools, agentManagementCreateSchema, agentManagementDeleteResponseSchema, agentManagementErrorCodeSchema, agentManagementErrorSchema, agentManagementListResponseSchema, agentManagementListSchema, agentManagementResponseSchema, agentManagementUpdateSchema, agentRequestsAskUserQuestion, agentRunUsesCheckpointer, agentStartupIngressMiddleware, agentStartupMilestones, agentStartupResults, agentStartupTelemetryMiddleware, agentSubagentsSchema, agentSupportContactSchema, agentToolOptionsSchema, agentToolResourcesSchema, agentUpdateSchema, agentUsesSubagentCompletionWakeups, aggregateCollectedUsage, aggregateEmittedUsage, aliasMCPToolOptions, allowHydratedFileReferences, anyAgentReplaysReasoningContent, apiNotFound, appendAnthropicBetaHeader, appendCodeEnvFile, appendCodeEnvFileIdentity, appendLeafSuffix, appendResolvedAskUserQuestion, appendYouTubeVideoParts, applyAdminRefresh, applyAttachmentOnlyText, applyAxiosProxyConfig, applyCheckpointDelivery, applyCitationLimits, applyContextToAgent, applyCspNonce, applyDefaultParams, applyGoogleAdminRefresh, applyModelSpecPreset, applyResumeContext, applyResumeModelParameters, applyRetainedAnswers, applySSRFSafeAgentIfDirect, applyTestRunHook, applyTurnDelivery, askUserQuestionToolSchema, assertAgentAttachmentLimits, assertAgentAttachmentTopology, assertAttachedCodeEnvironmentApprovalSupported, assertChatMutationAllowed, assertCodeApiJwtSigningReady, assertCodeExecutionApprovalBinding, assertConversationImportContentAllowed, assertConversationImportWriteSize, assertDirectToolOutputAllowed, assertExtractedTextInspectable, assertHydratedFileInspectable, assertJobStoreV2, assertModelBoundContent, assertModelBoundProviderContent, assertRemoteFileContentLength, assertRemoteFileURL, assertResumeContentAllowed, assertResumeRuntimeContentAllowed, assertSharedFileMetadataAllowed, assertSkillFileContentAllowed, assertStoredMessageBranchAllowed, assertStoredMessageMutationAllowed, assertUploadContentAllowed, assistantToolRolePermissions, attachAskUserQuestionAnswer, attachAskUserQuestionAnswers, attachAskUserQuestionArgs, auditInsightsPermissionChanges, autoMigrateLegacyLink, awaitOboOperation, azureAISearchSchema, backfillRemoteAgentPermissions, backgroundCompletionWakeupsEnabled, batchDeleteKeys, bindUpstreamTokenProviderResolver, blockFilteredActionProjection, blockFilteredMemoryContent, blockFilteredSkillFile, bootstrapCredentials, boundSubagentActivityUpdate, boundedClaim, boundedSubagentTaskResult, boundedTaskList, breakParentCycles, buildAbortedResponseMetadata, buildActionOAuthTokenDeleteQueries, buildAgentAdditionalInstructions, buildAgentContextAttachmentsByAgentId, buildAgentEventActorSkillMessages, buildAgentInitialToolSessions, buildAgentInstructions, buildAgentScopedAttachmentMap, buildAgentScopedContext, buildAggregatedResponse, buildAliasMatcherPattern, buildAskUserQuestionPayload, buildAttachedCodeEnvironmentAdmissionHooks, buildAttachedWorkspaceBashDescription, buildAttachedWorkspaceBashSchema, buildAuditContext, buildAuthUserDocCacheKey, buildAuthUserDocReverseIndexKey, buildBalanceUpdateFields, buildBudgetNotice, buildCodeEnvDownloadQuery, buildCodeEnvIdentityParams, buildCompletionUsage, buildCspDirectives, buildDeleteFilesResponse, buildFireClientRequestId, buildHITLRunWiring, buildHistoricalToolNames, buildImageToolContext, buildInitialToolSessions, buildInlineMemoryContext, buildInlineMemoryTool, buildMCPAuthRunStepCompletedEvent, buildMCPAuthRunStepDeltaEvent, buildMCPAuthRunStepEndDeltaEvent, buildMCPAuthRunStepEvent, buildMCPAuthStepId, buildMCPAuthToolCall, buildMessageFiles, buildNonStreamingResponse, buildOAuthFailureLog, buildOAuthToolCallName, buildOpenIDRefreshParams, buildPendingAction, buildPersistedContextUsage, buildPrompt, buildPromptGroupFilter, buildRecoveredSteerPayload, buildResolvedAskUserQuestion, buildResponse, buildResponsesNonStreamingResponse, buildResponsesUsage, buildRetainedAnswersContext, buildRunToolSet, buildSafeAuthLogContext, buildSafeRequestLogContext, buildSandboxImageReaderCode, buildSecurityHeaderOptions, buildServerNameAliases, buildShareFileEtag, buildSharedLinkStartupPayload, buildSkillPrimeContentParts, buildSkillPrimeMessage, buildSteerMedia, buildSubagentThreadTaskConfig, buildTenantIsolationErrorLogContext, buildTokenConfigMap, buildToolApprovalExecutionConfig, buildToolApprovalHooks, buildToolApprovalPayload, buildToolClassification, buildToolRegistryFromAgentOptions, buildToolSet, buildWebSearchContext, buildWebSearchDynamicContext, bulkWriteTransactions, cacheConfig, cadenceIntervalMinutes, cadenceToCron, canAgentGraphPause, canBackfillSharedServerInstructions, canInspectUploadExtractedTextAfterProcessing, canInspectUploadTranscriptAfterProcessing, canUseAppConnection, cancelAgentEventActor, cancelMCPToolsChanged, canonicalRecoveryFileIds, capabilityContextMiddleware, capabilityStore, captureActivityBlockContext, captureAgentCheckpointGeneration, captureAgentEventCheckpoint, captureCodeExecutionApprovalBinding, captureResumeModelParameters, captureScheduleFireContext, captureSubagentIdentity, checkAccess, checkAccessWithRequestCache, checkAgentPermissionsMigration, checkAgentUploadAuth, checkAndIncrementPendingRequest, checkBalance, checkCodeFileActive, checkConfig, checkCredentialDatabase, checkEmailConfig, checkHealth, checkInterfaceConfig, checkPluginAuth, checkPromptCacheSupport, checkPromptPermissionsMigration, checkRemoteAgentAccess, checkToolResourceUploadPermission, checkToolRolePermission, checkUserKeyExpiry, checkVariables, checkWebSearchConfig, checkpointOwnerNamespacePrefix, checkpointStorageConfigs, claimCodeDestination, classifyAgentEventRunOutcome, classifyBatch, classifyCodeArtifact, cleanupBulkSharedLinkPermissions, cleanupDeletedMCPServerOAuthUsers, cleanupMCPRequestContext, cleanupMCPRequestContextForReq, cleanupMCPServerOAuth, cleanupMCPToolSchemas, cleanupSharedLinkPermissions, clearCloudFrontCookies, clearMcpConfigCache, clearRemoteAgentAuthCache, clearStoredMCPOAuthState, clearToolApprovalHooks, cloneLineage, codeEnvironmentPairingLimiter, codeEnvironmentStatusIpLimiter, codeEnvironmentStatusLimiter, codeExecutionAuthHeaders, codeExecutionHeaders, codeServerHttpAgent, codeServerHttpsAgent, collectAgentAttachmentStats, collectAliasMatcherNames, collectAttachedCodeEnvironmentAgentIds, collectAttachedCodeEnvironmentPolicySettings, collectCodeExecutionProfileRoutes, collectEdgeAgentIds, collectFileIds, collectFreshSkillPrimeNames, collectMCPToolAliases, collectModelBoundHistoricalFileIdState, collectModelBoundHistoricalFileIds, collectReachableAgents, collectRetainedAnswers, collectRunMCPToolAliases, collectSteerStampTargets, collectToolCallIds, collectToolResourceFileIds, commandExecutorCapabilities, comparePassword, completeMCPAuthorizationWithTokenWaiters, computeAgentRequestFingerprint, computeCreateDigest, computeLegacyAgentRequestFingerprint, computeNextRunAt, computeSummaryUsedTokens, computeUsageCostUSD, configureAgentEventRuntime, configureFileConfigRegexEngine, configureMessageFilterRegexValidator, configureReasoning, configureServerTimeouts, conflictingAzureVariables, constructAzureChatBasePath, constructAzureInstanceBasePath, constructAzureResponsesURL, constructAzureURL, containsGraphTokenPlaceholder, contentFilterBlockResponse, contentFilterModelBoundBlockResponse, contentFilterUninspectableResponse, controlFingerprint, convertInputToMessages, convertJsonSchemaToZod, convertMessages, convertOcrToContextInPlace, convertWithResolvedRefs, countFormattedMessageTokens, countRetainedToolTokens, countTokens, createActivityLabelHook, createActivityLabelWiring, createActivityPhaseWiring, createAdminAuditLogHandlers, createAdminCodeEnvironmentHandlers, createAdminConfigHandlers, createAdminGrantsHandlers, createAdminGroupsHandlers, createAdminLangfuseHandlers, createAdminRolesHandlers, createAdminSkillsSyncAccess, createAdminSkillsSyncHandlers, createAdminUsersHandlers, createAgentAdminPermissionAccess, createAgentChatCompletion, createAgentContextFingerprint, createAgentContinuationResolver, createAgentEventActionRecorder, createAgentEventActorActionAdmissionId, createAgentEventActorDetachedActionLifecycle, createAgentEventActorSummary, createAgentEventActorTurn, createAgentEventBindingHandlers, createAgentEventContinueResolver, createAgentEventDetachedResumeHandler, createAgentEventTerminalHandler, createAgentExecutionContext, createAgentManagementAuth, createAgentManagementCreateHandler, createAgentManagementDeleteHandler, createAgentManagementFileHandlers, createAgentManagementReadHandlers, createAgentManagementUpdateHandler, createAgentManagementUploadResponse, createAgentMemoryCallback, createAgentMemoryPartitionMiddleware, createAgentQueuedTurnLifecycle, createAgentRunEnvelope, createAgentStartupTelemetry, createAgentTriggerDeliveryEngine, createAgentTriggerEnvelope, createAgentTriggerExecutionHost, createAgentTriggerIngressHandlers, createAgentTriggerService, createAgentUploadLock, createAggregatorEventHandlers, createAnthropicVertexClient, createApiKeyHandlers, createApiKeyServiceDependencies, createAppConfigService, createArchiveAllHandler, createAskUserQuestionTool, createAssistantPhaseStampingHandlers, createAttachedCodeEnvironmentPolicyHook, createAttachedWorkspaceBashTool, createAuthIdentityContext, createAxiosInstance, createBackgroundCodeResultHandler, createBackgroundToolCompletionWakeupHandler, createBackgroundToolCompletionWakeupResolver, createBackgroundToolDeadClaimRecovery, createBackgroundToolResultHandler, createBalanceReservations, createBearerAuthHeader, createCachedTokenCounter, createChatGptLineage, createChatRunFileBindings, createCheckAgentTriggerAccess, createCheckRemoteAgentAccess, createCheckpointDeletionReclaimer, createChunk, createClusteredFileSweep, createCodeApiRateLimitBudget, createCodeApiUploadRegistry, createCodeApiUploadScope, createCodeBridgePairing, createCodeBridgeStatusPoller, createCodeDestinationSet, createCodeEnvironmentHttpHandlers, createCodeEnvironmentRegistry, createCodeExecutionRouteKey, createCodeOutputPersistence, createCommandExecutor, createCompactionSemanticIndexProjection, createConcurrencyLimiter, createConfiguredContentInspector, createContentFilter, createContentIndexOffsetHandlers, createContextMetaPublisher, createCspPolicy, createCustomError, createDeadlineAbortSignal, createDeleteMemoryTool, createDeploymentSkillMethods, createDetachedSubagentUsageRecorder, createEdgeCollector, createEmptyPromptGroupsResponse, createEndpointsConfigService, createErrorResponse, createEventChildGenerationLeaseAcquirer, createExpiredFileSweepRequest, createFetch, createFunctionCallItem, createFunctionCallOutputItem, createGitHubSkillSyncRunner, createGitIdentityProgrammaticBashTool, createHandleLLMNewToken, createHandleOAuthToken, createImageAuthorizationMiddleware, createImportHandler, createInitialModelBoundAdmissionCallback, createInitializedAgentContextFingerprint, createInsightsAccessHandler, createInsightsAgentAccessResolver, createInsightsHandler, createInvite, createIoRedisSubscriber, createLangfuseTraceReader, createLazyAgentHistoryResolver, createLazyOboUpstreamTokenProvider, createLegacyPiiInspector, createLoadConfigModels, createMCPAuthorizationFenceRetryService, createMCPCatalogStore, createMCPRequestContext, createMCPRuntimeRequestBody, createMCPToolCacheService, createMemoryCallback, createMemoryManagementHandlers, createMemoryOnceStore, createMemoryProcessor, createMemoryTool, createMessageFilterPii, createMessageItem, createMessageRequestMiddleware, createMetrics, createMinimalRetentionRequest, createModelBoundChatModelCallback, createModelErrorTracker, createMultiAgentMapper, createOAuthStateStore, createOpenAIContentAggregator, createOpenAIHandlers, createOpenAIStreamTracker, createOpenIDCallbackAuthenticator, createOpenIDOboIdentityTuple, createOpenIDRefreshFlightService, createOpenIDRefreshIdentityTuple, createOpenIDRefreshOwnershipError, createOpenIDRefreshRecoveryService, createOpenIDSessionIdentity, createOpenIDSessionRefreshService, createOutputTextContent, createOwnedToolEndHandler, createParentSubagentIndexHandler, createPeoplePickerAccess, createPinnedOrderHandlers, createPluginHookPayload, createPrincipalSearch, createProjectHandlers, createProvisionFilesCallback, createProvisionService, createReasoningItem, createReasoningLabelHostWiring, createReasoningLabelWiring, createReasoningTextContent, createRedisRequestTelemetry, createRefreshTokenBridgeIdentity, createRefreshTokenBridgeService, createRemoteAgentAuth, createRemoteFileByteLimitTransform, createRequestAgentExecutionContext, createRequireApiKeyAuth, createResolveAgentFireAccess, createResponseAggregator, createResponseContext, createResponseTracker, createResponsesEventHandlers, createRun, createRunArtifactPublisher, createRunArtifactSnapshotAdapter, createRunFileHost, createRunFileManifest, createRunFileMessageEncoder, createRunFileSession, createSSRFSafeAgents, createSSRFSafeUndiciConnect, createSafeUser, createSameOriginGuard, createScheduleMCPPreflight, createScheduleUpstreamTokenProviderResolver, createScheduleWriteGate, createSchedulesHandlers, createSchedulesService, createSecurityHeaders, createSequentialChainEdges, createSetBalanceConfig, createShareContentPreflight, createSharePolicyMiddleware, createSharedLangfuseSessionResolver, createSharedLinkAccessMiddleware, createSharedLinkConfigMiddleware, createSkillContentDigest, createSkillManagementHandlers, createSkillSyncTriggerOrchestrator, createSkillsHandlers, createStatefulCodeEnvironmentPolicyError, createSteerDrainHook, createSteerIndexOffsetHandlers, createSteerPreemptBoundaryHook, createSteerPreemptPoll, createSteerTerminalContinuationHook, createStepBudgetHook, createStreamEventHandlers, createStreamServices, createSubagentActivityStreamHandler, createSubagentCompletionWakeupHandler, createSubagentCompletionWakeupResolver, createSubagentControlHandler, createSubagentThreadScopeId, createSubagentThreadTaskStore, createSubagentThreadTurnGuard, createSubagentThreadViewHandler, createSubagentUsageSink, createSubagentWakeupHandleHook, createTerminalRunErrorObserver, createTokenCounter, createToolExecuteHandler, createToolExecuteHandlers, createToolFavoritesHandlers, createTraceHandlers, createTraceReadLimiter, createUserPreferencesHandler, credentialMetadataCollection, credentialMetadataId, credentialNames, dalle3Schema, decrementPendingRequest, decrypt$1 as decrypt, decryptSensitiveValue, decryptV2, decryptV3, deferStateToStore, deleteAgentCheckpoint, deleteAgentEventCheckpointReference, deleteAgentEventCheckpointReferences, deleteAllSharedLinksWithCleanup, deleteConvoSharedLinksWithCleanup, deleteFileFromCloudFront, deleteFileFromS3, deleteMistralFile, deleteOwnedActorCheckpointScope, deleteOwnedAgentCheckpoints, deleteRagFile, deleteSharedLinkWithCleanup, deprecatedAzureVariables, deriveBaseURL, describeRunFiles, detachOnAbort, detectGenerationRetry, detectOAuthRequirement, deterministicHexSuffix, discoverConnectedAgents, dispatchAgentTrigger, dropUnusableSummaryParts, duplicateIoRedisClient, emitAttachment$1 as emitAttachment, emitError, emitFunctionCallArgumentsDelta, emitFunctionCallArgumentsDone, emitFunctionCallItemAdded, emitFunctionCallItemDone, emitFunctionCallOutputItem, emitMessageItemAdded, emitMessageItemDone, emitOutputTextDelta, emitOutputTextDone, emitReasoningContentPartAdded, emitReasoningContentPartDone, emitReasoningDelta, emitReasoningDone, emitReasoningItemAdded, emitReasoningItemDone, emitResponseCompleted, emitResponseCreated, emitResponseFailed, emitResponseInProgress, emitTextContentPartAdded, emitTextContentPartDone, encodeAndFormatAudios, encodeAndFormatDocuments, encodeAndFormatImages, encodeAndFormatVideos, encodeHeaderValue, encrypt$1 as encrypt, encryptSensitiveValue, encryptV2, encryptV3, enrichRemoteAgentPrincipals, enrichWithSkillConfigurable, enrollAgentExecution, ensureCollectionExists, ensureDirectoryPrincipalUser, ensureLinkPermissions, ensureRequiredCollectionsExist, escapeHtmlAttribute, escapeJsonPointer, escapeRegex, estimateMediaTokensForMessage, evalKeyvRedisScript, exchangeAdminCode, excludeHiddenModelSpecs, executeAgentEventActor, executeAgentRun, executeConversationImportWrites, executeWorkspaceTool, exemptAgentTriggerFromIpLimiter, exemptAskUserQuestionFromApproval, exemptFromConcurrencyLimiter, exemptFromUserLimiter, expandPluginVariables, extractAgentContent, extractAssistantActionContent, extractAssistantContent, extractBaseURL, extractChatContent, extractCodeArtifactInspectionText, extractCodeArtifactRawText, extractCodeArtifactText, extractConversationImportContent, extractConversationTitleContent, extractDefaultParams, extractDiscoveredToolsFromHistory, extractFeedbackContent, extractFileContent, extractFileContext, extractInspectableFileText, extractInvokedSkillsFromPayload, extractKeyFromS3Url, extractMCPServerDomain, extractMCPServers, extractMCPToolDefinition, extractManualSkills, extractMemoryContent, extractMessageContent, extractModelParameterContent, extractOpenIDTokenInfo, extractPresetContent, extractPresetPromptContent, extractPromptContent, extractSSEErrorMessage, extractSkillContent, extractStoredMessageContent, extractTerraMindParams, extractToolArgumentContent, extractWebSearchEnvVars, extractYouTubeUrls, fetchAnthropicModels, fetchModels, fetchOpenAIModels, fileSearchSchema, filterAccessibleIdsBySharedLogic, filterChatSelectableMCPServers, filterFilesByEndpointConfig, filterFilesByEndpointRuntimeConfig, filterMalformedContentParts, filterOrphanedEdges, filterPersistableAbortContent, filterUniquePlugins, finalizeMCPAuthorizationMutation, findAgentEventAppliedAction, findAllArtifacts, findAskUserQuestionContentIndex, findCheckpointSummaryPart, findDisallowedDecisions, findIncompleteDecisions, findMatchingPattern, findModelSpecByName, findOpenIDUser, findPiiMatchInMessages, findPrimaryAgentId, findShadowedServerNames, findUndecidedToolCalls, finishRedisRequestTelemetry, fireSchedule, flattenArtifactPath, fluxApiSchema, forceRefreshCloudFrontAuthCookies, forkAgentEventCheckpoint, formatAuthLogMessage, formatMCPServerTools, formatPromptGroupsResponse, formatQuotesAsMarkdown, geminiToolkit, genAzureChatCompletion, genAzureEndpoint, generateAdminExchangeCode, generateAgentTriggerToken, generateArtifactsPrompt, generateCapabilityCheck, generateCheckAccess, generateItemId, generateOAuthCsrfToken, generateReasoningLabelRevision, generateResponseId, generateServerNameFromTitle, generateShortLivedToken, generationRetryLimiter, generationRetryProbeLimiter, getAccessToken, getAdminPanelUrl, getAgentCheckpointer, getAgentContextAttachments, getAgentFileRetentionExpiry, getAgentMemoryPartitionAccess, getAgentStartupTelemetry, getAgentTriggerIdempotencyKey, getAllMessagesUpToParent, getAllToolDefinitions, getAnthropicModels, getAppConfigOptionsFromUser, getApprovalTtlMs, getAssistantToolDefinitions, getAttachmentTitleText, getAudioFormat, getAuthFailureErrorName, getAuthFailureReason, getAuthFailureReasonCategory, getAuthUserDocCacheMode, getAuthUserDocCacheTtlMs, getAuthorizedRunFileSnapshot, getAvatarFileStrategy, getAvatarSaveParams, getAzureContainerClient, getAzureCredentials, getAzureDeploymentName, getBalanceConfig, getBasePath, getBedrockModels, getBlockedOpaqueFileField, getBlockedUninspectableFileField, getBlockedUninspectableSkillFileField, getBlockedUploadTranscriptField, getBoundedAskUserAnswerValues, getBoundedCodeOutputByteLimit, getBoundedOwnEnumerableEntries, getBuiltInBaseURL, getCachedAuthUserDoc, getCachedPrincipals, getCanonicalFileInspectionCoverage, getCapturedUserSubmittedPathMetadata, getClaudeHeaders, getCloudFrontConfig, getCloudFrontCookieRefreshWindowSec, getCloudFrontCookieTiming, getCloudFrontDownloadURL, getCloudFrontFileStream, getCloudFrontURL, getCodeApiAuthHeaders, getCodeApiRetryAfterMs, getCodeApiTenantId, getCodeApiUploadOptions, getCodeBridgeWorkerStatus, getCodeEnvFileOptions, getCodeEnvUploadFilename, getCodeExecutionBaseUrl, getCodeExecutionRouteKey, getCodeFileInfo, getCodeWorkspaceSelectionErrorDetails, getCodeWorkspaceSelections, getConfiguredExpiredFileAssistantVersion, getConfiguredFileSizeLimit, getContentFilterError, getContentTraversalFragments, getContentTraversalScopes, getConversationExpirationDate, getCredentialFingerprints, getCredentialRuntimeState, getCustomEndpointConfig, getDeploymentPluginHookCapabilities, getDeploymentPluginMcpServers, getDeploymentPluginRegistry, getDeploymentPluginSkills, getDeploymentSkillById, getDeploymentSkillDownloadStream, getDeploymentSkillIds, getDeploymentSkillRegistry, getDynamicToolContexts, getEdgeKey, getEdgeParticipants, getEffectivePort, getEndpointsDropParamsMap, getEntraPrincipalSearchType, getEnvProxyDispatcher, getExpiredFileAssistantVersion, getExpiredFileEndpoint, getExpiredFileRetryDelay, getExtractedTextFormat, getFailedTurnTraceFields, getFileBasename, getFileExtractionLogDetails, getFileRetentionMaxAttempts, getFileRetentionSweepInterval, getFileStream, getFirebaseStorage, getForcedTokenEndpointAuthMethod, getGenerationElapsedMs, getGoogleConfig, getGoogleModels, getHttpsProxyAgent, getImageBasename, getInsightsPrincipalState, getInvite, getIssuerBoundConditions, getLLMConfig, getLabelUsageSequenceSeed, getLangChainErrorCode, getLangfuseDestinationId, getLangfuseSampleRate, getLangfuseTraceDestinationIds, getLangfuseTraceMessageFields, getLatestEventActorSummary, getLazySubagentConfigId, getLegacyCredentialNames, getMCPAppToolsPublicationGeneration, getMCPInstructionsForServers, getMCPOAuthLeaseId, getMCPRequestContext, getMCPRequestScope, getMCPServerGeneration, getMCPServerNamesFromTools, getMCPToolsChangedGeneration, getMachineClientId, getMemoryAgentId, getMemoryAgentIdParam, getMemoryToolDefinitions, getMissingCustomUserVars, getMissingJobStoreV2Methods, getMissingRuntimeBodyPlaceholderFields, getModelMaxOutputTokens, getModelMaxTokens, getModelRefusalInfo, getModelTokenValue, getNewS3URL, getOAuthFailureMessage, getOAuthPromptExpiresAt, getOpenAIConfig, getOpenAILLMConfig, getOpenAIModels, getOpenIdEmail, getOpenIdIssuer, getOpenIdProxyDispatcher, getOpenIdRoleSyncOptions, getOpenIdRolesForOpenIdSync, getPluginHookOnceStore, getPluginHookSource, getProviderConfig, getProxyDispatcher, getProxyEnvConfig, getProxyUrlForUrl, getRandomValues, getRateLimitReset, getReasoningKey, getReferencedQuoteEntries, getReferencedQuotes, getRegisteredToolApprovalHookCount, getRemainingShutdownMs, getRemoteAgentPermissions, getRemoteFileFetchMaxBytes, getRemoteFileFetchTimeoutMs, getReplayablePendingMCPOAuthStart, getReplayablePendingMCPOAuthStartFromFlow, getRequestMemories, getRequestedPrincipalTypes, getResumeAgentSnapshot, getResumeContentInspection, getResumeProvenance, getResumeUserSubmittedMessageFieldPaths, getResumeUserSubmittedPaths, getRetentionExpiry$1 as getRetentionExpiry, getRumProxyBodyLimit, getRumProxyClientUrl, getRumProxyTargetBaseUrl, getRumProxyTimeoutMs, getRunDiscoveredTools, getRuntimeBodyPlaceholderFields, getS3DownloadURL, getS3FileStream, getS3Key, getS3URL, getSafeErrorMetadata, getSafeErrorText, getSafeUserSubmittedPathSegments, getSafetySettings, getSandboxImageChunkBytes, getScheduleMCPFailureCode, getScoreDestinations, getServerCustomUserVars, getServerNameFromTool, getSharedLinkExpiration, getShellHandlerIssue, getShutdownElapsedMs, getSignedUrl, getSkewedTokenCacheTtlMs, getSkewedTokenExpiresAtMs, getSteerMaxLength, getStorageMetadata$1 as getStorageMetadata, getStorageMetadataForKey, getSummaryPartText, getTerraMindRolesForOpenIdSync, getThreadData, getTokenCacheTtlMs, getTokenConfigKey, getTokenExpiresAt, getTokenExpiresAtMs, getToolDefinition, getToolInputValidationDetails, getToolSchema, getToolkitKey, getTransactionsConfig, getTranscriptApplicability, getUploadExtractedTextPlan, getUploadFallbackTextPlan, getUploadedCodeEnvFilename, getUpstreamModelErrorMetadata, getUserFacingProviderError, getUserFacingResumeError, getUserMCPAuthMap, getUserSubmittedMessageFieldPathState, getUserSubmittedPathState, getValidOpenIdReuseUserId, getVertexCredentialOptions, getVertexDeploymentName, getViolationInfo, getWebSearchInstallEntries, getWebSearchUninstallFields, googleSearchSchema, grantCreationPermissions, graphEdgeSchema, guessMimeType, handleAgentQueuedTurnCancel, handleAgentQueuedTurnEnqueue, handleAgentQueuedTurnList, handleError, handleFilesUsageRequest, handleJsonParseError, handleKeyvRedisError, handleSteerArm, handleSteerCancel, handleSteerRequest, hasAbortInFlight, hasActiveFileFieldPolicy, hasActiveFilePolicy, hasCustomUserVars, hasDeploymentPluginHooks, hasDeploymentPluginToolApprovalHooks, hasDurableAgentInterruptCheckpoint, hasExpiredFileEndpointConfig, hasInvalidToolApprovalResolutions, hasLangfuseEnvCredentials, hasMCPToolsChangedHandler, hasModelBoundContentProtection, hasOfficeHtmlPath, hasPersistableAbortContent, hasRecordedPrimaryUsage, hasRecordedProviderUsage, hasResumeContentProtection, hasResumeHandoffInFlight, hasRuntimeContextPlaceholders, hasRuntimeUrlPlaceholders, hasSupportContact, hasUrlContextTool, hasUsableTokenExpiry, hasYouTubeVideoParts, hashBackupCode, healToolApprovalPolicy, hydrateMissingIndexTokenCounts, hydrateResumeRunSteps, inferClientAuthMethod, initializeAgent, initializeAnthropic, initializeAzureBlobService, initializeBedrock, initializeCloudFront, initializeCustom, initializeDeploymentPlugins, initializeDeploymentSkills, initializeFileStorage, initializeFirebase, initializeGoogle, initializeOpenAI, initializeS3, injectBootstrapConfig, injectConfiguredFooterBootstrap, injectManualSkillPrimes, injectSkillCatalog, injectSkillPrimes, inputSchema, inspectContent$1 as inspectContent, inspectContentWithTraversal, inspectLegacyPii, inspectPromptContent, instrumentIORedisClient, instrumentMongooseQueryMetrics, instrumentPtcToolMap, instrumentRedisCache, invalidateCachedAuthUserDoc, invalidateRequestMemories, ioredisClient, isAbortError, isAccessTokenJwt, isActionDomainAllowed, isActiveAgentWorkspaceConfiguration, isActiveExpirationDate, isAddressAllowed, isAdminPanelRedirect, isAgentAttachmentLimitError, isAgentEventRetentionActive, isAgentFadingTier, isAgentFadingTierEntries, isAgentPermissionsAdmin, isAgentRunCancellation, isAgentScopedFile, isAgentTriggerRequest, isAnthropicVertexCredentials, isAskUserQuestionAdminDisabled, isAttachmentObjectNotFoundError, isBinaryBuffer, isBooleanOrStringTrue, isCanonicalAzureURL, isChatCompletionValidationFailure, isChatSelectableMCPServer, isClientRejectionMessage, isCodeApiJwtAuthEnabled, isCodeApiRateLimitError, isCodeArtifactToolOutput, isCodeFileToolName, isCodeSessionToolName, isConcurrentLimitEnabled, isConfiguredProviderMediaType, isConfirmedGenerationRetry, isContentFilterError, isContentTraversalLimitError, isContentTraversalProtected, isConversationImportError, isDataUri, isDeepSeekReasoningProvider, isDeploymentSkillFilePath, isDeploymentSkillFileSource, isDeploymentSkillId, isDirectOpenIDBearerRecoveryEnabled, isEmailDomainAllowed, isEnabled, isFalsyEnvValue, isFatalAgentInitializationError, isFileAuthoringToolDefinition, isFileFilterFieldEnabled, isFileResourceToolName, isFileSnapshotEnabled, isFileSnapshotKillSwitchActive, isGoogleInvalidArgumentError, isHITLEnabled, isImplicitStatefulCodeRouteAvailable, isInvalidClientMessage, isKnownCustomProvider, isLangfuseCentralMediaUploadDisabled, isLangfuseConnectionAvailable, isLangfuseFanoutEnabled, isLangfuseTenantExportEnabled, isLangfuseTraceSampled, isLangfuseTracingEnabled, isLeader$1 as isLeader, isLegacyCredential, isLegacyFileUploadUX, isLegacyPiiFragment, isLikelyEncodedPayload, isMCPAllPlaceholder, isMCPDomainAllowed, isMCPDomainNotAllowedError, isMCPInitializationError, isMCPInspectionFailedError, isMCPOAuthSecretReentryRequiredError, isMCPTool, isMCPTransportAuthenticationError, isMemoryAgentEnabled, isMemoryEnabled, isMemoryToolAllowed, isMetricsConfigured, isMissingSandboxPathError, isModelBoundAttachmentFile, isModelParameterTraversalProtected, isModelSpecEndpointMatch, isNestedMessageTraversalProtected, isNormalizationSensitiveName, isOAuthAuthenticationError, isOAuthProtocolFailure, isOAuthServer, isOAuthUrlAllowed, isOboConfigStillTrusted, isOpenIDAvailable, isOpenIDRefreshOwnershipError, isOpenIDSessionIdentityMatch, isOpenIDSessionMissingError, isOpenIDTokenValid, isOwnedAbortError, isPendingActionExpired, isPendingActionStale, isPluginRelativePath, isPluginSourced, isPreliminaryMessageId, isPrivateIP, isRetryableOboExchangeError, isRumProxyEnabled, isSSRFTarget, isSafeSkillFilePath, isScheduleFireRequest, isShutdownInProgress, isSkillFilePath, isSkillPrimeMessage, isStandaloneSseConflict, isStatefulCodeEnvironmentToolName, isSteerPreemptRestartSupported, isSteerPreemptSupported, isSteerTerminalContinuationSupported, isSteeringSupported, isStepLimitError, isStopConfirmed, isSubagentThreadWriteBlocked, isTextLikeSkillFile, isTextualFileMimeType, isToolApprovalPauseCapable, isToolApprovalPayloadValid, isToolDeniedByApprovalPolicy, isToolModuleFile, isUnpersistedPreliminaryParent, isUsableSummaryPart, isUserIssuerAllowed, isUserProvided, isUserSourced, isValidCronExpression, isValidSharedLinksCursor, isValidSubagentControlRequest, isValidTimezone, isValidationFailure, isWithinRoot, issueCsp, keyvMongo, keyvRedisClient, keyvRedisClientReady, knownAnthropicParams, knownGoogleParams, knownOpenAIParams, legacyActionDomainEncode, limiterCache, linkChatGptCitations, listAgentModels, loadAddedAgent, loadAgent, loadAnthropicVertexCredentials, loadCustomEndpointsConfig, loadDeploymentSkillsFromDirectory, loadEphemeralAgent, loadMCPServerCatalogs, loadMemoryConfig, loadOCRConfig, loadPlugin, loadPluginHooks, loadPluginSkills, loadPluginsFromDirectory, loadServiceKey, loadSkillFromDirectory, loadSkillStates, loadThreadUserMessages, loadToolApprovalHooks, loadToolDefinitions, loadWebSearchAuth, loadYaml, logAgentMemorySnapshot, logAgentMigrationWarning, logAxiosError, logFile, logHeaders, logOpenIDCallbackFailure, logOpenIdRequestBody, logPromptMigrationWarning, logToolError, mapAgentManagementError, mapAskUserAnswer, mapAskUserAnswers, mapCollectedMetadataToUsage, mapToolApprovalPolicy, mapToolApprovalResolutions, markCompactionOutcome, markNativeCodeToolApprovalRequests, markPublicPromptGroups, markSandboxReady, maskAgentInsightsBit, matchModelName, matchesExpectedAction, math, maxOutputTokensMap, maxTokensMap, maybeInjectQueryDevtoolsBootstrap, maybePrewarmCodeSandbox, maybeRefreshCloudFrontAuthCookies, maybeRefreshCloudFrontAuthCookiesMiddleware, mcpConfig$1 as mcpConfig, mcpOptionsContainGraphTokenPlaceholder, mcpToolPattern, mediaTypeEssence, memoryDiagnostics, memoryInstructions, memoryToolUsageGuard, mergeAccessibleCodeEnvironments, mergeActionMetadataForUpdate, mergeAgentActionTools, mergeAgentOcrConversion, mergeCodeFilesIntoContext, mergeDeploymentSkillIds, mergeHeaders, mergeMessagesWithInput, mergeQuotedText, mergeQuotedTextForCount, mergeUserSubmittedMessageFieldPaths, mergeUserSubmittedPaths, mintCodeApiToken, modelMaxOutputs, modelSchema, narrowSandboxImageChunkBytes, needsRefresh, normalizeActionToolName, normalizeAgentEventActorDiscoveredTools, normalizeAgentEventActorSummary, normalizeAgentToolKeys, normalizeArtifactDeliveryFailure, normalizeCodeEnvironmentName, normalizeEnvValue, normalizeExpiresAt, normalizeExpiresIn, normalizeHttpError, normalizeJsonSchema, normalizeLimit, normalizeMCPToolKey, normalizeOpenIdIssuer, normalizePath, normalizeRedisUseCase, normalizeResumeRunStepIndices, normalizeServerName, normalizeSortDirection, normalizeSortField, normalizeStatefulCodeEnvironment, normalizeString, normalizeToolResourceFiles, notifyMCPToolsChanged, oaiToolkit, observeRedisOperation, omitResolvedCanonicalFileLocators, omitTitleOptions, openCheckpointDeletion, openWeatherSchema, optionalChainWithEmptyCheck, optionalEnabled, optsOutOfAttachedCodeEnvironment, orderConversationBranch, orderMessageLineage, orderParentTimestamps, overrideDeferLoadingForDiscoveredTools, parseAgentEventActorDetachedCompletion, parseAgentEventDetachedTerminalEvidence, parseAgentTriggerEnvelope, parseCloudFrontCookieScope, parseDocument, parseEnvSwitch, parseFrontmatter, parsePluginHooks, parseS3Key, parseSandboxImageChunk, parseSharedLinksPageSize, parseSkillMarkdown, parseText, parseTextNative, parseToolInputValidationError, partitionCommittedFiles, payloadParser, performOCR, performStartupChecks, persistMCPAuthorizationTransaction, pickResumeContext, planAgentActionUpdate, planPluginHooks, pluginHookGroupSchema, pluginHookHandlerSchema, pluginHooksDocumentSchema, preAuthTenantMiddleware, preProcessGraphTokens, preflightAssistantRunContent, preflightAssistantUserMessageContent, preflightCodeOutputBatch, preflightResumeContent, prepareAgentTriggerDelivery, prepareCodeOutputBufferForInspection, prepareMCPAuthorizationMutation, prepareMCPServerOAuthDeletion, prepareRetainedAnswerInvocationMessages, prepareRetainedAnswers, prepareStructuredTokenSpend, prepareTokenSpend, prependContentTraversalFragments, prependContextText, prependFileContext, prependQuotes, primeInvokedSkills, primeInvokedSkillsForProfiles, primeResources, primeSkillFiles, priorRunOutputTokens, processAudioFile, processCodeOutput$1 as processCodeOutput, processMCPEnv, processMemory, processModelData, processOpenIDPlaceholders, processTextWithTokenLimit, projectAgentManagementListResponse, projectAgentManagementResponse, projectModelBoundSourceFiles, projectPendingMCPOAuthPrompts, projectStoredMemories, projectStoredPreset, projectStoredPresets, projectStoredPrompt, projectStoredPromptGroup, projectStoredPromptGroups, projectStoredPrompts, providerConfigMap, proxyRumRequest, pruneOrphanSkillStates, publishMCPAuthorizationMutation, queryString, reachesBranchRoot, readCodeBridgeSecret, readFileAsBuffer, readFileAsString, readJsonFile, readMCPRecoveryGeneration, readMCPRecoveryGenerationAround, readMcpConfig, readResolvedConversationFiles, readScheduleFireContext, readWindowedSandboxImage, realpathAllowingMissing, reconcileAgentWorkspaceDefault, reconcileCodeEnvironmentLifecycle, recordAgentStartupMilestone, recordAgentStartupResult, recordCollectedUsage, recordContainsGraphTokenPlaceholder, recordFallbackTokenUsage, recordGenerationJob, recordGenerationStreamAttachment, recordGenerationStreamEarlyBufferOverflow, recordGenerationStreamRecovery, recordGenerationStreamResumePendingEvents, recordGenerationStreamSubscription, recordMongooseQuery, recordOpenIDUserLookup, recordRedisOperation, recordRumProxyRequest, recordShareLinkRejection, recordToolInputValidationError, recoverMCPServerCatalogs, redactAllServerSecrets, redactConfigSecretMaps, redactServerSecrets, redirectToAuthFailure, refreshAccessToken, refreshListAvatars, refreshS3FileUrls, refreshS3Url$1 as refreshS3Url, registerCodeExecutionTools, registerDeploymentPluginHooks, registerFileAuthoringTools, registerMemoryTools, registerOpenIdWithRetry, registerPluginHooks, registerShutdownTask, registerToolApprovalHook, reloadOpenIDSessionIfPersisted, remainingToolRounds, removePorts, renderRetainedAnswers, renewMCPToolsChangedGeneration, replaceArtifactContent, replaceEdgeSourceId, reportLocatorTraversalFailure, reportUnexecutedHooks, requestContextMiddleware, requireAdmin, requireFeedbackEnabled, requiresEphemeralUserConnection, requiresOAuthMachinery, requiresUserScopedConnection, reserveCodeDestination, reserveContentMaterialization, reserveMCPToolsChangedRevision, resetSandboxStateForTests, resolveActivityConfig, resolveActivityLabelModel, resolveActivityPhaseConfig, resolveActivityPhaseLabelModel, resolveAgentOwnerContact, resolveAgentScopedSkillIds, resolveAgentTokenConfig, resolveAgentTurnExecutionPlan, resolveAgentWorkspaceRestoreConfiguration, resolveAlwaysApplySkills, resolveAppConfigForUser, resolveAppUserId, resolveAskUserQuestionResume, resolveAssistantToolPermissions, resolveAttachedCodeApprovalMode, resolveAttachedWorkspaceCommandTimeoutMax, resolveAuthOpenIDIssuer, resolveBuildInfo, resolveCallerCapabilityProjectionSnapshot, resolveCanonicalFileReferenceUnits, resolveCanonicalFileReferences, resolveCheckpointerConfig, resolveCloudFrontCookieScope, resolveCodeEnvironmentDecisionVersion, resolveCodeEnvironmentMoveVersion, resolveCodeExecutionContext, resolveCodeExecutionWorkspaceContext, resolveConfigHeaders, resolveConfigSecret, resolveConversationAnchor, resolveConversationCodeEnvironmentDecision, resolveConversationCodeEnvironmentMove, resolveConversationTitle, resolveDefaultActiveOnShare, resolveDeploymentPluginDataDirectory, resolveDeploymentPluginDirectory, resolveDeploymentSkillDirectory, resolveDirectOpenIDBearerConfig, resolveDownloadPath, resolveEffectiveToolResource, type resolveEndpointRuntime, resolveExpiredFileSweepConfig, resolveFailedTurnContent, resolveFilesUsageHold, resolveGoogleVideoError, resolveGraphTokenPlaceholder, resolveGraphTokensInRecord, resolveHeaders, resolveHostnameSSRF, resolveImageMimeType, resolveImportMaxFileSize, resolveJsonSchemaRefs, resolveLangChainError, resolveLangfuseReadDestinations, resolveLangfuseSession, resolveLangfuseSessionUrl, resolveMCPReinitializeConfig, resolveMCPServerContext, resolveManualSkills, resolveMaxTextExtractBytes, resolveModelHeaders, resolveModelSpecForEndpoint, resolveModelSpecPromptPrefixVariables, resolveModelSpecSkillIds, resolveNestedObject, resolveOboToken, resolveOpenIDSubject, resolveOpenIdDiscovery, resolvePersistableCodeEnvironmentDecision, resolvePersistableFadingTier, resolvePersistableFadingTiers, resolveReachableGraph, resolveReasoningLabelConfig, resolveReasoningLabelModel, resolveReasoningParams, resolveRecursionLimit, resolveRefreshSubject, resolveRequestTenantId, resolveResendToolResources, resolveRetainedAnswersConfig, resolveRetainedToolTokens, resolveRumProxyTarget, resolveRunCodeWorkspaces, resolveRunContextMeta, resolveRunConversation, resolveRunFadingTiers, resolveRunFileCodeExecutionContext, resolveRunUsageContext, resolveSamlSubject, resolveScheduleProjectId, resolveScopedTurnAttachments, resolveSender, resolveServerInstructions, resolveSharedLinkConfig, resolveSkillActive, resolveSkillCatalog, resolveSkillFilePathParam, resolveStoredS3Key, resolveStreamLimits, resolveSubagentGraphs, resolveSubagentMaxTurns, resolveTenantId, resolveTitleTiming, resolveTokenConfigMap, resolveTokenEndpointAuthMethod, resolveToolApprovalPolicy, resolveToolApprovalResume, resolveToolRoleGrants, resolveToolRolePermissions, resolveTurnDeliveryRouting, resolveUploadAgent, resolveUploadEndpoint, resolveUploadErrorMessage, resolveUploadFallbackText, resolveWebSearchSSRFAgents, resolveWithinRoot, resolveYouTubeInjectionConfig, resolvedToolApprovalHooksCanMatch, restoreCompactionSemanticIndex, restoreCompactionSemanticIndexSnapshot, restoreScheduledTokenContext, restoreTenantContextFromReq, resumeAgentEventActor, retainedOutcome, revokeCodeBridgeWorker, revokeUserCodeEnvironmentWorkers, runGuardedEncode, runOutsideTracing, runWithRedisRequestTelemetry, safeStringify, safeValidatePromptGroupUpdate, sanitizeArtifactPath, sanitizeFileForTransmit, sanitizeFilename, sanitizeGeminiSchema, sanitizeInsightsPermissionPrincipals, sanitizeMcpIconPath, sanitizeMessageForTransmit, sanitizeModelName, sanitizeModelParameters, sanitizeModelSpecs, sanitizeResumeModelParameters, sanitizeTitle, sanitizeUrlForLogging, saveBufferToCloudFront, saveBufferToS3, saveURLToCloudFront, saveURLToCloudFrontWithMetadata, saveURLToS3, saveURLToS3WithMetadata, scanKeys, scheduleJitterMs, schemaVersion, scopeHeadersToDestination, scopeSkillIds, seedCodeFilesIntoSessions, selectCodeFiles, selectFileCitationSources, selectLazySubagentConfig, selectMCPUpstreamTokenProvider, selectOpenIdRole, selectRegistrationAuthMethod, selectRunContextMetaToPublish, selectSkillPrimesForTurn, selfOriginFromAddress, sendErrorResponse, sendEvent, sendFeedbackScore, sendFinalChunk, sendResponsesErrorResponse, sendUploadPolicyError, sendUploadSuccess, serializeAskUserAnswerVariants, serializeAuthIdentityTuple, serializeCspDirectives, serializeUserForExchange, sessionCache, setCachedAuthUserDoc, setCloudFrontCookies, setGenerationJobsInFlight, setMCPToolsChangedGenerationHandler, setMCPToolsChangedGenerationRenewalHandler, setMCPToolsChangedHandler, setMCPToolsChangedRevisionHandler, setOAuthCsrfCookie, setOAuthSession, setOAuthSessionCookie, setOpenIDMarkerCookies, setPluginHookOnceStore, setPluginHookSource, setRefreshTokenCookie, settleAgentEventActorHistoryTurn, settlePendingLabelFills, setupCheckpointIndexes, setupGracefulShutdown, setupStreamingResponse, shellCacheHeaders, shouldBypassProxy, shouldEnableQueryDevtools, shouldIncludeNestedSubmittedText, shouldPersistCodeWorkspaceInitializationError, shouldReplayReasoningContent, shouldSignalSandboxStart, shouldUseSecureCookie, shouldUseUploadSse, shouldValidateAgentWorkspaceDefaultBinding, skillManagementResponseSchema, skillManagementUpdateSchema, skipAgentCheck, snapshotExternalMessages, sortCodeFilesByDestinationPriority, splitAndTrim, splitMCPToolKey, stableDiffusionSchema, stampMcpServerIdentities, stampSteerPartMedia, standardCache, startCodeEnvironmentLifecycleReconciler, startExpiredFileSweep, startGitHubSkillSyncScheduler, startScheduleEngine, startScheduleErasureSweep, startUploadSseStream, storeAndStripChallenge, storeOpenIdSession, stringifyActivityEvidence, stripActivityLabelParts, stripCodeChallenge, stripFileIdsFromToolResources, stripGeminiFlashBlockedParams, stripServerNamePrefix, stripServerNamePrefixes, stripUnresolvedPlaceholders, stripUnusableSummaryParts, subagentActivityStreamId, summarizePtcArgs, supportsAdaptiveThinking, supportsProgrammaticCodeExecution, sweepExpiredFiles, synthesizeActivityLabelGapEvents, synthesizeReasoningLabelGapEvents, tavilySearchSchema, tenantContextMiddleware, toClientPendingAction, toLegacyPiiMatch, toOpenIDLogArgument, toPendingSteer, toProviderToolDefinition, toSkillStatesRecord, toSteerFileRef, toWireInFlight, toWireSchedule, tokenConfigCache, toolDefinitions$1 as toolDefinitions, toolOptionsSchema, toolResourceRolePermissions, toolRolePermissions, toolkitExpansion, toolkitParent, traceIdForMessage, traversaalSearchSchema, truncateMiddle, unescapeLaTeX, unionPrimeAllowedTools, updateDeploymentSkillFileCodeEnvRefs, updateInterfacePermissions, updatePromptGroupSchema, updateSharedLinkPermissionsExpiration, updateTrackerUsage, uploadAzureMistralOCR, uploadDocumentToMistral, uploadFileToCloudFront, uploadFileToS3, uploadGoogleVertexMistralOCR, uploadMistralOCR, userPrincipalsCache, usesDirectOpenIDBearerRecovery, usesLangfuseMultiTenantRouting, usesSubagentCompletionWakeups, validateActionOAuthEndpoint, validateActionOAuthMetadata, validateAgentModel, validateAgentWorkspaceDefaultBinding, validateAudio, validateBedrockDocument, validateEmailLogin, validateEndpointURL, validateImage, validateInsightsPermissionUpdates, validateMCPServerConfig, validateManifest, validateOAuthCsrf, validateOAuthSession, validatePdf, validatePromptGroupUpdate, validateRequest, validateResponseRequest, validateSkillStatesPayload, validateVideo, verifyAgentUploadPermission, verifyCodeChallenge, violationCache, violationFile, visitBoundedSubmittedArray, visitNestedStrings, waitForAgentExecutionWrites, waitForGenerationPersistence, waitForKeyvRedisClient, waitUntilDeadline, warnOnUnreachableDeliveryPaths, withBalanceReservations, withCodeApiRateLimit, withCodeApiUploadRecovery, withCodeApiUploadSlot, withRetainedAnswerTokenCounter, withTimeout, withoutTraceRefs, wolframSchema, wrapCodeApiUploadError, writeAttachmentEvent, writeDone, writeEvent, writeSSE };
//# sourceMappingURL=index.d.cts.map